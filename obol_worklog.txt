BRL-CAD Obol Migration Worklog
==============================

How To Use This File
--------------------

This file preserves the detailed Obol migration work history, design notes,
and rationale consolidated from the former root guidance documents.  It is a
reference log, not the first-stop active handoff.

Use obol_activework.txt first for current priorities, completion percentages,
test gates, and remaining work.  Return here when an implementation choice
needs historical context or more detailed design rationale.

Current Work Notes
------------------

2026-07-03 local-space shared database geometry:
libbrlobol database realization now separates BRL-CAD leaf instances from
primitive geometry.  `db_walk_tree` still supplies full paths, inherited
region/material state, and accumulated member transforms, but the drawable
geometry is built from identity-matrix `rt_db_get_internal` refetches and
cached once per primitive per realization.  Each visible database leaf owns a
metadata/draw-intent shape under an OpenInventor transform node and references
the shared local geometry through `sharedGeometry`.

The action stack was updated so export, measure, snap, summaries, and GED
readback use the active/shared geometry source while preserving all reported
identity and draw state from the visible instance.  Explicit shape geometry
setters detach from the shared source, preserving GED/direct mutation
semantics.  LoD-backed mesh instances keep an `SoBRLLodMeshShape` wrapper so
per-view staged/display LoD state does not mutate shared full-detail source
geometry.

The cache is realization-local by design: source refresh/removal drops the
realized children and releases the shared geometry targets together.  The
remaining long-term cleanup is to replace `db_walk_tree`'s unavoidable
per-instance primitive imports with a librt pre-import instance traversal API;
the Obol scene graph boundary is now ready for that swap.

Verification passed:
`ninja -C build test_brlobol_prototype ged_test_obol_draw_sync`,
`BRLCAD_ROOT=build ./build/src/libbrlobol/tests/test_brlobol_prototype`, and
`./build/src/libged/tests/draw/ged_test_obol_draw_sync`.

2026-07-03 libbrlobol realization hardening and librt poison findings:
`SoBRLDatabaseSource::clearExternalPrimaryGeometry` now preserves direct
primary Obol VLIST/mesh nodes and empties their geometry arrays instead of
removing the only display-state target.  This keeps GED shape refs and
display mutations valid after geometry clear without retaining large
point/index buffers.  Database-source string sync now uses stable copies and
no-op checks before writing OpenInventor string fields, fixing a libc++
hardened `std::string` overlap trap triggered by nested-source
`setDatabaseSourceDrawMode` display sync.  The database realization path now
validates callback `rt_db_internal` payload magic before primitive method
calls.  The subsequent local-space shared-geometry work now builds drawable
geometry from identity-matrix refetches rather than transformed callback
internals.

The librt poison remains a real lower-level issue, not a libbrlobol stale
callback retention bug.  In the passing guarded `ged_test_obol_draw_sync`,
an lldb conditional breakpoint at `src/librt/db5_io.c:1002` hit with
`ext.ext_buf == ip->idb_ptr` for `box.s` before `bu_free_external(&ext)`.
The stack was the normal `db_walk_tree` leaf import feeding
`SoBRLDatabaseSource::realizeDatabaseWireframe`.  Source inspection shows
`db_get_external` allocates the external object buffer and `rt_arb_import5`
allocates a distinct `struct rt_arb_internal`, so that alias is not an
expected ARB import ownership contract.  Next root-cause work should trace
allocator ownership/free history for the aliased external allocation.  The
guard is intentionally cheap in steady state and protects libbrlobol from
calling bomb-prone librt primitive methods on poisoned payloads.

Verification passed:
`ninja -C build test_brlobol_prototype ged_test_obol_draw_sync`,
`./src/libbrlobol/tests/test_brlobol_prototype`, and
`./src/libged/tests/draw/ged_test_obol_draw_sync`.

As of 2026-07-02, `obol_activework.txt` is the authoritative current handoff.
The active direction remains an Obol-primary database draw pivot, and the
large Capability 1 GED draw-scene behavior-family push has landed.  Retained
BSG scene/source implementation code may remain compiled as reference,
legacy, or no-owned-scene compatibility code, but database draw behavior
should no longer use retained BSG fallback to mask missing Obol coverage.

The immediate Capability 1 target is closeout: update the pivot guard and
dependency inventory for the newly migrated Obol-primary boundaries, audit
remaining database scene/source BSG use, and run the broad closeout gate.
This is not expected to be another open-ended database fallback refactoring
cycle.  After that gate, the next implementation phase is Capability 2:
stand up Obol/libbrlobol view node/store ownership for feature, polygon,
selection, overlay/diagnostic, annotation/auxiliary, qray/NIRT, vdraw/local
helper, and related non-database families as scope allows.

The latest completed slice split `ged_test_ged_draw_scene` into 16 CTest
shards and made Obol/libbrlobol the path-state, path-listing, has-path,
component source-rename, and reference-removal authority for the draw-scene
database behavior family.  Nested parent/child exposure is reconstructed from
Obol source ownership plus GED database-index validation instead of retained
BSG source records, and `prefix` now publishes object rename events before
comb-tree redraw events so draw reconciliation can retarget the old source.
Verification passed with the full draw-scene shard set and
`ged_test_state_services`.

The preceding completed code-state batch retires installed
`include/qtcad/QgObolEditPreview.h` and removes it from
`include/qtcad/CMakeLists.txt`.  The qged edit-preview utility and qtcad
edit-preview test now use private
`src/libqtcad/QgObolEditPreviewPrivate.h`.  `QgObolEditPreview.cpp`,
`test_qtcad_obol_edit_preview.cpp`, and
`src/qged/plugins/edit/qged_edit_preview_util.h` include that private header
directly.  The pivot guard rejects installed header resurrection, public
header registration, stale public includes, and missing private
header/source/CMake registration while preserving edit-intent metadata
coverage.

`obol_activework.txt` updates the qged/qtcad migration estimate to 92 percent
and keeps the remaining pre-feature implementation estimate at about 1-2
cycles if remaining ownership handoffs stay shallow, or about 2-3 cycles if
one retained draw/backend path needs a new neutral contract.  Exit-criteria
percentages otherwise did not change because retained backend/display
ownership, retained RT feature/line-set/polygon/selection internals, public
BSG-shaped GED draw/snap/scene-ref bridge shims, direct lower `libbsg` rows,
and `qged`'s direct `dm_plugins` dependency still determine retirement
readiness.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so qged_ell
qged_revolve test_qtcad_obol_edit_preview` completed after a CMake regenerate
removed stale `build/include/brlcad/qtcad/QgObolEditPreview.h`.  The targeted
guard/runtime verification completed 2/2 passing in 17.85 seconds:
`brlobol_pivot_guard` at 17.73 seconds and
`test_qtcad_obol_edit_preview` at 0.07 seconds.  The broader qtcad Obol
workflow group completed 17/17 passing in 42.67 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 27.07 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.03 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.18 seconds.

The preceding completed code-state batch retires installed
`include/qtcad/QgObolDatabaseSync.h` and removes it from
`include/qtcad/CMakeLists.txt`.  The qtcad draw-sync implementation and its
test-only direct database-source sync coverage now use private
`src/libqtcad/QgObolDatabaseSyncPrivate.h`.  `QgObolDatabaseSync.cpp`,
`QgObolDrawSync.cpp`, and `test_qtcad_obol_draw_sync.cpp` include that
private header directly.  The pivot guard rejects installed header
resurrection, public header registration, stale public includes, and missing
private header/source/CMake registration.  `obol_activework.txt` updated the
qged/qtcad migration estimate to 91 percent for that batch.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so qged
test_qtcad_obol_draw_sync ged_test_qged_swrast` completed after a CMake
regenerate removed stale
`build/include/brlcad/qtcad/QgObolDatabaseSync.h`.  The targeted
guard/runtime verification completed 3/3 passing in 18.09 seconds:
`brlobol_pivot_guard` at 17.81 seconds,
`test_qtcad_obol_draw_sync` at 0.12 seconds, and
`ged_test_qged_swrast` at 0.11 seconds.  The broader qtcad Obol workflow
group completed 17/17 passing in 42.74 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 27.09 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.22 seconds.

The preceding completed code-state batch retires installed
`include/qtcad/QgObolOverlaySync.h` and
`include/qtcad/QgObolSelectionSync.h` and removes them from
`include/qtcad/CMakeLists.txt`.  The qged/test-only GED diagnostic overlay
and selection-state sync helpers now live in private
`src/libqtcad/QgObolOverlaySyncPrivate.h` and
`src/libqtcad/QgObolSelectionSyncPrivate.h`.  `QgObolOverlaySync.cpp`,
`QgObolSelectionSync.cpp`, `QgEdApp.cpp`, and the qtcad Obol
overlay/selection tests include those private headers directly.  The pivot
guard rejects installed header resurrection, public header registration, and
stale public includes while requiring the private header/source/caller
boundary.  `obol_activework.txt` updated the qged/qtcad migration estimate to
90 percent for that batch.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so qged
test_qtcad_obol_overlay_sync test_qtcad_obol_selection_sync
ged_test_qged_swrast` completed after a CMake regenerate caused by retiring
the installed overlay/selection headers.  The targeted guard/runtime
verification completed 4/4 passing in 18.04 seconds:
`brlobol_pivot_guard` at 17.70 seconds,
`test_qtcad_obol_selection_sync` at 0.10 seconds,
`test_qtcad_obol_overlay_sync` at 0.09 seconds, and
`ged_test_qged_swrast` at 0.11 seconds.  The broader qtcad Obol workflow
group completed 17/17 passing in 42.71 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 27.07 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.20 seconds.

The preceding completed code-state batch retires installed
`include/qtcad/QgObolViewSync.h` and removes it from
`include/qtcad/CMakeLists.txt`.  The transitional active-view acceptance
helper now lives in private `src/libqtcad/QgObolViewSyncPrivate.h`, and
`QgObolViewSync.cpp`, `QgObolSelectionSync.cpp`, `QgObolOverlaySync.cpp`, and
`QgEdApp.cpp` include that private header directly.  The pivot guard rejects
installed header resurrection, public header registration, and stale public
includes while requiring the private active-view sync header/source/caller
boundary.  `obol_activework.txt` updated the qged/qtcad migration estimate to
89 percent for that batch.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so qged
test_qtcad_obol_draw_sync test_qtcad_obol_selection_sync
test_qtcad_obol_pick test_qtcad_obol_measure test_qtcad_obol_snap
ged_test_qged_swrast` completed after a CMake regenerate caused by retiring
the installed header.  The targeted guard/runtime verification completed 7/7
passing in 18.48 seconds: `brlobol_pivot_guard` at 17.77 seconds,
`test_qtcad_obol_draw_sync` at 0.12 seconds,
`test_qtcad_obol_selection_sync` at 0.10 seconds,
`test_qtcad_obol_pick` at 0.13 seconds, `test_qtcad_obol_snap` at 0.11
seconds, `test_qtcad_obol_measure` at 0.10 seconds, and
`ged_test_qged_swrast` at 0.11 seconds.  The broader qtcad Obol workflow
group completed 17/17 passing in 42.60 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.95 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.22 seconds.

The preceding completed code-state batch adds private
`ged_draw_scene_root_mutate` in `src/libged/bsg_ged_draw_scene_root.c`.
`src/libged/bsg_ged_draw_tree.c` now routes the root-scoped erase path,
erase-all-names, erase-all-paths, and full draw-clear mutation paths through
that scene-root facade instead of opening the retained GED root directly.
Direct `ged_draw_scene_root_ref` calls are now limited to the scene-root
owner file, where neutral GED root storage is converted at the retained draw
boundary.  The pivot guard requires the mutation facade and rejects direct
retained root reads from returning to tree, records, or navigation.

The preceding completed code-state batch removes three retired private qtcad
retained-DM shallow helpers:
`qg_legacy_view_scale_storage_get`,
`qg_legacy_view_display_manager_get`, and
`qg_legacy_view_dm_open_context`.  `src/libqtcad/QgLegacyViewDm.h` no longer
declares them, and `src/libqtcad/QgLegacyView.cpp` no longer implements
them.  `qg_legacy_view_dm_bind` now gets the scale storage through
`rt_view_context_scale_storage_get(qg_legacy_view_to_context(view))`, and
the swrast open path now calls `dm_open` with the neutral RT context directly.
The inactive DM backend benchmark reads the retained display-manager pointer
through `rt_view_context_display_manager_get` after converting the qtcad
legacy-view handle through the private `QgLegacyViewContext.h` adapter.

This cleanup also fixed a real FreeBSD/clang header hygiene failure:
`QgLegacyViewDm.h` now includes `vmath.h` explicitly for `fastf_t` instead of
depending on a transitive include.  `obol_activework.txt` keeps the qged/qtcad
migration estimate at 88 percent and keeps the remaining pre-feature
implementation estimate at 2-3 cycles if remaining ownership handoffs stay
shallow, or 3-4 cycles if one retained draw/backend path needs a new neutral
contract.  Exit-criteria percentages did not change because retained
DM/framebuffer host ownership, retained RT feature/line-set/polygon/selection
internals, GED scene-ref/root/scope handoffs, direct lower `libbsg` rows, and
`qged`'s direct `dm_plugins` dependency still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_dm_backend_draw_item ged_test_qged_swrast test_qtcad_obol_draw_sync
test_qtcad_obol_faceplate_sync` completed in 19.00 seconds after the explicit
`vmath.h` include fix.  The targeted guard/runtime verification completed 5/5
passing in 18.14 seconds: `brlobol_pivot_guard` 17.67 seconds,
`dm_backend_draw_item` 0.13 seconds, `test_qtcad_obol_draw_sync` 0.11
seconds, `test_qtcad_obol_faceplate_sync` 0.07 seconds, and
`ged_test_qged_swrast` 0.10 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.39 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.95 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.00 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.12
seconds; longest members were `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 17.89 seconds, `ged_test_drawing_select` at 16.32
seconds, and `ged_test_drawing_quad` at 12.69 seconds.

The preceding completed code-state batch removes the retired private qtcad
sketch helper bridge.  `src/libqtcad/QgLegacyViewSketch.h` is deleted, and
`src/libqtcad/QgLegacyView.cpp` no longer implements
`qg_legacy_view_sketch_edit_create`,
`qg_legacy_view_sketch_lines_create`,
`qg_legacy_view_sketch_lines_is_null`,
`qg_legacy_view_sketch_lines_set_points`, or
`qg_legacy_view_sketch_lines_destroy`.  `src/libqtcad/tests/qsketch.cpp`
now owns its small local sketch line-set handle and calls
`rt_edit_create_context`, `rt_view_context_line_set_create`, and
`rt_view_line_set_context_*` APIs directly through the private
`QgLegacyViewContext.h` adapter.  The pivot guard rejects the retired private
sketch bridge header and helper names from returning.  The retained RT edit
and line-set implementation remains BSG-backed until the wider feature and
line-set backend migrates.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so qsketch`
completed in 88.73 seconds, including the CMake regenerate caused by deleting
the private header.  The targeted guard/runtime gate completed 4/4 passing in
17.98 seconds: `brlobol_pivot_guard` 17.70 seconds, `rt_view_legacy_bsg`
0.02 seconds, `test_qtcad_obol_draw_sync` 0.11 seconds, and
`test_qtcad_obol_snap` 0.11 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.40 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.90 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.31
seconds; longest members were `libbsg_api_hygiene` at 41.14 seconds,
`brlobol_pivot_guard` at 17.76 seconds, `ged_test_drawing_select` at 16.31
seconds, and `ged_test_drawing_quad` at 12.70 seconds.

The qtcad private retained-DM shallow-helper cleanup has been completed.
`src/libqtcad/QgLegacyViewDm.h` and `src/libqtcad/QgLegacyView.cpp` no
longer declare or implement `qg_legacy_view_scale_storage_get`,
`qg_legacy_view_display_manager_get`, or
`qg_legacy_view_dm_open_context`.  `qg_legacy_view_dm_bind` now reads scale
storage through `rt_view_context_scale_storage_get` after converting the qtcad
legacy-view handle with `qg_legacy_view_to_context`, and the swrast open path
passes the neutral RT context directly to `dm_open`.  The inactive DM backend
benchmark now reaches the same private qtcad context adapter and uses
`rt_view_context_display_manager_get` directly.

This batch also fixed a FreeBSD/clang header dependency exposed by the
cleanup: `QgLegacyViewDm.h` now includes `vmath.h` explicitly for `fastf_t`.
`obol_activework.txt` records this as a private-wrapper cleanup and keeps the
qged/qtcad migration estimate at 88 percent.  Exit-criteria percentages did
not change: retained DM/framebuffer host ownership, retained RT
feature/line-set/polygon/selection internals, GED scene-ref/root/scope
handoffs, direct lower `libbsg` rows, and `qged`'s direct `dm_plugins`
dependency still determine retirement readiness.  The remaining
pre-feature implementation estimate remains 2-3 cycles if the remaining
ownership handoffs stay shallow, or 3-4 cycles if a retained draw/backend
path needs a new neutral contract.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so test_dm_backend_draw_item ged_test_qged_swrast
test_qtcad_obol_draw_sync test_qtcad_obol_faceplate_sync` in 19.00 seconds.
The targeted guard/runtime verification completed 5/5 passing in 18.14
seconds: `brlobol_pivot_guard` at 17.67 seconds,
`dm_backend_draw_item` at 0.13 seconds, `test_qtcad_obol_draw_sync` at 0.11
seconds, `test_qtcad_obol_faceplate_sync` at 0.07 seconds, and
`ged_test_qged_swrast` at 0.10 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.39 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.95 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.00 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.12
seconds; longest members were `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 17.89 seconds, `ged_test_drawing_select` at 16.32
seconds, and `ged_test_drawing_quad` at 12.69 seconds.

An earlier completed code-state batch removes the retired qtcad legacy simple
view readback helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_local2base_get`, `qg_legacy_view_base2local_get`,
`qg_legacy_view_center_get`, `qg_legacy_view_rotation_get`,
`qg_legacy_view_scale_get`, or `qg_legacy_view_perspective_get`.
`src/libqtcad/QgCanvasState.h`, `src/libqtcad/QgCanvasInput.cpp`, and
`src/libqtcad/QgQuadView.cpp` now include the private
`QgLegacyViewContext.h` adapter where needed and read camera and
unit-conversion state through neutral `rt/view.h` `rt_view_context_*`
accessors.  The pivot guard rejects those public qtcad simple view readback
helpers from returning to the installed header, legacy-view implementation,
qtcad canvas/input camera paths, or qtcad quad-view unit-copy path.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_draw_sync test_qtcad_obol_snap qsketch` completed in 32.80
seconds.  The targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|test_qtcad_obol_snap|ged_test_qged_swrast)$'
--timeout 180 --output-on-failure` completed 4/4 passing in 18.32 seconds:
`brlobol_pivot_guard` 17.94 seconds, `test_qtcad_obol_draw_sync` 0.11
seconds, `test_qtcad_obol_snap` 0.11 seconds, and `ged_test_qged_swrast`
0.11 seconds.  The full qtcad Obol workflow group completed 16/16 passing in
42.46 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.93 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.20 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.35
seconds; longest members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 17.89 seconds, `ged_test_drawing_select` at 16.28
seconds, and `ged_test_drawing_quad` at 12.68 seconds.

The preceding completed code-state batch removes the retired qtcad legacy simple
view setter helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_scale_set`, `qg_legacy_view_size_set`,
`qg_legacy_view_center_vec_set`, `qg_legacy_view_aet_set`, or
`qg_legacy_view_update`.  `src/libqtcad/QgCanvasState.h`,
`src/libqtcad/QgCanvasInput.cpp`,
`src/libqtcad/tests/test_qtcad_obol_draw_sync.cpp`,
`src/libqtcad/tests/test_qtcad_obol_snap.cpp`,
`src/libqtcad/tests/qsketch.cpp`, and
`src/qged/plugins/polygon/QPolyMod.cpp` now include the private
`QgLegacyViewContext.h` adapter where needed and write scale, size, center,
AET, and update state through neutral `rt/view.h` `rt_view_context_*`
accessors.  The pivot guard rejects those public qtcad simple view setter
helpers from returning to the installed header, legacy-view implementation,
qtcad canvas/input/test paths, qsketch, or qged polygon alignment path.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_draw_sync test_qtcad_obol_snap qged_polygon_modify qsketch`
completed in 31.59 seconds.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|test_qtcad_obol_snap|ged_test_qged_swrast)$'
--timeout 180 --output-on-failure` completed 4/4 passing in 18.33 seconds:
`brlobol_pivot_guard` 17.95 seconds, `test_qtcad_obol_draw_sync` 0.12
seconds, `test_qtcad_obol_snap` 0.11 seconds, and `ged_test_qged_swrast`
0.11 seconds.  The full qtcad Obol workflow group completed 16/16 passing in
42.32 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.87 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.00 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.29
seconds; longest members were `libbsg_api_hygiene` at 40.98 seconds,
`brlobol_pivot_guard` at 18.00 seconds, `ged_test_drawing_select` at 16.26
seconds, and `ged_test_drawing_quad` at 12.53 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
quad-view LoD/autoview helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_lod_policy_copy`, `qg_legacy_view_autoview_default`, or
`qg_legacy_view_lod_bounds_update`.  `src/libqtcad/QgQuadView.cpp` now
includes the private `QgLegacyViewContext.h` adapter and copies LoD policy,
performs default autoview, and updates LoD bounds through neutral
`rt/view.h` `rt_view_context_*` accessors.  The pivot guard rejects those
public qtcad quad-view helpers from returning to the installed header,
legacy-view implementation, or qtcad quad-view path.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_draw_sync test_qtcad_obol_real_models` completed in 19.76
seconds.  The targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.21 seconds:
`brlobol_pivot_guard` 17.94 seconds, `test_qtcad_obol_draw_sync` 0.11
seconds, and `ged_test_qged_swrast` 0.11 seconds.  The full qtcad Obol
workflow group completed 16/16 passing in 42.54 seconds; longest members
were `test_qtcad_obol_real_models_havoc_wire` at 27.06 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.01 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.61
seconds; longest members were `libbsg_api_hygiene` at 41.05 seconds,
`brlobol_pivot_guard` at 18.18 seconds, `ged_test_drawing_select` at 16.36
seconds, and `ged_test_drawing_quad` at 12.72 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
faceplate-state get/set helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_adc_state_get`, `qg_legacy_view_adc_state_set`,
`qg_legacy_view_center_dot_state_get`,
`qg_legacy_view_center_dot_state_set`,
`qg_legacy_view_grid_state_get`, `qg_legacy_view_grid_state_set`,
`qg_legacy_view_model_axes_state_get`,
`qg_legacy_view_model_axes_state_set`,
`qg_legacy_view_scale_overlay_state_get`,
`qg_legacy_view_scale_overlay_state_set`,
`qg_legacy_view_view_axes_state_get`,
`qg_legacy_view_view_axes_state_set`,
`qg_legacy_view_params_state_get`, or
`qg_legacy_view_params_state_set`.  `src/libqtcad/QgCanvasState.h`,
`src/libqtcad/QgCanvasInput.cpp`,
`src/libqtcad/tests/test_qtcad_obol_faceplate_sync.cpp`,
`src/libqtcad/tests/qsketch.cpp`, and the qged polygon create/modify plugins
now include the private `QgLegacyViewContext.h` adapter where needed and use
neutral `rt/view.h` `rt_view_context_*` faceplate-state accessors.  The pivot
guard rejects those public qtcad faceplate-state helpers from returning to
the installed header, legacy-view implementation, qtcad canvas/input/test
paths, qsketch, or qged polygon grid-snap paths.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_faceplate_sync qged_polygon_create qged_polygon_modify
qsketch` completed in 31.57 seconds.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_faceplate_sync|ged_test_qged_swrast)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.30 seconds:
`brlobol_pivot_guard` 18.07 seconds, `test_qtcad_obol_faceplate_sync` 0.07
seconds, and `ged_test_qged_swrast` 0.11 seconds.  The full qtcad Obol
workflow group completed 16/16 passing in 42.58 seconds; longest members
were `test_qtcad_obol_real_models_havoc_wire` at 27.08 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.00 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.18 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.46
seconds; longest members were `libbsg_api_hygiene` at 40.95 seconds,
`brlobol_pivot_guard` at 18.13 seconds, `ged_test_drawing_select` at 16.34
seconds, and `ged_test_drawing_quad` at 12.68 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
LoD policy get/apply helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_lod_policy_get` or `qg_legacy_view_lod_policy_apply`.
`src/qged/plugins/view/settings/CADViewSettings.cpp` now includes the private
`QgLegacyViewContext.h` adapter and reads/applies LoD policy, framebuffer
mode, ADC, grid, model axes, view axes, center-dot, scale-overlay, and
parameter state through neutral `rt/view.h` `rt_view_context_*` accessors.
The pivot guard rejects those public qtcad LoD policy helpers from returning
to the installed header or legacy-view implementation, and rejects qged view
settings from returning to public qtcad faceplate/LoD/framebuffer wrappers.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
qged_view_settings` completed in 23.41 seconds.  The targeted guard/runtime
gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|ged_test_qged_swrast)$' --timeout 180
--output-on-failure` completed 2/2 passing in 18.32 seconds:
`brlobol_pivot_guard` 18.16 seconds and `ged_test_qged_swrast` 0.11 seconds.
The full qtcad Obol workflow group completed 16/16 passing in 42.36 seconds;
longest members were `test_qtcad_obol_real_models_havoc_wire` at 26.85
seconds, `test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.01
seconds, and `test_qtcad_obol_real_models_m35_wire_interactions` at 4.18
seconds.  The expanded broad focused wrapper gate completed 27/27 passing in
116.76 seconds; longest members were `libbsg_api_hygiene` at 40.98 seconds,
`brlobol_pivot_guard` at 18.12 seconds, `ged_test_drawing_select` at 16.37
seconds, and `ged_test_drawing_quad` at 12.67 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
view-info/name/AET readback helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_info_get`, `qg_legacy_view_name_get`, or
`qg_legacy_view_aet_get`.  `src/qged/plugins/view/info/CADViewModel.cpp` and
`src/qged/plugins/extensions/common/HostStatusCommon.cpp` now include the
private `QgLegacyViewContext.h` adapter and read view info, view name, AET,
and view center through neutral `rt/view.h` `rt_view_context_*` accessors.
The pivot guard rejects those public qtcad readback helpers from returning to
the installed header, legacy-view implementation, qged view-info model, or
qged host-status common helper.  `qg_legacy_view_center_get` remains because
qtcad canvas/input internals still use it.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
qged_view_info qged_host_status_command qged_host_status_dialog
qged_host_status_panel` completed in 27.53 seconds.  The targeted
guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|ged_test_qged_swrast)$' --timeout 180
--output-on-failure` completed 2/2 passing in 18.34 seconds:
`brlobol_pivot_guard` 18.19 seconds and `ged_test_qged_swrast` 0.11 seconds.
The full qtcad Obol workflow group completed 16/16 passing in 42.43 seconds;
longest members were `test_qtcad_obol_real_models_havoc_wire` at 26.96
seconds, `test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.03
seconds, and `test_qtcad_obol_real_models_m35_wire_interactions` at 4.17
seconds.  The expanded broad focused wrapper gate completed 27/27 passing in
116.96 seconds; longest members were `libbsg_api_hygiene` at 40.98 seconds,
`brlobol_pivot_guard` at 18.00 seconds, `ged_test_drawing_select` at 16.65
seconds, and `ged_test_drawing_quad` at 12.96 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
snap source/line helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_snap_source_view_only_set`,
`qg_legacy_view_snap_source_db_set`, `qg_legacy_view_snap_lines_get`,
`qg_legacy_view_snap_lines_set`, or `qg_legacy_view_snap_exclude_clear`.
`src/qged/plugins/polygon/QPolyCreate.cpp`,
`src/qged/plugins/polygon/QPolyMod.cpp`, and
`src/libqtcad/tests/test_qtcad_obol_snap.cpp` now include the private
`QgLegacyViewContext.h` adapter and set snap source, snap lines, and
snap-exclude state through neutral `rt/view.h` `rt_view_context_*` accessors.
The pivot guard rejects those public qtcad snap source/line helpers from
returning to the installed header, legacy-view implementation, qged polygon
plugins, or qtcad Obol snap test.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_snap qged_polygon_create qged_polygon_modify` completed in
30.67 seconds.  The targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_snap|ged_test_qged_swrast)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.44 seconds:
`brlobol_pivot_guard` 18.17 seconds, `test_qtcad_obol_snap` 0.11 seconds,
and `ged_test_qged_swrast` 0.11 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.50 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.99 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.05 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 27/27 passing in 116.60
seconds; longest members were `libbsg_api_hygiene` at 41.04 seconds,
`brlobol_pivot_guard` at 18.15 seconds, `ged_test_drawing_select` at 16.34
seconds, and `ged_test_drawing_quad` at 12.67 seconds.

The preceding completed code-state batch removes the retired qtcad legacy
snap/tolerance query helpers.  `include/qtcad/QgLegacyView.h` and
`src/libqtcad/QgLegacyView.cpp` no longer publish or implement
`qg_legacy_view_db_snap_enabled`, `qg_legacy_view_snap_kind_mask_get`,
`qg_legacy_view_size_get`, or
`qg_legacy_view_snap_tolerance_factor_get`.  `src/libqtcad/QgViewFilter.cpp`
now includes the private `QgLegacyViewContext.h` adapter and reads snap policy,
snap-kind mask, size, and tolerance data through neutral `rt/view.h`
`rt_view_context_*` accessors.  The pivot guard rejects those public qtcad
query helpers from returning to the installed header, legacy-view
implementation, or qtcad view filter.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_snap test_qtcad_obol_draw_sync` completed in 19.73 seconds.
The targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_snap|test_qtcad_obol_draw_sync)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.61 seconds:
`brlobol_pivot_guard` 18.34 seconds, `test_qtcad_obol_draw_sync` 0.11
seconds, and `test_qtcad_obol_snap` 0.11 seconds.  The full qtcad Obol
workflow group completed 16/16 passing in 42.41 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.90 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.03 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.18 seconds.  The
expanded broad focused wrapper gate completed 27/27 passing in 116.33
seconds; longest members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 18.03 seconds, `ged_test_drawing_select` at 16.31
seconds, and `ged_test_drawing_quad` at 12.81 seconds.  `git diff --check`
was clean, the retired qtcad snap/tolerance query helper scan was empty, the
retired qtcad snap-kind alias scan was empty, and the targeted build and
`/tmp` scratch scans found no matching temporary files.

The preceding completed code-state batch removes the retired qtcad legacy
snap-kind aliases.  `include/qtcad/QgLegacyView.h` no longer publishes
`QG_LEGACY_VIEW_SNAP_KIND_*`; `qg_legacy_view_snap_kind_mask_get` returns the
neutral RT snap-kind mask directly, and `src/libqtcad/QgViewFilter.cpp` maps
`RT_VIEW_SNAP_KIND_*` bits to qtcad Obol snap records.  The pivot guard
rejects those qtcad snap aliases from returning to the public header,
legacy-view implementation, or qtcad view filter.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_snap test_qtcad_obol_draw_sync` completed in 19.55 seconds.
The targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_snap|test_qtcad_obol_draw_sync)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.32 seconds:
`brlobol_pivot_guard` 18.04 seconds, `test_qtcad_obol_draw_sync` 0.11
seconds, and `test_qtcad_obol_snap` 0.11 seconds.  The full qtcad Obol
workflow group completed 16/16 passing in 42.37 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.86 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.02 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.20 seconds.  The
expanded broad focused wrapper gate completed 27/27 passing in 116.49
seconds; longest members were `libbsg_api_hygiene` at 40.97 seconds,
`brlobol_pivot_guard` at 18.06 seconds, `ged_test_drawing_select` at 16.28
seconds, and `ged_test_drawing_quad` at 12.71 seconds.  `git diff --check`
was clean, the retired qtcad snap-kind alias scan was empty outside guard
patterns, and the targeted build and `/tmp` scratch scans found no matching
temporary files.

The preceding completed code-state batch removes the retired qtcad legacy
draw-mode aliases.  `include/qtcad/QgLegacyView.h` no longer publishes
`QG_LEGACY_VIEW_DRAW_MODE_*`, and `src/libqtcad/QgLegacyView.cpp` no longer
carries the matching self-checks.  qtcad Obol draw-sync callers already use
canonical `GED_DRAW_MODE_*` values through the neutral `ged/draw.h` surface,
so the public qtcad legacy-view header no longer carries a parallel draw-mode
namespace after the legacy draw transaction/result/appearance/observer and
helper APIs were retired.  The pivot guard rejects those qtcad aliases from
returning to the public header or legacy-view implementation.

Verification on FreeBSD/clang passed: `ninja -C build libqtcad.so
test_qtcad_obol_draw_sync` completed in 19.43 seconds, and
`ninja -C build test_qtcad_obol_window_host` completed in 5.90 seconds after
an initial full qtcad CTest run exposed a stale window-host test binary still
looking for the retired `QgSW(QWidget *, fb *)` symbol.  The targeted
guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|test_qtcad_obol_draw_sync)$' --timeout 180
--output-on-failure` completed 2/2 passing in 18.34 seconds:
`brlobol_pivot_guard` 18.17 seconds and `test_qtcad_obol_draw_sync` 0.12
seconds.  The full qtcad Obol workflow group completed 16/16 passing in
42.63 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.84 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.32 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 27/27 passing in 116.38
seconds; longest members were `libbsg_api_hygiene` at 40.98 seconds,
`brlobol_pivot_guard` at 18.04 seconds, `ged_test_drawing_select` at 16.28
seconds, and `ged_test_drawing_quad` at 12.76 seconds.  `git diff --check`
was clean, the retired qtcad draw-mode alias scan was empty outside guard
patterns, and the targeted build and `/tmp` scratch scans found no matching
temporary files.

The preceding completed code-state batch moves draw-source draft-pair root
creation off the retained `bsg_scene_ref` return path.
`ged_draw_scene_ref_create_draft_pair` now calls the neutral
`ged_draw_ensure_root_attached` helper when it only needs to force root
creation/attachment and does not consume the retained root ref.  Direct
retained root-returning calls remain only in
`src/libged/bsg_ged_draw_tree.c` and the scene-root owner/private declaration.
The pivot guard requires the neutral helper in
`src/libged/bsg_ged_draw_source.c` and rejects direct retained-root creation
from returning there.

Verification on FreeBSD/clang passed: `ninja -C build libged.so
ged_test_ged_draw_scene` completed in 1.94 seconds.  The targeted
guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|ged_test_ged_draw_scene|regress-mged_draw|regress-mged_view)$'
--timeout 180 --output-on-failure` completed 4/4 passing in 20.31 seconds:
`brlobol_pivot_guard` 18.01 seconds, `ged_test_ged_draw_scene` 1.42 seconds,
and the two MGED regressions 0.41 seconds each.  The expanded broad focused
wrapper gate completed 27/27 passing in 116.48 seconds; longest members were
`libbsg_api_hygiene` at 40.97 seconds, `brlobol_pivot_guard` at 18.09
seconds, `ged_test_drawing_select` at 16.38 seconds, and
`ged_test_drawing_quad` at 12.68 seconds.  `git diff --check` was clean,
side-effect-only and draft-pair root callers no longer had direct
`ged_draw_ensure_root` calls, the remaining direct retained root-ensure scan
was limited to the tree path-construction bridge, scene-root owner, and
private declaration, and the targeted build and `/tmp` scratch scans found no
matching temporary files.

The preceding completed code-state batch moved side-effect-only GED draw-root
creation off the retained `bsg_scene_ref` return path.  The scene-root bridge
now exposes `ged_draw_ensure_root_attached` for callers that only need to
force root creation/attachment, while retaining `ged_draw_ensure_root` for
bridge internals that still consume the retained root ref.  `src/libged/ged.cpp`,
`src/libged/dm/dm.c`, `src/libged/view/view.c`, and
`src/libged/view/objs.cpp` now use the neutral side-effect helper.  The pivot
guard requires the neutral helper in the side-effect callers and rejects
direct retained-root creation from returning there.

The preceding completed code-state batch moves the remaining GED draw-test
retained summary assertions from BSG-named RT wrappers to neutral `rt/view.h`
APIs.  `include/rt/view.h` now exposes
`rt_view_context_visible_render_summary`,
`rt_view_context_named_line_render_count`,
`rt_view_context_render_export_consistency`, and
`rt_view_context_feature_geometry_summary`; the retained implementation in
`src/librt/view_legacy_bsg.c` delegates to the existing BSG adapter while
callers use neutral records and entry points.
`src/libged/tests/draw/mged_bsg.cpp`,
`src/libged/tests/draw/quad.cpp`, and
`src/libged/tests/draw/view_command.cpp` now include only `rt/view.h` for
their render-summary, named-line, feature-geometry, and render/export/backend
consistency assertions.  The pivot guard requires the neutral summary tokens,
rejects `rt/view_legacy_bsg.h` and the retired `_bsg` summary wrappers from
those draw tests, and tracks neutral adapter coverage in
`src/librt/tests/view_legacy_bsg.c`.

Verification on FreeBSD/clang passed: `ninja -C build rt_view_legacy_bsg
ged_test_view_command ged_test_quad ged_test_mged_bsg` completed in 77.40
seconds after the `rt/view.h` update forced a broad `librt`, `libged`,
`libqtcad`, and swrast rebuild.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_view_command|ged_test_drawing_quad|ged_test_mged_bsg)$'
--timeout 180 --output-on-failure` completed 5/5 passing in 31.57 seconds:
`brlobol_pivot_guard` 18.02 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`ged_test_drawing_quad` 12.74 seconds, `ged_test_mged_bsg` 0.53 seconds, and
`ged_test_view_command` 0.21 seconds.  The expanded broad focused wrapper gate
completed 27/27 passing in 116.59 seconds; longest members were
`libbsg_api_hygiene` at 41.00 seconds, `brlobol_pivot_guard` at 18.05
seconds, `ged_test_drawing_select` at 16.49 seconds, and
`ged_test_drawing_quad` at 12.69 seconds.  `git diff --check` was clean, the
retired-wrapper scan over `src/libged/tests/draw` found no direct
`rt/view_legacy_bsg.h` or BSG-named RT wrappers, and the targeted build and
`/tmp` scratch scans found no matching temporary files.

The preceding completed code-state batch moves six GED BSG fixture setup paths
from BSG-named RT wrappers to neutral `rt/view.h` APIs.  The BSG
render-stability fixture now uses neutral display-manager access,
display-manager set, dimension sync, scale-storage access, and unit
conversion while preserving the BSG render stability check.  The Tcl overlay
lifecycle fixture now uses neutral display-manager get/set for its headless
`dm_draw_objs` NULL-DM check while preserving the GED draw context feature
assertions.  The MGED shaded-mode fixture now uses neutral setup/readback APIs
and no longer includes `rt/view_legacy_bsg.h`.  The main MGED BSG fixture now
uses neutral setup/readback, center-dot state, and edit-matrix APIs.  The
quad-stability
and rtwizard fixtures now use neutral view creation, view naming, view-set
registration/removal, display-manager setup, unit-conversion,
scene-attached/scene-anchor/scene-shared, independent-scope, and model2view
readback APIs where applicable; both no longer include `rt/view_legacy_bsg.h`.
The pivot guard requires the neutral setup tokens and rejects the retired
BSG-named setup, scene-anchor, scene-share, and model2view wrappers from
returning.

Verification on FreeBSD/clang passed: `ninja -C build
ged_test_bsg_render_stability` completed in 3.80 seconds,
`ninja -C build ged_test_tcl_overlay_bsg` completed in 3.84 seconds,
`ninja -C build ged_test_mged_shaded_mode_bsg` completed in 3.87 seconds,
`ninja -C build ged_test_mged_bsg` completed in 3.85 seconds, 3.86 seconds
after the final scene-attached cleanup, and 3.84 seconds after the center-dot
and edit-matrix neutralization,
`ninja -C build ged_test_bsg_quad_stability` completed in 3.81 seconds, and
`ninja -C build ged_test_rtwizard_bsg` completed in 3.83 seconds.
The render-stability targeted guard/runtime gate completed 2/2 passing in
18.26 seconds.  The Tcl overlay targeted guard/runtime gate completed 2/2
passing in 18.03 seconds.  The MGED targeted guard/runtime gate completed 3/3
passing in 18.99 seconds: `brlobol_pivot_guard` 17.92 seconds,
`ged_test_mged_bsg` 0.53 seconds, and `ged_test_mged_shaded_mode_bsg`
0.48 seconds.  The quad/rtwizard targeted guard/runtime gate completed 3/3
passing in 18.94 seconds, and the final touched-fixture gate completed 4/4
passing in 19.37 seconds: `brlobol_pivot_guard` 17.96 seconds,
`ged_test_bsg_quad_stability` 0.53 seconds, `ged_test_mged_bsg` 0.53 seconds,
and `ged_test_rtwizard_bsg` 0.29 seconds.  The follow-up MGED
center-dot/edit-matrix targeted gate completed 2/2 passing in 18.75 seconds:
`brlobol_pivot_guard` 18.17 seconds and `ged_test_mged_bsg` 0.53 seconds.
The expanded broad focused wrapper gate completed 27/27 passing in 116.60
seconds, then 27/27 passing in 116.46 seconds after the MGED state cleanup;
longest follow-up members were `libbsg_api_hygiene` at 41.01 seconds,
`brlobol_pivot_guard` at 18.03 seconds, `ged_test_drawing_select` at 16.43
seconds, and `ged_test_drawing_quad` at 12.73 seconds.

The preceding completed code-state batch moves the GED quad drawing fixture
setup path from BSG-named RT wrappers to neutral `rt/view.h` APIs.
`src/libged/tests/draw/quad.cpp` now uses neutral display-manager access,
dimension sync, scale-storage access, unit conversion, view-set membership,
view naming, and independent-view checks.  It still includes
`rt/view_legacy_bsg.h` only for retained feature-geometry and named-line
render summary assertions, which remain BSG-backed until the feature/render
summary backend boundary migrates.  The pivot guard requires the neutral setup
tokens and rejects the retired BSG-named setup wrappers from returning to the
fixture.

Verification on FreeBSD/clang passed: `ninja -C build ged_test_quad`
completed in 3.89 seconds.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|ged_test_drawing_quad)$' --timeout 180
--output-on-failure` completed 2/2 passing in 30.62 seconds:
`brlobol_pivot_guard` 17.92 seconds and `ged_test_drawing_quad`
12.65 seconds.  The expanded broad focused wrapper gate completed 22/22
passing in 114.83 seconds; longest members were `libbsg_api_hygiene` at
41.03 seconds, `brlobol_pivot_guard` at 18.03 seconds,
`ged_test_drawing_select` at 16.27 seconds, and `ged_test_drawing_quad` at
12.72 seconds.

The preceding completed code-state batch moves the GED selection drawing fixture
and AET multi-view fixture from BSG-named RT view wrappers to neutral
`rt/view.h` APIs.  `src/libged/tests/draw/select.cpp` and
`src/libged/tests/draw/aet.cpp` now use neutral display-manager access,
display-manager set, dimension sync, scale-storage access, unit conversion,
view-set membership, view naming, AET state, and view update where applicable.
The pivot guard requires those neutral tokens and rejects `rt/view_legacy_bsg.h`
or the retired BSG-named setup/view-state wrappers from returning to those
migrated fixtures.

Verification on FreeBSD/clang passed: `ninja -C build ged_test_select_draw
ged_test_aet` completed in 3.87 seconds.  `ged_test_aet` is not registered as
a normal CTest test, so it was run directly from `build/` as
`src/libged/tests/draw/ged_test_aet ../src/libged/tests/draw` and passed in
1.34 seconds; the generated `build/moss_aet_tmp.g` and
`build/ged_aet_test_cache` scratch paths were removed after the run.  The
targeted guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_drawing_select)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 34.36 seconds:
`brlobol_pivot_guard` 17.90 seconds, `rt_view_legacy_bsg` 0.02 seconds, and
`ged_test_drawing_select` 16.39 seconds.  The expanded broad focused wrapper
gate completed 22/22 passing in 114.92 seconds; longest members were
`libbsg_api_hygiene` at 40.96 seconds, `brlobol_pivot_guard` at
17.88 seconds, `ged_test_drawing_select` at 16.62 seconds, and
`ged_test_drawing_quad` at 12.75 seconds.

The preceding completed code-state batch moves common GED draw-test swrast setup
paths from BSG-named RT wrappers to neutral `rt/view.h` APIs.
`include/rt/view.h` now exposes `rt_view_context_frametime_set`; the retained
implementation in `src/librt/view_legacy_bsg.c` delegates to the existing BSG
adapter while callers use the neutral API.  `src/libged/tests/draw/util.cpp`,
`basic.cpp`, `faceplate.cpp`, `lod.cpp`, and `lod_crossrun.cpp` now use
neutral display-manager access, display-manager set, dimension sync,
scale-storage access, unit conversion, and frametime setup where applicable.
The pivot guard requires those neutral tokens and rejects `rt/view_legacy_bsg.h`
or the retired BSG-named setup wrappers from returning to those migrated test
paths.

Verification on FreeBSD/clang passed: `ninja -C build rt_view_legacy_bsg
ged_test_draw ged_test_faceplate ged_test_lod ged_test_lod_crossrun`
completed in 78.27 seconds after a broad `rt/view.h` rebuild through `librt`,
`libged`, `libqtcad`, and swrast.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_drawing_basic|ged_test_drawing_faceplate|ged_test_drawing_lod|ged_test_lod_crossrun)$'
--timeout 180 --output-on-failure` completed 6/6 passing in 40.50 seconds:
`brlobol_pivot_guard` 17.80 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`ged_test_drawing_basic` 7.55 seconds, `ged_test_drawing_faceplate`
1.74 seconds, `ged_test_drawing_lod` 10.92 seconds, and
`ged_test_lod_crossrun` 2.41 seconds.  The expanded broad focused wrapper
gate completed 21/21 passing in 98.45 seconds; longest members were
`libbsg_api_hygiene` at 41.03 seconds, `brlobol_pivot_guard` at
17.96 seconds, `ged_test_drawing_quad` at 12.75 seconds, and
`ged_test_drawing_lod` at 11.00 seconds.

The preceding completed code-state batch moves the GED selection semantics test
off BSG-named RT selection and pick-result wrappers.  `include/rt/view.h` now
exposes `rt_view_context_selection_available` and
`rt_view_context_selection_count`; the retained implementation in
`src/librt/view_legacy_bsg.c` delegates to the existing BSG adapter while
callers use the neutral API.  `src/libged/tests/draw/selection_semantics.cpp`
now includes only `rt/view.h` and uses neutral view size, point pick-result,
pick-result count/free, selection availability/count, selection apply, and
selection clear APIs.  The pivot guard requires those neutral tokens, tracks
the retained adapter coverage in the RT legacy header/source/test inventories,
and rejects the retired BSG-named pick/selection wrappers from returning to
that test.

Verification on FreeBSD/clang passed: `ninja -C build rt_view_legacy_bsg
ged_test_selection_semantics` completed in 76.49 seconds after a broad
`rt/view.h` rebuild through `librt`, `libged`, and `libqtcad`.  The targeted
guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_selection_semantics)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 18.01 seconds:
`brlobol_pivot_guard` 17.81 seconds, `rt_view_legacy_bsg` 0.02 seconds, and
`ged_test_selection_semantics` 0.12 seconds.  The focused migration gate
completed 9/9 passing in 18.83 seconds.  The broad focused wrapper gate
completed 18/18 passing in 83.21 seconds; longest members were
`libbsg_api_hygiene` at 41.00 seconds, `brlobol_pivot_guard` at
17.91 seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.52 seconds.

The preceding completed code-state batch moves the GED view-independent local
view setup and independent-scope checks from BSG-named RT wrappers to neutral
`rt/view.h` APIs.  `include/rt/view.h` exposes
`rt_view_context_lod_bounds_callback_is`; the retained implementation in
`src/librt/view_legacy_bsg.c` delegates to the existing BSG adapter while
callers use the neutral API.  `src/libged/tests/draw/view_independent.cpp`
now includes only `rt/view.h` and uses neutral view creation, view-set
registration, view naming, scale state, LoD callback status, size readback,
independent-view status, and independent-scope null checks.  The pivot guard
requires those neutral tokens and rejects the retired BSG-named setup and
independent-scope wrappers from returning to that test.

Verification on FreeBSD/clang passed: `ninja -C build rt_view_legacy_bsg
ged_test_view_independent` completed in 77.01 seconds after a broad
`rt/view.h` rebuild through `librt`, `libged`, and `libqtcad`.  The targeted
guard/runtime gate `ctest --test-dir build -R
'^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_view_independent)$'
--timeout 180 --output-on-failure` completed 3/3 passing in 17.96 seconds:
`brlobol_pivot_guard` 17.74 seconds, `rt_view_legacy_bsg` 0.02 seconds, and
`ged_test_view_independent` 0.16 seconds.  The focused migration gate
completed 8/8 passing in 18.41 seconds.  The broad focused wrapper gate
completed 17/17 passing in 83.60 seconds; longest members were
`libbsg_api_hygiene` at 41.41 seconds, `brlobol_pivot_guard` at
17.92 seconds, `ged_test_drawing_quad` at 12.74 seconds, and
`ged_test_drawing_basic` at 7.57 seconds.

The preceding completed code-state batch moves the GED view-command local
test-view setup and polygon inspection paths from BSG-named RT wrappers to
neutral `rt/view.h` APIs.  `src/libged/tests/draw/view_command.cpp` now uses
`rt_view_context_create`, `rt_view_context_name_set`,
`rt_view_context_dimensions_set`, `rt_view_set_context_add` /
`rt_view_set_context_remove`, `rt_view_context_polygon_find`,
`rt_view_polygon_ref_is_null`, and `rt_view_polygon_record_get`.  It still
includes `rt/view_legacy_bsg.h` for the retained render/export/backend
consistency shim only.  The pivot guard now requires those neutral setup and
polygon tokens and rejects the retired BSG-named setup/polygon utility calls
from returning to that test.

Verification on FreeBSD/clang passed: `ninja -C build ged_test_view_command`
completed in 3.55 seconds.  The targeted guard/runtime gate
`ctest --test-dir build -R
'^(brlobol_pivot_guard|ged_test_view_command)$' --timeout 180
--output-on-failure` completed 2/2 passing in 17.95 seconds:
`brlobol_pivot_guard` 17.75 seconds and `ged_test_view_command`
0.15 seconds.  The focused migration gate completed 7/7 passing in
18.32 seconds.  The broad focused wrapper gate completed 16/16 passing in
83.57 seconds; longest members were `libbsg_api_hygiene` at 41.39 seconds,
`brlobol_pivot_guard` at 18.20 seconds, `ged_test_drawing_quad` at
12.71 seconds, and `ged_test_drawing_basic` at 7.50 seconds.

The preceding completed code-state batch adds neutral RT semantic-path pick and
measure entry points.  `include/rt/view.h` now exposes
`rt_view_context_pick_semantic_path` and
`rt_view_context_measure_candidates`; the retained implementation in
`src/librt/view_legacy_bsg.c` still delegates to the BSG backend, but callers
can use the existing neutral pick-result context and measure result structs
without including `rt/view_legacy_bsg.h` or calling BSG-shaped wrappers.  The
GED measure semantics test now includes `rt/view.h` and uses neutral view
creation, dimensions, measure, and free APIs.  The GED view-command test still
uses legacy wrappers for the unrelated render/export consistency check, but its
semantic-path pick, snap result, and measure utility paths now use neutral RT
entry points.  The pivot guard requires the new neutral pick/measure tokens
and rejects the retired BSG-shaped calls from those migrated test paths.

Verification on FreeBSD/clang passed: the affected
`ninja -C build rt_view_legacy_bsg ged_test_measure_semantics
ged_test_view_command` build completed in 75.83 seconds after a broad
`rt/view.h` rebuild.  The standard affected wrapper build
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so
test_qtcad_obol_draw_sync qsketch ged_test_snap_semantics
ged_test_measure_semantics ged_test_view_command` then passed in
12.53 seconds.  The targeted guard/runtime gate completed 4/4 passing in
18.10 seconds: `brlobol_pivot_guard` 17.88 seconds, `rt_view_legacy_bsg`
0.02 seconds, `ged_test_measure_semantics` 0.01 seconds, and
`ged_test_view_command` 0.15 seconds.  The focused migration gate completed
7/7 passing in 18.20 seconds.  The broad focused wrapper gate, with the
migrated snap and measure tests included, completed 16/16 passing in
83.03 seconds; longest members were `libbsg_api_hygiene` at 41.08 seconds,
`brlobol_pivot_guard` at 17.74 seconds, `ged_test_drawing_quad` at
12.88 seconds, and `ged_test_drawing_basic` at 7.54 seconds.

The preceding completed code-state batch adds a neutral opaque RT snap-result
context API.  `include/rt/view.h` now exposes
`rt_view_snap_result_context_*` and
`rt_view_context_snap_candidates_result`; the current implementation in
`src/librt/view_legacy_bsg.c` still delegates to the retained BSG snap
backend, but callers no longer need `rt/view_legacy_bsg.h` or
`rt_view_snap_result_*_bsg` to inspect snap candidates.  The GED snap
semantics test now includes `rt/view.h` and uses neutral view setup, grid
state, snap source, snap-result, and snap-point APIs.  The GED view-command
test still uses legacy wrappers for unrelated render/export/pick/measure and
polygon checks, but its snap-result path has moved to the neutral RT
snap-result context.  The pivot guard requires the new neutral snap tokens
and rejects the retired BSG snap-result calls from those migrated test paths.

Verification on FreeBSD/clang passed: the affected
`ninja -C build rt_view_legacy_bsg ged_test_snap_semantics
ged_test_view_command` build completed after a broad `rt/view.h` rebuild.  The
standard affected wrapper build
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so
test_qtcad_obol_draw_sync qsketch ged_test_snap_semantics
ged_test_view_command` then passed in 12.50 seconds.  The targeted
guard/runtime gate completed 4/4 passing in 17.95 seconds:
`brlobol_pivot_guard` 17.72 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`ged_test_snap_semantics` 0.01 seconds, and `ged_test_view_command`
0.15 seconds.  The focused migration gate completed 6/6 passing in
18.17 seconds.  The broad focused wrapper gate, with the migrated snap tests
included, completed 15/15 passing in 83.18 seconds; longest members were
`libbsg_api_hygiene` at 41.23 seconds, `brlobol_pivot_guard` at
17.98 seconds, `ged_test_drawing_quad` at 12.68 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.

The preceding completed code-state batch retires the private retained GED root
accessors.  `ged_scene_root_ref` and `ged_scene_root_ref_set` are gone from
`src/libged/ged_private.h` and `src/libged/ged_util.cpp`; retained draw code
now reaches the GED root through `ged_draw_scene_root_ref`, which converts
from the neutral `ged_scene_root_rt_ref` value at the retained draw boundary.
The pivot guard rejects the retired accessor names from returning to the
private header, root-storage implementation, or scene-root bridge.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 18.61
seconds, the standard affected wrapper build in 0.48 seconds, the retired
retained root-accessor scan with no hits, standalone `brlobol_pivot_guard` in
17.91 seconds, the focused migration gate 4/4 in 18.02 seconds, and the
standard broad focused wrapper gate 13/13 in 82.66 seconds.  Longest current
members were `libbsg_api_hygiene` at 41.01 seconds,
`brlobol_pivot_guard` at 17.70 seconds in the broad gate,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.57 seconds.

The preceding completed code-state batch narrows GED scene-root reuse handoff.
`_sg_root` in `src/libged/bsg_ged_draw_scene_root.c` no longer reads the
retained GED root through `ged_scene_root_ref` when the root already exists.
It starts from the neutral `ged_scene_root_rt_ref` value, reattaches that
neutral root to active and known view contexts, and converts back to retained
`bsg_scene_ref` only when returning to the current BSG-backed draw
implementation.  The only direct `ged_scene_root_ref` call left in the
scene-root owner file at that batch boundary was the `ged_draw_scene_root_ref`
compatibility facade; the latest retained root-accessor retirement has since
removed that direct call as well.  The pivot guard requires the
neutral-to-retained conversion in the scene-root owner and rejects direct
retained GED-root reads there.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 1.12
seconds, the standard affected wrapper build in 0.43 seconds, standalone
`brlobol_pivot_guard` in 17.77 seconds, the focused migration gate 4/4 in
18.21 seconds, and the standard broad focused wrapper gate 13/13 in 82.86
seconds.  Longest current members were `libbsg_api_hygiene` at 40.96 seconds,
`brlobol_pivot_guard` at 17.92 seconds in the broad gate,
`ged_test_drawing_quad` at 12.81 seconds, and `ged_test_drawing_basic` at
7.57 seconds.

The preceding completed code-state batch neutralizes GED draw root storage.
`struct ged_drawable` now stores `gd_scene_root` as neutral
`rt_view_scene_ref` instead of retained `bsg_scene_ref`.  At that batch
boundary, the retained `ged_scene_root_ref` and `ged_scene_root_ref_set`
compatibility accessors converted at the accessor boundary; the latest
retained root-accessor retirement has since removed them.  `ged_scene_root_rt_ref`
and `ged_scene_root_rt_ref_set` read and write the stored neutral root directly.
`src/libged/ged_private.h` also drops its direct `bsg/scene_builder.h` include.
The pivot guard rejects the old `bsg_scene_ref gd_scene_root` field and direct
retained-root storage patterns from returning.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 19.22
seconds, the standard affected wrapper build in 0.47 seconds, the retired
retained root-storage scan with no hits, standalone `brlobol_pivot_guard` in
17.79 seconds, the focused migration gate 4/4 in 18.02 seconds, and the
standard broad focused wrapper gate 13/13 in 82.70 seconds.  Longest current
members were `libbsg_api_hygiene` at 41.15 seconds,
`brlobol_pivot_guard` at 17.83 seconds in the broad gate,
`ged_test_drawing_quad` at 12.53 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The preceding completed code-state batch narrows the GED draw view-private header
boundary.  Retained BSG-typed scene-ref conversion and overlay registry helper
declarations moved out of `src/libged/bsg_ged_draw_view_private.h` and into
the full retained private bridge header, `src/libged/bsg_ged_draw_private.h`.
The narrow view-private header no longer includes BSG scene headers or exposes
`bsg_scene_ref`; it stays on opaque view-context and neutral RT/GED adapter
declarations for `ged_view_legacy.cpp` and command-facing code.  The pivot
guard now rejects `bsg/` includes, `bsg_scene_ref`, retained scene-ref
conversion, and overlay registry helper declarations from returning to the
narrow header while requiring those retained declarations in the full private
bridge header.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 19.88
seconds, the standard affected wrapper build in 0.44 seconds, the narrowed
view-private retained BSG exposure scan with no hits, standalone
`brlobol_pivot_guard` in 17.79 seconds, the focused migration gate 4/4 in
18.02 seconds, and the standard broad focused wrapper gate 13/13 in 82.57
seconds.  Longest current members were `libbsg_api_hygiene` at 40.98 seconds,
`brlobol_pivot_guard` at 17.73 seconds in the broad gate,
`ged_test_drawing_quad` at 12.70 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

The preceding completed code-state batch removes the remaining public
`qg_legacy_view_draw_*` helper surface from installed
`include/qtcad/QgLegacyView.h` and from
`src/libqtcad/QgLegacyView.cpp`.  `src/libqtcad/QgModel.cpp` now uses the
private legacy-view context adapter plus neutral `ged/draw.h` APIs for drawn
state, scene-revision snapshots, draw transactions, and erase transactions.
`src/qged/QgEdApp.cpp` applies redraw transactions directly through neutral
GED transactions while preserving the explicit neutral transaction-result
lifecycle.  The qged ell edit plugin now uses neutral GED highlight APIs
directly instead of qtcad draw-highlight wrappers.

The pivot guard now rejects any return of `qg_legacy_view_draw_*` to installed
qtcad headers or production source, requires the new neutral `QgModel`/
`QgEdApp` draw transaction call sites, and requires the neutral qged ell
highlight path.  Verification on FreeBSD/clang passed: the affected
qtcad/qged build in 40.60 seconds, the targeted qtcad/QgModel test set 8/8 in
17.45 seconds, standalone `brlobol_pivot_guard` in 17.97 seconds, the focused
migration gate 4/4 in 17.92 seconds, and the standard broad focused wrapper
gate 13/13 in 82.75 seconds.  Longest current members were
`libbsg_api_hygiene` at 40.97 seconds, `brlobol_pivot_guard` at 17.75
seconds in the broad gate, `test_qgmodel_scale` at 13.13 seconds in the
targeted qtcad/QgModel set, and `ged_test_drawing_quad` at 12.90 seconds.

The preceding completed code-state batch removes the unused public qg legacy draw
transaction/result/appearance/observer layer from installed
`include/qtcad/QgLegacyView.h`.  `src/libqtcad/QgLegacyView.cpp` kept the
then-surviving simple qtcad draw helpers, but implemented redraw, draw-path
apply, and erase-path apply directly with neutral `ged_draw_transaction`,
`ged_draw_apply_transaction`, and explicit neutral
`ged_draw_transaction_result` lifecycle.

FreeBSD/clang caught a real compatibility issue during this batch:
`test_qgmodel_model_tester` and `test_qgmodel_scale` initially failed with
`0x7074626c mis-aligned bu_vls pointer` when the helper path passed a null GED
transaction result.  The fix was to preserve the old qg result cleanup and
notification behavior with explicit neutral `ged_draw_transaction_result_init`
and `ged_draw_transaction_result_free` calls.  The pivot guard now rejects the
retired qg transaction/result/appearance/observer tokens from the public
header and implementation, while requiring the surviving helper surface and
direct neutral GED transaction path.

Verification on FreeBSD/clang passed after the fix: the affected qtcad/qged
build in 14.19 seconds, the targeted qtcad/QgModel test set 8/8 in 17.45
seconds, standalone `brlobol_pivot_guard` in 17.84 seconds, the focused
migration gate 4/4 in 18.21 seconds, and the standard broad focused wrapper
gate 13/13 in 82.60 seconds.  Longest current members were
`libbsg_api_hygiene` at 40.97 seconds, `brlobol_pivot_guard` at 17.75
seconds in the broad gate, `test_qgmodel_scale` at 13.15 seconds in the
targeted qtcad/QgModel set, and `ged_test_drawing_quad` at 12.75 seconds.

The preceding completed code-state batch removes the last private
`qg_obol_sync_draw_transaction` bridge.  `src/libqtcad/QgObolDrawSync.cpp`
now owns only the neutral GED transaction sync path, and
`src/libqtcad/QgObolDrawSyncPrivate.h` declares only
`qg_obol_display_accepts_ged_draw_transaction_view`,
`qg_obol_ged_draw_transaction_has_view`, and
`qg_obol_sync_ged_draw_transaction`.

The qtcad Obol workflow tests now construct neutral
`ged_draw_transaction` values, apply them with `ged_draw_apply_transaction`,
and sync through `qg_obol_sync_ged_draw_transaction`.  The tests still use the
private qtcad legacy view-context adapter to scope the neutral transaction to
the test view, but they no longer use qg legacy draw transaction/result,
appearance, or observer wrappers for Obol draw-sync setup.  The pivot guard
now rejects the retired qg draw-sync helper and qg transaction/result tokens
from returning to the draw-sync implementation or migrated qtcad Obol tests.

Verification on FreeBSD/clang passed: the affected qtcad/qged build in 19.33
seconds, the converted qtcad workflow subset 5/5 in 0.59 seconds, the split
qtcad real-model workflow cases 5/5 in 41.44 seconds, standalone
`brlobol_pivot_guard` in 18.32 seconds, the focused migration gate 4/4 in
18.25 seconds, and the standard broad focused wrapper gate 13/13 in 82.90
seconds.  Longest current members were `libbsg_api_hygiene` at 40.98 seconds,
`test_qtcad_obol_real_models_havoc_wire` at 27.01 seconds,
`brlobol_pivot_guard` at 18.32 seconds standalone, and
`ged_test_drawing_quad` at 12.70 seconds.

The preceding completed code-state batch retires installed
`include/qtcad/QgObolDrawSync.h` and removes it from
`include/qtcad/CMakeLists.txt`.  qged already used the neutral GED
transaction helpers in `src/libqtcad/QgObolDrawSyncPrivate.h`, and the
follow-on test-path cleanup removed the last private qg transaction sync
helper.

The affected qtcad Obol tests now include `QgObolDrawSyncPrivate.h`
explicitly instead of `qtcad/QgObolDrawSync.h`.  The pivot guard now rejects
any return of the installed draw-sync header, CMake header registration, or
public include while requiring the private neutral GED draw-sync declarations.
Verification on FreeBSD/clang passed for that batch before the follow-on
test-path cleanup.

The preceding completed code-state batch removes the now-unused public qtcad
view-sync draw-transaction helper.  After qged moved its Obol observer to the
private GED transaction bridge, `qg_obol_display_accepts_draw_transaction_view`
had no in-tree callers.  It has been removed from
`include/qtcad/QgObolViewSync.h` and `src/libqtcad/QgObolViewSync.cpp`.
`QgObolViewSync.h` now exposes only
`qg_obol_display_accepts_ged_active_view`, directly includes
`qtcad/defines.h` for `QTCAD_EXPORT`, and no longer includes
`qtcad/QgLegacyView.h` or names qg legacy draw transaction types.

The preceding completed code-state batch moves qged's Obol draw observer off
the public qg legacy draw-observer wrapper.  `src/qged/QgEdApp.cpp` now
registers with neutral `ged_draw_observer_add` /
`ged_draw_observer_remove` and receives `ged_draw_transaction` plus
`ged_draw_transaction_result` callbacks from `ged/draw.h`.  qged does not
parse the transaction fields directly; the private qtcad/qged bridge in
`src/libqtcad/QgObolDrawSyncPrivate.h` exposes
`qg_obol_display_accepts_ged_draw_transaction_view`,
`qg_obol_ged_draw_transaction_has_view`, and
`qg_obol_sync_ged_draw_transaction`, while
`src/libqtcad/QgObolDrawSync.cpp` owns GED transaction path/result-name
extraction, scene revision folding, draw-mode lookup, and full-scene
path/list sync through neutral `ged_draw_path_state` and
`ged_draw_list_paths` calls.

The preceding completed code-state batch moves qtcad's production model draw
observer path off the qg legacy draw-observer bridge.  `QgModel` now registers
with neutral `ged_draw_observer_add` / `ged_draw_observer_remove` and reads
`ged_draw_transaction` plus `ged_draw_transaction_result` directly inside
`src/libqtcad/QgModel.cpp`.  The installed `include/qtcad/QgModel.h` does not
expose those GED transaction structs; it keeps the implementation detail behind
opaque private hooks and a friend accessor.  A later qtcad legacy-view public
API cleanup removed the qg legacy draw transaction/result wrappers from
`QgLegacyView.h`; the surviving qtcad draw helpers now use neutral GED
transactions directly.

The preceding completed code-state batch removes the remaining direct
`BSG_DRAW_MODE_*` token from the GED draw source owner file.  The semantic-path
fallback in `src/libged/bsg_ged_draw_source.c` still creates a retained BSG
draw intent at the scene-ref owner boundary, but it now expresses the fallback
with neutral `GED_DRAW_MODE_WIRE` before casting to the retained backend mode
type.

The pivot guard now requires that neutral semantic-path fallback expression in
the source owner and rejects direct BSG draw-mode tokens from returning there.
This does not change behavior or retained representation; it removes another
raw BSG enum token from the bridge surface so later Obol/libbrlobol source
replacement has less BSG-shaped policy to unwind.

Verification on FreeBSD/clang passed: an incremental
`ninja -C build libged.so` in 1.45 seconds, standalone
`brlobol_pivot_guard` in 18.24 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.38 seconds, and the standard broad
focused wrapper gate 13/13 in 83.25 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at 18.38
seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.

The preceding completed code-state batch narrows the GED draw util split
file's header and draw-mode boundary.  `src/libged/bsg_ged_draw_util.c` no
longer includes broad direct BSG backend headers and no longer returns
`BSG_DRAW_MODE_WIRE` from the default draw-mode fallback.  Its small shared
helpers now depend only on GED/logging headers and use neutral
`GED_DRAW_MODE_WIRE`.

The pivot guard now requires the neutral default draw-mode token in the util
split file and rejects direct BSG includes or `BSG_DRAW_MODE_*` tokens from
returning there.  This does not change behavior; it removes stale header and
token coupling from a utility file that no longer owns retained BSG backend
operations.

The preceding completed code-state batch moves GED registry retained root
draw-context owner lookup behind the scene-ref facade.  The new
`ged_draw_scene_ref_draw_context_owner` helper is declared in
`src/libged/bsg_ged_draw_private.h` and implemented in
`src/libged/bsg_ged_draw_source.c`, where the retained BSG scene-ref owner
boundary already holds direct `bsg_scene_parent` and `bsg_scene_draw_ctx`
access.

`src/libged/bsg_ged_draw_registry.c` now recovers the owning GED drawable
through that helper instead of including `bsg/draw_ctx.h`, walking the root
locally, and reading `bsg_scene_draw_ctx(root)->owner_data` directly.  The
same batch also removes stale direct BSG backend includes from
`src/libged/bsg_ged_draw_navigation.c`; navigation continues to use
scene-ref/draw-intent facade helpers for traversal and fallback draw-mode
reads.

The pivot guard now requires the draw-context owner helper in the private
bridge and registry, rejects direct BSG headers or draw-context owner access
from returning to the registry, and rejects direct BSG headers from returning
to the navigation wrapper.  This does not change retained representation or
registry token semantics; it narrows the wrapper-side BSG header surface and
keeps root draw-context ownership inside the scene-ref backend boundary.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 17.89
seconds, standalone `brlobol_pivot_guard` in 18.40 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.40 seconds, and the standard broad
focused wrapper gate 13/13 in 82.96 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at 18.08
seconds, `ged_test_drawing_quad` at 12.72 seconds, and
`ged_test_drawing_basic` at 7.57 seconds.

The preceding completed code-state batch removes direct BSG backend header and
draw-mode-token exposure from the GED state wrapper.  After the earlier
draw-intent facade cleanup, `src/libged/bsg_ged_draw_state.c` no longer needs
direct `bsg/` includes or raw BSG draw-mode constants; it now includes
`bsg_ged_draw_private.h` explicitly and uses neutral `GED_DRAW_MODE_WIRE` when
updating semantic path intent.

The pivot guard now rejects direct `bsg/` includes, `BSG_DRAW_MODE_*` tokens,
and raw draw-intent mutation/access from returning to the state wrapper.  This
batch does not change behavior or retained representation; it tightens the
state wrapper's ownership boundary so later Obol/libbrlobol replacement work
has one fewer BSG backend header and token dependency to unwind.

Verification on FreeBSD/clang passed: an incremental
`ninja -C build libged.so` in 1.12 seconds, standalone
`brlobol_pivot_guard` in 18.11 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.35 seconds, and the standard broad
focused wrapper gate 13/13 in 82.89 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at 18.09
seconds, `ged_test_drawing_quad` at 12.67 seconds, and
`ged_test_drawing_basic` at 7.53 seconds.

The preceding completed code-state batch removes direct BSG backend header
exposure from the GED material wrapper.  After the material read-side facade
cleanup,
`src/libged/bsg_ged_draw_material.c` no longer needs direct `bsg/` includes;
it now reaches retained material/color behavior through
`bsg_ged_draw_private.h` scene-ref facade helpers.

The pivot guard now rejects direct `bsg/` includes from returning to the
material wrapper, in addition to the existing material-wrapper direct
legacy-color/material-revision read and write checks.  This batch does not
change behavior or retained representation; it tightens ownership so the
material wrapper is no longer coupled to BSG backend headers for the color
refresh path.

Verification on FreeBSD/clang passed: an incremental
`ninja -C build libged.so` in 0.67 seconds, standalone
`brlobol_pivot_guard` in 18.30 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.38 seconds, and the standard broad
focused wrapper gate 13/13 in 83.01 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.02 seconds, `brlobol_pivot_guard` at 18.11
seconds, `ged_test_drawing_quad` at 12.69 seconds, and
`ged_test_drawing_basic` at 7.55 seconds.

The preceding completed code-state batch moves GED material refresh read-side
scene-ref access behind the scene-ref facade.  `src/libged/bsg_ged_draw_private.h`
now declares `ged_draw_scene_ref_legacy_region_id`,
`ged_draw_scene_ref_legacy_basecolor`,
`ged_draw_scene_ref_legacy_user_color`,
`ged_draw_scene_ref_legacy_default_color`, and
`ged_draw_scene_ref_material_revision`; `src/libged/bsg_ged_draw_source.c`
implements those helpers at the retained BSG scene-ref owner boundary.

`src/libged/bsg_ged_draw_material.c` now reads legacy region id, base color,
user-color/default-color policy, and material revision through those helpers
instead of calling `bsg_scene_legacy_*` or `bsg_scene_material_revision`
directly.  This preserves the current BSG-backed color policy behavior while
removing another direct retained scene-ref read family from the material
wrapper.

The pivot guard now requires the material read helpers in the private header
and material wrapper, and rejects direct material-wrapper BSG legacy-color or
material-revision reads from returning.  Exit-criteria percentages did not
change: retained scene-root/root-scope handoffs, BSG-backed feature/polygon
internals, public BSG-shaped draw snap/scene-ref surfaces, qtcad retained
draw ownership, and the direct `libbsg` rows in `libdm` and `libged` still
determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 17.09
seconds, standalone `brlobol_pivot_guard` in 18.23 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.33 seconds, and the standard broad
focused wrapper gate 13/13 in 82.85 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at 18.04
seconds, `ged_test_drawing_quad` at 12.68 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.

The preceding completed code-state batch moves wrapper-side retained scene-ref
equality behind the scene-ref facade.  `src/libged/bsg_ged_draw_private.h`
now declares `ged_draw_scene_ref_equal`, and
`src/libged/bsg_ged_draw_source.c` implements it at the retained BSG
scene-ref owner boundary.

`src/libged/bsg_ged_draw_navigation.c`,
`src/libged/bsg_ged_draw_draft.c`,
`src/libged/bsg_ged_draw_binding.c`, and
`src/libged/bsg_ged_draw_registry.c` now call
`ged_draw_scene_ref_equal` instead of `bsg_scene_ref_equal` directly.  This
does not change retained representation or equality semantics; it completes
another small part of the scene-ref facade contract so future Obol/libbrlobol
replacement work has one fewer raw BSG operation in traversal and registry
wrappers.

The pivot guard now requires the equality helper token in the private header
and affected wrappers, and rejects direct wrapper-side `bsg_scene_ref_equal`
calls from returning.  Exit-criteria percentages did not change: retained
scene-root/root-scope handoffs, BSG-backed feature/polygon internals, public
BSG-shaped draw snap/scene-ref surfaces, qtcad retained draw ownership, and
the direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 17.30
seconds, standalone `brlobol_pivot_guard` in 18.28 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.45 seconds, and the standard broad
focused wrapper gate 13/13 in 82.94 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.05 seconds, `brlobol_pivot_guard` at 18.01
seconds, `ged_test_drawing_quad` at 12.67 seconds, and
`ged_test_drawing_basic` at 7.59 seconds.

The preceding completed code-state batch moves transaction-side neutral
`rt_view_scene_ref` to retained scene-ref conversion behind scene-ref facade
helpers.  `src/libged/bsg_ged_draw_private.h` now declares
`ged_draw_scene_ref_revalidate_draw_intent_rt` and
`ged_draw_scene_ref_child_count_rt`, and
`src/libged/bsg_ged_draw_refs.c` implements both by converting the neutral
scene ref at the scene-ref bridge boundary before calling the retained BSG
helpers.

`src/libged/bsg_ged_draw_transactions.c` continues to compare neutral root
refs for DB-event revalidation, but it no longer calls
`ged_draw_scene_ref_from_rt_view_ref` directly.  DB-event root revalidation
now passes neutral root refs to `ged_draw_scene_ref_revalidate_draw_intent_rt`,
and transaction root child-count snapshots use
`ged_draw_scene_ref_child_count_rt`.  This keeps transaction code on neutral
root handles while retaining the current BSG-backed behavior inside the
scene-ref bridge.

The pivot guard now requires the new RT-scene-ref helper tokens in the
transaction wrapper and private header, and rejects direct transaction-side
`ged_draw_scene_ref_from_rt_view_ref` calls from returning.  Exit-criteria
percentages did not change: retained scene-root/root-scope handoffs,
BSG-backed feature/polygon internals, public BSG-shaped draw snap/scene-ref
surfaces, qtcad retained draw ownership, and the direct `libbsg` rows in
`libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 18.15
seconds, standalone `brlobol_pivot_guard` in 17.90 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.57 seconds, and the standard broad
focused wrapper gate 13/13 in 83.01 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.03 seconds, `brlobol_pivot_guard` at 18.10
seconds, `ged_test_drawing_quad` at 12.69 seconds, and
`ged_test_drawing_basic` at 7.58 seconds.

A new completed code-state batch moves GED tree independent-scope
neutral-to-retained scene-ref handoff behind a private scene-ref facade
helper.  `src/libged/bsg_ged_draw_private.h` now declares
`ged_draw_scene_ref_independent_scope`, and
`src/libged/bsg_ged_draw_refs.c` implements it by calling
`rt_view_context_independent_scope_ref` and converting the neutral
`rt_view_scene_ref` into the retained BSG scene-ref form at the scene-ref
bridge boundary.

`src/libged/bsg_ged_draw_tree.c` no longer owns a local
`_sg_independent_scope_ref` helper and no longer calls
`rt_view_context_independent_scope_ref` or
`ged_draw_scene_ref_from_rt_view_ref` directly for independent-scope refs.
Scoped add, erase-base, and clear paths now use
`ged_draw_scene_ref_independent_scope`.  The retained representation is still
BSG-backed; this batch only concentrates the handoff at the scene-ref bridge
so a later Obol/libbrlobol replacement has one fewer tree-side conversion to
remove.

The pivot guard now requires the tree wrapper and private header to carry the
new facade helper, and rejects direct tree-side
`rt_view_context_independent_scope_ref`, `ged_draw_scene_ref_from_rt_view_ref`,
or retired `_sg_independent_scope_ref` calls from returning.  Exit-criteria
percentages did not change: retained scene-root/root-scope handoffs,
BSG-backed feature/polygon internals, public BSG-shaped draw snap/scene-ref
surfaces, qtcad retained draw ownership, and the direct `libbsg` rows in
`libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 18.76
seconds, standalone `brlobol_pivot_guard` in 18.40 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.36 seconds, and the standard broad
focused wrapper gate 13/13 in 83.04 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at 18.14
seconds, `ged_test_drawing_quad` at 12.67 seconds, and
`ged_test_drawing_basic` at 7.64 seconds.

A new completed code-state batch moves GED draw active-view reads and
transaction save/restore writes behind shared draw facade helpers.  The
transaction-local `ged_draw_active_view_ctx` helper has been promoted to the
private draw surface in `src/libged/bsg_ged_draw_private.h`, and the matching
`ged_draw_active_view_ctx_set` helper now carries transaction active-view
save/restore updates around legacy draw walking.  Both helpers are implemented
in `src/libged/bsg_ged_draw_scene_root.c`, where the direct
`ged_view_active_ctx` and `ged_view_active_ctx_set` calls remain at the
scene-root/view-owner boundary.

`src/libged/bsg_ged_draw_tree.c`, `src/libged/bsg_ged_draw_records.c`,
`src/libged/bsg_ged_draw_overlay.c`, and transaction paths now use the draw
facade helpers instead of reaching into GED active-view storage directly.
This does not change retained BSG backend representation: group creation,
overlay creation, transaction legacy draw walking, and root plumbing still run
behind context-shaped helpers until those backend capabilities migrate to
Obol/libbrlobol.

The pivot guard now requires `ged_draw_active_view_ctx` and
`ged_draw_active_view_ctx_set` in the affected draw wrappers and private
header, keeps the scene-root bridge as the only direct
`ged_view_active_ctx`/`ged_view_active_ctx_set` owner in the draw wrapper set,
and rejects direct active-view reads or writes from returning to tree,
records, overlay, or transactions.
Exit-criteria percentages did not change: retained scene-root/root-scope
handoffs, BSG-backed feature/polygon internals, public BSG-shaped draw
snap/scene-ref surfaces, qtcad retained draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 17.50
seconds, standalone `brlobol_pivot_guard` in 18.05 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.40 seconds, and the standard broad
focused wrapper gate 13/13 in 83.26 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.31 seconds, `brlobol_pivot_guard` at 18.03
seconds, `ged_test_drawing_quad` at 12.66 seconds, and
`ged_test_drawing_basic` at 7.64 seconds.

A new completed code-state batch moves remaining GED draw traversal
root-storage reads behind a scene-root facade helper.
`src/libged/bsg_ged_draw_private.h` now declares
`ged_draw_scene_root_ref`, and `src/libged/bsg_ged_draw_scene_root.c`
implements it at the retained scene-root storage owner boundary by returning
the GED retained root handle.  `src/libged/bsg_ged_draw_tree.c`,
`src/libged/bsg_ged_draw_records.c`, and
`src/libged/bsg_ged_draw_navigation.c` now call that facade instead of
reading `ged_scene_root_ref` directly.

This batch does not change the backend representation: the retained draw root
is still BSG-backed, and the direct `ged_scene_root_ref` implementation/owner
use remains in `src/libged/ged_util.cpp` and the scene-root bridge.  The value
of the batch is architectural containment: traversal code no longer reaches
into GED root storage directly, which keeps the eventual Obol/libbrlobol root
handoff concentrated in the scene-root owner path.

The pivot guard now requires `ged_draw_scene_root_ref` in the private header
and traversal files, and rejects direct `ged_scene_root_ref` calls from
returning to the tree, records, or navigation wrappers.  Exit-criteria
percentages did not change: retained scene-root/root-scope handoffs,
BSG-backed feature/polygon internals, public BSG-shaped draw snap/scene-ref
surfaces, qtcad retained draw ownership, and the direct `libbsg` rows in
`libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so` in 18.33
seconds, standalone `brlobol_pivot_guard` in 17.92 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.31 seconds, and the standard broad
focused wrapper gate 13/13 in 83.02 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.06 seconds, `brlobol_pivot_guard` at 18.11
seconds, `ged_test_drawing_quad` at 12.68 seconds, and
`ged_test_drawing_basic` at 7.56 seconds.

A new completed code-state batch moves GED root scene-ref handoff paths onto
neutral `rt_view_scene_ref` accessors.  `src/libged/ged_private.h` and
`src/libged/ged_util.cpp` now expose `ged_scene_root_rt_ref` and
`ged_scene_root_rt_ref_set` beside the retained `bsg_scene_ref` root storage.
`src/libged/bsg_ged_draw_scene_root.c` uses the neutral root ref when
attaching the retained draw root to RT view contexts, with
`_sg_view_context_scene_root_set` accepting `rt_view_scene_ref` instead of a
BSG scene ref.  The scene-root owner still uses the retained root handle for
root creation and return because the wider draw-tree backend is still
BSG-backed.

`src/libged/bsg_ged_draw_transactions.c` now reads the GED root through
`ged_scene_root_rt_ref` for DB-event revalidation and transaction
child-count snapshots, converting to the retained scene-ref form only at the
scene-ref backend boundary.  The pivot guard requires the new private root
accessor declarations, requires scene-root and transaction users to carry the
neutral root-ref tokens, rejects direct transaction-side `ged_scene_root_ref`
calls, and rejects typed BSG root-attach signatures in the scene-root helper.

Exit-criteria percentages did not change: retained scene-root/root-scope
handoffs, BSG-backed feature/polygon internals, public BSG-shaped draw
snap/scene-ref surfaces, qtcad retained draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
standalone `brlobol_pivot_guard` in 18.00 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.28 seconds, and the standard broad
focused wrapper gate 13/13 in 83.05 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.09 seconds, `brlobol_pivot_guard` at 18.06
seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.53 seconds.

A new completed code-state batch moves GED draft BSG construction behind the
scene-ref source facade.  In `src/libged/bsg_ged_draw_draft.c`, the retained
file-local `_ged_draw_shape_draft_create_bsg` helper is retired.  Draft
creation now calls `ged_draw_scene_ref_create_draft_pair`, stores an opaque
`void *view_ctx`, and no longer directly includes or calls BSG database-source
or geometry construction APIs.  The new source-facade helper in
`src/libged/bsg_ged_draw_source.c` owns the retained
`bsg_database_source_ref_create`, `bsg_geometry_ref_create`, append, prepare,
source-ref link, and initial geometry-clear sequence, preserving the previous
creation behavior while moving it to the backend-owning boundary.

The pivot guard now requires `ged_draw_scene_ref_create_draft_pair` in the
private header, source facade, and draft wrapper; rejects the old file-local
draft BSG constructor, typed `struct bsg_view` draft storage/casts, direct BSG
database-source/geometry construction includes and calls, and direct
`ged_draw_ensure_root`/prepare/source-link calls from returning to
`src/libged/bsg_ged_draw_draft.c`.  Remaining BSG tokens in draft are the
private scene-ref handle storage/equality/null values, the retained
appearance-settings bridge, color-soltab policy handoff, and the
BSG-shaped `ged_draw_shape_draft_commit_to_scene_ref` return surface.  Those
remain until the wider draft/source backend and public scene-ref API boundary
migrate.

Exit-criteria percentages did not change: retained scene-root/root-scope
handoffs, BSG-backed feature/polygon internals, public BSG-shaped draw
snap/scene-ref surfaces, qtcad retained draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
standalone `brlobol_pivot_guard` in 17.82 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.26 seconds, and the standard broad
focused wrapper gate 13/13 in 83.18 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.02 seconds, `brlobol_pivot_guard` at 18.21
seconds, `ged_test_drawing_quad` at 12.69 seconds, and
`ged_test_drawing_basic` at 7.68 seconds.

A new completed code-state batch moves GED draft display-state and scene
attribute writes behind scene-ref facade helpers.  In
`src/libged/bsg_ged_draw_draft.c`, auxiliary display-state copy now calls
`ged_draw_scene_ref_copy_aux_display_state`, and update-bounds, draw
center/size, highlight, line-style/width, legacy color/default/eval/region
metadata, naming, DB-object mark, appearance application, appearance-revision
bump, material RGB, transform, bounds, visibility, transparency, draw mode,
and draw-matrix updates now call scene-ref facade helpers instead of direct
BSG scene attribute functions.  The exact auxiliary copy behavior was kept
separate from the broader source display-state copier because draft auxiliary
geometry did not previously inherit bounds/center/size.

The new source facade helpers live in `src/libged/bsg_ged_draw_source.c` and
are declared in `src/libged/bsg_ged_draw_private.h`.  The pivot guard now
requires the draft wrapper to use the new facade tokens, requires the source
facade and private header to own the helpers, and rejects direct draft-side
BSG scene attribute read/write calls such as `bsg_scene_set_visible`,
`bsg_scene_set_line_style`, `bsg_scene_material_set_rgb`,
`bsg_scene_apply_appearance_settings`, and `bsg_scene_update_bounds` from
returning.  Remaining BSG tokens in draft are intentionally limited to the
retained BSG draft constructor, scene-ref handle equality/null plumbing,
color-soltab policy handoff, and BSG-shaped scene-ref return surface until
the wider draft/source backend and public scene-ref API boundaries migrate.

Exit-criteria percentages did not change: retained scene-root/root-scope
handoffs, BSG-backed feature/polygon internals, public BSG-shaped draw
snap/scene-ref surfaces, qtcad retained draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
standalone `brlobol_pivot_guard` in 17.89 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.50 seconds, and the standard broad
focused wrapper gate 13/13 in 82.73 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.95 seconds, `brlobol_pivot_guard` at 17.97
seconds, `ged_test_drawing_quad` at 12.72 seconds, and
`ged_test_drawing_basic` at 7.51 seconds.

A new completed code-state batch moves GED draft scene-ref and draw-intent
copy behavior behind scene-ref facade helpers.  In
`src/libged/bsg_ged_draw_draft.c`, draft null checks, source child traversal,
auxiliary display-state draw-mode copy/set, draw-intent copy, source/shape
append, detach, and release now use `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_child_count`, `ged_draw_scene_ref_child_at`,
`ged_draw_scene_ref_draw_mode`, `ged_draw_scene_ref_set_draw_mode`,
`ged_draw_scene_ref_copy_draw_intent`, `ged_draw_scene_ref_append_child`,
`ged_draw_scene_ref_detach`, and `ged_draw_scene_ref_release` rather than
direct BSG scene-ref, draw-mode, draw-intent, append, remove, or destroy
calls.  The common retained draw-intent copy implementation now lives in
`src/libged/bsg_ged_draw_source.c` as
`ged_draw_scene_ref_copy_draw_intent`, and the duplicate draft/source copy
logic is removed.  Retained draft geometry, material, and primitive fallback
operations remain BSG-backed bridge internals until the wider draft/source
backend migrates.

The pivot guard now requires the draft wrapper to use those facade tokens,
requires the private header and source facade to expose
`ged_draw_scene_ref_copy_draw_intent`, and rejects direct draft-side
`bsg_scene_ref_is_null`, `bsg_scene_child_count`, `bsg_scene_child_at`,
`bsg_scene_dmode`, `bsg_scene_set_dmode`, `bsg_scene_draw_intent`,
`bsg_draw_intent_*`, `bsg_scene_append_child`, `bsg_scene_remove_child`,
`bsg_scene_ref_destroy`, and `bsg/draw_intent.h` access from returning.
Exit-criteria percentages did not change: retained scene-root/root-scope
handoffs, BSG-backed feature/polygon internals, public BSG-shaped draw
snap/scene-ref surfaces, qtcad retained draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
standalone `brlobol_pivot_guard` in 17.84 seconds,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
the focused migration gate 4/4 in 18.35 seconds, and the standard broad
focused wrapper gate 13/13 in 82.74 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 40.97 seconds, `brlobol_pivot_guard` at 17.99
seconds, `ged_test_drawing_quad` at 12.68 seconds, and
`ged_test_drawing_basic` at 7.50 seconds.

A new completed code-state batch moves GED navigation draw-intent path/mode
readback behind scene-ref facade helpers.  In
`src/libged/bsg_ged_draw_navigation.c`, `ged_draw_group_scene_ref_path` and
`ged_draw_group_scene_ref_mode` now use
`ged_draw_scene_ref_draw_intent_path` and
`ged_draw_scene_ref_draw_intent_mode` instead of including
`bsg/draw_intent.h` and reading the BSG draw-intent object directly.  The new
helpers live in `src/libged/bsg_ged_draw_source.c`, keeping the retained BSG
draw-intent path/mode access inside the scene-ref source facade until the
wider group/source API boundary migrates.  The no-intent fallback in
`ged_draw_group_scene_ref_mode` is unchanged: it still falls back to the first
shape draw mode and then to wire mode.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.90 seconds, the focused migration gate
4/4 in 18.26 seconds, and the standard broad focused wrapper gate 13/13 in
82.99 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.25
seconds, `brlobol_pivot_guard` at 17.92 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

A new completed code-state batch moves GED draw-view overlay scene-ref null
checks behind the scene-ref facade.  In
`src/libged/bsg_ged_draw_view.c`, overlay scene append and command-result
owner registration now use `ged_draw_scene_ref_is_null` rather than direct
`bsg_scene_ref_is_null`.  The retained BSG overlay registry and feature
backend remain in the draw-view context adapter until the wider draw-view
feature/overlay backend API boundary migrates.  The pivot guard now requires
the draw-view adapter to use the scene-ref null facade token and rejects
direct `bsg_scene_ref_is_null` from returning there.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.74 seconds, the focused migration gate
4/4 in 18.30 seconds, and the standard broad focused wrapper gate 13/13 in
82.86 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.98
seconds, `brlobol_pivot_guard` at 18.08 seconds,
`ged_test_drawing_quad` at 12.66 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

A new completed code-state batch moves GED binding/registry read-side
scene-ref access behind facade helpers.  In
`src/libged/bsg_ged_draw_binding.c`, retained database-source root walks,
source-data checks, database-source synchronization draw-mode readback, and
view-input/realization state checks now use `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_parent`, and `ged_draw_scene_ref_draw_mode` rather than
direct BSG null, parent, or draw-mode reads.  In
`src/libged/bsg_ged_draw_registry.c`, registry entry lookup, root discovery,
indexability checks, highlight cleanup, shape-state lookup, and public
shape/group token creation now use `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_parent`, and `ged_draw_scene_ref_is_group` rather than
direct BSG null, parent, or group-type reads.  Retained database-source
payload synchronization and draw-context owner lookup remain BSG-backed bridge
implementation details until the wider draw/source backend migrates.  The
pivot guard now requires the binding/registry read-side facade tokens and
rejects direct `bsg_scene_ref_is_null`, `bsg_scene_parent`,
`bsg_scene_dmode`, `bsg_scene_ref_type`, or `BSG_SCENE_ELEMENT_GROUP` from
returning to those wrappers.

Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.78 seconds, the focused migration gate
4/4 in 18.45 seconds, and the standard broad focused wrapper gate 13/13 in
82.98 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.99
seconds, `brlobol_pivot_guard` at 18.00 seconds,
`ged_test_drawing_quad` at 12.85 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

A new completed code-state batch moves GED navigation read-side scene-ref
access behind facade helpers.  In `src/libged/bsg_ged_draw_navigation.c`,
shape DFS traversal, first-shape discovery, shape snapshots, shape indexing,
shape-ref advancement, group-of-shape parent walking, and fallback draw-mode
selection now use `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_is_shape`, `ged_draw_scene_ref_child_count`,
`ged_draw_scene_ref_child_at`, `ged_draw_scene_ref_parent`, and
`ged_draw_scene_ref_draw_mode` rather than direct BSG null, type, child,
parent, or draw-mode reads.  The existing retained draw-intent path/mode
helpers in that file still read the BSG draw-intent object directly until the
wider group/source API boundary migrates.  The pivot guard now requires the
navigation read-side facade tokens and rejects direct
`bsg_scene_ref_is_null`, `bsg_scene_ref_type`,
`BSG_SCENE_ELEMENT_SHAPE`, `bsg_scene_parent`, `bsg_scene_child_count`,
`bsg_scene_child_at`, or `bsg_scene_dmode` from returning there.
Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.10 seconds, the focused migration gate
4/4 in 18.53 seconds, and the standard broad focused wrapper gate 13/13 in
82.76 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.97
seconds, `brlobol_pivot_guard` at 17.99 seconds,
`ged_test_drawing_quad` at 12.66 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

A new completed code-state batch moves GED node helper read-side scene-ref
access behind facade helpers.  In `src/libged/bsg_ged_draw_node.c`, the
release, detach, append, group-child ensure, nested erase, group free, and
group contents free helper paths now use `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_parent`, `ged_draw_scene_ref_child_count`, and
`ged_draw_scene_ref_child_at` rather than direct BSG null/parent/child reads.
The backend-owned mutation operations in that helper file still perform the
retained BSG release, detach, append, child creation, nested erase, and group
free operations until the wider retained group/source backend migrates.  The
pivot guard now requires the node helper read-side facade tokens and rejects
direct `bsg_scene_ref_is_null`, `bsg_scene_parent`, `bsg_scene_child_count`,
or `bsg_scene_child_at` from returning there.  Verification on FreeBSD/clang
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.11 seconds, the focused migration gate
4/4 in 18.23 seconds, and the standard broad focused wrapper gate 13/13 in
83.30 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.56
seconds, `brlobol_pivot_guard` at 17.96 seconds,
`ged_test_drawing_quad` at 12.64 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

A new completed code-state batch moves GED split-helper scene-ref null checks
behind the scene-ref facade.  In `src/libged/bsg_ged_draw_scene_root.c`,
`src/libged/bsg_ged_draw_material.c`, and
`src/libged/bsg_ged_draw_state.c`, retained scene-root reuse/creation checks,
material refresh record lookup checks, and draw-state prepare/fullpath checks
now call `ged_draw_scene_ref_is_null` instead of direct
`bsg_scene_ref_is_null`.  This keeps retained BSG payload reads and writes in
their existing backend-owning helpers while removing another set of direct
null-test leaks from split GED helper code.  The pivot guard now requires the
facade null-check token in those files and rejects direct split-helper
`bsg_scene_ref_is_null` use from returning.  Verification on FreeBSD/clang
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.06 seconds, the focused migration gate
4/4 in 18.50 seconds, and the standard broad focused wrapper gate 13/13 in
82.72 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.93
seconds, `brlobol_pivot_guard` at 17.95 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

A new completed code-state batch moves GED tree scene-ref null checks behind
the scene-ref facade.  In `src/libged/bsg_ged_draw_tree.c`, retained
draw-tree group/path helpers, scoped erase helpers, group lookup/append
helpers, clear helpers, and public group operations now call
`ged_draw_scene_ref_is_null` instead of direct `bsg_scene_ref_is_null` for
null testing.  Null-ref construction still remains intentionally BSG-shaped
where the private bridge returns `bsg_scene_ref` handles.  The pivot guard now
requires the tree facade null-check token and rejects direct tree-side
`bsg_scene_ref_is_null` use from returning.  Verification on FreeBSD/clang
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.95 seconds, the focused migration gate
4/4 in 18.23 seconds, and the standard broad focused wrapper gate 13/13 in
82.62 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.95
seconds, `brlobol_pivot_guard` at 17.92 seconds,
`ged_test_drawing_quad` at 12.65 seconds, and `ged_test_drawing_basic` at
7.50 seconds.

A new completed code-state batch moves GED transaction scene-ref null and
redraw draw-mode readback behind scene-ref facade helpers.  In
`src/libged/bsg_ged_draw_transactions.c`, transaction-side
`bsg_scene_ref_is_null` checks now call `ged_draw_scene_ref_is_null`, and the
redraw wireframe-mode gate now calls `ged_draw_scene_ref_draw_mode` rather
than reading `bsg_scene_dmode` directly.  The retained BSG-backed helper
implementations remain in `src/libged/bsg_ged_draw_source.c`, preserving the
current backend while removing another BSG readback leak from transaction
logic.  The pivot guard now requires the transaction facade tokens and rejects
direct transaction-side `bsg_scene_ref_is_null` or `bsg_scene_dmode` use from
returning.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.49 seconds, the focused migration gate
4/4 in 18.19 seconds, and the standard broad focused wrapper gate 13/13 in
82.97 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.01
seconds, `brlobol_pivot_guard` at 18.09 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

A new completed code-state batch moves GED refs scene-summary readback behind
scene-ref facade helpers.  In `src/libged/bsg_ged_draw_refs.c`, public
scene-context display/source/tree summaries, child/parent/name/subtree
readback, shape color/material summaries, shape visibility sync, and shape
view-context readback now dispatch through `ged_draw_scene_ref_*` helpers
rather than direct `bsg_scene_*` or database-source reads.  The retained
BSG-backed helper implementations live in
`src/libged/bsg_ged_draw_source.c`, with declarations in
`src/libged/bsg_ged_draw_private.h`.  BSG-backed geometry/line accessors and
broader draw-intent/database-source implementation details remain intentionally
behind the retained scene-ref helper boundary until the wider group/source API
boundary migrates.  The pivot guard now requires the new refs read-facade
tokens and rejects direct refs-side BSG scene/database-source readback from
returning.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.91 seconds, the focused migration gate
4/4 in 18.24 seconds, and the standard broad focused wrapper gate 13/13 in
82.74 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.99
seconds, `brlobol_pivot_guard` at 17.90 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

A new completed code-state batch moves GED records scene traversal and
record-field reads behind scene-ref facade helpers.  In
`src/libged/bsg_ged_draw_records.c`, parent traversal, view-scope checks,
view-context comparisons, visibility filtering, group/shape traversal,
highlight/evaluated-region reads, draw revision, transparency, line-width,
draw-center, and view-source/local-source record fields now use
`ged_draw_scene_ref_*` helpers rather than direct `bsg_scene_*` reads.  The
new retained helper implementations live in
`src/libged/bsg_ged_draw_source.c`, with declarations in
`src/libged/bsg_ged_draw_private.h`.  BSG export/database-source payload reads
remain intentionally BSG-backed until the draw-view feature/export backend
boundary migrates.  The pivot guard now requires the new records read-facade
tokens and rejects direct records-side BSG scene traversal or record-field
reads from returning.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.02 seconds, the focused migration gate
4/4 in 18.27 seconds, and the standard broad focused wrapper gate 13/13 in
82.77 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.00
seconds, `brlobol_pivot_guard` at 17.91 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

A new completed code-state batch moves GED records export/render-summary view
ownership to opaque view contexts at the public wrapper boundary.  In
`src/libged/bsg_ged_draw_records.c`,
`_ged_draw_foreach_view_export_record` now accepts `void *view_ctx`, and
`ged_draw_foreach_view_record_query`,
`ged_draw_foreach_view_db_object_record`,
`ged_draw_foreach_visible_view_db_object_record`,
`ged_draw_foreach_visible_view_db_object_record_mode`, and
`ged_draw_foreach_visible_view_record` no longer immediately cast their opaque
view input before dispatching to the retained export helper.  The public
`ged_draw_view_rendered_object_summary` entry point now dispatches through a
file-local retained helper rather than owning the immediate `struct bsg_view *`
cast itself.  The underlying BSG export request and backend-scene render
summary implementation remains intentionally BSG-backed until the draw-view
feature/export backend API boundary migrates.  The pivot guard now rejects
typed BSG export/render view ownership from returning at the public GED
records wrapper boundary.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.00 seconds, the focused migration gate
4/4 in 18.22 seconds, and the standard broad focused wrapper gate 13/13 in
82.80 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.06
seconds, `brlobol_pivot_guard` at 17.93 seconds,
`ged_test_drawing_quad` at 12.70 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

A new completed code-state batch moves GED records path/list view ownership to
opaque view contexts.  In `src/libged/bsg_ged_draw_records.c`,
`_draw_group_scene_ref_in_view`, `ged_draw_group_record_in_view`,
`_draw_path_state_ctx`, `_draw_path_list_ctx`, and `_draw_has_paths_ctx` now
carry or compare `void *view_ctx` values directly rather than storing
`struct bsg_view *` at the path-state/listing boundary.  The public
`ged_draw_path_state`, `ged_draw_list_paths`, and `ged_draw_has_paths` helpers
no longer immediately cast their opaque view input before command-visible
retained-path filtering.  Deeper view export and rendered-object summary
helpers remain BSG-backed bridge code for a later backend/export API boundary.
The pivot guard now rejects typed BSG path/list view context storage or
signatures from returning in the records wrapper.  Verification on
FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.94 seconds, the focused migration gate
4/4 in 18.32 seconds, and the standard broad focused wrapper gate 13/13 in
82.84 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.00
seconds, `brlobol_pivot_guard` at 17.93 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.59 seconds.

A new completed code-state batch moves GED tree scoped erase and clear-view
ownership to opaque view contexts.  In
`src/libged/bsg_ged_draw_tree.c`, scoped path erase, scoped prefix erase,
scoped component erase, prune, record-scan, path-prefix helper, and
`ged_draw_clear_view` helper paths now carry `void *view_ctx` rather than
`struct bsg_view *`.  The private scoped erase declarations in
`src/libged/bsg_ged_draw_private.h` now expose opaque view contexts as well,
matching the transaction callers that already passed `txn->view` as opaque
state.  The pivot guard now rejects typed BSG scoped erase/clear helper
storage or signatures from returning in the tree wrapper and private header.
Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.00 seconds, the focused migration gate
4/4 in 18.19 seconds, and the standard broad focused wrapper gate 13/13 in
82.78 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.02
seconds, `brlobol_pivot_guard` at 18.01 seconds,
`ged_test_drawing_quad` at 12.65 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

A new completed code-state batch moves GED transaction source-reexpand view
ownership to opaque view contexts.  In
`src/libged/bsg_ged_draw_transactions.c`, `ged_draw_reexpand_group_entry`,
`ged_draw_reexpand_source_ctx`, and `_ged_draw_reexpand_source_groups` now
carry `void *view_ctx` rather than `struct bsg_view *`.  Reexpand replay now
seeds `ged_draw_transaction.view` from the opaque context field, and the
fallback active-view lookup uses `ged_draw_active_view_ctx`; the file-local
typed `ged_draw_active_view` helper is retired.  The pivot guard now rejects
typed BSG reexpand transaction bookkeeping/signatures and rejects resurrection
of the local typed active-view helper, alongside the existing redraw/evaluated
transaction view-ownership checks.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.89 seconds, the focused migration gate
4/4 in 18.42 seconds, and the standard broad focused wrapper gate 13/13 in
82.79 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.92
seconds, `brlobol_pivot_guard` at 18.05 seconds,
`ged_test_drawing_quad` at 12.69 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

A new completed code-state batch moves GED transaction redraw/evaluated helper
view ownership to opaque view contexts.  In
`src/libged/bsg_ged_draw_transactions.c`, redraw context structs now store
`void *view_ctx` rather than `struct bsg_view *`, and local redraw,
wireframe-redraw, redraw-source, view-array, autoview, LoD-finalize, evaluated
shape creation, evaluated realization, evaluated draw, and draw-realized helper
signatures now accept `void *view_ctx`.  The retained active-view fallback
used by those paths now goes through `ged_draw_active_view_ctx`.  At that
stage, the typed `ged_draw_active_view` helper still remained for the separate
reexpand path; the newer source-reexpand batch above has since retired it.
The pivot guard now requires the opaque active-view helper and rejects typed
BSG redraw/evaluated transaction helper ownership from returning.
Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.90 seconds, the focused migration gate
4/4 in 18.18 seconds, and the standard broad focused wrapper gate 13/13 in
82.79 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.03
seconds, `brlobol_pivot_guard` at 17.85 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.58 seconds.

A new completed code-state batch neutralizes GED transaction draw-mode typing.
`src/libged/bsg_ged_draw_transactions.c` now uses `ged_draw_mode` and
`GED_DRAW_MODE_*` tokens for default/evaluated draw-mode decisions instead of
`bsg_draw_mode` and `BSG_DRAW_MODE_*`.  The private
`ged_draw_group_scene_ref_mode` helper now returns a neutral integer in
`src/libged/bsg_ged_draw_private.h`; its retained implementation in
`src/libged/bsg_ged_draw_navigation.c` translates retained BSG draw-intent
mode values without exposing the BSG enum type at the private header boundary.
`src/libged/bsg_ged_draw_records.c` and
`src/libged/bsg_ged_draw_tree.c` consume the neutral mode value directly.  The
pivot guard now requires the neutral transaction mode tokens and rejects
`bsg_draw_mode` or `BSG_DRAW_MODE_*` use from the transaction/private-helper
boundary.  Verification on FreeBSD/clang caught and fixed a
`-Wbad-function-cast` warning in the helper return path; final verification
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.11 seconds, the focused migration gate
4/4 in 18.35 seconds, and the standard broad focused wrapper gate 13/13 in
82.87 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.06
seconds, `brlobol_pivot_guard` at 17.94 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.56 seconds.

A new completed code-state batch moves GED transaction retained draw-intent
revalidation and group appearance snapshots behind scene-ref facade helpers.
`src/libged/bsg_ged_draw_transactions.c` now calls
`ged_draw_scene_ref_revalidate_draw_intent` for root and per-view DB-event
revalidation and `ged_draw_scene_ref_draw_intent_appearance` when preserving
group appearance for reexpand.  The retained BSG-backed helper implementations
live in `src/libged/bsg_ged_draw_source.c`, with declarations in
`src/libged/bsg_ged_draw_private.h`.  The pivot guard now requires both facade
tokens from the transaction wrapper and rejects direct
`bsg_scene_draw_intent_revalidate`, `bsg_scene_draw_intent`, or
`bsg_draw_intent_appearance` use from
`src/libged/bsg_ged_draw_transactions.c`.  Verification on FreeBSD/clang
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.88 seconds, the focused migration gate
4/4 in 18.53 seconds, and the standard broad focused wrapper gate 13/13 in
83.18 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.01
seconds, `brlobol_pivot_guard` at 17.97 seconds,
`ged_test_drawing_quad` at 13.00 seconds, and `ged_test_drawing_basic` at
7.61 seconds.

A new completed code-state batch moves retained draw-root group allocation in
`src/libged/bsg_ged_draw_scene_root.c` behind the view-context facade.  Scene
root creation now calls `ged_draw_view_context_group_create`, eliminating the
local active-view cast and direct `bsg_scene_group_create` call from the
scene-root wrapper.  The retained BSG-backed implementation lives in
`src/libged/bsg_ged_draw_node.c`, and the pivot guard now requires the facade
token from `src/libged/bsg_ged_draw_scene_root.c` while rejecting direct
`bsg/scene_builder.h`, `bsg_scene_group_create`, or local `view_ctx` to
`struct bsg_view *` root-group creation access from that file.  Verification
on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.85 seconds, the focused migration gate
4/4 in 18.20 seconds, and the standard broad focused wrapper gate 13/13 in
82.63 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.97
seconds, `brlobol_pivot_guard` at 17.86 seconds,
`ged_test_drawing_quad` at 12.68 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

A new completed code-state batch moves GED transaction redraw traversal
scene-ref reads behind facade helpers.  `_ged_draw_redraw_scene_ref` in
`src/libged/bsg_ged_draw_transactions.c` now uses
`ged_draw_scene_ref_is_shape`, `ged_draw_scene_ref_line_style`,
`ged_draw_scene_ref_child_count`, and `ged_draw_scene_ref_child_at`; the
transaction root child-count snapshot also uses `ged_draw_scene_ref_child_count`.
The retained implementation for the new `ged_draw_scene_ref_is_shape` helper
lives beside `ged_draw_scene_ref_is_group` in
`src/libged/bsg_ged_draw_source.c`.  The pivot guard now requires those facade
tokens from the transaction wrapper and rejects direct
`bsg_scene_ref_type`, `BSG_SCENE_ELEMENT_SHAPE`, `bsg_scene_line_style`,
`bsg_scene_child_count`, or `bsg_scene_child_at` use from
`src/libged/bsg_ged_draw_transactions.c`.  Verification on FreeBSD/clang
passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.82 seconds, the focused migration gate
4/4 in 18.36 seconds, and the standard broad focused wrapper gate 13/13 in
82.87 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.97
seconds, `brlobol_pivot_guard` at 18.00 seconds,
`ged_test_drawing_quad` at 12.78 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

A recent completed code-state batch moves the remaining GED tree subtree-bounds
and draw-tree-depth reads behind scene-ref facade helpers.
`src/libged/bsg_ged_draw_tree.c` now calls
`ged_draw_scene_ref_subtree_bounds` for `_sg_bounding_sph` and
`ged_draw_scene_ref_draw_tree_depth` when appending a shape under nested
draw-path groups.  The retained implementations live in
`src/libged/bsg_ged_draw_source.c`, which now includes `bsg/draw_set.h` for
the BSG-backed calls.  The pivot guard now requires the helper tokens and
rejects direct `bsg_scene_subtree_bbox` or `bsg_scene_draw_tree_depth` use
from the tree wrapper.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.84 seconds, the focused migration gate
4/4 in 18.18 seconds, and the standard broad focused wrapper gate 13/13 in
82.73 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.95
seconds, `brlobol_pivot_guard` at 17.89 seconds,
`ged_test_drawing_quad` at 12.74 seconds, and `ged_test_drawing_basic` at
7.56 seconds.

A recent completed code-state batch moves GED tree retained child/name/type reads
behind scene-ref facade helpers.  `src/libged/bsg_ged_draw_tree.c` now uses
`ged_draw_scene_ref_child_count`, `ged_draw_scene_ref_child_at`,
`ged_draw_scene_ref_name`, and the new `ged_draw_scene_ref_is_group` helper
for tree snapshotting, empty-group checks, last-group append lookup, group name
fallbacks, and erase-by-name recursion.  The retained implementation remains
in `src/libged/bsg_ged_draw_source.c`, and the pivot guard now rejects direct
`bsg_scene_child_count`, `bsg_scene_child_at`, `bsg_scene_name`,
`bsg_scene_ref_type`, or `BSG_SCENE_ELEMENT_GROUP` use from the tree wrapper.
Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.89 seconds, the focused migration gate
4/4 in 18.31 seconds, and the standard broad focused wrapper gate 13/13 in
82.52 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.92
seconds, `brlobol_pivot_guard` at 17.86 seconds,
`ged_test_drawing_quad` at 12.65 seconds, and `ged_test_drawing_basic` at
7.49 seconds.

A recent completed code-state batch moves GED tree retained nested subpath erase
behind a scene-ref facade.  `src/libged/bsg_ged_draw_tree.c` now calls
`ged_draw_scene_ref_erase_nested_subpath` while keeping GED-owned
`db_full_path` name extraction and path-match callback logic local to the tree
wrapper.  The retained BSG implementation lives in
`src/libged/bsg_ged_draw_node.c` and delegates to
`bsg_scene_erase_nested_subpath`, preserving its structural revision and bbox
invalidation behavior.  The pivot guard now requires the facade token and
rejects direct `bsg_scene_erase_nested_subpath` use from the tree wrapper.
Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 18.03 seconds, the focused migration gate
4/4 in 18.16 seconds, and the standard broad focused wrapper gate 13/13 in
82.62 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.90
seconds, `brlobol_pivot_guard` at 17.90 seconds,
`ged_test_drawing_quad` at 12.69 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

A recent completed code-state batch moves GED tree retained child group
find/create and parent revision bumping behind a view-context facade.
`src/libged/bsg_ged_draw_tree.c` now calls
`ged_draw_view_context_group_child_ensure` with the opaque active view context
instead of locally casting to `struct bsg_view *` and calling
`bsg_scene_group_find_child`, `bsg_scene_group_ensure_child`, and
`bsg_scene_bump_rev`.  The retained implementation lives in
`src/libged/bsg_ged_draw_node.c` beside the other scene-ref ownership helpers,
and preserves the existing behavior of returning an already-present child
before requiring a view context for allocation.  The pivot guard now requires
the helper token and rejects the retired direct tree calls and local active-view
cast helper.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.85 seconds, the focused migration gate
4/4 in 18.10 seconds, and the standard broad focused wrapper gate 13/13 in
82.68 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 40.94
seconds, `brlobol_pivot_guard` at 17.87 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.58 seconds.

A recent completed code-state batch moves GED tree/state semantic draw-intent
access through scene-ref facade helpers.  `src/libged/bsg_ged_draw_tree.c`
now routes group path, mode, appearance, and overlay-intent checks through
`ged_draw_scene_ref_set_draw_intent_path`,
`ged_draw_scene_ref_set_draw_intent_mode`,
`ged_draw_scene_ref_set_draw_intent_appearance`, and
`ged_draw_scene_ref_draw_intent_is_overlay`; `src/libged/bsg_ged_draw_state.c`
uses the same path helper for semantic shape path updates.  The retained BSG
implementation lives with `ged_draw_scene_ref_ensure_draw_intent` in
`src/libged/bsg_ged_draw_source.c`, and the pivot guard now rejects direct
`bsg_scene_draw_intent`, `bsg_draw_intent_set_*`, `bsg_draw_intent_is_overlay`,
draw-intent creation, or `bsg/draw_intent.h` access from the tree/state
wrappers.  Verification on FreeBSD/clang passed:
`ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.86 seconds, the focused migration gate
4/4 in 18.30 seconds, and the standard broad focused wrapper gate 13/13 in
82.77 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.00
seconds, `brlobol_pivot_guard` at 17.82 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.64 seconds.

A recent completed code-state batch moves GED tree child attachment through a
scene-ref facade.  `src/libged/bsg_ged_draw_tree.c` now routes shape
attachment append plus retained bbox invalidation through
`ged_draw_scene_ref_append_child`, implemented in
`src/libged/bsg_ged_draw_node.c` beside the other retained scene-ref ownership
helpers.  The pivot guard now requires that facade token and rejects direct
`bsg_scene_append_child` or `bsg_scene_bbox_invalidate` use from the tree
wrapper.  The selected direct scene mutation scan across the migrated wrapper
files is empty for the guarded set of direct scene setters, detach/release,
group-free, append, invalidation, and draw-intent construction calls.
Verification on FreeBSD/clang passed: `ninja -C build libged.so`,
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` in 17.85 seconds, the focused migration gate
4/4 in 18.15 seconds, and the standard broad focused wrapper gate 13/13 in
83.10 seconds.  Longest broad-gate members were `libbsg_api_hygiene` at 41.39
seconds, `brlobol_pivot_guard` at 17.86 seconds,
`ged_test_drawing_quad` at 12.74 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

A recent completed code-state batch retires the typed private
`ged_draw_shape_draft_create` export.  External draft creation now routes
through `ged_draw_shape_draft_create_context`, while the retained BSG draft
constructor remains file-local in `src/libged/bsg_ged_draw_draft.c`.
`ged_draw_shape_draft_publish_primitive_wireframe` now takes an opaque
`view_ctx`, and `ged_draw_shape_ref_publish_primitive_wireframe` no longer
casts that context before collecting neutral RT view-info snapshots.
Remaining work is still the wider public BSG-shaped GED draw snap/scene-ref
API surface, retained scene-ref/root/scope handoffs, retained
group/overlay/redraw BSG view use, BSG-backed feature/polygon internals behind
context helpers, qtcad retained draw transaction/observer ownership, and the
guarded direct `libbsg` rows in `libdm` and `libged`.
The verification baseline for this latest batch passed on FreeBSD/clang:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`,
standalone `brlobol_pivot_guard` passed in 17.75 seconds, the focused
migration gate passed 4/4 in 18.10 seconds, and the standard broad focused
wrapper gate passed 13/13 in 83.03 seconds.  Longest broad-gate members were
`libbsg_api_hygiene` at 41.16 seconds, `brlobol_pivot_guard` at 17.98 seconds,
`ged_test_drawing_quad` at 12.77 seconds, and `ged_test_drawing_basic` at
7.53 seconds.

The prior completed code-state batch retired the private GED mesh-LoD load
pass-through.  `src/libged/draw.cpp` converts retained `bsg_scene_ref` values
through `ged_draw_scene_ref_to_rt_view_ref` and calls neutral
`rt_mesh_lod_load_view_scene_ref` directly for BoT and BRep LoD realization.
`src/libged/ged_view_legacy.cpp` no longer implements
`ged_draw_mesh_lod_load_view_scene_ref_context`, and
`src/libged/bsg_ged_draw_view_private.h` no longer declares it.

An older completed code-state batch made GED private active-view storage
opaque.  `src/libged/ged_private.h` stores `ged_impl::ged_gvp` as `void *`,
and `src/libged/ged_view_legacy.cpp` assigns and returns that opaque context
directly for default-view install, active-view lookup, and active-view
replacement.  The pivot guard requires the opaque private storage token and
rejects typed `struct bsg_view *ged_gvp` storage or typed active-view casts
from returning there.

An older completed code-state batch moved GED retained view-set
initialization, teardown, and name lookup from direct retained BSG calls in
`src/libged/ged_view_legacy.cpp` to the neutral opaque
`rt_view_set_context_init`, `rt_view_set_context_free`, and
`rt_view_set_context_find_view` helpers declared in `include/rt/view.h`.
The BSG-backed implementations remain in `src/librt/view_legacy_bsg.c`, but
the GED adapter no longer calls `rt_view_set_init_bsg`,
`rt_view_set_free_bsg`, or `rt_view_set_find_view_bsg` directly.

An older completed code-state batch moved GED LoD bounds callback
installation from a direct retained BSG setter in
`src/libged/ged_view_legacy.cpp` to the neutral opaque
`rt_view_context_lod_bounds_callback_set` helper declared in
`include/rt/view.h`.  The BSG-backed implementation remains in
`src/librt/view_legacy_bsg.c`, but the GED adapter no longer casts the opaque
view context to `struct bsg_view *` for this path.

The prior completed code-state batch moved the private GED mesh-LoD load
helper to neutral scene-ref input.  `ged_draw_mesh_lod_load_view_scene_ref_context`
now takes `rt_view_scene_ref`, `src/libged/ged_view_legacy.cpp` passes that
ref directly to `rt_mesh_lod_load_view_scene_ref`, and `src/libged/draw.cpp`
performs retained-to-neutral conversion through `ged_draw_scene_ref_to_rt_view_ref`
before calling the helper.  The pivot guard rejects the old
`bsg_scene_ref visibility_ref` helper signature and rejects retained-to-neutral
scene-ref conversion from returning to `ged_view_legacy.cpp`.

The GED draw LoD bounds callback cleanup retired the remaining private typed
GED LoD callback helpers.  `src/libged/bsg_ged_draw_transactions.c` now calls
`ged_draw_view_context_lod_bounds_callback_set` while preparing transaction
views, and the follow-up opaque-setter cleanup now has
`src/libged/ged_view_legacy.cpp` call neutral
`rt_view_context_lod_bounds_callback_set` behind that opaque context helper.
The retained `rt_view_lod_bounds_callback_set_bsg` implementation is confined
to `src/librt/view_legacy_bsg.c`.
`src/libged/bsg_ged_draw_view_private.h` no longer declares
`struct bsg_view`, `struct bsg_view_set`,
`ged_draw_view_set_lod_bounds_update`, or
`ged_draw_view_has_lod_bounds_update`.  The pivot guard now requires the
context helpers and rejects the retired typed helper names from the private
header and the direct BSG callback setter from the GED legacy-view adapter.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.62 seconds.  The focused
migration gate completed 4/4 passing in 18.23 seconds:
`brlobol_pivot_guard` 17.94 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`test_qtcad_obol_draw_sync` 0.11 seconds, and `ged_test_qged_swrast` 0.11
seconds.  The broad focused wrapper gate completed 13/13 passing in 82.65
seconds; longest members were `libbsg_api_hygiene` at 41.02 seconds,
`brlobol_pivot_guard` at 17.89 seconds, `ged_test_drawing_quad` at 12.66
seconds, and `ged_test_drawing_basic` at 7.50 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.78 seconds.

The GED draw selection callback cleanup retired the private typed selection
path callback layer.  `src/libged/bsg_ged_draw_view.c` now implements
`ged_draw_view_context_selection_path_foreach` directly against retained BSG
selection storage and passes the original opaque `view_ctx` to the public
`ged_draw_view_context_selection_path_cb`.  The private
`ged_draw_view_selection_path_cb` typedef is gone from
`src/libged/bsg_ged_draw_view_private.h`, and the private typed iterator and
thunk are gone from the BSG-backed draw-view implementation.  The pivot guard
now rejects those retired typed callback symbols from the private header and
implementation.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.75 seconds.  The focused
migration gate completed 4/4 passing in 17.93 seconds:
`brlobol_pivot_guard` 17.64 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`test_qtcad_obol_draw_sync` 0.11 seconds, and `ged_test_qged_swrast` 0.11
seconds.  The broad focused wrapper gate completed 13/13 passing in 82.27
seconds; longest members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 17.61 seconds, `ged_test_drawing_quad` at 12.65
seconds, and `ged_test_drawing_basic` at 7.51 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.93 seconds.

The GED draw axes-state accessor cleanup retired the typed private axes-state
accessor trampolines.  `src/libged/bsg_ged_draw_view.c` now implements
`ged_draw_view_context_axes_state_get` and
`ged_draw_view_context_axes_state_replace` directly against the retained BSG
axes feature implementation.  The typed private
`ged_draw_view_axes_state_get` and `ged_draw_view_axes_state_replace`
declarations are gone from `src/libged/bsg_ged_draw_view_private.h`, and
their exported definitions are gone from the BSG-backed draw-view
implementation.

This is deliberately scoped to axes-state accessors that already had
context-shaped callers.  Axes creation, polygon, and retained shape
creation/replacement helpers still consume `struct bsg_view *` locally
because the underlying draw-view feature backend is still BSG-owned.  Those
remaining ownership points need the wider draw-view feature API migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.65 seconds.  The focused
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.98 seconds.  `brlobol_pivot_guard` completed in
17.69 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.59 seconds.  The longest member was
`libbsg_api_hygiene` at 41.03 seconds; `brlobol_pivot_guard` completed in
17.76 seconds, `ged_test_drawing_quad` in 12.67 seconds,
`ged_test_drawing_basic` in 7.53 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.67 seconds.

The GED draw label accessor cleanup retired the typed private label accessor
trampolines.  `src/libged/bsg_ged_draw_view.c` now implements
`ged_draw_view_context_label_count`, `ged_draw_view_context_label_copy`, and
`ged_draw_view_context_label_point_set` directly against the retained BSG
feature implementation.  The typed private `ged_draw_view_label_count`,
`ged_draw_view_label_copy`, and `ged_draw_view_label_point_set` declarations
are gone from `src/libged/bsg_ged_draw_view_private.h`, and their exported
definitions are gone from the BSG-backed draw-view implementation.

This is deliberately scoped to label accessors that already had
context-shaped callers.  Label creation/replacement, line/arrow creation,
axes, polygon, and retained shape creation/replacement helpers still consume
`struct bsg_view *` locally because the underlying draw-view feature backend
is still BSG-owned.  Those remaining ownership points need the wider
draw-view feature API migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.89 seconds.  The focused
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.89 seconds.  `brlobol_pivot_guard` completed in
17.60 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.32 seconds.  The longest member was
`libbsg_api_hygiene` at 40.92 seconds; `brlobol_pivot_guard` completed in
17.61 seconds, `ged_test_drawing_quad` in 12.66 seconds,
`ged_test_drawing_basic` in 7.55 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.66 seconds.

The GED draw line/arrow accessor cleanup retired the typed private line and
arrow accessor trampolines.  `src/libged/bsg_ged_draw_view.c` now implements
`ged_draw_view_context_line_style_get`,
`ged_draw_view_context_line_color_set`,
`ged_draw_view_context_line_width_set`,
`ged_draw_view_context_lines_points_copy`,
`ged_draw_view_context_arrow_tip_get`, and
`ged_draw_view_context_arrow_tip_set` directly against the retained BSG
feature implementation.  The typed private
`ged_draw_view_line_style_get`, `ged_draw_view_line_color_set`,
`ged_draw_view_line_width_set`, `ged_draw_view_lines_points_copy`,
`ged_draw_view_arrow_tip_get`, and `ged_draw_view_arrow_tip_set` declarations
are gone from `src/libged/bsg_ged_draw_view_private.h`, and their exported
definitions are gone from the BSG-backed draw-view implementation.

This is deliberately scoped to line and arrow accessors that already had
context-shaped callers.  Line, arrow, label, axes, polygon, and retained
shape creation/replacement helpers still consume `struct bsg_view *` locally
because the underlying draw-view feature backend is still BSG-owned.  Those
remaining ownership points need the wider draw-view feature API migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.94 seconds.  The focused
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 18.28 seconds.  `brlobol_pivot_guard` completed in
17.99 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.58 seconds.  The longest member was
`libbsg_api_hygiene` at 40.90 seconds; `brlobol_pivot_guard` completed in
17.75 seconds, `ged_test_drawing_quad` in 12.78 seconds,
`ged_test_drawing_basic` in 7.56 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.78 seconds.

The GED draw feature-accessor cleanup retired the typed private generic
feature helper trampolines.  `src/libged/bsg_ged_draw_view.c` now implements
the context-shaped feature existence, remove, prefix remove, visible,
visible-set, depth, depth-foreach, style-get, style-apply, realize,
points-copy, and axes-centers-copy functions directly against the retained BSG
feature implementation.  The typed private
`ged_draw_view_feature_exists`, `ged_draw_view_feature_remove`,
`ged_draw_view_features_remove_prefix`, `ged_draw_view_feature_visible`,
`ged_draw_view_feature_visible_set`, `ged_draw_view_feature_depth`,
`ged_draw_view_feature_depth_foreach`, `ged_draw_view_feature_style_get`,
`ged_draw_view_feature_style_apply`, `ged_draw_view_feature_realize`,
`ged_draw_view_feature_points_copy`, and
`ged_draw_view_feature_axes_centers_copy` declarations are gone from
`src/libged/bsg_ged_draw_view_private.h`, and their exported definitions are
gone from the BSG-backed draw-view implementation.

This is deliberately scoped to generic feature accessors that already had
context-shaped callers.  Shape creation/replacement, line/label/axes/arrow,
polygon, overlay geometry, and other retained feature construction helpers
still consume `struct bsg_view *` locally because the underlying draw-view
feature backend is still BSG-owned.  Those remaining ownership points need the
wider draw-view feature API migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.63 seconds.  The focused
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.93 seconds.  `brlobol_pivot_guard` completed in
17.64 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.73 seconds.  The longest member was
`libbsg_api_hygiene` at 41.08 seconds; `brlobol_pivot_guard` completed in
17.83 seconds, `ged_test_drawing_quad` in 12.69 seconds,
`ged_test_drawing_basic` in 7.54 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.72 seconds.

The GED draw overlay-context cleanup retired the typed private overlay
registry helpers.  `src/libged/bsg_ged_draw_overlay.c` now calls
`ged_draw_view_context_overlay_scene_find`,
`ged_draw_view_context_overlay_name_erase`, and
`ged_draw_view_context_overlay_scene_append`, while
`src/libged/bsg_ged_draw_view.c` owns those context helpers directly against
the retained BSG overlay implementation.  The typed private
`ged_draw_view_overlay_scene_find`, `ged_draw_view_overlay_name_erase`,
`ged_draw_view_overlay_scene_append`, and `ged_draw_view_overlay_create`
declarations are gone from `src/libged/bsg_ged_draw_view_private.h`, and the
typed implementations are gone from the BSG-backed draw-view implementation.

This is deliberately scoped to overlay registry access.  Overlay geometry
creation and bounds updates still consume a retained `struct bsg_view *`
locally because the underlying line-set, point-set, indexed-face-set, and
bounds APIs are still BSG-owned.  Those remaining ownership points need the
wider draw-view feature API migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
An initial standalone `brlobol_pivot_guard` run after the code edit passed in
21.85 seconds.  The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.89 seconds.  `brlobol_pivot_guard` completed in
17.60 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.77 seconds.  The longest member was
`libbsg_api_hygiene` at 40.94 seconds; `brlobol_pivot_guard` completed in
17.83 seconds, `ged_test_drawing_quad` in 12.92 seconds,
`ged_test_drawing_basic` in 7.50 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.69 seconds.

The GED draw active-view cleanup retired the private
`ged_draw_view_active_bsg` trampoline.  `src/libged/bsg_ged_draw_scene_root.c`,
`src/libged/bsg_ged_draw_tree.c`,
`src/libged/bsg_ged_draw_transactions.c`, and
`src/libged/bsg_ged_draw_overlay.c` now read the active view through neutral
`ged_view_active_ctx`, keeping the retained `struct bsg_view *` casts local
to the BSG-backed implementation files that still call retained group,
overlay, redraw, or scene-root APIs.  The private declaration in
`src/libged/bsg_ged_draw_view_private.h` and central implementation in
`src/libged/ged_view_legacy.cpp` are gone.

This is deliberately scoped to the private active-view read helper.  Retained
active-view use is still real bridge work where BSG group creation, overlay
creation, transaction redraw/finalize, and root attachment still consume
`struct bsg_view *`.  Those remaining ownership points need the wider
scene-root/draw-view API migration rather than another cross-file trampoline.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first `libged.so` rebuild exposed a unity-build static helper name
collision between the edited scene-root and tree files; after giving the
helpers file-specific names, the rebuild passed.  The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.83 seconds.  `brlobol_pivot_guard` completed in
17.54 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.60 seconds.  The longest member was
`libbsg_api_hygiene` at 41.16 seconds; `brlobol_pivot_guard` completed in
17.63 seconds, `ged_test_drawing_quad` in 12.69 seconds,
`ged_test_drawing_basic` in 7.53 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.75 seconds.

The GED draw mesh-LoD context-load cleanup retired the typed private
`ged_draw_mesh_lod_load_view_scene_ref` wrapper.  `src/libged/draw.cpp`
already used the context-shaped
`ged_draw_mesh_lod_load_view_scene_ref_context` helper for retained
scene-ref mesh-LoD loading, so this batch removed the typed private
`struct bsg_view *` trampoline from `src/libged/ged_view_legacy.cpp` and
`src/libged/bsg_ged_draw_view_private.h`.  The remaining context helper now
converts the retained `bsg_scene_ref` to `rt_view_scene_ref` and calls neutral
`rt_mesh_lod_load_view_scene_ref` directly with the opaque view context.

This is deliberately scoped to the private view-typed load wrapper.  The
retained public draw scene-ref handoff is still BSG-shaped, and the mesh-LoD
load context helper remains a bridge until that wider draw scene-ref API
surface is replaced.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
An initial standalone `brlobol_pivot_guard` run after the code edit passed in
22.60 seconds.  The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.86 seconds.  `brlobol_pivot_guard` completed in
17.58 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.40 seconds.  The longest member was
`libbsg_api_hygiene` at 40.92 seconds; `brlobol_pivot_guard` completed in
17.60 seconds, `ged_test_drawing_quad` in 12.73 seconds,
`ged_test_drawing_basic` in 7.54 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  A standalone `brlobol_pivot_guard` rerun after the status and
inventory updates passed in 17.7 seconds.

The GED draw transaction scene-root-ref cleanup retired the typed private
`ged_draw_view_scene_root_ref` wrapper and the unused typed
`ged_draw_view_has_scene_root` helper.  `src/libged/bsg_ged_draw_transactions.c`
now reads per-view scene roots through
`ged_draw_view_context_scene_root_ref` during DB-event revalidation, and
`src/libged/bsg_ged_draw_scene_root.c` shares that context-shaped helper with
the public opaque `ged_draw_view_context_scene_root` facade.

This is deliberately scoped to view-root reads.  Scene-root active-view group
creation and retained root attachment still consume `struct bsg_view *` and
need the wider scene-root/draw-view API migration; this batch only removes a
typed private accessor from transaction code.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first `libged.so` rebuild exposed a FreeBSD/clang
`-Wmisleading-indentation` failure in the edited DB-event revalidation block;
after fixing that indentation, the rebuild passed.  The focused migration
gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.87 seconds.  `brlobol_pivot_guard` completed in
17.58 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.43 seconds.  The longest member was
`libbsg_api_hygiene` at 41.01 seconds; `brlobol_pivot_guard` completed in
17.67 seconds, `ged_test_drawing_quad` in 12.64 seconds,
`ged_test_drawing_basic` in 7.54 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A final standalone `brlobol_pivot_guard` rerun after the
status and inventory updates passed in 17.88 seconds.

The GED draw scene-root view-set/recycle-pool cleanup retired
`ged_draw_view_set_views_for_ged_bsg` and
`ged_draw_view_set_recycle_pool_for_ged_bsg`.  `src/libged/bsg_ged_draw_scene_root.c`
now iterates GED views with `ged_view_set_views_ctx` and seeds the retained
draw context's recycle storage with
`rt_view_set_context_recycle_pool(ged_view_set_ctx(...))`.  The private
declarations in `src/libged/bsg_ged_draw_view_private.h` and central delegates
in `src/libged/ged_view_legacy.cpp` are gone.

This is deliberately scoped to scene-root view-set access and recycle-pool
lookup.  Scene-root active-view group creation and retained root attachment
still consume `struct bsg_view *` and need the wider scene-root/draw-view API
migration; this batch avoids replacing those remaining ownership points with
local casts.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 18.02 seconds.  `brlobol_pivot_guard` completed in
17.73 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.59 seconds.  The longest member was
`libbsg_api_hygiene` at 41.02 seconds; `brlobol_pivot_guard` completed in
17.76 seconds, `ged_test_drawing_quad` in 12.70 seconds,
`ged_test_drawing_basic` in 7.50 seconds, and `ged_test_ged_draw_scene` in
1.43 seconds.  A final standalone `brlobol_pivot_guard` rerun after the
status and inventory updates passed in 17.77 seconds.

The GED draw transaction active-set/view-set cleanup retired
`ged_draw_view_active_set_bsg` and removed transaction use of
`ged_draw_view_set_views_for_ged_bsg`.  `src/libged/bsg_ged_draw_transactions.c`
now saves/restores the active view with `ged_view_active_ctx_set` and obtains
GED-owned view tables with `ged_view_set_views_ctx` in DB-event revalidation
and transaction view-array loops.  The private declaration in
`src/libged/bsg_ged_draw_view_private.h` and central delegate in
`src/libged/ged_view_legacy.cpp` for the active-set wrapper are gone.

This is deliberately scoped to transaction active-view save/restore and
view-set iteration.  Transaction active-view fallbacks, scene-root refs, and
retained `struct bsg_view *` handoffs still need the wider scene-root/draw-view
API migration; replacing those locally would only hide retained-scene
ownership behind casts.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 18.07 seconds.  `brlobol_pivot_guard` completed in
17.78 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.12 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.35 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
17.59 seconds, `ged_test_drawing_quad` in 12.69 seconds,
`ged_test_drawing_basic` in 7.50 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A final standalone `brlobol_pivot_guard` rerun after the
status and inventory updates passed in 17.66 seconds.

The GED draw tree independent-scope cleanup retired the private
`ged_draw_view_independent_scope_ref_bsg` wrapper.  `src/libged/bsg_ged_draw_tree.c`
now gets independent-scope scene refs through
`ged_draw_view_context_independent_scope_ref`, and its clear loop iterates the
GED view table with `ged_view_set_views_ctx` instead of the private
`ged_draw_view_set_views_for_ged_bsg` helper.  The private declaration in
`src/libged/bsg_ged_draw_view_private.h` and central implementation in
`src/libged/ged_view_legacy.cpp` are now context-shaped; the BSG-view spelling
is rejected by the pivot guard.

This is deliberately scoped to independent-scope ref access.  Tree group
creation still needs a retained `struct bsg_view *` because the current BSG
scene builder APIs create groups against a retained view.  Moving that path
requires a broader scene-root/group-creation API migration, not a local cast.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.91 seconds.  `brlobol_pivot_guard` completed in
17.62 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.12 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.46 seconds.  The longest member was
`libbsg_api_hygiene` at 41.03 seconds; `brlobol_pivot_guard` completed in
17.59 seconds, `ged_test_drawing_quad` in 12.75 seconds,
`ged_test_drawing_basic` in 7.52 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  A final standalone `brlobol_pivot_guard` rerun after the
status and inventory updates passed in 17.63 seconds.

The GED draw-record selection-boundary cleanup moved selected/highlighted
shape-ref selection paths off private `struct bsg_view *` selection helpers.
`src/libged/bsg_ged_draw_records.c` now uses `ged_view_active_ctx` and private
`ged_draw_view_context_selection_*` helpers for selection availability,
selected-path lookup, highlighted-ref replacement, and selected-shape
insertion.  The obsolete private `ged_draw_view_selection_add_shape_ref`
entry point was removed from `src/libged/bsg_ged_draw_private.h`, and
`src/libged/bsg_ged_draw_view_private.h` now exposes only context-shaped
selection helpers for the record boundary.

`src/libged/bsg_ged_draw_view.c` still owns the retained BSG selection
mechanics, but its BSG-view selection operations are now file-local helpers
behind the context wrappers.  This keeps the retained implementation honest
while shrinking the private BSG-view surface visible to other draw modules.
The pivot guard requires the context-shaped record path, requires the bridge
to keep the BSG mechanics contained locally, and rejects the retired
BSG-view selection calls from returning to the record implementation or
private headers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.79 seconds.  `brlobol_pivot_guard` completed in
17.50 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.68 seconds.  The longest member was
`libbsg_api_hygiene` at 41.15 seconds; `brlobol_pivot_guard` completed in
17.72 seconds, `ged_test_drawing_quad` in 12.68 seconds,
`ged_test_drawing_basic` in 7.54 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  A final standalone `brlobol_pivot_guard` rerun after the
status and inventory updates passed in 17.64 seconds.

The dead private GED draw-view wrapper cleanup removed unused compatibility
declarations and delegates from the retained draw bridge.  The BSG-suffixed
private helpers for view info/name/perspective/scale/OBB snapshots, LoD
policy get/apply, default autoview, generic view-set passthroughs, and
independent-view boolean checks are gone from
`src/libged/bsg_ged_draw_view_private.h` and
`src/libged/ged_view_legacy.cpp`.  Their former call sites had already moved
to neutral public GED/RT context helpers, so this batch removes dead surface
area rather than changing runtime behavior.

The pivot guard now rejects these retired private wrappers from returning to
the private GED draw-view header or central adapter.  The current residual
wrapper scan is correspondingly smaller: it still reports retained active-view
and GED-owned view-set helpers, independent-scope/scene-root scene-ref
helpers, the mesh-LoD load handoff, central active retained bridge
definitions, and BSG-internal selection/feature/overlay/polygon conversions.
Those remaining pieces are real retained bridge ownership and still need a
neutral API boundary or public draw-view API migration before libbsg
retirement.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first short gate exposed a stale pivot-guard view-scope requirement list
that still expected `ged_draw_view_is_independent_bsg`,
`ged_draw_view_set_views_bsg`, `ged_draw_view_set_recycle_pool_bsg`, and
`rt_view_is_independent_bsg`; after converting that guard list to rejection
coverage, the short migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.94 seconds.  `brlobol_pivot_guard` completed in
17.65 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.36 seconds.  The longest member was
`libbsg_api_hygiene` at 40.96 seconds; `brlobol_pivot_guard` completed in
17.58 seconds, `ged_test_drawing_quad` in 12.69 seconds,
`ged_test_drawing_basic` in 7.55 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.

The GED draw refs mesh-LoD free-path cleanup retired one more private
BSG-shaped GED draw-view wrapper.  `src/libged/bsg_ged_draw_refs.c` now frees
retained mesh-LoD scene-ref state in `ged_draw_shape_ref_reset_node` by
converting the retained `bsg_scene_ref` to a neutral `rt_view_scene_ref` and
calling `rt_mesh_lod_free_scene_ref` directly.  The private
`ged_draw_mesh_lod_free_scene_ref` declaration and implementation were removed
from `src/libged/bsg_ged_draw_view_private.h` and
`src/libged/ged_view_legacy.cpp`.

The pivot guard now requires the neutral `rt_mesh_lod_free_scene_ref` path in
the refs implementation, rejects `ged_draw_mesh_lod_free_scene_ref` from
returning to refs, the private GED draw-view header, or
`ged_view_legacy.cpp`, and no longer requires the central adapter to own that
free path.  The retained mesh-LoD load path still stays behind
`ged_draw_mesh_lod_load_view_scene_ref_context` while the public
BSG-shaped scene-ref handoff surface exists.  This does not change migration
percentages: the retained mesh-LoD load handoff, public draw snap/scene-ref
bridge surface, retained qtcad draw transaction/observer ownership, and lower
libged/libdm dependency rows still block libbsg retirement.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first short gate exposed a stale pivot-guard requirement that still
expected `rt_mesh_lod_free_scene_ref` in `src/libged/ged_view_legacy.cpp`;
after moving that requirement to the refs implementation, the short migration
gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.74 seconds.  `brlobol_pivot_guard` completed in
17.45 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.51 seconds.  The longest member was
`libbsg_api_hygiene` at 41.01 seconds; `brlobol_pivot_guard` completed in
17.59 seconds, `ged_test_drawing_quad` in 12.77 seconds,
`ged_test_drawing_basic` in 7.53 seconds, and `ged_test_ged_draw_scene` in
1.44 seconds.

The GED draw tree/transaction/record independent-check cleanup moved retained
draw boolean independent-view checks off the private BSG-suffixed GED
draw-view wrapper.  `src/libged/bsg_ged_draw_tree.c`,
`src/libged/bsg_ged_draw_transactions.c`, and
`src/libged/bsg_ged_draw_records.c` now use
`rt_view_context_is_independent` for independent-view tests.  The tree source
still uses `ged_draw_view_independent_scope_ref_bsg` where it needs retained
BSG scene refs for scoped draw roots, so this batch only neutralizes the
boolean checks and leaves scene-ref ownership in the retained bridge.

The pivot guard now requires `rt_view_context_is_independent` in the
tree/transaction/record sources and rejects
`ged_draw_view_is_independent_bsg` from returning there.  The central
compatibility declaration and delegate remain in the private GED draw-view
bridge and `src/libged/ged_view_legacy.cpp` while the wider public BSG-shaped
draw API surface still exists.  This does not change migration percentages:
the public draw snap/scene-ref bridge surface, retained qtcad draw
transaction/observer ownership, and lower libged/libdm dependency rows still
block libbsg retirement.

Status reporting now distinguishes expected retained bridge internals from
actionable retirement blockers.  The current residual draw-bridge scan still
finds active-view/view-set/recycle-pool helpers, scene-root and
independent-scope scene-ref helpers, mesh-LoD scene-ref handoff helpers,
central private compatibility declarations/definitions, and BSG-internal
selection/feature/overlay/polygon conversion helpers.  Those should not be
renamed away locally: they need either a neutral API boundary or migration of
the public BSG-typed draw-view surface.  The retirement blockers remain the
public BSG-shaped GED draw snap/scene-ref surface, retained qtcad
transaction/observer and DM/framebuffer host ownership, direct `libbsg` rows
in `libdm` and `libged`, qged's direct `dm_plugins` edge, and lower backend
ownership.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The short migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.98 seconds.  `brlobol_pivot_guard` completed in
17.69 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.53 seconds.  The longest member was
`libbsg_api_hygiene` at 41.08 seconds; `brlobol_pivot_guard` completed in
17.64 seconds, `ged_test_drawing_quad` in 12.72 seconds,
`ged_test_drawing_basic` in 7.50 seconds, and `ged_test_ged_draw_scene` in
1.42 seconds.  The standalone `brlobol_pivot_guard` rerun after status and
inventory updates passed in 17.52 seconds; a final post-edit guard rerun
passed in 17.54 seconds.

The GED draw draft/refs/view snapshot cleanup moved the remaining simple
retained draw bridge info/LoD/scale reads off private BSG-suffixed GED
draw-view wrappers.  `src/libged/bsg_ged_draw_draft.c` and
`src/libged/bsg_ged_draw_refs.c` now read adaptive primitive-wireframe
`rt_view_info` snapshots through `rt_view_context_info_get`;
`src/libged/bsg_ged_draw_refs.c` reads shape-ref LoD policy through
`rt_view_context_lod_policy_get`; and `src/libged/bsg_ged_draw_view.c` reads
realization-policy LoD, realization-policy scale, and polygon
area/overlap scale through `rt_view_context_lod_policy_get` and
`rt_view_context_scale_get`.  The behavior remains the same as the old
delegating helpers while removing those BSG-spelled calls from the retained
draw bridge sources.

The pivot guard now requires the neutral RT context helper tokens in those
bridge files and rejects `ged_draw_view_info_from_bsg`,
`ged_draw_view_lod_policy_from_bsg`, and `ged_draw_view_scale_from_bsg` from
returning there.  The central compatibility declarations and delegates remain
in the private GED draw-view bridge and `src/libged/ged_view_legacy.cpp` while
the wider public BSG-shaped draw API surface still exists.  This does not
change migration percentages: the public draw snap/scene-ref bridge surface,
retained qtcad draw transaction/observer ownership, and lower libged/libdm
dependency rows still block libbsg retirement.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The short migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.83 seconds.  `brlobol_pivot_guard` completed in
17.55 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.44 seconds.  The longest member was
`libbsg_api_hygiene` at 41.05 seconds; `brlobol_pivot_guard` completed in
17.52 seconds, `ged_test_drawing_quad` in 12.72 seconds,
`ged_test_drawing_basic` in 7.53 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  The standalone `brlobol_pivot_guard` rerun after status and
inventory updates passed in 17.60 seconds; a final post-edit guard rerun
passed in 17.88 seconds.

The GED draw transaction view-info/LoD/autoview cleanup moved another
bridge-owned transaction path off private BSG-suffixed GED draw-view wrappers.
`src/libged/bsg_ged_draw_transactions.c` now reads redraw LoD policy through
`rt_view_context_lod_policy_get`, reads the redraw `rt_view_info` snapshot
through `rt_view_context_info_get`, and runs transaction autoview through
`rt_view_context_autoview` with `RT_VIEW_AUTOVIEW_SCALE_DEFAULT`.  This keeps
the behavior equivalent to the old delegating helpers while removing the BSG
spelling from the transaction implementation.  Draft and refs sources still
use their retained bridge wrappers where they remain BSG-owning boundaries.

The pivot guard now requires the neutral RT context tokens in
`src/libged/bsg_ged_draw_transactions.c`, rejects
`ged_draw_view_info_from_bsg`, `ged_draw_view_lod_policy_from_bsg`, and
`ged_draw_view_autoview_default_bsg` from returning there, and splits a
previous mixed draft/refs/transactions guard requirement so only the sources
that still need `ged_draw_view_info_from_bsg` are required to keep it.  This
does not change migration percentages: the public BSG-shaped GED draw
snap/scene-ref bridge surface, retained qtcad draw transaction/observer
ownership, and the lower libged/libdm dependency rows still block libbsg
retirement.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first short gate exposed the stale mixed-file guard requirement noted
above; after correcting that guard, the short migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.90 seconds.  `brlobol_pivot_guard` completed in
17.62 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.21 seconds.  The longest member was
`libbsg_api_hygiene` at 40.93 seconds; `brlobol_pivot_guard` completed in
17.52 seconds, `ged_test_drawing_quad` in 12.64 seconds,
`ged_test_drawing_basic` in 7.52 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  The standalone `brlobol_pivot_guard` rerun after status and
inventory updates passed in 17.44 seconds.

The GED draw.cpp view-snapshot cleanup moved retained draw realization view
name, perspective, OBB, and scale reads off private BSG-suffixed GED draw-view
helpers.  `src/libged/draw.cpp` now uses `ged_view_context_name_get`,
`ged_view_context_perspective_get`, `rt_view_context_obb_get`, and
`ged_view_context_scale_get` directly.  The local debug helper is now
`draw_view_context_name`; an initial FreeBSD/clang unity-build attempt exposed
that the shorter `draw_view_name` spelling collided with an anonymous-
namespace variable from another unity member, so the helper was renamed before
the passing rebuild.

The pivot guard now requires those neutral draw.cpp tokens and rejects
`draw_view_name_bsg`, `ged_draw_view_context_name_from_bsg`,
`ged_draw_view_context_perspective_from_bsg`,
`ged_draw_view_context_scale_from_bsg`, and
`ged_draw_view_context_obb_from_bsg` from returning to the draw realization
controller.  This does not remove the private compatibility helpers from the
central bridge yet; it removes another production caller from their
BSG-spelled surface.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first build failed in `src/libged/draw.cpp` because the local
`draw_view_name` helper conflicted with another unity-build symbol; after
renaming it to `draw_view_context_name`, the same target set rebuilt cleanly.
The short migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.78 seconds.  `brlobol_pivot_guard` completed in
17.50 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.19 seconds.  The longest member was
`libbsg_api_hygiene` at 40.97 seconds; `brlobol_pivot_guard` completed in
17.42 seconds, `ged_test_drawing_quad` in 12.67 seconds,
`ged_test_drawing_basic` in 7.55 seconds, and `ged_test_ged_draw_scene` in
1.41 seconds.  The standalone `brlobol_pivot_guard` rerun after status
updates passed in 17.44 seconds.

The GED draw-view context facade cleanup moved public info and LoD policy
context helpers in `include/ged/draw.h` to neutral names:
`ged_draw_view_context_info_get`,
`ged_draw_view_context_lod_policy_get`,
`ged_draw_view_context_lod_policy_apply`, and
`ged_draw_view_context_lod_policy_apply_bot_threshold`.  Production draw,
view-LoD, and standalone LoD callers now use those neutral names.  The old
BSG-suffixed helpers remain only in the private GED draw-view compatibility
header and as delegating wrappers in `src/libged/ged_view_legacy.cpp`, so
external/public code no longer sees the BSG spelling for this context
info/LoD surface.

The pivot guard now requires the neutral public declarations and migrated
production uses, while rejecting
`ged_draw_view_context_info_from_bsg`,
`ged_draw_view_context_lod_policy_from_bsg`,
`ged_draw_view_context_lod_policy_apply_bsg`, and
`ged_draw_view_context_lod_policy_apply_bsg_bot_threshold` from returning to
`include/ged` or the migrated draw/LoD callers.  Compatibility coverage stays
centralized in the private bridge and central GED legacy-view implementation.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The build rebuilt the touched libged/qtcad slice cleanly.  The short migration
gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.72 seconds.  `brlobol_pivot_guard` completed in
17.39 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.15 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.33 seconds.  The longest member was
`libbsg_api_hygiene` at 41.12 seconds; `brlobol_pivot_guard` completed in
17.44 seconds, `ged_test_drawing_quad` in 12.66 seconds,
`ged_test_drawing_basic` in 7.52 seconds, and `ged_test_ged_draw_scene` in
1.40 seconds.  Static scans confirmed the old BSG-suffixed helper names are
absent from `include/ged` and migrated production draw/LoD callers, the old
names remain only in the private compatibility bridge/central implementation/
guard patterns, `git diff --check` passed for the touched code/guard/status
files, and the project-pattern `/tmp` scan was empty.  The standalone
`brlobol_pivot_guard` rerun after status updates passed in 17.37 seconds.

The TclCAD direct build dependency cleanup removed `libbsg` from the
`libtclcad` row in `src/source_dirs.cmake`, the guarded direct dependency row
in `doc/notes/obol_legacy_dependency_inventory.txt`, and the pivot guard's
exact-row allowlist.  `libtclcad` now directly depends on `libged`, `libdm`,
`libbn`, and `libbu`.  This matches the current source state: TclCAD has no
direct RT legacy-view/edit include and no direct BSG API call sites.  The only
retained direct BSG header/type usage in production TclCAD is the guarded
`src/libtclcad/view_data.c` `bsg/tcl_data.h` layout compatibility block, which
does not require a direct `libbsg` target row.  TclCAD still receives
transitional `libbsg` linkage through lower-level `libged`/`libdm`, so the
remaining retirement work stays in those bridge/backend layers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libtclcad.so libqtcad.so mged gsh qged test_qtcad_obol_draw_sync qsketch`.
The build regenerated CMake for the `src/source_dirs.cmake` change and then
rebuilt the affected GED/TclCAD/qtcad/qged/MGED/GSH slice cleanly.  The short
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.66 seconds.  `brlobol_pivot_guard` completed in
17.37 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.29 seconds.  The longest member was
`libbsg_api_hygiene` at 40.96 seconds; `brlobol_pivot_guard` completed in
17.50 seconds, `ged_test_drawing_basic` in 7.50 seconds, and
`ged_test_drawing_quad` in 12.73 seconds.  Static TclCAD scans confirmed only
the guarded `view_data.c` `bsg/tcl_data.h` layout compatibility block remains
as direct BSG type/header usage, stale direct dependency-row scans were clean,
`git diff --check` passed for the touched status/guard/dependency files, and
the project-pattern `/tmp` scan was empty.  The final standalone
`brlobol_pivot_guard` rerun after status updates passed in 17.38 seconds.

The qtcad direct build dependency cleanup removed `libbsg` from the
`libqtcad` row in `src/source_dirs.cmake`, the guarded direct dependency row
in `doc/notes/obol_legacy_dependency_inventory.txt`, and the pivot guard's
exact-row allowlist.  `libqtcad` now directly depends on `libbrlobol`,
`libged`, `libdm`, `libbg`, `libbn`, and `libbu`.  This matches the current
source state: a direct qtcad source scan finds no BSG API/header usage, while
qtcad still retains its direct `libdm` dependency plus draw transaction/
observer and display/backend bridge work for later migration.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libtclcad.so libqtcad.so mged gsh qged test_qtcad_obol_draw_sync qsketch`.
The build regenerated CMake for the `src/source_dirs.cmake` change and then
rebuilt the affected GED/TclCAD/qtcad/qged/MGED/GSH slice cleanly.  The short
migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.72 seconds.  `brlobol_pivot_guard` completed in
17.43 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.12 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.66 seconds.  The longest member was
`libbsg_api_hygiene` at 41.09 seconds; `brlobol_pivot_guard` completed in
17.58 seconds, `ged_test_drawing_basic` in 7.53 seconds, and
`ged_test_drawing_quad` in 12.88 seconds.  Static qtcad BSG-token and direct
dependency-row scans were clean, `git diff --check` passed for the touched
status/guard/dependency files, and the project-pattern `/tmp` scan was empty.

The internal GED draw implementation include cleanup moved the BSG-backed
`src/libged/bsg_ged_draw*` implementation/private files off the BSG-named
public wrapper and onto `ged/draw.h`.  As of this cleanup, a direct non-test
production scan of `include` and `src` finds no `ged/bsg_ged_draw.h` includes.
The pivot guard now has a central production scan that rejects any such direct
include from returning, while still allowing explicit compatibility tests to
exercise the old include.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libtclcad.so libqtcad.so mged gsh qged test_qtcad_obol_draw_sync qsketch`.
The build rebuilt the touched libged/libtclcad/MGED/GSH/qged/qtcad slice
cleanly.  The focused gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.65 seconds.  `brlobol_pivot_guard` completed in
17.36 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 82.20 seconds.  The longest member was
`libbsg_api_hygiene` at 41.08 seconds; `brlobol_pivot_guard` completed in
17.34 seconds, `ged_test_drawing_basic` in 7.51 seconds, and
`ged_test_drawing_quad` in 12.68 seconds.  After the status updates, the
standalone `brlobol_pivot_guard` completed 1/1 passing in 17.48 seconds.

The GED draw public-header ownership cleanup moved the public GED draw-scene
declarations into `include/ged/draw.h`.  The BSG-named
`include/ged/bsg_ged_draw.h` header is now a compatibility wrapper that
includes `ged/draw.h`, preserving existing BSG-backed implementation and
compatibility include paths while making the neutral installed header the
declaration owner.  The pivot guard requires the public draw-view tokens in
`ged/draw.h`, requires `ged/bsg_ged_draw.h` to remain a wrapper, rejects public
draw declarations from returning to the BSG-named header, and continues to
reject the old BSG-named include from migrated production consumers.

The production GED draw facade consumer cleanup moved the remaining non-test
production consumers off direct `ged/bsg_ged_draw.h` includes.  Libged command
and helper sources, TclCAD view helpers, MGED, GSH, qtcad, and the private
consumer-facing libged draw helper header now include the neutral installed
`ged/draw.h` facade.  Direct production `ged/bsg_ged_draw.h` includes are
now fully retired from non-test `include` and `src` production files.  The
pivot guard now requires `ged/draw.h` for the migrated production consumer
list and rejects the BSG-named draw header from returning there.

This is still not hard removal of the BSG draw implementation.  The neutral
public header owns the declarations, but the retained draw implementation
remains BSG-backed until the wider GED draw-view API boundary moves to neutral
ownership.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libtclcad.so libqtcad.so mged gsh qged test_qtcad_obol_draw_sync qsketch`.
The build rebuilt the touched libged/libtclcad/MGED/GSH/qged/qtcad slice
cleanly.  The focused gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.00 seconds.  `brlobol_pivot_guard` completed in
16.72 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.87 seconds.  The longest member was
`libbsg_api_hygiene` at 41.23 seconds; `brlobol_pivot_guard` completed in
16.77 seconds, `ged_test_drawing_basic` in 7.55 seconds, and
`ged_test_drawing_quad` in 12.72 seconds.  After the status updates, the
standalone `brlobol_pivot_guard` completed 1/1 passing in 16.87 seconds.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libtclcad.so libqtcad.so mged gsh qged test_qtcad_obol_draw_sync qsketch`.
The build rebuilt the touched libged/libtclcad/MGED/GSH/qged/qtcad slice
cleanly.  The focused gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.06 seconds.  `brlobol_pivot_guard` completed in
16.77 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.67 seconds.  The longest member was
`libbsg_api_hygiene` at 41.06 seconds; `brlobol_pivot_guard` completed in
16.76 seconds, `ged_test_drawing_basic` in 7.58 seconds, and
`ged_test_drawing_quad` in 12.67 seconds.  After the status updates, the
standalone `brlobol_pivot_guard` completed 1/1 passing in 16.76 seconds.

The qtcad retained draw include cleanup added `include/ged/draw.h` as the
neutral installed GED draw-scene facade and registered it in
`include/ged/CMakeLists.txt`.  At that stage the facade still included the
transitional BSG-backed `ged/bsg_ged_draw.h` declarations; the later public
header ownership cleanup reversed that direction so the BSG-named header is
now the compatibility wrapper.

`src/libqtcad/QgLegacyView.cpp` now includes `ged/draw.h` instead of
`ged/bsg_ged_draw.h`.  The pivot guard requires the neutral facade to exist
and be installed, requires qtcad to use it, and rejects direct qtcad use of
the BSG-named draw header from returning.  Remaining qtcad migration work is
retained draw transaction/observer behavior behind the neutral facade plus
the retained DM/framebuffer host; remaining GED work is still the wider
public BSG-shaped draw-view API boundary.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The installed GED header change regenerated CMake and rebuilt a broad
RT/GED/qtcad dependent slice cleanly.  The first focused gate run exposed an
overly strict guard install check for `draw.h`; after fixing that guard bug,
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.00 seconds.  `brlobol_pivot_guard` completed in
16.71 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.12 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.59 seconds.  The longest member was
`libbsg_api_hygiene` at 40.99 seconds; `brlobol_pivot_guard` completed in
16.74 seconds, `ged_test_drawing_basic` in 7.54 seconds, and
`ged_test_drawing_quad` in 12.71 seconds.  After the status updates, the
final standalone `brlobol_pivot_guard` completed 1/1 passing in 16.69 seconds.

The GED retained update-callback cleanup moved the last targeted direct
BSG-named RT wrapper call out of `src/libged/ged_view_legacy.cpp`.
`include/rt/view.h` now declares neutral
`rt_view_context_update_callback_t` and
`rt_view_context_update_callback_set`.  The BSG-backed implementation in
`src/librt/view_legacy_bsg.c` installs a correctly typed BSG callback
trampoline with RT-owned callback state instead of relying on a
function-pointer cast, frees that bridge state when the callback is
overwritten, cleared, or the retained BSG view is released through RT helper
paths, and keeps the raw BSG callback setter for compatibility callers.

`src/libged/ged_view_legacy.cpp` now calls
`rt_view_context_update_callback_set` directly.  The targeted GED/qtcad
BSG-named RT wrapper scan is empty; remaining GED draw migration work is the
wider public BSG-shaped draw-view API boundary rather than a local RT wrapper
call.  The pivot guard requires the neutral callback declaration,
implementation, adapter-test coverage, and GED bridge use, and rejects
`rt_view_context_update_callback_set_bsg` from returning to
`ged_view_legacy.cpp`.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
Because `rt/view.h` changed, this rebuilt a broad RT/GED/qtcad dependent
slice cleanly.  The focused GED update-callback gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.92 seconds.  `brlobol_pivot_guard` completed in
16.63 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.98 seconds.  The longest member was
`libbsg_api_hygiene` at 41.08 seconds; `brlobol_pivot_guard` completed in
16.96 seconds, `ged_test_drawing_basic` in 7.63 seconds, and
`ged_test_drawing_quad` in 12.71 seconds.  After the status updates, the
standalone `brlobol_pivot_guard` completed 1/1 passing in 16.65 seconds.

The GED retained scene-ref/mesh-LoD cleanup moved the remaining direct
scene-ref and mesh-LoD RT-BSG calls out of
`src/libged/ged_view_legacy.cpp`.  `include/rt/view.h` now declares neutral
`rt_view_scene_ref`, `RT_VIEW_SCENE_REF_NULL_INIT`,
`rt_view_scene_ref_null`, `rt_view_scene_ref_is_null`,
`rt_view_scene_ref_equal`, `rt_view_context_independent_scope_ref`,
`rt_mesh_lod_load_view_scene_ref`, and `rt_mesh_lod_free_scene_ref` helpers.
`src/librt/view_legacy_bsg.c` implements them as transitional delegates to
the retained BSG scene-ref and mesh-LoD backend, and `rt_view_legacy_bsg`
covers the neutral null/ref paths alongside the remaining BSG compatibility
calls.

`src/libged/ged_view_legacy.cpp` still preserves the public BSG-shaped GED
draw scene-ref signatures, but it now converts those handles to neutral RT
scene refs internally before calling the RT adapter.  Its targeted
BSG-named RT wrapper scan is now down to
`rt_view_context_update_callback_set_bsg`, whose callback type and lifetime
contract still need a separate design pass.  The pivot guard requires the
neutral scene-ref/mesh-LoD declarations, implementation, adapter-test
coverage, and GED use, and rejects the retired
`rt_view_independent_scope_ref_bsg`,
`rt_mesh_lod_load_view_scene_ref_bsg`, and
`rt_mesh_lod_free_scene_ref_bsg` calls from returning to
`ged_view_legacy.cpp`.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
Because `rt/view.h` changed, this rebuilt a broad RT/GED/qtcad dependent
slice cleanly.  The focused GED scene-ref/mesh-LoD gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.90 seconds.  `brlobol_pivot_guard` completed in
16.58 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.13 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.83 seconds.  The longest member was
`libbsg_api_hygiene` at 41.06 seconds; `brlobol_pivot_guard` completed in
16.96 seconds, `ged_test_drawing_basic` in 7.49 seconds, and
`ged_test_drawing_quad` in 12.72 seconds.  After the status updates, the
standalone `brlobol_pivot_guard` completed 1/1 passing in 16.80 seconds.

The qtcad sketch edit-create cleanup moved the last qtcad sketch edit
creation call off the BSG-named RT edit helper.  `include/rt/edit.h` now
declares neutral context helpers:
`rt_edit_view_from_context`, `rt_edit_create_context`,
`rt_edit_reinit_context`, and `rt_edit_knob_cmd_process_context`.
`src/librt/view_legacy_bsg.c` implements them as transitional delegates to the
existing BSG-backed edit adapters, and `rt_view_legacy_bsg` covers the neutral
view snapshot, create, reinit, and null-argument knob helper paths alongside
the remaining BSG compatibility calls.

`src/libqtcad/QgLegacyView.cpp` now includes `rt/edit.h` and calls
`rt_edit_create_context` for sketch edit creation.  It no longer includes
`rt/edit_legacy_bsg.h` or calls `rt_edit_create_bsg`.
At that stage the remaining qtcad BSG-shaped bridge header was
`ged/bsg_ged_draw.h` for retained draw transactions/observers, plus the
retained DM/framebuffer host surface; a later cleanup moved qtcad's include to
the neutral `ged/draw.h` facade.  The pivot guard requires the neutral
edit-context declarations/implementation/adapter-test coverage and qtcad use,
and rejects the legacy edit header/call from returning to `QgLegacyView.cpp`.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
Because `rt/edit.h` changed, the first rebuild also rebuilt RT/GED/qtcad
dependents cleanly.  The focused qtcad sketch edit-create gate
`ctest --test-dir build -R "^(rt_view_legacy_bsg|brlobol_pivot_guard|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.94 seconds.  `brlobol_pivot_guard` completed in
16.65 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.28 seconds.  The longest member was
`libbsg_api_hygiene` at 41.00 seconds; `brlobol_pivot_guard` completed in
16.51 seconds, `ged_test_drawing_basic` in 7.54 seconds, and
`ged_test_drawing_quad` in 12.64 seconds.
After the status and inventory refresh, the standalone `brlobol_pivot_guard`
completed 1/1 passing in 16.62 seconds; after the final stale-wording cleanup,
the standalone guard completed 1/1 passing in 16.73 seconds.

The qtcad retained polygon cleanup moved the remaining qtcad polygon bridge
calls off BSG-named RT helpers.  `include/rt/view.h` now declares the neutral
polygon record callback type plus neutral `rt_view_context_polygon_*` and
`rt_view_polygon_*` helpers covering ref/record access, create/select/find/
dup, record visitation, snap counts, point-selection clear, updates,
screen-point updates, move/name/context/visual/open/close/selected-point/
remove, user data, CSG, sketch import/export, and snap-exclude set.

`src/librt/view_legacy_bsg.c` implements those neutral helpers as
transitional delegates to the existing BSG compatibility layer.  The
`rt_view_legacy_bsg` adapter test now covers the neutral polygon paths
alongside the remaining BSG compatibility calls, including null handling,
find/dup/select, record visitation, snap-count/exclusion behavior,
point-selection clearing, record reads, mutation helpers, user-data,
CSG/remove, and sketch import/export null handling.

`src/libqtcad/QgLegacyView.cpp` now routes the retained qtcad polygon surface
through neutral RT names and no longer directly includes
`rt/view_legacy_bsg.h`.  The pivot guard's direct production legacy-view
include allowlist is down to `src/libged/ged_view_legacy.cpp` and
`src/librt/view_legacy_bsg.c`; at that point qtcad's remaining BSG-shaped
bridge surface was the retained draw header, the then-retained sketch edit
entry point, and retained DM/framebuffer hosting rather than RT view-wrapper
calls.  The guard requires the neutral polygon declarations/implementations/
test coverage and rejects the retired BSG-named polygon helpers from returning
to GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`;
`ninja -C build libqtcad.so` then rebuilt cleanly after retiring qtcad's
direct `rt/view_legacy_bsg.h` include.  The first focused rerun exposed the
stale guard requirement for that qtcad include, which was corrected.  The
focused qtcad polygon/direct-include gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
then completed 4/4 passing in 17.07 seconds.  `brlobol_pivot_guard`
completed in 16.79 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.78 seconds.  The longest member was
`libbsg_api_hygiene` at 40.88 seconds; `brlobol_pivot_guard` completed in
16.84 seconds inside that run, `ged_test_drawing_basic` in 7.67 seconds, and
`ged_test_drawing_quad` in 12.81 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
16.61, 16.65, and 16.66 seconds total.

The qtcad retained feature/edit-preview cleanup moved the remaining qtcad
feature and edit-preview bridge calls off BSG-named RT helpers.  `include/rt/view.h`
now declares neutral retained feature helpers:
`rt_view_feature_ref_is_null`,
`rt_view_context_edit_preview_publish_event`,
`rt_view_context_feature_overlay_ensure`,
`rt_view_context_feature_label_ensure`, `rt_view_context_feature_remove`,
and the `rt_view_feature_*` context/style/geometry helpers for context
rebinding, visibility, color, touch, label replacement, point replacement,
and geometry clear.

`src/librt/view_legacy_bsg.c` implements the neutral helpers as transitional
delegates to the existing BSG compatibility layer.  `rt_view_legacy_bsg`
now covers the neutral feature/edit-preview calls alongside the remaining BSG
compatibility calls, including null handling, label creation/replacement,
style updates through both neutral and legacy setters, edit-preview callback
bridging, lifecycle event publishing, point replacement/readback, geometry
clear, and feature removal.

`src/libqtcad/QgLegacyView.cpp` now routes retained edit-preview event
publication, overlay/label ensure, feature removal, feature view rebinding,
visibility/color/touch updates, label replacement, point replacement, and
geometry clear through the neutral RT helper names.  The pivot guard now
requires the neutral helper declarations/implementations/test coverage and
rejects the retired BSG-named feature/edit-preview calls from returning to
GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused qtcad feature/edit-preview gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.57 seconds.  `brlobol_pivot_guard` completed in
16.28 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.01 seconds.  The longest member was
`libbsg_api_hygiene` at 40.95 seconds; `brlobol_pivot_guard` completed in
16.28 seconds inside that run, `ged_test_drawing_basic` in 7.51 seconds, and
`ged_test_drawing_quad` in 12.69 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
16.28 seconds and then 16.30 seconds total.

The qtcad retained bounds-update cleanup moved another qtcad bridge surface
off BSG-named RT helpers without exposing a new BSG-shaped callback type.
`include/rt/view.h` now declares `rt_view_context_bounds_update_suspend` and
`rt_view_context_bounds_update_restore`.  `src/librt/view_legacy_bsg.c`
implements them by capturing the current retained bounds callback into an
opaque RT-owned state object, clearing it on suspend, restoring it on demand,
optionally refreshing bounds, and freeing the opaque state inside the RT
legacy adapter boundary.  `rt_view_legacy_bsg` covers null handling, suspend
clearing, restore with refresh, and null-view restore cleanup alongside the
existing BSG compatibility callback accessors.

`src/libqtcad/QgLegacyView.cpp` now stores only the opaque RT suspend state in
`qg_legacy_view_bounds_update_state` and restores it through the neutral RT
helper.  The pivot guard requires the neutral helpers in the public RT header,
RT legacy adapter, adapter test, and qtcad bridge where migrated, and rejects
the old BSG-named context bounds callback accessors from returning to
GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused qtcad bounds-update gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.35 seconds.  `brlobol_pivot_guard` completed in
16.07 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.06 seconds.  The longest member was
`libbsg_api_hygiene` at 41.01 seconds; `brlobol_pivot_guard` completed in
16.04 seconds inside that run, `ged_test_drawing_basic` in 7.68 seconds, and
`ged_test_drawing_quad` in 12.74 seconds.  Standalone
`brlobol_pivot_guard` reruns after the status and inventory updates passed in
16.30 seconds and then 16.27 seconds total.

The qtcad retained pick/result/selection cleanup moved another qtcad bridge
surface off BSG-named RT helpers.  `include/rt/view.h` now declares neutral
opaque helpers for qtcad-used pick operations, pick-result context lifecycle
and accessors, copy append, first-result filtering, selection set from an
opaque pick-result context, and selection clear.  `src/librt/view_legacy_bsg.c`
implements those helpers as transitional retained-view delegates, and
`rt_view_legacy_bsg` covers null handling plus neutral result-copy,
first-result, and selection behavior alongside the existing BSG compatibility
calls.

`src/libqtcad/QgLegacyView.cpp` now routes qtcad point/nearest/rectangle pick,
pick-result object lifecycle/accessors/filtering/copy, and selection set/clear
through the neutral RT helper names.  The pivot guard requires the neutral
helpers in the public RT header, RT legacy adapter, adapter test, and qtcad
bridge where migrated, and rejects the old BSG-named qtcad
pick/result/selection helper calls from returning to GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused qtcad pick/result/selection gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.38 seconds.  `brlobol_pivot_guard` completed in
16.10 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.95 seconds.  The longest member was
`libbsg_api_hygiene` at 41.03 seconds; `brlobol_pivot_guard` completed in
16.04 seconds inside that run, `ged_test_drawing_basic` in 7.53 seconds, and
`ged_test_drawing_quad` in 12.77 seconds.  Standalone
`brlobol_pivot_guard` reruns after the status and inventory updates passed in
16.17 seconds and then 16.12 seconds total.

The qtcad retained view-set attach cleanup moved another qtcad bridge surface
off a BSG-named RT helper.  `include/rt/view.h` now declares
`rt_view_context_view_set_attach`, `src/librt/view_legacy_bsg.c` implements it
as a transitional retained-view delegate, and `rt_view_legacy_bsg` covers
neutral null, attach, and detach behavior alongside the existing BSG
compatibility call.

`src/libqtcad/QgLegacyView.cpp` now routes qtcad GED view-set attach through
the neutral RT helper, matching the already-migrated qtcad GED view-set
add/remove helpers.  The pivot guard requires the neutral helper in the
public RT header, RT legacy adapter, adapter test, and qtcad bridge where
migrated, and rejects the old BSG-named attach call from returning to
GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused qtcad view-set attach gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.40 seconds.  `brlobol_pivot_guard` completed in
16.11 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.01 seconds.  The longest member was
`libbsg_api_hygiene` at 41.05 seconds; `brlobol_pivot_guard` completed in
16.12 seconds inside that run, `ged_test_drawing_basic` in 7.54 seconds, and
`ged_test_drawing_quad` in 12.71 seconds.  A standalone
`brlobol_pivot_guard` run after the status and inventory updates passed in
16.01 seconds.

The qtcad release-storage and sketch line-set cleanup moved another retained
qtcad bridge surface off BSG-named RT helpers.  `include/rt/view.h` now
declares `rt_view_context_release_storage`,
`rt_view_context_line_set_create`, `rt_view_line_set_context_is_null`,
`rt_view_line_set_context_set_points`, and
`rt_view_line_set_context_destroy`.  `src/librt/view_legacy_bsg.c`
implements them as transitional retained-view delegates, and
`rt_view_legacy_bsg` covers neutral release-storage and line-set null,
create, point-update, clear, and destroy behavior alongside the existing BSG
compatibility calls.

`src/libqtcad/QgLegacyView.cpp` now routes the historical local retained-view
shallow free path and qtcad sketch preview line-set creation/null checks,
point replacement, and destruction through the neutral RT helpers.  The pivot
guard requires those neutral helpers in the public RT header, RT legacy
adapter, adapter test, and qtcad bridge where migrated, and rejects the old
BSG-named release-storage and line-set calls from returning to GED/qtcad
bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused qtcad release-storage/line-set gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.32 seconds.  `brlobol_pivot_guard` completed in
16.03 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.86 seconds.  The longest member was
`libbsg_api_hygiene` at 41.04 seconds; `brlobol_pivot_guard` completed in
16.03 seconds inside that run, `ged_test_drawing_basic` in 7.53 seconds, and
`ged_test_drawing_quad` in 12.66 seconds.  A standalone
`brlobol_pivot_guard` run after the status and inventory updates passed in
16.19 seconds.

The retained lifecycle create/free cleanup moved another GED/qtcad bridge
surface off BSG-named RT helpers.  `include/rt/view.h` now declares
`rt_view_context_create`, `rt_view_context_create_with_set`,
`rt_view_context_create_copy_with_set`, and `rt_view_context_free`.
`src/librt/view_legacy_bsg.c` implements them as transitional retained-view
delegates, and `rt_view_legacy_bsg` covers normal create, create-with-set,
create-copy-with-set, free, and null-free behavior alongside the existing BSG
compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained default-view
creation, owned-context cleanup, and public GED create/free facades through
the neutral RT lifecycle helpers.  `src/libqtcad/QgLegacyView.cpp` now routes
local retained-view create/destroy through the same neutral helpers; a later
qtcad cleanup also moved its historical shallow local-view free path to a
neutral release-storage helper.  The pivot guard requires the neutral
lifecycle helpers in the public RT header, RT legacy adapter, adapter test,
GED bridge, and qtcad bridge where migrated, and rejects the old BSG-named
create/free calls from returning to GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync`.
The qtcad sketch smoke target also passed with `ninja -C build qsketch`.
The focused lifecycle create/free gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.29 seconds.  `brlobol_pivot_guard` completed in
16.00 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.12 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.75 seconds.  The longest member was
`libbsg_api_hygiene` at 40.97 seconds; `brlobol_pivot_guard` completed in
15.94 seconds inside that run, `ged_test_drawing_basic` in 7.53 seconds, and
`ged_test_drawing_quad` in 12.71 seconds.  A final standalone
`brlobol_pivot_guard` run after the status and inventory updates passed in
16.39 seconds.

The retained view-set passthrough cleanup moved another GED/qtcad bridge
surface off BSG-named RT helpers.  `include/rt/view.h` now declares
`rt_view_set_context_views`, `rt_view_set_context_recycle_pool`,
`rt_view_set_context_add`, and `rt_view_set_context_remove`.
`src/librt/view_legacy_bsg.c` implements them as transitional retained
view-set delegates, and `rt_view_legacy_bsg` covers null, empty, populated,
add, and remove behavior alongside the existing BSG compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained view-set list access,
recycle-pool access, default-view insertion, and the public
`ged_view_set_context_add` facade through the neutral helpers.
`src/libqtcad/QgLegacyView.cpp` now routes retained qtcad GED view-set
add/remove through the same neutral helpers.  The pivot guard requires the
neutral RT view-set helpers in the public RT header, RT legacy adapter,
adapter test, GED bridge, and qtcad bridge where migrated, and rejects the old
BSG-named view-set calls from returning to GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync`.
The focused view-set passthrough gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.22 seconds.  `brlobol_pivot_guard` completed in
15.94 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.83 seconds.  The longest member was
`libbsg_api_hygiene` at 40.94 seconds; `brlobol_pivot_guard` completed in
15.95 seconds inside that run, `ged_test_drawing_basic` in 7.58 seconds, and
`ged_test_drawing_quad` in 12.77 seconds.  The latest standalone
`brlobol_pivot_guard` run passed in 15.97 seconds.

The retained snap first-candidate cleanup moved another GED draw bridge path
off direct BSG snap-result plumbing.  `include/rt/view.h` now declares
`rt_view_context_snap_first_candidate`, and
`src/librt/view_legacy_bsg.c` implements it as a transitional retained-view
adapter that zeroes the output candidate, queries the existing BSG snap
candidate implementation inside the RT legacy boundary, copies candidate zero,
and frees the temporary BSG snap result before returning.  The
`rt_view_legacy_bsg` test now covers null arguments and verifies the neutral
first-candidate result against the existing snap-candidate adapter.

`src/libged/ged_view_legacy.cpp` no longer includes `bsg/snap_action.h`,
allocates `struct bsg_snap_result`, calls `bsg_snap_result_init/free`, or calls
`rt_view_snap_candidates_bsg` for retained snap first-candidate queries.  GED
still exposes the transitional BSG-typed public draw snap entry point, but the
implementation now maps the public GED snap kind to neutral
`RT_VIEW_SNAP_KIND_*` bits and delegates to the neutral RT context helper.
The pivot guard now requires the neutral helper in the public RT header, RT
legacy adapter, adapter test, and GED bridge, and rejects direct BSG
snap-result plumbing from returning to GED/qtcad bridge files.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so`.
The focused snap first-candidate gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 15.89 seconds.  `brlobol_pivot_guard` completed in
15.72 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and
`ged_test_qged_swrast` in 0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.68 seconds.  The longest member was
`libbsg_api_hygiene` at 41.10 seconds; `brlobol_pivot_guard` completed in
15.73 seconds inside that run, `ged_test_drawing_basic` in 7.57 seconds, and
`ged_test_drawing_quad` in 12.71 seconds.  The latest standalone
`brlobol_pivot_guard` run passed in 15.74 seconds.

The retained OBB/snap-kind cleanup moved another small GED bridge surface off
BSG-named RT details.  `include/rt/view.h` now declares
`rt_view_context_obb_get`, and `src/librt/view_legacy_bsg.c` implements it as
a transitional delegate to the existing BSG-backed OBB adapter.  The
`rt_view_legacy_bsg` test covers null-argument handling and retained-view
copy behavior for both the existing compatibility entry point and the neutral
helper.  `src/libged/ged_view_legacy.cpp` now calls the neutral OBB helper
and maps GED snap kinds to neutral `RT_VIEW_SNAP_KIND_*` constants instead of
`RT_VIEW_SNAP_KIND_*_BSG` constants.  The pivot guard now requires the neutral
OBB helper in the RT header/adapter and GED bridge, and rejects
`rt_view_context_obb_from_bsg` plus BSG snap-kind constants from returning to
GED/qtcad bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so`.
The focused OBB/snap-kind gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 15.89 seconds.  `brlobol_pivot_guard` completed in
15.71 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and
`ged_test_qged_swrast` in 0.11 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.46 seconds.  The longest member was
`libbsg_api_hygiene` at 40.87 seconds; `brlobol_pivot_guard` completed in
15.80 seconds inside that run, `ged_test_drawing_basic` in 7.51 seconds, and
`ged_test_drawing_quad` in 12.68 seconds.  A final standalone
`brlobol_pivot_guard` run after the documentation updates passed in
15.91 seconds.

The active handoff has been refreshed for the current migration state.
`obol_activework.txt` is now a compact status guide instead of a long
per-batch history: it records the current 3-file production direct
`rt/view_legacy_bsg.h` owner count, the exact remaining GED and qtcad wrapper
buckets, direct target dependency blockers, current completion estimates,
scan commands, and the focused test gates.  Detailed historical notes remain
in this worklog.

The legacy dependency inventory has also been corrected for the current
TclCAD state.  Production TclCAD direct legacy-view ownership is zero files;
the remaining TclCAD transition surface is indirect through public GED
view-context facades, public GED draw-view context helpers, and the
`view_data.c` `bsg/tcl_data.h` layout compatibility check.

The pivot guard scan was tightened without reducing coverage.  The GED Tcl
overlay BSG fixture checks had been embedded inside the generic per-file
Obol-canonical scan, which meant the same fixture was re-read and rechecked
once for every scanned file.  Those exact token and rejection checks now run
once through `_brlobol_pivot_guard_check_ged_tcl_overlay_bsg_test()`, before
the generic file loop.  A guard-only verification run passed on FreeBSD/clang:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 15.67 seconds.  The broad focused wrapper gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.63 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
15.86 seconds inside that run, `ged_test_drawing_basic` in 7.50 seconds, and
`ged_test_drawing_quad` in 12.69 seconds.

The latest independent/scene context cleanup added neutral RT context helpers
in `include/rt/view.h` for retained independent-view checks,
independent-scope null checks, independent-scope destroy, scene-attached
checks, scene-anchor ensure, and scene-shared checks.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view independent
checks, independent-scope null checks, and independent-scope destroy calls
through the neutral RT context helpers.  `src/libqtcad/QgLegacyView.cpp` now
routes retained qtcad scene-attached checks through the neutral RT context
helper.  The direct production legacy-view owner count was still 3 at that point because
GED, qtcad, and librt still own transitional bridge responsibilities, but the
pivot guard now requires the neutral scope-context calls where migrated and
rejects the old BSG-named context helper forms from returning to those bridge
callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused independent/scene context gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.15 seconds.  `brlobol_pivot_guard` completed in
16.87 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.74 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.09 seconds; `brlobol_pivot_guard` completed in
16.84 seconds, `ged_test_drawing_quad` in 12.72 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous GED knob-surface cleanup added neutral RT context helpers in
`include/rt/view.h` for retained knob-state get/set, knob-state hash,
knob-value snapshots, knob reset/calibrate, knob command processing, knob
translate, knob rotate, and knob rate-flag updates.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view knob-state,
knob-value, knob reset/calibrate, knob command processing, knob translate,
knob rotate, and knob rate-flag facade calls through the neutral RT context
helpers.  qtcad did not own callsites for this GED knob batch, so its direct
bridge status is unchanged from the previous neutralized state groups.  The
direct production legacy-view owner count was still 3 at that point because GED, qtcad, and
librt still own transitional bridge responsibilities, but the pivot guard now
requires the neutral knob calls where migrated and rejects the old BSG-named
context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused GED knob gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.25 seconds.  `brlobol_pivot_guard` completed in
16.96 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.62 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.97 seconds; `brlobol_pivot_guard` completed in
16.88 seconds, `ged_test_drawing_quad` in 12.67 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous GED state/control cleanup added neutral RT context helpers in
`include/rt/view.h` for retained clear, cleared-state get/set, user-data
get/set, TclCAD data binding, callback-table binding, edit-matrix set/clear,
and frame-revision reads.  `src/librt/view_legacy_bsg.c` implements those
helpers as transitional delegates to the current BSG-backed adapter, and
`rt_view_legacy_bsg` now covers their null-argument and retained-view behavior
alongside the existing BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view clear,
cleared-state, user-data, TclCAD data, callback-table, edit-matrix, and
frame-revision facade calls through the neutral RT context helpers.  qtcad did
not own callsites for this GED state/control batch, so its direct bridge
status is unchanged from the previous neutralized state groups.  The direct
production legacy-view owner count was still 3 at that point because GED, qtcad, and librt
still own transitional bridge responsibilities, but the pivot guard now
requires the neutral state/control calls where migrated and rejects the old
BSG-named context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused GED state/control gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.07 seconds.  `brlobol_pivot_guard` completed in
16.78 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.76 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
16.96 seconds, `ged_test_drawing_quad` in 12.67 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous runtime context cleanup added neutral RT context helpers in
`include/rt/view.h` for retained update, autoview, autoview-bounds, adjust,
hash, unique-object naming, display-manager set, framebuffer-mode set, and
settings-shared checks.  `src/librt/view_legacy_bsg.c` implements those
helpers as transitional delegates to the current BSG-backed adapter, and
`rt_view_legacy_bsg` now covers their null-argument and retained-view behavior
alongside the existing BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view update, hash,
display-manager set, framebuffer-mode set, autoview, autoview-bounds, and
settings-shared facade calls through the neutral RT context helpers.
`src/libqtcad/QgLegacyView.cpp` now routes qtcad retained adjust,
display-manager set, hash, unique-object naming, framebuffer-mode set,
autoview, and update bridge calls through the same neutral boundary.  The
direct production legacy-view owner count was still 3 at that point because GED, qtcad, and
librt still own transitional bridge responsibilities, but the pivot guard now
requires the neutral runtime calls where migrated and rejects the old
BSG-named context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused runtime-context gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.95 seconds.  `brlobol_pivot_guard` completed in
16.66 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.56 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.03 seconds; `brlobol_pivot_guard` completed in
16.57 seconds, `ged_test_drawing_quad` in 12.83 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous snap/LoD state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained snap grid/point helpers, Tcl snap
preparation, snap-lines get/set, snap-source flags get/set, snap-kind mask
reads, snap-exclude clear, center-linesnap, snap tolerance get/set, LoD policy
get/apply/copy, and LoD bounds update.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view snap grid,
Tcl snap preparation, 2D snap point, snap-lines, center-linesnap, snap
tolerance, and LoD policy facades through the neutral RT context helpers.
`src/libqtcad/QgLegacyView.cpp` now routes qtcad retained LoD policy/copy,
LoD bounds update, snap source, snap-lines, snap-kind mask, snap tolerance,
and snap-exclude clear bridge calls through the same neutral boundary.  The
direct production legacy-view owner count was still 3 at that point because GED, qtcad, and
librt still own transitional bridge responsibilities, but the pivot guard now
requires the neutral snap/LoD calls where migrated and rejects the old
BSG-named context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused snap/LoD-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 17.25 seconds.  `brlobol_pivot_guard` completed in
16.97 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.39 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.95 seconds; `brlobol_pivot_guard` completed in
16.64 seconds, `ged_test_drawing_quad` in 12.66 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous faceplate/overlay state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained ADC, grid, model-axes, view-axes,
center-dot, scale-overlay, and parameter overlay state get/set operations.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view ADC, grid,
model-axes, view-axes, center-dot, scale-overlay, and parameter overlay facade
calls through the neutral RT context helpers.  `src/libqtcad/QgLegacyView.cpp`
routes the same retained qtcad overlay state bridge calls through the neutral
boundary.  The direct production legacy-view owner count was still 3 at that point because
GED, qtcad, and librt still own transitional bridge responsibilities, but the
pivot guard now requires the neutral faceplate/overlay state calls where
migrated and rejects the old BSG-named context helper forms from returning to
those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused faceplate/overlay-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.64 seconds.  `brlobol_pivot_guard` completed in
16.35 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.17 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
16.35 seconds, `ged_test_drawing_quad` in 12.67 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The latest pointer/interaction state cleanup added neutral RT context helpers
in `include/rt/view.h` for retained screen-point reads, current-point
get/set, previous-mouse get/set, mouse-delta settings reads, mouse-state
writes, and interactive-rectangle get/set.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view screen-point,
previous-mouse, mouse-delta settings, mouse-state, and interactive-rectangle
facade calls through the neutral RT context helpers.
`src/libqtcad/QgLegacyView.cpp` now routes qtcad retained-view screen-point,
current-point, mouse-state, and interactive-rectangle calls through the same
neutral boundary.  The direct production legacy-view owner count was still 3
at that point because GED, qtcad, and librt still owned transitional bridge
responsibilities,
but the pivot guard now requires the neutral pointer/interaction calls where
migrated and rejects the old BSG-named context helper forms from returning to
those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused pointer/interaction-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.39 seconds.  `brlobol_pivot_guard` completed in
16.11 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.86 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.93 seconds; `brlobol_pivot_guard` completed in
16.12 seconds, `ged_test_drawing_quad` in 12.69 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous GED view-control state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained eye-position get/set, keypoint get/set,
rotate-about get/set, coord get/set, and zclip get/set.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view eye position,
keypoint, rotate-about, coord, and zclip facade calls through the neutral RT
context helpers.  qtcad did not own callsites for this small GED view-control
batch, so its direct bridge status is unchanged from the previous neutralized
state groups.  The direct production legacy-view owner count was still 3
at that point because GED, qtcad, and librt still owned transitional bridge
responsibilities,
but the pivot guard now requires the neutral view-control calls in GED and
rejects the old BSG-named context helper forms from returning there.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused GED view-control gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.31 seconds.  `brlobol_pivot_guard` completed in
16.02 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 81.32 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.15 seconds; `brlobol_pivot_guard` completed in
16.37 seconds, `ged_test_drawing_quad` in 12.71 seconds, and
`dm_backend_draw_item` in 0.13 seconds.

The previous orientation/info state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained view-info snapshots, orientation quaternion
reads, AET get/set/state-set, and plane reads.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view info, AET,
orientation, and plane facade calls through the neutral RT context helpers,
and `src/libqtcad/QgLegacyView.cpp` now routes retained qtcad info and AET
calls through the same neutral boundary.  The direct production legacy-view
owner count was still 3 at that point because GED, qtcad, and librt still own transitional
bridge responsibilities, but the pivot guard now requires the neutral
orientation/info state calls where migrated and rejects the old BSG-named
context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused orientation/info-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.26 seconds.  `brlobol_pivot_guard` completed in
15.98 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.84 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.99 seconds; `brlobol_pivot_guard` completed in
15.99 seconds, `ged_test_drawing_quad` in 12.73 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous transform view-state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained model2view/view2model/pmodel2view/pmat
get/set, center matrix reads, center-vector writes, and rotation get/set.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view transform
facade calls through the neutral RT context helpers, and
`src/libqtcad/QgLegacyView.cpp` now routes retained qtcad center, rotation,
model2view, and view2model calls through the same neutral boundary.  The
direct production legacy-view owner count was still 3 at that point because GED, qtcad, and
librt still own transitional bridge responsibilities, but the pivot guard now
requires the neutral transform calls where migrated and rejects the old
BSG-named context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused transform-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.30 seconds.  `brlobol_pivot_guard` completed in
16.01 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.62 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.94 seconds; `brlobol_pivot_guard` completed in
15.93 seconds, `ged_test_drawing_quad` in 12.66 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous scalar view-state cleanup added neutral RT context helpers in
`include/rt/view.h` for retained view scale get/set, live scale storage,
combined scale-state set, initial/absolute scale get/set, size get/set,
inverse-size reads, perspective get/set, and radius reads.
`src/librt/view_legacy_bsg.c` implements those helpers as transitional
delegates to the current BSG-backed adapter, and `rt_view_legacy_bsg` now
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view scalar facade
calls through the neutral RT context helpers, and
`src/libqtcad/QgLegacyView.cpp` now routes retained qtcad scale, perspective,
size, scale-storage, and radius reads/writes through the same neutral
boundary.  The direct production legacy-view owner count was still 3 at that point because
GED, qtcad, and librt still own transitional bridge responsibilities, but the
pivot guard now requires the neutral scalar calls where migrated and rejects
the old BSG-named context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused scalar-state gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 16.25 seconds.  `brlobol_pivot_guard` completed in
15.96 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`test_qtcad_obol_draw_sync` in 0.11 seconds, and `ged_test_qged_swrast` in
0.11 seconds.
The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.81 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.36 seconds; `brlobol_pivot_guard` completed in
15.75 seconds, `ged_test_drawing_quad` in 12.63 seconds, and
`dm_backend_draw_item` in 0.13 seconds.

The previous name/unit-conversion cleanup added neutral RT context helpers in
`include/rt/view.h` for retained view name get/set, unit-conversion set,
local-to-base reads, and base-to-local reads.  `src/librt/view_legacy_bsg.c`
implements those helpers as transitional delegates to the current BSG-backed
adapter, and `rt_view_legacy_bsg` now covers their null-argument and
retained-view behavior alongside the existing BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes GED retained-view name and
unit-conversion facade calls through the neutral RT context helpers, and
`src/libqtcad/QgLegacyView.cpp` now routes retained qtcad name and
unit-conversion bridge calls through the same neutral boundary.  The direct
production legacy-view owner count was still 3 at that point because GED, qtcad, and librt
still own transitional bridge responsibilities, but the pivot guard now
requires the neutral calls where migrated and rejects the old BSG-named
context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused name/unit-conversion gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 15.88 seconds.  The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.48 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.08 seconds; `brlobol_pivot_guard` completed in
15.56 seconds and `ged_test_drawing_quad` in 12.72 seconds.

The previous refresh lifecycle cleanup added neutral RT context helpers in
`include/rt/view.h` for retained refresh dirty/consume/complete, refresh
enabled/suppressed state, suppression begin/end, and drawn-count get/set.
`src/librt/view_legacy_bsg.c` implements those helpers as the current
transitional delegates to the BSG-backed adapter, and `rt_view_legacy_bsg`
covers their null-argument and retained-view behavior alongside the existing
BSG-named compatibility calls.

`src/libged/ged_view_legacy.cpp` now routes the public GED retained-view
refresh lifecycle facade through the neutral RT context helpers, and
`src/libqtcad/QgLegacyView.cpp` now routes retained qtcad refresh
consume/complete through the same neutral boundary.  The direct production
legacy-view owner count was still 3 at that point because GED, qtcad, and librt still own
transitional bridge responsibilities, but the pivot guard now requires the
neutral refresh lifecycle calls where migrated and rejects the old BSG-named
context helper forms from returning to those bridge callers.

Verification on FreeBSD/clang passed
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so bin/mged test_qtcad_obol_draw_sync qsketch`.
The focused refresh/qtcad/qged gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 15.91 seconds.  The broader focused migration gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.38 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.95 seconds; `brlobol_pivot_guard` completed in
15.59 seconds and `ged_test_drawing_quad` in 12.74 seconds.

The active handoff documentation has been compacted.  `obol_activework.txt`
now keeps only the current migration snapshot, remaining direct legacy-view
counts, active queue, verification gates, and done criteria; detailed slice
history remains here in `obol_worklog.txt`.  This restores activework to its
intended role as a short first-read file instead of a growing historical
archive.

The pivot guard now has a central production allowlist for direct
`rt/view_legacy_bsg.h` includes.  The detailed boundary-specific CMake checks
remain in place, but the direct legacy-view owner count now has one obvious
list to update as MGED/TclCAD/libdm/qtcad/librt files migrate.  A compiled
O3 scanner remains a reasonable future optimization for repetitive
file/token checks if the CMake guard becomes a bottleneck; it was not added
in this pass because the guard still completes comfortably under the
180-second cap and the current script contains many semantic checks that are
not worth reimplementing prematurely.

Current status reporting now calls out libbsg retirement readiness explicitly:
the migration is not retire-ready while the 2 direct production
`rt/view_legacy_bsg.h` owners, qtcad's remaining BSG-shaped draw bridge
header, and guarded target dependency rows remain.  The app/shell and qtcad
direct legacy-view thresholds are now met: TclCAD, MGED, GSH, and qtcad have
zero direct legacy-view header owners.  Phase 5 now owns the remaining lower
display/backend and bridge cleanup.

The previous GED bridge cleanup moved a focused set of
`src/libged/ged_view_legacy.cpp` public GED retained-view facade operations
from BSG-named RT context helpers to neutral `rt_view_context_*` helpers:
refresh request, display-manager lookup, dimensions, width/height,
framebuffer-mode reads, and screen-to-view conversion.  The source remains a
direct `rt/view_legacy_bsg.h` owner because it still owns transitional GED
view storage plus retained BSG compatibility adapters, but the migrated public
facade operations now use the same neutral RT context boundary as libdm and
qtcad.  The pivot guard requires those neutral calls in the GED bridge and
rejects the old BSG-named helper forms from returning there.  Verification
passed `ninja -C build libged.so bin/mged test_qtcad_obol_draw_sync qsketch`.
The focused guard/hygiene/GED/qtcad/MGED smoke gate
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.33 seconds.  The longest single test was
`libbsg_api_hygiene` at 41.05 seconds; `brlobol_pivot_guard` completed in
15.49 seconds, `ged_test_drawing_quad` in 12.69 seconds, and
`dm_backend_draw_item` in 0.13 seconds.

The previous qtcad bridge cleanup moved a focused set of `QgLegacyView.cpp`
retained-view operations from BSG-named RT context helpers to neutral
`rt_view_context_*` helpers: dimensions, width/height, refresh request,
display-manager lookup, framebuffer-mode read, and screen-to-view conversion.
`QgLegacyView.cpp` remains a direct `rt/view_legacy_bsg.h` owner because it
still owns retained qtcad feature, pick, polygon, draw, view-set, DM, and
framebuffer bridge operations, but the migrated operations now follow the same
neutral RT context boundary introduced for libdm.  The pivot guard requires
the neutral qtcad calls and rejects the old BSG-named forms from returning to
that bridge.  Verification passed
`ninja -C build libqtcad.so test_qtcad_obol_draw_sync qsketch` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|ged_test_qged_swrast)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 15.76 seconds.  `brlobol_pivot_guard` completed in
15.49 seconds and both qtcad smoke tests completed in 0.11 seconds.

The previous libdm bridge cleanup moved `src/libdm/view.c` off direct
`rt/view_legacy_bsg.h` ownership.  Libdm's view facade now includes neutral
`rt/view.h` and routes retained-context detection, dimensions, refresh
requests, display-manager lookup, label screen-to-view conversion, and
framebuffer mode reads through neutral `rt_view_context_*` helpers.  Those
helpers are declared in `include/rt/view.h`, implemented in
`src/librt/view_legacy_bsg.c`, and covered by `rt_view_legacy_bsg`; retained
BSG render-item/backend draw behavior in `src/libdm/view.c` remains Phase 5
migration work.

The central direct legacy-view production allowlist is now 3 files:
`src/libged/ged_view_legacy.cpp`, `src/libqtcad/QgLegacyView.cpp`, and
`src/librt/view_legacy_bsg.c`.  Verification for the libdm direct
legacy-view include retirement passed
`ninja -C build rt_view_legacy_bsg lib/librt.so lib/libdm.so libexec/dm/libdm-swrast.so libexec/dm/libdm-tkswrast.so libexec/dm/libdm-qtgl.so`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg)$" --timeout 180 --output-on-failure`
completed 2/2 passing in 15.49 seconds.  `brlobol_pivot_guard` completed in
15.43 seconds and `rt_view_legacy_bsg` completed in 0.02 seconds.
The broader focused backend/GED/qtcad/MGED smoke gate then passed
`ninja -C build test_dm_backend_draw_item libged.so libqtcad.so bin/mged qsketch test_qtcad_obol_draw_sync rt_view_legacy_bsg`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(qged_swrast|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 13/13 passing in 80.57 seconds.  The longest single test was
`libbsg_api_hygiene` at 40.99 seconds; `brlobol_pivot_guard` completed in
15.82 seconds, `ged_test_drawing_quad` in 12.66 seconds, and
`dm_backend_draw_item` in 0.12 seconds.

The previous GED draw-view RT-adapter consolidation moved RT legacy-view wrapper
delegation out of `src/libged/bsg_ged_draw_view.c` and into the existing
private GED retained-view bridge, `src/libged/ged_view_legacy.cpp`.
`bsg_ged_draw_view.c` no longer includes `rt/view_legacy_bsg.h`; it now owns
the retained BSG draw feature, selection, HUD, overlay, and polygon adapter
logic while the RT-backed view snapshots, LoD policy, autoview, snap,
independent-scope, view-set, and mesh-LoD adapters live with the GED retained
view lifecycle/storage bridge.

The pivot guard now rejects direct `rt/view_legacy_bsg.h` ownership and direct
RT legacy-view adapter calls from returning to `bsg_ged_draw_view.c`, while
requiring the moved RT adapter tokens in `ged_view_legacy.cpp`.  Moving the
snap helper from C to C++ exposed one FreeBSD/clang warning-as-error:
`struct bsg_snap_result sres = {0}` was not accepted under
`-Wmissing-field-initializers`, so the helper now uses
`bsg_snap_result_init(&sres)` before calling the retained snap adapter.

Verification for the GED draw-view RT-adapter consolidation passed
`ninja -C build lib/libged.so`, then
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged lib/libqtcad.so qsketch`.
The pivot guard passed with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.54 seconds.  The focused GED/qtcad/MGED smoke set
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|refresh|knob))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 16/16 passing in 176.21 seconds.  The longest single test was
`ged_test_state_services` at 95.46 seconds, with `libbsg_api_hygiene` at
40.92 seconds and `brlobol_pivot_guard` at 15.40 seconds; no member crossed
the 180-second per-test cap.

The previous GED private-storage cleanup moved the transitional retained active
view, view set, and GED-owned view cleanup table out of public `struct ged`
and into private `struct ged_impl`.  `include/ged/defines.h` no longer
includes `bsg/defines.h` or exposes `struct bsg_view`, `bsg_view_set`,
`ged_gvp`, `ged_views`, or `ged_free_views`; `src/libged/ged_private.h` owns
those temporary BSG-backed fields until the storage is replaced with
Obol/libbrlobol state.  `src/libged/ged_view_legacy.cpp` now resolves the
private implementation pointer before touching retained view storage, keeping
public callers on the existing opaque GED view-context functions.

The pivot guard now rejects public GED retained view-storage exposure in
`include/ged/defines.h`, requires the private storage tokens in
`src/libged/ged_private.h`, and keeps direct source reach-through confined to
`src/libged/ged_view_legacy.cpp`.  Removing the public `bsg/defines.h`
transitive include exposed one FreeBSD/clang build issue in `src/mged/edsol.c`:
the file had inherited the old `UP`/`DOWN` edit-state macros from BSG.  That
dependency is gone; `edsol.c` now uses local `MGED_OEDIT_APPLY_CONTINUE` and
`MGED_OEDIT_APPLY_FINISH` enum values.

Verification for the GED private-storage cleanup passed
`ninja -C build lib/libged.so`, then
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged lib/libqtcad.so qsketch`.
The pivot guard passed with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.44 seconds.  Because the public `struct ged` layout changed, the
affected test executables were rebuilt with
`ninja -C build ged_test_state_services ged_test_draw ged_test_quad ged_test_mged_bsg ged_test_ged_draw_scene ged_test_view_independent ged_test_view_command test_qtcad_obol_draw_sync ged_test_qged_swrast rt_view_legacy_bsg test_bsg_api_hygiene`;
after that, the focused GED/qtcad/MGED smoke set
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|refresh|knob))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 16/16 passing in 176.80 seconds.  The longest single test was
`ged_test_state_services` at 95.99 seconds, with `libbsg_api_hygiene` at
40.96 seconds; no member crossed the 180-second per-test cap.

The previous GED view-storage helper cleanup reduced direct public GED retained
view-field reach-through without changing storage layout.  `include/ged/defines.h`
now declares `ged_view_context_owned_add`, implemented in
`src/libged/ged_view_legacy.cpp`, so GED-owned opaque view contexts are
registered for cleanup through a neutral helper instead of direct
`gedp->ged_free_views` insertions.  MGED setup, TclCAD command view creation,
GED `dm attach`, and the draw tests now use that helper.  The retained draw
bridge also gained private helpers for active-view lookup/set, GED view-list
lookup, and GED recycle-pool lookup; draw tree, transaction, scene-root,
record, overlay, and `ged_rt_fb_refresh` call sites no longer read or write
`gedp->ged_gvp` or `gedp->ged_views` directly.  The actual BSG storage fields
still remain in public `struct ged`, so this is positioning work for moving
storage behind private GED state rather than the final storage migration.

The same batch added `rt_view_set_recycle_pool_bsg` to the RT legacy view
adapter so GED's storage owner can route recycle-pool access through the RT
compatibility surface rather than pulling lower-level BSG draw-set details into
the GED facade.  `src/librt/tests/view_legacy_bsg.c` now covers the wrapper
against `bsg_set_fsos`.  The pivot guard now requires the new helper tokens,
keeps the direct legacy-view include allowlist at 5 files, and rejects direct
`ged_gvp`, `ged_views`, or `ged_free_views` reach-throughs outside
`src/libged/ged_view_legacy.cpp`.

Verification for the GED view-storage helper cleanup passed
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged lib/libqtcad.so qsketch`.
The pivot guard passed with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.38 seconds.  The focused GED/qtcad/MGED smoke set
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|refresh|knob))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 16/16 passing in 178.10 seconds.  The longest single test was
`ged_test_state_services` at 97.32 seconds, with `libbsg_api_hygiene` at
40.96 seconds; no member crossed the 180-second per-test cap.  After rebuilding
the updated RT adapter test with `ninja -C build rt_view_legacy_bsg`,
`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`
passed in 0.02 seconds.

The latest GED bridge-source extraction moved the retained GED view-context
facade implementation and default-view lifecycle setup/free out of
`src/libged/ged.cpp` into the dedicated bridge source
`src/libged/ged_view_legacy.cpp`.  Core `ged.cpp` no longer directly includes
`rt/view_legacy_bsg.h`; it calls private `ged_view_legacy_state_init/free`
helpers.  The central direct legacy-view allowlist now lists
`src/libged/ged_view_legacy.cpp` instead of `src/libged/ged.cpp`, so the direct
production owner count remains 5 while the core GED object implementation is
cleaner and the remaining GED legacy reach-through is isolated in bridge-owned
sources.

Verification for the GED bridge-source extraction passed
`ninja -C build lib/libged.so`, then
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged lib/libqtcad.so qsketch`.
The pivot guard passed with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.29 seconds.  The focused GED/qtcad/MGED smoke set
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|refresh|knob))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 16/16 passing in 175.15 seconds.  The longest single test was
`ged_test_state_services` at 94.55 seconds, with `libbsg_api_hygiene` at
40.91 seconds; no member crossed the 180-second per-test cap.

The latest RT edit-adapter centralization retired the standalone
`src/librt/edit_legacy_bsg.cpp` source.  The transitional `rt_edit_*_bsg`
entry points remain declared in `include/rt/edit_legacy_bsg.h`, but their
implementation now lives in `src/librt/view_legacy_bsg.c` beside the retained
view snapshot adapters.  The pivot guard now fails if the old source file or
its `LIBRT_SOURCES` row returns, and it requires the edit adapter entry points
to remain centralized in `view_legacy_bsg.c`.  The production direct
legacy-view include scan now reports 5 non-test owners total.

Verification for the RT edit-adapter centralization passed
`ninja -C build lib/librt.so`, then
`ninja -C build lib/librt.so lib/libqtcad.so bin/mged`, and
`ninja -C build qsketch`.  The pivot guard passed with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.33 seconds.  The edit-focused smoke set
`ctest --test-dir build -R "^(rt_view_legacy_bsg|ged_test_edit|test_qtcad_obol_edit_preview|ged_test_qged_swrast|regress-mged_(knob|prim_edit|rot_edit|sca_edit|tra_edit))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 9/9 passing in 2.73 seconds.  The lower-level hygiene check
`ctest --test-dir build -R "^libbsg_api_hygiene$" -E "^slow-" --timeout 180 --output-on-failure`
passed in 40.93 seconds.

The previous libdm backend batch moved `src/libdm/qtgl/dm-qtgl.cpp`,
`src/libdm/swrast/dm-swrast.cpp`, and
`src/libdm/tkswrast/dm-tkswrast.cpp` off direct `rt/view_legacy_bsg.h`.
`include/dm/view.h` now exposes neutral `dm_view_context_*` helpers and
`DM_VIEW_REFRESH_*` flags for retained-context rejection, swrast dimension
sync, and tkswrast refresh/dimension requests.  The temporary delegation to
the retained RT legacy adapter stays centralized in `src/libdm/view.c`, which
is now the only libdm production source that directly includes the legacy view
adapter.

The pivot guard now removes the three libdm backend files from the central
direct legacy-view owner allowlist, requires the new libdm facade tokens in
those backend files, and rejects direct RT legacy-view includes/calls, typed
`struct bsg_view`, direct BSG includes, direct BSG context checks, and direct
RT/BSG refresh or dimension access from returning there.  The production
direct legacy-view include scan now reports 6 non-test owners total: the GED
bridge files, `src/libdm/view.c`, the qtcad retained bridge, and the RT legacy
adapter implementation files.

Verification for the libdm backend facade batch passed
`ninja -C build lib/libdm.so libexec/dm/libdm-swrast.so libexec/dm/libdm-tkswrast.so libexec/dm/libdm-qtgl.so`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.26 seconds, and the backend-focused smoke set
`ctest --test-dir build -R "^(dm_backend_draw_item|ged_test_qged_swrast|test_qtcad_obol_draw_sync|ged_test_(mged_bsg|drawing_basic|drawing_quad))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 6/6 passing in 21.08 seconds.  The longest single test was
`ged_test_drawing_quad` at 12.60 seconds.  Low-level compatibility checks
`ctest --test-dir build -R "^(libbsg_api_hygiene|rt_view_legacy_bsg)$" -E "^slow-" --timeout 180 --output-on-failure`
completed 2/2 passing in 41.00 seconds; `libbsg_api_hygiene` completed in
40.93 seconds.

The previous GSH batch moved `src/gtools/gsh/gsh.cpp` off direct
`rt/view_legacy_bsg.h`.  The geometry shell now includes `ged/view.h` and
routes active-view display hashing, cleared-state reads/writes, and retained
display-manager lookup/storage through public GED view-context facades.
`include/ged/view.h` gained `ged_view_context_cleared_get` and
`ged_view_context_hash`, with `src/libged/ged.cpp` keeping the temporary
delegation to the retained RT legacy adapter inside the GED-owned boundary.

The pivot guard now removes `src/gtools/gsh/gsh.cpp` from the central direct
legacy-view owner allowlist and transitional legacy-header list, requires the
GED facade tokens in GSH, and rejects direct RT legacy-view includes/calls,
typed `struct bsg_view`, direct BSG includes, direct BSG view hash or
cleared-state calls, and direct retained display-manager field access from
returning there.  The production direct legacy-view include scan now reports
9 non-test owners total, with TclCAD, MGED, and GSH at zero.

Verification for the GSH facade batch passed
`ninja -C build bin/gsh lib/libged.so`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.28 seconds, and the focused smoke set
`ctest --test-dir build -R "^(libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|mged_bsg)|regress-mged_(view|refresh|draw))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 8/8 passing in 141.51 seconds.  The longest single test was
`ged_test_state_services` at 98.53 seconds, below the 180 second cap;
`libbsg_api_hygiene` completed in 41.00 seconds.  `git diff --check` passed,
the targeted GSH legacy-token scan was clean, the direct non-test production
legacy-view owner scan listed the expected 9 files, and no project-matching
temporary files were found in `/tmp`.

The previous TclCAD batch moved `src/libtclcad/commands.c` off direct
`rt/view_legacy_bsg.h`.  The command path now includes `ged/view.h` plus
neutral `rt/view.h` and routes display-manager access, dimension sync,
view-name get/set, unit-conversion updates, model/view snapshot reads,
screen-to-view conversion, previous-mouse get/set, interactive rectangle
reads, LoD policy updates, grid reads, coord get/set, snap-lines reads,
center-line snap, zclip get/set, temporary view creation/free, view-set add,
callback-table binding, and Tcl snap preparation/application through public
GED view-context facades.  `include/ged/view.h` gained
`ged_view_context_name_set`, `ged_view_context_callbacks_set`, and
`ged_view_context_center_linesnap`, with `src/libged/ged.cpp` keeping the
temporary delegation to the retained RT legacy adapter inside the GED-owned
boundary.

The pivot guard now removes `src/libtclcad/commands.c` from the central direct
legacy-view owner allowlist and transitional include lists, requires the GED
command/view/snap facade tokens in `commands.c`, and rejects direct RT
legacy-BSG view-context calls, direct retained view fields, typed BSG helper
signatures, or raw retained Tcl state access from returning there.  The
production direct legacy-view include scan now reports 10 non-test owners
total, with TclCAD and MGED both at zero.

Verification for the commands batch passed
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
in 15.52 seconds, and the focused smoke set
`ctest --test-dir build -R "^(libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|mged_bsg)|regress-mged_(view|refresh|draw))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 8/8 passing in 134.66 seconds.  The broad focused
guard/hygiene/MGED-view/drawing gate completed 28/28 passing in 187.93
seconds; the longest single test was `ged_test_state_services` at
93.87 seconds, followed by `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 15.29 seconds, and `ged_test_drawing_quad` at
12.71 seconds.  The quick standalone `simulate_test` plus `regress-mged_arb`
check completed 2/2 passing in 1.60 seconds.

The previous TclCAD batch moved `src/libtclcad/mouse.c` off direct
`rt/view_legacy_bsg.h`.  The mouse input path now includes `ged/view.h` plus
neutral `rt/view.h` and routes display-manager lookup, dimension sync,
view-name reads, width/height fallback reads, size/scale/matrix/rotation
reads, screen-to-view conversion, previous-mouse get/set, mouse delta settings,
and Tcl snap preparation/application through public GED view-context facades.
The neutral `rt_view_mouse_delta_settings` snapshot moved from the legacy BSG
adapter header into `include/rt/view.h`, and `include/ged/view.h` gained
`ged_view_context_previous_mouse_get` and
`ged_view_context_mouse_delta_settings_get` facades while `src/libged/ged.cpp`
keeps the temporary delegation to the retained RT legacy adapter inside the
GED-owned boundary.

The MGED core headers, `mged.c`, `dozoom.c`, `chgview.c`, and `cmd.cpp` are no
longer direct legacy-view include owners.  `src/mged/mged.h` dropped the stale
`rt/view_legacy_bsg.h` include,
`src/mged/mged_dm.h` now routes shared ADC, grid, view-size, view-scale, and
view-settings helpers through public GED view-context facades, and
`src/mged/mged.c` routes model2view, update, zclip, refresh state, scale, ADC,
and grid access through GED facades.  `src/mged/dozoom.c` now routes
draw-frame display-manager sync, perspective/projection state, edit-matrix
staging, frame-revision reads, and drawn-count writes through GED facades.
`src/mged/chgview.c` now routes view-ring save/restore, edit/slew reads,
scale baseline updates, knob-state synchronization, coordinate staging,
zoom-refresh LoD policy inspection, and status-query reads through GED
view-context facades.  `src/mged/cmd.cpp` now routes simulation view argument
injection, GED command display-manager handoff, command view dimension sync,
command staging/cache copy and restore, temporary staging-view creation/free,
retained knob-state synchronization, and mutation hashing through GED
view-context facades.  The retained `struct bsg_view *` storage is still
forward-declared in `mged_dm.h` until retained MGED view storage itself moves
behind an opaque or Obol-backed view context.  The cleanup also moved MGED
refresh requests to neutral
`GED_VIEW_REFRESH_*` flags and removed the old `owner` display-manager macro,
which had been relying on legacy headers being included before MGED's macro
aliases in unity builds.

The latest MGED batch moved `src/mged/cmd.cpp` off direct
`rt/view_legacy_bsg.h`.  Public GED view-context facades now cover temporary
view creation/free, scale-state copy/restore, local unit conversion getters,
model/pmodel matrix setters, AET state restore, and retained knob hashing.
The pivot guard now removes `cmd.cpp` from the direct legacy-view owner
allowlist and transitional view-scope lists, requires the GED facade tokens for
the command staging/cache/display-manager/simulation paths, and rejects direct
RT legacy-BSG view-context calls from returning there.  Verification for the
batch passed `ninja -C build lib/libged.so bin/mged` and
`brlobol_pivot_guard` in 15.38 seconds, the focused 28-test
guard/MGED/drawing regression set in 185.79 seconds with
`ged_test_state_services` the longest single test at 91.33 seconds,
`simulate_test` in 1.13 seconds, and `regress-mged_arb` in 0.42 seconds.

The latest MGED batch moved `src/mged/chgview.c` off direct
`rt/view_legacy_bsg.h`.  Public GED view-context facades now cover the scale
baseline getters/setters and retained knob-state synchronization that
`chgview.c` still needs during the transitional view backend.  The pivot guard
now removes `chgview.c` from the direct legacy-view owner allowlist, requires
the GED facade tokens for its status/edit/slew/knob/LoD paths, and rejects
direct RT legacy-BSG view-context calls from returning there.  Verification for
the batch passed `ninja -C build lib/libged.so bin/mged`, `brlobol_pivot_guard`
in 15.16 seconds, and the focused 28-test guard/MGED/drawing regression set in
186.32 seconds with `ged_test_state_services` the longest single test at
92.32 seconds.

The MGED setup/attach lifecycle boundary now uses public GED view-context
facades instead of direct `rt/view_legacy_bsg.h` access.  `src/mged/setup.c`
creates and registers the initial view with `ged_view_context_create_with_set`
and `ged_view_set_context_add`, installs `mged_view_callback` through
`ged_view_context_update_callback_set`, and reads the initial center through
`ged_view_context_center_get`.  `src/mged/attach.c` now uses GED facades for
ADC state writes, DM scale-storage handoff, copied-view creation/update
callback setup, and copied-display LoD policy adjustment.  The public
`include/ged/view.h` surface now has opaque create-copy and update-callback
facades, with the raw copy allocation contained in the RT legacy adapter.

The pivot guard now treats `setup.c` and `attach.c` as migrated facade
consumers: it requires the new GED lifecycle/callback/LoD/ADC/scale-storage
tokens and rejects direct legacy-header inclusion, local `struct bsg_view`
allocation, direct RT legacy lifecycle/callback/LoD/ADC/scale-storage calls,
and raw BSG view-center access from returning.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 6 in `src/mged`, 13 in
`src/libtclcad`, and 29 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 29.62 seconds, `ninja -C build bin/mged`
completing successfully, and focused guard/hygiene/MGED-view/drawing
verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|overlay|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 28/28 passing in 186.65 seconds.  The longest member was
`ged_test_state_services` at 93.48 seconds, followed by
`libbsg_api_hygiene` at 40.91 seconds, `brlobol_pivot_guard` at
14.84 seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`regress-gchecker` at 8.32 seconds.  The previously used extra MGED ARB
regression was also checked separately with
`ctest --test-dir build -R "^regress-mged_arb$" --timeout 180 --output-on-failure`,
completing 1/1 passing in 0.46 seconds real time with the test itself at
0.41 seconds.

A one-hundred-forty-sixth MGED display-manager event facade cleanup moved the
remaining `src/mged/dm-generic.c` retained-view reads and simple state writes
behind public GED view-context facades.  `include/ged/view.h` now exposes
`ged_view_context_refresh_request`, `ged_view_context_zclip_set`,
`ged_view_context_lod_policy_get`, and
`ged_view_context_lod_policy_apply`, with `src/libged/ged.cpp` keeping the
temporary delegation to the retained RT legacy adapter inside the GED-owned
boundary.  `dm-generic.c` now uses GED facades for zoom-refresh LoD policy
inspection, mouse/ADC center, scale, model-to-view, and view-to-model reads,
display-setting refresh requests, zclip sync, and display-manager handoff.

`dm-generic.c` dropped `rt/view_legacy_bsg.h`.  The pivot guard now requires
the GED facade tokens in this MGED boundary, splits `dm-generic.c` out of the
older legacy display-manager and refresh classifications, and rejects stale
`rt/view_legacy_bsg.h`, direct retained view-field reads/writes, and direct
RT legacy adapter calls from returning there.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 8 in `src/mged`, 13 in
`src/libtclcad`, and 31 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 14.79 seconds, and `ninja -C build bin/mged`.

Focused guard/hygiene/MGED-view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|overlay|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob|arb)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 29/29 passing in 189.44 seconds.  The longest member was
`ged_test_state_services` at 95.63 seconds, followed by
`libbsg_api_hygiene` at 40.96 seconds, `brlobol_pivot_guard` at
14.80 seconds, `ged_test_drawing_quad` at 12.66 seconds,
`regress-gchecker` at 8.32 seconds, and `ged_test_drawing_basic` at
7.51 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-forty-fifth MGED highlight/edit/RTIF facade cleanup moved
another app-side retained-view batch behind public GED view-context facades.
`include/ged/view.h` now exposes `ged_view_context_refresh_drawn_count_get`,
with `src/libged/ged.cpp` keeping the temporary delegation to the retained RT
legacy adapter inside the GED-owned boundary.  `src/mged/usepen.c` now uses
GED facades for highlight refresh drawn-count reads and view-centered
transform center reads.  `src/mged/edsol.c` now uses GED scale,
model-to-view, and view-to-model facades for edit-solid view snapshot reads.
`src/mged/rtif.c` now uses GED facades for RT command display-manager handoff
and rmats view scale, rotation, center, and view-to-model updates.

All three MGED files dropped `rt/view_legacy_bsg.h`; `usepen.c` keeps neutral
RT view-coordinate constants on `rt/view.h`.  The pivot guard now requires the
GED facade tokens in these MGED boundaries, removes `usepen.c` from older
legacy refresh/view-scope classifications, and rejects stale
`rt/view_legacy_bsg.h`, direct retained view-field reads/writes, and direct
RT legacy adapter calls from returning there.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 9 in `src/mged`, 13 in
`src/libtclcad`, and 32 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 14.88 seconds, and `ninja -C build bin/mged`.

Focused guard/hygiene/MGED-view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|overlay|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob|arb|edit|rt|rmats)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 29/29 passing in 185.96 seconds.  The longest member was
`ged_test_state_services` at 92.15 seconds, followed by
`libbsg_api_hygiene` at 40.92 seconds, `brlobol_pivot_guard` at
14.82 seconds, `ged_test_drawing_quad` at 12.73 seconds,
`regress-gchecker` at 8.51 seconds, and `ged_test_drawing_basic` at
7.58 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-forty-fourth MGED view-state facade cleanup moved another
app-side retained-view batch behind public GED view-context facades.
`include/ged/view.h` now exposes `ged_view_context_coord_set`, with
`src/libged/ged.cpp` keeping the temporary delegation to the retained RT
legacy adapter inside the GED-owned boundary.  `src/mged/overlay.c` now uses
`ged_view_context_display_manager_set` for overlay display-manager handoff.
`src/mged/scroll.c` now uses `ged_view_context_scale_get` for absolute-slider
scale reads.  `src/mged/set.c` now uses GED model-to-view, center, scale,
perspective, coord, and rotate-about facades for set-command view reads and
simple setters.  `src/mged/axes.c` now uses GED info, model-to-view, and
rotation facades for edit/model/view axes overlays.  `src/mged/adc.c` now
uses GED scale, model-to-view, and view-to-model facades while keeping ADC
math on neutral `rt/view.h` helpers.

All five MGED files dropped `rt/view_legacy_bsg.h`; `axes.c` and `adc.c`
keep neutral RT view/axes/ADC records on `rt/view.h`.  The pivot guard now
requires the GED facade tokens in these MGED boundaries and rejects stale
`rt/view_legacy_bsg.h`, direct retained view-field reads/writes, and direct
RT legacy adapter calls from returning there.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 12 in `src/mged`, 13 in
`src/libtclcad`, and 35 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`.

Focused guard verification with
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.65 seconds.

Focused guard/hygiene/MGED-view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|axes|overlay|scroll|set|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 28/28 passing in 187.14 seconds.  The longest member was
`ged_test_state_services` at 94.18 seconds, followed by
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at
14.74 seconds, `ged_test_drawing_quad` at 12.67 seconds,
`regress-gchecker` at 8.23 seconds, and `ged_test_drawing_basic` at
7.51 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-forty-third MGED view-math facade cleanup moved another small
group of app-side retained-view reads and writes behind public GED
view-context facades.  `src/mged/edpipe.c` now uses
`ged_view_context_view2model_get` for pipe pick direction setup.
`src/mged/rect.c` now uses `ged_view_context_center_get`,
`ged_view_context_model2view_get`, and `ged_view_context_view2model_get` for
rectangle zoom calculations.  `src/mged/doevent.c` now uses
`ged_view_context_scale_get`, `ged_view_context_center_get`, and
`ged_view_context_view2model_get` for X11 motion-event handling.
`src/mged/grid.c` now uses GED view-context facades for grid auto-size view
info, draw/snap scale and model-to-view reads, keypoint snap matrix reads,
view-center snap center/matrix reads and writes, and view snap center and
view-to-model reads/writes.

All four files dropped `rt/view_legacy_bsg.h`; `grid.c` and `doevent.c` keep
neutral RT view/grid records on `rt/view.h`.  The pivot guard now requires the
GED facade tokens in these MGED boundaries and rejects stale
`rt/view_legacy_bsg.h`, direct retained view-field reads/writes, and direct
RT legacy adapter calls from returning there.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 17 in `src/mged`, 13 in
`src/libtclcad`, and 40 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`.

Focused guard/hygiene/MGED-view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(arb|draw|refresh|draw_e_erase_d_who|view|zoom|size|setview|rot_view|sca_view|tra_view|knob)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 27/27 passing in 186.19 seconds.  The longest member was
`ged_test_state_services` at 93.61 seconds, followed by
`libbsg_api_hygiene` at 40.88 seconds, `brlobol_pivot_guard` at
14.78 seconds, `ged_test_drawing_quad` at 12.67 seconds,
`regress-gchecker` at 8.33 seconds, and `ged_test_drawing_basic` at
7.49 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-forty-second MGED view snapshot facade cleanup moved another
small app-side retained-view batch behind public GED view-context facades.
`src/mged/buttons.c` now uses `ged_view_context_scale_get`,
`ged_view_context_rotation_get`, `ged_view_context_center_get`,
`ged_view_context_scale_set`, `ged_view_context_rotation_set`, and
`ged_view_context_center_vec_set` for saved-view capture and restore, while
keeping ADC state on neutral `rt/view.h`.  `src/mged/titles.c` now uses
`ged_view_context_info_get`, `ged_view_context_center_get`,
`ged_view_context_aet_get`, `ged_view_context_perspective_get`, and
`ged_view_context_scale_get` for title/status display, while keeping
`rt_view_info` and ADC state on neutral `rt/view.h`.  `src/mged/chgmodel.c`
now uses GED center and scale facades for make-default placement, and
`src/mged/plot.c` uses the GED rotation facade for area export.

All four files dropped `rt/view_legacy_bsg.h`.  The pivot guard now requires
the GED facade tokens in those MGED boundaries and rejects stale
`rt/view_legacy_bsg.h`, direct retained view-field reads/writes, and direct
RT legacy adapter calls from returning there.  Direct non-test
`rt/view_legacy_bsg.h` users are now down to 21 in `src/mged`, 13 in
`src/libtclcad`, and 44 across the broader production source scan.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`.

Focused guard/hygiene/MGED-view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(plot|status|view|draw|refresh|draw_e_erase_d_who|saveview|setview|rot_view|sca_view|tra_view|size|zoom|knob|adc)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 30/30 passing in 193.70 seconds.  The longest member was
`ged_test_state_services` at 99.69 seconds, followed by
`libbsg_api_hygiene` at 40.96 seconds, `brlobol_pivot_guard` at
14.76 seconds, `ged_test_drawing_quad` at 12.58 seconds,
`regress-gchecker` at 8.48 seconds, and `ged_test_drawing_basic` at
7.52 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-forty-first libged display-manager command facade cleanup added
public GED view-context facades for retained view creation with an existing
view set, view-set context registration, display-manager set, width/height
reads, dimension writes, and scale-storage handoff.  `include/ged/view.h` now
exposes `ged_view_context_create_with_set`, `ged_view_set_context_add`,
`ged_view_context_display_manager_set`, `ged_view_context_width_get`,
`ged_view_context_height_get`, `ged_view_context_dimensions_set`, and
`ged_view_context_scale_storage_get`; `src/libged/ged.cpp` keeps the temporary
delegation to the retained RT legacy adapter inside the GED-owned boundary.

`src/libged/dm/dm.c` now uses GED facades for DM name lookup, current-DM
resolution, DM list enumeration, attach-time duplicate-view diagnostics,
attach-view creation and registration, fallback dimensions, scale-storage
handoff, and display-manager binding.  The file no longer includes
`rt/view_legacy_bsg.h`, no longer calls direct RT legacy view-context
adapters, and no longer owns local typed `struct bsg_view` allocation for
attach-view creation.  Production `src/libged` direct legacy-header ownership
is now confined to the bridge-owned `src/libged/ged.cpp` and
`src/libged/bsg_ged_draw_view.c` files.

The pivot guard now requires the GED display-manager, view-name,
create/register, dimension, and scale-storage facade tokens in `dm.c`, rejects
stale `rt/view_legacy_bsg.h` and direct RT legacy adapter calls from returning
there, and preserves the remaining bridge-owned RT legacy adapter requirements
inside `ged.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/DM/view/drawing/RT-adapter verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(bots|nirt|gchecker|gdiff|mged_(autoview|draw_e_erase_d_who|draw|erase|loadview|overlay|plot|ps|refresh|view|who|zoom|knob)))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 30/30 passing in 189.09 seconds.  The longest member was
`ged_test_state_services` at 92.60 seconds, followed by
`libbsg_api_hygiene` at 40.99 seconds, `brlobol_pivot_guard` at
14.59 seconds, `ged_test_drawing_quad` at 12.67 seconds,
`regress-gchecker` at 8.44 seconds, and `ged_test_drawing_basic` at
7.52 seconds.  All tests stayed below the 180 second per-test cap.

A one-hundred-fortieth libged ADC/knob facade cleanup added public GED
view-context facades for retained ADC state get/set and view knob value
snapshots, rate reset, calibration, command processing, translation,
rotation, and rate-flag updates.  `include/ged/view.h` now exposes
`ged_view_context_adc_state_get`, `ged_view_context_adc_state_set`,
`ged_view_context_knob_values_get`, `ged_view_context_knobs_reset`,
`ged_view_context_knobs_calibrate`, `ged_view_context_knobs_cmd_process`,
`ged_view_context_knobs_translate`, `ged_view_context_knobs_rotate`, and
`ged_view_context_knobs_update_rate_flags`; `src/libged/ged.cpp` keeps the
temporary delegation to the retained RT legacy adapter inside the GED-owned
boundary.

`src/libged/adc/adc.c` now uses GED scale, model-to-view, view-to-model, and
ADC-state facades while keeping ADC math on neutral `rt/view.h` helpers.
`src/libged/view/knob.c` now uses GED facades for knob-value snapshots,
unit-conversion sync, coordinate defaults, rate reset, calibration, knob
command processing, translation, keypoint-origin lookup, rotation, and
rate-flag updates, and uses the neutral `RT_VIEW_KNOBS_RATE` category.  Both
files dropped `rt/view_legacy_bsg.h`.  The neutral
`rt_view_knob_values` snapshot moved from `include/rt/view_legacy_bsg.h` to
`include/rt/view.h`.

The pivot guard now requires the GED ADC and knob facade tokens in the
migrated command files, rejects stale `rt/view_legacy_bsg.h` includes there,
and rejects direct RT legacy ADC, matrix, knob, unit-conversion, and
BSG-suffixed knob-category calls from returning to those command boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/ADC/knob/view/drawing/RT-adapter verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene|view_independent)|regress-mged_(view|draw|refresh|draw_e_erase_d_who|erase|overlay|plot|ps|adc|knob)|regress-(nirt|gchecker|gdiff))$" --timeout 180 --output-on-failure`
completed 21/21 passing in 171.91 seconds.  The longest member was
`ged_test_state_services` at 93.68 seconds, followed by
`libbsg_api_hygiene` at 40.91 seconds and `brlobol_pivot_guard` at
14.59 seconds.  `regress-gchecker` completed in 8.34 seconds,
`ged_test_drawing_basic` in 7.50 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `regress-nirt` in 0.66 seconds, `regress-mged_adc` in
0.40 seconds, `regress-mged_knob` in 0.41 seconds, and
`rt_view_legacy_bsg` in 0.02 seconds.

A one-hundred-thirty-ninth libged view/autoview/object facade cleanup added
public GED view-context facades for retained view-name reads,
independent-scope lifecycle operations, autoview operations, and mouse-state
updates.  `include/ged/view.h` now exposes `ged_view_context_name_get`,
`ged_view_context_independent_scope_is_null`,
`ged_view_context_independent_scope_destroy`, `ged_view_context_autoview`,
`ged_view_context_autoview_bounds`, and
`ged_view_context_mouse_state_set`; `src/libged/ged.cpp` keeps the temporary
delegation to the retained RT legacy adapter inside the GED-owned boundary.

`src/libged/view/view.c`, `src/libged/view/autoview2.cpp`, and
`src/libged/view/objs.cpp` now use GED view-context facades for view names,
independent-view checks, independent-scope creation/destruction,
model-to-view and view-info reads, autoview updates, and mouse-state updates
instead of direct RT legacy adapters.  All three files dropped
`rt/view_legacy_bsg.h`.  The autoview scale default moved to neutral
`include/rt/view.h`; `include/rt/view_legacy_bsg.h` now keeps only a
compatibility guard for existing adapter callers and tests.

The pivot guard now requires the GED view/autoview/object facade tokens in
the migrated command files, rejects stale `rt/view_legacy_bsg.h` includes
there, and rejects direct RT legacy name, independent-scope, model-to-view,
view-info, autoview, and mouse-state calls from returning to those command
boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view/drawing/RT-adapter verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene|view_independent)|regress-mged_(view|draw|refresh|draw_e_erase_d_who|erase|overlay|plot|ps)|regress-(nirt|gchecker|gdiff)|rt_view_legacy_bsg)$" --timeout 180 --output-on-failure`
completed 19/19 passing in 172.38 seconds.  The longest member was
`ged_test_state_services` at 94.86 seconds, followed by
`libbsg_api_hygiene` at 40.99 seconds and `brlobol_pivot_guard` at
14.54 seconds.  `regress-gchecker` completed in 8.37 seconds,
`ged_test_drawing_basic` in 7.55 seconds, `ged_test_ged_draw_scene` in
1.43 seconds, `regress-nirt` in 0.66 seconds, and `rt_view_legacy_bsg`
in 0.02 seconds.

A one-hundred-thirty-eighth libged screen/plane conversion facade cleanup
added public GED view-context facades for retained screen-to-view,
screen-point, and plane reads.  `include/ged/view.h` now exposes
`ged_view_context_screen_to_view`, `ged_view_context_screen_point`, and
`ged_view_context_plane_get`; `src/libged/ged.cpp` keeps the temporary
delegation to the retained RT legacy adapter inside the GED-owned boundary.

`src/libged/polyclip.cpp`, `src/libged/view/labels.c`, and
`src/libged/view/polygons.c` now use GED view-context facades for plane,
screen-to-view, screen-point, model-to-view, and view-to-model reads instead
of calling direct RT legacy conversion adapters.  All three files dropped
`rt/view_legacy_bsg.h`.

The pivot guard now requires the GED screen/plane/matrix facade tokens in the
migrated polyclip, label, and polygon command files, rejects stale
`rt/view_legacy_bsg.h` includes there, and rejects direct RT legacy
screen-to-view, screen-point, plane, model-to-view, and view-to-model
conversion calls from returning to those command boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view/drawing verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene|view_independent)|regress-mged_(view|draw|refresh|draw_e_erase_d_who|erase|overlay|plot|ps)|regress-(nirt|gchecker|gdiff))$" --timeout 180 --output-on-failure`
completed 18/18 passing in 169.97 seconds.  The longest member was
`ged_test_state_services` at 92.36 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds and `brlobol_pivot_guard` at
14.71 seconds.  `regress-gchecker` completed in 8.51 seconds,
`ged_test_drawing_basic` in 7.54 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, `regress-nirt` in 0.66 seconds, and the selected MGED
regressions completed in about 0.41 seconds each.

A one-hundred-thirty-seventh libged draw/erase/zap view-scope facade cleanup
added public GED view-context facades for retained independent-view checks,
view clearing, and cleared-state writes.  `include/ged/view.h` now exposes
`GED_VIEW_CLEAR_DB`, `GED_VIEW_CLEAR_VIEW`, `GED_VIEW_CLEAR_LOCAL`,
`ged_view_context_is_independent`, `ged_view_context_clear`, and
`ged_view_context_cleared_set`; `src/libged/ged.cpp` translates the GED clear
flags to the retained RT/BSG clear flags inside the GED-owned delegation
point.

`src/libged/draw/draw2.cpp`, `src/libged/erase/erase2.cpp`, and
`src/libged/zap/zap2.cpp` now use those GED view-context facades and
GED-spelled clear flags instead of direct RT legacy independent, clear, and
cleared-state adapters.  All three files dropped `rt/view_legacy_bsg.h`.

The pivot guard now requires the GED view-scope facades and flags in the
migrated draw, erase, and zap command files, rejects stale
`rt/view_legacy_bsg.h` includes there, and rejects direct RT legacy
independent, clear, cleared-state, and BSG-spelled clear-flag tokens from
returning to those command boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/draw-erase-zap verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene|view_independent)|regress-mged_(view|draw|refresh|draw_e_erase_d_who|erase))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 158.79 seconds.  The longest member was
`ged_test_state_services` at 91.98 seconds, followed by
`libbsg_api_hygiene` at 40.91 seconds and `brlobol_pivot_guard` at
14.53 seconds.  `ged_test_drawing_basic` completed in 7.54 seconds,
`ged_test_ged_draw_scene` in 1.42 seconds, `ged_test_view_independent` in
0.16 seconds, `ged_test_view_command` in 0.15 seconds, and the selected MGED
regressions completed between 0.40 and 0.42 seconds each.

A one-hundred-thirty-sixth libged shared-utility stale include cleanup removed
the unused `rt/view_legacy_bsg.h` include from `src/libged/ged_util.cpp`.
The helper already routes default coordinate predicates, zclip/perspective
checks, RT eye-model view snapshots, and write-script view snapshots through
public GED view-context facades, so the legacy adapter header was no longer
needed in this utility boundary.  `src/libged/ged.cpp` remains the GED-owned
delegation point to the transitional RT legacy adapter.

The pivot guard now rejects stale `rt/view_legacy_bsg.h` includes in
`ged_util.cpp` while continuing to require the GED view-to-model, center,
rotation, zclip, and perspective facade tokens there.  The same focused batch
also leaves the NIRT guard requiring GED center, rotation, scale, and
view-to-model facade tokens and rejecting direct RT legacy NIRT snapshot
adapters.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/utility-and-NIRT verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene)|regress-(nirt|mged_(view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`
completed 11/11 passing in 163.87 seconds.  The longest member was
`ged_test_state_services` at 96.91 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds and `brlobol_pivot_guard` at
14.62 seconds.  `ged_test_drawing_basic` completed in 7.54 seconds,
`ged_test_ged_draw_scene` in 1.45 seconds, `ged_test_view_command` in
0.15 seconds, `regress-nirt` in 0.66 seconds, and the selected MGED
regressions completed between 0.40 and 0.42 seconds each.

A one-hundred-thirty-fifth libged NIRT camera facade cleanup moved the
remaining NIRT/qray view snapshot reads behind existing public GED
view-context facades.  `src/libged/nirt/nirt.cpp` now uses
`ged_view_context_center_get` for default shot-center setup,
`ged_view_context_rotation_get` for shot direction, and
`ged_view_context_view2model_get` for `vnirt` view-coordinate conversion.
The file dropped `rt/view_legacy_bsg.h`; `src/libged/ged.cpp` remains the
GED-owned delegation point to the transitional RT legacy adapter.

The pivot guard now requires the GED center, rotation, scale, and
view-to-model facade tokens in `nirt.cpp`, rejects stale
`rt/view_legacy_bsg.h` includes and direct RT legacy center, rotation, scale,
or view-to-model adapters, and continues to require qray stale-feature
cleanup through the public GED draw-view context facade.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/NIRT verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene)|regress-(nirt|mged_(view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`
completed 11/11 passing in 160.34 seconds.  The longest member was
`ged_test_state_services` at 93.36 seconds, followed by
`libbsg_api_hygiene` at 41.07 seconds and `brlobol_pivot_guard` at
14.46 seconds.  `ged_test_drawing_basic` completed in 7.52 seconds,
`ged_test_ged_draw_scene` in 1.43 seconds, `ged_test_view_command` in
0.15 seconds, `regress-nirt` in 0.66 seconds, and the selected MGED
regressions completed in 0.41 seconds each.

A one-hundred-thirty-fourth libged select facade cleanup moved
`select`/`rselect` view snapshots behind existing public GED view-context
facades.  `src/libged/select/select.c` now uses
`ged_view_context_model2view_get` for BoT point selection, visible-record
selection, and partial-selection projection.  It also uses
`ged_view_context_interactive_rect_state_get` for rectangle selection.  The
file now includes neutral `rt/view.h` for `struct rt_view_interactive_rect_state`
and dropped `rt/view_legacy_bsg.h`; `src/libged/ged.cpp` remains the
GED-owned delegation point to the transitional RT legacy adapter.

The pivot guard now requires GED matrix and interactive-rectangle facade
tokens in `select.c`, rejects stale `rt/view_legacy_bsg.h` includes and direct
RT legacy model-to-view/interactive-rectangle adapters, and no longer
preserves the older transitional select exception in the duplicate neutral
interactive-rectangle guard.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/select verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_select|selection_semantics|ged_draw_scene)|test_qtcad_obol_selection_sync|regress-mged_(view|draw|refresh|draw_e_erase_d_who))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 168.88 seconds.  The longest member was
`ged_test_state_services` at 93.45 seconds, followed by
`libbsg_api_hygiene` at 41.01 seconds and `ged_test_drawing_select` at
16.31 seconds.  `brlobol_pivot_guard` completed in 14.58 seconds,
`ged_test_selection_semantics` in 0.12 seconds,
`ged_test_ged_draw_scene` in 1.42 seconds,
`test_qtcad_obol_selection_sync` in 0.10 seconds, and the selected MGED
regressions completed between 0.41 and 0.44 seconds each.

A one-hundred-thirty-third libged primitive/subentity matrix facade cleanup
moved the compact primitive edit and nearest-subentity matrix batch behind the
existing public GED view-context matrix facades.
`src/libged/metaball/metaball.c` and `src/libged/pipe/pipe.c` now use
`ged_view_context_model2view_get` and/or `ged_view_context_view2model_get`
for point-addition and nearest-point workflows.
`src/libged/move_arb_edge/move_arb_edge.c` and `src/libged/bot/edbot.c` now
use `ged_view_context_model2view_get` for nearest ARB/BoT subentity
selection.  Those four files dropped `rt/view_legacy_bsg.h`;
`src/libged/ged.cpp` remains the GED-owned delegation point to the
transitional RT legacy adapter.

The pivot guard now requires GED matrix facade tokens in the migrated
primitive edit and subentity selection paths and rejects stale
`rt/view_legacy_bsg.h` includes plus direct RT legacy model-to-view and
view-to-model adapters from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/primitive-matrix verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|bot_dumping)|regress-(bots|mged_(3ptarb|arb|view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 153.67 seconds.  The longest member was
`ged_test_state_services` at 92.27 seconds, followed by
`libbsg_api_hygiene` at 40.92 seconds.  `brlobol_pivot_guard` completed in
14.48 seconds, `regress-bots` in 3.21 seconds, `ged_test_bot_dumping` and
`ged_test_view_command` in 0.15 seconds each, and the selected MGED
regressions completed at 0.41 seconds each.

A one-hundred-thirty-second libged faceplate-state facade cleanup added
`ged_view_context_model_axes_state_get`,
`ged_view_context_model_axes_state_set`,
`ged_view_context_view_axes_state_get`,
`ged_view_context_view_axes_state_set`,
`ged_view_context_center_dot_state_get`,
`ged_view_context_center_dot_state_set`,
`ged_view_context_scale_overlay_state_get`,
`ged_view_context_scale_overlay_state_set`,
`ged_view_context_params_state_get`, and
`ged_view_context_params_state_set` to the public GED view-context facade.
`src/libged/view/faceplate/faceplate_grid.c` now uses the existing GED grid
state get/set facades.  `src/libged/view/faceplate/faceplate_axes.c` now uses
GED facades for model and view axes records.
`src/libged/view/faceplate/faceplate.c` now uses GED facades for center-dot,
scale-overlay, and params records while keeping framebuffer mode on the GED
framebuffer-mode facade and HUD sync on the public GED draw-view context
facade.  Those three files dropped `rt/view_legacy_bsg.h`;
`src/libged/ged.cpp` remains the GED-owned delegation point to the
transitional RT legacy adapter.

The pivot guard now requires the public GED faceplate state facade tokens,
requires the GED view-context facades in the migrated libged faceplate grid,
axes, and scalar paths, and rejects stale `rt/view_legacy_bsg.h` includes
plus direct RT legacy faceplate/grid/axes state adapters from returning
there.  TclCAD and qtcad transitional faceplate checks are unchanged in this
slice.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/faceplate-state verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_faceplate|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|draw_e_erase_d_who))$" --timeout 180 --output-on-failure`
completed 10/10 passing in 151.94 seconds.  The longest member was
`ged_test_state_services` at 92.75 seconds, followed by
`libbsg_api_hygiene` at 40.96 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.50 seconds,
`ged_test_drawing_faceplate` in 1.78 seconds,
`ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, and the selected MGED regressions
completed between 0.40 and 0.41 seconds each.

A one-hundred-thirty-first libged open/loadview/preview facade cleanup added
`ged_view_context_unit_conversion_set` to the public GED view-context facade.
`src/libged/open/open.cpp` now uses the GED unit-conversion facade for active
view database unit sync.  `src/libged/draw/loadview.cpp` now uses GED facades
for rotation and center restore writes while keeping size, perspective, and
refresh behind existing GED view-context helpers.
`src/libged/draw/preview.cpp` now uses GED facades for preview camera rotation,
center, view-to-model, and refresh access.  All three files dropped
`rt/view_legacy_bsg.h`; `src/libged/ged.cpp` remains the GED-owned delegation
point to the transitional RT legacy adapter.

The pivot guard now requires the public GED unit-conversion facade token,
requires the GED view-context facades in the migrated open/loadview/preview
paths, and rejects stale `rt/view_legacy_bsg.h` includes plus direct RT legacy
unit-conversion, rotation, center, view-to-model, and update adapters from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/open-loadview-preview verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|snap_semantics|tcl_overlay_bsg|drawing_faceplate)|regress-mged_(view|draw|refresh|draw_e_erase_d_who|loadview)|test_qtcad_obol_edit_preview)$" --timeout 180 --output-on-failure`
completed 13/13 passing in 152.76 seconds.  The longest member was
`ged_test_state_services` at 93.20 seconds, followed by
`libbsg_api_hygiene` at 40.88 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.49 seconds,
`ged_test_drawing_faceplate` in 1.75 seconds,
`ged_test_snap_semantics` in 0.01 seconds,
`ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds,
`test_qtcad_obol_edit_preview` in 0.07 seconds, and the selected MGED
regressions completed between 0.40 and 0.42 seconds each.

A one-hundred-thirtieth libged snap-state facade cleanup added
`ged_view_context_snap_lines_get`, `ged_view_context_snap_lines_set`,
`ged_view_context_snap_tolerance_factor_get`, and
`ged_view_context_snap_tolerance_factor_set` to the public GED view-context
facade.  `src/libged/view/snap.c` now uses GED facades for snap-tolerance
reads/writes and model-to-view/view-to-model matrix reads before its existing
public GED draw-view snap-candidate lookup.  `src/libged/view/data_lines.c`
now uses GED facades for snap-line enablement while keeping retained
data-line feature edits on the public GED draw-view context helpers.  Both
files dropped `rt/view_legacy_bsg.h`; `src/libged/ged.cpp` remains the
GED-owned delegation point to the transitional RT legacy adapter.

The pivot guard now requires the public GED snap-line and snap-tolerance
facade tokens, requires those GED facades in the migrated `view snap` and
`view data_lines` paths, and rejects stale `rt/view_legacy_bsg.h` includes,
direct RT legacy snap-line, snap-tolerance, and model/view matrix adapters,
and direct retained BSG snap state from returning there.  An initial focused
CTest caught a duplicate older snap guard that still required the legacy snap
adapter include for `view snap`; that guard now preserves the old transitional
requirement for TclCAD snap users while requiring GED facades for migrated
libged `snap.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/snap-state verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|snap_semantics|tcl_overlay_bsg|drawing_faceplate)|regress-mged_(view|draw|refresh|draw_e_erase_d_who))$" --timeout 180 --output-on-failure`
completed 11/11 passing in 151.57 seconds.  The longest member was
`ged_test_state_services` at 92.25 seconds, followed by
`libbsg_api_hygiene` at 40.93 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.68 seconds,
`ged_test_drawing_faceplate` in 1.74 seconds, `ged_test_snap_semantics` in
0.01 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, and the selected MGED regressions
completed between 0.41 and 0.42 seconds each.

A one-hundred-twenty-ninth libged grid/rectangle state facade cleanup added
`ged_view_context_grid_state_get`, `ged_view_context_grid_state_set`,
`ged_view_context_snap_grid_2d`,
`ged_view_context_interactive_rect_state_get`, and
`ged_view_context_interactive_rect_state_set` to the public GED view-context
facade.  `src/libged/grid/grid.c` now uses GED facades for center,
model-to-view, view-to-model, 2D grid snapping, grid-state reads/writes, and
derived refresh.  `src/libged/rect/rect.c` now uses GED facades for rectangle
zoom center/model/view matrix reads, center writes, interactive-rectangle
state reads/writes, scale updates, and derived refresh.  The faceplate
interactive-rectangle command now uses the same GED interactive-rectangle
state get/set facades.  All three files dropped `rt/view_legacy_bsg.h`; the
neutral state record definitions come from `rt/view.h`, and `src/libged/ged.cpp`
remains the GED-owned delegation point to the transitional RT legacy adapter.

The pivot guard now requires the public GED grid/snap/interactive-rectangle
facade tokens, requires those GED facades in the migrated `grid`, `rect`, and
faceplate interactive-rectangle paths, and rejects stale
`rt/view_legacy_bsg.h` includes, direct RT legacy grid, snap-grid,
interactive-rectangle, center, and matrix adapters, and direct retained
BSG-shaped state from returning there.  An initial focused CTest caught a
duplicate older neutral-state guard that still required the legacy
interactive-rectangle adapter in `rect.c` and the faceplate command; that
guard now preserves the old transitional requirement for `select.c` while
requiring GED facades for the migrated rectangle files.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/grid-rectangle verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_faceplate)|regress-mged_(draw_e_erase_d_who|draw|refresh|view))$" --timeout 180 --output-on-failure`
completed 9/9 passing in 155.85 seconds.  The longest member was
`ged_test_state_services` at 96.73 seconds, followed by
`libbsg_api_hygiene` at 40.93 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.61 seconds,
`ged_test_drawing_faceplate` in 1.75 seconds, `ged_test_view_command` in
0.15 seconds, and the selected MGED regressions completed between 0.40 and
0.41 seconds each.

A one-hundred-twenty-eighth libged view-utility facade cleanup added
`ged_view_context_view2model_set` to the public GED view-context facade and
moved the compact lookat, align, and shared rotation/translation utility batch
behind GED-owned helpers.  `src/libged/view/lookat.c` now uses GED facades for
view-to-model reads, AET reads/writes, center writes, scale reads, and derived
refresh.  `src/libged/view/align.c` now uses GED facades for center reads,
view-to-model reads/writes, AET reads/writes, and derived refresh.
`src/libged/vutil.c` now uses GED facades for rotation reads/writes,
model-to-view and view-to-model reads, center reads/writes, rotate-about,
keypoint, inverse-size, and derived refresh.  All three files dropped
`rt/view_legacy_bsg.h`; `src/libged/ged.cpp` remains the GED-owned delegation
point to the transitional RT legacy adapter.

The pivot guard now requires `ged_view_context_view2model_set` in the public
GED view header, requires the GED view-context facades in the migrated lookat,
align, and vutil paths, and rejects stale `rt/view_legacy_bsg.h` includes,
direct RT legacy view matrix, center, AET, rotation, and update adapters, and
direct retained `gv_*` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-utility verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services)|regress-mged_(draw_e_erase_d_who|draw|knob|lookat|orientation|refresh|rot_edit|rotobj|rot_view|setview|view))$" --timeout 180 --output-on-failure`
completed 15/15 passing in 154.17 seconds.  The longest member was
`ged_test_state_services` at 93.84 seconds, followed by
`libbsg_api_hygiene` at 40.98 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.63 seconds,
`ged_test_view_command` completed in 0.15 seconds, and the selected MGED
regressions completed between 0.40 and 0.42 seconds each.

A one-hundred-twenty-seventh libged projection-matrix facade cleanup added
`ged_view_context_pmodel2view_get`, `ged_view_context_pmat_get`, and
`ged_view_context_pmat_set` to the public GED view-context facade and moved
the compact production projected-model/projection-matrix command batch behind
it.  `src/libged/pmodel2view/pmodel2view.c` now queries projected
model-to-view snapshots through the GED facade.  `src/libged/pmat/pmat.c`,
`src/libged/perspective/perspective.c`, and
`src/libged/eye_pos/eye_pos.c` now use GED facades for projection-matrix
reads/writes while keeping final view refreshes on `ged_view_context_update`.
Those command files no longer include `rt/view_legacy_bsg.h` for
projected-model or PMAT access; `src/libged/ged.cpp` remains the GED-owned
delegation point to the transitional RT legacy adapter.

The pivot guard now requires the public GED projection facade tokens, requires
the GED projection facades in the migrated `pmodel2view`, `pmat`,
`perspective`, and `eye_pos` command paths, and rejects stale
`rt/view_legacy_bsg.h` includes, direct
`rt_view_context_pmodel2view_from_bsg`, `rt_view_context_pmat_from_bsg`,
`rt_view_context_pmat_set_bsg`, typed RT projection calls, and direct
`gv_pmodel2view`/`gv_pmat` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/projection verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services)|rt_opt_(perspective|eye_backoff)|regress-mged_(view|set_perspective|eye_pt|draw|refresh|draw_e_erase_d_who))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 151.68 seconds.  The longest member was
`ged_test_state_services` at 93.43 seconds, followed by
`libbsg_api_hygiene` at 40.92 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.44 seconds,
`ged_test_view_command` completed in 0.15 seconds, `rt_opt_perspective` and
`rt_opt_eye_backoff` completed in 0.11 seconds each, and the selected MGED
regressions completed between 0.41 and 0.42 seconds each.

A one-hundred-twenty-sixth libged AET facade cleanup added
`ged_view_context_aet_get` and `ged_view_context_aet_set` to the public GED
view-context facade and moved the compact production AET command/export pair
behind it.  `src/libged/view/aet.c` now uses the GED AET facades for AET query
output, incremental AET calculations, and AET writes before its existing
`ged_view_context_update` refresh.  `src/libged/brep/tikz.cpp` now uses
`ged_view_context_aet_get` for TikZ camera orientation output.  Both files
drop `rt/view_legacy_bsg.h` for AET access, while `src/libged/ged.cpp`
remains the GED-owned delegation point to the transitional RT legacy adapter.

The pivot guard now requires `ged_view_context_aet_get` and
`ged_view_context_aet_set` in the public GED view header, requires the GED AET
facades in the migrated `view aet` and `brep tikz` paths, and rejects stale
`rt/view_legacy_bsg.h` includes, direct `rt_view_context_aet_from_bsg`,
`rt_view_context_aet_set_bsg`, typed RT AET calls, and direct `gv_aet` access
from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/AET-view verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services)|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|regress-mged_(view|setview|orientation|draw|refresh))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 150.02 seconds.  The longest member was
`ged_test_state_services` at 91.99 seconds, followed by
`libbsg_api_hygiene` at 41.00 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.68 seconds, the three
selected BREP checks completed below 0.06 seconds each, `ged_test_view_command`
completed in 0.15 seconds, and the selected MGED regressions completed between
0.40 and 0.42 seconds.

A one-hundred-twenty-fifth libged pick/ray snapshot facade cleanup moved the
compact production viewport pick and ray-query camera snapshot batch behind
the opaque GED view-context center, rotation, and model-to-view facades.
`src/libged/bot/pick.cpp` and `src/libged/brep/pick.cpp` now derive viewport
pick rays with `ged_view_context_center_get` and
`ged_view_context_rotation_get`.  `src/libged/solids_on_ray/solids_on_ray.c`
now reads center, rotation, and model-to-view snapshots with
`ged_view_context_center_get`, `ged_view_context_rotation_get`, and
`ged_view_context_model2view_get`, while keeping its existing
`ged_view_context_scale_get` read.  All three files dropped
`rt/view_legacy_bsg.h` for this camera snapshot access.

The pivot guard now requires the GED center/rotation/model2view facade tokens
in this migrated pick/ray subset and rejects stale `rt/view_legacy_bsg.h`
includes, direct `rt_view_context_center_from_bsg`,
`rt_view_context_rotation_from_bsg`,
`rt_view_context_model2view_from_bsg`, typed RT camera snapshot calls, and
direct `gv_center`/`gv_rotation`/`gv_model2view` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/pick-ray verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|bot_dumping)|test_qtcad_obol_pick|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|regress-(bots|solids|ged_lint_raytrace)|rt_dsp_raytrace_check)$" --timeout 180 --output-on-failure`
completed 13/13 passing in 160.57 seconds.  The longest member was
`ged_test_state_services` at 90.91 seconds, followed by
`libbsg_api_hygiene` at 41.05 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 15.36 seconds,
`rt_dsp_raytrace_check` in 3.81 seconds, `regress-bots` in 3.19 seconds,
`regress-ged_lint_raytrace` in 2.98 seconds, `regress-solids` in
2.71 seconds, and the selected BREP, BoT, qtcad pick, and view-command tests
completed below 0.16 seconds each.

A one-hundred-twenty-fourth libged center/eye facade cleanup moved the compact
production center and eye view commands behind the opaque GED view-context
center and matrix facades.  `src/libged/view/center.cpp` now uses
`ged_view_context_center_get` for center query output and
`ged_view_context_center_vec_set` for center writes.
`src/libged/view/eye.c` now uses `ged_view_context_view2model_get` for eye
position calculations and `ged_view_context_center_vec_set` for center writes.
Both command files no longer include `rt/view_legacy_bsg.h` for center or
view-to-model access, while their existing `ged_view_context_update` refreshes
remain unchanged.

The pivot guard now requires the GED center/view2model facades in this
migrated command pair and rejects stale `rt/view_legacy_bsg.h` includes,
direct `rt_view_context_center_from_bsg`,
`rt_view_context_center_vec_set_bsg`,
`rt_view_context_view2model_from_bsg`, typed RT center/view2model calls, and
direct `gv_center`/`gv_view2model` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-command verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services)|regress-mged_(view|center|eye_pt|draw|refresh|setview|orientation))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 151.81 seconds.  The longest member was
`ged_test_state_services` at 93.33 seconds, followed by
`libbsg_api_hygiene` at 40.90 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.50 seconds,
`rt_view_legacy_bsg` in 0.02 seconds, `ged_test_view_command` in
0.15 seconds, `regress-mged_center` and `regress-mged_eye_pt` each completed
in 0.41 seconds, and the selected MGED regressions completed between 0.41 and
0.41 seconds.

A one-hundred-twenty-third libged orientation command facade cleanup moved
another compact production rotation/orientation command batch behind the
opaque GED view-context rotation and quaternion facades.
`src/libged/orient/orient.c`, `src/libged/setview/setview.c`,
`src/libged/view/ypr.c`, `src/libged/view/qvrot.c`, and
`src/libged/view/quat.c` now use `ged_view_context_rotation_get`,
`ged_view_context_rotation_set`, and/or
`ged_view_context_orientation_quat_get` for rotation snapshots, rotation
writes, and quaternion query output.  Those five command files no longer
include `rt/view_legacy_bsg.h` for rotation or quaternion access, while their
existing `ged_view_context_update` refreshes remain unchanged.

The pivot guard now requires the GED rotation/quaternion facades in this
migrated command batch and rejects stale `rt/view_legacy_bsg.h` includes,
direct `rt_view_context_rotation_from_bsg`,
`rt_view_context_rotation_set_bsg`,
`rt_view_context_orientation_quat_from_bsg`, typed RT rotation/quaternion
calls, and direct `gv_rotation` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-command verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services)|regress-mged_(view|orientation|setview|qvrot|rot_view|draw|refresh))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 152.38 seconds.  The longest member was
`ged_test_state_services` at 93.87 seconds, followed by
`libbsg_api_hygiene` at 40.88 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.55 seconds,
`rt_view_legacy_bsg` in 0.02 seconds, `ged_test_view_command` in
0.15 seconds, and the selected MGED regressions completed between 0.40 and
0.42 seconds.

A one-hundred-twenty-second libged rotation facade cleanup moved a small
production rotation/vector query batch behind the opaque GED view-context
rotation facade.  `src/libged/rmat/rmat.c`,
`src/libged/rot_point/rot_point.c`,
`src/libged/view2model_vec/view2model_vec.c`, and
`src/libged/view/viewdir.c` now use `ged_view_context_rotation_get` for
rotation snapshots; `rmat.c` also uses `ged_view_context_rotation_set` for
rotation writes before its existing `ged_view_context_update` refresh.  Those
four command files no longer include `rt/view_legacy_bsg.h` for rotation
access.

The pivot guard now requires the GED rotation facade in this migrated
command batch and rejects stale `rt/view_legacy_bsg.h` includes, direct
`rt_view_context_rotation_from_bsg`,
`rt_view_context_rotation_set_bsg`, typed RT rotation reads/writes, and
direct `gv_rotation` access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-command verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services)|regress-mged_(view|orientation|qvrot|draw|refresh|setview|rot_view))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 152.87 seconds.  The longest member was
`ged_test_state_services` at 94.40 seconds, followed by
`libbsg_api_hygiene` at 40.95 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.43 seconds,
`rt_view_legacy_bsg` in 0.02 seconds, `ged_test_view_command` in
0.15 seconds, and the selected MGED regressions completed between 0.40 and
0.41 seconds.

A one-hundred-twenty-first libged conversion-matrix facade cleanup moved the
small production model/view conversion command batch behind the opaque GED
view-context matrix facade.  `src/libged/model2view/model2view.c`,
`src/libged/view2model/view2model.c`,
`src/libged/m2v_point/m2v_point.c`,
`src/libged/v2m_point/v2m_point.c`,
`src/libged/model2view_lu/model2view_lu.c`,
`src/libged/view2model_lu/view2model_lu.c`,
`src/libged/model2grid_lu/model2grid_lu.c`,
`src/libged/view2grid_lu/view2grid_lu.c`,
`src/libged/grid2model_lu/grid2model_lu.c`, and
`src/libged/grid2view_lu/grid2view_lu.c` now use
`ged_view_context_model2view_get` and/or
`ged_view_context_view2model_get` for model/view matrix snapshots.  Those
command files no longer include `rt/view_legacy_bsg.h` for matrix reads; the
six local-unit conversion commands continue to use `ged_view_context_scale_get`
for view-scale reads.

The pivot guard now requires the GED matrix facades in the migrated
conversion-command batch and rejects direct
`rt_view_context_model2view_from_bsg`,
`rt_view_context_view2model_from_bsg`, typed RT matrix reads, direct
`gv_model2view`/`gv_view2model` reads, and stale
`rt/view_legacy_bsg.h` includes from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-command verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services)|regress-mged_(view|draw|refresh|setview|orientation|rot_view|sca_view|tra_view)|regress-nirt)$" --timeout 180 --output-on-failure`
completed 14/14 passing in 150.56 seconds.  The longest member was
`ged_test_state_services` at 90.96 seconds, followed by
`libbsg_api_hygiene` at 41.02 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.45 seconds,
`rt_view_legacy_bsg` in 0.02 seconds, `ged_test_view_command` in
0.15 seconds, `regress-nirt` in 0.66 seconds, and the selected MGED
regressions completed between 0.40 and 0.41 seconds.

A one-hundred-twentieth libged camera snapshot facade cleanup moved selected
production output/query camera reads behind the opaque GED view-context
facade.  `include/ged/view.h` now exposes
`ged_view_context_model2view_get`, `ged_view_context_view2model_get`,
`ged_view_context_center_get`, `ged_view_context_center_vec_set`,
`ged_view_context_rotation_get`, `ged_view_context_rotation_set`,
`ged_view_context_info_get`, and
`ged_view_context_orientation_quat_get`, implemented in `src/libged/ged.cpp`
as retained RT legacy adapter delegations.  `src/libged/png/png.c`,
`src/libged/ps/ps.c`, `src/libged/plot/plot.c`,
`src/libged/savekey/savekey.c`,
`src/libged/get_eyemodel/get_eyemodel.c`, `src/libged/check/check.c`,
`src/libged/rtwizard/rtwizard.c`, and `src/libged/ged_util.cpp` now use the
GED facades for the migrated camera snapshots.  `png`, `ps`, `plot`,
`savekey`, `get_eyemodel`, `check`, and `rtwizard` no longer include
`rt/view_legacy_bsg.h` for those camera snapshot reads; `ged_util.cpp` still
keeps that header for other retained bridge details.

The pivot guard now requires the GED camera snapshot facades in the migrated
output/query paths and rejects direct `rt_view_context_model2view_from_bsg`,
`rt_view_context_view2model_from_bsg`, `rt_view_context_center_from_bsg`,
`rt_view_context_rotation_from_bsg`, `rt_view_context_info_from_bsg`, and
`rt_view_context_orientation_quat_from_bsg` calls from returning there.
Broader command-math matrix paths remain transitional for later slices.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/export/query verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|ged_draw_scene|rtwizard_bsg|tcl_overlay_bsg)|regress-mged_(plot|ps|png|saveview|savekey|view|draw|refresh|rt|orientation|eye_pt|set_perspective)|regress-(nirt|gchecker)|rt_opt_perspective)$" --timeout 180 --output-on-failure`
completed 22/22 passing in 183.05 seconds.  The longest member was
`ged_test_state_services` at 91.89 seconds, followed by
`libbsg_api_hygiene` at 40.86 seconds; both remained below the 180 second
per-test limit.  `brlobol_pivot_guard` completed in 14.43 seconds,
`rt_view_legacy_bsg` in 0.02 seconds, `ged_test_drawing_basic` in
7.59 seconds, `ged_test_drawing_quad` in 12.70 seconds,
`ged_test_rtwizard_bsg` in 0.39 seconds, `ged_test_ged_draw_scene` in
1.55 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, `rt_opt_perspective` in
0.11 seconds, `regress-gchecker` in 8.85 seconds, and the selected
MGED/NIRT regressions completed between 0.40 and 0.66 seconds.

A one-hundred-nineteenth libged simple-field facade cleanup moved the
remaining straightforward production command-side size, inverse-size,
eye-position, keypoint, rotate-about, coordinate, and zclip reads/writes
behind the opaque GED view-context facade.  `include/ged/view.h` now exposes
`ged_view_context_size_get/set`, `ged_view_context_inverse_size_get`,
`ged_view_context_eye_pos_get/set`, `ged_view_context_keypoint_get/set`,
`ged_view_context_rotate_about_get/set`, `ged_view_context_coord_get`, and
`ged_view_context_zclip_get`, implemented in `src/libged/ged.cpp` as retained
RT legacy adapter delegations.  `src/libged/arot/arot.c`,
`src/libged/keypoint/keypoint.c`, `src/libged/isize/isize.c`,
`src/libged/rot/rotate_about.c`, `src/libged/view/size.c`,
`src/libged/eye_pos/eye_pos.c`, `src/libged/draw/loadview.cpp`,
`src/libged/vutil.c`, `src/libged/view/knob.c`,
`src/libged/ged_util.cpp`, `src/libged/png/png.c`, and `src/libged/ps/ps.c`
now use those GED helpers for the migrated simple fields.
`arot`, `keypoint`, `isize`, `rotate_about`, and `view size` no longer
include `rt/view_legacy_bsg.h`; files that still include it do so for larger
retained view records such as PMAT, matrices, view-info, knob records, or
rotation/center state.

The pivot guard now requires the GED simple-field facades in the migrated
command/export/utility paths and rejects direct
`rt_view_context_size_*_bsg`, `rt_view_context_inverse_size_from_bsg`,
`rt_view_context_eye_pos_*_bsg`, `rt_view_context_keypoint_*_bsg`,
`rt_view_context_rotate_about_*_bsg`, `rt_view_context_coord_from_bsg`, and
`rt_view_context_zclip_from_bsg` calls from returning there.  A static
production search now leaves `src/libged/ged.cpp` as the only non-test libged
owner of those simple RT legacy adapter calls.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/view-command/export verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|ged_draw_scene|tcl_overlay_bsg)|regress-mged_(arot|eye_pt|keypoint|orientation|view|zoom|size|setview|set_perspective|rot_view|sca_view|tra_view|loadview|ps|plot|refresh|draw|knob)|regress-nirt|rt_opt_perspective)$" --timeout 180 --output-on-failure`
completed 29/29 passing in 181.03 seconds.  The longest member was
`ged_test_state_services` at 95.29 seconds, followed by `libbsg_api_hygiene`
at 40.92 seconds; both remained below the 180 second per-test limit.
`brlobol_pivot_guard` completed in 14.48 seconds, `rt_view_legacy_bsg` in
0.02 seconds, `ged_test_drawing_basic` in 7.49 seconds,
`ged_test_drawing_quad` in 12.74 seconds, `ged_test_ged_draw_scene` in
1.43 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, `rt_opt_perspective` in
0.11 seconds, and the selected MGED/NIRT regressions completed between
0.40 and 0.66 seconds.

A one-hundred-eighteenth libged perspective facade cleanup moved the remaining
straightforward production command/export perspective reads and writes behind
the opaque GED view-context facade.  `include/ged/view.h` now exposes
`ged_view_context_perspective_get(const void *view_ctx)` and
`ged_view_context_perspective_set(void *view_ctx, fastf_t perspective)`,
implemented in `src/libged/ged.cpp` as retained RT legacy adapter
delegations.  `src/libged/rt/rt.c`, `src/libged/dm/ert.cpp`,
`src/libged/view/saveview.c`, `src/libged/perspective/perspective.c`,
`src/libged/draw/loadview.cpp`, `src/libged/png/png.c`,
`src/libged/ps/ps.c`, `src/libged/rtwizard/rtwizard.c`, and
`src/libged/ged_util.cpp` now use the GED facade for perspective access.
`rt.c`, `ert.cpp`, and `saveview.c` no longer include
`rt/view_legacy_bsg.h` for perspective-only access; other migrated files keep
that header only where larger retained view records such as pmat, model/view
matrices, eye position, view info, or orientation snapshots remain
transitional.

The pivot guard now requires the GED perspective facade in the migrated
command/export paths and rejects direct
`rt_view_context_perspective_from_bsg`,
`rt_view_context_perspective_set_bsg`, or typed
`rt_view_perspective_*_bsg` calls from returning there.  A static production
search now leaves `src/libged/ged.cpp` as the GED facade owner and
`src/libged/bsg_ged_draw_view.c` as the private draw-view bridge owner for
non-test direct perspective adapter calls.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/draw/export/perspective verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(drawing_basic|drawing_faceplate|drawing_quad|ged_draw_scene|view_command|tcl_overlay_bsg|rtwizard_bsg)|regress-mged_(view|setview|set_perspective|perspective|loadview|saveview|plot|ps|rt|draw|refresh|overlay)|rt_opt_framebuffer)$" --timeout 180 --output-on-failure`
completed 21/21 passing in 83.70 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
14.55 seconds, `rt_view_legacy_bsg` in 0.02 seconds,
`ged_test_drawing_basic` in 7.51 seconds, `ged_test_drawing_faceplate` in
1.75 seconds, `ged_test_drawing_quad` in 12.70 seconds,
`ged_test_rtwizard_bsg` in 0.29 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, `rt_opt_framebuffer` in
0.10 seconds, and each selected MGED regression completed between 0.40 and
0.41 seconds.

A one-hundred-seventeenth libged framebuffer-mode cleanup moved the remaining
production command-side retained framebuffer-mode reads/writes behind the
opaque GED view-context facade.  `include/ged/view.h` now exposes
`ged_view_context_framebuffer_mode_get(const void *view_ctx)` and
`ged_view_context_framebuffer_mode_set(void *view_ctx, int mode)`,
implemented in `src/libged/ged.cpp` as retained RT legacy adapter
delegations.  `src/libged/fbclear/fbclear.c`,
`src/libged/dm/ert.cpp`, and `src/libged/view/faceplate/faceplate.c` now use
those GED helpers for framebuffer mode.  `fbclear.c` no longer includes
`rt/view_legacy_bsg.h`; after the later perspective facade cleanup,
`ert.cpp` no longer includes it for retained perspective state, and
`faceplate.c` still includes it for center-dot, scale-overlay, and params
state while those larger faceplate records remain transitional.

The pivot guard now requires the GED framebuffer-mode facade in the migrated
paths and rejects direct `rt_view_context_framebuffer_mode_*_bsg`, typed
`rt_view_framebuffer_mode_*_bsg`, or direct BSG framebuffer-mode API calls
from returning there.  A static production search now leaves
`src/libged/ged.cpp` as the only non-test direct
`rt_view_context_framebuffer_mode_*_bsg` caller; those calls are the GED
facade delegations.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/draw/faceplate/display verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|imgstream_fb_compat_test|ged_test_drawing_basic|ged_test_drawing_faceplate|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_tcl_overlay_bsg|ged_test_view_command|rt_opt_framebuffer|regress-mged_draw_e_erase_d_who|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_view)$" --timeout 180 --output-on-failure`
completed 15/15 passing in 80.99 seconds.  The longest member was
`libbsg_api_hygiene` at 40.88 seconds; `brlobol_pivot_guard` completed in
14.43 seconds, `imgstream_fb_compat_test` in 0.01 seconds,
`ged_test_drawing_basic` in 7.52 seconds, `ged_test_drawing_faceplate` in
1.74 seconds, `ged_test_drawing_quad` in 12.53 seconds,
`ged_test_ged_draw_scene` in 1.41 seconds, `ged_test_tcl_overlay_bsg` in
0.12 seconds, `ged_test_view_command` in 0.15 seconds,
`rt_opt_framebuffer` in 0.10 seconds, and each selected MGED regression
completed between 0.40 and 0.42 seconds.

A one-hundred-sixteenth libged display-manager facade cleanup moved simple
production command-side retained display-manager lookups behind the opaque GED
view-context facade.  `include/ged/view.h` now exposes
`ged_view_context_display_manager_get(const void *view_ctx)`, implemented in
`src/libged/ged.cpp` as the retained RT legacy adapter delegation.
`src/libged/fbclear/fbclear.c`, `src/libged/fb2pix/fb2pix.c`,
`src/libged/pix2fb/pix2fb.c`, `src/libged/png2fb/png2fb.c`,
`src/libged/overlay/overlay.c`, `src/libged/dm/ert.cpp`,
`src/libged/rt/rt.c`, `src/libged/illum/illum.c`,
`src/libged/nmg/nmg.c`, `src/libged/dm/screengrab.c`, and
`ged_rt_fb_refresh` now use that GED facade for display-manager reads.
The pure display-manager consumers (`fb2pix`, `pix2fb`, `png2fb`, `overlay`,
`illum`, `nmg`, and `dm/screengrab.c`) no longer include
`rt/view_legacy_bsg.h`; after the later perspective facade cleanup, `ert` and
`rt` no longer keep it for perspective state.

The pivot guard now requires `ged_view_context_display_manager_get` in the
migrated command/readback paths and rejects direct
`rt_view_context_display_manager_from_bsg`, typed
`rt_view_display_manager_from_bsg`, direct `struct bsg_view`, or direct `dmp`
field access from returning there.  A static production search now leaves
`src/libged/ged.cpp` and `src/libged/dm/dm.c` as the only non-test,
non-draw-bridge direct `rt_view_context_display_manager_from_bsg` callers:
`ged.cpp` owns the GED facade delegation, and `dm.c` owns the retained DM
command bridge.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/draw/display verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|ged_test_tcl_overlay_bsg|regress-mged_overlay|imgstream_fb_compat_test|rt_opt_framebuffer)$" --timeout 180 --output-on-failure`
completed 10/10 passing in 77.74 seconds.  The longest member was
`libbsg_api_hygiene` at 40.91 seconds; `brlobol_pivot_guard` completed in
14.38 seconds, `imgstream_fb_compat_test` in 0.01 seconds,
`ged_test_drawing_basic` in 7.52 seconds, `ged_test_drawing_quad` in
12.70 seconds, `ged_test_ged_draw_scene` in 1.40 seconds,
`ged_test_tcl_overlay_bsg` in 0.12 seconds, `ged_test_view_command` in
0.15 seconds, `rt_opt_framebuffer` in 0.10 seconds, and
`regress-mged_overlay` in 0.41 seconds.

A one-hundred-fifteenth libged scale-read cleanup moved the remaining simple
production command-side view-scale reads behind the opaque GED view-context
scale facade.  `src/libged/adc/adc.c`,
`src/libged/overlay/overlay.c`, `src/libged/plot/plot.c`,
`src/libged/nirt/nirt.cpp`, `src/libged/polyclip.cpp`,
`src/libged/solids_on_ray/solids_on_ray.c`, and
`src/libged/rtcheck/rtcheck2.cpp` now call
`ged_view_context_scale_get(view_ctx)` for ADC value printing/update scaling,
overlay default glyph sizing, plot export scale, qray glyph sizing, polygon
overlap scale, solids-on-ray screen scaling, and rtcheck overlap glyph sizing.
Their remaining center, rotation, model/view matrix, plane, display-manager,
and ADC state reads stay on opaque RT context helpers while those larger
surfaces are transitional.  `rtcheck2.cpp` no longer includes
`rt/view_legacy_bsg.h`, since overlap glyph scale now arrives through the GED
facade.

The pivot guard now requires `ged_view_context_scale_get` in those migrated
paths and rejects direct `rt_view_scale_from_bsg`,
`rt_view_context_scale_from_bsg`, or direct BSG `gv_scale` reads from
returning there.  A static production search now leaves
`src/libged/ged.cpp` as the only non-test direct
`rt_view_context_scale_from_bsg` caller; that call is the GED facade
delegation.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Focused guard/hygiene/draw/command verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_adc|regress-mged_overlay|regress-mged_plot|regress-nirt|regress-solids)$" --timeout 180 --output-on-failure`
completed 11/11 passing in 81.64 seconds.  The longest member was
`libbsg_api_hygiene` at 40.93 seconds; `brlobol_pivot_guard` completed in
14.39 seconds, `ged_test_drawing_basic` in 7.50 seconds,
`ged_test_drawing_quad` in 12.61 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, `ged_test_view_command` in 0.15 seconds,
`regress-mged_adc` in 0.41 seconds, `regress-mged_overlay` in 0.41 seconds,
`regress-mged_plot` in 0.40 seconds, `regress-nirt` in 0.66 seconds, and
`regress-solids` in 2.71 seconds.

A one-hundred-fourteenth libged local-unit conversion cleanup moved six
command-side view-scale reads behind the opaque GED view-context scale facade.
`src/libged/model2view_lu/model2view_lu.c`,
`src/libged/view2model_lu/view2model_lu.c`,
`src/libged/model2grid_lu/model2grid_lu.c`,
`src/libged/view2grid_lu/view2grid_lu.c`,
`src/libged/grid2model_lu/grid2model_lu.c`, and
`src/libged/grid2view_lu/grid2view_lu.c` now call
`ged_view_context_scale_get(view_ctx)` for LU conversion scaling.  Their
model-to-view and view-to-model matrix snapshots remain on
`rt_view_context_*_from_bsg` while that matrix surface is transitional.

The pivot guard now requires `ged_view_context_scale_get` for those LU
conversion commands and rejects direct `rt_view_scale_from_bsg`,
`rt_view_context_scale_from_bsg`, or direct BSG `gv_scale` reads from
returning there.  The retained overlay scale-read guard remains separate
while that path still uses RT context scale access.

Verification on FreeBSD/clang passed with:
`ninja -C build libged ged_test_plugins`.

The `ged_test_plugins` executable was built for plugin compile coverage; a
direct no-argument run reports `Usage: build/src/libged/tests/ged_test_plugins file.g`,
so no throwaway database was created for this slice.

Focused guard/view verification with
`ctest --test-dir build -R "^(brlobol_pivot_guard|ged_test_view_command|regress-mged_adc|regress-mged_view|regress-mged_sca_view|regress-mged_setview|regress-mged_tra_view|regress-mged_rot_view)$" --timeout 180 --output-on-failure`
completed 8/8 passing in 17.02 seconds.  `brlobol_pivot_guard` completed in
14.36 seconds and each selected view/MGED regression completed in
0.42 seconds or less.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_adc|regress-mged_view|regress-mged_sca_view|regress-mged_setview|regress-mged_tra_view|regress-mged_rot_view)$" --timeout 180 --output-on-failure`
completed 12/12 passing in 79.71 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
14.35 seconds, `ged_test_drawing_basic` in 7.63 seconds,
`ged_test_drawing_quad` in 12.69 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_view_command` in 0.15 seconds, and each selected MGED
regression completed in 0.41 seconds.

A one-hundred-thirteenth libged refresh-facade cleanup removed the remaining
production command-side direct `rt_view_context_update_bsg` calls from
`src/libged` outside the GED facade implementation itself.  `src/libged/grid/grid.c`,
`src/libged/draw/loadview.cpp`, `src/libged/draw/preview.cpp`, and
`src/libged/mirror/mirror.c` now call `ged_view_context_update(view_ctx)` for
their final view refreshes.  `mirror.c` no longer includes
`rt/view_legacy_bsg.h`, since its only retained view operation is now the
public GED active-view lookup plus update facade.

The pivot guard now requires `ged_view_context_update` in those grid,
loadview, preview, and mirror paths and rejects direct `rt_view_update_bsg`,
`rt_view_context_update_bsg`, or `bsg_update` calls from returning there.  It
also rejects `rt/view_legacy_bsg.h` from returning to `mirror.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.35 seconds.

Focused command/draw verification with
`ctest --test-dir build -R "^(regress-mged_mirror|regress-mged_loadview|ged_test_view_command|ged_test_drawing_basic|ged_test_drawing_faceplate|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_state_services)$" --timeout 180 --output-on-failure`
completed 8/8 passing in 117.31 seconds.  The longest member was
`ged_test_state_services` at 92.83 seconds; the next longest was
`ged_test_drawing_quad` at 12.60 seconds.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_faceplate|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_loadview|regress-mged_mirror)$" --timeout 180 --output-on-failure`
completed 9/9 passing in 79.78 seconds.  The longest member was
`libbsg_api_hygiene` at 40.88 seconds; `brlobol_pivot_guard` completed in
14.39 seconds, `ged_test_drawing_basic` in 7.51 seconds,
`ged_test_drawing_faceplate` in 1.77 seconds, `ged_test_drawing_quad` in
12.80 seconds, `ged_test_ged_draw_scene` in 1.42 seconds,
`ged_test_view_command` in 0.15 seconds, `regress-mged_loadview` in
0.41 seconds, and `regress-mged_mirror` in 0.40 seconds.

A one-hundred-twelfth libged small-view refresh cleanup moved another command
batch from direct RT legacy update calls to the opaque GED view-context
refresh facade.  `src/libged/eye_pos/eye_pos.c`,
`src/libged/view/ypr.c`, `src/libged/view/size.c`,
`src/libged/view/qvrot.c`, `src/libged/view/quat.c`,
`src/libged/orient/orient.c`, `src/libged/perspective/perspective.c`,
`src/libged/pmat/pmat.c`, `src/libged/vutil.c`,
`src/libged/rmat/rmat.c`, `src/libged/view/eye.c`,
`src/libged/view/center.cpp`, `src/libged/view/align.c`,
`src/libged/view/aet.c`, and `src/libged/setview/setview.c` now call
`ged_view_context_update(view_ctx)` for their final refresh.  Their
field-specific transitional reads and writes remain on `rt_view_context_*_bsg`
helpers until those surfaces are migrated.

The pivot guard now requires `ged_view_context_update` in that small-view
command group and rejects `rt_view_update_bsg`,
`rt_view_context_update_bsg`, direct `bsg_update`, or direct BSG view-field
refresh access from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.40 seconds.

Targeted command verification with
`ctest --test-dir build -R "^(ged_test_view_command|regress-mged_qvrot|regress-mged_qorot|regress-mged_orientation|regress-mged_set_perspective|regress-mged_setview|regress-mged_view|regress-mged_vrot|regress-mged_mrot|regress-mged_tra_view|regress-mged_lookat|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 13/13 passing in 5.10 seconds.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_lookat|regress-mged_mrot|regress-mged_orientation|regress-mged_qorot|regress-mged_qvrot|regress-mged_sca_view|regress-mged_set_perspective|regress-mged_setview|regress-mged_tra_view|regress-mged_view|regress-mged_vrot|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 18/18 passing in 82.14 seconds.  The longest member was
`libbsg_api_hygiene` at 40.86 seconds; `brlobol_pivot_guard` completed in
14.57 seconds, `ged_test_drawing_basic` in 7.52 seconds,
`ged_test_drawing_quad` in 12.68 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_view_command` in 0.15 seconds, and each selected MGED
view regression completed in 0.41 seconds or less.

A one-hundred-eleventh libged lookat view-context cleanup kept
`src/libged/view/lookat.c` on an opaque `void *` active-view context
throughout the command.  The command now routes view-to-model snapshots, AET
reads/writes, and center writes through `rt_view_context_*_bsg` helpers, while
the scale read and final refresh use the opaque GED facade
`ged_view_context_scale_get` and `ged_view_context_update`.

The pivot guard now requires the RT/GED context helper mix in `lookat.c` and
rejects direct typed RT view calls, direct RT scale/update context calls, and
direct BSG view-field reads from returning to the lookat command path.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

Targeted command verification with
`ctest --test-dir build -R "^(regress-mged_lookat|ged_test_view_command|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 1.42 seconds.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_lookat|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 9/9 passing in 78.44 seconds.  The longest member was
`libbsg_api_hygiene` at 40.94 seconds; `brlobol_pivot_guard` completed in
14.41 seconds, `ged_test_drawing_basic` in 7.50 seconds,
`ged_test_drawing_quad` in 12.75 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_view_command` in 0.15 seconds,
`regress-mged_lookat` in 0.40 seconds, and the scale/zoom MGED regressions in
0.41 seconds each.

A one-hundred-tenth libged rectangle-zoom view-scale cleanup moved the
remaining direct RT legacy scale/update calls in `src/libged/rect/rect.c`
behind the opaque GED view-context facade.  The rectangle command still uses
RT legacy context helpers for center snapshots, model/view matrix snapshots,
center writes, and interactive rectangle state while that broader view-state
surface remains transitional, but rectangle zoom now uses
`ged_view_context_scale_get`, `ged_view_context_scale_set`, and
`ged_view_context_update` for scale and refresh.

The pivot guard now requires those GED facade tokens in `rect.c`, keeps the
remaining center/matrix/interactive-rectangle RT context helper requirements,
and rejects direct `rt_view_context_scale_from_bsg`,
`rt_view_context_scale_set_bsg`, and `rt_view_context_update_bsg` calls from
returning to the rectangle command path.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.42 seconds.

Targeted command verification with
`ctest --test-dir build -R "^(ged_test_view_command|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 1.02 seconds.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 8/8 passing in 77.91 seconds.  The longest member was
`libbsg_api_hygiene` at 41.00 seconds; `brlobol_pivot_guard` completed in
14.34 seconds, `ged_test_drawing_basic` in 7.53 seconds,
`ged_test_drawing_quad` in 12.60 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, `ged_test_view_command` in 0.15 seconds, and both MGED
regressions in 0.41 seconds each.

A one-hundred-ninth libged view-scale command cleanup moved the `scale` and
`zoom` command implementations behind an opaque GED view-context scale/update
facade.  `include/ged/view.h` now exposes
`ged_view_context_scale_get`, `ged_view_context_scale_set`, and
`ged_view_context_update`; `src/libged/ged.cpp` owns the retained
`rt_view_context_*_bsg` delegation for those operations.

`src/libged/scale/scale.c` and `src/libged/zoom/zoom.c` now call the GED
facade helpers after `ged_view_active_ctx` lookup and no longer include
`rt/view_legacy_bsg.h` directly.  The pivot guard requires the GED facade
tokens in both command files and rejects direct RT legacy scale/update calls
or the old direct legacy include from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.40 seconds.

Targeted command verification with
`ctest --test-dir build -R "^(regress-mged_sca_view|regress-mged_zoom|ged_test_view_command)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 1.02 seconds.

A focused guard/hygiene/draw/view-command gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene|ged_test_view_command|regress-mged_sca_view|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 8/8 passing in 78.28 seconds.  The longest member was
`libbsg_api_hygiene` at 40.92 seconds; `brlobol_pivot_guard` completed in
14.72 seconds, `ged_test_drawing_basic` in 7.52 seconds,
`ged_test_drawing_quad` in 12.69 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_view_command` in 0.15 seconds, and both MGED
regressions in 0.41 seconds each.

A one-hundred-eighth libged draw-view context cleanup moved the remaining
direct RT legacy view-context reads out of `src/libged/draw.cpp`.
`src/libged/bsg_ged_draw_view_private.h` and
`src/libged/bsg_ged_draw_view.c` now expose
`ged_draw_view_context_name_from_bsg`,
`ged_draw_view_context_perspective_from_bsg`,
`ged_draw_view_context_obb_from_bsg`, and
`ged_draw_view_context_scale_from_bsg` wrappers.

`src/libged/draw.cpp` now uses those wrappers for retained draw debug names,
CSG OBB culling, perspective checks, and adaptive view-scale comparisons, and
no longer includes `rt/view_legacy_bsg.h` directly.  RT legacy view-state
knowledge for this path is now confined to the GED draw-view adapter
implementation.

The pivot guard now requires those GED draw-view wrapper tokens in
`draw.cpp` and rejects direct `rt/view_legacy_bsg.h` inclusion or the old
`rt_view_context_*_from_bsg` calls from returning there.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.39 seconds.

A focused draw/guard/hygiene gate with
`ctest --test-dir build -R "^(libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 62.74 seconds.  The longest member was
`libbsg_api_hygiene` at 41.05 seconds; `ged_test_drawing_basic` completed in
7.52 seconds, `ged_test_drawing_quad` in 12.68 seconds, and
`ged_test_ged_draw_scene` in 1.44 seconds.

A one-hundred-seventh libged scene-ref operation cleanup moved another batch
of command-side retained scene operations out of `src/libged/draw.cpp`.
`src/libged/bsg_ged_draw_private.h` and `src/libged/bsg_ged_draw_source.c`
now expose private `ged_draw_scene_ref_is_null`,
`ged_draw_scene_ref_bounds`, `ged_draw_scene_ref_set_bounds`,
`ged_draw_scene_ref_child_count`, and `ged_draw_scene_ref_child_at` wrappers.

`src/libged/draw.cpp` now uses those wrappers for LoD bounds restoration,
CSG visibility bounds checks, child realization traversal, and overlay setup
failure handling.  The failed overlay path also uses the existing
`ged_draw_scene_ref_release` helper instead of calling `bsg_scene_ref_destroy`
directly.  The transitional `bsg_scene_ref` handle type remains at this
private draw bridge boundary, but the command-side file no longer calls the
retained BSG scene-ref utility functions directly for this batch.

The pivot guard now requires these bridge wrapper tokens in `draw.cpp` and
rejects direct `bsg_scene_ref_is_null`, bounds, child traversal, and
scene-ref destroy calls from returning there.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.48 seconds.

`ctest --test-dir build -R "^ged_test_drawing_basic$" --timeout 180 --output-on-failure`
completed 1/1 passing in 7.56 seconds.

A focused draw/guard/hygiene gate with
`ctest --test-dir build -R "^(libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 63.02 seconds.  The longest member was
`libbsg_api_hygiene` at 41.34 seconds; `ged_test_drawing_basic` completed in
7.51 seconds, `ged_test_drawing_quad` in 12.71 seconds, and
`ged_test_ged_draw_scene` in 1.41 seconds.

A one-hundred-sixth libged draw-mode cleanup removed the remaining direct BSG
draw-mode constants and stale direct BSG headers from `src/libged/draw.cpp`.
The draw realization controller now uses `GED_DRAW_MODE_WIRE`,
`GED_DRAW_MODE_SHADED_BOTS`, `GED_DRAW_MODE_SHADED`,
`GED_DRAW_MODE_EVAL_WIRE`, `GED_DRAW_MODE_HIDDEN_LINE`, and
`GED_DRAW_MODE_EVAL_POINTS` for its command-side routing decisions.  The file
no longer includes `bsg/draw_intent.h` or `bsg/view_state.h`; retained
scene-ref details continue to arrive through the GED draw private bridge.

The pivot guard now requires the neutral GED draw-mode tokens in
`src/libged/draw.cpp` and rejects direct `bsg/` includes or
`BSG_DRAW_MODE_*` constants from returning to that boundary.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.39 seconds.

A focused draw/guard/hygiene gate with
`ctest --test-dir build -R "^(libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 62.58 seconds.  The longest member was
`libbsg_api_hygiene` at 40.95 seconds; `ged_test_drawing_basic` completed in
7.53 seconds, `ged_test_drawing_quad` in 12.65 seconds, and
`ged_test_ged_draw_scene` in 1.41 seconds.

A one-hundred-fifth libged draw-walk appearance cleanup moved the remaining
draw subtree-walk appearance storage off `struct bsg_appearance_settings`.
`src/libged/ged_private.h` now stores `draw_data_t::vs` as a
`struct ged_draw_appearance_settings *`, and `src/libged/draw.cpp` seeds that
state with `GED_DRAW_APPEARANCE_SETTINGS_INIT`.

`src/libged/draw.cpp` no longer includes `bsg/appearance.h` for this path.
Subtree-walk shape drafts now apply appearance through
`ged_draw_shape_draft_apply_appearance_settings`, leaving conversion to BSG
inside the retained draw bridge.  The tessellation-failure strict-fallback
query moved behind `ged_draw_scene_ref_strict_fallback`, implemented in
`src/libged/bsg_ged_draw_source.c`, so command-side draw realization no
longer calls `bsg_scene_strict_fallback` directly.

The pivot guard now requires neutral draw-walk appearance storage, the neutral
draft appearance helper, and the strict-fallback bridge wrapper; it rejects
direct BSG appearance storage, `bsg/appearance.h`, raw BSG appearance apply,
and direct strict-fallback calls from returning to this draw path.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.50 seconds.

`ctest --test-dir build -R "^ged_test_drawing_basic$" --timeout 180 --output-on-failure`
completed 1/1 passing in 7.69 seconds.

A focused draw/guard/hygiene gate with
`ctest --test-dir build -R "^(libbsg_api_hygiene|ged_test_drawing_basic|ged_test_drawing_quad|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`
completed 4/4 passing in 62.53 seconds.  The longest member was
`libbsg_api_hygiene` at 41.00 seconds; `ged_test_drawing_basic` completed in
7.57 seconds, `ged_test_drawing_quad` in 12.49 seconds, and
`ged_test_ged_draw_scene` in 1.42 seconds.

A one-hundred-fourth GSH stale-BSG-include cleanup removed direct
`bsg/defines.h` inclusion from `src/gtools/gsh/gsh.cpp`.  GSH still carries
the transitional retained active-view casts needed by its display hash,
dirty-state, refresh, and display-manager close paths, but the source no
longer pulls in BSG headers for those operations.  It uses
`rt/view_legacy_bsg.h` for `rt_view_hash_bsg`,
`rt_view_cleared_from_bsg`, `rt_view_cleared_set_bsg`,
`rt_view_display_manager_from_bsg`, and
`rt_view_display_manager_set_bsg`.

The pivot guard now rejects direct `bsg/` includes from returning to
`src/gtools/gsh/gsh.cpp`, alongside the existing checks for direct BSG
view-hash, cleared-state, and display-manager field access.

Verification on FreeBSD/clang passed with:
`ninja -C build gsh`.

`build/bin/gsh help` returned successfully.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.45 seconds.

`ctest --test-dir build -R "^libbsg_api_hygiene$" --timeout 180 --output-on-failure`
completed 1/1 passing in 40.93 seconds.

A one-hundred-third RT legacy header transitive-BSG cleanup removed direct
`bsg/scene_builder.h` and `bsg/view_state.h` inclusion from
`include/rt/view_legacy_bsg.h`.  The header now exposes
`rt_view_scene_ref_bsg`, `RT_VIEW_SCENE_REF_BSG_NULL_INIT`,
`rt_view_scene_ref_null_bsg`, `rt_view_scene_ref_is_null_bsg`, and
`rt_view_scene_ref_equal_bsg` for the few retained scene-ref handoff paths
that still cross the RT legacy adapter.  Refresh, snap-source, clear-source,
and snap-kind RT legacy constants are now literal RT-side aliases instead of
preprocessor expansions to BSG macro names.

`src/librt/view_legacy_bsg.c` converts between `rt_view_scene_ref_bsg` and
`bsg_scene_ref` internally before calling retained BSG helpers, and
`src/libged/bsg_ged_draw_view.c` performs the same conversion at libged's
private retained draw bridge.  `src/librt/tests/view_legacy_bsg.c` now tests
scene-ref null/equality behavior through the RT legacy wrapper helpers.  The
pivot guard requires the RT scene-ref wrapper API and rejects direct BSG
header includes or `bsg_scene_ref` handles from returning to
`include/rt/view_legacy_bsg.h`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged`.

`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`
completed 1/1 passing in 0.07 seconds.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.37 seconds.

A focused MGED guard/hygiene regression gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 59.07 seconds.  The longest member was
`libbsg_api_hygiene` at 41.02 seconds; `brlobol_pivot_guard` completed in
14.30 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and the nine MGED
regressions completed between 0.40 and 0.41 seconds.

A one-hundred-second RT knob-state ownership cleanup moved neutral knob-state
reset/hash ownership from the BSG legacy adapter into neutral RT view code.
`include/rt/view.h` now declares `enum rt_view_knobs_category`,
`RT_VIEW_KNOBS_RATE`, `RT_VIEW_KNOBS_ABS`,
`rt_view_knobs_state_reset`, and `rt_view_knobs_state_hash`; the
implementations now live in `src/librt/view.c`.  The retained BSG adapter keeps
only the actual BSG-facing context conversion helpers,
`rt_view_context_knobs_state_from_bsg` and
`rt_view_context_knobs_state_set_bsg`, plus the retained BSG view operations.

`src/librt/tests/view_legacy_bsg.c` now exercises neutral reset calls with the
neutral `RT_VIEW_KNOBS_RATE` category while retaining `_BSG` category constants
for BSG adapter operations.  The pivot guard requires the neutral knob-state
API in `include/rt/view.h` and `src/librt/view.c`, and rejects neutral
reset/hash declarations or implementations from returning to
`include/rt/view_legacy_bsg.h` or `src/librt/view_legacy_bsg.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged`.

`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`
completed 1/1 passing in 0.07 seconds.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.32 seconds.

A focused MGED guard/hygiene regression gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 58.92 seconds.  The longest member was
`libbsg_api_hygiene` at 40.87 seconds; `brlobol_pivot_guard` completed in
14.31 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and the nine MGED
regressions completed between 0.40 and 0.41 seconds.

A one-hundred-first MGED neutral knob-state cleanup moved shared MGED view
knob storage off `struct bsg_view_knobs`.  `include/rt/view.h` now owns
`struct rt_view_knobs`, a neutral RT-side record with the historical view knob
fields used by MGED.  `include/rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` expose
`rt_view_context_knobs_state_from_bsg` and
`rt_view_context_knobs_state_set_bsg` helpers for retained BSG conversion.

`src/mged/mged_dm.h` now stores `struct rt_view_knobs k` and no longer
includes `bsg/view_state.h`.  `src/mged/cmd.cpp` and `src/mged/chgview.c`
synchronize MGED view knob state to and from retained views through the new
context helpers instead of assigning `vs_gvp->k`, staging-view `k`, or
temporary pre-knob snapshots directly.  The existing MGED direct edits of
`view_state->k` remain source-compatible because the neutral record preserves
the field names and semantics.

`src/librt/tests/view_legacy_bsg.c` now covers neutral knob-state get, set,
hash, and reset behavior against the retained BSG state.  The pivot guard
requires the neutral knob-state helper tokens in the migrated MGED files,
requires `struct rt_view_knobs` in `mged_dm.h`, and rejects
`bsg/view_state.h`, `bsg_view_knobs`, and direct retained-view knob-field
access from returning to those MGED app-side boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.32 seconds after the guard update.

`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`
completed 1/1 passing in 0.07 seconds.

A focused MGED guard/hygiene regression gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 59.06 seconds.  The longest member was
`libbsg_api_hygiene` at 40.90 seconds; `brlobol_pivot_guard` completed in
14.42 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and the nine MGED
regressions completed between 0.40 and 0.41 seconds.

A one-hundredth MGED stale-BSG-include cleanup removed unused direct BSG
headers from app-side MGED command and draw files.  `src/mged/buttons.c`,
`src/mged/chgview.c`, and `src/mged/cmd.cpp` no longer include
`bsg/appearance.h`; `src/mged/dozoom.c` no longer includes
`bsg/appearance.h` or `bsg/defines.h`; and `src/mged/chgtree.c` no longer
includes `bsg/node.h`.

The affected files still exercise retained MGED view state and draw behavior,
but through `mged_dm.h`, `rt/view_legacy_bsg.h`, public GED draw/view
facades, and the existing MGED view-state structures.  The removed BSG
headers had no direct symbol use in those files.

The pivot guard now rejects those stale direct BSG headers from returning to
the same MGED app-side boundaries.

Initial verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.35 seconds after the guard update.

A focused MGED guard/hygiene regression gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 59.15 seconds.  The longest member was
`libbsg_api_hygiene` at 41.02 seconds; `brlobol_pivot_guard` completed in
14.38 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and the nine MGED
regressions completed between 0.40 and 0.42 seconds.

A ninety-ninth shared GED utility stale-include cleanup removed direct
`bsg/view_state.h` inclusion from `src/libged/ged_util.cpp`.  The utility file
still owns transitional retained-scene root reference helpers through
`bsg_scene_ref`, but its migrated view snapshot and diagnostic line-layer paths
continue to route through opaque RT view-context helpers and the public GED
draw-view context facade rather than direct BSG view-state declarations.

The pivot guard now rejects `bsg/view_state.h` from returning to
`src/libged/ged_util.cpp` in the shared utility view-snapshot/uplot boundary,
alongside the existing checks for neutral BG geometry commands, public GED
draw-view context publication, and no typed `struct bsg_view` access.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/GED utility gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(drawing_basic|ged_draw_scene|view_command|state_services))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 160.04 seconds.  The longest member was
`ged_test_state_services` at 95.60 seconds; `libbsg_api_hygiene` completed in
41.11 seconds, `brlobol_pivot_guard` in 14.23 seconds,
`ged_test_drawing_basic` in 7.50 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninety-eighth draw private-header transitive cleanup removed
`../ged_private.h` from `src/libged/draw/ged_draw.h`.  The shared draw
client-data header no longer drags libged's broad private BSG/GED internals
into every include site; draw command implementation files that need those
internals include the private header themselves.  `ged_draw.h` keeps the
public GED draw facade, public `ged.h`, and explicit `<time.h>` support for
its own declarations.

The pivot guard now rejects `ged_private.h` and direct BSG headers from
returning to `src/libged/draw/ged_draw.h`, alongside the existing checks for
opaque view-context and neutral appearance client-data storage.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completed 1/1 passing in 14.36 seconds after the guard update.

A ninety-seventh draw client-data appearance cleanup moved
`src/libged/draw/ged_draw.h` off `bsg_appearance_settings` for
`ged_shape_data` and `_ged_client_data`.  The draw and evaluated-wireframe
command paths now carry `struct ged_draw_appearance_settings` while retained
bridge helpers convert those neutral settings to BSG appearance settings
inside the bridge.

`src/libged/bsg_ged_draw_transactions.c` now exposes the bridge-owned neutral
to BSG appearance conversion helper; `src/libged/bsg_ged_draw_draft.c` and
`src/libged/bsg_ged_draw_tree.c` provide neutral wrapper calls for shape-draft
appearance application and group appearance setup.  `src/libged/draw/draw.c`
uses those wrappers and no longer directly includes BSG draw/appearance
headers.  `src/libged/draw/bigE.c` uses the neutral group appearance wrapper
when seeding evaluated-wireframe appearance state.

The pivot guard now requires neutral appearance client-data storage and the
neutral wrapper calls in the draw command boundary, and rejects
`bsg_appearance_settings`, `BSG_APPEARANCE_*`, direct BSG draw/appearance
headers, and old BSG appearance apply calls from returning to
`draw/ged_draw.h`, `draw.c`, or `bigE.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(drawing_basic|drawing_quad|ged_draw_scene|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 76.98 seconds.  The longest member was
`libbsg_api_hygiene` at 40.96 seconds; `brlobol_pivot_guard` completed in
14.23 seconds, `ged_test_drawing_basic` in 7.51 seconds,
`ged_test_drawing_quad` in 12.67 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninety-sixth stale-BSG-include cleanup removed unused direct BSG headers
from `src/libged/ged.cpp`, `src/libged/bot/dump/bot_dump.cpp`, and
`src/mged/edsol.c`.  `ged.cpp` already routes retained view lifecycle,
view-name, and display-manager access through opaque RT view-context helpers;
`bot_dump.cpp` already routes path queries and visible-record export scans
through GED draw helpers; and `edsol.c` already routes edit-solid view
snapshots through RT context helpers.  The stale `bsg/defines.h`,
`bsg/node.h`, `bsg/field.h`, `bsg/appearance.h`, and `bsg/draw_source.h`
includes are now gone from those files.

The pivot guard now rejects those direct BSG headers from returning to the
same source files, alongside the existing typed-view and direct-field
regression checks for those boundaries.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`.

A focused guard/hygiene/mged draw gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(mged_bsg|mged_shaded_mode_bsg|drawing_basic|ged_draw_scene|view_command))$" --timeout 180 --output-on-failure`
completed 7/7 passing in 65.47 seconds.  The longest member was
`libbsg_api_hygiene` at 40.93 seconds; `brlobol_pivot_guard` completed in
14.37 seconds, `ged_test_drawing_basic` in 7.49 seconds,
`ged_test_mged_bsg` in 0.53 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, `ged_test_view_command` in 0.16 seconds, and
`ged_test_mged_shaded_mode_bsg` in 0.52 seconds.

A ninety-fifth NIRT stale-BSG-include cleanup removed
`src/libged/nirt/nirt.cpp` from direct `bsg/appearance.h` and `bsg/node.h`
inclusion.  NIRT/qray still routes active-view snapshots through opaque RT
view-context helpers and stale qray cleanup through the public GED draw-view
context facade; the removed BSG headers were no longer used by the command
file.

The pivot guard now rejects those direct BSG headers plus BSG appearance/node
tokens from returning to `src/libged/nirt/nirt.cpp`, alongside the existing
checks for opaque RT view-context helpers and public GED draw-view qray
cleanup.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/NIRT gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(drawing_basic|ged_draw_scene|view_command)|regress-nirt)$" --timeout 180 --output-on-failure`
completed 6/6 passing in 65.11 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
14.34 seconds, `ged_test_drawing_basic` in 7.51 seconds,
`ged_test_ged_draw_scene` in 1.42 seconds, `ged_test_view_command` in
0.15 seconds, and `regress-nirt` in 0.66 seconds.

A ninety-fourth draw/redraw public-facade cleanup removed
`src/libged/draw/draw2.cpp` from the broader `../ged_private.h` include.  The
command file now uses explicit public includes for GED database/view checks,
database lookup, string comparison, draw transactions, path-state queries,
mode constants, and neutral appearance settings.  It preserves the old
`_ged_cmd_help` output pattern with a file-local `draw_cmd_help` wrapper over
`bu_opt_describe`, and uses a file-local `DRAW_MODE_UNSET` sentinel instead of
`_GED_SHADED_MODE_UNSET`.

The pivot guard now rejects `../ged_private.h`, the local draw-private header,
`_ged_cmd_help`, `_GED_SHADED_MODE_UNSET`, direct BSG draw/appearance tokens,
and typed BSG active-view access from returning to `src/libged/draw/draw2.cpp`.
The file remains on public GED draw transactions, public view-context helpers,
neutral draw modes, and neutral appearance settings.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 76.86 seconds.  The longest member was
`libbsg_api_hygiene` at 40.90 seconds; `brlobol_pivot_guard` completed in
14.17 seconds, `ged_test_drawing_basic` in 7.51 seconds,
`ged_test_drawing_quad` in 12.66 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninety-third draw/redraw private-header cleanup removed
`src/libged/draw/draw2.cpp` from libged's local draw-private `./ged_draw.h`
include.  The command file now obtains draw transactions, path-state queries,
mode constants, and neutral appearance settings from the public
`ged/bsg_ged_draw.h` facade.  It still includes the broader `ged_private.h`
for existing command help and shaded-mode internals; removing that remaining
private dependency is a separate command cleanup.

The pivot guard now requires the public GED draw facade include in
`src/libged/draw/draw2.cpp` and rejects the local draw-private header from
returning, alongside the existing opaque view-context and neutral
appearance/draw-mode checks.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 76.85 seconds.  The longest member was
`libbsg_api_hygiene` at 40.90 seconds; `brlobol_pivot_guard` completed in
14.30 seconds, `ged_test_drawing_basic` in 7.53 seconds,
`ged_test_drawing_quad` in 12.51 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninety-second draw/redraw appearance cleanup moved
`src/libged/draw/draw2.cpp` off command-side BSG appearance settings.
`draw2.cpp` now collects CLI color, transparency, line-width, strict
fallback, subtract-line, and draw-mode options in
`struct ged_draw_appearance_settings`, initialized with
`GED_DRAW_APPEARANCE_SETTINGS_INIT`.  The retained draw transaction bridge
still converts those neutral settings into BSG appearance settings internally
before applying retained draw-scene updates.

The pivot guard now requires neutral appearance tokens in
`src/libged/draw/draw2.cpp` and rejects `bsg_appearance_settings`,
`bsg_draw_mode`, `BSG_APPEARANCE_*`, and `BSG_DRAW_MODE_*` from returning to
that command file, alongside the existing opaque view-context checks.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 77.00 seconds.  The longest member was
`libbsg_api_hygiene` at 40.85 seconds; `brlobol_pivot_guard` completed in
14.21 seconds, `ged_test_drawing_basic` in 7.54 seconds,
`ged_test_drawing_quad` in 12.78 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninety-first draw/redraw draw-mode mapping cleanup moved
`src/libged/draw/draw2.cpp` off command-side `BSG_DRAW_MODE_*` constants.
The file already included the public GED draw facade and kept draw/redraw view
selection on opaque view contexts; `draw_user_mode_to_internal` now returns
`GED_DRAW_MODE_*` facade values for legacy integer draw modes while retained
draw transaction execution remains bridge-owned.

The pivot guard now requires neutral draw-mode tokens in
`src/libged/draw/draw2.cpp` and rejects `BSG_DRAW_MODE_*` from returning to
that command file, alongside the existing opaque view-context checks for
draw/redraw view selection.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 77.03 seconds.  The longest member was
`libbsg_api_hygiene` at 40.96 seconds; `brlobol_pivot_guard` completed in
14.19 seconds, `ged_test_drawing_basic` in 7.54 seconds,
`ged_test_drawing_quad` in 12.73 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

A ninetieth evaluated-wireframe draw-mode facade cleanup moved
`src/libged/draw/bigE.c` off direct `bsg/draw_intent.h` inclusion for
evaluated-wireframe mode setup.  The command still seeds retained appearance
settings and opaque draw client data for the same evaluated-wireframe
pipeline, but it now assigns `GED_DRAW_MODE_EVAL_WIRE` from the public GED
draw facade instead of naming `BSG_DRAW_MODE_EVAL_WIRE`.

The pivot guard now rejects direct `bsg/draw_intent.h` inclusion and
`BSG_DRAW_MODE_*` tokens from the evaluated-wireframe command/evaluator
boundary, and specifically requires `src/libged/draw/bigE.c` to retain the
neutral `GED_DRAW_MODE_EVAL_WIRE` token alongside the opaque active-view
context setup.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 77.07 seconds.  The longest member was
`libbsg_api_hygiene` at 41.03 seconds; `brlobol_pivot_guard` completed in
14.20 seconds, `ged_test_drawing_basic` in 7.50 seconds,
`ged_test_drawing_quad` in 12.72 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, and `ged_test_view_command` in 0.15 seconds.

An eighty-ninth sampled evaluated-points shape-ref facade batch moved
`src/libged/points_eval.c` off the private `bsg_scene_ref` evaluator
signature.  `include/ged/bsg_ged_draw.h` now extends
`ged_draw_shape_ref_source_snapshot` with the borrowed leaf directory and
exposes `ged_draw_shape_ref_publish_indexed_face_set` plus
`ged_draw_shape_ref_eval_points` as narrow shape-ref facade helpers.  The
retained draw bridge in `src/libged/draw.cpp` still owns the BSG scene-ref
realization path, converts the scene ref to a stable `ged_draw_shape_ref`,
and then calls the sampled evaluated-point helper.

`points_eval.c` still uses the same `rt_obj_sampled_face_set` path and the
same indexed-face-set payloads.  Its source lookup now comes from
`ged_draw_shape_ref_source_snapshot`, and final geometry publication goes
through `ged_draw_shape_ref_publish_indexed_face_set`, leaving the BSG
scene-ref extraction inside `src/libged/bsg_ged_draw_refs.c`.  The stale
`ged_draw_scene_ref_eval_points` declaration was removed from the private
draw bridge header.

The pivot guard now requires the sampled evaluated-point shape-ref facade
tokens and rejects `ged_private.h`, `bsg_scene_ref`, and old
`ged_draw_scene_ref_*` source/indexed-face/evaluator calls from returning to
`points_eval.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 77.16 seconds.  The longest member was
`libbsg_api_hygiene` at 40.98 seconds; `brlobol_pivot_guard` completed in
14.25 seconds, `ged_test_drawing_basic` in 7.52 seconds,
`ged_test_drawing_quad` in 12.78 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, and `ged_test_view_command` in 0.15 seconds.

An eighty-eighth evaluated-wireframe shape-ref facade batch moved
`src/libged/wireframe_eval.c` off direct BSG scene-builder inclusion and off
the private `bsg_scene_ref` evaluator signature.  `include/ged/bsg_ged_draw.h`
now exposes `ged_draw_shape_ref_source_snapshot`,
`ged_draw_shape_ref_publish_line_set`, and
`ged_draw_shape_ref_eval_wireframe` as narrow shape-ref facade helpers.  The
retained draw bridge in `src/libged/draw.cpp` still owns the BSG scene-ref
realization path, converts the scene ref to a stable `ged_draw_shape_ref`, and
then calls the evaluated-wireframe helper.

`wireframe_eval.c` now reads its borrowed database source, full path, name,
and tolerances through `ged_draw_shape_ref_source_snapshot` and publishes the
generated evaluated line set through `ged_draw_shape_ref_publish_line_set`.
Its CSG/NMG evaluated-wireframe algorithm and neutral BG move/draw commands
are unchanged; only the source/publication boundary moved behind the GED draw
facade.  The stale `ged_draw_scene_ref_eval_wireframe` declaration was removed
from the private draw bridge header.

The pivot guard now requires the evaluated-wireframe shape-ref facade tokens
and rejects direct BSG scene-builder headers, `bsg_scene_ref`, and old
`ged_draw_scene_ref_*` source/line-set/evaluator calls from returning to
`wireframe_eval.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged`.

A focused guard/hygiene/draw-scene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|drawing_basic|drawing_quad|view_command))$" --timeout 180 --output-on-failure`
completed 6/6 passing in 77.15 seconds.  The longest member was
`libbsg_api_hygiene` at 41.04 seconds; `brlobol_pivot_guard` completed in
14.25 seconds, `ged_test_drawing_basic` in 7.53 seconds,
`ged_test_drawing_quad` in 12.72 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, and `ged_test_view_command` in 0.15 seconds.

An eighty-third libged selection-state public-facade batch now moves native
selection draw-sync off the private draw-view header and off BSG scene-ref
index callbacks.  `include/ged/bsg_ged_draw.h` now owns the neutral
`ged_draw_shape_ref_index_cb` callback type plus
`ged_draw_shape_ref_index_for_component`,
`ged_draw_shape_ref_index_for_path_hash`,
`ged_draw_shape_ref_view_context`,
`ged_draw_view_context_selection_path_foreach`,
`ged_draw_view_context_selection_clear`, and
`ged_draw_view_selection_add_shape_ref_context`.

`src/libged/selection_state.cpp` no longer includes
`bsg_ged_draw_view_private.h` directly and no longer mentions
`bsg_scene_ref`.  Its native selection sync still uses the retained draw
index for component and path-hash lookups, but the public wrapper converts
the retained scene ref to a `ged_draw_shape_ref` before crossing into the
selection-state service.  The retained BSG scene-ref index helpers remain
private for bridge-owned draw transaction code.

The pivot guard now requires the public selection facade tokens in
`include/ged/bsg_ged_draw.h`, requires `selection_state.cpp` to use the
public shape-ref index helpers, and rejects private-header inclusion,
`bsg_scene_ref`, `ged_draw_shape_ref_from_scene_ref`, and the old scene-ref
index helpers from returning to this service.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`.

A focused draw/view gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|ged_draw_scene|drawing_basic|drawing_quad|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 79.20 seconds.  The longest member was
`libbsg_api_hygiene` at 40.99 seconds; `brlobol_pivot_guard` completed in
14.22 seconds, `ged_test_drawing_basic` in 7.54 seconds,
`ged_test_drawing_quad` in 12.67 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, and the five MGED regressions
completed in 0.41 seconds each.

The retained Tcl overlay regression now no longer includes libged's private
draw-view header.  `src/libged/tests/draw/tcl_overlay_bsg.cpp` was already
using the public `ged/bsg_ged_draw.h` context facade plus
`rt/view_legacy_bsg.h` for active-view lookup, feature creation, summary,
geometry point/command checks, style updates, arrow/axes/polygon/label facade
coverage, removal, scene-attachment checks, and the NULL-display-manager
`dm_draw_objs` safety check.  The stale
`../../bsg_ged_draw_view_private.h` include is now removed, leaving direct
`bsg_ged_draw_view_private.h` mentions only in bridge ownership and CMake
listing paths.

The pivot guard now rejects `../../bsg_ged_draw_view_private.h` from returning
to `tcl_overlay_bsg.cpp`, in addition to its existing rejection of direct BSG
headers, `gedp->ged_gvp`, typed `struct bsg_view`, `v->dmp`, direct
`bsg_feature_*`/`bsg_view_*` access, BSG feature/geometry/overlay constants,
and non-context GED draw-view feature calls.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_tcl_overlay_bsg`.

A focused guard/hygiene/regression gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_tcl_overlay_bsg)$" --timeout 180 --output-on-failure`
completed 3/3 passing in 55.16 seconds.  `brlobol_pivot_guard` completed in
14.17 seconds, `libbsg_api_hygiene` in 40.82 seconds, and
`ged_test_tcl_overlay_bsg` in 0.12 seconds.

A focused selection/draw gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(ged_draw_scene|selection_semantics|view_command|drawing_basic|drawing_quad|select|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completed 13/13 passing in 79.45 seconds.  The longest member was
`libbsg_api_hygiene` at 41.00 seconds, followed by
`brlobol_pivot_guard` at 14.34 seconds, `ged_test_drawing_quad` at
12.68 seconds, and `ged_test_drawing_basic` at 7.53 seconds.  The separate
`ged_test_drawing_select` check passed in 16.46 seconds, and
`libbsg_selection` plus `test_qtcad_obol_selection_sync` passed together in
0.15 seconds.  No individual test exceeded the 180-second cap.

An eighty-second libged view-command public-facade batch now moves the
top-level `view` command implementation off the private draw-view header.
`include/ged/bsg_ged_draw.h` now owns
`ged_draw_view_feature_depth_cb`,
`ged_draw_view_context_selection_count`,
`ged_draw_view_context_feature_depth`, and
`ged_draw_view_context_feature_depth_foreach`.

`src/libged/view/view.c` no longer includes
`bsg_ged_draw_view_private.h` directly.  It keeps independent-view handling,
named-view lookup, view selection counts, width/height queries, and `view vZ`
feature-depth resolution on opaque active-view handles, using the public GED
draw-view context facade for retained selection and feature-depth state and
RT view-context wrappers for view matrices and dimensions.  The BSG-typed
adapter declarations remain in the private draw-view bridge.

The pivot guard now requires the public selection/feature-depth declarations
in `include/ged/bsg_ged_draw.h`, requires `src/libged/view/view.c` to include
the public facade, and rejects private-header inclusion or direct typed BSG
selection/feature reach-through from returning to the migrated top-level
view command.  A companion guard fix makes `src/mged/cmd.cpp` include the
public GED draw facade explicitly while the simulation wrapper continues to
inject view orientation, eye point, and size through
`rt_view_context_*_from_bsg` helpers.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so bin/mged`.

A follow-up focused view-command/MGED gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|drawing_basic|drawing_quad|ged_draw_scene|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completed 12/12 passing in 79.17 seconds.  The longest member was
`libbsg_api_hygiene` at 40.90 seconds, followed by
`brlobol_pivot_guard` at 14.25 seconds, `ged_test_drawing_quad` at
12.72 seconds, and `ged_test_drawing_basic` at 7.52 seconds.  No individual
test exceeded the 180-second cap.

An eighty-first libged NIRT/faceplate public-facade batch now moves qray
feature cleanup and final faceplate HUD sync off the private draw-view header.
`include/ged/bsg_ged_draw.h` now exposes
`ged_draw_view_context_hud_sync` beside the existing public
`ged_draw_view_context_feature_remove` helper.

`src/libged/nirt/nirt.cpp` no longer includes
`bsg_ged_draw_view_private.h` directly.  It continues to route NIRT shot
center, direction, qray glyph scale, and vnirt view-to-model conversion through
RT view-context helpers, and it now uses the public GED draw-view context
facade for stale qray feature cleanup.  `src/libged/view/faceplate/faceplate.c`
also no longer includes the private draw-view header directly; scalar
faceplate and framebuffer state remain behind RT view-context wrappers, while
the final HUD sync uses the public GED draw-view context facade.

The pivot guard now requires the public facade in both migrated files, requires
`ged_draw_view_context_hud_sync` in the public header, and rejects private
draw-view header inclusion, direct BSG feature access, typed BSG view access,
or direct HUD sync from returning to the migrated pair.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`.

A follow-up focused NIRT/faceplate gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(drawing_faceplate|view_command|drawing_basic|ged_draw_scene|tcl_overlay_bsg)|regress-(mged_(draw|refresh|zoom|knob|view)|nirt))$" --timeout 180 --output-on-failure`
completed 13/13 passing in 68.78 seconds.  The longest member was
`libbsg_api_hygiene` at 40.88 seconds, followed by
`brlobol_pivot_guard` at 14.14 seconds, `ged_test_drawing_basic` at
7.58 seconds, `ged_test_drawing_faceplate` at 1.75 seconds, and
`ged_test_ged_draw_scene` at 1.41 seconds.  No individual test exceeded the
180-second cap.

An eightieth libged LoD command public-facade batch now moves the standalone
`lod` command and `view lod` subcommand off the private draw-view header.
`include/ged/bsg_ged_draw.h` now owns the neutral
`ged_draw_view_lod_policy` alias for `struct rt_view_lod_policy` and the
context-form LoD policy helpers used by command-side paths:
`ged_draw_view_context_lod_policy_from_bsg`,
`ged_draw_view_context_lod_policy_apply_bsg`, and
`ged_draw_view_context_lod_policy_apply_bsg_bot_threshold`.

`src/libged/lod/lod.c` and `src/libged/view/lod.cpp` no longer include
`bsg_ged_draw_view_private.h` directly.  They include the public GED draw
facade plus `rt/view.h` for the concrete RT-owned policy fields, keep their
view handles as opaque contexts, and continue to use context-form LoD policy
read/apply helpers.  The private draw-view header still exposes the BSG-typed
owner declarations for bridge-owned code.

The pivot guard now requires the public LoD policy alias and context helper
declarations in `include/ged/bsg_ged_draw.h`, requires the migrated LoD
command files to use the public facade, and rejects private-header inclusion
or typed LoD policy calls from returning to the migrated pair.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`.

A follow-up focused LoD/view gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|drawing_basic|drawing_quad|ged_draw_scene|lod_crossrun|drawing_lod)|regress-mged_(draw|refresh|zoom|knob)|rt_view_legacy_bsg)$" --timeout 180 --output-on-failure`
completed 13/13 passing in 91.88 seconds.  The longest member was
`libbsg_api_hygiene` at 40.87 seconds, followed by
`brlobol_pivot_guard` at 14.14 seconds, `ged_test_drawing_quad` at
12.64 seconds, `ged_test_drawing_lod` at 11.08 seconds, and
`ged_test_drawing_basic` at 7.50 seconds.  No individual test exceeded the
180-second cap.

A seventy-ninth libged data-lines/snap public-facade batch now moves two more
view subcommands off the private draw-view header.  `include/ged/bsg_ged_draw.h`
now owns the neutral `ged_draw_view_snap_kind` enum and the context-form
helpers used by those command-side paths:
`ged_draw_view_context_feature_visible`,
`ged_draw_view_context_snap_first_candidate`,
`ged_draw_view_context_line_style_get`,
`ged_draw_view_context_feature_line_command_at`, and
`ged_draw_view_context_lines_points_copy`.

`src/libged/view/data_lines.c` and `src/libged/view/snap.c` no longer include
`bsg_ged_draw_view_private.h` directly.  They keep active-view storage opaque,
include `view/ged_view.h`, and use the public GED draw-view context facade
for retained data-line feature inspection, line-style/point snapshots, and
snap-candidate lookup.  The private draw-view header still exposes the
BSG-typed owner declarations for bridge-owned code, but no longer defines this
neutral snap enum.

The pivot guard now requires the moved neutral enum and helper declarations in
`include/ged/bsg_ged_draw.h`, requires `view/data_lines.c` and `view/snap.c`
to stay on the public view-command path, and rejects the private draw-view
header plus typed snap/feature reach-through from returning to the migrated
pair.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`.

A follow-up focused data-lines/snap/view gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|drawing_basic|drawing_quad|ged_draw_scene|tcl_overlay_bsg|snap_semantics)|regress-mged_(draw_e_erase_d_who|draw|overlay|refresh|zoom|data|grid|rect|knob))$" --timeout 180 --output-on-failure`
completed 14/14 passing in 79.46 seconds.  The longest member was
`libbsg_api_hygiene` at 40.86 seconds, followed by
`brlobol_pivot_guard` at 14.10 seconds, `ged_test_drawing_quad` at
12.80 seconds, and `ged_test_drawing_basic` at 7.49 seconds.  No individual
test exceeded the 180-second cap.

A seventy-eighth libged view-command public-facade batch now moves shared
view-command state and the label/line/axes view subcommands off the private
draw-view header.  `include/ged/bsg_ged_draw.h` now owns the neutral
`ged_draw_view_axes_state`, polygon type/update enums, polygon handle/record
types, and the context-form helpers used by those command-side paths:
`ged_draw_view_context_label_create`,
`ged_draw_view_context_lines_create_model_annotation`,
`ged_draw_view_context_lines_append_point`,
`ged_draw_view_context_axes_create`,
`ged_draw_view_context_axes_state_get`,
`ged_draw_view_context_axes_state_replace`, and the context-form polygon
find/create/import/update/area/overlap/CSG helpers plus neutral polygon
record/fill/current-contour helpers.

`src/libged/view/ged_view.h` now includes only the public GED draw facade for
`_ged_view_info::polygon_ref`, and no longer includes
`bsg_ged_draw_view_private.h`.  `src/libged/view/labels.c`,
`src/libged/view/lines.c`, and `src/libged/view/axes.c` also no longer include
the private draw-view header directly.  The private header still exposes the
BSG-typed owner declarations for bridge-owned code, but no longer defines the
neutral axes or polygon command types.

The pivot guard now requires the moved neutral types and helpers in
`include/ged/bsg_ged_draw.h`, requires `view/ged_view.h` to use the public
facade, and rejects the private draw-view header from returning to the
migrated shared view-command header and label/line/axes subcommands.

Initial verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`.
The pivot guard completed in 14.20 seconds.

A follow-up focused view-command/MGED gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|drawing_basic|drawing_quad|ged_draw_scene|tcl_overlay_bsg)|regress-mged_(draw_e_erase_d_who|draw|overlay|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completed 13/13 passing in 79.45 seconds.  The longest member was
`libbsg_api_hygiene` at 40.86 seconds, followed by
`brlobol_pivot_guard` at 14.16 seconds, `ged_test_drawing_quad` at
12.75 seconds, and `ged_test_drawing_basic` at 7.51 seconds.  No individual
test exceeded the 180-second cap.

A seventy-seventh libged diagnostic draw-view public-facade batch now moves a
command-side retained overlay cluster off libged's private draw-view header.
`include/ged/bsg_ged_draw.h` now exposes the neutral
`ged_draw_view_line_layer_data` record and the context-form retained
draw-view helpers used by diagnostic/overlay producers:
`ged_draw_view_context_features_remove_prefix`,
`ged_draw_view_context_indexed_face_set_replace`,
`ged_draw_view_context_lines_replace`,
`ged_draw_view_context_line_layer_builder_replace`,
`ged_draw_view_context_line_layers_replace`, and
`ged_draw_view_context_labels_replace`.

`src/libged/check/check_overlaps.c`, `draw/preview.cpp`, `brep/plot.cpp`,
`gqa/gqa.cpp`, `gdiff/gdiff.c`, `lint/lint.cpp`, `bot/bot.cpp`,
`bot/check.cpp`, `bot/bot_fuse.c`, `illum/illum.c`, `nmg/nmg.c`,
`rtcheck/rtcheck2.cpp`, `vutil.c`, `ged_util.cpp`, and `joint/joint.c` now
use the public `ged/bsg_ged_draw.h` facade for retained line, label,
line-layer, indexed-face-set, and feature-prefix cleanup operations instead
of including `bsg_ged_draw_view_private.h`.  The private draw-view header
keeps the BSG-owning typed adapter declarations, but no longer owns the public
line-layer data record definition.

The pivot guard now requires the public facade declarations, requires the
migrated command batch to include `ged/bsg_ged_draw.h`, and rejects private
draw-view header inclusion, typed `struct bsg_view` spelling, or non-context
typed GED draw-view calls from returning to those files.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`.
The pivot guard completed in 14.22 seconds.

A follow-up focused libged/MGED gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(gqa|tcl_overlay_bsg|bot_dumping)|brep_ppx|regress-(gdiff|ged_lint_raytrace|gqa|bots|mged_(draw|overlay|refresh|zoom|knob)))$" --timeout 180 --output-on-failure`
completed 15/15 passing in 67.15 seconds.  The longest member was
`libbsg_api_hygiene` at 40.96 seconds, followed by `brlobol_pivot_guard` at
14.37 seconds, `regress-bots` at 3.17 seconds,
`regress-ged_lint_raytrace` at 2.96 seconds, and `regress-gqa` at
2.76 seconds.  No individual test exceeded the 180-second cap.

A seventy-sixth TclCAD view-data initializer ownership batch now removes the
stale RT-level TclCAD data initializer.  `rt_view_tclcad_data_init_bsg` is
retired from `include/rt/view_legacy_bsg.h`, `src/librt/view_legacy_bsg.c`,
and the RT legacy adapter test; the retained RT adapter now only binds and
unbinds the TclCAD data pointer through `rt_view_context_tclcad_data_set_bsg`.

`src/libtclcad/view_data.c` now owns TclCAD retained view-data defaults with
local `tclcad_polygon_state_init`, `tclcad_view_state_init`, and
`tclcad_view_data_init` helpers.  The local initializer preserves the old BSG
defaults that matter for behavior: both polygon states start with
`bg_Union` clip mode, their matrix snapshots are zeroed, and primitive labels
start with the old default font size.  TclCAD view creation now calls
`tclcad_view_data_init`, so initialization ownership matches the
TclCAD-private records introduced by the previous slice.

The pivot guard now rejects `rt_view_tclcad_data_init_bsg` from returning to
the RT legacy header or TclCAD view-data adapter, requires the local TclCAD
initializer helpers, and keeps the opaque retained binding adapter in place.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg lib/libtclcad.so`,
`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`.
The RT adapter test completed in 0.02 seconds and the pivot guard completed
in 14.33 seconds.

A follow-up focused TclCAD/MGED gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|regress-mged_(draw|overlay|refresh|zoom|data|grid|rect|knob))$" --timeout 180 --output-on-failure`
completed 10/10 passing in 57.29 seconds.  The longest member was
`libbsg_api_hygiene` at 40.86 seconds, followed by
`brlobol_pivot_guard` at 14.06 seconds.  No individual test exceeded the
180-second cap.

A seventy-fifth TclCAD public/private boundary batch now narrows two retained
legacy surfaces without changing TclCAD behavior.  TclCAD production
draw-view move/readback, arrows, axes, labels, polygons, command, and mouse
paths now include the public `ged/bsg_ged_draw.h` opaque context facade
instead of libged's private `bsg_ged_draw_view_private.h` header.  The public
facade now exposes the neutral line-style, feature-style, label-data, and
TclCAD retained overlay context helper declarations needed by those callers,
while the private libged header keeps the BSG-typed owner declarations.

`src/libtclcad/tclcad_private.h` now owns TclCAD-private records for retained
view, polygon, label, line, arrow, and axes state instead of typedef aliases
to `bsg_data_tclcad`, `bsg_data_polygon_state`, and
`bsg_data_label_state`.  `src/libtclcad/view_data.c` is now the single
TclCAD adapter file that includes `bsg/tcl_data.h`; it initializes and binds
the retained BSG Tcl-data pointer through the RT legacy adapter and uses
compile-time size/offset assertions to keep the TclCAD-private records layout
compatible with the legacy BSG records while the retained bridge remains.

The pivot guard now requires migrated TclCAD files to use the public
`ged/bsg_ged_draw.h` facade, rejects resurrection of the private libged
draw-view header in those TclCAD files, rejects BSG Tcl-data type exposure in
`tclcad_private.h`, and requires the layout-check tokens in `view_data.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libtclcad.so`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|regress-mged_(draw|overlay|refresh|zoom|data|grid|rect|knob))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 10/10 passing in 57.32 seconds.  The longest
member was `libbsg_api_hygiene` at 40.90 seconds, followed by
`brlobol_pivot_guard` at 14.03 seconds.  No individual test exceeded the
180-second cap.

A seventy-fourth MGED command-view staging/context batch now removes the last
typed RT-view helper calls from `src/mged/cmd.cpp`.  The autosize view-ring
scale snapshot, `cmd_view` cache save/restore path, staging copy helpers,
mutation hash, temporary staging dimension/lifecycle setup, and shared-view
unit-conversion refresh now call opaque `rt_view_context_*_bsg` wrappers
instead of the older typed retained-view adapters.

The RT legacy adapter now exposes
`rt_view_context_aet_state_set_bsg`,
`rt_view_context_model2view_set_bsg`,
`rt_view_context_pmodel2view_set_bsg`, and
`rt_view_context_knobs_hash_bsg`, implemented as narrow delegates over the
existing typed compatibility helpers.  `rt_view_legacy_bsg` test coverage now
checks null handling and live roundtrips for those context helpers.  The
pivot guard now requires the context helper tokens in `src/mged/cmd.cpp`,
requires the new adapter API tokens in the header/source/test inventories,
and rejects the older typed cache/staging/hash helper calls from returning.

The corrected remaining MGED typed RT-view helper count is now zero under:
`rg -n -P "\brt_view_(?!(context_|set_context_))[A-Za-z0-9_]+_bsg\s*\(" src/mged`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.69 seconds.  The longest
member was `libbsg_api_hygiene` at 40.98 seconds, followed by
`brlobol_pivot_guard` at 14.12 seconds.  No individual test exceeded the
180-second cap.

A seventy-third MGED chgview knob-baseline cleanup now removes the final
typed RT-view helper call from `src/mged/chgview.c`.  `mged_svbase` still
preserves object absolute rotation baselines, but now syncs MGED's local knob
record into the retained view, resets absolute knob baselines through
`rt_view_context_knobs_reset_bsg`, syncs the reset record back to the local
view state, and then restores the preserved object absolute rotation fields
before the existing final retained-view sync.  The pivot guard token inventory
now requires the context knob helpers for `chgview.c`.

The corrected remaining MGED typed RT-view helper count is now 115, all in
`src/mged/cmd.cpp`; `src/mged/chgview.c` is clean under the targeted typed
RT-view helper scan.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.70 seconds.  The longest
member was `libbsg_api_hygiene` at 40.97 seconds, followed by
`brlobol_pivot_guard` at 14.18 seconds.  No individual test exceeded the
180-second cap.

A seventy-second RT scale adapter cleanup batch now exposes
`rt_view_context_initial_scale_from_bsg`,
`rt_view_context_initial_scale_set_bsg`,
`rt_view_context_absolute_scale_from_bsg`, and
`rt_view_context_absolute_scale_set_bsg` from `rt/view_legacy_bsg.h`.  The
implementation delegates to the existing typed retained-view scale helpers,
and `rt_view_legacy_bsg` adapter coverage now checks null and live behavior
for both typed and context forms.

`src/mged/chgview.c` now uses those context helpers for view size/zoom,
status reporting, `size_reset`, `mged_svbase`, `mged_vscale`, and `cmd_sca`
initial/absolute scale reads and writes.  The pivot guard now requires those
context helper tokens in `chgview.c` and rejects the older typed
initial/absolute scale helper calls from returning.  The corrected remaining
MGED typed RT-view helper count is now 116: 115 in `src/mged/cmd.cpp` and
one local `rt_view_knob_state_reset_bsg` call in `src/mged/chgview.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.51 seconds.  The longest
member was `libbsg_api_hygiene` at 40.88 seconds, followed by
`brlobol_pivot_guard` at 14.06 seconds.  No individual test exceeded the
180-second cap.

A seventy-first MGED chgview context batch now keeps most of
`src/mged/chgview.c` from calling typed RT BSG view helpers directly.
View-ring save/restore now reads and writes rotation, center, and scale
through context helpers.  Edit rotation and translation paths, status query
snapshots, slew/view center reads, scale reads/writes that already have
context coverage, temporary coord switching, view knob command processing,
view knob translate/rotate/reset handling, and zoom-refresh LoD policy
inspection now use `rt_view_context_*_bsg` helpers.  The typed
`rt_edit_knob_cmd_process_bsg` edit path still receives its retained view as
the current legacy API requires, but its temporary coord save/restore now
uses context helpers.

The pivot guard now requires the migrated `chgview.c` context helper tokens
and rejects the older typed scale, center, rotation, matrix, coord,
rotate-about, unit-conversion, knob, and LoD helper calls from returning.
The corrected remaining MGED typed RT-view helper count is now 130: 115 in
`src/mged/cmd.cpp` and 15 in `src/mged/chgview.c`.  The remaining
`chgview.c` calls are initial/absolute scale helpers plus a local
`rt_view_knob_state_reset_bsg` use, which should be handled as a separate
adapter cleanup.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.49 seconds.  The longest
member was `libbsg_api_hygiene` at 40.89 seconds, followed by
`brlobol_pivot_guard` at 14.04 seconds.  No individual test exceeded the
180-second cap.

A seventieth MGED solid-edit context batch now keeps `src/mged/edsol.c` from
calling typed RT BSG view helpers directly.  Solid-edit rotation point
selection, NMG loop plane projection, ARS pick and point-move projection,
metaball view-direction projection, absolute-translation bookkeeping, BOT
mouse projection, object edit mouse projection, and parameter translation
scaling now read view scale and model/view matrices through
`rt_view_context_scale_from_bsg`,
`rt_view_context_model2view_from_bsg`, and
`rt_view_context_view2model_from_bsg`.

The pivot guard now requires those context helper tokens in
`src/mged/edsol.c` and rejects the older typed scale, model-to-view, and
view-to-model helper calls from returning.  The corrected remaining MGED
typed RT-view helper count is now 182, all in `src/mged/cmd.cpp` and
`src/mged/chgview.c`.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.27 seconds.  The longest
member was `libbsg_api_hygiene` at 40.88 seconds, followed by
`brlobol_pivot_guard` at 13.83 seconds.  No individual test exceeded the
180-second cap.

A sixty-ninth MGED main-loop/refresh context batch now keeps
`src/mged/mged.c` from calling typed RT BSG view helpers directly.  The edit
matrix and view callback paths borrow the retained view as an opaque
`void *view_ctx` and read model-to-view matrices through
`rt_view_context_model2view_from_bsg`.  Main view updates and zclip sync now
use `rt_view_context_update_bsg` and `rt_view_context_zclip_set_bsg`.
Interactive edit-rate scale computations read through
`rt_view_context_scale_from_bsg`.  The refresh paths now use
`rt_view_context_refresh_request_bsg`,
`rt_view_context_refresh_dirty_from_bsg`,
`rt_view_context_refresh_consume_bsg`, and
`rt_view_context_refresh_complete_bsg`.  Default ADC/grid setup now uses
`rt_view_context_grid_state_set_bsg`,
`rt_view_context_adc_state_from_bsg`, and
`rt_view_context_adc_state_set_bsg`.

The pivot guard now requires those context helper tokens in
`src/mged/mged.c` and rejects the older typed matrix, update, zclip, refresh,
ADC, and grid helper calls from returning.  The corrected remaining MGED typed
RT-view helper count is now 202, all in `src/mged/cmd.cpp`,
`src/mged/chgview.c`, and `src/mged/edsol.c`.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.35 seconds.  The longest
member was `libbsg_api_hygiene` at 40.93 seconds, followed by
`brlobol_pivot_guard` at 13.83 seconds.  No individual test exceeded the
180-second cap.

A sixty-eighth MGED setup/attach lifecycle context batch now keeps the
remaining small MGED view initialization, copy, callback, display-manager,
ADC, and LoD handoff calls from using typed RT BSG helpers directly.
`src/mged/setup.c` borrows the newly allocated retained view as
`void *view_ctx`, initializes it through `rt_view_context_init_bsg`, installs
the update callback through `rt_view_context_update_callback_set_bsg`, reads
the initial center through `rt_view_context_center_from_bsg`, and registers
the view through `rt_view_set_context_add_bsg`.  `src/mged/attach.c` uses
`rt_view_context_adc_state_set_bsg`,
`rt_view_context_scale_storage_from_bsg`,
`rt_view_context_init_copy_bsg`,
`rt_view_context_update_callback_set_bsg`,
`rt_view_context_lod_policy_from_bsg`, and
`rt_view_context_lod_policy_apply_bsg` for the copied-DM view path.

The RT legacy adapter now exposes
`rt_view_context_update_callback_set_bsg` and
`rt_view_context_init_copy_bsg`, implemented as delegating wrappers over the
existing typed update-callback and init-copy adapters.  `rt_view_legacy_bsg`
test coverage now checks null and live behavior for both wrappers.  The pivot
guard requires the context helper tokens in `src/mged/setup.c` and
`src/mged/attach.c`, and rejects the old typed lifecycle, callback,
scale-storage, ADC, and copied-display LoD helper calls from returning.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build rt_view_legacy_bsg bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.47 seconds.  The longest
member was `libbsg_api_hygiene` at 41.15 seconds, followed by
`brlobol_pivot_guard` at 13.74 seconds.  No individual test exceeded the
180-second cap.

A sixty-seventh MGED display-manager header context batch now keeps shared
MGED view-state helpers from calling typed RT BSG helpers directly.
`src/mged/mged_dm.h` reads ADC state, grid state, shared-settings identity,
view size, and view scale through `rt_view_context_adc_state_from_bsg`,
`rt_view_context_grid_state_from_bsg`,
`rt_view_context_settings_shared_bsg`,
`rt_view_context_size_from_bsg`, and
`rt_view_context_scale_from_bsg`, and writes grid state through
`rt_view_context_grid_state_set_bsg`.

The RT legacy adapter now exposes
`rt_view_context_settings_shared_bsg`, implemented as a delegating wrapper
over the existing typed shared-settings adapter.  `rt_view_legacy_bsg` test
coverage now checks null, distinct, and shared context behavior for the new
wrapper.  The pivot guard requires the context helper tokens in
`src/mged/mged_dm.h` and rejects the old typed ADC, grid, shared-settings,
size, and scale helper calls from returning to that header.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build rt_view_legacy_bsg bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.52 seconds.  The longest
member was `libbsg_api_hygiene` at 41.13 seconds, followed by
`brlobol_pivot_guard` at 13.83 seconds.  No individual test exceeded the
180-second cap.

A sixty-sixth MGED saved-view button context batch now keeps the front-end
button save/restore path from calling typed RT BSG helpers directly.
`src/mged/buttons.c` borrows the retained view as `void *view_ctx` in
`bsg_vsave` and `bsg_vrestore`, reads saved scale, rotation, and center
snapshots through `rt_view_context_scale_from_bsg`,
`rt_view_context_rotation_from_bsg`, and
`rt_view_context_center_from_bsg`, and restores saved scale, rotation, and
center through `rt_view_context_scale_set_bsg`,
`rt_view_context_rotation_set_bsg`, and
`rt_view_context_center_vec_set_bsg`.

The pivot guard now requires the context helper tokens in `src/mged/buttons.c`
and rejects the old typed saved-view scale, rotation, and center helper calls
from returning to the button path.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|saveview|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 59.14 seconds.  The longest
member was `libbsg_api_hygiene` at 40.87 seconds, followed by
`brlobol_pivot_guard` at 13.69 seconds.  No individual test exceeded the
180-second cap.

A sixty-fifth MGED ADC context batch now keeps angle/distance cursor
view-math snapshots from calling typed RT BSG helpers directly.
`src/mged/adc.c` borrows the retained view as `void *view_ctx` and reads
scale, model-to-view, and view-to-model state through
`rt_view_context_scale_from_bsg`,
`rt_view_context_model2view_from_bsg`, and
`rt_view_context_view2model_from_bsg`.  The neutral `rt_view_adc_*`
calculation helpers remain unchanged.

The pivot guard now requires the context helper tokens in `src/mged/adc.c`
and rejects `rt_view_scale_from_bsg`, `rt_view_model2view_from_bsg`, and
`rt_view_view2model_from_bsg` from returning to the ADC command/draw path.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(adc|draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 13/13 passing in 58.78 seconds.  The longest
member was `libbsg_api_hygiene` at 40.87 seconds, followed by
`brlobol_pivot_guard` at 13.76 seconds.  No individual test exceeded the
180-second cap.

A sixty-fourth MGED usepen/grid context batch now keeps highlighted-shape
selection bookkeeping and grid snap/draw view-state access from calling typed
RT BSG helpers directly.  `src/mged/usepen.c` borrows the retained view as
`void *view_ctx` and reads refresh drawn-count and view-center state through
`rt_view_context_refresh_drawn_count_from_bsg` and
`rt_view_context_center_from_bsg`, while the highlighted-shape selection
mirror continues to pass the retained view as an opaque draw-selection
context.  `src/mged/grid.c` uses context helpers for grid auto-size view info,
draw/snap scale and model-to-view reads, keypoint and view-center
view-to-model reads, center snapshots, and snapped center-vector writes.

The pivot guard now requires the context helper tokens in both files and
rejects `rt_view_refresh_drawn_count_from_bsg`, `rt_view_center_from_bsg`,
`rt_view_info_from_bsg`, `rt_view_scale_from_bsg`,
`rt_view_center_vec_set_bsg`, `rt_view_model2view_from_bsg`, and
`rt_view_view2model_from_bsg` from returning to those MGED paths as
applicable.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|keypoint|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 12/12 passing in 58.37 seconds.  The longest
member was `libbsg_api_hygiene` at 40.88 seconds, followed by
`brlobol_pivot_guard` at 13.74 seconds.  No individual test exceeded the
180-second cap.

A sixty-third MGED axes/set context batch now keeps axes overlay view
snapshots and simple view setting hooks from calling typed RT BSG helpers
directly.  `src/mged/axes.c` borrows the retained view as `void *view_ctx`
and reads view info, model-to-view, and rotation through
`rt_view_context_info_from_bsg`, `rt_view_context_model2view_from_bsg`, and
`rt_view_context_rotation_from_bsg` for edit axes, model axes, and view axes.
`src/mged/set.c` uses context helpers for absolute translation model-to-view,
center, and scale reads, and uses context setters for perspective, coordinate
mode, and rotate-about mode.

The pivot guard now requires the context helper tokens in both files and
rejects `rt_view_info_from_bsg`, `rt_view_model2view_from_bsg`,
`rt_view_rotation_from_bsg`, `rt_view_scale_from_bsg`,
`rt_view_center_from_bsg`, `rt_view_perspective_set_bsg`,
`rt_view_coord_set_bsg`, and `rt_view_rotate_about_set_bsg` from returning to
those MGED paths as applicable.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 57.98 seconds.  The longest
member was `libbsg_api_hygiene` at 40.98 seconds, followed by
`brlobol_pivot_guard` at 13.67 seconds.  No individual test exceeded the
180-second cap.

A sixty-second MGED make/setup/edpipe context batch now keeps three small
retained-view snapshot paths from calling typed RT BSG helpers directly.
`src/mged/chgmodel.c` borrows the retained view as `void *view_ctx` and reads
make-default center/scale through `rt_view_context_center_from_bsg` and
`rt_view_context_scale_from_bsg`.  `src/mged/setup.c` uses
`rt_view_context_center_from_bsg` for the initial view-origin snapshot after
view initialization.  `src/mged/edpipe.c` uses
`rt_view_context_view2model_from_bsg` for pipe-pick direction setup.

The pivot guard now requires the context helper tokens in all three files and
rejects `rt_view_center_from_bsg`, `rt_view_scale_from_bsg`, and
`rt_view_view2model_from_bsg` from returning to those MGED paths as
applicable.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 58.20 seconds.  The longest
member was `libbsg_api_hygiene` at 41.18 seconds, followed by
`brlobol_pivot_guard` at 13.70 seconds.  No individual test exceeded the
180-second cap.

A sixty-first MGED title/plot/scroll context batch now keeps three
read-only MGED view snapshot paths from calling typed RT BSG helpers
directly.  `src/mged/titles.c` now borrows the retained view as an opaque
`void *view_ctx` and reads view info, center, AET, perspective, and scale
through `rt_view_context_info_from_bsg`, `rt_view_context_center_from_bsg`,
`rt_view_context_aet_from_bsg`, `rt_view_context_perspective_from_bsg`, and
`rt_view_context_scale_from_bsg`.  The area-export path in
`src/mged/plot.c` reads rotation through `rt_view_context_rotation_from_bsg`,
and the slider path in `src/mged/scroll.c` reads scale through
`rt_view_context_scale_from_bsg`.

The pivot guard now requires the context helper tokens in all three files
and rejects the older typed `rt_view_*_from_bsg` helper calls from returning
to these MGED title/status, area-plot, and slider paths.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 57.89 seconds.  The longest
member was `libbsg_api_hygiene` at 40.87 seconds, followed by
`brlobol_pivot_guard` at 13.69 seconds.  No individual test exceeded the
180-second cap.

A sixtieth MGED motion/rectangle context batch now keeps two small MGED
view-state snapshot paths from calling typed RT BSG helpers directly.  The
X11 motion-event handler in `src/mged/doevent.c` now borrows the current
retained view as an opaque `void *view_ctx` and reads scale, center, and
view-to-model state through `rt_view_context_scale_from_bsg`,
`rt_view_context_center_from_bsg`, and
`rt_view_context_view2model_from_bsg`.  Rectangle zoom in
`src/mged/rect.c` now uses `rt_view_context_center_from_bsg`,
`rt_view_context_model2view_from_bsg`, and
`rt_view_context_view2model_from_bsg` for its view snapshot.

The pivot guard now requires the context helper tokens in both files and
rejects `rt_view_scale_from_bsg`, `rt_view_center_from_bsg`,
`rt_view_model2view_from_bsg`, and `rt_view_view2model_from_bsg` from
returning to those MGED paths as applicable.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 57.96 seconds.  The longest
member was `libbsg_api_hygiene` at 40.98 seconds, followed by
`brlobol_pivot_guard` at 13.62 seconds.  No individual test exceeded the
180-second cap.

A fifty-ninth MGED dm-generic context batch now keeps
`src/mged/dm-generic.c` from calling typed RT BSG view helpers for its
remaining mouse/ADC input snapshots and common view hook updates.  The `dm m`
branch borrows `view_state->vs_gvp` as an opaque `void *view_ctx` and reads
center, model-to-view, and view-to-model state through
`rt_view_context_center_from_bsg`, `rt_view_context_model2view_from_bsg`, and
`rt_view_context_view2model_from_bsg`.  The `dm adc` branch reads scale and
view-to-model state through `rt_view_context_scale_from_bsg` and
`rt_view_context_view2model_from_bsg`.  The shared view hooks now call
`rt_view_context_refresh_request_bsg` and `rt_view_context_zclip_set_bsg`.

The pivot guard now requires those context helper tokens in `dm-generic.c`
and rejects `rt_view_scale_from_bsg`, `rt_view_center_from_bsg`,
`rt_view_model2view_from_bsg`, `rt_view_view2model_from_bsg`,
`rt_view_refresh_request_bsg`, and `rt_view_zclip_set_bsg` from returning to
this MGED common-DM path.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 57.79 seconds.  The longest
member was `libbsg_api_hygiene` at 40.87 seconds, followed by
`brlobol_pivot_guard` at 13.60 seconds.  No individual test exceeded the
180-second cap.

A fifty-eighth MGED RTIF RT-command context batch now keeps the `rmats`
matrix-file animation loop in `src/mged/rtif.c` from calling typed RT BSG
view helpers directly.  The command still borrows the current retained MGED
view from `view_state->vs_gvp`, but stores it as an opaque `void *view_ctx`
for the loop and routes scale, rotation, center, and view-to-model access
through `rt_view_context_scale_set_bsg`,
`rt_view_context_rotation_set_bsg`, `rt_view_context_center_vec_set_bsg`, and
`rt_view_context_view2model_from_bsg`.

The pivot guard now requires those context helper tokens in `rtif.c`, keeps
the display-manager handoff requirement on
`rt_view_context_display_manager_set_bsg`, and rejects
`rt_view_scale_set_bsg`, `rt_view_rotation_set_bsg`,
`rt_view_center_vec_set_bsg`, and `rt_view_view2model_from_bsg` from returning
to this MGED RT command path.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build bin/mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 11/11 passing in 57.92 seconds.  The longest
member was `libbsg_api_hygiene` at 40.90 seconds, followed by
`brlobol_pivot_guard` at 13.66 seconds.  No individual test exceeded the
180-second cap.

A fiftieth TclCAD label and faceplate state-accessor batch now keeps the
remaining active TclCAD command files from reaching through retained Tcl-data
members.  `src/libtclcad/view/labels.c` now obtains staged/live label state
through `tclcad_view_label_state_from_view_ctx`, and
`src/libtclcad/view/faceplate.c` reads/writes primitive-label settings through
neutral `rt_view_other_state` snapshots via
`tclcad_view_prim_labels_state_from_view_ctx` and
`tclcad_view_prim_labels_state_set`.

`src/libtclcad/view_data.c` now owns the retained `gv_data_labels`,
`gv_sdata_labels`, and `gv_prim_labels` field access, together with the
existing polygon/data helpers.  The pivot guard requires the new helper
declarations, adapter implementations, label/faceplate caller tokens, and
rejects raw retained label or primitive-label Tcl-data member access from
returning to the callers.  A targeted scan now finds retained Tcl-data member
spelling only in `view_data.c`, the intended TclCAD adapter owner.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 156.63 seconds.  The longest
member was `ged_test_state_services` at 97.77 seconds, followed by
`libbsg_api_hygiene` at 40.88 seconds and `brlobol_pivot_guard` at
13.76 seconds.  No individual test exceeded the 180-second cap.

A forty-ninth TclCAD mouse polygon state-accessor batch now keeps
`src/libtclcad/mouse.c` from reaching through retained Tcl-data members for
mouse-side polygon state and polygon mode reads.  The edit-motion path now
stores its initial edit mode through `tclcad_view_polygon_mode_from_view_ctx`,
the circle/contour/ellipse/rectangle mouse polygon builders fetch staged or
live polygon state through `tclcad_view_polygon_state_from_view_ctx`, and
square-mode rectangle handling reads polygon mode through the same helper
boundary.

This removes the remaining `gv_data_polygons`, `gv_sdata_polygons`, and
`gv_polygon_mode` member access from `mouse.c`, leaving that command family
aligned with the helper boundary already used by `commands.c` and
`polygons.c`.  The pivot guard now requires the mouse helper tokens and
rejects raw retained Tcl polygon-state member spelling from returning there.
The remaining active TclCAD retained Tcl-data member users are label state in
`src/libtclcad/view/labels.c` and primitive-label faceplate settings in
`src/libtclcad/view/faceplate.c`.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 152.76 seconds.  The longest
member was `ged_test_state_services` at 93.86 seconds, followed by
`libbsg_api_hygiene` at 40.93 seconds and `brlobol_pivot_guard` at
13.74 seconds.  No individual test exceeded the 180-second cap.

A forty-eighth TclCAD polygon state-accessor batch now keeps
`src/libtclcad/polygons.c` from reaching through retained Tcl-data members for
polygon state selection, polygon mode reads/writes, and data-vZ reads.  The
polygon command family now uses the shared TclCAD-private state accessors from
`src/libtclcad/view_data.c`: `tclcad_view_polygon_state_from_view_ctx`,
`tclcad_view_polygon_mode_from_view_ctx`, `tclcad_view_polygon_mode_set`, and
`tclcad_view_data_vZ_from_view_ctx`.

This removes the polygon-local `tclcad_polygons_tcl_data` and
`tclcad_polygons_state` wrappers and keeps command setup, append/replace
parsing, mode builders, contour-build cleanup, and draw-view synchronization
on the same TclCAD-local state boundary as `commands.c`.  The pivot guard now
requires those shared helpers in `polygons.c`, stops requiring
`tclcad_view_state` there, and rejects the removed wrappers plus direct
polygon Tcl-data member names from returning.  The underlying storage remains
transitional, but two active TclCAD command families now depend on helper
semantics rather than retained member layout.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 153.10 seconds.  The longest
member was `ged_test_state_services` at 94.45 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds and `brlobol_pivot_guard` at
13.56 seconds.  No individual test exceeded the 180-second cap.

A forty-seventh TclCAD command state-accessor batch now keeps
`src/libtclcad/commands.c` from reaching through
`tclcad_view_tcl_data_from_view_ctx(...)->gv_*` for command-side retained
Tcl-data fields.  `src/libtclcad/view_data.c` now owns TclCAD-private
accessors for polygon mode, data vZ, hide state, data/sdata polygon-state
selection, and polygon contour-build cflag handling.  The command layer now
uses that helper surface instead of directly spelling retained Tcl-data member
names for ordinary command state transitions, data move/pick checks, data-vZ
get/set, hide query, and idle-mode cleanup.

The pivot guard now requires those helper declarations/definitions in
`src/libtclcad/tclcad_private.h` and `src/libtclcad/view_data.c`, requires
`commands.c` to use the helper surface, and rejects command-side
`tclcad_view_tcl_data_from_view_ctx(...)->gv_*` reach-through or direct
`gv_polygon_mode`, `gv_data_vZ`, `gv_hide`, `gv_data_polygons`, and
`gv_sdata_polygons` spellings from returning.  The underlying storage is still
the transitional TclCAD view state alias, but another active command layer no
longer depends on its retained field layout.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged ged_test_state_services`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 157.47 seconds.  The longest
member was `ged_test_state_services` at 98.64 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds and `brlobol_pivot_guard` at
13.74 seconds.  No individual test exceeded the 180-second cap.

A forty-sixth libged polyclip polygon-export snapshot batch now keeps
`src/libged/polyclip.cpp` from depending on `bsg/tcl_data.h` or
`bsg_data_polygon_state`.  `include/ged/view.h` now defines neutral
`struct ged_polygon_export_state` with the transform, scale, depth, and
`bg_polygons` snapshot needed for sketch export, and `ged_export_polygon`
accepts `const struct ged_polygon_export_state *` instead of an opaque pointer
that libged casts to retained BSG polygon state.

`src/libtclcad/polygons.c` now owns the transitional TclCAD-to-GED adapter:
when the Tcl polygon export command runs, it copies the private
`tclcad_polygon_state` fields into the neutral GED export snapshot and passes
that snapshot to libged.  `src/libged/tests/test_state_services.cpp` constructs
the neutral snapshot directly for its event-order fixture, so that test no
longer relies on the BSG polygon-state typedef either.  The pivot guard
requires the neutral `ged_export_polygon` signature in `include/ged/view.h` and
rejects `bsg/tcl_data.h` or `bsg_data_polygon_state` from returning to
`polyclip.cpp`.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libged libtclcad mged ged_test_state_services`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 151.53 seconds.  The longest
member was `ged_test_state_services` at 92.63 seconds, followed by
`libbsg_api_hygiene` at 40.84 seconds and `brlobol_pivot_guard` at
13.84 seconds.  No individual test exceeded the 180-second cap.

A forty-fifth RT/TclCAD Tcl-data opaque adapter boundary batch now keeps
`include/rt/view_legacy_bsg.h` from exposing retained `struct bsg_data_tclcad`
in TclCAD data adapter signatures.  `rt_view_context_tclcad_data_set_bsg`
now accepts `void *tcl_data`; `src/librt/view_legacy_bsg.c` owns the cast
back to the retained Tcl-data storage while the adapter test continues to
verify the actual retained view binding.  A later TclCAD initializer
ownership slice retired the old RT-level TclCAD data initializer entirely, so
`src/libtclcad/view_data.c` now owns the transitional TclCAD data
initialization/bind/unbind flow without forcing the RT legacy header to
publish a BSG Tcl-data type.

The pivot guard now requires the opaque Tcl-data adapter signatures in
`include/rt/view_legacy_bsg.h`, requires the two TclCAD adapter symbols to
remain present, and rejects typed `struct bsg_data_tclcad` signatures or
forward declarations from returning to that public RT legacy adapter header.
This is still a bridge: TclCAD's private alias block intentionally remains
backed by retained Tcl-data structs until the storage itself is re-homed, but
the public RT/TclCAD boundary is now narrower.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build rt_view_legacy_bsg libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 151.31 seconds.  The longest
member was `ged_test_state_services` at 92.57 seconds, followed by
`libbsg_api_hygiene` at 40.96 seconds and `brlobol_pivot_guard` at
13.56 seconds.  No individual test exceeded the 180-second cap.

A forty-fourth TclCAD private state-alias batch now keeps migrated TclCAD
command, mouse, polygon, label, and view-data code from spelling BSG Tcl-data
type names directly.  `src/libtclcad/tclcad_private.h` owns TclCAD-local
aliases for the transitional per-view Tcl state, polygon state, and label
state.  `src/libtclcad/commands.c`, `src/libtclcad/mouse.c`,
`src/libtclcad/polygons.c`, `src/libtclcad/view/labels.c`, and
`src/libtclcad/view_data.c` now use those local names instead of
`bsg_data_tclcad`, `bsg_data_polygon_state`, or `bsg_data_label_state`.

This is still a transitional bridge: the actual storage remains backed by the
retained Tcl-data structs while the drawing backend migration proceeds.  The
benefit is that active TclCAD code now talks in TclCAD-local terms and the BSG
type names are isolated to the private alias block.  The pivot guard requires
that alias block and rejects the old BSG Tcl-data type spellings from
returning to the migrated TclCAD files.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 151.64 seconds.  The longest
member was `ged_test_state_services` at 92.79 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds and `brlobol_pivot_guard` at
13.74 seconds.  No individual test exceeded the 180-second cap.

A forty-third TclCAD commands new-view lifecycle batch now keeps
`src/libtclcad/commands.c` from naming retained `struct bsg_view` locally.
The `to_new_view` path now uses opaque `void *` view contexts throughout:
it reuses the active GED view when that view has not yet been bound to
TclCAD data, otherwise creates a retained context with
`rt_view_context_create_with_set_bsg`; it then sets display-manager,
view-name, TclCAD view-data binding, callback-table ownership, view-set
membership, GED free-table ownership, framebuffer client data, default
bindings, Tcl command client data, and active-view handoff through opaque
RT/GED/TclCAD context helpers.

`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
`rt_view_context_callbacks_set_bsg`, with coverage in
`src/librt/tests/view_legacy_bsg.c`, so TclCAD no longer writes
`new_gdvp->callbacks` directly.  The pivot guard now requires the opaque
new-view lifecycle tokens and rejects local `struct bsg_view`, typed
allocation, typed lifecycle init, `new_gdvp`/`active_gdvp`, and direct
callback-field writes from returning to `commands.c`.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build rt_view_legacy_bsg libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 156.98 seconds.  The longest
member was `ged_test_state_services` at 98.40 seconds, followed by
`libbsg_api_hygiene` at 40.84 seconds and `brlobol_pivot_guard` at
13.52 seconds.  No individual test exceeded the 180-second cap.

A forty-second TclCAD commands command-local opaque-view-context batch now
keeps ordinary `src/libtclcad/commands.c` command-side view handling from
naming retained `struct bsg_view *gdvp` locals.  Named-view command lookups
now store `ged_view_find_ctx` results as `void *`, draw-view callbacks carry
their incoming `draw_view_ctx` opaquely, view-table iteration uses opaque
`BU_PTBL_GET` results, and the data move/pick helper families now take opaque
view contexts.  Default binding setup and edited-path redraw callback storage
use the same opaque context form.

The pivot guard now requires opaque command-local lookup/helper tokens and
rejects typed command-local `gdvp` signatures, `ged_view_find_ctx` casts,
draw-view casts, and view-table casts from returning.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 150.78 seconds.  The longest
member was `ged_test_state_services` at 92.12 seconds, followed by
`libbsg_api_hygiene` at 40.90 seconds and `brlobol_pivot_guard` at
13.53 seconds.  No individual test exceeded the 180-second cap.

A forty-first TclCAD mouse opaque-view-context sweep now keeps
`src/libtclcad/mouse.c` from naming retained `struct bsg_view` locally.  The
local mouse display, view-name, display-dimension, previous-mouse,
delta-clamp, rotate-scale, scale-scale, and inverse-rotation helpers now accept
opaque view contexts and route through the existing RT context helper surface.
All named-view command lookups in `mouse.c`, including the scale macro
callers, now store `void *` contexts instead of casting to a retained BSG view.

This is deliberately still a transitional bridge: `mouse.c` continues to call
RT/GED context-form BSG adapters while the backend migration proceeds, but the
TclCAD mouse command layer no longer depends on the retained BSG view type
layout.  The pivot guard now requires the opaque helper signatures and
named-view lookup tokens in `mouse.c`, and rejects any local `struct bsg_view`
spelling or retained-view casts from returning there.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
`ninja -C build libtclcad mged`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 151.54 seconds.  The longest
member was `ged_test_state_services` at 93.00 seconds, followed by
`libbsg_api_hygiene` at 40.82 seconds and `brlobol_pivot_guard` at
13.48 seconds.  No individual test exceeded the 180-second cap.

A fortieth TclCAD mouse polygon opaque-view-context batch now keeps the
mouse-side polygon builder helper boundary off `struct bsg_view`.  The
`go_mouse_poly_circ`, `go_mouse_poly_cont`, `go_mouse_poly_ell`, and
`go_mouse_poly_rect` active-view entry points now carry `void *` contexts from
their draw-view argument, and the corresponding named-view command wrappers now
use `ged_view_find_ctx` without retaining a local typed BSG view.  The
`to_mouse_poly_*_func` helpers and private TclCAD prototypes now accept
`void *view_ctx` and pass that opaque context into RT view mouse-position,
view-math, snap, active-view, polygon-data replacement, and refresh helpers.

The pivot guard now requires the four mouse polygon helper signatures and
opaque context handoff tokens in `mouse.c`/`tclcad_private.h`, and rejects
typed BSG mouse polygon helper signatures from returning.  This completes the
mouse-side handoff enabled by the previous polygon command batch without
pretending all of `mouse.c` is opaque yet; broader TclCAD typed BSG view
plumbing remains in other mouse command/view-motion code.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 152.23 seconds.  The longest
member was `ged_test_state_services` at 93.51 seconds, followed by
`libbsg_api_hygiene` at 40.94 seconds and `brlobol_pivot_guard` at
13.59 seconds.  No individual test exceeded the 180-second cap.

A thirty-ninth TclCAD polygon opaque-view-context batch now keeps
`src/libtclcad/polygons.c` from naming retained `struct bsg_view` locally.
Polygon command helper signatures, named-view lookups, GUI callback wrappers,
polygon view snapshots, and polygon mode builders now carry `void *` view
contexts into the existing RT/GED context helper surface.  The private
`to_data_polygons_func`, `to_poly_circ_mode_func`,
`to_poly_cont_build_end_func`, `to_poly_ell_mode_func`, and
`to_poly_rect_mode_func` prototypes now take opaque view contexts, which lets
the mouse polygon builders pass their current retained views without exposing
the polygon command layer to the retained BSG type.  The redundant
`to_poly_cont_build` named-view lookup was removed while the handoff was being
converted.

The pivot guard now requires an opaque context token in `polygons.c` and
rejects local `struct bsg_view` spelling or retained-view casts from returning
there.  Remaining TclCAD typed BSG view plumbing is now concentrated in
`commands.c`, `mouse.c`, and the intentionally deferred mouse polygon helper
prototypes in `tclcad_private.h`.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 154.01 seconds.  The longest
member was `ged_test_state_services` at 95.19 seconds, followed by
`libbsg_api_hygiene` at 40.86 seconds and `brlobol_pivot_guard` at
13.73 seconds.  No individual test exceeded the 180-second cap.

A thirty-eighth TclCAD polygon Tcl-data state batch now keeps
`src/libtclcad/polygons.c` off direct retained BSG `gv_tcl` access.  Polygon
data/sdata state selection, polygon mode reads/writes, data-vZ reads, and
contour-build cflag resets now route through `tclcad_view_tcl_data_from_view_ctx`
via local `tclcad_polygons_tcl_data` and `tclcad_polygons_state` helpers instead
of spelling `gdvp->gv_tcl`.  This leaves the existing typed view plumbing in
place for later migration while removing the polygon command layer's dependency
on the BSG Tcl-data member layout.

The pivot guard now requires the Tcl-data accessor path in `polygons.c` and
rejects direct `gdvp->gv_tcl` reach-through from returning there.  With the
previous command-side batch, direct TclCAD `gv_tcl` code access is now removed
from the active `commands.c`/`polygons.c` migration pockets; broader typed BSG
view plumbing still remains in `commands.c`, `mouse.c`, and `polygons.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 152.65 seconds.  The longest
member was `ged_test_state_services` at 94.06 seconds, followed by
`libbsg_api_hygiene` at 40.90 seconds and `brlobol_pivot_guard` at
13.48 seconds.  No individual test exceeded the 180-second cap.

A thirty-seventh TclCAD commands Tcl-data state batch now keeps
`src/libtclcad/commands.c` off direct retained BSG `gv_tcl` access.
`src/libtclcad/view_data.c` now exposes
`tclcad_view_tcl_data_from_view_ctx`, and command-side polygon/edit-mode
state, data polygon state, data-vZ, and hide reads/writes now route through
that accessor instead of spelling `gdvp->gv_tcl`.  This preserves the current
TclCAD data owner while removing the command layer's dependency on the BSG
view member layout.

The pivot guard now requires `tclcad_view_tcl_data_from_view_ctx` in
`commands.c` and `view_data.c`, and rejects direct `gdvp->gv_tcl`
reach-through from returning to `commands.c`.  Remaining TclCAD direct
`gv_tcl` work is now concentrated in `src/libtclcad/polygons.c`; broader typed
view plumbing also remains in `polygons.c` and `mouse.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 151.65 seconds.  The longest
member was `ged_test_state_services` at 92.72 seconds, followed by
`libbsg_api_hygiene` at 40.86 seconds and `brlobol_pivot_guard` at
13.73 seconds.  No individual test exceeded the 180-second cap.

A thirty-sixth TclCAD commands view-data lifecycle batch now keeps
`src/libtclcad/commands.c` off direct retained BSG `u_data` ownership.  TclCAD
command callbacks now use `tclcad_view_data_from_view_ctx` for per-view data
reads, the new-view path binds TclCAD view data after RT view initialization
with `tclcad_view_data_bind_view_ctx`, and Tcl object teardown clears the
view's TclCAD binding with `tclcad_view_data_unbind_view_ctx` before freeing
the TclCAD storage.

`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
`rt_view_context_user_data_set_bsg` and
`rt_view_context_tclcad_data_set_bsg`, with direct coverage in
`src/librt/tests/view_legacy_bsg.c`.  `src/libtclcad/view_data.c` owns the
private TclCAD bind/unbind helpers on top of those RT context setters.  The
pivot guard requires the new helpers and rejects direct `commands.c`
`gdvp->u_data`, `data->gdvp->u_data`, active/new view `u_data`, and lifecycle
`gv_tcl` assignment from returning.  Remaining command-side direct `gv_tcl`
member reads/writes are polygon/edit-mode state and remain a later migration
pocket.

Verification on FreeBSD/clang passed with:
`ninja -C build librt libtclcad mged rt_view_legacy_bsg`,
`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 15/15 passing in 154.27 seconds.  The longest
member was `ged_test_state_services` at 95.61 seconds, followed by
`libbsg_api_hygiene` at 40.91 seconds and `brlobol_pivot_guard` at
13.53 seconds.  No individual test exceeded the 180-second cap.

A thirty-fifth TclCAD mouse view-data lookup batch now keeps
`src/libtclcad/mouse.c` from reading `struct bsg_view::u_data` directly for
TclCAD per-view data.  Mouse constrained rotation/translation callbacks,
object edit-motion callbacks, polygon mouse-build helpers, view rotate/scale,
and view translate now call `tclcad_view_data_from_view_ctx(gdvp)` instead of
casting `gdvp->u_data` locally.  This leaves the surrounding typed BSG mouse
view plumbing in place for later view-math/context migrations while removing
the local view-storage field dependency from the mouse command layer.

The pivot guard now requires `tclcad_view_data_from_view_ctx` in
`src/libtclcad/mouse.c` and rejects direct `gdvp->u_data` reach-through from
returning there.  Remaining direct TclCAD view-data lifecycle reads/writes are
still concentrated in `src/libtclcad/commands.c` and should move as a separate
ownership slice.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 14/14 passing in 154.00 seconds.  The longest
member was `ged_test_state_services` at 95.39 seconds, followed by
`libbsg_api_hygiene` at 40.87 seconds and `brlobol_pivot_guard` at
13.54 seconds.  No individual test exceeded the 180-second cap.

A thirty-fourth TclCAD view-data batch now keeps
`src/libtclcad/view_data.c` off direct BSG view storage.  TclCAD per-view data
lookup now calls `rt_view_context_user_data_from_bsg` instead of including
`bsg/defines.h`, casting the opaque context to `struct bsg_view *`, and
reading `u_data` locally.  `include/rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` now expose the narrow user-data context adapter,
with direct coverage in `src/librt/tests/view_legacy_bsg.c`.

The pivot guard now requires `src/libtclcad/view_data.c` to use the context
user-data adapter and rejects the old BSG defines include, local
`struct bsg_view` spelling/cast, direct `u_data` access, and direct
`bsg_data_tclcad_init` route from returning to this view-data isolation file.

Verification on FreeBSD/clang passed with:
`ninja -C build librt libtclcad mged rt_view_legacy_bsg`,
`ctest --test-dir build -R "^rt_view_legacy_bsg$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_(state_services|view_command|view_independent|drawing_basic|drawing_quad|drawing_select|drawing_lod|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 22/22 passing in 205.20 seconds.  The longest
member was `ged_test_state_services` at 97.30 seconds, followed by
`libbsg_api_hygiene` at 40.85 seconds, `ged_test_drawing_select` at
16.37 seconds, `brlobol_pivot_guard` at 13.48 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_lod` at
10.99 seconds.  No individual test exceeded the 180-second cap.

A thirty-third GED draw realization batch now keeps `src/libged/draw.cpp`'s
retained realization controller on opaque view contexts.  The CSG adaptive
wireframe path, BoT/BRep mesh-LoD realization paths, standard wireframe
fallback path, draw-mode realization dispatch, bounds refresh, scene-view
policy snapshot, and primitive wireframe publication now carry `void *view_ctx`
and call context-form RT/GED helpers instead of storing local
`struct bsg_view *` values.

`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
`rt_view_context_obb_from_bsg`, with direct unit coverage in
`src/librt/tests/view_legacy_bsg.c`, so `draw.cpp` can perform the adaptive
orthographic visibility check without reading BSG view OBB fields.  The
retained draw bridge owns the other context wrappers:
`ged_draw_scene_ref_realization_set_view_context_policy`,
`ged_draw_mesh_lod_load_view_scene_ref_context`,
`ged_draw_scene_ref_update_bounds_context`, and
`ged_draw_scene_ref_view_context`.  The private primitive wireframe publisher
now accepts an opaque view context at the `ged_draw_scene_ref_*` boundary.

The pivot guard now requires the context-form draw realization helpers in
`draw.cpp` and rejects local `struct bsg_view` storage/casts, direct
`bsg_scene_update_bounds`/`bsg_scene_view`, old typed
view-info/LoD/perspective/scale helpers, direct OBB field access, and the old
typed mesh-LoD view-load
helper from returning to this file.  The broad non-bridge libged typed-view
scan now returns no production hits.

Verification on FreeBSD/clang passed with:
`ninja -C build libged librt mged`,
`ninja -C build rt_view_legacy_bsg`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(state_services|view_command|view_independent|drawing_basic|drawing_quad|drawing_select|drawing_lod|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 22/22 passing in 201.31 seconds.  The longest
member was `ged_test_state_services` at 93.50 seconds, followed by
`libbsg_api_hygiene` at 40.87 seconds, `ged_test_drawing_select` at
16.26 seconds, `brlobol_pivot_guard` at 13.66 seconds,
`ged_test_drawing_quad` at 12.68 seconds, and `ged_test_drawing_lod` at
10.90 seconds.  No individual test exceeded the 180-second cap.

A thirty-second GED lifecycle/storage batch now keeps `src/libged/ged.cpp`'s
GED-owned default view allocation, GED free-view teardown, active-view setter,
and framebuffer refresh display-manager lookup on opaque RT view-context
helpers.  `ged_init` now creates the default view through
`rt_view_context_create_with_set_bsg`, registers it with
`rt_view_set_context_add_bsg`, and stores the opaque context in GED's
free-view table.  `ged_free` tears down GED-owned views through
`rt_view_context_free_bsg`, `ged_view_active_ctx_set` avoids a local typed BSG
cast, and `ged_rt_fb_refresh` uses
`rt_view_context_display_manager_from_bsg`.

The pivot guard now requires the `ged.cpp` lifecycle/display-manager path to
use the context-form create, set-add, free, and display-manager helpers.  It
rejects local `struct bsg_view` storage/casts, typed BSG view allocation,
`rt_view_init_bsg`, `rt_view_free_bsg`, `rt_view_set_add_view_bsg`, and
`rt_view_display_manager_from_bsg` from returning to this file.  The broad
non-bridge libged typed-view scan now returns no production hits after the
later draw realization batch.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(state_services|view_command|view_independent|drawing_basic|drawing_quad|drawing_select|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 20/20 passing in 187.74 seconds.  The longest
member was `ged_test_state_services` at 91.06 seconds, followed by
`libbsg_api_hygiene` at 40.82 seconds, `ged_test_drawing_select` at
16.35 seconds, `brlobol_pivot_guard` at 13.44 seconds, and
`ged_test_drawing_quad` at 12.69 seconds.  No individual test exceeded the
180-second cap.

A thirty-first GED display-manager attach lifecycle batch now keeps freshly
created `dm attach` target views on opaque RT allocation helpers.  In
`src/libged/dm/dm.c`, the fallback attach path now calls
`rt_view_context_create_with_set_bsg`, registers the returned opaque context
with `rt_view_set_context_add_bsg`, and records that same context in GED's
free-view table.  The command-side display-manager implementation no longer
allocates, stores, or casts local `struct bsg_view *` values.

The pivot guard now requires the create-with-set lifecycle helper in `dm.c`
and rejects typed `struct bsg_view` storage/casts, typed BSG view allocation,
`rt_view_context_init_bsg`, `rt_view_init_bsg`, and
`rt_view_set_add_view_bsg` from returning to this command-side
display-manager path.  The broad non-bridge libged typed-view scan now
reports only `src/libged/draw.cpp` retained realization internals after the
later GED lifecycle/storage batch.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|view_independent|drawing_basic|drawing_quad|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 18/18 passing in 81.00 seconds.  The longest
member was `libbsg_api_hygiene` at 40.86 seconds, followed by
`brlobol_pivot_guard` at 13.76 seconds, `ged_test_drawing_quad` at
12.99 seconds, and `ged_test_drawing_basic` at 7.52 seconds.  No individual
test exceeded the 180-second cap.

A thirtieth GED selection-state batch now keeps native selection draw-sync on
opaque GED draw-view contexts.  `src/libged/selection_state.cpp` now stores
per-view selected-path snapshots as `std::map<void *, ...>`, iterates listed
GED views as `void *`, routes retained selection path snapshots through
`ged_draw_view_context_selection_path_foreach`, clears draw selections through
`ged_draw_view_context_selection_clear`, resolves a shape ref's owning view
through `ged_draw_shape_ref_view_context`, and republishes selected shape refs
through `ged_draw_view_selection_add_shape_ref_context`.

The retained bridge owns the new narrow wrappers for this service boundary:
`ged_draw_view_context_selection_path_foreach` and
`ged_draw_view_context_selection_clear` in
`src/libged/bsg_ged_draw_view.c`,
`ged_draw_shape_ref_view_context` in `src/libged/bsg_ged_draw_refs.c`, and
`ged_draw_view_selection_add_shape_ref_context` in
`src/libged/bsg_ged_draw_records.c`.  The typed BSG casts remain in the bridge
instead of the selection-state service.

The pivot guard now requires the context-form retained selection helpers in
`selection_state.cpp`; it rejects local `struct bsg_view` storage/casts and
the old typed shape-ref view, selection iteration, selected-shape publication,
and selection-clear calls from returning to this service.
The broad non-bridge libged typed-view scan no longer reports
`selection_state.cpp`; the remaining production hits are now concentrated in
`draw.cpp` retained realization internals and `ged.cpp` lifecycle/storage.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(drawing_select|selection_semantics|ged_draw_scene|view_command|view_independent|drawing_basic|drawing_quad|mged_bsg|mged_shaded_mode_bsg)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 18/18 passing in 97.78 seconds.  The longest
member was `libbsg_api_hygiene` at 41.27 seconds, followed by
`ged_test_drawing_select` at 16.78 seconds, `brlobol_pivot_guard` at
13.57 seconds, `ged_test_drawing_quad` at 12.79 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.  No individual test exceeded the
180-second cap.

A twenty-ninth GED zap command batch now keeps `src/libged/zap/zap2.cpp` on
opaque view contexts after named and listed view lookup.  The command now
carries `void *view_ctx`, routes independent-view checks through
`rt_view_context_is_independent_bsg`, routes BSG clear operations through
`rt_view_context_clear_bsg`, routes cleared-state writes through
`rt_view_context_cleared_set_bsg`, and passes the same opaque context into
the draw transaction API's existing `void *view` field for scoped DB clears.

`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
the narrow context clear and cleared-state adapters used by zap:
`rt_view_context_clear_bsg`, `rt_view_context_cleared_from_bsg`, and
`rt_view_context_cleared_set_bsg`.  `src/librt/tests/view_legacy_bsg.c` covers
the new wrappers alongside the existing typed bridge checks.

The pivot guard now requires zap to use opaque context lookup plus context
clear, independent-state, and cleared-state helpers; it rejects local
`struct bsg_view` storage/casts and the old typed RT clear,
independent-state, and cleared-state calls from returning to this command.
The broad non-bridge libged typed-view scan no longer reports `zap2.cpp`; the
remaining production hits are concentrated in `selection_state.cpp`,
`ged.cpp` lifecycle/storage, and `draw.cpp` retained realization internals.

Verification on FreeBSD/clang passed with:
`ninja -C build libged librt mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|libbsg_api_hygiene|ged_test_(view_independent|view_command|ged_draw_scene|mged_bsg|mged_shaded_mode_bsg|drawing_basic|drawing_quad|drawing_lod|drawing_select)|regress-mged_(draw|erase|view|refresh|draw_e_erase_d_who|autoview|loadview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 19/19 passing in 107.86 seconds.  The longest
member was `libbsg_api_hygiene` at 41.15 seconds, followed by
`ged_test_drawing_select` at 16.40 seconds, `brlobol_pivot_guard` at
13.66 seconds, `ged_test_drawing_quad` at 12.58 seconds,
`ged_test_drawing_lod` at 10.89 seconds, and `ged_test_drawing_basic` at
7.53 seconds.  No individual test exceeded the 180-second cap.

A twenty-eighth GED view-command context batch now keeps the libged `view`
command family on opaque GED draw-view contexts after command dispatch.
`src/libged/view/ged_view.h` now stores `_ged_view_info::cv` as `void *`.
`src/libged/view/view.c` routes independent-view handling, independent-scope
destruction, view listing, selection counts, vZ depth calculation, vZ feature
iteration, and visible-record traversal through RT or private GED draw-view
context helpers.  `src/libged/view/labels.c`,
`src/libged/view/lines.c`, `src/libged/view/axes.c`,
`src/libged/view/lod.cpp`, `src/libged/view/polygons.c`,
`src/libged/view/objs.cpp`, and `src/libged/view/gobjs.cpp` now use
context-form feature, overlay, object, line, label, axes, LoD, and polygon
helpers instead of command-side `struct bsg_view *` calls.  The auxiliary draw
handoff now carries `void *view_ctx` in `draw_data_t`, and
`src/libged/draw.cpp` uses the context shape-draft creation path.

The retained bridge owns the new narrow adapters for this batch:
`ged_draw_view_context_selection_count`,
`ged_draw_view_context_overlay_create`,
`ged_draw_view_context_feature_depth`,
`ged_draw_view_context_feature_depth_foreach`,
`ged_draw_view_context_feature_style_apply`,
`ged_draw_view_context_feature_realize`,
`ged_draw_view_context_lines_create_model_annotation`,
`ged_draw_view_context_lines_append_point`,
`ged_draw_view_context_label_create`,
`ged_draw_view_context_axes_create`,
`ged_draw_view_context_axes_state_get`,
`ged_draw_view_context_axes_state_replace`,
the context polygon helpers, `rt_view_context_independent_scope_destroy_bsg`,
and `ged_draw_shape_ref_realize_context`.  The typed casts remain inside the
bridge/owner layer instead of the command implementation.

The pivot guard now requires opaque `_ged_view_info::cv` storage and context
helper tokens across the migrated `view` command files; it rejects typed
private view-command storage/casts and the old non-context feature, object,
polygon, selection, depth, line, label, axes, and LoD calls from returning to
this batch.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|drawing_lod|drawing_select|drawing_faceplate|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|erase|refresh|draw_e_erase_d_who|loadview|autoview)|regress-rtwiz_m35_E)$" --timeout 180 --output-on-failure`.
The focused CTest run completed 22/22 passing in 212.69 seconds.  The longest
member was `ged_test_state_services` at 96.39 seconds, followed by
`libbsg_api_hygiene` at 40.81 seconds, `ged_test_drawing_select` at
16.27 seconds, `brlobol_pivot_guard` at 13.45 seconds,
`ged_test_drawing_quad` at 12.72 seconds, `ged_test_drawing_lod` at
10.91 seconds, `ged_test_drawing_basic` at 7.50 seconds, and
`regress-rtwiz_m35_E` at 7.14 seconds.  No individual test exceeded the
180-second cap.

A twenty-seventh GED draw client-data batch now keeps the legacy draw and
evaluated-wireframe command pipelines on opaque GED draw-view contexts after
active-view lookup.  `src/libged/draw/ged_draw.h` now stores `void *view_ctx`
in both `ged_shape_data` and `_ged_client_data`; `src/libged/draw/draw.c`
routes draw shape-draft creation through
`ged_draw_shape_draft_create_context`, primitive face-set view snapshots
through `ged_draw_view_context_info_from_bsg`, draw LoD policy
save/restore/BoT-threshold adjustment through
`ged_draw_view_context_lod_policy_*`, and diagnostic edge-use fallback
publishing through `ged_draw_view_context_line_layer_builder_replace`.
`src/libged/draw/bigE.c` seeds evaluated-wireframe client data with
`ged_view_active_ctx`, and `src/libged/bsg_ged_draw_transactions.c` seeds
retained draw transactions through the same opaque client-data field.

The private retained bridge now exposes the narrow context-form adapters
needed by that command-side pipeline:
`ged_draw_shape_draft_create_context` in `bsg_ged_draw_private.h` and
`ged_draw_view_context_info_from_bsg` in `bsg_ged_draw_view_private.h`.
The typed bridge implementation still owns the internal casts.  The broad
libged typed-view read scan is now down to 7 hits: six in the intentional
`bsg_ged_draw_view.c` bridge and one remaining `view.c` dispatch assignment.

The pivot guard now requires `view_ctx` draw client-data storage and the
context-form draft, view-info, LoD, and line-layer helper tokens in
`draw.c`; it rejects typed draw client-data view storage, typed active-view
casts, typed shape-draft creation, typed view-info calls, and typed LoD calls
from returning to this draw command batch.  It also requires
evaluated-wireframe setup in `bigE.c` and retained transaction setup in
`bsg_ged_draw_transactions.c` to seed `_ged_client_data` through `view_ctx`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|drawing_lod|drawing_select|drawing_faceplate|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|erase|refresh|draw_e_erase_d_who|loadview|autoview)|regress-rtwiz_m35_E)$" --timeout 180 --output-on-failure`.
The focused CTest run completed 22/22 passing in 209.51 seconds.  The longest
member was `ged_test_state_services` at 92.89 seconds, followed by
`libbsg_api_hygiene` at 40.86 seconds, `ged_test_drawing_select` at
16.32 seconds, `brlobol_pivot_guard` at 13.37 seconds,
`ged_test_drawing_quad` at 12.76 seconds, `ged_test_drawing_lod` at
10.91 seconds, and `regress-rtwiz_m35_E` at 7.42 seconds.  No individual
test exceeded the 180-second cap.

A twenty-sixth GED display-manager command batch now keeps the libged `dm`
command's view access on opaque GED/RT view-context helpers.  In
`src/libged/dm/dm.c`, `_dm_from_view` now takes a `void *` context and calls
`rt_view_context_display_manager_from_bsg`; `_dm_find`, `_dm_cmd_list`, and
`_dm_cmd_attach` carry active and named views as `void *`; and the attach
fallback path uses `rt_view_context_width_from_bsg`,
`rt_view_context_height_from_bsg`,
`rt_view_context_dimensions_set_bsg`,
`rt_view_context_scale_storage_from_bsg`,
`rt_view_context_display_manager_set_bsg`,
`rt_view_context_init_bsg`, and `rt_view_set_context_add_bsg`.

The only remaining local `struct bsg_view` use in `dm.c` is the transitional
owned allocation for a freshly created attach view, which remains registered
with GED's existing free-view table.  The broad libged typed-view read scan is
now down to 9 hits: six in the intentional `bsg_ged_draw_view.c` bridge, plus
the larger `draw.c`, `bigE.c`, and `view.c` command/shared-struct cleanups.

The pivot guard now requires the context display-manager, fallback dimension,
scale-storage, lifecycle-init, and view-set-add helper tokens in `dm.c`; it
rejects typed active-view casts, typed display-manager calls, typed
width/height/dimension/scale-storage calls, and typed view init/set-add calls
from returning to this display-manager command batch.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`,
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|erase|refresh|draw_e_erase_d_who|loadview|autoview))$" --timeout 180 --output-on-failure`.
The focused CTest run completed 18/18 passing in 173.54 seconds, with
`ged_test_state_services` longest at 93.17 seconds, `libbsg_api_hygiene` at
41.02 seconds, `brlobol_pivot_guard` at 13.55 seconds,
`ged_test_drawing_quad` at 12.58 seconds, and no individual test above the
180-second cap.

A twenty-fifth GED draw/display selection batch now keeps draw/redraw view
selection and screengrab display-manager lookup on opaque context helpers.
`src/libged/draw/draw2.cpp` carries active, named, and shared fallback draw
views as `void *` contexts, checks independent-view state through
`rt_view_context_is_independent_bsg`, and passes the opaque context into draw
transactions and path checks.  `src/libged/dm/screengrab.c` resolves active
and named display-manager lookups through
`rt_view_context_display_manager_from_bsg` instead of local typed
`struct bsg_view` casts or typed display-manager reads.

The pivot guard now rejects typed draw/redraw view selection casts, typed
independent-view calls, typed screengrab display-manager reads, and local
`struct bsg_view` casts from returning to this draw/display selection batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.45 seconds.  Focused regression
verification then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|mged_shaded_mode_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|erase|refresh|draw_e_erase_d_who|loadview|autoview))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 18/18 passing in 171.61 seconds, with
`ged_test_state_services` longest at 91.68 seconds, `libbsg_api_hygiene` at
40.80 seconds, `brlobol_pivot_guard` at 13.34 seconds,
`ged_test_drawing_quad` at 12.55 seconds, `ged_test_drawing_basic` at
7.49 seconds, `ged_test_ged_draw_scene` at 1.42 seconds, and the remaining
tests below 0.54 seconds.

A twenty-fourth GED diagnostic/LoD/erase view-context batch now keeps BREP
plot fallback publishing, standalone LoD policy command access, and erase
view scoping on opaque context helpers after view lookup.
`src/libged/brep/plot.cpp` routes diagnostic line-layer fallback publication
through `ged_draw_view_context_line_layer_builder_replace` and BREP
indexed-face-set publication through
`ged_draw_view_context_indexed_face_set_replace`.  `src/libged/lod/lod.c`
routes LoD policy reads and writes through
`ged_draw_view_context_lod_policy_from_bsg` and
`ged_draw_view_context_lod_policy_apply_bsg`.  `src/libged/erase/erase2.cpp`
keeps active and named erase view contexts opaque, checks independent-view
scope through `rt_view_context_is_independent_bsg`, and passes the opaque
view context into draw transactions.

`src/libged/bsg_ged_draw_view.c` owns the retained BSG casts for the
context-form LoD policy and indexed-face-set wrappers; the indexed-face-set
context declaration is now part of the public GED draw facade.

The pivot guard now rejects the old typed private GED draw-view calls, typed
LoD policy calls, typed independent-view call, and local `struct bsg_view`
casts from returning to this diagnostic/LoD/erase batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.43 seconds.  Focused regression
verification then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|ged_test_(view_command|state_services|drawing_basic|drawing_quad|drawing_lod|lod_crossrun|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(bots|solids|mged_(view|draw|erase|refresh|draw_e_erase_d_who|loadview|autoview)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 22/22 passing in 190.62 seconds, with
`ged_test_state_services` longest at 92.07 seconds, `libbsg_api_hygiene` at
40.91 seconds, `brlobol_pivot_guard` at 13.55 seconds,
`ged_test_drawing_quad` at 12.73 seconds, `ged_test_drawing_lod` at
10.90 seconds, `ged_test_drawing_basic` at 7.50 seconds, `regress-bots` at
3.15 seconds, `regress-solids` at 2.74 seconds,
`ged_test_lod_crossrun` at 2.38 seconds, and the remaining tests below
1.42 seconds.

A twenty-third GED utility view-snapshot batch now keeps shared libged helper
paths on opaque view contexts after active-view lookup.  `src/libged/ged_util.cpp`
routes default rotation/translation coordinate reads through
`rt_view_context_coord_from_bsg`, uplot line-layer feature publishing through
the public `ged_draw_view_context_line_layers_replace` facade, RT eye-model
snapshots through context zclip, perspective, view-to-model, center, and
rotation helpers, and write-script view-info and orientation snapshots through
context info/quaternion helpers.  `src/libged/bsg_ged_draw_view.c` retains
the BSG line-layer replacement cast inside the draw-view bridge.

The pivot guard now rejects the old typed RT snapshot calls, the typed private
line-layer replacement call, and local `struct bsg_view` casts from returning
to this shared utility helper batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.39 seconds.  Focused regression
verification then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(nirt|gchecker|bots|mged_(autoview|draw_e_erase_d_who|draw|loadview|refresh|view)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 18/18 passing in 184.65 seconds, with
`ged_test_state_services` longest at 92.48 seconds, `libbsg_api_hygiene` at
40.95 seconds, `brlobol_pivot_guard` at 13.44 seconds,
`ged_test_drawing_quad` at 12.63 seconds, `regress-gchecker` at 9.01 seconds,
`ged_test_drawing_basic` at 7.54 seconds, `regress-bots` at 3.17 seconds, and
the remaining tests below 1.43 seconds.

A twenty-second GED view-object snapshot batch now keeps label creation,
polygon creation, vZ depth calculation, and view width/height queries on
opaque RT view-context helpers.  `src/libged/view/labels.c` routes
screen-to-view and view-to-model reads through
`rt_view_context_screen_to_view_from_bsg` and
`rt_view_context_view2model_from_bsg`, `src/libged/view/polygons.c` routes
screen-point conversion through `rt_view_context_screen_point_from_bsg`, and
`src/libged/view/view.c` routes vZ model-to-view plus width/height info reads
through `rt_view_context_model2view_from_bsg` and
`rt_view_context_info_from_bsg`.

The pivot guard now rejects the old typed RT screen-to-view, screen-point,
view-to-model, model-to-view, and view-info calls from returning to this
view-object snapshot batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.38 seconds.  Focused regression
verification then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|ged_draw_scene)|test_qtcad_obol_(draw_sync|snap|selection_sync)|regress-(mged_(view|draw|refresh|draw_e_erase_d_who|snap|rect|data|center|zoom)|solids))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 17/17 passing in 171.89 seconds, with
`ged_test_state_services` longest at 90.28 seconds, `libbsg_api_hygiene` at
40.87 seconds, `brlobol_pivot_guard` at 13.33 seconds,
`ged_test_drawing_quad` at 12.70 seconds, `ged_test_drawing_basic` at
7.50 seconds, `regress-solids` at 2.69 seconds, and the remaining tests below
1.42 seconds.

A twenty-first GED active-view command/helper batch now keeps the libged
`select`/`rselect` command implementation on opaque RT view-context helpers.
`src/libged/select/select.c` keeps active-view access as `void *view_ctx`,
routes BoT point selection plus visible-record and partial-selection
model-to-view snapshots through `rt_view_context_model2view_from_bsg`, routes
rectangular selection through `rt_view_context_interactive_rect_state_from_bsg`,
and passes the opaque context into the local draw-record iteration helpers.

The pivot guard now rejects typed `struct bsg_view` command-side casts and the
old typed RT model-to-view or interactive-rectangle calls from returning to
this selection batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.47 seconds.  Focused verification
then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|regress-(bots|mged_(view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 14/14 passing in 168.88 seconds, with
`ged_test_state_services` longest at 92.45 seconds, `libbsg_api_hygiene` at
40.95 seconds, `ged_test_drawing_select` at 16.36 seconds,
`brlobol_pivot_guard` at 13.54 seconds, and the remaining tests below
3.17 seconds.

A twentieth GED active-view command/helper batch now keeps NIRT/qray and
rtcheck diagnostics on opaque RT and GED draw-view context helpers.
`src/libged/nirt/nirt.cpp` keeps active-view access as `void *view_ctx`,
routes default shot center, shot direction, qray glyph scale, and vnirt
view-to-model reads through `rt_view_context_*_bsg` helpers, and uses
`ged_draw_view_context_feature_remove` for stale qray cleanup.
`src/libged/rtcheck/rtcheck2.cpp` keeps active-view access opaque, routes
overlap glyph scale through `rt_view_context_scale_from_bsg`, and uses the
new `ged_draw_view_context_features_remove_prefix` wrapper for stale rtcheck
feature cleanup.

The pivot guard now rejects typed `struct bsg_view` command-side casts, old
typed RT diagnostics snapshot calls, and old typed GED draw-view cleanup calls
from returning to this diagnostics batch.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 22.08 seconds.  Focused verification
then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(nirt|gchecker|mged_(view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 13/13 passing in 169.54 seconds, with
`ged_test_state_services` longest at 95.37 seconds, `libbsg_api_hygiene` at
40.80 seconds, `brlobol_pivot_guard` at 13.36 seconds, `regress-gchecker` at
8.45 seconds, `ged_test_drawing_basic` at 7.53 seconds, and the remaining
tests below 1.43 seconds.

A nineteenth GED active-view command/helper batch now keeps the remaining
view-math/query primitive edit and subentity selection transforms on opaque RT
view-context helpers.  `src/libged/polyclip.cpp`,
`src/libged/metaball/metaball.c`, `src/libged/pipe/pipe.c`,
`src/libged/move_arb_edge/move_arb_edge.c`, and
`src/libged/bot/edbot.c` keep active-view access as `void *view_ctx`.
Polygon overlap/fill routes plane, scale, model-to-view, and view-to-model
reads through context helpers; metaball and pipe point workflows route
model-to-view and view-to-model reads through context helpers; and ARB/BoT
subentity selection routes model-to-view reads through context helpers.

The pivot guard now rejects typed `struct bsg_view` command-side casts and the
old typed RT transform calls from returning to this remaining
view-math/query subset.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the pivot guard completed 1/1 passing in 13.65 seconds.  Focused verification
then passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|regress-(bots|solids|mged_(3ptarb|arb|draw|refresh|view|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 17/17 passing in 172.95 seconds, with
`ged_test_state_services` longest at 93.06 seconds, `libbsg_api_hygiene` at
40.82 seconds, `ged_test_drawing_select` at 16.41 seconds,
`brlobol_pivot_guard` at 13.53 seconds, and the remaining tests below
3.24 seconds.

An eighteenth GED active-view command/helper batch now keeps view-math/query
ray and check snapshots on opaque RT view-context helpers.
`src/libged/check/check.c`, `src/libged/brep/pick.cpp`,
`src/libged/bot/pick.cpp`, and
`src/libged/solids_on_ray/solids_on_ray.c` keep active-view access as
`void *view_ctx`.  `check` routes view-info and orientation-quaternion reads
through context helpers, BREP/BoT pick route center/rotation reads through
context helpers, and `solids_on_ray` routes center, rotation, model-to-view,
and scale reads through context helpers.

The pivot guard now rejects typed `struct bsg_view` command-side casts and the
old typed RT view snapshot calls from returning to this view-math/query subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|regress-(bots|solids|gchecker|mged_(view|draw|refresh|draw_e_erase_d_who)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 19/19 passing in 179.84 seconds, with
`ged_test_state_services` longest at 91.95 seconds, `libbsg_api_hygiene` at
40.96 seconds, `ged_test_drawing_select` at 16.35 seconds,
`brlobol_pivot_guard` at 13.30 seconds, and the remaining tests below
8.97 seconds.

A seventeenth GED active-view command/helper batch now keeps `adc` on opaque
RT view-context helpers.  `src/libged/adc/adc.c` keeps the active view as
`void *view_ctx`, routes ADC state get/set, scale, model-to-view, and
view-to-model reads through `rt_view_context_*_bsg` helpers, and passes the
opaque context into its local ADC anchor/print helpers.  The pivot guard now
rejects typed `struct bsg_view` command-side casts and the old typed RT
ADC/matrix/scale adapter calls from returning to this command.

Initial verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services)|regress-mged_(adc|view|draw|refresh))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 9/9 passing in 147.62 seconds, with
`ged_test_state_services` longest at 91.50 seconds, `libbsg_api_hygiene` at
40.81 seconds, `brlobol_pivot_guard` at 13.34 seconds, and the remaining
tests below 0.43 seconds.

A sixteenth GED active-view command/helper batch now keeps output/export
camera snapshots on opaque RT view-context helpers.
`src/libged/png/png.c`, `src/libged/ps/ps.c`, `src/libged/plot/plot.c`, and
`src/libged/rtwizard/rtwizard.c` keep active-view access as `void *view_ctx`.
`png` and `ps` route model-to-view, perspective, and eye-position reads
through context helpers, `plot` routes model-to-view, center, and scale reads
through context helpers, and `rtwizard` routes perspective, view-info, and
orientation-quaternion reads through context helpers.  The local
visible-record iteration helpers in the export commands now receive the
opaque view context directly.  The pivot guard now rejects typed
`struct bsg_view` command-side casts and the old typed RT export/snapshot
adapter calls from returning to this output/export batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|rtwizard_bsg)|regress-mged_(plot|ps|saveview|draw|refresh|view))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 12/12 passing in 148.10 seconds, with
`ged_test_state_services` longest at 90.94 seconds, `libbsg_api_hygiene` at
40.85 seconds, `brlobol_pivot_guard` at 13.25 seconds, and the remaining
tests below 0.42 seconds.

A fifteenth GED active-view command/helper batch now keeps
framebuffer/display-facing command reads on opaque RT view-context helpers.
`src/libged/fbclear/fbclear.c`, `src/libged/fb2pix/fb2pix.c`,
`src/libged/pix2fb/pix2fb.c`, `src/libged/png2fb/png2fb.c`,
`src/libged/overlay/overlay.c`, `src/libged/dm/ert.cpp`, and
`src/libged/rt/rt.c` keep active-view access as `void *view_ctx` and route
display-manager lookup, framebuffer-mode get/set, perspective, and
plot-overlay scale through `rt_view_context_*_bsg` helpers.  The pivot guard
now rejects typed `struct bsg_view` command-side casts plus the old typed RT
display-manager, framebuffer-mode, perspective, and scale adapter calls from
returning to this migrated display-facing batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|tcl_overlay_bsg)|test_qtcad_obol_overlay_sync|regress-mged_(overlay|draw|draw_e_erase_d_who|refresh|view))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 11/11 passing in 56.48 seconds, with
`libbsg_api_hygiene` longest at 40.79 seconds, `brlobol_pivot_guard` at
13.21 seconds, and the remaining tests below 0.42 seconds.

A fourteenth GED active-view command/helper batch now keeps residual path/query
and visible-record export lookups opaque.  `src/libged/who/who.c` passes the
active `void *view_ctx` directly to `ged_draw_list_paths`.
`src/libged/how/how.c` stores the borrowed active view as an opaque context for
`ged_draw_group_record_in_view`, and
`src/libged/bot/dump/bot_dump.cpp` passes the active context directly into
`ged_draw_foreach_visible_view_db_object_record`.  The pivot guard now rejects
typed `struct bsg_view` command-side casts and direct retained view-set
reach-through from returning to this migrated path/query/export batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|bot_dumping|ged_draw_scene)|regress-mged_(who|draw|draw_e_erase_d_who|refresh|view))$" -E "^slow-" --timeout 180 --output-on-failure`;
the focused CTest run completed 11/11 passing in 148.55 seconds, with
`ged_test_state_services` longest at 90.63 seconds,
`libbsg_api_hygiene` at 40.83 seconds, `brlobol_pivot_guard` at
13.26 seconds, `ged_test_ged_draw_scene` at 1.42 seconds,
`ged_test_bot_dumping` and `ged_test_view_command` at 0.15 seconds, and the
MGED regressions at about 0.41 seconds.

A thirteenth GED active-view command/helper batch now keeps `labelvert` and
`labelface` on opaque context helpers.  `src/libged/illum/illum.c` and
`src/libged/nmg/nmg.c` keep active-view access as `void *view_ctx`, pass that
context into visible DB-object iteration, publish named retained label
features through the public `ged_draw_view_context_labels_replace` facade,
and use `rt_view_context_display_manager_from_bsg` for repaint
notification.  The pivot guard now rejects typed `struct bsg_view`
command-side casts, old typed GED draw-view label replacement calls, and old
typed RT display-manager reads from returning to this migrated pair.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|nmg_(mk|copy)|ged_test_(view_command|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|regress-mged_(view|draw|refresh|draw_e_erase_d_who))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 13/13 passing in 78.20 seconds, with
`libbsg_api_hygiene` longest at 41.01 seconds, `brlobol_pivot_guard` at
13.20 seconds, `ged_test_drawing_quad` at 12.69 seconds,
`ged_test_drawing_basic` at 7.51 seconds, `ged_test_ged_draw_scene` at
1.40 seconds, and the remaining tests below 0.55 seconds.

A twelfth GED active-view command/helper batch now keeps mirror refresh and
named diagnostic line-overlay publication on opaque context helpers.
`src/libged/mirror/mirror.c` keeps the active view as `void *view_ctx` and
routes post-draw refresh through `rt_view_context_update_bsg`.
`src/libged/joint/joint.c`, `src/libged/bot/bot_fuse.c`,
`src/libged/bot/check.cpp`, `src/libged/bot/bot.cpp`,
`src/libged/lint/lint.cpp`, `src/libged/gdiff/gdiff.c`, and the already
migrated `src/libged/draw/preview.cpp` now publish/remove named retained line
features through `ged_draw_view_context_lines_replace` and
`ged_draw_view_context_feature_remove`.  The pivot guard now rejects typed
`struct bsg_view` command-side casts, old typed RT view-refresh calls, and old
typed GED draw-view line-feature calls from returning to this migrated batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|regress-(gdiff|ged_lint_raytrace|bots|mged_(mirror|draw|draw_e_erase_d_who|refresh|view)))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 11/11 passing in 62.79 seconds, with
`libbsg_api_hygiene` longest at 40.88 seconds,
`brlobol_pivot_guard` at 13.23 seconds, `regress-bots` at 3.16 seconds,
`regress-ged_lint_raytrace` at 2.98 seconds, `ged_test_view_command` at
0.15 seconds, and the remaining overlay/MGED regressions below 0.42 seconds.

An eleventh GED active-view command/helper batch now keeps database-open unit
setup and draw/view restore preview paths on opaque context helpers.
`src/libged/open/open.cpp` keeps the active view as `void *view_ctx` for
database unit-conversion sync.  `src/libged/draw/loadview.cpp` keeps view
restore operations opaque for view-size, rotation, center, update, and
perspective writes.  `src/libged/draw/preview.cpp` keeps preview camera
initialization, preview frame camera restoration, view-to-model reads, center
updates, and eye-path retained feature cleanup/publication on opaque RT and
GED draw-view context helpers.  The public
`ged_draw_view_context_lines_replace` facade covers preview eye-path
publication while the retained BSG cast stays in `bsg_ged_draw_view.c`.  The
pivot guard now rejects typed `struct bsg_view`
command-side casts, old typed RT view restore calls, and old typed GED
draw-view feature calls from returning to this migrated batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|regress-mged_(loadview|view|refresh))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 6/6 passing in 55.56 seconds, with
`libbsg_api_hygiene` longest at 40.86 seconds,
`brlobol_pivot_guard` at 13.27 seconds, `ged_test_view_command` at
0.15 seconds, and the MGED regressions at about 0.41 seconds.

A tenth GED active-view command batch now keeps `view knob` on opaque context
helpers.  `src/libged/view/knob.c` keeps active view access as
`void *view_ctx`, reads printed knob values through the new neutral
`struct rt_view_knob_values` snapshot, and routes unit-conversion sync,
coordinate default reads, rate reset, calibrate, command processing,
translation, keypoint-origin lookup, rotation, and rate-flag updates through
RT view-context helpers.  `include/rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` now expose the narrow context knob adapter
surface, and `src/librt/tests/view_legacy_bsg.c` compares the new context
wrappers against the retained direct BSG behavior.  The pivot guard now
rejects typed `struct bsg_view` command-side casts, direct knob field access,
old typed RT knob/unit/coord/keypoint/absolute-scale calls, and direct BSG
knob utility calls from returning to `view knob`.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_knob)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 5/5 passing in 54.63 seconds, with
`libbsg_api_hygiene` longest at 40.77 seconds,
`brlobol_pivot_guard` at 13.24 seconds, `rt_view_legacy_bsg` at
0.02 seconds, `ged_test_view_command` at 0.15 seconds, and
`regress-mged_knob` at 0.41 seconds.

A ninth GED active-view command batch now keeps grid and retained data-line
paths on opaque context helpers.  `src/libged/grid/grid.c` keeps active view
access as `void *view_ctx`, routes grid state, center updates, model/view
matrix reads, and 2D grid snapping through RT context helpers, and uses the
new `rt_view_context_snap_grid_2d_bsg` wrapper.  `src/libged/view/data_lines.c`
keeps its command state on an opaque view context, routes snap-line state
through RT context helpers, and routes retained data-line feature visibility,
removal, style, color, width, point-copy, and Tcl replacement through private
GED draw-view context helpers.  `include/rt/view_legacy_bsg.h`,
`src/librt/view_legacy_bsg.c`, and `src/librt/tests/view_legacy_bsg.c` cover
the new grid-snap context adapter, while
`src/libged/bsg_ged_draw_view_private.h` and
`src/libged/bsg_ged_draw_view.c` expose the private line-style and point-copy
context wrappers needed by `view data_lines`.  The pivot guard now rejects
typed `struct bsg_view` command-side casts, old typed RT grid/snap-line calls,
and old typed GED draw-view calls from returning to this migrated batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|snap_semantics)|test_qtcad_obol_snap|regress-mged_(view|refresh|knob))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 9/9 passing in 55.61 seconds, with
`libbsg_api_hygiene` longest at 40.81 seconds,
`brlobol_pivot_guard` at 13.23 seconds, `rt_view_legacy_bsg` at
0.02 seconds, `ged_test_snap_semantics` at 0.01 seconds,
`ged_test_view_command` at 0.15 seconds, `test_qtcad_obol_snap` at
0.11 seconds, and the MGED regressions at about 0.41 seconds.

An eighth GED active-view command batch now keeps autoview and output snapshot
paths on opaque context helpers.  `src/libged/view/autoview2.cpp` keeps both
the active view and `-V` named-view lookup as `void *view_ctx` and routes
full-scene and explicit-bounds framing through RT context autoview helpers.
`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
`rt_view_context_autoview_bounds_bsg`, and
`src/librt/tests/view_legacy_bsg.c` checks the context bounds adapter against
the direct retained BSG result.  `src/libged/savekey/savekey.c`,
`src/libged/view/saveview.c`, and `src/libged/brep/tikz.cpp` now keep active
view access opaque for RT -M view snapshots, render-script perspective/path
output, and TikZ AET output.  The pivot guard now rejects typed
`struct bsg_view` command-side casts and old typed RT calls from returning to
this migrated batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_(autoview|saveview|view|refresh)|brep_ppx)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 9/9 passing in 55.82 seconds, with
`libbsg_api_hygiene` longest at 40.79 seconds,
`brlobol_pivot_guard` at 13.17 seconds, `rt_view_legacy_bsg` and
`brep_ppx` at 0.01 seconds, `ged_test_view_command` at 0.15 seconds, and the
MGED regressions at about 0.40-0.41 seconds.

A seventh GED active-view command batch now keeps retained snap and
interactive-rectangle view access opaque.  `src/libged/view/snap.c` keeps
`ged_view_active_ctx` results as `void *view_ctx`, uses RT context helpers
for snap tolerance plus model/view matrix reads, and uses the new private
`ged_draw_view_context_snap_first_candidate` wrapper for retained snap
candidate lookup.  `src/libged/rect/rect.c` keeps active-view access and
rectangle zoom/state reads and writes on `void *view_ctx` plus RT context
helpers for interactive-rectangle state, center, model/view matrices, scale,
and update access.  The pivot guard rejects typed `struct bsg_view`
active-view casts, old typed RT calls, and the old typed GED snap helper from
returning to this migrated two-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|snap_semantics)|test_qtcad_obol_snap|regress-mged_(view|zoom|refresh))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 9/9 passing in 55.50 seconds, with
`libbsg_api_hygiene` longest at 40.79 seconds,
`brlobol_pivot_guard` at 13.14 seconds, `ged_test_snap_semantics` at
0.01 seconds, `test_qtcad_obol_snap` at 0.11 seconds,
`ged_test_view_command` at 0.15 seconds, `rt_view_legacy_bsg` at
0.02 seconds, and the MGED regressions at about 0.40-0.42 seconds.

A sixth GED active-view command/helper batch now keeps retained view-state
access on opaque RT context helpers.
`src/libged/view/center.cpp`, `src/libged/view/lookat.c`,
`src/libged/view/align.c`, and `src/libged/vutil.c` keep
`ged_view_active_ctx` results as `void *view_ctx` and call
`rt_view_context_*_bsg` helpers for center, view-to-model, model-to-view,
AET, scale, keypoint, rotate-about, inverse-size, rotation, and update
access.  No new RT adapter functions were needed; the existing context helper
coverage in `src/librt/tests/view_legacy_bsg.c` covers these helper forms.
The pivot guard rejects typed `struct bsg_view` active-view casts and old
typed RT calls from returning to this migrated four-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|regress-mged_(center|lookat|mrot|orot|qorot|rot_view|tra_view))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 10/10 passing in 57.04 seconds, with
`libbsg_api_hygiene` longest at 40.80 seconds,
`brlobol_pivot_guard` at 13.16 seconds, each MGED regression at about
0.40-0.41 seconds, and `ged_test_view_command` at 0.15 seconds.

A fifth GED active-view scalar/orientation command batch now keeps retained
view-state access on opaque RT context helpers.
`src/libged/view/aet.c`, `src/libged/view/viewdir.c`,
`src/libged/view/size.c`, `src/libged/view/eye.c`, and
`src/libged/eye_pos/eye_pos.c` keep `ged_view_active_ctx` results as
`void *view_ctx` and call `rt_view_context_*_bsg` helpers for AET,
rotation, view-info, size, view-to-model, center, eye-position,
projection-matrix, and update access.  `include/rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` now expose context forms for eye-position reads
and writes, with coverage in `src/librt/tests/view_legacy_bsg.c`.  The pivot
guard rejects typed `struct bsg_view` active-view casts and the old typed RT
calls from returning to this migrated five-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_(eye_pt|size|view))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 7/7 passing in 55.47 seconds, with
`libbsg_api_hygiene` longest at 40.97 seconds,
`brlobol_pivot_guard` at 13.07 seconds, `regress-mged_eye_pt` at
0.41 seconds, `regress-mged_size` at 0.40 seconds,
`regress-mged_view` at 0.41 seconds, `ged_test_view_command` at
0.15 seconds, and `rt_view_legacy_bsg` at 0.02 seconds.

A fourth GED active-view conversion/query command batch now keeps retained
view-state access on opaque RT context helpers.
`src/libged/view2model/view2model.c`,
`src/libged/view2grid_lu/view2grid_lu.c`,
`src/libged/grid2view_lu/grid2view_lu.c`,
`src/libged/grid2model_lu/grid2model_lu.c`,
`src/libged/model2grid_lu/model2grid_lu.c`, and
`src/libged/get_eyemodel/get_eyemodel.c` keep `ged_view_active_ctx` results
as `void *view_ctx` and call `rt_view_context_*_bsg` helpers for
model-to-view, view-to-model, scale, view-info, and orientation-quat reads.
No new RT adapter functions were needed; the existing context helper coverage
in `src/librt/tests/view_legacy_bsg.c` covers these helper forms.  The pivot
guard rejects typed `struct bsg_view` active-view casts and old typed RT calls
from returning to this migrated six-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_view)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 5/5 passing in 54.59 seconds, with
`libbsg_api_hygiene` longest at 40.99 seconds,
`brlobol_pivot_guard` at 12.99 seconds, `regress-mged_view` at
0.41 seconds, `ged_test_view_command` at 0.15 seconds, and
`rt_view_legacy_bsg` at 0.02 seconds.

A third GED active-view scalar/projection/orientation command batch now keeps
retained view-state access on opaque RT context helpers.
`src/libged/scale/scale.c`, `src/libged/pmodel2view/pmodel2view.c`,
`src/libged/pmat/pmat.c`, `src/libged/perspective/perspective.c`,
`src/libged/setview/setview.c`, `src/libged/view/ypr.c`,
`src/libged/view/qvrot.c`, and `src/libged/view/quat.c` keep
`ged_view_active_ctx` results as `void *view_ctx` and call
`rt_view_context_*_bsg` helpers for scale, projected model-to-view,
projection-matrix, perspective, rotation, quaternion, and update access.
`include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now expose
context forms for projected model-to-view reads, projection-matrix writes,
perspective writes, and orientation-quat reads, with coverage in
`src/librt/tests/view_legacy_bsg.c`.  The pivot guard rejects typed
`struct bsg_view` active-view casts and the old typed RT calls from returning
to this migrated eight-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_(qvrot|setview|view))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 7/7 passing in 55.24 seconds, with
`libbsg_api_hygiene` longest at 40.83 seconds,
`brlobol_pivot_guard` at 12.98 seconds, `regress-mged_qvrot` at
0.41 seconds, `regress-mged_setview` at 0.40 seconds,
`regress-mged_view` at 0.41 seconds, `ged_test_view_command` at
0.15 seconds, and
`rt_view_legacy_bsg` at 0.02 seconds.

A second GED active-view scalar command batch now keeps retained view-state
access on opaque RT context helpers.  `src/libged/arot/arot.c`,
`src/libged/orient/orient.c`, `src/libged/keypoint/keypoint.c`,
`src/libged/isize/isize.c`, `src/libged/model2view/model2view.c`,
`src/libged/model2view_lu/model2view_lu.c`,
`src/libged/m2v_point/m2v_point.c`, and
`src/libged/v2m_point/v2m_point.c` keep `ged_view_active_ctx` results as
`void *view_ctx` and call `rt_view_context_*_bsg` helpers for coord, rotation
set/update, keypoint, inverse-size, model-to-view, view-to-model, and scale
access.  `include/rt/view_legacy_bsg.h` and `src/librt/view_legacy_bsg.c` now
expose context forms for keypoint get/set and inverse-size reads, with
coverage in `src/librt/tests/view_legacy_bsg.c`.  The pivot guard rejects
typed `struct bsg_view` active-view casts and the old typed RT calls from
returning to this migrated eight-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_(arot|keypoint|orientation|view|zoom))$" --timeout 180 --output-on-failure`;
the focused CTest run completed 9/9 passing in 56.14 seconds, with
`libbsg_api_hygiene` longest at 40.88 seconds,
`brlobol_pivot_guard` at 12.99 seconds, each MGED regression at
0.41 seconds, `ged_test_view_command` at 0.15 seconds, and
`rt_view_legacy_bsg` at 0.02 seconds.

A small GED active-view command batch now keeps retained view-state access on
opaque RT context helpers instead of typed BSG view pointers.
`src/libged/zoom/zoom.c`, `src/libged/rmat/rmat.c`,
`src/libged/rot_point/rot_point.c`,
`src/libged/view2model_lu/view2model_lu.c`,
`src/libged/view2model_vec/view2model_vec.c`, and
`src/libged/rot/rotate_about.c` keep `ged_view_active_ctx` results as
`void *view_ctx` and call `rt_view_context_*_bsg` helpers for scale, update,
rotation, view-to-model, and rotate-about state.  `include/rt/view_legacy_bsg.h`
and `src/librt/view_legacy_bsg.c` now expose context forms for rotation set
and rotate-about get/set, with coverage in
`src/librt/tests/view_legacy_bsg.c`.  The pivot guard rejects typed
`struct bsg_view` active-view casts and the old typed RT calls from returning
to this migrated six-file batch.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command|regress-mged_zoom)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 5/5 passing in 54.32 seconds, with
`libbsg_api_hygiene` longest at 40.78 seconds,
`brlobol_pivot_guard` at 12.92 seconds, `regress-mged_zoom` at 0.41 seconds,
`ged_test_view_command` at 0.15 seconds, and `rt_view_legacy_bsg` at
0.02 seconds.

The TclCAD data-arrow, data-axes, data-label, and data-line command callback
paths now keep their view handoffs opaque.  `src/libtclcad/view/view.h`
declares `to_data_arrows_func`, `to_data_axes_func`, and
`to_data_labels_func` with `void *view_ctx` parameters.
`src/libtclcad/view/arrows.c` passes both the generic draw-view context and
the named `view data_arrows` lookup result through that opaque signature
before using the retained `ged_draw_view_context_*` helpers for feature
existence, visibility, style, color, line width, point copy, feature removal,
arrow replacement, and arrow-tip access.  `src/libtclcad/view/axes.c` now
does the same for generic and named `view data_axes` calls, the data-axes
display-scale helper reads display manager and view info through opaque RT
view-context helpers, and its model/view axes commands read, write, and
refresh axes state through opaque `view_ctx` handoffs instead of local
`struct bsg_view` pointers.
`src/libtclcad/view/labels.c` now does the same for generic and named
`view data_labels` calls, uses `tclcad_view_data_from_view_ctx` to reach the
retained Tcl label state, and no longer includes `rt/view_legacy_bsg.h`,
names `struct bsg_view`, or reaches through `gv_tcl` directly.
`src/libtclcad/view/lines.c` saves, installs, restores, and refreshes active
views through `void *` contexts for both the generic draw-view entry point and
the named `view data_lines` path, and no longer includes
`rt/view_legacy_bsg.h`.  The pivot guard requires those opaque arrow, axes,
label, and line handoffs and rejects typed BSG data callback signatures, typed
axes and active-view casts, direct `struct bsg_view` use in the migrated
axes/label/line files, direct label `gv_tcl` access, and stale legacy RT
includes in the migrated label/line files.

The TclCAD faceplate command now keeps named-view access opaque too.
`src/libtclcad/view/faceplate.c` stores the `ged_view_find_ctx` result as
`void *view_ctx`, passes it through the retained RT context-form center-dot,
params, and scale-overlay state helpers, refreshes by opaque context, and
reaches primitive-label state through `tclcad_view_data_from_view_ctx`
instead of direct `gv_tcl` access.  The pivot guard requires the opaque
faceplate view handoff and rejects `struct bsg_view`, typed
`ged_view_find_ctx` casts, local `gdvp` use, and direct `gv_tcl` access from
returning to that file.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 53.90 seconds, with
`libbsg_api_hygiene` longest at 40.86 seconds,
`brlobol_pivot_guard` at 12.86 seconds, and
`tclcad_test_ged_cmds_exist` at 0.14 seconds.

The private TclCAD wrapper/autoview/draw/refresh/viewable helper surface now
keeps view-context-only operations opaque.  `src/libtclcad/view/view.h`
declares `to_autoview_view`, `go_draw`, `to_refresh_view`, and
`to_is_viewable` with `void *view_ctx` parameters instead of
`struct bsg_view *`.  `src/libtclcad/wrapper.c` keeps `ged_view_find_ctx`
results, active-view setup, refresh clientdata, display-manager perspective
sync, LoD policy reads, zoom-refresh dimension sync, TclCAD per-view data
lookup, and final refresh calls on opaque contexts.  `src/libtclcad/view/autoview.c`
keeps its helper signature, active-view setup, view lookup, refresh call,
TclCAD per-view data lookup, and autoview-all view-set traversal opaque.
`src/libtclcad/view/refresh.c` keeps refresh drawing, framebuffer overlay/
interlay/underlay handling, unit-conversion stash/restore, and `dm_draw_objs`
calls on opaque contexts while using `tclcad_view_data_from_view_ctx` for
TclCAD per-view storage.  `src/libtclcad/view/draw.c` keeps edit-redraw
view-set traversal opaque before calling
`ged_draw_foreach_view_db_object_record`, and `src/libtclcad/fb.c` passes
framebuffer callback data directly to `to_refresh_view` while
`to_open_fbs`, `to_close_fbs`, `to_set_fb_mode`, and `to_listen` keep view
lookups and refreshes opaque.  `src/libtclcad/view_data.c` contains the single
direct retained BSG `u_data` reach-through for this migrated cluster in
`tclcad_view_data_from_view_ctx`.  The pivot guard requires the opaque
signatures and call sites and rejects the older typed helper signatures, typed
`ged_view_find_ctx`, typed active-view sync, framebuffer callback/helper
signatures, local `gdvp->u_data` access, and view-table casts from returning.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 54.05 seconds, with
`libbsg_api_hygiene` longest at 40.81 seconds,
`brlobol_pivot_guard` at 13.04 seconds, and
`tclcad_test_ged_cmds_exist` at 0.14 seconds.

The private qtcad legacy-view bridge now distinguishes retained compatibility
context traffic from direct BSG pointer access.
`src/libqtcad/QgLegacyViewContext.h` owns private
`qg_legacy_view_from_context` and
`qg_legacy_view_to_context` helpers.  `src/libqtcad/QgLegacyView.cpp` uses the
context helpers for
migrated RT/GED context-wrapper calls, GED draw transaction view storage,
GED active-view lookup/set, sketch edit creation, sketch line-set contexts,
draw path state/list/existence queries, bounds callback suspension/restoration,
DM open context handoff, DM draw handoff, and ray/view-state helper reads.  The
historical shallow local-view free path now uses
`rt_view_context_release_storage_bsg`; local retained-view create/destroy use
`rt_view_context_create_bsg` and `rt_view_context_free_bsg` rather than
caller-side BSG allocation/free-contents spelling.  The retired
`QgLegacyViewBsg.h` adapter is no longer present, and `QgLegacyView.cpp` no
longer directly includes `bsg/view_state.h`.  The pivot guard requires the
private context helpers and rejects direct BSG view-state includes, typed BSG
casts, `struct bsg_view`, and `qg_legacy_view_to_bsg` calls from returning to
the qtcad legacy-view implementation.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad qsketch test_qtcad_obol_pick test_qtcad_obol_draw_sync test_qtcad_obol_selection_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|test_qtcad_obol_pick|test_qtcad_obol_selection_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 6/6 passing in 54.15 seconds, with
`libbsg_api_hygiene` longest at 40.88 seconds,
`brlobol_pivot_guard` at 12.87 seconds,
`rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds,
`test_qtcad_obol_selection_sync` at 0.10 seconds, and
`test_qtcad_obol_pick` at 0.12 seconds.

The private qtcad sketch retained-line bridge now routes sketch preview
line-set creation, null checks, point updates, and destruction through opaque
RT line-set context wrappers.  `src/librt/view_legacy_bsg.c` owns the retained
BSG line-set/node storage, color assignment, scene-root attachment, point
update, and node destruction through the new
`rt_view_context_line_set_create_bsg`,
`rt_view_line_set_context_is_null_bsg`,
`rt_view_line_set_context_set_points_bsg`, and
`rt_view_line_set_context_destroy_bsg` adapters.  `src/libqtcad/QgLegacyView.cpp`
keeps the qtcad sketch API unchanged but now stores only an opaque line-set
context for qsketch preview lines and no longer includes BSG geometry/node/
separator headers or calls direct BSG line-set, node, or separator helpers for
that retained sketch surface.  The pivot guard requires the context forms and
rejects those direct BSG sketch line-set calls from returning to that migrated
qtcad bridge subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad qsketch`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 54.19 seconds, with
`libbsg_api_hygiene` longest at 41.29 seconds,
`brlobol_pivot_guard` at 12.84 seconds, and `rt_view_legacy_bsg` at
0.02 seconds.  `qsketch` is built as an executable/demo target rather than
registered as a CTest test in the current build.

The private qtcad legacy-view bridge now routes the view-taking polygon subset
through opaque RT view-context wrappers.  New context adapters and
`rt_view_legacy_bsg` coverage were added for polygon create, select, find,
duplicate, record visitation, snap-count, point-selection clear, update,
screen-point update, view rebinding, sketch import, and snap-exclude set.
`src/libqtcad/QgLegacyView.cpp` uses those context forms for polygon
operations that need a retained view.  Polygon-ref-only object helpers, such
as record access, move, rename, visual/open state, removal, user data, CSG,
and sketch export, remain on retained RT polygon-ref wrappers until the
polygon representation itself migrates.  The pivot guard requires the context
forms and rejects the older typed view-taking polygon wrappers from returning
to that migrated qtcad bridge subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_pick`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_pick)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 53.87 seconds, with
`libbsg_api_hygiene` longest at 40.85 seconds,
`brlobol_pivot_guard` at 12.83 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and `test_qtcad_obol_pick` at 0.12 seconds.  No standalone qtcad polygon
CTest target surfaced in the current build; `qsketch` is built as an
executable/demo target rather than registered as a CTest test.

The private qtcad legacy-view bridge now routes pick-result object lifecycle,
accessors, first-result filtering, copy append, and selection application's
pick-result argument through opaque RT pick-result context wrappers.  New RT
context adapters and `rt_view_legacy_bsg` coverage were added for the retained
pick-result object surface.  `src/libqtcad/QgLegacyView.cpp` now stores the
qtcad pick-result bridge as an opaque result context, no longer spells
`struct rt_view_pick_result_bsg` or calls typed pick-result helpers, and uses
`rt_view_context_selection_set_pick_result_context_bsg` so both the retained
view and retained pick result cross the RT boundary opaquely.  The pivot guard
requires the context forms and rejects typed pick-result object calls from
returning to that migrated qtcad bridge subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_pick test_qtcad_obol_selection_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_pick|test_qtcad_obol_selection_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 5/5 passing in 54.02 seconds, with
`libbsg_api_hygiene` longest at 40.86 seconds,
`brlobol_pivot_guard` at 12.86 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_selection_sync` at 0.10 seconds, and
`test_qtcad_obol_pick` at 0.12 seconds.

The private qtcad legacy-view bridge now routes the view-taking
feature/edit-preview subset through opaque RT view-context wrappers.  New
context adapters and `rt_view_legacy_bsg` coverage were added for edit-preview
event publication, overlay/label ensure, feature removal, feature summaries,
and feature view rebinding.  `src/libqtcad/QgLegacyView.cpp` uses those
context forms for feature operations that need a retained view, while
feature-ref-only geometry/style helpers remain on retained RT feature-ref
wrappers until the feature representation itself migrates.  The pivot guard
requires the context forms and rejects the older typed view-taking
feature/edit-preview wrappers from returning to that migrated qtcad bridge
subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_edit_preview`,
then `ninja -C build rt_view_legacy_bsg` after restoring explicit typed
publish-adapter test coverage, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_edit_preview)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 53.81 seconds, with
`libbsg_api_hygiene` longest at 40.81 seconds,
`brlobol_pivot_guard` at 12.87 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and `test_qtcad_obol_edit_preview` at 0.07 seconds.

The private qtcad legacy-view bridge now routes local retained-view lifecycle
and attachment operations through opaque RT view-context wrappers.  New context
adapters and `rt_view_legacy_bsg` coverage were added for caller-owned view
init/free-contents and retained view-set attach/detach.
`src/libqtcad/QgLegacyView.cpp` uses those context forms for local view
creation, destruction, GED view-set attachment, and scene-attached checks, and
the pivot guard requires them while rejecting typed `rt_view_init_bsg`, typed
`rt_view_free_bsg`, direct `vset` assignment, and direct
`bsg_view_scene_attached` calls from returning to that migrated qtcad bridge
subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_draw_sync test_qtcad_obol_pick test_qtcad_obol_selection_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|test_qtcad_obol_pick|test_qtcad_obol_selection_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 6/6 passing in 54.16 seconds, with
`libbsg_api_hygiene` longest at 40.82 seconds,
`brlobol_pivot_guard` at 12.94 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and the qtcad Obol workflow tests at or below 0.13 seconds.

The private qtcad legacy-view bridge now routes another retained-view
operations batch through opaque RT view-context wrappers.  New context
adapters and `rt_view_legacy_bsg` coverage were added for hash reads,
unique-object-name generation, bounds-update callback get/set/call, adjust,
LoD policy copy, LoD bounds update, autoview, and snap-exclude clear.
`src/libqtcad/QgLegacyView.cpp` uses those context forms, and the pivot guard
requires them while rejecting the older typed RT calls from returning for that
migrated qtcad bridge subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_draw_sync test_qtcad_obol_snap test_qtcad_obol_pick test_qtcad_obol_selection_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|test_qtcad_obol_snap|test_qtcad_obol_pick|test_qtcad_obol_selection_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 7/7 passing in 54.18 seconds, with
`libbsg_api_hygiene` longest at 40.80 seconds,
`brlobol_pivot_guard` at 12.87 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and the qtcad Obol workflow tests at or below 0.12 seconds.

The private qtcad legacy-view bridge now routes retained GED view-set
add/remove plus qtcad pick and selection operations through existing opaque RT
view-context wrappers.  `src/libqtcad/QgLegacyView.cpp` uses
`rt_view_set_context_*_bsg`, `rt_view_context_pick_*_bsg`, and
`rt_view_context_selection_*_bsg` for those view-context operations, while
pick-result object helpers still use the retained RT result wrappers.  The
pivot guard requires those context forms and rejects the older typed
view-set, pick, and selection calls from returning to that migrated qtcad
bridge subset.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libqtcad test_qtcad_obol_draw_sync test_qtcad_obol_snap`
followed by
`ninja -C build test_qtcad_obol_pick test_qtcad_obol_selection_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|test_qtcad_obol_draw_sync|test_qtcad_obol_snap|test_qtcad_obol_pick|test_qtcad_obol_selection_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 6/6 passing in 54.21 seconds, with
`libbsg_api_hygiene` longest at 40.90 seconds,
`brlobol_pivot_guard` at 12.82 seconds, and the qtcad Obol workflow tests at
or below 0.13 seconds.

The private qtcad legacy-view bridge now routes a broader existing-context
view-state subset through opaque RT wrappers.  `src/libqtcad/QgLegacyView.cpp`
uses context forms for dimensions, width/height, unit conversion, refresh
request/consume/complete, local/base unit reads, view info,
center/rotation/scale/perspective reads, scale storage, display-manager
get/set, LoD policy get/apply, snap-source and snap-line state, AET get/set,
view update, screen-to-view, model/view matrix reads, view2model writes, scale
and size writes, ADC state, snap kind-mask and snap-tolerance reads,
center-vector writes, screen/current point reads and writes, mouse-state
writes, and radius reads.  The new context adapters are covered by
`rt_view_legacy_bsg`, and older typed RT calls for the migrated subset are
rejected by the pivot guard.  Typed calls for legacy operations without
context equivalents, such as view lifecycle, bounds callbacks, adjust,
autoview, LoD copy/bounds update, snap-exclude, pick/result handling,
selection, feature, polygon, and sketch operations, were left for later
wrapper-surface batches.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libqtcad test_qtcad_obol_draw_sync test_qtcad_obol_faceplate_sync test_qtcad_obol_snap test_qtcad_obol_measure`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|test_qtcad_obol_faceplate_sync|test_qtcad_obol_snap|test_qtcad_obol_measure)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 7/7 passing in 54.21 seconds, with
`libbsg_api_hygiene` longest at 41.04 seconds,
`brlobol_pivot_guard` at 12.71 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and all four qtcad Obol workflow tests at or below 0.11 seconds.

Production libged faceplate commands and the private qtcad legacy-view bridge
now route retained scalar faceplate, grid, interactive-rectangle, axes, params,
and framebuffer state through opaque RT view-context wrappers.
`src/libged/view/faceplate/faceplate.c`
uses `rt_view_context_center_dot_state_*_bsg`,
`rt_view_context_framebuffer_mode_*_bsg`,
`rt_view_context_scale_overlay_state_*_bsg`,
`rt_view_context_params_state_*_bsg`, and the private
`ged_draw_view_context_hud_sync` wrapper instead of typed RT faceplate,
framebuffer, or GED draw-view HUD sync helpers.  `faceplate_grid.c` uses
context-form grid state helpers, `interactive_rect.c` uses context-form
interactive-rectangle state helpers, `faceplate_axes.c` uses context-form
model/view axes state helpers, and
`src/libqtcad/QgLegacyView.cpp` uses the same context-form faceplate, grid,
interactive-rectangle, and framebuffer helpers for its retained qtcad bridge
surface.  The pivot guard requires the context tokens and rejects typed RT
faceplate/grid/interactive-rectangle/framebuffer wrappers and typed HUD sync
calls from returning to those migrated paths.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged libqtcad ged_test_faceplate test_qtcad_obol_faceplate_sync test_qtcad_obol_draw_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_drawing_faceplate|test_qtcad_obol_faceplate_sync|test_qtcad_obol_draw_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 6/6 passing in 55.33 seconds, with
`libbsg_api_hygiene` longest at 40.80 seconds,
`ged_test_drawing_faceplate` at 1.75 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and
`test_qtcad_obol_faceplate_sync` at 0.07 seconds.

The follow-up libged faceplate grid/interactive-rectangle batch passed with:
`ninja -C build libged ged_test_faceplate` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_faceplate)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 55.14 seconds, with
`libbsg_api_hygiene` longest at 40.81 seconds and
`ged_test_drawing_faceplate` at 1.76 seconds.

The matching qtcad legacy-view grid/interactive-rectangle bridge cleanup
passed with:
`ninja -C build libqtcad ged_test_faceplate test_qtcad_obol_faceplate_sync test_qtcad_obol_draw_sync`
and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_faceplate|test_qtcad_obol_faceplate_sync|test_qtcad_obol_draw_sync)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 5/5 passing in 55.66 seconds, with
`libbsg_api_hygiene` longest at 40.90 seconds,
`ged_test_drawing_faceplate` at 1.75 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and
`test_qtcad_obol_faceplate_sync` at 0.07 seconds.

Production libged draw realization debug logging no longer reads BSG view
names directly.  `src/libged/draw.cpp` routes retained draw-scene, wireframe,
CSG wireframe, BoT LoD, and BREP LoD log view names through
`rt_view_context_name_from_bsg` via the local `draw_view_name_bsg` helper, and
the pivot guard rejects direct `gv_name` logging from returning there.

Production libged view-name command paths now use RT legacy view-context
adapters.  `src/libged/view/view.c` routes independent-view redraw/zap
arguments and view-list output through `rt_view_context_name_from_bsg`,
`src/libged/dm/dm.c` routes the dm attach duplicate-view diagnostic through
the same context getter, and `src/libged/ged.cpp` sets the default GED view
name through `rt_view_context_name_set_bsg` instead of writing `gv_name`
directly.  The pivot guard rejects direct `gv_name` access from returning to
those files.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged ged_test_ged_draw_scene ged_test_view_command` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene|ged_test_view_command)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 54.92 seconds, with
`libbsg_api_hygiene` longest at 40.77 seconds,
`ged_test_ged_draw_scene` at 1.40 seconds, and
`ged_test_view_command` at 0.15 seconds.

Production TclCAD display-manager and view-state access now use opaque RT
view-context adapters.  The command wrapper, framebuffer, mouse, polygon, and
view utility/draw/refresh/axes paths route retained display-manager lookup
through `rt_view_context_display_manager_from_bsg` instead of calling the
typed `rt_view_display_manager_from_bsg` adapter directly.  Mouse headless
width/height fallbacks route through `rt_view_context_width_from_bsg` and
`rt_view_context_height_from_bsg`, mouse scale/size reads route through
`rt_view_context_scale_from_bsg` and `rt_view_context_size_from_bsg`, TclCAD
command and polygon scale/center reads, polygon and mouse rotation reads,
command/mouse/polygon view2model reads, mouse and polygon screen-to-view
conversion, and command/mouse/polygon previous-mouse state access route
through `rt_view_context_scale_from_bsg`,
`rt_view_context_center_from_bsg`, `rt_view_context_rotation_from_bsg`,
`rt_view_context_view2model_from_bsg`,
`rt_view_context_screen_to_view_from_bsg`, and
`rt_view_context_previous_mouse_*_bsg`.  TclCAD faceplate center-dot, params,
and scale-overlay state access routes through
`rt_view_context_center_dot_state_*_bsg`,
`rt_view_context_params_state_*_bsg`, and
`rt_view_context_scale_overlay_state_*_bsg`.  TclCAD axes display-scale
view-info reads and model/view axes state access route through
`rt_view_context_info_from_bsg`,
`rt_view_context_model_axes_state_*_bsg`, and
`rt_view_context_view_axes_state_*_bsg`.  TclCAD draw, command, mouse, and
polygon model2view reads route through
`rt_view_context_model2view_from_bsg`, TclCAD wrapper/draw/command
perspective, projection-matrix, and LoD policy access routes through
`rt_view_context_perspective_from_bsg`,
`rt_view_context_pmat_from_bsg`, and
`rt_view_context_lod_policy_*_bsg`, TclCAD command, mouse, and polygon
snap-mask preparation and snap-point application route through
`rt_view_context_prepare_tcl_snap_bsg` and
`rt_view_context_snap_point_2d_bsg`, TclCAD mouse delta settings route
through `rt_view_context_mouse_delta_settings_from_bsg`, and
TclCAD command and view-refresh unit-conversion writes route through
`rt_view_context_unit_conversion_set_bsg`.  TclCAD command framebuffer refresh
and view-refresh interactive rectangle reads, unit-conversion stash reads, and
refresh lifecycle operations route through
`rt_view_context_interactive_rect_state_from_bsg`,
`rt_view_context_local2base_from_bsg`,
`rt_view_context_base2local_from_bsg`, and
`rt_view_context_refresh_*_bsg`.  The wrapper, commands, mouse, and
polygon dimension sync helpers write dimensions through
`rt_view_context_dimensions_set_bsg` instead of the typed
`rt_view_dimensions_set_bsg` adapter.  TclCAD command grid-state, coord,
snap-line, line-snap recenter, zclip, and display-manager set access plus
polygon plane reads route through `rt_view_context_grid_state_from_bsg`,
`rt_view_context_coord_*_bsg`, `rt_view_context_snap_lines_*_bsg`,
`rt_view_context_center_linesnap_bsg`, `rt_view_context_zclip_*_bsg`,
`rt_view_context_display_manager_set_bsg`, and
`rt_view_context_init_bsg`, `rt_view_set_context_add_bsg`, and
`rt_view_context_plane_from_bsg` instead of the typed BSG adapters.  TclCAD
view creation name writes and production command/mouse/polygon view-name reads
route through `rt_view_context_name_*_bsg` and file-local name helpers instead
of direct `gv_name` access.  The pivot
guard now requires the
context helpers for this TclCAD cluster and rejects typed display-manager,
view-state-read, axes-state, faceplate-state, previous-mouse, screen-to-view,
snap-mask, snap-point, mouse-delta-settings, perspective, projection-matrix, LoD-policy,
unit-conversion-write, unit-conversion-read, interactive-rectangle,
refresh-lifecycle, grid-state, coord, snap-line, line-snap recenter, zclip,
view lifecycle init/add, view-name access in TclCAD command/mouse/polygon paths, plane, and dimension-write adapter calls from
returning there.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libtclcad` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|tclcad_test_ged_cmds_exist)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 53.59 seconds, with
`libbsg_api_hygiene` longest at 40.87 seconds.

TclCAD command wrapper dimension synchronization now uses opaque RT
view-context adapters.  `src/libtclcad/wrapper.c` and
`src/libtclcad/commands.c` route display-manager lookup and view dimension
writes through `rt_view_context_display_manager_from_bsg` and
`rt_view_context_dimensions_set_bsg`, and the draw/more-args wrappers pass
`ged_view_active_ctx` directly instead of casting it back to
`struct bsg_view *` only to synchronize dimensions.  The pivot guard now
requires the context tokens and rejects the older typed sync-helper
signatures, typed active-view casts at those sync call sites, and direct
`rt_view_dimensions_set_bsg` calls from returning to those two TclCAD files.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 52.90 seconds.

Production libged diagnostic line-layer fallback producers now publish retained
feature replacement through an opaque GED draw-view context helper.  The
check-overlaps, gqa overlap overlay, draw edge-use, and vutil NMG edge-use
fallback paths call `ged_draw_view_context_line_layer_builder_replace` rather
than the typed `ged_draw_view_line_layer_builder_replace`; the typed BSG cast
is contained inside the private GED draw-view bridge.  The pivot guard now
requires the context helper for these four producers and rejects typed
line-layer fallback calls from returning.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libged gqa ged_test_gqa` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_gqa)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 52.81 seconds.

Production `src/libtclcad` no longer uses the older typed GED draw-view
feature/label helper facade for retained Tcl overlay features.  TclCAD
data-axes view commands now route feature existence, visibility, style reads,
color and line-width mutation, point-copy, axes-center copy, axes replacement,
and feature removal through `ged_draw_view_context_*` helpers.  TclCAD polygon
outline sync now uses context-form feature removal and polygon replacement.
The pivot guard requires the axes/polygon context tokens and rejects
non-context `ged_draw_view_feature_*`, `ged_draw_view_line_*`,
`ged_draw_view_tcl_*`, `ged_draw_view_label_*`, and
`ged_draw_view_arrow_tip_*` helper calls from returning anywhere in production
`src/libtclcad`.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the focused guard run completed 1/1 passing in 12.05 seconds.

TclCAD data-label and data-arrow view commands now use opaque GED draw-view
context helpers for retained overlay feature access.  `view/labels.c` routes
label replacement, feature existence, label count, and label copy through
`ged_draw_view_context_*` helpers.  `view/arrows.c` routes feature existence,
visibility, style reads, color/line-width mutation, point copy, feature
removal, arrow replacement, and arrow-tip get/set through the same context
helper boundary.  The private GED draw-view header now declares the existing
`ged_draw_view_context_feature_remove` helper for private consumers, and the
pivot guard rejects the older typed GED draw-view feature/label helper calls
from returning to these two TclCAD view command files.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the focused guard run completed 1/1 passing in 20.59 seconds.

The TclCAD retained draw-view move/readback helper path now routes data
arrow, axes, line, and label movement through opaque GED draw-view context
helpers.  `draw_view_move_helpers.h` accepts `void *` view contexts and calls
`ged_draw_view_context_feature_points_copy`,
`ged_draw_view_context_feature_axes_centers_copy`,
`ged_draw_view_context_feature_style_get`,
`ged_draw_view_context_tcl_arrows_replace`,
`ged_draw_view_context_tcl_lines_replace`,
`ged_draw_view_context_feature_visible_set`, and
`ged_draw_view_context_tcl_axes_replace` instead of typed
`struct bsg_view` feature helper entry points.  The matching
`commands.c` and `mouse.c` data-move, pick/readback, and scale paths now use
context-form feature existence and label count/copy/update helpers, and the
pivot guard rejects the older typed GED draw-view feature/label helper calls
from returning to this TclCAD cluster.

Focused verification on FreeBSD/clang passed with:
`ninja -C build libtclcad` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`;
the focused CTest run completed 1/1 passing in 23.67 seconds.

The `ged_test_tcl_overlay_bsg` regression now routes its retained headless
Tcl overlay lifecycle coverage through opaque GED/RT view-context helpers.
`tcl_overlay_bsg.cpp` uses `ged_view_active_ctx`,
`ged_draw_view_context_*` feature/Tcl facade helpers, and
`rt_view_context_display_manager_*_bsg` for the NULL-display-manager
`dm_draw_objs` safety check instead of direct `gedp->ged_gvp`,
`struct bsg_view`, `v->dmp`, direct `bsg/feature.h` or BSG view headers,
`bsg_feature_*`, `bsg_view_*`, `BSG_FEATURE_*`, or `BSG_GEOMETRY_*` access.
The GED bridge gained context forms for feature existence, visibility, style,
point copy, line-command reads, Tcl line/arrow/axes/polygon/label replacement,
label copy/update, and arrow/axes helpers; the new line-command accessor maps
retained BSG line commands to existing `GED_DRAW_VIEW_LINE_MOVE/DRAW` values
for test assertions.  The pivot guard now requires those context helper tokens
and rejects direct BSG feature/view access from returning to
`tcl_overlay_bsg.cpp`.

Focused verification on FreeBSD/clang passed with:
`ninja -C build ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_tcl_overlay_bsg)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 3/3 passing in 52.80 seconds, with
`ged_test_tcl_overlay_bsg` at 0.12 seconds.

The retained quad draw fixture now routes both its multi-view swrast setup and
its shared-line validation through opaque GED/RT view-context wrappers.
`quad.cpp` still exercises retained GED/DM drawing behavior, but shared-line
feature geometry checks now use
`rt_view_context_feature_geometry_summary_bsg`, and named visible line
render-item counting now uses `rt_view_context_named_line_render_count_bsg`
instead of direct `bsg/feature.h`, `bsg/render.h`, `bsg_feature_*`,
`bsg_render_*`, or `BSG_RENDER_*` access in the test.  The RT legacy adapter
test covers null/missing feature geometry summary behavior, a two-point
feature geometry summary, and named-line render-count null/empty behavior.
The pivot guard requires the new quad wrapper tokens and rejects direct BSG
feature/render assertions from returning to `quad.cpp`.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_quad` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_drawing_quad)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 64.12 seconds, with
`ged_test_drawing_quad` at 12.65 seconds.

The DM backend draw-item regression now stays off direct libbsg linking.
`src/libdm/tests/test_backend_draw_item.c` still validates retained BSG-backed
render-item dispatch and annotation drawing behavior, but it no longer includes
BSG headers or constructs BSG render-item/annotation fixtures directly.  Retained
view-context lifecycle, line render-item setup, display-manager attachment,
identity model matrices, and annotation curve/text fixture setup now route
through `rt/view_legacy_bsg.h`.  The target links `libdm;librt;libbu`;
generated link commands can still carry libbsg transitively through retained
`libdm`/`librt` owners until those migrate.  The pivot guard rejects direct BSG
fixture headers/calls, typed `rt_view_dimensions_set_bsg` use, and a direct
libbsg relink for the test.  Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg test_dm_backend_draw_item` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|dm_backend_draw_item)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 50.10 seconds.

The GED draw-scene regression has been source-narrowed without removing its
intentional direct libbsg target dependency.  `ged_draw_scene.cpp` remains the
broad structural regression for retained GED BSG draw-scene behavior, but its
ordinary transparency, visibility, highlight, selection-bridge, and view
scene-attached operation checks now route through GED draw shape/group records,
GED view-object records, or `rt/view_legacy_bsg.h` rather than direct BSG state
probes.  The neutral `ged_draw_view_db_object_record` now carries selected
render/export state, a neutral geometry-kind name, neutral view/local source
flags, database-intent state, cache/source identity, and narrow annotation and
line-set accessors plus indexed-face-set surface summary accessors for typed
annotation, visible line geometry, and shaded surface payload checks; shape-ref
line accessors cover draft-published geometry samples before the view-export
path is involved.  The draw-scene selection bridge verifies
selected highlighted database
records through
`ged_draw_foreach_visible_view_db_object_record` instead of constructing a raw
BSG render request, and the qray/uplot view-layer regression verifies its two
colored line records through `ged_draw_foreach_view_record_query` and
`ged_draw_view_db_object_record_foreach_segment` instead of owning a raw BSG
render batch; its retained feature-family, child-count, command-count, remove,
and post-remove absence checks now route through
`rt_view_feature_summary_bsg` and `rt_view_feature_remove_bsg` instead of
direct `bsg_feature_*` calls.  The strict direct face-set fallback regression now verifies
absence of a visible `indexed-face-set` database record through the same GED
view-record surface instead of collecting a raw BSG render batch.  The ANNOT
typed annotation regression now validates geometry kind through
`ged_draw_shape_context_geometry_summary`, and validates export and redraw
payload counts, display-space status, line segment endpoints, reference point,
text, and the backend-scene cache/source identity bridge through
`ged_draw_view_db_object_record_annotation_summary` instead of separate raw BSG
export and redraw render-batch queries.  The shaded BoT/BREP typed surface and
CW-winding regressions plus the NMG visualized-normal face/line payload checks
now validate indexed-face-set counts, material payload, wire/line segments,
exported points, cache/source identity, and sampled winding indices through
GED visible view records, `ged_draw_view_db_object_record_surface_summary`, and
`ged_draw_view_db_object_record_surface_index_at` instead of owning separate raw
BSG render/export batches.  The NMG vector/no-surface, surface-normal,
mixed-wire, and snurb/cnurb line-set sample checks now validate exact line
points and move/draw commands through `ged_draw_shape_ref_line_summary`,
`ged_draw_shape_ref_line_point_at`, and
`ged_draw_shape_ref_line_command_at` for primary shape refs plus
`ged_draw_shape_context_line_summary`,
`ged_draw_shape_context_line_point_at`, and
`ged_draw_shape_context_line_command_at` for borrowed auxiliary normal/wire
shape contexts rather than direct `bsg_line_set_ref_*` probes.  The same
borrowed NMG normal/wire checks now read parent source-container state, display
state, draw intent, style, transparency, highlight, and material color through
`ged_draw_scene_context_display_summary` rather than direct BSG scene probes.
The NMG
surface-normal and mixed-wire primary face geometry count checks, plus the
SUBMODEL nested typed child-shape search, now use
`ged_draw_shape_context_geometry_summary` for borrowed geometry names and
coordinate/index counts instead of constructing BSG line/point/face or
indexed-face-set typed refs in the test.  The SUBMODEL source, nested-source,
leaf-source, and leaf-shape database-source/draw-intent checks now also use
`ged_draw_scene_context_display_summary` rather than direct BSG source and
draw-intent probes in the test.  The shape-count/index regression's
database-source path, stale-source revision, and source-container sync checks
now use `ged_draw_scene_context_source_summary`; its legacy evaluated-region
toggle uses `ged_draw_shape_ref_set_evaluated_region`, and the highlight/material
revision regression now uses `ged_draw_shape_set_highlighted`,
`ged_draw_shape_ref_material_summary`, and
`ged_draw_shape_ref_set_material_color` instead of direct BSG database-source,
legacy eval-flag, highlight, or material-stamp probes.  The draw-scene
registration smoke test now uses `ged_draw_scene_context_tree_summary` for
retained root/child depth and type checks, and the cached aggregate-bounds
regression uses `ged_draw_scene_context_subtree_bounds` for the uncached
cross-check instead of direct BSG tree-depth or subtree-bbox calls.  The
nested group-tree and shape dbpath lookup checks now use local
`test_scene_is_group`/`test_scene_is_shape` helpers backed by
`ged_draw_scene_context_tree_summary` instead of direct BSG group/shape type
predicates.  The highlight-revision regression now changes highlighted and
non-highlighted shape visibility through `ged_draw_shape_ref_set_visible`
instead of touching raw BSG visibility fields, while preserving the invariant
that visibility mutations do not change highlight identity or highlight
revision.  The
draw-scene
regression no longer owns raw BSG
render request/batch or BSG export-query mechanics for these operation-state
payload checks, and its retained rendered-object backend consistency checks
now call `ged_draw_view_rendered_object_summary` instead of creating or walking
a BSG backend scene in the test.  Scene-node, render, export, and other
structural checks remain direct until backend-neutral replacements exist.  The
pivot guard requires the neutral record/RT adapter tokens and rejects the
removed direct BSG helper forms from returning.  Focused verification on
FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_ged_draw_scene` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`;
the most recent focused CTest run completed 4/4 passing in 52.37 seconds, with
`ged_test_ged_draw_scene` itself completing in 1.42 seconds.

The GED mged BSG draw test now stays off direct libbsg linking.  Its retained
edit-matrix render readiness check still verifies that visible highlighted
render items are available, but `mged_bsg.cpp` now asks the RT legacy adapter
for an `rt_view_render_summary_bsg` through
`rt_view_visible_render_summary_bsg` instead of creating BSG render
requests/batches directly.  Scene attachment and center-dot state setup also
route through `rt/view_legacy_bsg.h`: `rt_view_scene_attached_bsg` replaces
the direct scene-attached probe, and a local helper uses neutral
`rt_view_other_state` plus `rt_view_center_dot_state_from_bsg` /
`rt_view_center_dot_state_set_bsg` instead of `struct bsg_other_state` and
`bsg_view_center_dot_get/set`.  `src/librt/tests/view_legacy_bsg.c` covers
the new visible-render summary adapter's null and empty-view behavior, while
`ged_test_mged_bsg` covers the highlighted non-empty GED draw path.  The
target now links `libged;libdm`; generated link commands can still carry
libbsg transitively through retained owner libraries until those migrate.  The
pivot guard rejects the direct BSG utility/view-state/render headers, direct
BSG render calls, and a direct libbsg relink for this test.  Focused
verification passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_mged_bsg)$" --timeout 180 --output-on-failure`.

The GED quad-stability and rtwizard draw tests now stay off direct libbsg
linking for their retained view and scene-anchor checks.  `bsg_quad_stability.cpp`
and `rtwizard_bsg.cpp` still validate retained GED/DM BSG-backed scene
behavior, including quad-view stability, null-DM rtwizard views, swrast
rendering, and shared scene-root behavior, but retained view setup and scene
inspection now route through opaque GED/RT context wrappers instead of direct
`struct bsg_view`, `ged_gvp`, `v->dmp`, `gv_name`, typed RT setup calls, or
direct BSG scene-anchor helpers.  `rt/view_legacy_bsg.h` now owns
`rt_view_context_create_with_set_bsg` in addition to the scene-anchor,
attached, shared-scene, and model2view context helpers; the create-with-set
wrapper preserves BSG view-set scene-root sharing before a secondary null-DM
view receives its retained scene anchor.  `src/librt/tests/view_legacy_bsg.c`
covers that shared-root create-with-set behavior, and `ged_test_rtwizard_bsg`
covers the real GED null-DM view path.  The two test targets now link through
their GED/DM/ICV owners without directly listing libbsg; generated link
commands may still include libbsg transitively until the owning lower-level
libraries migrate.  The pivot guard now rejects direct BSG retained-view or
scene-anchor source use and direct libbsg relinks for both tests.

Focused verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_rtwizard_bsg` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_rtwizard_bsg)$" --timeout 180 --output-on-failure`;
the focused CTest run completed 4/4 passing in 51.83 seconds, with
`libbsg_api_hygiene` at 40.86 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
and `ged_test_rtwizard_bsg` at 0.28 seconds.

The GED mged shaded-mode draw test now stays off direct libbsg linking.
`mged_shaded_mode_bsg.cpp` still validates the retained GED/DM shaded render
path, including lighting, zbuffer, zclip, swrast screengrab output, and
semantic draw-record stability, but it no longer includes the stale
`bsg/defines.h` header and no longer needs `libbsg` in its direct target
dependency row.  The test continues to route retained view setup through
`rt/view_legacy_bsg.h` helpers and links `libged;libdm;libicv`; generated
link commands may still see libbsg through those owner libraries until the
lower-level GED/DM migration removes that transitive edge.  The pivot guard
now rejects direct BSG defines use and a direct libbsg relink for the shaded
mode test.

The GED independent-view draw test now stays off direct libbsg linking.
`view_independent.cpp` still validates retained BSG-backed independent-scope
behavior while the GED view stack is transitional, but it no longer includes
`bsg/util.h` or calls `bsg_scene_ref_is_null` directly.  The new
`rt_view_independent_scope_is_null_bsg` helper keeps independent-scope null
inspection inside `rt/view_legacy_bsg.h`, and the `rt_view_legacy_bsg` adapter
test covers null, live, and destroyed scope states.  The target now links
`libged;libbu` instead of `libged;libbsg`; generated link commands still see
libbsg transitively through retained owner libraries until the lower-level
GED/RT migration removes those edges.  The pivot guard now requires the RT
null-inspection wrapper in `view_independent.cpp`, rejects direct BSG utility
access, and rejects a direct libbsg relink for the test.

The GED view-command draw test now stays off direct libbsg linking.  Its
retained backend-scene render/export consistency check still verifies that the
drawn `all.g` export record, visible render item, and retained backend-scene
node agree on cache identity, source identity, geometry revision, payload
revision, draw mode, and visibility, but that structural check now routes
through `rt_view_render_export_consistency_bsg` instead of constructing BSG
export requests, render batches, and backend scenes in `view_command.cpp`.
The existing snap, measure, and polygon-record checks also continue through
`rt/view_legacy_bsg.h` wrappers.  `src/librt/tests/view_legacy_bsg.c` covers
null and empty-view behavior for the new render/export consistency adapter,
and `ged_test_view_command` covers the real drawn-object path.  The target now
links `libged;libbu`; generated link commands can still carry libbsg
transitively through retained owner libraries until those migrate.  The pivot
guard rejects direct BSG backend-scene/export/render/semantic headers and
calls plus a direct libbsg relink for this test.  Focused verification passed
with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command)$" --timeout 180 --output-on-failure`.

The GED measure semantics draw test now stays off direct libbsg linking.
`measure_semantics.cpp` still validates retained BSG-backed view behavior, but
its lifecycle setup, dimension setup, candidate collection, result inspection,
and null-guard checks now go through `rt/view_legacy_bsg.h` instead of
including BSG measure/util headers or calling `bsg_measure_candidates`
directly.  `rt/view.h` now owns a neutral `rt_view_measure_result` value
record, and `src/librt/view_legacy_bsg.c` converts the retained
`bsg_measure_result` into that RT record through
`rt_view_measure_candidates_bsg`.  `src/librt/tests/view_legacy_bsg.c`
verifies that the adapter matches direct BSG results for normal, null-view,
and zero-length cases.  `src/libged/tests/draw/CMakeLists.txt` therefore no
longer lists libbsg directly for `ged_test_measure_semantics`, and the pivot
guard now rejects direct BSG measure source use or a direct relink for that
target.  The generated link line still includes libbsg transitively through
retained `librt`; removing that edge remains part of the lower-level librt
migration.

The GED snap semantics draw test now stays off direct libbsg linking.
`snap_semantics.cpp` still validates retained BSG-backed view behavior, but
its lifecycle setup, grid-state setup, snap-source setup, candidate
collection, result inspection, and 2D snap calls now go through
`rt/view_legacy_bsg.h` instead of including BSG snap/grid headers or calling
BSG snap/grid APIs directly.  `rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` now provide an opaque
`rt_view_snap_result_bsg` surface with create/free/count/point/distance/kind
helpers plus `rt_view_snap_candidates_result_bsg`, and
`src/librt/tests/view_legacy_bsg.c` verifies those helpers against the raw BSG
result path.  `src/libged/tests/draw/CMakeLists.txt` therefore no longer
lists libbsg directly for `ged_test_snap_semantics`, and the pivot guard now
rejects direct BSG snap/grid source use or a direct relink for that target.
The generated link line still includes libbsg transitively through retained
`librt`; removing that edge remains part of the lower-level librt migration.

The GED selection semantics draw test now stays off direct libbsg linking.
`selection_semantics.cpp` still exercises the retained transitional view path,
but its pick-result and selection operations are routed through
`rt/view_legacy_bsg.h` helpers (`rt_view_pick_point_bsg`,
`rt_view_pick_result_count_bsg`,
`rt_view_selection_set_pick_result_ref_bsg`,
`rt_view_selection_count_bsg`, and `rt_view_selection_clear_bsg`) rather than
including BSG pick/selection headers or calling BSG pick/selection APIs.
`src/libged/tests/draw/CMakeLists.txt` therefore no longer lists libbsg
directly for `ged_test_selection_semantics`, and the pivot guard now rejects a
direct relink while keeping the existing source-level adapter checks.

The qtcad/QgSW swrast integration test now stays off direct libbsg linking.
`ged_test_qged_swrast.cpp` already uses only qtcad legacy-view helpers for GED
active-view binding, view-set registration, unit conversion setup, and scene
attachment checks, so `src/libqtcad/tests/CMakeLists.txt` no longer lists
libbsg directly for that target.  The disabled DM backend benchmark link
template was updated to avoid preserving the same stale direct-link hint.

The test still links retained libqtcad/libged/libdm and therefore still pulls
libbsg transitively until those owner libraries migrate.  The pivot guard now
checks that `ged_test_qged_swrast` keeps using the qtcad bridge surface without
a direct libbsg row.

The Phase 5 lower-level dependency cleanup has removed stale build-only
direct libbsg links from selected converter and simulation targets whose
sources do not use BSG APIs directly.  The Unigraphics `ug-g`, Cubit
`g-sat`, and Creo `creo-brl` direct shim/package rows now list only their
neutral RT/WDB/BG/NMG/BN/BU dependency sets, and the `ged-simulate` plugin
link row now omits libbsg as well.  The generated link commands still include
libbsg transitively through retained libraries such as librt, libwdb, and
libged; removing those transitive edges belongs to the owning librt/libged
migration work.

This is intentionally a build-boundary cleanup rather than a source migration:
the source trees already had no direct `bsg_*`, `BSG_*`, or `bsg/` usage, so
the direct libbsg rows were preserving a noisier dependency graph than the
code required.  The pivot guard now checks the selected CMake files for stale
libbsg relinks and scans the corresponding source trees to make sure direct
BSG API use does not appear without an explicit migration decision.

Consolidated former root notes:

* fb_plan.txt
* lod_pipeline.txt
* lod_result_wakeup.txt
* modern_drawing.txt
* obol_migration.txt
* obol_prototype.txt
* pop_lod.txt
* review.txt


Original Obol Pivot Migration Plan
----------------------------------

Purpose
-------

BRL-CAD should use Obol as the canonical scene, view, interaction, and
rendering API for interactive 3D applications.

The migration is parallel-first: build the Obol path beside the current
BSG/DM path until it reaches production parity, then remove public libbsg,
libdm, dm_plugins, and BSG/DM-shaped application APIs.  Temporary internal
bridges are allowed while a phase is incomplete.  No long-lived public BSG
or DM compatibility layer is part of the target design.

libbrlobol is the BRL-CAD-specific Obol extension library.  It should define
BRL-CAD Obol nodes, actions, details, and narrow application services.  It
must not become a generic replacement for BSG's render-item/backend-scene
model.

The first production target is qged/qtcad.  After qged is usable on Obol,
migrate GED/TclCAD/mged, then remove lower-level BSG/DM utility
dependencies.


Guiding Decisions
-----------------

* Obol cameras, transforms, SoViewport, SoRenderManager, actions, nodes,
  fields, and details are authoritative for scene, view, render, and
  interaction state.
* New drawing code should use Obol directly or use BRL-CAD Obol extensions.
  It should not add new BSG/DM dependencies outside explicit legacy
  allowlists.
* BRL-CAD-specific policy belongs in libbrlobol.  Generic scene graph,
  traversal, rendering, text, picking, and backend features should be exposed
  through Obol rather than rewrapped as parallel BSG concepts.
* C++ is acceptable for code that directly interacts with the drawing API.
  C and Tcl callers may use opaque bridges backed by Obol.
* bg_vlist and bg_line_layer are low-level geometry/debug containers, not
  scene APIs.  Application-visible drawing state should be Obol nodes.
* UNIX plot3 and TIG-style helpers are legacy plot/export/diagnostic support.
  They should not become Obol scene concepts.
* bg_line_layer should stay independent from plot3.  It is an in-memory
  diagnostic/overlay container; plot3 is a legacy file-stream format and
  helper API.  Conversion helpers between bg_line_layer, plot3 output, and
  Obol overlay nodes are appropriate, but moving line_layer into plot3.h
  would couple normal diagnostic geometry to a legacy export format.
* Scene text, labels, HUD text, and annotations should use Obol 2D/3D text.
  If flattened text geometry is needed, use an Obol-backed text-to-geometry
  conversion/export path rather than preserving vectfont as a scene API.
* Obol's pluggable backend model is the intended replacement for libdm's
  rendering/display role.
* libimgstream is the target home for live mutable image streams, async image
  producers, dirty-update notification, transport, and framebuffer protocol
  compatibility.  It is not a display backend system.
* libicv remains the target home for static image containers, codecs, image
  file I/O, image transforms, and neutral lookup-table/colormap operations.
* Obol owns image display, placement, layering, texture upload, readback, and
  rendering backend selection.  Framebuffer pan/zoom/cursor/overlay behavior
  is display compatibility state realized through Obol/application display
  hosts, not core image-stream storage.
* fb compatibility belongs inside libimgstream as a clearly scoped
  compatibility layer while callers migrate.  It should preserve useful
  host:port, ipc:<addr>, PKG_ADDR, FBSERV_TOKEN, FBSERV_TLS, and MSG_FB*
  workflows without preserving libdm's framebuffer backend abstraction.


Current State
-------------

libbrlobol and qtcad:

* brlobol_init registers BRL-CAD Obol classes and supports replacing the
  active Obol context manager after initial setup.
* BRLObolViewController owns the application scene root, active camera,
  SoViewport, SoRenderManager, viewport region, and render-request lifecycle.
* qtcad canvases expose the Obol controller and an Obol viewport image
  readback path.
* qtcad installs an application-owned Obol context manager backed by Qt
  offscreen GL, with OSMesa fallback when Qt offscreen creation is not
  available.
* qtcad GL/SW viewport capture uses Obol readback directly when migrated Obol
  scene content exists, consumes the Obol render request, and does not
  initialize the legacy display manager for that content.  SW visible paint
  follows the same direct Obol readback path.  GL migrated-content paint now
  renders through BRLObolViewController::renderPending with the Qt GL context
  current before legacy DM initialization, and falls back to legacy DM drawing
  only when Obol rendering is not available.
* qtcad default keyboard/mouse/wheel navigation no longer requires a legacy
  display manager before it can update the transitional view and synchronize
  Obol camera/render state.
* qtcad point, rectangle, and ray selection filters prefer Obol pick identity
  for migrated content and do not initialize the legacy display manager when
  Obol supplies the selection paths.  The public qtcad selection filter API
  now exposes normalized paths instead of BSG pick/interaction result
  accessors, and the qged selector plugin consumes those paths directly.
  The qtcad point-pick helper also has the first nonblocking source-backed
  exact mesh pick integration: it keeps ordinary display picks as the first
  pass, then delegates source-backed full-detail mesh picking to the shared
  BRLObolViewController exact-ray helper.  Rectangle picks inherit this
  through their point samples.  qtcad point, ray, and rectangle picks also use
  the controller-owned reusable librt exact-ray cache as a database-source
  fallback for implicit solids and implicit-backed combs when no ordinary
  display/source-backed mesh hit is available.  qtcad pick records now carry
  source-backed mesh face vertex and nearest edge/vertex identity plus
  edit-intent metadata for exact mesh hits.  qtcad point, rectangle, and ray
  selection filters also honor pending source-backed exact-pick submissions by
  clearing immediate selections and skipping legacy BSG fallback until the
  exact detail result is ready.  The explicit-ray selection path derives
  screen-to-view and view-radius data through the RT legacy view adapter
  boundary, and qtcad sketch plus 2D measurement screen-to-view conversions use
  the same adapter instead of calling the BSG helper directly.  Legacy BSG pick
  paths remain internal fallback.
* qtcad Obol measurement and snap workflows operate on migrated content
  without initializing the legacy display manager.  Measurement overlays are
  now published exclusively through Obol; the qtcad measurement filter no
  longer creates BSG measurement features or caches BSG measure-result
  records, and 3D measurement no longer falls back through BSG pick
  candidates or direct raytrace prep.  qtcad now exposes full-detail snap,
  export, and geometry-measure helpers that delegate source-backed LoD
  drain/submit work to BRLObolViewController while the ordinary interactive
  snap helper remains display-level.  Those exact helper records report
  source-backed triangle identity, counts, bounds, area/length, and
  nearest-primitive plus nearest face/vertex/edge identity, with edit-intent
  metadata preserved.  They also report submitted source-detail work when a
  nonblocking exact call has no result yet, giving tool callers an explicit
  retry/pending signal instead of conflating that state with a true miss.
  The controller passes its exact full-detail face/point
  budget to source-backed provider tasks so over-budget whole-object reloads
  can fall back before mesh allocation.  Snap
  retains
  transitional legacy refinement fallback for non-migrated or view-scoped
  cases.
* qtcad Obol workflow tests cover pinewood.g, havoc.g, and the larger
  mixed-primitive m35.g sample, including real-model readback, pick, snap,
  and measurement overlay helpers.  doc/notes/obol_realization_coverage.txt
  now records share/db/faa/Generic_Twin.g as the larger FAA maturity candidate
  once the large-model staged-LoD path is ready for routine gating, and
  test_qtcad_obol_real_models exposes it as an opt-in
  BRLOBOL_QTCAD_GENERIC_TWIN=1 draw/readback check.
* libbrlobol mesh face picks now preserve the picked triangle's source
  coordinate indices in SoBRLPickDetail, and SoBRLExportAction triangle
  records carry the same vertex-index identity.  Face pick details also record
  the nearest source edge endpoints, nearest source vertex, and object-space
  hit point for deterministic mesh sub-entity inspection.  SoBRLSnapAction
  can now report source-indexed mesh vertex and edge-nearest candidates, and
  SoBRLMeasureAction records nearest-face vertex/edge identity while
  preserving the existing face-nearest measurement result.  Edit-preview
  geometry now carries typed edit-intent role/id metadata through explicit
  SoBRLEditPreview fields, shape fields, pick details, snap candidates,
  measure nearest results, export records, and source-backed mesh request
  identity.  The qtcad edit-preview helper has an additive update-with-intent
  entry point so callers can keep preview-node identity separate from the
  live edit operation or target being previewed, and the helper test verifies
  controller-published custom intent metadata through snap/measure/export
  traversal.
  Full standalone edge and vertex picking for non-mesh/native curved entities
  plus broader live edit-tool lifecycle/state semantics remain queued.
* libbrlobol BoT mesh realization now copies existing RT-owned mesh LoD
  metadata onto SoBRLMeshShape fields when a neutral rt_mesh_lod cache result
  is already available.  It records active level, counts, snapped-point/normal
  flags, and bounds without exposing BSG LoD containers or forcing synchronous
  cache generation during normal Obol realization.  The bridge now expresses
  that copied metadata as a provider-neutral BRLObolLodRequest/Result and
  geometry-handle record before publishing the existing Obol fields.
  The neutral BRLObolLodMeshPayload also owns optional active RT mesh normals
  as one normal per triangle-corner index when a provider publishes them, and
  rejects mismatched normal counts before staged payload application.
  Cached POP mesh-LoD normal publication is now bounded by the loaded
  triangle-index buffer size before librt exposes it, and the brlobol RT
  mesh-LoD provider test seeds a normal-bearing cache entry to verify the
  normals survive the RT facade and neutral payload copy.  The RT
  `db_mesh_lod_store_mesh` and transitional `bsg_mesh_lod_cache` public
  comments now make the generated-store normal contract explicit: optional
  normals are one normal per triangle corner in face-index order, and the
  neutral rt_view_info test stores a normal-bearing generated mesh to verify
  active LoD data reports `normal_count == face_count * 3`.  Direct cached
  mesh-LoD views with no full-detail callback now clamp requests above the
  cached POP threshold to the highest cached POP level instead of clearing the
  POP arrays and leaving stale active mesh pointers.  The RT full-detail
  mesh-LoD callback bridge now also validates optional callback normals before
  lending full-detail arrays to the POP implementation: normals must be absent
  with a zero count or present as one normal per triangle corner, and malformed
  normal metadata leaves no active borrowed payload.  Replacing a mesh-LoD
  detail callback now invokes the previous full-detail free hook before the
  callback data pointer is overwritten, so provider-owned callback state does
  not leak when ownership changes on a live LoD handle.  A neutral
  `rt_mesh_lod_detail_callbacks_clear` helper now releases the installed
  detail callback owner without destroying the LoD handle, allowing callers to
  return a live handle to cached POP-only data.  If full-detail callback setup
  fails, the requested level load now fails, leaves the active level invalid,
  and clears previously published active mesh arrays instead of reporting a
  current full-detail level with no active data.
  Full-detail callback payloads with missing required faces, display points,
  or original-point arrays, or with face indices outside the supplied
  display-point or original-point arrays, are now rejected before publication,
  and the RT regressions cover those failures after a valid POP payload is
  active.
  The public RT mesh-LoD active/data/info helpers now use the same required
  original-point contract, so they do not report a usable active mesh unless
  faces, display points, and original points are all present.
  Producer setup callbacks that return failure after filling a partial detail
  record now have their clear hook invoked before the load fails, and the RT
  regression verifies stale active data is still cleared.
  Built-in BoT full-detail publication and cache refresh/store paths now share
  the same RT mesh-array validation as neutral callbacks, and invalid BoT
  cache generation is rejected before BSG sees malformed face indices.
  Invalid generated-mesh replacement stores now return the current cache
  status and leave an existing generated cache entry intact instead of failing
  before object status is populated.
  Replacing or clearing callback ownership while borrowed full-detail arrays
  are active now invalidates that level immediately, so same-level reloads
  cannot skip setup against freed provider storage; callback-owner changes
  made while cached POP data is active republish the POP arrays instead of
  dropping visible data.
  SoBRLExportAction triangle records now carry that same copied LoD summary
  for diagnostic export and staged-realization verification.
* libbrlobol now has the first provider-neutral BRLObolLodService skeleton
  for staged realization work.  It owns explicit start/stop, joinable worker
  threads, generation cancellation, dependency-aware task scheduling, UI-thread
  result draining, in-flight diagnostics, cancellable task-level debug delay
  controls, stale result rejection, owned realization-task data cleanup, and
  one serialized cache-write lane without exposing BSG LoD APIs.  It also has
  the coalesced result-ready wakeup described in the consolidated LoD notes:
  workers only signal that queued results should be drained, and the owning
  view/controller performs the main-thread drain/apply pass.  The LoD result
  contract now carries typed Stage 0/1/2 directory/dependency records, display
  attributes, AABB results, and proxy/OBB payloads so early placeholder states
  can be generated and inspected before final mesh payloads arrive.
  SoBRLMeshShape can now consume staged LoD results into Obol fields, reject
  stale result keys, and publish cached RT mesh results through the same
  staged-result path.  Staged mesh results can now carry an owned Obol-side
  triangle payload copied from an active RT mesh-LoD data view, and
  SoBRLMeshShape can apply that payload through its existing indexed-triangle
  fields without borrowing RT or BSG arrays.  SoBRLMeshShape can also build a
  provider-neutral BRLObolLodRequest from its Obol identity, policy, counts,
  and bounds fields, and BRLObolViewController can derive a sanitized
  rt_view_info snapshot from its Obol camera and viewport state.  The first
  SoBRLMeshLodSubmitAction can walk Obol mesh nodes, build deterministic
  requests from those fields, own RT mesh-cache provider state through the
  service task lifetime, and submit view-driven cached mesh refresh/load work
  explicitly.  BRLObolViewController now exposes a narrow controller hook that
  derives the active rt_view_info snapshot, submits per-database-source mesh
  LoD requests through that action, drains completed service results, and
  applies them back through the update action while recording counts and
  diagnostics; it can retain an active LoD service subscription, mark pending
  results from the wakeup callback without touching scene nodes, and process
  those results before rendering.  It also has the first coalesced auto-submit
  gate for changed view/policy/source signatures, so render-time preparation
  can enqueue fresh RT mesh-cache provider work without duplicate submissions
  for the same stable scene.  The first SoBRLLodUpdateAction drains LoD service
  results on the UI/main thread, matches them to SoBRLMeshShape nodes by
  source path/name, applies accepted staged fields and owned mesh payloads,
  and records rejected or unmatched result diagnostics.  The first RT
  mesh-cache provider task can refresh/load cached RT mesh LoD through the
  service, optionally force a requested RT LoD level, shrink active RT arrays
  after copying, and return the neutral staged mesh result with an owned mesh
  copy.  SoBRLLodMeshShape now provides
  the first dedicated LoD-backed BoT mesh representation while preserving the
  existing SoBRLMeshShape full-field path for small or synchronous meshes, and
  LoD submission filters ordinary meshes unless explicitly configured
  otherwise.  BoT database realization now uses lodBotThreshold so the
  full-mesh path remains default and only threshold-matching BoTs become
  LoD-backed.  SoBRLMeshShape now has an explicit full-detail preservation
  policy: ordinary meshes preserve their pre-LoD indexed triangles when
  applying an active display-LoD payload, while SoBRLLodMeshShape opts out of
  that backup and keeps lightweight source counts/bounds for stable request
  identity after the full-detail fields are replaced.  For preserving meshes,
  SoBRLExportAction/SoBRLMeasureAction/SoBRLSnapAction can use that preserved
  source geometry for full-detail/detail-preserving policy requests while
  DISPLAY_LEVEL continues to use the displayed LoD mesh.  When no preserved
  full-detail payload is resident, exact export/measure/snap now record
  BRLObolSourceMeshRequest records carrying source identity, counts, and
  bounds instead of treating display LoD as exact geometry.  The first RT
  source full-detail provider can reload direct BoT source objects into owned
  Obol mesh payloads through BRLObolLodService, validates source-count
  mismatches as stale results, and exposes whole-object face/point limits so
  callers can refuse over-budget full-detail reloads before falling through to
  chunked detail reads.  BRLObolViewController now owns a per-view exact
  full-detail budget, and qtcad exact export/pick/snap/measure submit paths
  pass those limits to source-backed provider tasks.  Export, measure, and snap
  actions can now convert their recorded source requests into submit-ready RT
  full-detail LoD requests, and BRLObolLodService can submit those source
  requests with provider-owned cleanup.  Those actions can also consume
  returned full-detail mesh payloads using the recorded transform and
  shape-level metadata, and now expose action-level submit and drained-result
  consume helpers for recorded source-backed requests.  BRLObolViewController
  wraps those action-level paths for export, measure, snap, and source-backed
  mesh ray picking so higher-level callers can share filtered LoD-result drain,
  source-backed submission, and exact full-detail budget handling.  Exact mesh
  picking now has a matching
  source-backed traversal action that records full-detail source requests for
  LoD-backed meshes in exact-pick mode, can submit them through the LoD
  service, and can consume drained full-detail results via the shared
  pick-result ray tester.  qtcad point and rectangle picking now route exact
  mesh picks through the controller helper around provider drain points without
  blocking the UI, and qtcad's explicit full-detail snap, export, and geometry
  measure helpers now delegate source-backed LoD drain/submit work to
  BRLObolViewController while preserving source-backed face, vertex/edge,
  nearest-subentity, and edit-intent identity in their qtcad result records.
  Other
  exact-operation tools still need matching caller-side drain/submit
  integration.  The LoD service now supports request-filtered
  result draining, which lets
  source-backed exact-operation callers consume matching full-detail results
  without stealing unrelated display-LoD results from the shared queue.
  BRLObolViewController now resolves each source-backed exact request to the
  matching database source before submission, stamps controller-submitted
  full-detail requests with database/source revision identity, and has a
  multi-source test proving exact reloads do not fall through to the first
  database source in the view.  BRLObolLodService now exposes active-request
  lookup plus a unique-submit path, and the RT source full-detail submit
  helper uses it so repeated exact retries do not enqueue duplicate source
  reloads while an identical provider request is already queued or running.
  Controller exact helpers now also treat partially ready multi-request
  operations as a single pass: they consume ready source-backed payloads and
  submit the still-missing export/measure/snap or exact-pick requests before
  returning, avoiding an extra retry cycle per missing mesh.
  Measure, snap, and pick source-backed exact requests now carry source-local
  query metadata into LoD provider params: measure records nearest-query point
  bounds, snap records bounded query boxes and tolerance, and pick records
  source-local rays.  This gives the future chunked BoT reader the object
  identity and query scope needed to avoid whole-object reloads, while leaving
  the current whole-direct-BoT read path in place.  The direct BoT provider
  can trim returned face payloads and compact subset vertices for explicit
  bounded measure/snap requests carrying bounds plus tolerance and for
  exact-pick requests carrying a source-local ray, and it now applies exact
  full-detail face/point budgets to those scoped payloads after reduction.
  Only `source_query.space=source_local`
  enables direct BoT payload reduction or scoped budget bypass; mixed-kind,
  duplicate, malformed, or non-source-local query metadata stays whole-object
  for correctness.  Scoped bounds/ray misses now return a terminal no-result fallback instead of
  silently broadening to a whole-object direct BoT payload.  Those subset
  payloads now include optional source face- and vertex-index maps so export,
  measure, snap, and pick consumers preserve the original BoT ids instead of
  reporting packed subset slots.  The source-request-to-LoD conversion helper
  now strips stale `source_query.*` params from template requests before
  appending the current source-backed query metadata, so request identity and
  provider execution do not drift through duplicate or mixed template params.
  Pick
  consumers accept partial source
  payloads only for ray-scoped requests; non-query full-detail results still
  require whole-object face counts.  Unbounded measure hint requests
  deliberately stay whole-object because global nearest-primitive measurement
  needs a complete search domain.  Measure now has an explicit query distance
  limit for bounded-neighborhood semantics; with that opt-in, source-backed
  measure requests carry bounds plus tolerance, resident measurement filters
  nearest candidates by the same distance, the direct BoT provider may reduce
  returned faces after scoped bounds selection, and the measure consumer
  accepts compact source subsets only when source face and compact vertex maps
  are present.
  The LoD submit-action gate now also pins non-forced view-policy active-level
  scheduling by comparing a submitted provider result with the level selected
  directly by `rt_mesh_lod_load_view` for the same `rt_view_info`; forced-level
  overrides remain covered separately.  The submit action also skips
  duplicate provider work when a ready mesh LoD result for the same
  view/policy/provider request key is already resident or already
  queued/running in the LoD service, while reset and forced-level submissions
  still bypass that no-op path.
  Mesh ray
  picking now has the same initial policy split: display-level is the viewport
  default, while explicit full-detail picks use resident preserved source
  triangles when available and otherwise refuse to treat an active display-LoD
  payload as exact geometry.  That preservation is an interim correctness
  bridge for meshes that are already resident, not the target policy for large
  geometry.
  Production LoD-backed BoTs must be
  source-backed and memory-budgeted, keeping identity/bounds/active display
  payloads resident while exact operations fetch full-detail or bounded chunks
  from the .g/RT provider path on demand.  Exact picks for CSG implicit solids
  and implicit-solid-backed comb instances should use a librt ray query rather
  than tessellated display geometry.  libbrlobol now has the first direct
  librt ray helper for explicit database object paths; it returns the nearest
  implicit/comb hit with point, normal, distance, region/material data, source
  primitive name/type, and BRL-CAD pick-detail identity.  qtcad point,
  explicit ray, and rectangle picks now call that helper for database-source
  implicit/comb fallback hits, including camera-derived rays for point-pick
  scenes that have database sources but no display-pickable Obol geometry.
  BRLObolViewController owns reusable RT exact-pick caches for its current
  database sources, keeps them across repeated point, ray, and rectangle pick
  calls when the source signature is unchanged, and clears or rebuilds them
  when scene/source ownership or source revisions change.  qtcad point and
  ray picks now merge librt exact candidates into the same distance-ordered
  record set as display and source-backed mesh picks while suppressing
  same-path RT/display duplicates, and rectangle picks inherit that ordering
  through sampled point picks.  QgSelectRayFilter now tries the Obol
  explicit-ray helper before its legacy BSG/RT fallback, and that Obol ray
  selection path can run without the filter's legacy `dbip` field.
  POP payload ownership, active-level selection, action/pick consumption of
  source-backed full-detail reloads, chunked detail reads from the recorded
  source requests when no preserved payload is resident,
  optional coarse POP-build spatial/index sidecar metadata for targeted exact
  picks/snaps, remaining non-qtcad tool integration of the librt exact-pick
  helper, applying the merged ordering policy to those exact-pick tools, and
  richer policy-aware provider scheduling are still queued.
* libbrlobol shaded PNTS realization now preserves all RT PNTS point-record
  variants as Obol vlist point primitives rather than failing the shaded path
  or forcing a mesh.  SoBRLVListShape exposes point primitive inspection plus
  optional per-point color, scale, and normal accessors; scale contributes to
  scene/export bounds, SoBRLExportAction exports point records with source and
  attribute identity with transform-aware point normals/scale bounds, snap
  treats point primitives as endpoint candidates, and prototype tests cover
  shaded PNTS realization, bbox, pick detail, export, snap behavior, every
  intermediate color/scale/normal attribute layout, global scale fallback,
  per-point scale precedence, and transformed attribute export.  A native
  point/marker node remains deferred.
* libbrlobol shaded sketch and annotation realization now uses the same RT
  plot-backed SoBRLVListShape geometry as wire mode for those curve-like
  non-surface primitives.  This keeps annotations and 2-D sketch curves
  drawable, pickable, snappable, exportable, and measurable in shaded scenes
  without pretending they are tessellated surface meshes; native Obol text and
  curve nodes remain queued.
* libbrlobol standalone material database realization now has a stable Obol
  representation.  SoBRLMaterialObject stores database path/source identity,
  material name, parent/source strings, and grouped property key/value pairs
  without contributing drawable geometry.  Prototype tests cover wire and
  shaded material-object realization plus no-draw bbox, pick, snap, export,
  and measure behavior.
* qtcad still has a transitional BSG-to-Obol camera bridge.  The bridge is
  correctly kept in libqtcad, not libbrlobol.
* qtcad's visible rendering path is still transitional.  Obol render/readback
  works and GL/SW migrated-content paints bypass libdm, but libdm/BSG are
  still part of current application rendering for non-migrated content and
  legacy fallback paths.

Dependency guardrails:

* brlobol_pivot_guard is active as a transitional CTest guard.
* The guard blocks new BSG/DM leakage in Obol-canonical code and rejects
  direct BSG vlist/vlblock symbols in libbrlobol.
* doc/notes/obol_legacy_dependency_inventory.txt records the phase-owned
  direct BSG/DM dependency rows for the Obol pivot, and the guard checks the
  exact rows against src/source_dirs.cmake.
* The guard checks that qtcad Obol workflow tests do not directly link libdm,
  libbsg, or dm_plugins while they exercise the transitional application path.
* The guard rejects reintroducing BSG measurement feature/result fallback
  symbols or the old BSG-pick/raytrace 3D measurement fallback in
  QgMeasureFilter while the rest of qtcad continues its staged migration.
* The guard rejects reintroducing public BSG/raytrace selection accessors in
  QgSelectFilter and direct BSG interaction consumption in the qged selector
  plugin.
* The guard checks that the default plot3 implementation is owned by the
  normal libbg implementation unit and is not reintroduced through libbsg or
  standalone bg test executables.  It also checks that bg_line_layer keeps
  its explicit plot3 diagnostic export helpers and that standalone plot3
  utilities do not relink libbsg.
* The framebuffer/image-stream plan records the split: libimgstream owns
  non-display stream storage, transport, producer/subscriber lifecycle, and
  fb compatibility; libicv owns static images/codecs/lookup tables; Obol and
  libbrlobol own image display and window-host behavior.
* The initial libimgstream target now provides an opaque imgstream_* C API for
  RGB/RGBA memory streams, resize, clear, full-frame and rectangle
  read/write, stream info/metadata snapshots, dirty-region generation,
  subscriber notification, producer begin/end state, and icv_image_t snapshot
  conversion without linking libdm, libbsg, or libbrlobol.
* libbrlobol now provides SoBRLImageSource as the first Obol image-stream
  source ownership node.  It can wrap static icv_image_t data or borrowed
  libimgstream streams, tracks stream metadata/revisions in Obol fields, and
  subscribes to dirty-region notifications.  SoBRLViewportImage and
  SoBRLImagePlane are the first display consumers: they read source streams,
  rebuild standard Obol texture quads when source data/dirty revisions change,
  and expose the viewport/image-plane policy fields needed by framebuffer
  display migration.  Incremental backend dirty-region texture uploads and
  real window-host integration remain queued.
* The first libimgstream framebuffer compatibility unit is in place as
  imgstream/fb_compat.h and src/libimgstream/fb_compat.c.  It maps memory
  framebuffer requests to RGB imgstream buffers, preserves legacy view,
  window, zoom, cursor, screen-cursor, and cursor-shape compatibility
  spellings plus reset, viewport, poll, and poll-rate compatibility as
  non-display state/operations, supports RGB and BW rectangle compatibility,
  and supports raw RGB file sink/source compatibility for
  ordinary file paths plus explicit /dev/disk:<path> or /dev/disk=<path>
  spelling.  It also supports /dev/stack fan-out to memory/file child sinks
  and /dev/null, /dev/debug, and /dev/txt diagnostic sinks without a display
  backend.  It preserves legacy 256-entry RGB colormap state through a neutral
  imgstream_fb_colormap type and rmap/wmap compatibility calls.  It classifies
  and normalizes remote/display specifiers through imgstream_fb_spec_info,
  including numeric port specs, host:port, host:port:device, tcp:, ipc:, and
  display framebuffer names.  Display-capable framebuffer opens now create an
  RGB stream and delegate open/close, view, cursor, viewport, flush, reset,
  and poll behavior to a registered toolkit-neutral display host callback;
  without that callback they still fail explicitly until the Obol/libbrlobol
  display-host migration connects real windows.
* The guard still needs to grow as more directories become Obol-canonical.

Low-level neutralization:

* include/bg/vlist.h owns bg_vlist, bg_vlblock, BG_VLIST_* commands, helper
  macros, and pure helper declarations.
* src/libbg/vlist.c owns the pure vlist helper implementations, including
  command counting, bbox, validation, copy/import/export/cleanup, vlblock
  helpers, RPP emission, ARB8 wire emission, text stroking, and plot3/uplot
  export.
* BG-owned polygon and SAT interfaces no longer include BSG headers.  The
  pivot guard prevents include/bg and src/libbg from reintroducing direct BSG
  header dependencies.
* include/rt/vlist.h exposes RT-facing aliases over bg_vlist.  The RT-named
  text/uplot helpers are wrappers over the BG-owned implementation.
* include/bsg/vlist.h, src/libbsg/vlist.c, and src/libbsg/font.c are
  compatibility layers.  They should disappear with libbsg.
* Most librt primitive wire emitters, the NMG edit-test helper, prep plot
  export, annotation text stroking, and librt text/uplot bridges now include
  rt/vlist.h and use RT_VLIST_* or RT-named helper APIs instead of BSG vlist
  names.  The pivot guard prevents those callers from regressing to
  bsg/vlist.h, bsg_vlist/bsg_vlblock, or BSG_*VLIST macros.  The BREP debug
  vlblock declaration also uses rt_vlblock, so src/librt no longer has direct
  bsg/vlist.h users.
* librt primitive wire/LoD line emitters use RT_PRIMITIVE_* line-command names
  for move, draw, and point records instead of BSG_GEOMETRY_* names.  The
  command encodings intentionally match BG line-layer and legacy BSG streams,
  and the pivot guard prevents src/librt/primitives from regressing to
  bsg/geometry.h or BSG_GEOMETRY_* names.
* include/rt and non-test src/librt no longer include the broad bsg.h umbrella
  header.  Remaining transitional BSG use in librt is routed through specific
  headers and explicit legacy adapters while BSG-backed compatibility paths are
  retired.  The pivot guard prevents reintroducing the BSG
  umbrella include.
* include/rt/edit.h owns rt_edit_knobs plus its reset/hash helpers and no
  longer includes bsg/defines.h or embeds bsg_view_knobs.  It also owns the
  rt_edit_view snapshot used by rt_edit runtime state, so struct rt_edit no
  longer stores a BSG view pointer.  The main edit create/reinit/knob APIs now
  accept rt_edit_view; callers that still own BSG view state opt into the
  transitional include/rt/edit_legacy_bsg.h adapters.  The neutral
  src/librt/edit.cpp implementation is free of BSG headers and adapter logic;
  the BSG edit view-state adapter implementation is centralized in
  src/librt/view_legacy_bsg.c.  The src/librt/tests/edit primitive edit tests now
  construct RT-owned rt_edit_view snapshots directly through a local test
  helper instead of creating BSG views.
* include/rt/view.h owns the neutral rt_view_info, rt_view_lod_settings, and
  rt_view_lod_policy snapshots used by RT primitive plot, LoD realization,
  indexed-face-set callbacks, and transitional view LoD policy adapters, plus
  RT_VIEW_* normalized view-coordinate constants and RT_VIEW_MIN_SIZE /
  RT_VIEW_MIN_SCALE limits for callers that only need historical view numeric
  policy.  rt_view_info_sanitize and rt_view_lod_policy_sanitize normalize
  invalid dimensions, view size, and nonpositive LoD scales to neutral defaults
  at adapter boundaries.
  include/rt/functab.h and the primitive callback declarations no longer
  expose bsg_view for those callbacks; BSG-backed callers adapt through the
  transitional include/rt/view_legacy_bsg.h boundary.
  src/librt/view.c contains only the neutral RT view helper implementation and
  exposes RT-prefixed view metric helpers for LoD curve scale, BoT threshold,
  average sample spacing, and solid point spacing; primitive LoD code uses
  those RT names instead of private primitive/view helper declarations.
  src/librt/view_legacy_bsg.c owns the BSG view, LoD policy get/set/copy,
  LoD bounds-update/callback, view-scale/size/inverse-size, keypoint,
  rotate-about, coord, and scene-ref LoD load/free adapter implementation.  BSG
  mesh-LoD unwrapping is private to librt implementation units rather than an
  installed legacy adapter surface.
  src/libged's private primitive wireframe and face-set publication helpers
  now consume rt_view_info snapshots before invoking RT primitive callbacks;
  BSG-to-RT view adaptation is centralized in the transitional
  src/libged/bsg_ged_draw_view.c helper used by the BSG-owning caller
  wrappers, and those wrappers now receive the adapter declaration through the
  private bsg_ged_draw_private.h boundary rather than one-off declarations.
  The same helper wraps the RT-owned LoD policy snapshot for draw.cpp, draw
  refs, transaction redraw paths, and the draw command's temporary
  BoT-threshold mutation/restore compatibility path, plus the transitional BSG
  LoD bounds-update hook used by transaction redraw.  The libged lod and view
  lod commands use that same private wrapper, while raw BSG LoD policy
  get/set, BSG view bounds-callback identity, view-scale extraction, and
  scene-ref LoD load/free calls stay inside the RT legacy adapter
  implementation.
  libged plot, vnirt, solids_on_ray, ADC, and DM window-bound
  helpers now use RT_VIEW_* and RT_INV_VIEW constants instead of BSG_VIEW_*,
  BSG_INV_*, or INV_BV spellings for normalized view-coordinate math, and
  solids_on_ray also reads ray center, rotation, model2view, and scale through
  the RT legacy adapter; rtcheck2 reads overlap glyph scale through that same
  boundary.  libged
  size, scale, and zoom commands now use RT_VIEW_MIN_SIZE/RT_VIEW_MIN_SCALE
  instead of BSG_MINVIEWSIZE/BSG_MINVIEWSCALE for view bounds, and the pivot
  guard checks those command paths.  libged check -i overlap setup,
  get_eyemodel output, _ged_rt_write script emission, rtwizard command
  construction, NIRT default shot center/direction, NIRT qray glyph scale,
  vnirt view-to-model conversion, GED eye-model helper snapshots, BoT/BREP
  viewport pick ray setup, BREP TikZ view AET output, libged
  selection/polyclip/view-snap/view-polygon/vZ coordinate transforms,
  pipe/metaball/rot_point/label transform helpers, BoT/ARB nearest-subentity
  transform helpers, draw refresh view snapshots through the private GED draw
  bridge, and libged view
  size/center/AET/ypr/quat/rmat/perspective/eye_pos/width/height query output
  plus TclCAD polygon command view snapshots and coordinate conversions now
  read through the RT legacy view adapter; TclCAD data move/pick/screen-to-model
  command conversions also snapshot model-to-view and view-to-model matrices
  through that boundary, and TclCAD mouse BREP/joint selection rays plus simple
  metaball/pipe point lookups now snapshot view-to-model matrices and view
  rotation through that boundary; remaining TclCAD mouse inverse-rotation
  edit-motion calculations now use a local adapter helper rather than direct
  `gv_rotation` reads.  TclCAD mouse BoT single-point setup, polygon
  circle/contour/ellipse/rectangle creation, and data-scale display
  conversions now also snapshot model-to-view, view-to-model, and scale values
  through the same adapter boundary.  RT now exposes legacy raw width/height,
  view-size, inverse-size, initial-scale, absolute-scale, unit-conversion,
  keypoint, rotate-about, and coord adapters; TclCAD mouse delta scaling
  uses the size adapter instead of direct `gv_size` reads, and libged
  isize/keypoint/rotate_about queries plus rotation/translation utility helper
  reads use the same boundary; libged, TclCAD, and MGED default/query coord
  reads plus MGED edit-knob coord save reads now use that adapter boundary
  while coord setters and unsupported staging/hash fields remain transitional.
  MGED view command staging/cache/hash reads for adapter-covered fields, MGED
  temp staging width/height reads, and libged DM fallback width/height checks
  now use the same boundary.  libged pmat/pmodel2view query output, TclCAD
  draw projection-matrix loads, and MGED staging/cache/hash projection-matrix
  reads now also snapshot through the adapter while projection writes remain
  transitional.  MGED staging/cache/hash initial/absolute scale reads plus
  libged and MGED knob absolute-scale query output now also use scalar
  adapters, and MGED cache plus TclCAD refresh unit-conversion stash reads use
  the same boundary.  MGED VIEWSIZE/VIEWFACTOR macros now expand through the
  adapter boundary while scale and unit-conversion writes remain transitional.
  libged AET incremental updates, eye-position
  perspective-matrix updates, and perspective command matrix updates now also
  avoid direct BSG readbacks after writing transitional view state.  libged
  knob keypoint-origin rotation now snapshots keypoint through the adapter,
  libged size/scale/zoom commands compute paired size/scale/isize values
  from local snapshots rather than direct view readbacks, and libged
  pure-rotation plus MGED view-scale ratio readbacks now snapshot current view
  state before writing transitional BSG fields.  MGED
  simulation output view argument injection now snapshots
  orientation, eye position, and size through the adapter boundary, and MGED
  command autoview ring-scale snapshots use the same read boundary.  MGED make
  default center/scale
  reads and area view-rotation diagnostic output now use the same adapter
  boundary, and libged ADC view scale/matrix conversion reads now use that
  boundary.  MGED slider view-scale reads now use the same adapter boundary,
  and libged grid vsnap, eye, and rectangle-zoom read-side view snapshots now
  use RT adapters while their live view writes remain transitional,
  and libged lookat/qvrot read-side view snapshots follow the same pattern,
  and MGED axes overlay view size/rotation/model-to-view reads use RT
  adapters, and MGED rectangle zoom, pipe pick direction, view-centered
  transform, and initial view-center setup reads now use the same adapter
  boundary, and MGED status query output now reads view scale and matrices
  through that boundary as well.  MGED grid auto-size, draw/snap scaling, and
  grid coordinate-transform reads now use RT adapter snapshots while live grid
  view-center writes remain transitional, and MGED ADC view scale and matrix
  conversions, absolute translation bookkeeping, and chgview knob/edit/slew/size
  read snapshots now use the same adapter boundary.  MGED generic mouse and X
  motion/ADC input conversions now snapshot view scale, center, and matrices
  through that boundary while leaving event dispatch unchanged,
  while the GED-owned eye-model
  helper calculation remains in GED because it depends on draw bounds and
  clipping behavior.  RT command, rtwizard, saveview,
  embedded-RT, and GED
  eye-model-helper perspective reads also use the same adapter boundary, and
  plot/PS/PNG diagnostic export now copies
  model2view, center, scale, perspective, and eye-position camera values
  through that boundary before invoking the legacy export routines.  The librt
  edit BSG compatibility bridge now builds `rt_edit_view` snapshots through
  the RT view adapter boundary instead of reading BSG view fields directly.
  Legacy DM view-scale pointer setup now uses
  `rt_view_scale_storage_from_bsg` for the remaining live pointer contract,
  so libged DM attach, MGED attach, and qtcad GL/SW fallback setup no longer
  name `gv_scale` directly when binding DM view storage.  qtcad drag-bounds
  callback suspend/restore now uses `rt_view_bounds_update_callback_*_bsg`
  helpers rather than reading or writing `gv_bounds_update` directly.  qtcad
  snap policy reads, simple GED/TclCAD snap-line access, TclCAD Tcl-snap
  preparation, TclCAD snap-line recentering, and qtcad/libged snap tolerance
  factor callers now use `rt_view_snap_*_bsg` and
  `rt_view_center_linesnap_bsg` helpers instead of calling BSG view-state or
  snap helpers directly.  TclCAD/MGED/libged z-clip view-state get/set paths now use
  `rt_view_zclip_*_bsg` helpers rather than direct BSG view-state calls.
  libged/qtcad framebuffer-mode command and toggle paths now use
  `rt_view_framebuffer_mode_*_bsg` helpers as a narrow scalar compatibility
  migration; this does not resolve the broader framebuffer display ownership
  work that belongs with libimgstream/Obol display migration.  gsh/libged
  cleared-state tracking now uses `rt_view_cleared_*_bsg` helpers instead of
  direct BSG view-state calls.  libged/TclCAD polygon clipping and
  area/overlap helpers now derive their view plane through
  `rt_view_plane_from_bsg` instead of calling `bsg_view_plane` directly.
  MGED shared-settings checks now use `rt_view_settings_shared_bsg` instead
  of calling the BSG view-state helper directly.  libged loadview restore
  writes now use RT legacy setter adapters for size, rotation, center, and
  perspective instead of calling BSG utility setters directly.  qtcad
  keybinding/canvas AET writes now use the same RT legacy utility-setter
  boundary; the qtcad Obol draw-sync and snap test center/query setup now use
  the opaque qtcad legacy-view wrapper for that same retained state.  qsketch
  view setup/fit-to-view writes now use qtcad legacy-view helpers as well.
  qged polygon
  snap-line/source-flag/snap-exclude UI state
  and libged snap-semantics test setup now route through the same snap-state
  adapter helpers, and the libged view-independent oversized view setup now
  uses an exact scale-state adapter while polygon refs and grid state remain
  transitional.
  libdm framebuffer-mode draw/open checks also use the RT legacy framebuffer
  mode helpers, while broader framebuffer display ownership remains queued for
  libimgstream/Obol display-host migration.
  A low-risk setter batch routes qged polygon align-to-poly and
  libged view/zoom/scale/rect/orient/rmat/setview/vutil writes for AET,
  center, rotation, size, and scale through the RT legacy setter boundary
  while leaving unsupported derived view-to-model/projection writes and
  lifecycle/ownership changes as separate transitional work.
  A follow-on simple-field setter batch adds RT legacy adapters for keypoint,
  coord, and rotate-about writes and routes libged keypoint/rotate_about,
  TclCAD set_coord, and MGED coord/rotate-about setting hooks through them.
  MGED chgview's librt-edit temporary coord override/restore now uses that
  same coord setter boundary.  MGED view-command cache/staging restore/copy
  paths now also use the RT legacy setters for perspective, coord, and
  rotate-about while leaving scalar/matrix staging writes transitional.  MGED
  saved-view and view-ring restore paths now use the RT legacy setters for
  scale, rotation, and center before their existing `new_mats` refresh.  MGED
  and libged grid center updates now use the RT legacy center setter while
  preserving the existing `new_mats`/`bsg_update` calls.  libged preview
  camera rotation/center writes now use the RT legacy setters while preserving
  the existing preview `bsg_update` calls.  MGED RT command view updates now
  use the RT legacy scale, rotation, and center setters while preserving the
  existing `new_mats` refresh sequence.  `mged_vscale` now routes its
  view-scale update through the RT legacy scale setter while preserving the
  existing absolute-scale knob bookkeeping, and live chgview initial/absolute
  scale bookkeeping now uses RT legacy setters.  Libged pmat, perspective, and
  eye_pos projection-matrix/eye-position updates plus MGED dozoom temporary
  projection-matrix updates now use RT legacy setters while preserving their
  existing `bsg_update` or frame-local restore behavior.  MGED cmd
  staging/cache restore/copy now uses RT legacy exact-state setters for
  scalar tuples, unit conversions, raw AET, eye/keypoint, rotation/center,
  derived matrices, and projection matrices.
  The libged perspective command and MGED perspective variable hooks now also
  route direct perspective field writes through the RT legacy setter while
  preserving their existing projection-matrix and display-manager sync logic.
  qtcad/qged production viewport dimension sync now uses a paired RT legacy
  width/height setter for GL, SW, shared canvas-state, quad-view, and
  current-view application updates; test fixture direct width/height setup
  remains transitional.  TclCAD wrapper, command, mouse, and polygon
  DM-to-view width/height sync now uses the same RT dimensions setter, and
  MGED draw/temp-staging plus libged headless DM fallback dimension writes now
  do the same; the pivot guard rejects reintroduced direct production
  assignments there.  Direct screen-to-view conversion calls now route through
  `rt_view_screen_to_view_from_bsg` in libged label creation, TclCAD
  mouse/polygon/screen conversion paths, and legacy libdm label-arrow drawing,
  leaving remaining direct `bsg_screen_to_view` usage contained to the
  adapter/test implementation.  Direct screen-pixel-to-model-point
  conversions now route through `rt_view_screen_point_from_bsg` in libged
  view-object/polygon commands and qtcad view, sketch, and polygon filters,
  leaving direct `bsg_screen_pt` usage contained to the adapter/test
  implementation plus libbsg internals.  Interactive rectangle state access
  now routes through RT legacy adapters in qtcad box selection, libged
  rectangle/select/faceplate commands, and TclCAD refresh/rectangle-refresh
  paths.  Those command and tool paths now use neutral
  `rt_view_interactive_rect_state` records and wrappers, leaving BSG-shaped
  rectangle records in the legacy adapter/test implementation plus libbsg
  internals.  The public libdm rectangle draw helper also takes neutral
  interactive rectangle state.  View refresh
  request/dirty/consume/complete/enabled/suppression/drawn-count state now
  also routes through RT legacy refresh helpers in qtcad canvas/view refresh,
  TclCAD refresh dispatch, MGED redraw/drawn-count paths, and Tk swrast
  repaint requests, leaving direct BSG refresh-state calls contained to the
  adapter/test implementation plus libbsg internals.  Faceplate center-dot,
  scale overlay, and params scalar state now route through RT legacy adapters,
  with direct BSG calls contained to the adapter/test implementation plus
  libbsg internals.  QGED view settings now uses neutral
  `rt_view_adc_state`, `rt_view_grid_state`, `rt_view_axes_state`,
  `rt_view_other_state`, and `rt_view_params_state` records for its top-level
  faceplate state, while qtcad Obol faceplate sync and qtcad keybinding
  toggles use neutral ADC/grid/axes records for the state they publish.
  TclCAD model-axes and view-axes commands now use neutral
  `rt_view_axes_state` records for their top-level axes state updates, and
  TclCAD command grid-snap decisions read grid state through that boundary
  as well.  The libged ADC command now snapshots and commits ADC state through
  the same adapter boundary, standalone libged grid and faceplate grid
  commands now use neutral `rt_view_grid_state` records, and libged faceplate
  model-axes/view-axes commands now use neutral `rt_view_axes_state` records
  with conversion confined to the RT legacy view adapter.  MGED ADC/grid
  display-state helpers and startup defaults now use
  the same adapter boundary, leaving direct top-level faceplate state calls
  contained to the adapter implementation and tests.  qtcad AET keybinding/
  canvas updates, libged grid vsnap recentering, and MGED `new_mats` derived
  view refresh now route through `rt_view_update_bsg`.  libged view update
  commands for zoom, size, scale, center, eye, eye_pos, AET/YPR/quaternion/
  rotation setters, perspective/projection, lookat, align, rectangle zoom, and
  vutil transformations now use the same derived-view refresh adapter.  libged
  preview/loadview camera restoration and QGED polygon alignment now use that
  adapter as well, while mirror post-draw refresh uses the opaque
  `rt_view_context_update_bsg` wrapper.  GED autoview, qtcad quad-view
  default framing, and GED draw transaction autoview now route
  through RT/GED private autoview adapters instead of direct BSG utility
  calls.  qtcad mouse and wheel navigation now route BSG-backed adjust
  math through RT view-adjust adapter flags rather than calling
  `bsg_adjust` or naming BSG adjust-mode constants directly.  qtcad canvas
  view-hash tracking now uses `rt_view_hash_bsg` instead of calling
  `bsg_hash` directly, and gsh display-dirty view hashing now uses the same
  RT view-hash adapter.  libged/MGED independent-view checks, independent
  scope lookup, zap clearing, and MGED view init-copy now use RT or GED-private
  legacy adapters instead of direct BSG utility entry points.  TclCAD 2D
  snapping, GED view snap candidate queries, and GED grid-center snapping now
  route through RT snap adapters instead of direct BSG snap utility calls.  GED,
  MGED, qtcad, and GED draw-record selection storage lookups now route through
  RT or GED-private selection adapters instead of calling `bsg_view_selection`
  directly.  MGED highlighted-shape selection mirroring now uses a GED draw
  helper instead of constructing BSG interaction records in MGED.
  TclCAD, GED, gsh, and retained GED draw view enumeration and view-name
  lookup now use RT or GED-private view-set adapters instead of calling
  `bsg_set_views` or `bsg_set_find_view` directly.  TclCAD edit-redraw DB
  object iteration now uses a GED draw callback helper instead of direct BSG
  export requests, and MGED area-plot DB object checks plus segment iteration
  now use the same GED draw callback boundary.  Libged view vZ DB-object
  segment iteration now uses that boundary as well.  Libged plot, PS, and PNG
  diagnostic export loops now consume visible records and segments through the
  same GED draw facade instead of direct BSG export requests.  Libged
  labelvert, labelface, and select visible DB-object scans now also use that
  facade for record, segment, and point iteration instead of direct BSG export
  requests, and libged RT-write full-path flag/animation scans plus BoT dump
  visible DB-object traversal now use the same boundary.  The `who solids`
  detailed visible DB-object report path now also uses the facade, including
  draw-mode filtering, highlighted state, vlist counts, and geometry-report
  forwarding.  Libged view-object list/find/info export scans now use a
  general GED draw view-record query facade for view-object, DB-object,
  local-only, and glob-filtered queries instead of direct BSG export requests.
  GED, MGED, qtcad, TclCAD,
  and libdm BSG-backed view initialization, free, and view-set membership
  updates now route through RT lifecycle adapters.  GED and MGED view knob
  reset/hash/command-process/transform/rate-flag utility calls now route
  through RT legacy view adapters, and retained GED draw root recycle-pool
  lookup now uses a GED-private adapter instead of calling `bsg_set_fsos`
  directly.  Libged retained draw debug logging now goes through
  `ged_draw_log`, which lets the broad `ged_private.h` header avoid
  `bsg/util.h`; the helper is self-contained, disabled by default like the
  old BSG logging path, and split GED draw bridge files no longer include the
  broad utility header.
  TclCAD view creation now initializes its transitional view-data block
  through a libtclcad-private helper, which now owns the TclCAD record
  defaults locally while retaining only the RT context binding adapter; public
  `tclcad/draw.h` no longer exposes the concrete TclCAD
  view-data layout or BSG Tcl data payload.
  qtcad, TclCAD, MGED, and Tk swrast refresh requests now use RT legacy
  refresh-flag aliases instead of naming `BSG_VIEW_REFRESH_*` constants
  directly; direct BSG refresh flags are left only in libbsg and adapter
  parity tests.
  qtcad snap refinement, qged polygon snap toggles, and the libged snap
  command now also use RT legacy snap source/kind aliases instead of naming
  `BSG_SNAP_*` constants directly.  GED zap clear-scope flags now use RT
  legacy clear aliases instead of naming `BSG_SOURCE_*` constants directly.
	  qtcad Obol snap and faceplate-sync tests now route setup view-state through
	  opaque qtcad legacy-view helpers instead of direct BSG constants/getters/
	  setters or direct RT legacy-BSG view calls.
	  QgSW offscreen integration test view-set/unit-conversion setup now routes
	  through qtcad legacy-view helpers, and qsketch view lifecycle,
	  derived-update, fit-to-view, and grid setup paths now use the same opaque
	  qtcad helper surface instead of direct BSG view utility calls.
	  GED multi-view draw, view-command, view-independent, rtwizard, shared
	  draw utility, and qtcad DM-backend benchmark test setup now route
	  view-set lifecycle, dimensions, and unit-conversion writes through the
	  same RT legacy view adapters.
	  libdm swrast/Tk swrast view-dimension synchronization, qtcad quad-view
	  unit copying, and libdm backend draw-item test dimensions now also use
	  RT legacy view adapters instead of direct BSG view field access.
	  qged current-view unit refresh and GED open/knob database-unit setup now
	  also route through RT legacy view adapters instead of direct BSG unit
	  field writes, with pivot-guard checks covering those production setup
	  paths.
	  Remaining GED draw-test dimension/unit setup and scale-pointer handoff now
	  also uses RT legacy view adapters, and the pivot guard rejects direct
	  setup writes or `&v->gv_scale` in those tests.  The measure semantics
	  test now links `librt` for the RT adapter symbol on FreeBSD/clang.
	  The LoD drawing test now uses its existing `-U` unstable-test switch for
	  the build-config-sensitive BRep LoD pixel baseline; default CTest still
	  verifies that BRep LoD renders non-empty geometry.
	  Public `dm.h` no longer includes the BSG umbrella header; it forward declares
  the remaining pointer-only transitional BSG draw-state types, and libged
  selection handling now includes its concrete BSG selection dependency
  directly.  The public BSG framebuffer payload member is now named `fb`
  instead of `fbp`, avoiding MGED's legacy `fbp` macro in FreeBSD/clang unity
  builds.  The GED BSG-backed view adapter declarations now live in a narrow
  `bsg_ged_draw_view_private.h` header, so LoD commands and view-independent
  draw tests no longer include the full retained draw-scene private bridge.
  Public `dm/view.h` now keeps `struct bsg_view` opaque instead of including
  `bsg/defines.h` solely for draw-entry pointer declarations.
  Top-level public `ged.h` no longer directly includes BSG headers; the
  remaining public GED BSG dependency is isolated to `ged/defines.h` while
  `struct ged` still embeds transitional `bsg_view_set` storage.  The swrast
  private header now forward declares its retained `struct bsg_view` context
  pointer instead of pulling in the broad BSG umbrella, and `dm-gl.h` no longer
  includes `bsg/defines.h` directly beyond the retained vlist boundary.
  qtcad/qged-facing polygon, sketch, view-info, and polygon-settings headers
  no longer include the broad BSG umbrella; retained transitional polygon/view
  declarations now use narrow BSG headers or pointer-only forward declarations.
  qtcad canvas, quad-view, selection, measurement, polygon, and shared
  view-filter implementation files now also use narrow BSG headers instead of
  the umbrella.  libged grid, view object, label, line, polygon, axes, and
  faceplate command files now also include only their needed BSG subsystem
  headers instead of the umbrella.  The remaining production broad BSG
  umbrella includes in gsh, libged utility code, and TclCAD mouse/polygon
  handlers were likewise replaced with narrower subsystem headers.  The
  matching GED/qtcad/librt test fixtures now also include specific BSG
  subsystem headers instead of the umbrella.  libged polygon clipping and zoom
  command code now also avoid `bsg/util.h`; their retained view reads stay on
  the RT legacy adapter boundary.  qtcad sketch editing also avoids
  `bsg/util.h` and reads Qt event coordinates directly instead of reaching
  back into `bsg_view` mouse fields.  qtcad 2-D/3-D measurement filters now
  capture Qt event coordinates in the filter state and use those for
  screen-to-view and Obol pick queries instead of reading BSG mouse fields.
  The measurement overlay clear helper was also made view-implicit, so
  `QgMeasureFilter.h` no longer exposes `bsg_view` for that private path; the
  retained staged BSG view access remains confined to `QgMeasureFilter.cpp`.
  qtcad point, box, and ray selection filters likewise use Qt event
  coordinates for Obol and retained BSG fallback pick queries, and shared
  qtcad view-sync now pushes legacy mouse/current-point state through the RT
  adapter boundary instead of writing BSG fields directly.  qtcad polygon create,
  update, select, point-edit, and move filters now pass Qt event
  coordinates/points into the retained BSG polygon API instead of reading BSG
  mouse/current/previous point fields directly.  qtcad view updates and qged
  view-change dispatch no longer keep `bsg/util.h` solely for disabled
  `bsg_log` tracing; local qtcad slot tracing remains.  qged polygon
  settings now performs retained view-object unique-name checks through the RT
  legacy adapter boundary instead of including `bsg/util.h` directly.
  qged plugin context no longer exposes a public BSG active-view accessor;
  plugins that still need staged compatibility view pointers now derive them
  from `getViewWidget()` inside implementation-local helpers, retaining the
  existing `QgModel`/`QgSession` startup fallback.
  qtcad view filters now use the same widget boundary: `QgViewFilter.h` exposes
  only `set_view_widget`/`view_widget`, while selection, measurement, sketch,
  polygon, and qged plugin filter implementations resolve retained BSG view
  pointers locally from the widget.
  The stale public qtcad model/sketch header BSG spellings were also removed:
  `QgModel.h` no longer forward declares `bsg_view`, and `QgSketchFilter.h`
  describes installation through `QgView` without telling callers to obtain the
  retained pointer.
  GED view-object mouse/current-point updates and align command view2model
  writeback now also use the RT legacy view adapter boundary, while qtcad
  snap-test current-point checks use the opaque qtcad legacy-view helper
  backed by that boundary.
  TclCAD refresh unit stashing, TclCAD command snap setup, and MGED knob
  view-mode unit synchronization now set legacy view unit factors through the
  same adapter boundary instead of writing BSG view fields directly.
  RT now exposes a narrow previous-mouse adapter, and TclCAD polygon-mode and
  command seed points use it instead of reading or writing previous mouse
  fields directly.
  TclCAD mouse simple previous-mouse readback, BREP/joint selection drag start
  points, polygon point seeds, and shared mouse-scale updates now use that
  previous-mouse adapter, and the remaining TclCAD mouse delta paths now
  snapshot/update previous mouse through a local adapter helper instead of
  reading or writing previous mouse fields directly.  TclCAD mouse delta
  clamp and rotate/scale factors now use a grouped RT adapter instead of
  reading BSG mouse delta fields directly.
  libdm normalized view clipping, ADC drawing, label placement, GL z-clipping,
  framebuffer canvas dimension setup, display-manager default clip bounds,
  and internal pixel/view conversion macros now use RT legacy view adapters or
  `RT_VIEW_MIN/MAX`, `RT_VIEW_RANGE`, `RT_INV_VIEW`, and `RT_INV_4096` from
  `rt/view.h` instead of naming BSG view-state details directly; libdm scene
  drawing no longer keeps `bsg/util.h` solely for disabled `bsg_log` tracing.
  A broad
  direct-production BSG view-field scan across libged, libtclcad, MGED, qged,
  and qtcad is now clean for the tracked assignment/matrix-copy patterns.  The
  former `src/libged/edit/edit.cpp`
  `rt_edit_view` initializer hit now relies on neutral `rt_edit_view_init`
  defaults rather than a direct field write or the BSG adapter boundary.
  savekey
  now uses the same RT legacy adapter surface for RT -M size, rotation, and
  view2model values instead of reading the transitional BSG view fields
  directly.  The first TclCAD and MGED callers now
  follow the same boundary: libtclcad display-bound setup, libtclcad axes
  size scaling, and MGED ADC view-unit math, shared MGED GED2PM1 conversion,
  MGED axes overlay scaling,
  MGED attach window bounds, and MGED view-scale clamping include rt/view.h
  for RT_VIEW_*, RT_INV_VIEW, and RT_VIEW_MIN_SIZE.  MGED menu and faceplate
  drawing plus MGED slider/pen normalized-coordinate handling now use the same
  RT-owned min/max/range/inverse constants.  MGED event and generic display
  manager mouse/ADC paths now use the same RT-owned numeric constants while
  keeping their transitional display/view ownership unchanged.  qtcad quad
  views, TclCAD commands/wrapper paths, and MGED zoom/idle/attach paths now
  copy, inspect, or apply transitional BSG LoD policy through
  rt/view_legacy_bsg.h instead of naming BSG LoD policy structs directly.  The
  pivot
  guard now scans src/mged for the retired BSG numeric aliases so new MGED
  code must use RT-owned view numeric policy.
  src/librt/tests/view_info.c covers the neutral defaults, RT view metric
  helpers, and mesh-LoD helper surface without including BSG headers;
  src/librt/tests/view_legacy_bsg.c covers the transitional BSG view/LoD
  policy mapping.
* include/rt/view.h exposes mesh LoD cache lookups through an opaque
  rt_mesh_lod handle instead of returning bsg_mesh_lod directly.  The current
  cache implementation remains BSG-backed internally, but RT now exposes
  neutral rt_mesh_lod_load_level, rt_mesh_lod_load_view, and
  rt_mesh_lod_data_get helpers for loading and inspecting cached mesh data
  without exposing the BSG container.  rt_mesh_lod_current_level reports the
  active level without changing residency, and rt_mesh_lod_memshrink releases
  active CPU-side POP/full-detail arrays while preserving the current level so
  callers can reload on demand.  rt_mesh_lod_info_get provides a copied
  RT-owned active-level, count, bounds, snapped-point, and normal summary for
  staged realization decisions that should not borrow provider arrays.
  db_mesh_lod_status, db_mesh_lod_refresh, and db_mesh_lod_invalidate expose
  neutral cache-key, cached-payload, stale-entry, generated, and cleared-entry
  reporting for staged realization and invalidation logic; libged database
  index invalidation now uses the RT-owned invalidation helper rather than
  direct BSG mesh-LoD key/cache mutation.  db_mesh_lod_clear handles
  per-database cache clearing and rt_mesh_lod_cache_clear_all owns the explicit
  global cache-clear operation, so libged view lod cache-clear commands no
  longer call BSG cache clearing directly.  The same view lod cache command
  now refreshes BoT cache entries through db_mesh_lod_refresh and checks cache
  presence through db_mesh_lod_status.  The BSG-backed draw.cpp BoT mesh LoD
  realization path now uses db_mesh_lod_status/refresh/get for cache
  ownership, stores the RT-owned rt_mesh_lod handle in the draw source state,
  publishes active mesh geometry through the copied RT mesh-LoD data view, and
  passes that RT handle to private GED adapters that own the remaining
  transitional BSG scene-ref visibility/load unwrapping.  db_mesh_lod_store_mesh now
  stores generated mesh payloads through the same RT cache facade, so libged
  view lod and draw.cpp BRep paths no longer write BSG mesh-LoD cache keys directly;
  BRep tessellation itself remains a transitional libged-side producer until
  RT owns BRep mesh-LoD generation.
  Legacy callers no longer get an installed BSG mesh-LoD extraction helper;
  BSG unwrapping stays private to librt implementation units, and transitional
  BSG scene-ref loading is exposed only through narrow RT legacy helper calls.
  librt private database state owns an opaque rt_mesh_lod_context rather than
  bsg_mesh_lod_context; the RT cache path self-initializes that private context
  and only the cache implementation unwraps the current BSG backing context.
  libged draw_data_t and ged_draw_source_state no longer carry the old
  bsg_mesh_lod_context pass-through, and ged_draw_source_state no longer stores
  a borrowed bsg_mesh_lod pointer; draw realization asks RT cache helpers
  for LoD state when it needs it.  Public struct ged no longer exposes or
  manually creates/destroys a BSG mesh-LoD context; GED draw tests invalidate
  LoD cache entries through the RT cache facade.  BSG draw helper units that
  do not own transitional mesh-LoD payload operations no longer include
  bsg/lod.h, and draw.cpp no longer includes bsg/lod.h directly.  The private
  libged BSG-to-RT view helper no longer includes bsg/lod.h or names direct
  BSG bounds/scene-ref LoD APIs.  BRep full-detail callback registration now
  uses RT mesh-LoD detail callbacks, so src/libged no longer includes
  bsg/lod.h for LoD publication.
  src/librt/tests/lod.c now validates cached mesh-LoD levels through the
  RT-owned helpers instead of creating BSG views, mesh nodes, or LoD contexts.
* The former POP, LoD pipeline, LoD wakeup notes, and cache_drawing.cpp
  define the LoD rework direction for Obol/libbrlobol.  LoD realization
  should be a provider-neutral staged service below Obol, not a BSG LoD node
  port.  Obol/libbrlobol should
  request immutable realized geometry for database path, database identity,
  source revision or content hash, view state, draw mode, LoD policy, provider
  id/version, and provider parameters, then apply staged results on the
  UI/main thread only when source/view/policy generation tokens still match.
  The staged result sequence should be: metadata/hierarchy, attributes plus
  AABB, OBB or other cheap proxy, fast display LoD such as POP Buffers,
  qualified high-quality/progressive mesh LoD for clean meshes, and
  exact/full-detail fallback for editing, exact picking, export, or
  still-frame refinement.
  The old cache_drawing.cpp experiment contributes useful production concepts:
  fast-to-slow preparation, main-thread result draining, one serialized
  bu_cache/LMDB write lane, debug delay knobs for observing intermediate
  states, explicit AABB/OBB/LoD milestones, and queue-based handoff between
  independent workers.  Do not carry forward its BSG-specific API,
  object-name-only cache keys, detached unmanaged workers, void* lifetime
  pattern, fixed linear pipeline assumption, or rt_db_internal ownership by
  comment.  Use an owned task service with explicit start/stop, joinable
  shutdown, cancellation tokens, generation ids, explicit in-flight counts,
  task-graph dependencies, and clear final ownership of cracked internals.
  concurrentqueue.h may be used for multi-producer/multi-consumer stage and
  result handoff, but size_approx() must remain diagnostic, idle-wait, or
  settled-check only.  Correctness must come from enqueue/dequeue success,
  shutdown/cancellation tokens, generation ids, and in-flight counters.
  Result records should carry object path/name, source revision, view/policy
  revision, result kind, quality tier, provider id, cache key or geometry
  handle, bounds, primitive counts, estimated error when known,
  terminal/fallback status, and diagnostics.
  The LoD wakeup design adds the required wakeup contract: BRLObolLodService
  should emit a lightweight, coalesced result-ready callback when the result
  queue transitions from empty to non-empty; callbacks must not mutate Obol
  nodes or deliver results; the owning controller sets a pending flag, wakes
  or is observed by the main loop, and drains/applies results on the UI/main
  thread with bounded-drain support.  This uses the existing mutex/condition
  service queues for now; concurrentqueue.h remains an option for later
  high-volume inter-stage/result handoff once the public contract is stable.
  The concrete Obol/libbrlobol build-out should start by closing the neutral
  RT facade gaps that libbrlobol will need: current active level/status,
  active-data residency status, memory-shrink/reload hooks, cache
  invalidation or stale-entry status, and copied metadata that avoids
  borrowing provider arrays.  The initial
  provider-neutral BRLObolLodRequest, BRLObolLodResult, cache-key, and
  geometry-handle contract is in place, including deterministic cache keys
  over database/source/view/policy/provider identity, bounds, counts, and
  provider parameters.  The initial owned BRLObolLodService now provides
  start/stop, joinable workers, generation cancellation, task dependencies,
  main-thread result draining, in-flight diagnostics, cancellable task-level
  debug delay controls, stale-result rejection, owned realization-task data
  cleanup, a coalesced result-ready subscription/wakeup, and a serialized
  cache-write lane.  Typed Stage 0/1/2 result payloads and
  constructors now cover dependency/directory records, display attributes,
  AABB, and proxy/OBB milestones.  The first RT mesh-cache provider adapter
  can run as a service task, refresh missing cache entries, load the active
  RT mesh-LoD view level, copy the active RT mesh-LoD arrays into an owned
  staged mesh payload, and publish the neutral mesh result without exposing
  BSG.  Next wire POP/provider payload ownership and a dedicated LoD-backed
  Obol node/update pass while the existing direct mesh/vlist realization
  remains available for small and synchronous cases.  The first
  SoBRLLodUpdateAction already handles UI-thread result drains and applies
  staged metadata/attribute/proxy records plus owned mesh payloads to matching
  SoBRLMeshShape nodes.  Mesh nodes can now build deterministic staged LoD
  requests from their Obol identity fields, and the view controller can expose
  a sanitized rt_view_info snapshot.  The first explicit
  SoBRLMeshLodSubmitAction uses those pieces to submit RT mesh-cache provider
  tasks for matching mesh nodes, and BRLObolViewController now has explicit
  submit/drain/apply methods plus a retained LoD service subscription and
  pending-result processing path that wire the action pair to the active
  scene, view snapshot, service, and database source list.  It also has the
  first auto-submit gate, keyed by view, policy, and source signature, for
  render-time LoD request preparation; that source signature includes
  lodBotThreshold policy input.  The remaining controller/render integration
  work is to make scheduling richer and provider/policy-aware.
  Large BoTs
  should realize through a dedicated
  LoD-backed mesh node, preferably SoBRLLodMeshShape, or a clearly separated
  LoD-backed mode on SoBRLMeshShape.  That node should store persistent
  source/material/selection/LoD policy fields in Obol while keeping large
  active POP arrays private, reloadable, and governed by RT-owned
  rt_mesh_lod handles.  A narrow SoBRLMeshLodUpdateAction or extension of the
  current action/controller pass should build rt_view_info from the Obol camera
  and viewport, call the
  neutral rt_mesh_lod_load_view/data APIs, update backend/private geometry
  only when the active level changes or a reload is requested, and mark the
  node dirty through normal Obol notification.  Actions must distinguish
  display-geometry behavior from exact/model behavior: viewport pick may use
  active display LoD, while precision measure, edit snap, and default export
  need an explicit full-detail or detail-preserving policy.  Export, measure,
  and snap now expose that policy split; full-detail reloads and pick policy
  wiring remain.
* include/rt/primitives/sketch.h exposes sketch-to-polygon and
  polygon-to-sketch conversion through an opaque rt_sketch_polygon handle
  instead of bsg_polygon_ref.  The current BSG view-polygon workflow opts into
  include/rt/primitives/sketch_legacy_bsg.h for the old
  db_sketch_to_view_polygon* and db_view_polygon_ref_to_sketch adapters.
  src/librt/primitives/sketch/polygons.c now stores the opaque handle in
  RT-owned polygon state and is free of BSG headers; BSG polygon-ref
  translation is limited to
  src/librt/primitives/sketch/polygons_legacy_bsg.c.
* src/libnmg now includes bg/vlist.h directly, uses bg_vlist/bg_vlblock and
  BG_VLIST_* helper APIs for NMG plot/debug output, and no longer links
  libbsg directly.  The pivot guard prevents these NMG files from regressing
  to BSG vlist names.
* libanalyze's NIRT segment plotting now includes bg/vlist.h directly and uses
  BG vlist/vlblock helpers.  libanalyze records a direct libbg dependency for
  that use, and the pivot guard prevents NIRT from regressing to BSG vlist
  names.
* dxf-g, IGES drawing import, g-jack, g-off, ganalyze, and tea_nmg use
  bg/vlist.h directly for their low-level vlist/uplot work instead of
  bsg/vlist.h compatibility aliases.  The pivot guard prevents those utility
  paths from regressing to BSG vlist names.
* src/libbrep uses BG-owned vlist/plot helpers for debug plot output and no
  longer records a direct libbsg dependency.  Any remaining BREP BSG
  publication is in GED/librt transitional scene paths, not libbrep.  The
  pivot guard prevents src/libbrep and include/brep from regressing to BSG
  names.
* include/bg/line_layer.h and src/libbg/line_layer.c own the neutral
  color-keyed line-layer builder and the simple bg_line_layer-to-plot3 stream
  export helpers used for legacy diagnostic output.
* BREP, NMG, GQA, and check diagnostic paths now build bg_line_layer data and
  cross back into BSG only at the temporary publication boundary.
* include/bg/plot3.h is the canonical installed plot3 header.
  include/bsg/plot3.h is a transitional wrapper.
* Standalone plot3 utilities now link through libbg/libbu as appropriate,
  not libbsg.  plot3-dm remains a legacy display/Tk utility path.
* The old BSG TIG and vectfont headers are no longer part of the public BSG
  header set after the newdraw rebase.
* The default plot3_* implementation now lives in a normal libbg
  implementation unit.  The PLOT3_IMPLEMENTATION header block remains only
  for private prefixed local implementations; plot3.h is expected to remain a
  useful debugging/export interface.


What We Should Not Rebuild
--------------------------

Do not recreate BSG inside libbrlobol.

The earlier BSG direction was useful because it separated application drawing
state from individual display backends.  Obol already provides the stronger
version of that model: a real scene graph, traversal/actions, details,
render managers, text, cameras, and pluggable backends.  The cleaner long
term design is to put BRL-CAD-specific behavior into Obol extensions and let
applications use Obol features directly.

libbrlobol may provide narrow controllers and services where BRL-CAD needs
coordination, such as database realization, diagnostics, edit preview, and
application render scheduling.  Those services should hold Obol objects and
BRL-CAD policy, not generic render lists, payload stores, or backend-neutral
scene abstractions parallel to Obol.


Target Public API Shape
-----------------------

libbrlobol:

* brlobol_init initializes Obol and BRL-CAD Obol classes.
* Database source nodes own BRL-CAD database/path realization state as
  normal Obol node fields.
* BRLObolViewController coordinates scene root, camera, SoViewport,
  SoRenderManager, render scheduling, readback, realization, and diagnostics.
* Realization, export, snap, measure, pick, diagnostics, edit preview, HUD,
  overlays, and text are expressed as Obol nodes/actions/details.
* SoBRLSceneController remains a narrow realization/diagnostic service.
* No public bsg_render_item, bsg_backend_scene, generic payload store, or
  BSG-like render list appears in libbrlobol.

qtcad/qged:

* Migrated code uses BRLObolViewController and Obol scene/camera objects.
* Existing bsg_view, dm, and fb accessors remain only during parallel
  migration.
* Final public drawing APIs do not expose bsg_view, dm, fb, or dm_plugins.
* vlist is not exposed as an application scene abstraction.  Wire output is
  converted to Obol line/shape nodes at the scene boundary.

GED/TclCAD/mged:

* GED drawing services update Obol database source nodes, Obol camera/view
  state, and Obol selection/pick details.
* C/Tcl callers use an opaque bridge backed by Obol where needed.
* No public BSG/DM compatibility shim survives final removal.

Image streams, framebuffer compatibility, and window hosting:

* libimgstream provides opaque imgstream_* stream lifecycle, metadata, pixel
  read/write, rect update, snapshot, subscriber, producer, and transport APIs.
* libimgstream/fb_compat preserves the current framebuffer API/protocol
  spellings needed by rt, remrt, fbserv, GED/TclCAD, and src/fb tools while
  translating them to image-stream storage, transport, or explicit
  unsupported diagnostics.
* SoBRLImageSource represents static icv_image_t data and live libimgstream
  streams as Obol data sources.
* SoBRLViewportImage displays an image source in viewport-aligned underlay,
  overlay, HUD, render-progress, and framebuffer-compatibility roles.
* SoBRLImagePlane displays an image source as a textured scene-space plane.
* libbrlobol owns the toolkit-neutral BRLObolWindowHost contract around
  BRLObolViewController, normalized input events, semantic input profiles,
  and display-host callbacks used by display-capable fb_open and dm_open
  compatibility.  The neutral contract and initial imgstream display-host
  bridge are in place; display-capable framebuffer streams can attach to
  SoBRLImageSource and SoBRLViewportImage nodes under the controller.
  BRLObolHeadlessWindowHost provides the first offscreen adapter and drains
  those streams through Obol readback; qtcad's QgObolWindowHost provides the
  first Qt canvas adapter for the same display-host contract and can create an
  owned visible Qt toplevel QgSW fallback when no qtcad canvas exists.  General
  dm_open routing, final Qt host selection, and Tk/MGED adapters remain.
* libimgstream must not depend on libbrlobol.  Display-capable framebuffer
  compatibility obtains or creates an image stream, then delegates window or
  viewport display through a registered libbrlobol/application display host.


Progress Snapshot
-----------------

These are approximate completion estimates for the current checked-in path.
They measure exit-criteria completion, not implementation effort spent.

Next Push:

* Obol realization coverage matrix: 98 percent.  The matrix and required
  pinewood.g, havoc.g, and m35.g gates exist, and Generic_Twin.g is documented
  as the larger FAA maturity candidate with an opt-in
  BRLOBOL_QTCAD_GENERIC_TWIN=1 draw/readback hook for the final mixed
  implicit/BoT gate; standalone edge entity picking, standalone vertex entity
  picking, and broader live edit-tool lifecycle semantics remain.  Mesh face
  picks and triangle export records now preserve source vertex indices for
  face-level inspection/export, and face pick details record
  nearest edge and vertex identity hints.  Preview-level edit-intent identity
  can now be distinct from preview-node identity and propagates through the
  action/export paths.  PNTS point primitives now have shaded Obol realization
  coverage for all RT point-record layouts.  The required pinewood.g/havoc.g
  prototype hierarchy checks now use export and measure action summary counts
  instead of recursive random-access `getRealizedShape/Mesh(i)` counting, and
  the count-only wire hierarchy measure now opts out of connected-angle
  searching while preserving the default measurement behavior elsewhere.
  Default wire measurement now enumerates connected-angle candidates with
  endpoint buckets, so sparse line sets avoid the previous all-pairs segment
  scan while keeping the same shared-vertex tolerance and nearest-angle
  selection path.  The remaining runtime was havoc.g's shaded realization,
  which duplicated shaded hierarchy coverage already retained by pinewood.g;
  havoc.g now stays as the larger wire hierarchy traversal.  This reduces the
  FreeBSD/clang prototype test from roughly 107 seconds to roughly 1.4 seconds
  without weakening the hierarchy coverage.  A later timing pass found the
  remaining large hierarchy measurement cost in SoBRLMeasureAction's indexed
  vlist access pattern: getSegment(i) rescanned the SoBRLVListShape command
  stream for every segment.  Measurement and snap action traversal now walk
  the command stream once, keeping the same segment ordinals, point primitive
  indices, filters, transforms, bounds, nearest results, and angle behavior.
  On this FreeBSD/clang build, pinewood.g wire measure fell from about 0.279
  seconds to about 0.003 seconds, havoc.g wire measure fell from about 0.159
  seconds to about 0.049 seconds, and libbrlobol_prototype CTest fell from
  about 1.44 seconds to about 0.96 seconds.
* libbrlobol database realization: 98 percent.  Real hierarchy realization,
  partial refresh, identity fields, mesh/vlist representation, and tests are
  in place; mesh face pick/export details carry source vertex indices and face
  picks expose nearest edge/vertex identity hints; BoT meshes can publish
  cached RT LoD metadata into Obol fields and export records through the
  initial provider-neutral LoD result contract, and the first async
  BRLObolLodService skeleton plus typed Stage 0/1/2 dependency, attribute,
  AABB, and proxy payloads are available for staged work.  The first RT
  mesh-cache provider task can refresh/load cached RT mesh LoD through the
  async service, force a requested RT LoD level, shrink active RT arrays after
  copying, and return a neutral staged mesh result with an owned Obol-side
  triangle payload; explicit database invalidation is now covered at the
  provider boundary, with no-refresh providers surfacing cache misses and
  refresh-enabled providers rebuilding the RT cache before returning an Obol
  payload.  SoBRLMeshShape can
  now consume staged results into Obol fields and reject stale, cancelled,
  error, and cache-miss result statuses, and SoBRLLodUpdateAction can drain
  service results and apply accepted staged records and owned mesh payloads to
  matching scene nodes;
  request/view helper boundaries now exist, and the first explicit
  SoBRLMeshLodSubmitAction can use them to submit RT mesh-cache provider work
  from an Obol scene and view snapshot; BRLObolViewController now exposes
  explicit submit/drain/apply LoD methods with counters and diagnostics for
  application/controller integration, and the service/controller wakeup path
  can mark queued results pending, process them on the main thread, and
  request a render after applied staged results; the controller can now
  coalesce automatic LoD submissions by view/policy/source signature so
  render-time preparation avoids duplicate requests for an unchanged scene and
  lodBotThreshold policy changes are part of that source signature; it also
  tracks a sanitized RT view signature so direct camera field changes can
  trigger a fresh LoD submission without replacing the camera node, and
  forced RT LoD levels can be requested through both SoBRLMeshLodSubmitAction
  and BRLObolViewController as an explicit policy input;
  SoBRLLodMeshShape now provides the first dedicated LoD-backed BoT node
  identity, BoT realization uses it only when lodBotThreshold is positive and
  the BoT face count meets/exceeds the threshold while currently loading
  full-field mesh data for first realization, and LoD submission skips
  ordinary meshes unless explicitly configured otherwise; dedicated
  LoD-backed mesh nodes now disable full-detail backup when display-LoD
  payloads replace active fields, while preserving lightweight source
  counts/bounds for request identity.  Ordinary meshes can still preserve
  full-detail indexed triangles as an interim resident-mesh fallback.  Mesh
  nodes can now evict both preserved full-detail arrays and active
  display-LoD payloads while retaining source-backed request identity, source
  counts, bounds, exact-operation provider routing, and bbox contribution.
  SoBRLMeshResidencyAction and BRLObolViewController's explicit and optional
  controller-owned mesh-budget paths can now enforce a resident mesh byte
  budget across a scene by evicting preserved full detail first and
  source-backed active display payloads second, including automatic
  enforcement after LoD results are applied.  Measure, snap, and pick exact
  source requests now carry source-local bounded-query metadata into LoD
  provider params, so the partial-read provider can distinguish whole-object
  exact reloads from nearest-point, bbox, or ray-scoped source queries.  The
  direct BoT provider now uses snap bounds/tolerance and exact-pick rays to
  reduce returned face payloads, compact subset vertices, preserve source
  face/vertex ids, and honor face/point budgets after source-local scoped
  snap/pick reduction without broadening scoped misses to whole-object
  payloads or trusting mixed/ambiguous/malformed/non-source-local query
  metadata while keeping measure hint requests whole-object for correctness.
  The production large-BoT path still needs source-backed full-detail access
  that can fetch bbox/intersection-targeted subsets or chunk very large meshes
  instead of requiring a full resident copy.  A candidate
  exact-query optimization is a cached coarse spatial/index sidecar emitted
  while the POP build is already walking source triangles: assign triangles to
  normalized Morton/grid leaves, union real triangle bboxes into leaf bounds,
  and store source face ids or ranges for provider subset queries.  Keep it
  optional and coarse; it becomes an out-of-core win only when paired with a
  librt BoT partial-read or cached full-detail chunk API.  Export and
  measure now have explicit full-detail versus display-LoD policies that use
  preserved full-detail source geometry when available, snap defaults to
  display-level behavior with an explicit full-detail detail-preserving mode
  that can also use preserved source geometry, and mesh ray picking has an
  explicit display/default versus preserved-full-detail policy split;
  libbrlobol has the first reusable BRLObolRtPickCache for exact CSG implicit
  and comb-instance hits, preparing explicit database object paths once and
  shooting repeated rays through that RT instance while the existing one-shot
  helper remains as a wrapper.  It returns path/source identity,
  region/material data, hit point, normal, and distance.  BRLObolViewController
  owns the interactive cache lifetime/invalidation path and now exposes both
  a nonblocking source-backed mesh exact ray helper and a cache-backed exact
  RT ray helper for non-qtcad callers; qtcad point, explicit ray, and
  rectangle picks delegate to those shared helpers before applying their
  display/source duplicate suppression.  Broader non-qtcad exact-pick tool
  integration remains.
  qtcad rectangle picks already reuse that cache across sampled
  implicit/comb exact-pick rays; PNTS shaded point
  realization/export/snap is covered, including all RT point-record layouts
  with optional color, scale, and normal attributes on the fallback point
  shape plus global-scale fallback, per-point scale precedence, and
  transform-aware attribute export; sketch and annotation shaded realization
  now use plot-backed vlist geometry with pick/snap/export/measure coverage
  for curve-like non-surface primitives; standalone material database objects
  now realize as no-draw SoBRLMaterialObject metadata nodes;
  native text/curve/material integration refinements, standalone edge and
  vertex entity picking, POP/provider payload ownership, richer provider-level
  active-level policy scheduling, and more shaded converter hardening remain.
  Controller-owned database-source replacement now configures watched source
  fields atomically so realization is not invalidated by stale delayed field
  sensors immediately after qtcad sync.
* Obol diagnostic overlays: 45 percent.  qtcad Obol line-layer/HUD overlay
  sync exists; GQA/check/BREP/NMG publication still needs broader Obol-first
  routing.
* qtcad visible Obol rendering: 64 percent.  Obol readback, GL/SW capture,
  navigation, selection, snap, and measure gates exist for migrated content;
  qtcad Obol camera sync, snap tolerance, input scaling, sketch transform, and
  selection ray/rectangle view snapshots now use the RT legacy view adapter
  boundary, as do 2D measurement view-to-model conversion, quad-view source
  unit/size copies, and SW/GL fallback framebuffer-size or matrix-load reads;
  quad-view visible-path propagation now uses Obol export identity in
  display-level mode instead of a direct BSG export query; qtcad selection's
  public header now exposes selected paths without naming legacy BSG
  pick-result objects, keeping the remaining BSG pick-result fallback internal;
  qtcad GED draw observer sync and real-model Obol draw/readback workflows now
  keep controller-owned database sources realized after delayed Obol field
  sensors drain; the full qtcad Obol workflow gate passes for the pinewood.g,
  havoc.g, and m35.g baseline.  The Qt display-host adapter can create an
  owned visible toplevel for display-capable framebuffer requests; legacy
  fallback remains for non-migrated paths.
* Guardrails and inventory: 97 percent.  Transitional guard and dependency
  inventory are active and now checks RT-owned view-coordinate constant use in
  the first libged command/helper set, TclCAD/MGED view numeric callers,
  MGED-wide retired numeric alias scans, RT-owned view size/scale limits, and
  qtcad/MGED LoD policy use through the RT legacy adapter.  It also now
  rejects direct BSG LoD bounds, view-scale, and scene-ref load/free calls in
  the private libged BSG-to-RT view helper, rejects direct savekey RT -M BSG
  view reads, rejects direct MGED title/status BSG view-field reads for
  center/size/AET/perspective/scale, rejects direct NIRT snapshot view reads
  and BoT/BREP viewport pick ray reads, rejects direct BREP TikZ AET output,
  rejects direct GED eye-model helper snapshot reads, rejects direct libged
  selection/polyclip/view-snap/view-polygon/vZ coordinate transform reads,
  rejects direct pipe/metaball/rot_point/label transform helper reads, rejects
  direct BoT/ARB nearest-subentity transform reads, rejects direct draw refresh
  view snapshot reads in draw.cpp, rejects direct libged simple conversion
  helper matrix/rotation/scale reads plus
  viewdir, solids_on_ray ray view reads, and view command query output, rejects
  direct ypr/quat/rmat/perspective/eye_pos/width/height query output, rejects
  rtcheck2 overlap glyph scale reads,
  rejects direct libtclcad view draw matrix/perspective reads, wrapper
  perspective/zoom-refresh LoD policy reads, commands LoD policy reads/writes
  and command center/scale reads, axes size-scaling reads, polygon command
  view snapshot reads, and qged view-info size/width/height/AET/center reads,
  plus direct MGED make default center/scale and area view-rotation reads,
  direct MGED slider view-scale reads, and direct libged ADC view scale/matrix
  reads, plus direct libged grid/eye/rectangle-zoom read-side view matrix and
  center/scale reads, direct libged lookat/qvrot read-side view reads, and
  direct MGED axes overlay view reads, direct MGED rectangle zoom view reads,
  direct MGED pipe pick direction reads, direct MGED view-centered transform
  reads, direct MGED initial view-center setup reads, and direct MGED status
  query output view reads, plus direct MGED grid view size/scale/matrix and
  center-snapshot reads, direct MGED ADC view scale/matrix reads, direct MGED
  absolute translation view reads, and direct MGED chgview edit/slew/size read
  snapshots, plus direct MGED generic mouse and X motion/ADC input view reads,
  direct MGED edit matrix/rate-knob scale reads, and direct libged align and
  rotation/slew/translate utility helper view reads, plus direct libged preview
  camera rotation/view-to-model reads, direct MGED saved-view snapshot reads,
  direct MGED RT command eyepoint-correction view-to-model reads, and direct
  MGED view-ring save/autoview ring-scale snapshot reads plus direct MGED
  command autoview ring-scale reads, and direct MGED edsol
  absolute-translation, rotate-about, NMG/ARS/metaball, BoT/NMG edit-pick, and
  solid/object edit mouse-projection view snapshot reads, plus direct MGED
  dozoom perspective, eye-position, and model-to-view read snapshots, and
  direct libged inverse-size/keypoint/rotate-about query plus utility-helper
  reads, plus direct libged AET/eye-position/perspective setter readbacks and
  direct libged knob keypoint-origin plus size/scale/zoom setter readbacks,
  direct libged pure-rotation and MGED view-scale ratio readbacks,
  plus direct libged/TclCAD/MGED default/query coord, MGED edit-knob coord
  save, MGED cmd staging/cache/hash adapter-covered view reads, and direct
  MGED temp-staging/libged-DM fallback width/height reads, plus direct libged
  pmat/pmodel2view query output, TclCAD draw projection-matrix loads, MGED
  staging/cache/hash projection-matrix reads, direct initial/absolute scale
  readbacks in MGED cache/hash and knob query output, and direct
  unit-conversion stash reads in MGED cache and TclCAD refresh paths, plus
  direct MGED VIEWSIZE/VIEWFACTOR macro view-size/scale reads,
  direct qtcad camera/snap/input/sketch/selection read-side view snapshots and
  qtcad measurement, quad-view source-copy, and SW/GL fallback framebuffer or
  matrix-load view reads, plus direct controller-owned database-source
  replacement field assignments that would bypass the grouped configuration
  helper,
  rejects direct BSG mesh-LoD detail callbacks in the BRep LoD producer, and
  pins the RT source full-detail LoD provider contract, direct BoT database
  read path, source-request conversion/submission helpers, exact-result
  consumption helpers plus action-level submit/drain helpers for
  export/measure/snap and exact mesh pick request collection/consumption,
  qtcad source-backed exact pick, snap, and measure caller integration plus
  controller exact full-detail budget submission, multi-source exact
  database-source matching, duplicate-active exact source reload suppression,
  stale source-metrics behavior, and over-budget whole-object fallback coverage;
  strict mode and final allowlist removal remain.
* Low-level cleanup: 98 percent.  vlist, plot3, line-layer, RT view-info,
  neutral RT LoD policy snapshots, mesh-LoD handles, sketch polygons,
  normalized view-coordinate constants and
  view size/scale limits in selected libged/TclCAD/MGED command/helper paths,
  the private libged BSG-to-RT view adapter declaration and LoD policy
  snapshot/apply boundary for draw and LoD commands, and several utility paths
  including libged database-index LoD cache invalidation and view lod cache
  clear/status/BoT-refresh operations plus draw BoT LoD cache ownership are
  neutralized; active draw LoD publication now reads the RT mesh-LoD data view
  rather than BSG container fields; private BSG draw LoD adapters now take
  RT mesh-LoD handles instead of exposing borrowed BSG mesh pointers to
  draw.cpp; generated BRep mesh cache writes now go through RT helpers,
  draw source state no longer stores BSG mesh-LoD context or borrowed BSG mesh
  state, public GED no longer owns a BSG mesh-LoD context, and draw-ref reset
  now frees transitional scene-ref LoD state through the private adapter
  rather than including bsg/lod.h directly; draw tests now verify the
  transaction bounds-update hook through the same private adapter instead of
  naming bsg_view_bounds directly; the private libged view adapter now
  delegates BSG bounds-callback, view-scale, and scene-ref LoD load/free
  behavior to the RT legacy adapter instead of
  including bsg/lod.h; BRep full-detail callbacks now publish through neutral
  RT mesh-LoD detail callbacks instead of BSG callback signatures, so libged
  no longer includes bsg/lod.h for LoD work; qtcad quad views and MGED zoom/idle/attach
  paths no longer use direct BSG LoD policy structs, and qged view-info now
  reads size, dimensions, AET, and center through the RT legacy adapter, but
  lower-level framebuffer/libdm, BRep LoD tessellation producers, and
  some BSG scene paths remain.
* libimgstream migration batch: 57 percent.  Core streams, icv snapshots, and
  memory/file/stack/diagnostic fb_compat are in place; remote/display spec
  normalization plus display-host callback delegation are in place; remote
  transport and fbserv protocol behavior remain.
* Framebuffer display ownership: 39 percent.  The libimgstream/libicv/Obol
  split is defined, non-display compatibility has started, and display-capable
  fb_compat requests can delegate to a registered display host; SoBRLImageSource
  owns image-source metadata and dirty stream notifications, while
  SoBRLViewportImage and SoBRLImagePlane now consume those sources as standard
  Obol texture quads.  BRLObolWindowHost now provides the first neutral bridge
  from display framebuffer streams to Obol image nodes, and
  BRLObolHeadlessWindowHost can poll and capture those scenes through
  SoOffscreenRenderer.  QgObolWindowHost can attach the same display-host
  route to a qtcad Qt canvas, create an owned Qt toplevel QgSW host when no
  canvas exists, and consume Qt readback.  Incremental dirty-region texture
  uploads, broader dm_open routing, hardware-preferred Qt host selection, and
  Tk/MGED adapters remain.

Migration Phases:

* Phase 1, architecture freeze and guardrails: 80 percent.
* Phase 2, productionize libbrlobol: 97 percent.
* Phase 3, migrate qged/qtcad: 53 percent.
* Phase 4, migrate libged, libtclcad, and mged: 26 percent.
* Phase 5, remove lower-level BSG/DM dependencies: 52 percent.
* Phase 6, hard removal: 5 percent.


Next Push
---------

The next implementation push should make the Obol path real enough to test
against application workflows, not just isolated controller/readback pieces.

1. Define the Obol realization coverage matrix.

* Use share/db/pinewood.g and share/db/havoc.g as required hierarchy tests.
  Use share/db/faa/Generic_Twin.g as the larger mixed-primitive maturity
  candidate before the final qtcad migration gate: it is the largest in-tree
  example, includes valid and invalid BoTs, and mixes in implicit solids.
* List every primitive and database object type that the old drawing path can
  draw.
* For each type, record the intended Obol representation: native Obol shape,
  BRL-CAD Obol node, tessellated mesh, wire/vlist fallback, or deferred.
* Record the required preserved identity: db path, primitive, face, edge,
  vertex, material, visibility, selection state, and edit intent.

Completion for this step:

* A checked-in coverage table exists.
* pinewood.g and havoc.g can be loaded by the Obol realization path without
  requiring BSG scene objects.
* Unsupported types fail explicitly with diagnostics instead of silently
  disappearing.

2. Productionize libbrlobol database realization.

* Make SoBRLDatabaseSource and SoBRLSceneController responsible for real .g
  hierarchy realization and partial refresh.
* Keep traversal-relevant state in Obol fields/details, not controller side
  tables.
* Convert bg_vlist/rt_vlist wire output to Obol line/shape nodes only at the
  realization boundary.
* For large BoTs and future high-cost geometry, use the staged LoD realization
  pipeline described in the consolidated LoD notes: publish metadata, attributes/AABB,
  OBB/proxy, POP fast mesh, qualified progressive/high-quality mesh, and
  full-detail fallback as separate revision-tagged results instead of blocking
  Obol realization on a single final mesh.
  The first synchronous bridge is limited to copying already-cached
  rt_mesh_lod_info into SoBRLMeshShape metadata fields and SoBRLExportAction
  triangle records; it must not become the final staged task service or force
  POP generation in ordinary realization.
* LoD implementation sub-queue:
  1. Continue extending the RT-owned mesh-LoD facade before libbrlobol depends
     on POP residency.  Neutral current-level queries, active-data residency
     status, copied active metadata, memory shrink/reload hooks, and cache
     status/refresh/invalidate reporting are in place; richer provider
     diagnostics remain.
  2. Define provider-neutral request/result/geometry-handle types and cache
     keys that include database identity, source revision or content hash,
     object path/name, view/policy revision, draw mode, provider id/version,
     provider parameters, quality tier, bounds, primitive counts, estimated
     error when known, terminal/fallback status, and diagnostics.  Initial
     BRLObolLodRequest, BRLObolLodResult, BRLObolLodGeometryHandle, and
     deterministic cache-key helpers are in place, including typed
     dependencies, attributes, proxy payloads, and owned mesh payloads for
     staged results; the first RT mesh-cache provider task adapter is in
     place, can force a requested RT LoD level, and copies active RT mesh-LoD
     arrays before shrinking borrowed active arrays and publishing results;
     provider-specific POP mesh payload ownership and broader provider wiring
     remain.
  3. Add an owned async realization service with explicit start/stop,
     joinable workers, cancellation/generation tokens, explicit in-flight
     counts, task-graph dependencies, result drain on the UI thread, and one
     serialized bu_cache/LMDB write lane for persistent writes and stale-entry
     clears.  The initial BRLObolLodService is in place with tests for
     dependency ordering, late generation cancellation, stale result rejection,
     UI-thread result draining, cancellable debug-delay diagnostics, and
     serialized cache writes, plus owned realization-task data cleanup for
     completed and never-started queued tasks.  It now has a tested RT
     mesh-cache provider task adapter, provider-specific free helper, and
     coalesced result-ready subscription/wakeup;
     integration with persistent cache-write operations and provider-owned POP
     payloads remains.
  4. Use moodycamel::ConcurrentQueue where it fits inter-stage and result
     handoff, but keep correctness based on enqueue/dequeue success,
     cancellation/shutdown tokens, generation ids, and in-flight counts rather
     than size_approx().  size_approx() is acceptable only for diagnostics,
     idle waits, and settled checks.  The first service uses standard
     mutex/condition-variable queues as the correctness baseline; a
     concurrentqueue substitution can be evaluated later without changing the
     public service contract.
  5. Implement Stage 0/1/2 outputs first: directory/dependency records,
     display attributes, AABB, and OBB/proxy.  Preserve the cache_drawing.cpp
     debug-delay idea with production-owned diagnostics so intermediate
     placeholder states can be inspected during large-model debugging.  The
     provider-neutral result payloads and constructors for these milestones
     are in place, and SoBRLLodUpdateAction can apply those records to matching
     mesh shapes after a UI-thread service drain; the service now has
     cancellable task-level debug delay controls and delayed-task diagnostics;
     the RT mesh-cache provider task is available for cached mesh results, can
     force a requested RT LoD level, can shrink active RT arrays after copying,
     and can publish owned mesh payloads; lodBotThreshold now gates when database
     BoT realization creates LoD-backed mesh nodes.  POP provider tasks,
     provider-level active-level updates, and richer provider/policy
     scheduling remain.
  6. Wire Obol/libbrlobol to consume staged results while preserving existing
     full-field SoBRLMeshShape and vlist realization for small or synchronous
     geometry.  Late results must be ignored harmlessly when their
     source/view/policy generation tokens no longer match.  SoBRLMeshShape now
     exposes staged result fields for dependencies, attributes, proxy state,
     provider status, diagnostics, and cache identity, and can reject stale
     result keys while preserving existing full mesh realization.
     SoBRLLodUpdateAction drains staged results from BRLObolLodService, matches
     them to mesh nodes by source path/name, applies accepted records and owned
     mesh payloads, and reports stale or unmatched results.  Mesh shapes can
     build deterministic request identities from their Obol fields, and the
     view controller can produce an RT view snapshot from Obol camera/viewport
     state.  The first explicit SoBRLMeshLodSubmitAction uses those identities
     and snapshots to enqueue RT mesh-cache provider tasks for matching
     LoD-backed mesh
     nodes, and BRLObolViewController now wires explicit controller-level
     submit/drain/apply calls plus retained service wakeups over its active
     scene and database source list.  It also has an auto-submit gate that
     coalesces changed view/policy/source signatures, includes lodBotThreshold
     policy input in the source signature, tracks sanitized RT view signatures
     including direct camera field changes, exposes a forced-level policy that
     is passed to SoBRLMeshLodSubmitAction, and cancels the previous active
     generation before scheduling new RT mesh-cache requests.
     SoBRLLodMeshShape now marks the first dedicated LoD-backed BoT mesh
     representation, and BoT database realization creates it only when
     lodBotThreshold is positive and the BoT face count meets/exceeds the
     threshold while retaining existing full-field mesh data.  The first
     forced-level provider policy path is wired; richer render-time level
     selection and scheduling remain.
  7. Add a LoD-backed BoT representation, preferably SoBRLLodMeshShape, plus
     SoBRLMeshLodUpdateAction or equivalent controller logic that updates the
     active RT mesh-LoD level from Obol camera/view state before rendering and
     updates private/backend geometry only when the active level changes or a
     reload is requested.  The initial SoBRLLodMeshShape subclass is in place,
     BoT database realization uses lodBotThreshold for policy-threshold
     selection, LoD submission filters out ordinary full-detail mesh shapes
     unless explicitly configured otherwise, and export now has explicit
     full-detail versus display-LoD policy with full-detail preserving
     behavior as the default for ordinary meshes.  Dedicated LoD-backed mesh
     shapes now avoid mandatory full-detail backup when a display-LoD payload
     is active, while keeping source counts and bounds stable for request
     identity.  They can use preserved source triangles for export, measure,
     and snap only when that source geometry is already resident.  Large-geometry LoD must avoid mandatory full-detail residency:
     scene nodes should retain source identity, bounds, counts, and the
     display payload only while memory policy keeps it resident, and exact
     operations should reload or stream bounded full-detail data from the .g/RT
     provider path, including chunked
     bbox/intersection-targeted reads for meshes too large to fit in memory.
     Cached coarse spatial/index sidecars generated while building POP payloads
     are a candidate optimization for pick/snap: reuse the POP pass over source
     triangles to assign faces to normalized Morton/grid leaves, store unioned
     leaf bboxes plus source face ids or ranges, and query those leaves before
     requesting bounded exact data.  Keep this optional and useful only when
     paired with a clean librt BoT partial-read interface or cached full-detail
     chunks.
     Basic forced-level update is wired; richer view-policy level selection
     and source-backed detail access remain.
  8. Move POP generation behind the neutral provider interface as the first
     production fast-BoT provider.  Add BoT topology qualification, mark bad
     mesh topology as POP-terminal when appropriate, and add
     progressive/high-quality providers only after the POP path is reliable.
  9. Make pick, snap, measure, and export choose display-level or full-detail
     geometry explicitly, and add tests for large-BoT realization,
     view-dependent level changes, memory shrink/reload, full-detail export,
     stale late-result rejection, serialized cache writes, and absence of BSG
     LoD headers in libbrlobol.  Provider-level database invalidation coverage
     now verifies explicit RT cache-key clearing, no-refresh cache-miss
     reporting, and refresh-enabled payload regeneration.  SoBRLExportAction
     and SoBRLMeasureAction now default to FULL_DETAIL/detail-preserving
     behavior that skips active display-LoD mesh payloads and uses preserved
     full-detail source geometry when available, while DISPLAY_LEVEL opts into
     the displayed LoD geometry.  SoBRLSnapAction defaults to DISPLAY_LEVEL
     for interactive snapping while FULL_DETAIL skips active display-LoD
     payloads and can use preserved source geometry for edit/precision
     callers.  Dedicated LoD-backed mesh tests now verify display-LoD updates
     do not retain full-detail payloads but still keep source metrics for
     request identity.  Mesh ray picking now defaults to display-level
     geometry and has an explicit full-detail policy that uses resident
     preserved source triangles when available, skipping active display-LoD
     payloads when no exact source geometry is resident.  RT/source-backed
     reload when no preserved full-detail payload is resident is wired for the
     first exact-operation paths.  Mesh nodes now report estimated display,
     preserved full-detail, and total resident mesh bytes, and can explicitly
     evict preserved full-detail arrays and active display payloads while
     retaining source counts and bounds so source-backed requests still work.
     A dedicated scene traversal action plus explicit and optional
     controller-owned budget paths can now enforce a resident mesh byte budget
     by evicting preserved detail first and source-backed display payloads
     second.  Source-backed exact measure, snap, and pick requests now carry
     source-local nearest-query bounds, snap bounds/tolerance, or ray metadata
     through LoD provider params, and the direct BoT provider can reduce snap
     and pick response payloads from those bounds/tolerance and ray hints
     while preserving compact subset source face/vertex ids and applying
     exact full-detail budgets after source-local scoped payload reduction;
     scoped misses now stay no-result fallbacks instead of whole-object
     payloads, and mixed/ambiguous/malformed/non-source-local query metadata
     stays whole-object.  Template source-query params are replaced during
     full-detail source-request conversion so stale request metadata cannot
     create duplicate or mixed query hints.  A reusable
     libbrlobol RT pick cache now covers exact implicit-solid and comb-instance
     hit identity without re-prepping the same explicit object set for every
     ray.
     Chunked/bbox-targeted detail reads for out-of-core meshes and
     remaining tool integration/refinement of librt-backed exact CSG/comb
     picks remain.
     Evaluate a coarse POP-build spatial sidecar plus selective BoT read or
     cached full-detail chunk API for exact pick/snap subsets before committing
     to that complexity.
* Add tests for draw, erase, partial refresh, selection identity, path
  identity, bbox/autoview data, and unsupported-object diagnostics.

Completion for this step:

* libbrlobol can realize, update, inspect, select, and render the required
  model set without libbsg/libdm scene objects.
* Rebuilding a controller from an existing Obol scene does not lose
  authoritative state.
* Staged realization results can arrive asynchronously, apply only to matching
  source/view/policy revisions, and leave stale late results harmless.

3. Replace BSG diagnostic publication with Obol overlays.

* Define the Obol-side representation for diagnostic lines, points, labels,
  axes, grids, faceplate/HUD, edit previews, and selectable diagnostics.
* Convert GQA/check/BREP/NMG diagnostic publication to the Obol overlay path.
* Keep bg_line_layer only as an internal producer/container while legacy code
  still emits line-layer data.

Completion for this step:

* GQA/check overlap overlays are visible through Obol without
  bsg_feature_replace_line_layer_builder in the new path.
* BREP/NMG debug overlays can publish to Obol.
* Legacy BSG publication remains only behind an explicit compatibility path.

4. Make qtcad visibly render through Obol.

* Route QgGL paint through BRLObolViewController::renderPending and the owned
  SoRenderManager after making the correct Qt GL context current.
* Make QgSW/offscreen use Obol OSMesa/NanoRT/readback paths rather than libdm
  swrast for the Obol mode.
* Make Obol camera/view state authoritative for qtcad navigation, autoview,
  image capture, selection, picking, snapping, and measuring.
* Keep the BSG camera bridge only as a temporary legacy adapter until the old
  visible path is removed.

Completion for this step:

* qtcad opens real .g files and displays wire and shaded Obol output.
* navigation, autoview, selection, picking, snapping, measuring, overlays,
  edit preview, and screenshot/readback work through Obol.
* qtcad Obol mode does not require libdm or dm_plugins.

5. Expand guardrails and inventory remaining legacy dependencies.

* Expand brlobol_pivot_guard as directories become Obol-canonical.
* Maintain an explicit allowlist for remaining BSG/DM references.
* Add dependency tests for qtcad Obol mode and libbrlobol.
* Keep conflict-marker and removed-header checks in the focused test set.

Completion for this step:

* New Obol-canonical code cannot include BSG/DM headers or link BSG/DM
  targets without failing CTest.
* The remaining BSG/DM dependency graph is documented and phase-owned.

6. Continue low-level cleanup.

* Finish retargeting non-scene callers from BSG names to BG/RT names.
* Keep BG-owned headers and implementation files free of direct BSG includes.
* Continue retiring public RT callback signatures that expose BSG view,
  mesh-LoD, or sketch-polygon state.  Primitive plot/LoD/face-set callbacks
  now use rt_view_info, primitive LoD code reads view metric policy through
  RT-prefixed helpers in include/rt/view.h, libged's lower primitive
  publication helpers pass rt_view_info into those callbacks,
  BSG-to-RT view snapshot adaptation for those helpers is isolated in
  src/libged/bsg_ged_draw_view.c, and BSG-owning wrapper files include
  bsg_ged_draw_private.h for that adapter declaration rather than redeclaring
  it.  BSG LoD policy extraction for draw.cpp, draw refs, transaction redraw,
  the transaction bounds-update hook, view-scale extraction, scene-ref LoD
  load/free, and the draw command threshold
  compatibility override/restore is isolated behind the same private adapter;
  libged lod and view lod commands also use the private snapshot/apply helper
  instead of direct BSG policy get/set, while the underlying BSG policy mapping
  and BSG view/scene-ref calls are now owned by the RT legacy adapter so qtcad quad views and MGED
  zoom/idle/attach paths can use the same boundary,
  src/librt/view.c is free of BSG adapter logic, src/librt/edit.cpp is free of
  BSG edit adapter logic, src/librt/tests/edit now exercises RT-owned edit
  views without direct BSG setup, and db_mesh_lod_get now returns opaque
  rt_mesh_lod; libged plot, vnirt, solids_on_ray, ADC, and DM window-bound
  helpers now consume RT_VIEW_* and RT_INV_VIEW constants for normalized view
  coordinates instead of BSG_VIEW_*, BSG_INV_*, or INV_BV spellings, and
  solids_on_ray ray construction obtains center, rotation, model2view, and
  scale through the RT legacy adapter; libged
  size, scale, and zoom consume RT_VIEW_MIN_SIZE/RT_VIEW_MIN_SCALE instead of
  BSG_MINVIEWSIZE/BSG_MINVIEWSCALE; libged check -i overlaps, get_eyemodel,
  _ged_rt_write, rtwizard, NIRT default shot setup and qray glyph scaling,
  vnirt view-to-model conversion, GED eye-model helper snapshots, BoT/BREP
  viewport pick ray setup, BREP TikZ view AET output, libged
  selection/polyclip/view-snap/view-polygon/vZ coordinate transforms,
  pipe/metaball/rot_point/label transform helpers, BoT/ARB nearest-subentity
  transform helpers, draw refresh view snapshots through the private GED draw
  bridge, and libged view
  size/center/AET/ypr/quat/rmat/perspective/eye_pos/width/height query output
  and TclCAD polygon command view snapshots/coordinate conversions now read
  through the RT legacy view adapter, and TclCAD data move/pick/screen-to-model
  command conversions use adapter matrix snapshots; TclCAD mouse BREP/joint
  selection rays and simple metaball/pipe point lookups now use adapter
  view-to-model/rotation snapshots, and remaining TclCAD mouse inverse-rotation
  edit-motion calculations use a local adapter helper.  TclCAD mouse BoT
  single-point setup, polygon circle/contour/ellipse/rectangle creation, and
  data-scale display conversions now use adapter model-to-view/view-to-model
  and scale snapshots; mouse delta scaling now uses the RT legacy view-size
  adapter.  MGED simulation output view argument injection now uses adapter
  orientation, eye, and size snapshots, and MGED command autoview ring-scale
  snapshots use adapter scale reads.  MGED make default
  center/scale and
  area view-rotation diagnostic reads now use the same adapter boundary while
  libged ADC view scale/matrix conversion reads and MGED slider view-scale
  reads use that boundary.  libged grid vsnap, eye, and rectangle-zoom
  read-side snapshots plus lookat/qvrot read-side snapshots also use the
  adapter boundary, and MGED axes overlay view reads plus MGED rectangle zoom,
  pipe pick direction, view-centered transform, and initial view-center setup
  reads use that boundary; MGED status query output reads view scale and
  matrices through that boundary as well, and MGED grid auto-size, draw/snap
  scaling, and coordinate-transform reads use that boundary while live grid
  view-center writes remain transitional; MGED ADC view scale and matrix
  conversions, absolute translation bookkeeping, and chgview knob/edit/slew/size
  read snapshots use that boundary too; MGED generic mouse and X motion/ADC
  input conversions also snapshot view scale, center, and matrices through
  that boundary, and MGED edit matrix composition/rate-knob translation scaling
  plus libged align and rotation/slew/translate utility helpers snapshot their
  view inputs through that boundary before preserving the existing BSG state
  updates; libged preview camera initialization and eye-path marker calculations
  now use adapter snapshots for rotation and view-to-model inputs; MGED
  saved-view snapshots and RT command eyepoint correction now read
  scale/rotation/center/view-to-model state through the same adapter boundary,
  and the RT command view update path now uses the RT legacy scale, rotation,
  and center setters while preserving the existing `new_mats` refreshes;
  `mged_vscale` now routes its view-scale update through the RT legacy scale
  setter while preserving absolute-scale knob bookkeeping; MGED
  autoview ring-scale updates plus view-ring save-current snapshots now use
  adapter snapshots while preserving view-ring restore writes; the
  first narrow edsol slice now routes absolute-translation bookkeeping
  scale/model-to-view/view-to-model reads, solid-rotation rotate-about
  view/eye points, NMG/ARS/metaball view-direction snapshots, and BoT/NMG
  edit-pick plus solid/object edit mouse-projection view snapshots through the
  adapter; MGED dozoom perspective, eye-position, model-to-view, and saved
  projection-matrix read snapshots now use the same adapter while live
  projection-matrix writes remain transitional; the stereo fallback
  perspective write now uses the RT legacy perspective setter;
  libged selection semantics pick-point width/height reads and rtwizard
  view-matrix comparison reads also use that boundary; libged
  isize/keypoint/rotate_about
  queries and rotation/translation utility helpers now read raw width/height,
  inverse size, initial-scale, absolute-scale, unit-conversion, keypoint,
  rotate-about, and coord state through the same boundary; libged,
  TclCAD, and MGED default/query coord reads plus MGED edit-knob coord save
  reads now use that adapter while coord writes and unsupported staging/hash
  fields remain transitional; MGED view command staging/cache/hash reads for
  adapter-covered fields, MGED temp staging width/height reads, and libged DM
  fallback width/height checks now use the same boundary; libged
  pmat/pmodel2view query output, TclCAD draw projection-matrix loads, and MGED
  staging/cache/hash projection-matrix reads now also snapshot through the
  adapter while projection writes remain transitional; MGED cmd staging/cache
  restore/copy now uses RT legacy exact-state setters for scalar tuples, unit
  conversions, raw AET, eye/keypoint, rotation/center, derived matrices, and
  projection matrices, and shared staging unit-conversion refresh uses the
  same boundary while hash/address handling and RT-native/non-BSG setup fields
  stay transitional; MGED staging/cache/hash
  initial/absolute scale reads plus libged and MGED knob absolute-scale query
  output now also use scalar adapters while remaining staging/cache scale
  writes remain transitional;
  MGED cache plus TclCAD refresh unit-conversion stash reads now use the same
  boundary.  MGED VIEWSIZE/VIEWFACTOR macros now expand through the adapter
  boundary while unit-conversion writes remain transitional; libged AET
  incremental updates now avoid direct readbacks after writes, and libged
  eye-position plus perspective command matrix updates now use local/adapter
  snapshots and projection-matrix/eye-position setters instead of direct BSG
  readbacks or direct field writes; libged knob keypoint-origin rotation,
  size/scale/zoom paired-field updates, libged pure-rotation composition, and
  MGED view-scale ratio updates now also avoid direct BSG readbacks, leaving GED-specific
  eye-model calculation in GED; RT command, rtwizard, saveview,
  embedded-RT, and GED eye-model-helper perspective reads use the same adapter
  boundary,
  and plot/PS/PNG diagnostic
  export copies its camera values through the same adapter before calling the
  legacy export routines; the librt edit BSG compatibility bridge now builds
  `rt_edit_view` snapshots through the RT view adapter boundary instead of
  reading BSG view fields directly; simple libged point/matrix/grid/vector conversion
  helpers, viewdir, and overlay default scale read model2view, view2model,
  view rotation, view scale, and rtcheck2 overlap glyph scale through the same
  adapter; savekey now uses RT
  legacy adapter copies for RT -M size, rotation, and view2model output; MGED
  title/status display now reads
  center, size, AET, perspective, and scale through the RT legacy adapter;
  libtclcad view draw matrix/perspective, wrapper perspective reads, command
  center/scale reads, axes size scaling, polygon command view snapshots, and
  qged view-info numeric display reads also go through the RT legacy
  adapter; qtcad Obol camera sync, snap tolerance, input scaling, sketch
  transform, selection ray/rectangle view snapshots, 2D measurement
  view-to-model conversion, quad-view source unit/size copies, and SW/GL
  fallback framebuffer-size or matrix-load reads plus qtcad test/demo view
  setup writes now use the same RT view adapter boundary, and qtcad quad-view
  visible-path propagation now uses
  Obol export identity in display-level mode instead of direct BSG export
  queries; qtcad selection's public header now exposes selected paths without
  naming legacy BSG pick-result objects while keeping the remaining fallback
  internal; qtcad GED draw observer sync and real-model Obol
  draw/readback workflows now keep controller-owned database sources realized
  after delayed Obol field sensors drain; libtclcad display-bound setup, MGED ADC
  view-unit math, the shared GED2PM1 conversion, MGED axes overlay scaling,
  MGED attach window bounds, MGED view-scale clamping, and MGED menu/faceplate
  drawing plus slider/pen normalized-coordinate handling now use the same RT
  view numeric boundary, and MGED event/generic display-manager mouse and ADC
  numeric paths no longer use BSG-owned view-coordinate constants.  Sketch
  conversion now uses opaque rt_sketch_polygon in the neutral header
  and RT-owned implementation, with BSG polygon refs limited to the explicit
  legacy adapter header and implementation unit.  rt_mesh_lod loading/data
  access, copied active-state metadata, cache status, reported refresh, and
  explicit invalidation are available through RT-owned helpers.  The libged
  draw BoT LoD path also obtains cache status, refreshes missing or stale
  entries, owns the returned rt_mesh_lod handle, publishes active geometry
  through rt_mesh_lod_data_get, and passes that RT handle to private GED
  adapters for the remaining transitional BSG scene-ref visibility/load behavior.
  Generated BRep mesh payloads are stored with db_mesh_lod_store_mesh, and
  BRep draw realization now owns an rt_mesh_lod handle before borrowing the
  BSG payload through the RT legacy adapter for transitional scene-ref load
  behavior; the full-detail BRep
  BSG callback registration and payload field mutation are isolated in
  bsg_ged_draw_brep.cpp instead of draw.cpp.  Direct BSG cache mutation is
  confined to the RT cache implementation; BRep tessellation remains the documented
  transitional libged-side producer.  BSG mesh-LoD unwrapping remains private
  to librt implementation units, with private GED adapters passing RT-handle
  parameters through the RT legacy BSG view/scene-ref boundary.
  rt_view_info adapter,
  sanitize, RT view
  metric helper, and mesh-LoD helper behavior are covered by the BSG-free
  src/librt/tests/view_info.c; src/librt/tests/view_legacy_bsg.c covers the
  transitional BSG adapter; rt_lod validates cached LoD levels through the
  neutral helper surface.  libged database-index LoD cache invalidation uses
  db_mesh_lod_invalidate instead of direct BSG key/cache APIs, and view lod
  cache clearing uses db_mesh_lod_clear/rt_mesh_lod_cache_clear_all instead of
  direct BSG cache-clear calls.  View lod BoT cache refresh and cache-exists
  checks use db_mesh_lod_refresh/status rather than direct BSG key lookup; the
  draw BoT mesh-LoD path uses db_mesh_lod_status/refresh/get and RT handle
  ownership instead of direct BSG key/cache generation, while generated BRep
  mesh payloads are written through db_mesh_lod_store_mesh instead of direct
  BSG cache APIs.  The remaining BRep tessellation producers in view lod and
  draw are explicitly transitional.  The draw source state and tree-walk
  draw_data_t no longer carry bsg_mesh_lod_context or ged_lod through to
  realization, ged_draw_source_state no longer stores a borrowed bsg_mesh_lod
  pointer, and public struct ged no longer exposes or manually manages a
  BSG mesh-LoD context.  General BSG draw helper units without mesh-LoD
  payload operations, plus the GED draw tests that now use RT cache helpers,
  no longer include bsg/lod.h; draw.cpp routes BRep mesh-LoD full-detail
  setup through the private BREP draw helper, and that helper now registers
  neutral RT mesh-LoD detail callbacks rather than BSG callback signatures.
  Draw-ref reset calls the private scene-ref LoD free
  adapter rather than direct BSG LoD APIs.  The private libged BSG-to-RT view
  adapter now uses only RT legacy helpers for BSG LoD bounds callbacks,
  view scale, and scene-ref LoD load/free.  Draw tests check the transaction
  bounds-update hook through the private helper rather than direct
  bsg_view_bounds comparisons.  qtcad quad views, MGED zoom/idle/attach paths,
  and libtclcad commands/wrapper redraw checks copy, inspect, and apply LoD
  policy through rt/view_legacy_bsg.h instead of direct BSG policy structs or
  get/set calls.  All are enforced by the pivot guard.
* The initial provider-neutral LoD request, result, cache-key, and
  geometry-handle types are in place before wiring libbrlobol to POP.  Keep
  POP as the first production fast BoT provider behind RT/provider APIs; add
  BoT topology qualification and progressive-mesh generation later as separate
  providers.  Result records carry object path, source revision, view/policy
  revision, result kind, quality tier, provider id, cache key or geometry
  handle, bounds, primitive counts, estimated error when known,
  terminal/fallback status, and diagnostics.  The current SoBRLMeshShape and
  SoBRLExportAction LoD summary fields are only the first visible metadata
  slice of that result contract.  The initial BRLObolLodService provides the
  staged worker, generation cancellation, dependency, result-drain, and
  serialized cache-write lane needed by future providers.  Stage 0/1/2 result
  payloads now carry dependency records, display attributes, AABB milestones,
  and proxy/OBB records.  Staged mesh results can also carry owned
  indexed-triangle payloads copied from RT mesh-LoD data.  SoBRLMeshShape
  consumes those staged records into Obol fields, applies owned mesh payloads
  through its existing geometry fields, and rejects stale, cancelled, error,
  and cache-miss result statuses.  SoBRLLodUpdateAction drains service results
  and applies accepted staged records to matching mesh nodes.  Mesh shapes can
  build deterministic request identities from their Obol fields, and
  BRLObolViewController can produce sanitized rt_view_info snapshots from its
  Obol camera/viewport state.  The first explicit SoBRLMeshLodSubmitAction
  uses those identities and snapshots to enqueue RT mesh-cache provider tasks
  for matching LoD-backed mesh nodes and lets BRLObolLodService own provider
  task cleanup after completion,
  cancellation, or shutdown before execution.  BRLObolViewController now has
  an explicit controller-facing submit/drain/apply path over database sources
  so applications can exercise the staged path without hand-instantiating the
  actions, plus a retained result-ready subscription that turns worker queue
  publication into a main-thread pending-result drain before render.  It can
  also coalesce automatic submissions by changed view/policy/source signature
  before render.  The first RT
  mesh-cache provider task can refresh/load cached RT mesh LoD through the
  service and uses the RT active-data residency query to keep unloaded active
  levels on the cache-miss result path.  Dedicated LoD-backed Obol mesh nodes
  now opt out of preserving full-detail arrays when display-LoD payloads are
  applied and retain lightweight source metrics for stable request keys, but
  the service does not yet own POP payload residency, source-backed
  full-detail fallback, chunked/bounded
  detail fetches for exact operations, optional acceleration/index sidecars
  for targeted triangle subset queries, memory-budget eviction policy, richer
  provider/policy scheduling, or update dedicated LoD-backed Obol mesh nodes.
* Keep the default plot3 implementation in its normal libbg implementation
  unit.
* Keep standalone plot3 utility links on libbg/libbu instead of libbsg, except
  for explicit display/Tk compatibility utilities such as plot3-dm.
* Keep bg_line_layer as the neutral in-memory diagnostic container.  The
  bg_line_layer-to-plot3 export helpers are available as a simple diagnostic
  output path that does not require Obol execution.
* Keep bsg/plot3.h only as a compatibility wrapper until libbsg removal.
* Do not reintroduce old BSG TIG or vectfont public headers.
* Move useful framebuffer/image-streaming behavior out of libdm according to
  the framebuffer plan: libimgstream for mutable streams, transport, producers,
  subscribers, fan-out, fbserv compatibility, and fb_* wrappers; libicv for
  static image/codecs/lookup-table operations; Obol/libbrlobol for display,
  texture nodes, readback, image placement, and window/input hosting.
* Preserve useful legacy transport and protocol workflows through
  libimgstream compatibility while replacing display-capable framebuffer
  backends with Obol image display.

Completion for this step:

* Low-level libraries no longer need BSG names for vlist, line-layer, plot,
  or diagnostic containers.
* Remaining libdm framebuffer use is either deleted, routed through
  libimgstream/fb_compat, replaced by Obol readback/display, or assigned to
  libicv image-processing utilities.

7. Continue the libimgstream migration batch.

* The libimgstream build target and initial opaque C API are in place.
* The first in-memory RGB/RGBA stream layer implements imgstream_get_info,
  resize, clear, full frame read/write, rect read/write, dirty rectangle
  generation, subscriber callbacks, producer begin/end, and
  imgstream_snapshot_icv conversion.
* Keep expanding focused tests for stream lifecycle, resize behavior, pixel
  operations, dirty-update generation, subscriber notification, and snapshot
  conversion as compatibility callers arrive.
* Add the remaining metadata/state APIs needed by framebuffer compatibility
  before routing old fb state through libimgstream.
* The first fb compatibility wrapper entry points are in place for memory
  stream open/close, clear, read/write, RGB readrect/writerect,
  BW readrect/writerect, view/getview, window, zoom, cursor/getcursor,
  scursor, setcursor, reset, viewport, and no-op non-display poll/poll-rate.
* Raw RGB file compatibility is in place for ordinary file paths and explicit
  /dev/disk:<path> or /dev/disk=<path> spelling.  File-backed streams load
  existing exact-sized pixel data, expose the same imgstream operations, and
  flush raw RGB data on request or close.
* /dev/stack fan-out compatibility is in place for semicolon-separated
  memory/file child sinks.  Writes, rectangle writes, clear, view, cursor,
  poll, and flush propagate to every child; reads use the first child, matching
  the legacy stack framebuffer behavior.
* /dev/null, /dev/debug, and /dev/txt diagnostic sink compatibility is in
  place.  These sinks expose dimensions and preserved view/cursor state,
  discard writes without mutating the backing stream, return zero pixels for
  reads, and keep /dev/debug logging behavior for operation tracing.
* Neutral colormap compatibility is in place.  imgstream_fb_colormap stores the
  legacy 256-entry RGB transfer table with 16-bit channels, rmap/wmap preserve
  per-stream state, and wmap(NULL) resets to the default linear map.
* Remote/display framebuffer spec normalization is in place.  The
  imgstream_fb_spec_info helper records parsed file/fan-out targets,
  remote TCP or IPC targets, host/port/device fields, display kind, and
  diagnostic kind for legacy spelling such as numeric ports, host:port,
  host:port:device, tcp:, ipc:, and /dev/qtgl.
* Display-host callback registration is in place.  Display-capable
  framebuffer opens create an RGB stream and delegate open/close, view,
  cursor, screen-cursor, cursor-shape, viewport, reset, flush, poll, and
  poll-rate operations to the registered display host.  If no host is
  registered, display specifiers still fail open explicitly.
* Add remote transport, fbserv protocol, and real Obol/libbrlobol display-host
  implementations in later batches.

Completion for this step:

* A non-display image stream can be created, updated, observed, read back, and
  converted to/from libicv data without linking libdm.
* The initial API remains small, opaque, and uses imgstream_* names.
* Memory framebuffer compatibility requests route to libimgstream-owned RGB
  buffers, file compatibility requests route to libimgstream-backed raw RGB
  file sinks/sources, and /dev/stack fan-out compatibility routes to supported
  child sinks.  Diagnostic framebuffer compatibility routes to non-display
  diagnostic sinks.  Legacy colormap state is preserved through neutral
  libimgstream types.  Remote and other old framebuffer specifiers are parsed
  for future migration and rejected explicitly for now; display framebuffer
  specifiers open only when a toolkit-neutral display host is registered.
* No Obol display code depends on libimgstream until the stream layer is
  independently tested.

8. Extract framebuffer compatibility and display ownership.

* Move useful /dev/mem behavior to core libimgstream in-memory streams.
* Move file sink/source and colormap image transforms to libicv/libimgstream
  as appropriate.
* Move /dev/remote, host:port, ipc:<addr>, PKG_ADDR, FBSERV_TOKEN,
  FBSERV_TLS, and MSG_FB* behavior to libimgstream transport/fb_compat.
* Preserve legacy fb_view, fb_getview, fb_window, fb_zoom, cursor, poll,
  expose/resize, linger, and simple mouse behavior as compatibility/display
  state.
* SoBRLImageSource is in place for static icv_image_t data and borrowed live
  imgstream sources, including stream metadata fields and dirty-region
  subscription updates.  SoBRLViewportImage and SoBRLImagePlane are in place as
  standard Obol texture-quad consumers driven by source data/dirty revisions;
  replace full rebuilds with dirty-region backend texture uploads and connect
  the display-host/window-host paths.
* The neutral BRLObolWindowHost contract and imgstream display-host
  registration are in place for display-capable fb_open.  The first
  headless/offscreen BRLObolHeadlessWindowHost adapter can drain render
  requests and capture pixels through SoOffscreenRenderer.  qtcad's
  QgObolWindowHost can attach a Qt canvas to the same display-host path and
  can create an owned visible Qt toplevel QgSW host for display-capable
  framebuffer requests when no canvas exists.  Connect dm_open/display-host
  routing through that contract, replace the temporary owned-window fallback
  with final qtcad/qged host policy, and add the Tk/MGED adapter.

Completion for this step:

* fbserv, rt -F, remrt, and representative src/fb tools can run through
  libimgstream compatibility while display-capable requests are shown by an
  Obol/libbrlobol display host or fail explicitly when no host is registered.
* Old framebuffer backend identities no longer define the implementation
  architecture.


Migration Phases
----------------

Phase 1: Architecture freeze and guardrails

* Document Obol as BRL-CAD's canonical drawing API.
* Keep brlobol_pivot_guard active in transitional mode.
* Inventory all current BSG/DM dependencies.
* Convert the guard to strict mode before hard removal.

Done when:

* The policy is documented.
* New Obol-canonical directories are clean of BSG/DM dependencies.
* Legacy violations are inventoried and assigned to later phases.

Phase 2: Productionize libbrlobol

* Complete primitive and hierarchy realization.
* Add staged provider-neutral LoD realization for large BoTs, starting with
  POP behind the RT mesh-LoD facade and preserving full-detail fallback for
  exact operations.
* Preserve identity, material, visibility, selection, draw intent, edit
  preview, and diagnostics on Obol nodes/fields/details.
* Implement snap, measure, pick, export, partial refresh, HUD, overlays,
  faceplate, grid, axes, ADC, and text behavior with Obol concepts.

Done when:

* libbrlobol can load, realize, inspect, select, pick, snap, measure, export,
  and render the agreed model set without libbsg/libdm.
* Large BoTs can render through LoD-backed Obol geometry, refine from view
  changes, and still support explicit full-detail pick/measure/snap/export
  policies.
* Controller rebuild from an existing Obol scene works without lost state.
* libbrlobol has no generic BSG replacement API.

Phase 3: Migrate qged/qtcad

* Replace visible qtcad rendering with Obol rendering.
* Move qged open/draw/erase/view/autoview/navigation/pick/select/snap/
  measure/edit-preview/HUD/screenshot workflows to Obol.
* Use Obol software/offscreen fallback paths instead of libdm swrast.

Done when:

* qged/qtcad render real models through Obol only.
* qged/qtcad support the expected interactive workflows through Obol.
* qged/qtcad no longer link libbsg/libdm or depend on dm_plugins.

Phase 4: Migrate libged, libtclcad, and mged

* Replace bsg_ged_draw* with an Obol-backed GED scene service.
* Port TclCAD view creation, input handling, refresh, faceplate, and readback
  to the Obol controller bridge.
* Keep mged supported with a proper Tcl/Tk Obol widget for performant native
  rendering when system graphics APIs are available.  Obol offscreen rendering
  plus Tk image presentation is a fallback path, not the target design.
* Route display-window creation through the libbrlobol window host contract
  rather than preserving dm_open as a public display-manager abstraction.

Done when:

* libged and libtclcad no longer link libbsg/libdm.
* mged core display workflows pass scripted regression tests using Obol.
* No GED/TclCAD command requires a public bsg_view or dm object.

Phase 5: Remove lower-level BSG/DM dependencies

* Finish moving neutral geometry/debug/export containers to libbg/librt names.
* Retire BSG vlist and line-layer wrappers.
* Replace scene text and labels with Obol text.
* Grow libimgstream from the initial memory-stream target into the owner of
  useful framebuffer storage, streaming, producer/subscriber, transport,
  fbserv, and fb compatibility behavior.
* Move static image/codecs/lookup-table behavior to libicv.
* Replace libdm framebuffer display behavior with Obol image nodes and
  libbrlobol display-host/window-host paths.

Done when:

* No non-legacy target in src/source_dirs.cmake depends on libbsg/libdm.
* Low-level libraries do not include BSG/DM headers for scene or display
  behavior.
* Framebuffer tools use libimgstream/fb_compat, libicv, or Obol readback and
  display paths rather than libdm framebuffer backends.

Phase 6: Hard removal

* Delete installed BSG and DM public headers.
* Delete libbsg, libdm, and dm_plugins from build and install.
* Remove temporary allowlists and transitional bridges.
* Remove obsolete fb_* compatibility wrappers only after rt, remrt, qged,
  TclCAD/mged, and required tools use libimgstream or Obol directly.  Keep
  transport and MSG_FB* compatibility where it remains useful independent of
  the old libdm framebuffer backend API.
* Update documentation, examples, man pages, package metadata, pkg-config
  files, and build files.
* Enable strict dependency checks.

Done when:

* Full build/install produces no libbsg, no libdm, no dm_plugins, and no
  public BSG/DM headers.
* No source target links libbsg/libdm.
* No source code includes BSG/DM headers outside intentionally archived
  historical documentation.
* Obol is documented as the canonical BRL-CAD scene API.


Consolidated Historical Notes
-----------------------------

This section preserves the useful context from the former root notes that were
not meant to stay as separate active handoffs.

newdraw branch review:

* The newdraw branch was a large architectural migration, not a cosmetic
  libbv rename.  The review snapshot measured roughly 1050 changed files,
  155298 insertions, and 42636 deletions.
* The strongest value in newdraw was centralized semantic drawing state:
  libged owned draw records, draw transactions, database indexes, selection
  state, stale tracking, and shared frontend semantics; libdm gained a backend
  render-item/resource-cache boundary; MGED, TclCAD, and qged moved toward one
  common draw-state model.
* The main risk was scope and public API stability.  libbsg was broader than
  libbv: scene graph, view state, render items, payloads, selection, snapping,
  overlays, HUD, LoD, and backend adapters.  The Obol migration keeps the
  useful semantic lessons but avoids preserving libbsg as the canonical public
  drawing API.
* The review's validation lesson still applies: make the risk bounded with
  main-compatible behavioral tests, unchanged tests on the migrated path,
  Obol/newdraw-only semantic tests, stress/scale/performance gates, and an
  explicit evidence package before broad legacy removal.

Modern drawing/model handoff:

* The database remains authoritative.  Draw records, selections, edit
  previews, Qt models, indexes, and application state are derived and
  rebuildable.
* Native GED services are the desired ownership model:
  GedDbIndex owns object/path/reverse-use/affected-path indexing;
  GedEventTxn owns mutation batching, event coalescing, reconcile phases, and
  post-reconcile observer notification; GedDrawState owns retained draw
  intents, typed realized geometry, stale state, draw/erase/clear/redraw/who
  behavior, and shared/independent view transactions; GedSelectionState owns
  semantic selection and highlight synchronization.
* Do not recreate DbiState, BViewState, gedp->dbi_state,
  ged_dbi_state_*, include/ged/dbi_state.h, src/libged/dbi.h, or
  src/libged/dbi_state.cpp as the ownership model.
* Modern drawing publication is typed: line sets, point sets, indexed line
  sets, indexed face sets, text, annotations, images, overlays, proxies, and
  edit previews.  Modern paths should not expose raw vlists/vlblocks or raw
  BSG internals.
* Observers should see stable post-reconcile state only.  Avoid notifying UI
  or application consumers while database, draw, selection, or edit state is
  partially mutated.
* Scale work should be bounded by affected objects, active draw intents,
  expanded Qt branches, selected/edit targets, and active views.  Full rebuilds
  are acceptable for open-file, explicit rebuild, corruption, batch import, or
  events whose scope cannot yet be localized.
* Avoid full path-string scans in hot update paths.  Prefer path hashes,
  reverse-use answers, affected-path hints, and loaded-row indexes.  If full
  path matching is unavoidable, benchmark it at scale and add a regression
  gate.

Obol prototype lessons:

* The prototype proved enough to proceed with Obol-canonical work: database
  source nodes, SoBRLRealizeAction, real librt/db_walk_tree traversal,
  combination instance transforms, SoBRLVListShape, SoBRLMeshShape, shaded
  ARB8/RPP/BoT realization, NMG tessellation fallback, pick details, snap,
  measure, export, draw-intent fields, edit preview, field-sensor
  invalidation, partial source refresh, scene-controller rebuild from existing
  Obol nodes, grid/axes/ADC overlays, direct Obol GL rendering, non-GL
  renderScene proof, NanoRT proof, readback, and Obol HUD use without libdm.
* Every new feature should first be expressed as normal Obol scene
  programming: node fields, field sensors/notification, actions, details,
  state/elements only where traversal state is genuinely needed, and
  application-owned backends through Obol's context/backend interface.
* Do not add a generic BRL-CAD node/field/action/render layer unless a
  concrete test shows Obol cannot represent the needed behavior cleanly.
* Continue the Obol pivot if libbrlobol remains a small BRL-CAD extension
  layer on top of Obol.  Pause and reconsider if libbrlobol starts exposing
  generic scene, field, render-item, or backend-adapter types that duplicate
  Obol.

LoD pipeline details:

* The source database is authoritative.  LoD data is disposable derived
  display/cache data.
* The staged service should produce metadata/dependency records, attributes
  and AABB, OBB or cheap proxy, fast display LoD, qualified high-quality LoD,
  and exact/full-detail fallback as separate revision-tagged results.
* Provider-neutral request keys should include database identity, source
  revision or content hash, object path/name, view/policy revision, draw mode,
  provider id/version, provider parameters, quality tier, bounds, primitive
  counts, estimated error where known, terminal/fallback status, and
  diagnostics.
* POP Buffer generation is the first production fast-BoT provider because it
  is robust for triangle soup and invalid BoTs.  Progressive mesh generation
  should be a later provider for qualified clean topology.
* The older cache_drawing.cpp experiment contributed useful shape: staged
  fast-to-slow workers, main-thread result queue, serialized cache writer,
  debug delay knobs, and AABB/OBB/LoD milestones.  Do not carry forward its
  detached workers, object-name-only keys, void* lifetime, fixed linear
  assumptions, or informal rt_db_internal ownership.
* concurrentqueue.h may be used for high-volume producer/consumer handoff if
  it improves implementation quality, but correctness must come from
  enqueue/dequeue success, cancellation/shutdown tokens, generation ids, and
  in-flight counts.  size_approx() is only diagnostic.
* Result-ready wakeups are signals only.  Workers publish BRLObolLodResult
  records and coalesced wakeups; the owning view/controller drains results on
  the UI/main thread and applies SoBRLLodUpdateAction to matching scene nodes.
* Large LoD-backed BoTs must avoid mandatory full-detail residency.  Exact
  operations should request source-backed whole-object reloads, bounded
  chunks, or bbox/intersection-targeted subsets through the RT/provider path.
* A POP-build spatial sidecar is plausible and should remain optional: while
  walking source triangles, assign faces to coarse normalized Morton/grid
  buckets, union leaf bboxes, store source face ids or ranges, and build a
  shallow tree.  It is useful only if paired with a BoT partial-read API or
  cached full-detail chunk provider.

Framebuffer and image-stream plan:

* Obol owns display, placement, layering, texture upload, readback, backend
  selection, window hosting, and input integration.
* libimgstream owns mutable streams, pixel storage, dimensions, dirty
  rectangles, generation counters, clear/read/write/read-rect/write-rect,
  snapshots, producers, subscribers, fan-out, in-memory streams, file
  sinks/sources, transport, fb compatibility, null/debug/txt diagnostics, and
  fbserv/remrt/rt workflows during migration.
* libicv owns static image containers, codecs, file I/O, image transforms, and
  neutral colormap/lookup-table operations.
* fb compatibility lives inside libimgstream as fb_compat.h/fb_compat.c unless
  implementation evidence justifies a split.
* Preserve useful transport mechanisms: host:port, ipc:<addr>, PKG_ADDR
  handoff, FBSERV_TOKEN, FBSERV_TLS, and MSG_FB* where needed.
* Treat legacy view, cursor, poll, expose/resize, linger, simple mouse, and
  pan/zoom behavior as compatibility/display state.  Do not let it define the
  core image-stream API.
* Device spelling maps under compatibility: /dev/mem becomes in-memory stream
  behavior; /dev/disk becomes file sink/source behavior; /dev/stack becomes
  fan-out; /dev/null, /dev/debug, and /dev/txt become diagnostic sinks;
  remote, host:port, tcp:, and ipc: spellings become transport endpoints;
  display specifiers such as /dev/qtgl, /dev/ogl, /dev/wgl, /dev/X, and
  /dev/swrast request display through an active Obol/libbrlobol host or fail
  explicitly when no host exists.
* The libbrlobol window-host contract should hide toolkit details.  Qt, Tk,
  GLX/WGL, and other native handles belong in adapter-private code or explicit
  application integration hooks.  Long-term input bindings should be semantic,
  not Tcl strings, although the Tk adapter may translate legacy Tcl bindings
  during migration.


Verification Baseline
---------------------

Use this short baseline after doc-only or small migration changes:

* ninja -C build
* ctest --test-dir build -R
  '^(brlobol_pivot_guard|rt_view_info|bg_vlist_basic|bg_line_layer_basic|libbsg_feature|libbsg_api_hygiene|libbsg_public_headers|ged_test_gqa|ged_test_cmd_schemas|ged_test_ged_draw_scene|regress-gchecker|regress-gqa)$'
  --output-on-failure
* git diff --check
* rg -n '^<<<<<<< |^=======$|^>>>>>>> |bsg/tig\.h|bsg/vectfont\.h' include
  src obol_activework.txt obol_worklog.txt CMakeLists.txt misc/CMake

Use broader testing before removing a legacy path:

* full ctest --test-dir build --output-on-failure
* qged/qtcad workflow tests against pinewood.g, havoc.g, and
  share/db/faa/Generic_Twin.g once the Obol path is mature enough for the
  larger mixed implicit/BoT model.
* Obol render backend tests for Qt GL, offscreen/OSMesa, and NanoRT/software
  paths as applicable.
* libimgstream tests for stream lifecycle, resize, pixel operations,
  dirty-region updates, producer/subscriber lifecycle, icv snapshots,
  transport, and framebuffer protocol compatibility as applicable.
* Dependency checks proving the removed BSG/DM path is no longer installed,
  linked, or included.


Performance Gates
-----------------

Before final BSG/DM removal:

* Initial realization/render on selected regression models should be within
  25 percent of the current path, or have an explicit documented and accepted
  reason.
* Camera-only redraw and selection/pick updates should be equal or faster
  than the old path on the selected regression models.
* Small imports and small hierarchy updates must not show pathological
  scaling.
* regress-gqa should remain a sub-second test unless the test scope is
  deliberately expanded.


Open Questions
--------------

* Which Obol upstream APIs need to be added or improved once qtcad uses Obol
  as the visible renderer?  This needs more implementation evidence and
  discussion before turning into concrete migration tasks.

Resolved decisions:

* plot3 should gain a normal libbg implementation unit; plot3.h is unlikely to
  be retired.
* bg_line_layer-to-plot3 export should remain available as a simple diagnostic
  output path for debugging without requiring Obol execution.
* MGED should target a proper Tcl/Tk Obol widget for performant native
  rendering when system OpenGL/Vulkan/etc. are available; OSMesa/offscreen
  rendering plus image presentation remains a slower fallback.
* Residual libdm framebuffer behavior should split as described in
  the framebuffer plan: libimgstream for mutable streams, producer/subscriber state,
  transport, fbserv behavior, and fb compatibility; libicv for static images,
  codecs, and lookup-table image transforms; Obol/libbrlobol for image
  display, texture nodes, readback, display-host callbacks, and window/input
  hosting.


Final Completion Criteria
-------------------------

The migration is complete when:

* Obol is the only scene/rendering API used by BRL-CAD interactive
  applications.
* qged/qtcad and mged pass their Obol display workflows.
* libbsg, libdm, and dm_plugins are absent from build, install, public
  headers, pkg-config files, and linked target graphs.
* libbrlobol remains BRL-specific and does not contain a generic BSG
  replacement.
* Documentation names Obol as the canonical scene API and explains where
  BRL-CAD Obol extensions live.


2026-06-21: libged view feature adapter slices
----------------------------------------------

Moved the `view obj ... line` command's direct BSG feature-overlay operations
behind the private GED draw view adapter.  `src/libged/view/lines.c` now asks
the adapter to test line-feature existence, create persistent model annotation
line features, and append points.  The BSG-backed implementation remains in
`src/libged/bsg_ged_draw_view.c`, which keeps the transitional feature,
geometry, and overlay calls at the draw bridge boundary.

The follow-up batches moved label creation, data-axes state, and Tcl data-line
overlay state through the same boundary.  `src/libged/view/labels.c` now uses
the adapter for label feature existence and creation/data replacement.
`src/libged/view/axes.c` now uses a neutral GED draw axes-state record for
data-axes creation, position, size, line width, and color updates while the
bridge maps those fields to the temporary BSG axes payload.  `view data_lines`
and `view sdata_lines` now use the adapter for feature visibility/removal,
style reads/writes, point copying, and paired line replacement while retaining
their existing RT snap-line adapter calls.  The gobject overlay create/remove
path now asks the same adapter to test feature existence, create the temporary
overlay scene ref, and remove the overlay feature, leaving the wireframe
gathering and draw-shape fullpath state unchanged for this slice.  The main
`view obj remove` feature path now also uses the adapter for existence and
removal, so ordinary removal of those temporary overlay features no longer
calls `bsg_feature_remove` directly from `src/libged/view/objs.cpp`.  The
generic view-object draw/color/arrow/update paths now use a neutral
`ged_draw_view_feature_style` record plus adapter style-get, style-apply, and
feature-realize helpers, keeping `bsg_feature_ref`, `struct bsg_feature_style`,
and `bsg_feature_realize` inside `src/libged/bsg_ged_draw_view.c`.
The top-level `view vZ` feature-depth calculation now uses named and
iterating feature-depth helpers from the same private adapter, so
`src/libged/view/view.c` no longer includes `bsg/feature.h` or calls
`bsg_feature_find`, `bsg_feature_view_depth`, or `bsg_feature_visit` directly
for that deprecated calculation path.
The `view selections` count path now uses `ged_draw_view_selection_count`,
leaving the temporary BSG selection count lookup inside
`src/libged/bsg_ged_draw_view.c`.
Faceplate command dispatch now calls `ged_draw_view_hud_sync`, leaving the
temporary BSG HUD synchronization call inside the same private view adapter.
The `view snap` grid and line snap paths now call
`ged_draw_view_snap_first_candidate`, leaving temporary BSG snap-kind mapping,
candidate lookup, result storage, and result cleanup inside the same private
view adapter.
The `view obj ... polygon` command path now uses neutral
`ged_draw_view_polygon_ref` handles and `ged_draw_view_polygon_record` state
for polygon creation, point editing, contour open/close, area/overlap
queries, fill and fill-color updates, sketch import/export, and CSG.  The
temporary `bsg_polygon_ref` storage, polygon record/data reads,
`bsg_polygon_*` mutations, BSG view-polygon lookups, and RT sketch legacy BSG
conversion calls are now confined to `src/libged/bsg_ged_draw_view.c`.
The same adapter now also has a neutral `ged_draw_view_lines_replace` helper
for named diagnostic line overlays with optional move/draw command streams.
`src/libged/joint/joint.c`, `src/libged/bot/bot_fuse.c`,
`src/libged/bot/check.cpp`, `src/libged/bot/bot.cpp`,
`src/libged/lint/lint.cpp`, `src/libged/gdiff/gdiff.c`, and
`src/libged/draw/preview.cpp` use that helper and the neutral feature-style
record instead of directly replacing, creating, coloring, or removing BSG line
features.  The lint and preview/bot face-plot paths now carry their existing
move/draw command streams through the adapter using neutral `BG_GEOMETRY_*`
line commands rather than forcing BSG geometry tokens to stay in the command
files.
The labelvert/labelface producers in `src/libged/illum/illum.c` and
`src/libged/nmg/nmg.c` now build neutral `ged_draw_view_label_data` arrays and
publish them through `ged_draw_view_context_labels_replace`; BSG label-data
structs, label feature creation, label replacement, and feature removal stay
inside the private GED draw view adapter.
The adapter now also exposes `ged_draw_view_line_layer_builder_replace` for
the retained BSG fallback used when no Obol diagnostic line-layer handler is
registered.  `src/libged/check/check_overlaps.c`,
`src/libged/gqa/gqa.cpp`, `src/libged/draw/draw.c`, and
`src/libged/vutil.c` now publish through `ged_diagnostic_line_layer_publish`
first and use that adapter helper for the fallback, so those files no longer
include `bsg/feature.h` or cast `bg_line_layer_builder` to BSG line-layer
builder types.  `src/libged/brep/plot.cpp` now uses the context-form
line-layer fallback adapter, and its separate indexed face-set publishing path
later moved through the same private context adapter boundary.
The same adapter now also exposes `ged_draw_view_line_layers_replace` for
named diagnostic line-layer arrays.  `src/libged/ged_util.cpp` uses
`ged_draw_view_line_layer_data` plus `BG_GEOMETRY_*` command constants for
uplot/qray/rtcheck/overlay feature publishing, removing direct
`bsg/feature.h`, `bsg/geometry.h`, `bsg_feature_line_layer`, and
`bsg_feature_replace_line_layers` use from the uplot parser/publisher.
The adapter now also owns prefix feature cleanup through
`ged_draw_view_features_remove_prefix`.  `src/libged/nirt/nirt.cpp` uses the
single-feature remove adapter for stale qray output cleanup, and
`src/libged/rtcheck/rtcheck2.cpp` uses the prefix cleanup adapter before
republishing rtcheck uplot output, removing direct feature remove, visit, and
record traversal from both command files.
The BRep indexed face-set publishing path now uses
`ged_draw_view_indexed_face_set_replace` and the neutral feature-style record,
so `src/libged/brep/plot.cpp` no longer includes `bsg/feature.h` or calls
`bsg_feature_replace_indexed_face_set` directly.
The evaluated-wireframe line-set builders in `src/libged/draw/bigE.c` and
`src/libged/wireframe_eval.c` now use `bg/line_layer.h` and
`BG_GEOMETRY_LINE_*` command tokens instead of including `bsg/geometry.h` for
line move/draw command values.
The typed overlay insertion record now uses a GED-private
`ged_draw_overlay_geometry_kind` enum rather than exposing
`bsg_geometry_node_kind`.  `src/libged/vdraw/vdraw.c` now fills that neutral
kind and uses `BG_GEOMETRY_LINE_*` commands for line overlays; the conversion
to BSG geometry node kinds remains in `src/libged/bsg_ged_draw_overlay.c`.
The mesh-LoD indexed-face update path in `src/libged/draw.cpp` now calls
`ged_draw_scene_ref_update_indexed_face_set`, leaving the temporary
`bsg_geometry_ref_update_indexed_face_set` operation inside
`src/libged/bsg_ged_draw_source.c`.  The now-unused raw
`ged_draw_scene_ref_geometry_ref` accessor was removed from the private draw
bridge surface.

The pivot guard now requires `src/libged/view/lines.c`,
`src/libged/view/labels.c`, `src/libged/view/axes.c`,
`src/libged/view/data_lines.c`, and `src/libged/view/gobjs.cpp` to use those
adapter helpers and rejects direct `bsg_feature_*`, `BSG_FEATURE_*`,
`BSG_GEOMETRY_*`, `BSG_OVERLAY_*`, and `struct bsg_axes` usage in the migrated
command files.  It also rejects direct BSG feature API use in
`src/libged/view/objs.cpp` and the `view vZ` implementation in
`src/libged/view/view.c`, plus direct BSG selection count reads in the same
view command file, direct BSG snap-result handling in the snap command file,
direct BSG HUD sync in the faceplate command file, and the migrated
diagnostic line-overlay producers.
It also rejects direct BSG feature API use in the migrated labelvert/labelface
label producers and the migrated diagnostic line-layer fallback producers.
It also rejects direct BSG feature or geometry use in the migrated uplot
line-layer feature publisher, and direct BSG feature cleanup use in the qray
and rtcheck cleanup paths.  It also rejects direct BSG feature use in the BRep
indexed face-set publishing path, and rejects BSG geometry command tokens from
the evaluated-wireframe line-set builders.  It also rejects direct BSG overlay
geometry access from `src/libged/vdraw/vdraw.c` and prevents
`bsg_geometry_node_kind` from returning to the private overlay geometry record.
It also rejects direct BSG geometry updates and raw draw geometry-ref access
from `src/libged/draw.cpp` while requiring the BSG-backed draw source to own
the indexed-face update bridge.
Broader non-feature edit paths and lower-level BSG/geometry bridge cleanup
remain comparable cleanup candidates.

The libged `adc` command path now uses neutral `struct rt_view_adc_state`
records and RT-owned ADC coordinate helpers for model/view/grid conversion and
reset behavior.  The retained BSG faceplate ADC get/set calls are confined to
`src/librt/view_legacy_bsg.c`, which now exposes neutral
`rt_view_adc_state_from_bsg` and `rt_view_adc_state_set_bsg` adapters beside
the older BSG-shaped transitional adapters.  The pivot guard now requires the
neutral ADC record/helpers in `src/libged/adc/adc.c` and rejects
`bsg/adc.h`, `struct bsg_adc_state`, the old BSG-shaped ADC adapter calls, and
direct BSG ADC math helpers from returning to that command file.

The standalone libged `grid` command path now follows the same pattern for
faceplate grid state.  `src/libged/grid/grid.c` uses neutral
`struct rt_view_grid_state` records and `rt_view_grid_state_from_bsg` /
`rt_view_grid_state_set_bsg` while the BSG-shaped grid record and
`bsg_view_grid_get` / `bsg_view_grid_set` calls remain inside the RT legacy
view adapter.  The command's `vsnap` operation already used the RT snap/grid
adapter and was left unchanged.  The pivot guard now rejects the old
BSG-shaped grid record and adapter calls from returning to
`src/libged/grid/grid.c`.

The `view faceplate grid` command path now uses the same neutral grid record
and adapters for its top-level draw toggles and subcommand dispatch.  The
temporary BSG grid state shape remains inside the RT legacy view adapter, and
the pivot guard now rejects `struct bsg_grid_state`, the older BSG-shaped
grid adapters, and direct BSG grid get/set calls from
`src/libged/view/faceplate/faceplate_grid.c`.

The libged faceplate center-dot, scale, and params command paths now use
neutral `struct rt_view_other_state` and `struct rt_view_params_state`
records.  `src/libged/view/faceplate/faceplate.c` now calls neutral
center-dot, scale-overlay, and params adapters, while the BSG-shaped
`bsg_other_state` / `bsg_params_state` records and direct
`bsg_view_center_dot_*`, `bsg_view_scale_state_*`, and `bsg_view_params_*`
calls remain confined to the RT legacy view adapter.  The pivot guard now
requires the neutral scalar records in the libged command file; qged and
libtclcad follow the same neutral scalar record pattern in follow-up
faceplate settings batches.

The libged `view faceplate model_axes` and `view faceplate view_axes` command
paths now use neutral `struct rt_view_axes_state` records and neutral RT
legacy axes adapters.  The BSG-shaped `bsg_axes` records and direct
`bsg_view_model_axes_*` / `bsg_view_view_axes_*` calls remain confined to the
RT legacy view adapter implementation and its focused tests.  The pivot guard
now requires the neutral axes record and neutral model/view axes adapters in
`src/libged/view/faceplate/faceplate_axes.c`.

The follow-up axes batch moved QGED view settings, qtcad Obol faceplate sync,
qtcad faceplate keybinding toggles, and TclCAD model/view axes command paths
onto the same neutral `struct rt_view_axes_state` adapters.  Those files now
read and write top-level axes draw, position, and size state without naming
the BSG-shaped axes record or old BSG-shaped RT wrappers.  The pivot guard now
rejects the old axes wrapper calls from those migrated qged, qtcad, and
TclCAD callers; a follow-up cleanup deleted those obsolete BSG-shaped RT
wrapper symbols from the adapter surface after the remaining callers moved to
the neutral state records.

The libdm/MGED axes draw-boundary batch moved the public libdm axes drawing
helpers onto neutral `struct rt_view_axes_state` records.  `include/dm.h` no
longer exposes `struct bsg_axes`, `src/libdm/axes.c` draws only the neutral
record, and MGED edit/model/view axes drawing now constructs neutral axes
state before calling libdm.  The remaining conversion from legacy
`bsg_axes` render payloads is local to `src/libdm/view.c`, where BSG render
items are bridged into neutral axes state before the libdm draw calls.  The
pivot guard now rejects public BSG axes draw signatures, BSG-shaped axes
state in the libdm axes implementation, and direct BSG axes draw handoffs
from the libdm view bridge and MGED axes drawing path.

The next faceplate settings batch moved the remaining QGED view settings
state and qtcad faceplate/keybinding state off BSG-shaped ADC, grid, scalar,
and ADC keybinding records.  `CADViewSettings.cpp` now uses neutral
`rt_view_adc_state`, `rt_view_grid_state`, `rt_view_axes_state`,
`rt_view_other_state`, and `rt_view_params_state` records for its top-level
faceplate controls.  qtcad Obol faceplate sync now consumes neutral ADC, grid,
and axes records before publishing Obol overlay nodes, and qtcad keybinding
handlers now toggle ADC state through `rt_view_adc_state_*` adapters.  The
direct qtcad BSG ADC/defines includes tied to those state records were also
removed, and the pivot guard now rejects the old BSG-shaped state records and
old RT faceplate wrappers from those migrated qged/qtcad files.

The TclCAD scalar faceplate batch moved `to_faceplate` center-dot,
view-params, and view-scale commands onto neutral `rt_view_other_state` and
`rt_view_params_state` records.  `src/libtclcad/view/faceplate.c` no longer
names the BSG-shaped scalar faceplate records or old scalar wrappers; the
BSG conversion remains confined to the RT legacy view adapter.  The pivot
guard now enforces the neutral scalar records for both libtclcad and qged
faceplate scalar callers.

The grid-state cleanup batch moved qged polygon grid-snapping controls,
qtcad sketch-test grid setup, and TclCAD command grid-snap decisions onto
neutral `rt_view_grid_state` records.  `QPolyCreate.cpp`, `QPolyMod.cpp`,
`src/libqtcad/tests/qsketch.cpp`, and `src/libtclcad/commands.c` no longer
name `bsg_grid_state` or call the older BSG-shaped grid wrappers for those
paths; the BSG grid conversion remains inside the RT legacy view adapter.
The pivot guard now requires the neutral grid-state adapters in those callers
and rejects the old wrappers from returning there.

The MGED grid-state batch moved the `mged_dm_grid_state_get/set` boundary,
`default_grid_state`, grid command/structparse storage, share/rset grid
copies, refresh draw checks, and motion/common-DM grid-snap reads onto
neutral `rt_view_grid_state` records.  `src/mged/grid.c`,
`src/mged/mged_dm.h`, `src/mged/share.c`, `src/mged/mged.c`,
`src/mged/doevent.c`, and `src/mged/dm-generic.c` no longer name
`struct bsg_grid_state` or call the older BSG-shaped grid wrappers for those
paths.  The pivot guard now rejects the old grid record and wrappers from
returning to the migrated MGED files.  The lower-level libdm grid draw
signature was handled in the follow-up draw-boundary cleanup below.

The MGED ADC-state batch moved `mged_dm_adc_state_get/set`, the `adc`
command, ADC draw/reset/conversion helpers, ADC status/scroll/keybinding
callers, share/startup copies, and motion/common-DM ADC reads onto neutral
`rt_view_adc_state` records.  `src/mged/adc.c` now calls the RT-owned
`rt_view_adc_model_to_view`, `rt_view_adc_grid_to_view`,
`rt_view_adc_view_to_grid`, and `rt_view_adc_reset` helpers instead of
maintaining BSG-shaped conversion/reset logic.  `src/mged/adc.c`,
`src/mged/attach.c`, `src/mged/buttons.c`, `src/mged/titles.c`,
`src/mged/scroll.c`, `src/mged/chgview.c`, `src/mged/share.c`,
`src/mged/mged.c`, `src/mged/doevent.c`, `src/mged/dm-generic.c`, and
`src/mged/mged_dm.h` no longer name `struct bsg_adc_state` or call the old
BSG-shaped ADC RT wrappers for those paths.  The pivot guard now rejects the
old ADC record and wrappers from returning to the migrated MGED files.  The
lower-level libdm ADC draw signature was handled in the follow-up
draw-boundary cleanup below.

The libdm ADC/grid draw-boundary cleanup moved the public `dm_draw_adc` and
`dm_draw_grid` signatures in `include/dm.h` onto neutral
`rt_view_adc_state` and `rt_view_grid_state` records.  `src/libdm/adc.c` no
longer includes `bsg/adc.h`, no longer names `struct bsg_adc_state`, and uses
the RT-owned ADC model/view/grid helper functions for its coordinate updates.
`src/libdm/grid.c` now draws from `rt_view_grid_state` without naming
`struct bsg_grid_state`.  The legacy HUD grid render-item path in
`src/libdm/view.c` still receives a BSG overlay payload, but it now copies
that payload into a neutral `rt_view_grid_state` before calling
`dm_draw_grid`, keeping the retained BSG dependency at the HUD/render bridge
instead of in the public draw helper.  The pivot guard now rejects the old
BSG-shaped ADC/grid draw signatures and helper dependencies from returning.

The interactive rectangle draw-boundary cleanup moved the public
`dm_draw_rect` signature in `include/dm.h` and the `src/libdm/rect.c`
implementation onto neutral `rt_view_interactive_rect_state`.  qtcad box
selection and TclCAD refresh/rectangle-refresh now use the neutral
interactive rectangle adapters for their top-level rectangle state, leaving
the BSG-shaped rectangle record in `src/librt/view_legacy_bsg.c`, its tests,
and libbsg internals.  The pivot guard now rejects the BSG-shaped rectangle
record, old BSG-shaped rectangle wrappers, and direct BSG rectangle get/set
calls from those migrated draw/tool paths.

The RT legacy faceplate wrapper-surface cleanup removed the obsolete
BSG-shaped `rt_view_*_from_bsg` / `rt_view_*_set_bsg` wrappers for ADC, grid,
model/view axes, center-dot, scale overlay, view params, and interactive
rectangle state.  `include/rt/view_legacy_bsg.h` now exposes only the neutral
`*_state_from_bsg` / `*_state_set_bsg` variants for those faceplate records,
`src/librt/view_legacy_bsg.c` keeps BSG record handling internal to those
neutral adapters, and the focused adapter test now verifies the neutral
records against the BSG backing state without calling the deleted wrappers.
The pivot guard now treats reintroducing the old BSG-shaped wrapper symbols
in the header, implementation, or focused test as a regression.

The next RT legacy wrapper-surface cleanup removed three more adapter/test-only
exports from `rt/view_legacy_bsg.h`: `rt_view_selection_const_bsg`,
`rt_view_snap_exclude_feature_from_bsg`, and
`rt_view_knob_state_hash_bsg`.  Production callers use the mutable selection
adapter, snap-exclude set/clear adapters, and view-level knob hash adapter
instead.  The focused adapter test now checks the remaining wrappers against
direct BSG backing-state queries where needed, and the pivot guard rejects
those retired helper symbols, plus the underlying direct const-selection BSG
getter, from returning to the legacy header, implementation, or test.

The libged native-selection sync cleanup moved the remaining direct BSG
selection/interaction record handling out of `src/libged/selection_state.cpp`.
The BSG-backed GED draw bridge now owns helpers for selected-path enumeration,
view selection clearing, and shape-ref selection adds; `selection_state.cpp`
uses those helpers while still using the RT view-set adapter for transitional
view iteration.  The pivot guard now requires those helper calls and rejects
direct BSG selection/interaction includes, `bsg_selection_*`,
`bsg_interaction_*`, `rt_view_selection_bsg`, and the older shape-ref
selection-record escape hatch from returning to that file.

The qtcad selection-filter cleanup moved pick-result selection application and
view-selection clearing behind RT legacy selection helpers.  `QgSelectFilter`
still owns legacy BSG pick-result production for the staged pick migration,
but it no longer includes BSG selection/interaction headers, stores
interaction results, calls `rt_view_selection_bsg`, or calls
`bsg_selection_*`/`bsg_interaction_*` directly.  The RT adapter test now covers
null, empty, callback-only, apply, and clear behavior for the new helpers, and
the pivot guard requires the helper route while rejecting direct selection API
use from the qtcad filter.

The qtcad selection-filter header cleanup made the protected selection helper
API view-implicit.  `QgSelectFilter::set_selected_result` and
`set_selected_paths` now resolve the installed filter view inside
`QgSelectFilter.cpp`, so `QgSelectFilter.h` no longer forward declares or names
`struct bsg_view` for those helpers.  The retained BSG-backed selection
application stays in the implementation behind the RT legacy adapter, and the
pivot guard now rejects `bsg_view` spelling from the public selection-filter
header.

The qtcad selection pick-result fallback cleanup moved the remaining direct
BSG pick-result ownership in `QgSelectFilter.cpp` behind opaque RT legacy
pick-result adapters.  Point and rectangle fallback picks now call
`rt_view_pick_point_bsg`, `rt_view_pick_nearest_bsg`, and
`rt_view_pick_rect_bsg`; result count/path/hit-distance/free operations, first
result filtering, and ray-hit result copying use `rt_view_pick_result_*_bsg`
helpers; and selection application now uses
`rt_view_selection_set_pick_result_ref_bsg`.  The RT adapter still owns the
temporary BSG implementation, but qtcad no longer includes `bsg/pick.h`, names
`bsg_pick_result` or `bsg_pick_record`, or calls `bsg_pick_*` directly.  The
focused adapter test covers the opaque pick-result helpers and the pivot guard
now rejects direct BSG pick API use from the qtcad selection filter.

The RT legacy selection wrapper-surface cleanup retired the old public
`rt_view_selection_set_pick_result_bsg` export from `rt/view_legacy_bsg.h`.
Selection pick application now exposes only
`rt_view_selection_set_pick_result_ref_bsg` for opaque
`rt_view_pick_result_bsg` handles, while the BSG-shaped pick-result conversion
and interaction application stay private inside `src/librt/view_legacy_bsg.c`.
The focused adapter test constructs pick results through
`rt_view_pick_result_create_bsg` / `rt_view_pick_result_append_path_bsg`, and
the pivot guard now requires the opaque helper tokens while rejecting the
retired public setter from the RT adapter header, implementation, and test.

The libged draw-test pick cleanup added
`rt_view_pick_semantic_path_bsg` and moved the remaining direct BSG pick-result
use in `view_command.cpp` and `selection_semantics.cpp` behind the opaque RT
legacy pick-result helpers.  `view_command.cpp` now checks semantic-path pick
results via `rt_view_pick_result_count_bsg` and
`rt_view_pick_result_path_bsg`, while `selection_semantics.cpp` creates point
pick results with `rt_view_pick_point_bsg` and applies them through
`rt_view_selection_set_pick_result_ref_bsg`.  Both tests dropped
`bsg/pick.h` and no longer name `bsg_pick_result`, `bsg_pick_record`, or call
`bsg_pick_*` directly.  The pivot guard now requires those RT adapter tokens
and rejects direct BSG pick/interaction access from the migrated test
surfaces.

The RT legacy selection getter cleanup retired the public
`rt_view_selection_bsg` selection-object export from `rt/view_legacy_bsg.h`.
The RT adapter now exposes only `rt_view_selection_available_bsg`,
`rt_view_selection_count_bsg`, `rt_view_selection_clear_bsg`, and opaque
pick-result application for migrated selection callers; the raw
`bsg_selection` lookup is private to `src/librt/view_legacy_bsg.c`.  The
focused RT adapter test and `selection_semantics.cpp` now assert selection
availability/count through those RT helpers and no longer include
`bsg/selection.h` or call `bsg_selection_count` directly.  The GED-private
draw bridge still owns the staged BSG selection object for record-level
contains/add/enumeration paths, but its selection-count and clear helpers now
route through the RT count/clear helpers.  The pivot guard requires the new
selection availability/count tokens and rejects the retired RT getter from the
adapter header, implementation, focused test, qtcad selection filter, and
migrated libged selection test surfaces.

The GED-private selection bridge cleanup removed the remaining raw
`ged_draw_view_selection_bsg` getter from `bsg_ged_draw_view_private.h`.
`bsg_ged_draw_view.c` now owns the staged BSG selection object internally and
exposes narrower private helpers for selection availability, record contains,
record add, and record replacement.  `bsg_ged_draw_records.c` now uses those
record-level helpers for selected-state queries, highlighted-shape mirroring,
and selection adds; it no longer includes `bsg/selection.h`, names
`struct bsg_selection`, or calls `bsg_selection_*` directly.  The pivot guard
now rejects the retired private getter and direct BSG selection calls from the
records/header surfaces while still allowing the transitional implementation
inside the GED view bridge.

The public GED draw interaction-record cleanup removed
`ged_draw_shape_interaction_record` from `ged/bsg_ged_draw.h` and dropped that
header's `bsg/interaction.h` dependency.  The draw-scene selection bridge test
now verifies selected state through `ged_draw_shape_record_get` and the neutral
`selected` field instead of constructing a `bsg_interaction_record` and
querying `bsg_selection` directly.  `bsg_ged_draw_records.c` keeps the
temporary BSG interaction-record construction as a private helper for the
staged selection backing path.  The pivot guard now rejects the retired public
interaction-record helper and BSG interaction types/includes from the public
GED draw header and rejects direct BSG interaction/selection checks from the
migrated draw-scene test.

The qged/TclCAD low-risk cleanup removed two more direct BSG call-site leaks.
The revolve, extrude, and BOT edit-preview plugins now publish
begin/update/commit lifecycle events through `qged_edit_preview_util.h`; the
shared helper owns the temporary BSG interaction-record bridge, and the plugin
files no longer include `bsg/interaction.h` or call `bsg_interaction_*`
directly.  TclCAD polygon/mouse/command snap callers now store
`rt_view_prepare_tcl_snap_bsg` results in the adapter's neutral integer mask
type instead of naming `bsg_snap_kind_mask`.  The pivot guard checks both
boundaries.

The TclCAD polygon-mode naming cleanup restored TclCAD-owned circle and
contour mode constants in `include/tclcad/defines.h` and moved the remaining
polygon/command callers off `BSG_POLY_CIRCLE_MODE` and
`BSG_POLY_CONTOUR_MODE`.  The values are unchanged, and the pivot guard now
rejects those BSG mode names from returning to the TclCAD caller surface.

The qged/qtcad polygon view-level cleanup added narrow
`rt_view_polygon_create_bsg`, `rt_view_polygon_select_bsg`,
`rt_view_polygon_find_bsg`, `rt_view_polygon_dup_bsg`,
`rt_view_polygon_visit_records_bsg`, `rt_view_polygon_snap_count_bsg`, and
`rt_view_polygon_clear_point_selection_bsg` adapters.  The qged create/modify
tools and qtcad polygon create/select filters now route view-level polygon
creation, selection, lookup, duplication, enumeration, snap-count, and
point-selection-clear calls through the RT legacy view adapter.  The retained
BSG polygon view queries now live in the adapter with focused adapter-test
coverage, and the pivot guard rejects the direct calls from returning to the
qged/qtcad polygon caller surface.

The qtcad polygon type/update-name cleanup added qtcad-owned polygon type and
update-mode constants in `QgPolyFilter.h` and moved qged/qtcad polygon callers
off `BSG_POLYGON_GENERAL`, `BSG_POLYGON_CIRCLE`, `BSG_POLYGON_ELLIPSE`,
`BSG_POLYGON_RECTANGLE`, `BSG_POLYGON_SQUARE`, and
`BSG_POLYGON_UPDATE_*` names.  Numeric values are unchanged for the staged
BSG-backed polygon implementation.  The pivot guard now rejects those BSG
type/update names from the migrated qged/qtcad polygon caller surface while
leaving BSG polygon refs, records, and object mutation calls for the later
polygon-object migration.

The qtcad polygon ref-spelling cleanup narrowed `QgPolyFilter.h` away from the
broad `bsg/polygon.h` include for its public polygon-reference member and added
qtcad-owned `qg_polygon_ref` plus `QG_POLYGON_REF_NULL_INIT` aliases backed by
the existing feature-ref storage.  qged create/modify plugins and qtcad polygon
filters now use the qtcad-owned ref spelling in their public/private caller
surface.  The pivot guard rejects raw `bsg_polygon_ref` and
`BSG_POLYGON_REF_NULL_INIT` spelling from returning to that migrated polygon
surface while intentionally leaving BSG polygon predicates, records, and object
mutation calls staged for the later polygon-object migration.

The next qtcad/qged polygon-object cleanup added RT legacy adapters for the
remaining low-level BSG polygon object operations used by the Qt polygon tools:
reference null checks, record reads, update/update-from-screen, close/open,
view/style/name mutation, point-selection clear, move, CSG/remove, and
user-data get/set.  `QgPolyFilter`, `QPolyCreate`, and `QPolyMod` now call
those adapters rather than the BSG polygon object functions directly.  The
focused RT adapter test covers the new wrapper surface, and the pivot guard
rejects the direct BSG function calls from returning to the migrated polygon
caller surface.  The BSG polygon record struct and sketch import/export helpers
remain staged for a later neutral-record/sketch bridge cleanup.

The qtcad/qged polygon-record cleanup added the neutral
`rt_view_polygon_record` structure in `rt/view.h`, changed the RT legacy
polygon record getter and visitor callback to return that neutral record, and
confined BSG-to-RT record copying to `src/librt/view_legacy_bsg.c`.
`QgPolyFilter`, `QPolyCreate`, `QPolyMod`, and `QPolySettings` now use the
qtcad-owned `qg_polygon_record` alias and no longer include `bsg/polygon.h` or
name `struct bsg_polygon_record` directly.  The pivot guard now requires the
neutral/qtcad record tokens and rejects direct BSG polygon record spelling or
direct BSG polygon includes from the migrated qtcad/qged polygon caller
surface.  Sketch import/export remained the next staged polygon bridge cleanup.

The qged polygon sketch-bridge cleanup added
`rt_view_polygon_import_sketch_bsg` and `rt_view_polygon_export_sketch_bsg`
to the RT legacy view adapter.  `QPolyCreate` and `QPolyMod` now route sketch
import/export through those wrappers instead of including
`rt/primitives/sketch_legacy_bsg.h` or calling
`db_sketch_to_view_polygon_ref` / `db_view_polygon_ref_to_sketch` directly.
The lower-level BSG-backed sketch conversion implementation remains confined
to `src/librt/primitives/sketch/polygons_legacy_bsg.c`, and the pivot guard
rejects the direct sketch bridge from returning to qged polygon callers.

The qtcad/qged polygon-ref storage cleanup moved the caller-facing polygon ref
alias from `bsg_feature_ref` to RT-owned `rt_view_polygon_ref`.  The polygon
members, vectors, callbacks, and qged plugin state still use the local
`qg_polygon_ref` spelling, but that alias is now backed by `rt/view.h` rather
than `bsg/feature.h`.  The RT legacy polygon adapter converts between
`rt_view_polygon_ref` and BSG feature refs internally for the staged BSG-backed
implementation, and qged polygon line-snap exclusion now uses
`rt_view_polygon_snap_exclude_set_bsg` instead of passing polygon refs to the
generic feature-exclude setter.  The pivot guard rejects direct
`bsg_feature_ref` spelling and `bsg/feature.h` includes from the migrated
qtcad/qged polygon surface.

The qged polygon widget-boundary cleanup removed the private `bsg_view`
accessors and qtcad view/filter header includes from the create/modify/settings
widget headers.  `QPolyCreate`, `QPolyMod`, and `QPolySettings` now resolve the
active staged view through `QgPluginContext` inside implementation files, and
polygon view-name validation no longer reaches through `gedp->ged_gvp`.
`QPolySettings::uniq_obj_name` now also supports its validation-only null
output-buffer mode and releases its temporary name buffers, fixing the staged
UI collision check while preserving the RT legacy unique-name adapter boundary.

The qged view-plugin header/view-name cleanup added `rt_view_name_from_bsg`
to the RT legacy view adapter and covered normal, empty, and null view-name
queries in `rt_view_legacy_bsg`.  `CADViewModel` and `HostStatusCommon` now use
that adapter instead of reading `gv_name` directly.  `CADViewSelector` and
`CADViewMeasure` no longer include the qtcad filter headers or `ged.h` from
their widget headers, and their event filters resolve active staged views
through `QgPluginContext` instead of `gedp->ged_gvp`.  The pivot guard now
requires those routes and rejects direct field/header regressions from the
migrated qged view-plugin surface.

The qtcad plugin-context boundary cleanup removed the public `bsg_view`
forward declaration, `viewAccessor`, and `getView()` convenience wrapper from
`QgPluginContext`.  qged view/info/settings/status, polygon, and edit-preview
plugin implementations now resolve the remaining staged compatibility pointer
from `getViewWidget()` first and fall back to `QgModel::session()->activeView()`
for startup/null-host behavior.  The pivot guard now rejects public
`QgPluginContext` BSG active-view exposure and old qged plugin `getView()` call
sites.  After rebuilding the stale `test_qtcad_plugin_reuse` binary against the
new header layout, the plugin reuse test passed with the updated plugins.

The qtcad view-filter boundary cleanup removed the public raw
`QgViewFilter::set_view`/`view` API and the `struct bsg_view` forward
declaration from `QgViewFilter.h`.  `QgView::installFilter` now supplies only
the owning `QgView` widget, and `QgViewFilter::view_sync` plus the selection,
measurement, polygon, and sketch filter implementations derive the retained
compatibility view from `view_widget()` locally.  qged select/measure/polygon
plugins now set filter widgets rather than raw views, and the focused
pick/measure/snap tests do the same.  The pivot guard locks the public header
and install path against the retired raw filter view API.

The qtcad public-header spelling cleanup removed an unused `struct bsg_view`
forward declaration from `QgModel.h` and rewrote the stale `QgSketchFilter.h`
usage note to avoid exposing retained view pointers to callers.  The pivot
guard now rejects `bsg_view` spelling and BSG includes from both headers.

The qged framebuffer-server header cleanup removed the direct
`dm/fbserv.h` include from `src/qged/fbserv.h`.  A later tightening removed
the retained `fbserv_obj` type from that header entirely: the Qt helper
classes now keep opaque `void *` server state, while `src/qged/fbserv.cpp`
owns the retained libdm framebuffer-server include, callback signatures, and
field access.  The pivot guard now rejects libdm/BSG header exposure, retained
`fbserv_obj` spelling, and raw qdm framebuffer-server callback prototypes from
the qged framebuffer-server header while requiring the implementation file to
own the compatibility dependency.

The public GED fbserv include cleanup removed `dm/fbserv.h` from
`include/ged/defines.h`.  The retained `ged_fbs` member is still present for
the staged framebuffer-server bridge, but the public GED defines header now
forward declares `struct fbserv_obj`; `ged.cpp`, `ged_util.cpp`, qged app
setup, and ERT include the libdm fbserv header at the implementation boundary
where they allocate, dereference, or call the retained framebuffer-server API.
The pivot guard now rejects public `dm/fbserv.h` exposure from
`include/ged/defines.h`.

The qtcad input-binding cleanup deleted the obsolete `bindings.h` and
`bindings.cpp` free-function layer.  `QgCanvasInput` had already replaced that
API with per-canvas input handlers and per-instance drag-update state, and the
old files were no longer built or called.  `src/libqtcad/CMakeLists.txt` no
longer inventories the dead files, and the pivot guard now requires
`QgCanvasInput.cpp` as the only qtcad input adapter path while rejecting the
deleted files and `CAD*Event` free-function tokens from returning.

The follow-on qtcad input handle cleanup moved `QgCanvasInput.h` from retained
`struct bsg_view *` parameters to the opaque `qg_legacy_view *` boundary and
moved the drag bounds-update callback maps behind an implementation pimpl.
`QgCanvasInput.cpp` now owns the explicit `QgLegacyViewBsg.h` conversion and
the `rt_view_bounds_update_callback_bsg_t` storage needed by the retained
legacy view path.  `QgGL` and `QgSW` adapt their current staged BSG view only
at the input-handler call sites, and the pivot guard now rejects BSG view
spelling from `QgCanvasInput.h` while requiring the implementation-side
adapter tokens.

The qtcad/qged active-view sync cleanup added
`qg_obol_display_accepts_ged_active_view` as a narrow transitional helper for
Obol overlay, selection, and draw-observer display acceptance.  The helper
keeps the remaining GED active-view comparison in `QgObolViewSync.cpp`, while
`QgObolSelectionSync`, `QgObolOverlaySync`, and the qged observer handlers call
the helper instead of comparing `display->view()` with `gedp->ged_gvp`
directly.  The public helper header forward declares `struct ged` and `QgView`
without exposing BSG view types.  The same helper surface now includes
`qg_obol_display_accepts_draw_transaction_view`, keeping qged draw-observer
transaction-view scoping out of app code as well.  The pivot guard now enforces
both helper routes plus the CMake registrations.

The qtcad model/tree draw-state cleanup added `QgModel::drawnPathState` for
tree and model-display queries.  `QgTreeView::do_draw_toggle` now asks the
model for draw state instead of reading `m->ged()->ged_gvp` and calling
`ged_draw_path_state` itself, and `QgModel::data` routes `DrawnDisplayRole`
through the same helper.  `QgModel.cpp` keeps the remaining active-view lookup
behind a local `qgmodel_active_view` helper for draw-state queries and
draw/erase transaction setup.  The pivot guard now rejects direct GED/BSG
draw-state view access from `QgTreeView` and keeps the model's direct
`ged_gvp` usage confined to that helper.

The qgmodel model-tester draw-state cleanup moved the test's canonical draw
state assertions onto `QgModel::drawnPathState` as well.  The test no longer
includes `ged/bsg_ged_draw.h`, names `ged_draw_path_state`, or reads
`gedp->ged_gvp` / `event_gedp->ged_gvp`; direct GED command execution remains
where the event-transaction bridge itself is under test.  The pivot guard now
rejects those direct draw-state view-access patterns from returning to the
qtcad model tester.  Verified with `ninja -C build test_qgmodel_model_tester`
and CTest for `brlobol_pivot_guard` plus `test_qgmodel_model_tester`.

The qtcad session active-view cleanup added `QgSession::activeView` as the
session-owned transitional reader for GED's current BSG-backed view.  The
qtcad model draw-state helper now asks the session for the active view instead
of reading `ged_gvp`, and qged's plugin-context fallback view accessor uses
the same session method when the main window has not been constructed yet.
The write side is now owned by `QgSession::setActiveView` as well: qged's
startup current-view assignment and quad-view switch handling call the session
setter instead of writing `ged_gvp` directly.  The qged main-window public
interface no longer exposes `CurrentView()` as a BSG-returning method; qged app
code resolves the current display's staged view through an implementation-local
helper when it still needs the legacy view pointer.  The pivot guard now
requires the session read/write accessors, keeps `QgModel.cpp` and
`QgEdApp.cpp` free of direct `ged_gvp` reads/writes, and rejects the removed
main-window active-view surface.

The qtcad quad-view active-view cleanup moved `QgQuadView` from a raw GED
constructor argument to `QgSession`.  Quad-view still uses the GED context for
the staged BSG view set and for replaying visible paths into newly-created
quadrants, but startup active-view selection, temporary per-quadrant draw
propagation swaps, and quad-mode reset now use `QgSession::activeView` and
`QgSession::setActiveView` rather than writing `ged_gvp` directly.  The pivot
guard now rejects the raw-GED quad-view constructor and direct `ged_gvp` writes
from returning to the quad-view path.  The unused public
`QgQuadView::view(int)` accessor has also been removed, so `QgQuadView.h` no
longer exposes raw GED or BSG view spelling.

The qged app active-view cleanup continued by routing command-time unit
conversion and palette element dimension refresh through `QgSession::activeView`
instead of asking the current display widget for its staged `view()` pointer.
The pivot guard now rejects the old qged patterns that pulled `qged_current_view`
for unit conversion or called `rt_view_dimensions_set_bsg(curr_view->view(), ...)`.

The qged app legacy-view adapter cleanup added neutral qtcad helper functions
on the opaque `qg_legacy_view *` handle for dimension and unit-conversion
writes.  `QgLegacyView.cpp` now owns the explicit `QgLegacyViewBsg.h` cast and
`rt/view_legacy_bsg.h` calls, while `QgEdApp.cpp` calls
`qg_legacy_view_dimensions_set` and `qg_legacy_view_unit_conversion_set` on the
session active view.  This removes the direct staged-BSG cast header and RT
legacy-view adapter include from qged app code for those command/palette paths.
The pivot guard now requires the new helper source/build registration and
rejects the old direct adapter tokens from `QgEdApp.cpp`.  Verified with
`ninja -C build libqtcad qged` and CTest for `brlobol_pivot_guard`,
`ged_test_qged_swrast`, and `test_qtcad_plugin_reuse`.

The qtcad view-control toolbar cleanup moved `QgViewCtrl` from a raw GED
pointer to `QgSession`.  Framebuffer clear/raytrace command paths still
retrieve the GED context from the session, while framebuffer mode toggle/icon
updates use `QgSession::activeView` through an implementation-local helper.
`QgViewCtrl.h` no longer exposes raw GED or BSG view spelling for that path,
and the pivot guard enforces the session constructor plus rejects direct
`ged_gvp` reads in the toolbar.

The qtcad view-control framebuffer-mode cleanup added neutral
`qg_legacy_view_framebuffer_mode_get` and
`qg_legacy_view_framebuffer_mode_set` helpers on the opaque legacy-view
handle.  `QgViewCtrl.cpp` now calls those helpers on the session active view
instead of including `QgLegacyViewBsg.h` and calling the RT legacy
framebuffer-mode adapters directly.  The pivot guard now requires the helper
surface and rejects the old direct adapter tokens from returning to the
toolbar.  Verified with `ninja -C build libqtcad qged` and CTest for
`brlobol_pivot_guard`, `ged_test_qged_swrast`, and
`test_qtcad_plugin_reuse`.

The qtcad GL/SW framebuffer-size cleanup extended `QgLegacyView.cpp` with
opaque width and height getters and routed `QgGL.cpp` / `QgSW.cpp` viewport
dimension synchronization through `qg_legacy_view_dimensions_set`,
`qg_legacy_view_width_get`, and `qg_legacy_view_height_get`.  The retained RT
dimension adapters remain in the qtcad helper implementation, while the
canvas widgets no longer call those get/set wrappers directly.  The pivot
guard now rejects `rt_view_dimensions_set_bsg`, `rt_view_width_from_bsg`, and
`rt_view_height_from_bsg` from returning to those widget files.  Verified with
`ninja -C build libqtcad qged` and CTest for `brlobol_pivot_guard`,
`ged_test_qged_swrast`, and `test_qtcad_plugin_reuse`.

The qtcad quad-view unit/size copy cleanup added opaque
`qg_legacy_view_local2base_get` and `qg_legacy_view_base2local_get` helpers
and moved `QgQuadView.cpp` initial quadrant unit/dimension copies plus the
post-draw-propagation dimension refresh onto the `QgLegacyView` helper
surface.  The remaining retained BSG view-set, LoD policy, and autoview calls
stay in `QgQuadView.cpp` for later slices, but direct RT unit/dimension
get/set wrappers are now confined to `QgLegacyView.cpp` for this path.  The
pivot guard rejects those wrappers from returning to the quad-view unit/size
copy path.  Verified with `ninja -C build libqtcad qged` and CTest for
`brlobol_pivot_guard`, `ged_test_drawing_quad`,
`ged_test_bsg_quad_stability`, `ged_test_qged_swrast`, and
`test_qtcad_plugin_reuse`.

The qged plugin-context active-view cleanup moved the startup/null-host
fallback from each plugin implementation into `QgPluginContext::activeView`.
The new accessor returns the existing opaque `qg_legacy_view *` handle: it
prefers the active `QgView` widget and falls back to `QgModel::session()` only
inside libqtcad.  View, polygon, host-status, and edit-preview qged plugins
now call `ctx->activeView()` and convert to the retained staged view pointer
only where they still invoke legacy RT/BSG APIs.  The pivot guard now requires
the opaque plugin-context accessor, keeps BSG conversion out of
`QgPluginContext.cpp`, and rejects qged plugins from reintroducing
open-coded `ctx->model->session()->activeView()` fallback logic.

The qtcad view-filter and draw-sync test include cleanup removed stale direct
BSG dependencies.  `QgViewFilter.cpp` now gets snap-source, snap-kind,
mouse-state, and tolerance access exclusively through `rt/view_legacy_bsg.h`
instead of including `bsg/defines.h` directly.  The draw-sync test no longer
includes `bsg/util.h` and now routes active-view, transaction-view, center,
and scale setup through qtcad legacy-view helpers; it still uses
`ged/bsg_ged_draw.h` for the retained GED transaction records it intentionally
exercises.  The pivot guard now rejects stale direct BSG view setup from
returning to this path, and the focused build/tests passed:
`ninja -C build libqtcad qged`, `ninja -C build test_qtcad_obol_draw_sync`,
and CTest for `brlobol_pivot_guard`, `test_qtcad_obol_pick`,
`test_qtcad_obol_snap`, `test_qtcad_obol_measure`,
`test_qtcad_plugin_reuse`, and `test_qtcad_obol_draw_sync`.

The qtcad canvas/session legacy-view boundary cleanup introduced
`QgLegacyView.h` as the public opaque handle declaration and
`QgLegacyViewBsg.h` as the explicit transitional BSG adapter.  `QgCanvasBase`,
`QgView`, `QgSession`, `QgGL`, and `QgSW` now expose `qg_legacy_view *` rather
than `struct bsg_view *`; BSG conversion is confined to implementation files,
qged plugin helpers, the Qt libdm framebuffer bridges, and tests that
intentionally exercise retained staged BSG state.  The focused qtcad Obol
tests were updated to use the adapter explicitly, and the pivot guard now
requires the new headers while rejecting BSG view exposure from the main
public qtcad canvas/session headers.

The qtcad dense real-model test was split into named CTest entries with
explicit 180-second timeouts, and the dense m35 pick-all workflow is now a
separate `m35_wire_pick_all_stress` case.  The normal m35 interaction case
uses nearest-pick semantics for the identity assertions it actually needs,
while the stress case asserts multiple pick-all hits and nearest-first
ordering.  `QgObolPick.cpp` now appends pick-all records and stable-sorts
after Obol/source-detail/RT fallback records are merged, replacing the old
per-hit middle insertion path that made dense wireframe pick-all effectively
quadratic.  The pivot guard now requires the append/sort helper and rejects
the old insert loop, and the focused qtcad suite passed with the stress case
under the per-test cap.

The qtcad private canvas-state boundary now stores opaque `qg_legacy_view *`
handles for both the active view and the widget-owned local view.  `QgGL` and
`QgSW` allocate/free the retained BSG view through explicit local adapter
variables, return the opaque handle from `view()`, and pass that opaque handle
to `qgcanvas_set_view`; legacy RT/DM calls convert at the implementation
boundary through `QgLegacyViewBsg.h` and the `qgcanvas_bsg_view` helpers.
The pivot guard now requires `QgCanvasState.h` to keep the opaque fields and
rejects reverting `qgcanvas_set_view` to a raw `struct bsg_view *` signature.
The shared canvas-state dimension sync and canvas input motion-scale height
read now use the opaque qtcad legacy-view helpers too, so those direct RT
legacy BSG size wrappers are no longer named in the canvas state/input files.
The qtcad view, sketch, and selection filters now use the opaque legacy-view
width/height helpers for snap tolerance, vertex-snap scaling, and
box-selection rectangle dimensions.  Their remaining BSG adapter use is
limited to the retained staged snap, transform, selection, and pick APIs.
The QGED view settings plugin now uses the same opaque qtcad legacy-view
helper surface for LoD policy, framebuffer mode, and ADC/grid/axes/scalar
faceplate state get/set operations.  `CADViewSettings.cpp` still edits
neutral `rt/view.h` records, but it no longer includes the qtcad BSG adapter,
`rt/view_legacy_bsg.h`, or direct RT legacy-BSG state wrappers.
The qged view-info and host-status plugins now use the opaque qtcad
legacy-view helper surface for view info, view name, AET, and center reads.
Those small qged plugins no longer include `QgLegacyViewBsg.h` or
`rt/view_legacy_bsg.h`; the retained read-only RT legacy-BSG calls are
confined to `QgLegacyView.cpp`.
The qged measure and selector plugins now use opaque `qg_legacy_view *`
handles for their active-view presence checks instead of converting to
`struct bsg_view *`.  The actual measurement and selection interaction logic
remains in the libqtcad filters, but the qged plugin files no longer include
`QgLegacyViewBsg.h`, call `qg_legacy_view_to_bsg`, or spell `bsg_view`.
The qged polygon settings widget now routes view-object unique-name checks
through `qg_legacy_view_unique_object_name`, leaving the retained
`rt_view_unique_object_name_bsg` call in `QgLegacyView.cpp`.  `QPolySettings`
no longer includes the qtcad BSG adapter or `rt/view_legacy_bsg.h`.
The qged polygon create/modify plugins now route their remaining view-backed
polygon and snap operations through `QgLegacyView.h` as well.  `QgLegacyView`
owns wrappers for polygon duplicate/find/visit, sketch import/export, snap
source/line/exclude state, grid snap state, alignment view writes, polygon
ref metadata, CSG, visual updates, removal, and user data.  `QPolyCreate.cpp`
and `QPolyMod.cpp` no longer include `QgLegacyViewBsg.h` or
`rt/view_legacy_bsg.h`, call `qg_legacy_view_to_bsg`, spell
`struct bsg_view`, or call the RT legacy-BSG polygon/view wrappers directly.
The pivot guard now requires those qtcad helper routes and rejects direct BSG
adapter tokens from returning to the qged polygon create/modify files.
The same wrapper surface now covers `QgPolyFilter.cpp`: screen-point
conversion, polygon create/select, screen-point updates, close, move,
name/visual updates, CSG, removal, and record/ref checks route through
`QgLegacyView.h`.  The filter no longer includes `QgLegacyViewBsg.h` or
`rt/view_legacy_bsg.h`, calls `qg_legacy_view_to_bsg`, spells
`struct bsg_view`, or names retained RT legacy-BSG polygon helpers directly.
FreeBSD/clang caught that the screen-point adapter needs a mutable staged
view handle, so `qg_legacy_view_screen_point_get` intentionally takes
`qg_legacy_view *`.
The qtcad measurement filter now uses that same opaque helper boundary for
its remaining staged view reads.  `QgLegacyView.h` owns screen-to-view and
view-to-model wrappers, and `QgMeasureFilter.cpp` no longer includes
`QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
`qg_legacy_view_to_bsg`, spells `struct bsg_view`, or names
`rt_view_screen_to_view_from_bsg` / `rt_view_view2model_from_bsg` directly.
The pivot guard requires those qtcad helper calls and rejects the direct
measurement adapter tokens from returning.
The qtcad sketch filter now follows the same pattern for its remaining
retained view reads.  `QgLegacyView.h` owns the added model-to-view wrapper
beside the screen-to-view, screen-point, and width helpers, and
`QgSketchFilter.cpp` no longer includes `QgLegacyViewBsg.h` or
`rt/view_legacy_bsg.h`, calls `qg_legacy_view_to_bsg`, spells
`struct bsg_view`, or names `rt_view_model2view_from_bsg`,
`rt_view_screen_to_view_from_bsg`, or `rt_view_screen_point_from_bsg`
directly.  The pivot guard now requires the opaque sketch helper tokens and
rejects the old direct adapter path.
Building the standalone `qsketch` fixture on FreeBSD/clang exposed a retained
test/demo boundary mismatch: its retained staged view setup had not yet
matched `QgView::set_view`'s opaque `qg_legacy_view *` API.  The first fix
adapted the raw view through the explicit `QgLegacyViewBsg.h` boundary; the
later qsketch cleanup moved local view ownership and routine view-state setup
onto the qtcad legacy-view helper surface, leaving that private bridge only
for retained edit and sketch line-set scene graph calls.
The shared qtcad view filter is now on the opaque helper boundary as well.
`QgLegacyView.h` owns qtcad-local snap kind constants plus helpers for DB snap
eligibility, snap kind mask translation, view size, snap tolerance factor,
current point get/set, and mouse-state synchronization.  `QgViewFilter.cpp`
uses those helpers for Obol snap refinement and event-stream synchronization,
so it no longer includes `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
`qg_legacy_view_to_bsg`, spells `struct bsg_view`, names RT legacy-BSG snap
policy helpers, or names `RT_VIEW_SNAP_*_BSG` constants directly.  The pivot
guard now requires the qtcad helper route and rejects the direct shared-filter
adapter path.

The qtcad session and Obol active-view sync boundary now use the same opaque
legacy-view helper surface.  `QgLegacyView.h` exposes helpers to get/set GED's
active staged view and to read a draw transaction's scoped view as
`qg_legacy_view *`, leaving the retained BSG cast and direct `ged_gvp` /
`txn->view` reads in `QgLegacyView.cpp`.  `QgSession.cpp` and
`QgObolViewSync.cpp` no longer include `QgLegacyViewBsg.h`, call
`qg_legacy_view_to_bsg` / `qg_legacy_view_from_bsg`, or read those raw GED draw
view fields directly.  The pivot guard now requires the opaque helper route
and rejects direct BSG/GED active-view access from returning to those files.
`QgView.cpp` now follows that same boundary for semantic refresh requests:
qtcad-local `QG_LEGACY_VIEW_REFRESH_*` flag names replace direct
`RT_VIEW_REFRESH_*_BSG` spelling, and the staged-view refresh request goes
through `qg_legacy_view_refresh_request`.  The wrapper widget no longer
includes `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, and the pivot guard
locks that helper route while leaving the lower canvas backend internals on
their retained RT legacy refresh path.
The qtcad input adapter also moved its simple key-driven view state updates
onto the opaque helper surface: `QgCanvasInput::keyPressEvent` now toggles
ADC, model axes, and view axes through `qg_legacy_view_*_state_get/set`
helpers and applies AET view presets through `qg_legacy_view_aet_set` plus
`qg_legacy_view_update`.  The mouse/wheel adjustment path, drag-center lookup,
and drag bounds callback suspend/restore now go through
`qg_legacy_view_adjust`, `qg_legacy_view_center_get`, and an opaque
`qg_legacy_view_bounds_update_state` token, so `QgCanvasInput.cpp` no longer
includes `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
`qg_legacy_view_to_bsg`, spells `struct bsg_view`, or names the retained RT
legacy input adapter functions.  The pivot guard requires the input helper
route and rejects the old direct RT legacy faceplate/AET/adjust/bounds calls
from returning.
`QgModel.cpp` now uses the same opaque helper surface for draw-view scoping:
drawn-state queries call `qg_legacy_view_draw_path_state`, blank-slate draw
checks call `qg_legacy_view_draw_has_paths`, and draw/erase transactions set
their scoped view through `qg_legacy_view_draw_transaction_view_set`.  The
retained `ged_draw_path_state`, `ged_draw_has_paths`, and transaction
`bsg_view *` assignment stay inside `QgLegacyView.cpp`; the model no longer
includes `QgLegacyViewBsg.h`, calls `qg_legacy_view_to_bsg`, or spells
`struct bsg_view`.  The pivot guard rejects those direct draw-view access
patterns from returning to `QgModel.cpp`.

The Obol draw transaction sync now consumes that helper surface too.
`QgLegacyView.h` exposes `qg_legacy_view_draw_list_paths`, and
`QgObolDrawSync.cpp` gets transaction-scoped views through
`qg_legacy_view_draw_transaction_view_get`, enumerates full-scene paths
through `qg_legacy_view_draw_list_paths`, and resolves per-path draw mode with
`qg_legacy_view_draw_path_state`.  The sync implementation no longer includes
`QgLegacyViewBsg.h`, calls `qg_legacy_view_to_bsg`, spells `struct bsg_view`,
or calls `ged_draw_path_state` / `ged_draw_list_paths` directly.  The pivot
guard locks that route while leaving the retained GED transaction mode
constants in place.

qtcad mouse-adjust mode spelling has also moved behind the opaque legacy-view
boundary.  `QgLegacyView.h` now exposes `QG_LEGACY_VIEW_ADJUST_*` values that
are numeric-compatible with the retained RT adjustment backend, and the qtcad
input/controller/canvas initialization code uses those names instead of
`RT_VIEW_ADJUST_*`.  `QgViewCtrl.cpp` no longer includes
`rt/view_legacy_bsg.h`; the remaining canvas backend files keep that include
only for retained BSG view construction and DM/libdm bridge operations.  The
pivot guard now requires the qtcad adjust constants and rejects direct RT
adjust constants from the migrated qtcad paths.

The qtcad quad-view lifecycle now follows the same opaque helper pattern.
`QgLegacyView.h` exposes helpers for staged view naming, GED view-set
add/remove/attach, LoD policy copy, default autoview, and LoD bounds update.
`QgQuadView.cpp` uses those helpers for quadrant setup and layout transitions,
so it no longer includes `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
`qg_legacy_view_to_bsg`, spells `struct bsg_view`, or names the retained RT
view-set/LoD/autoview adapters directly.  The pivot guard now requires that
helper route and removes `QgQuadView.cpp` from the direct transitional
view-set allowlist.

The long `havoc_wire` qtcad real-model runtime was traced with
`BRLOBOL_QTCAD_REAL_MODEL_TIMING=1`.  Before the fix the case spent about
22.2 seconds in the legacy GED draw transaction, about 0.5 seconds in Obol
sync, about 0.1 seconds in render/readback, and about 49.0 seconds in the
test helper's geometry-count validation.  That helper was quadratic because
it called `getRealizedShape(i)` for each realized shape, and each indexed
lookup recursively searched the scene graph from the root.  The test now
counts realized wire shapes/segments and mesh shapes/triangles with one
recursive traversal, preserving the validation while reducing the count phase
to about 0.010 seconds.  The focused qtcad suite now reports `havoc_wire` at
about 23.6 seconds and 20/20 focused tests pass in about 50 seconds.

The qged edit-preview feature-boundary cleanup moved the ELL, revolve,
extrude, and BOT plugin feature refs and overlay operations behind
`qged_edit_preview_util.h`.  The plugins now use `qged_edit_feature_ref` plus
helper calls for preview overlay ensure/remove, visibility, view assignment,
color/touch updates, lifecycle callback attachment, and ELL edit labels.  The
plugin files no longer include `bsg/feature.h`, `bsg/overlay.h`, or
`bsg/hud.h`, name `bsg_feature_ref`, or call `bsg_feature_*` directly.  The
helper now uses qged-owned line-command tokens for move/draw/point preview
geometry, so it no longer includes `bsg/geometry.h` or spells
`BSG_GEOMETRY_*` command names.  The retained BSG feature/overlay operations
have also been moved out of that header into
`qged_edit_preview_util.cpp`, so `qged_edit_preview_util.h` no longer includes
`bsg/feature.h`, names `bsg_feature_ref` / `bsg_edit_preview_ops`, or spells
`BSG_FEATURE_*`, `BSG_OVERLAY_*`, or `BSG_ANCHOR_*` constants directly.  The
edit-preview interaction-record create/free bridge has also been moved out of
that header into `qged_edit_preview_util.cpp`, so `qged_edit_preview_util.h`
no longer includes `bsg/interaction.h`, names `struct bsg_interaction_record`,
or maps `BSG_EDIT_PREVIEW_*` operation constants directly.  The pivot guard
now enforces that boundary for the four edit-preview plugins while continuing
to require update/commit lifecycle events to publish through the helper.  The
same qged edit-preview cleanup removed the stale direct `ged/bsg_ged_draw.h`
includes from the ELL, revolve, extrude, and BOT plugin implementations and
removed their private `bsg_view`-returning `getView()` declarations from the
plugin headers.  The shared qged edit-preview helper API now takes
`QgPluginContext` for staged view access, adapts qged-owned callback signatures
to BSG callbacks inside `qged_edit_preview_util.cpp`, and keeps direct
`bsg_view` spelling out of the helper header and plugin files.

The qtcad selection filter now routes its remaining staged pick and view
snapshot work through `QgLegacyView.h`.  `QgLegacyView` owns opaque
`qg_legacy_view_pick_result` handles plus helpers for point/nearest/rectangle
fallback picks, result create/free/count/path/hit-distance/copy/filter-first,
selection apply/clear, interactive rectangle get/set, and screen-ray
construction.  `QgSelectFilter.h` no longer forward declares
`rt_view_pick_result_bsg`, and `QgSelectFilter.cpp` no longer includes
`QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
`qg_legacy_view_to_bsg`, spells `struct bsg_view`, or calls direct
`rt_view_*_bsg` pick/selection/view snapshot helpers.  The retained RT legacy
BSG calls now live in `QgLegacyView.cpp`, and the pivot guard has been flipped
to require the qtcad helper route while rejecting direct BSG adapter tokens
from returning to the selection filter.

The qged application draw-observer boundary now follows the same opaque helper
route.  `QgLegacyView` exposes qtcad-local helpers for GED draw observer
registration/removal, manual redraw transactions, and transaction view-presence
checks; `QgEdApp.cpp` uses those helpers instead of including
`ged/bsg_ged_draw.h`, calling `ged_draw_observer_*`,
`ged_draw_transaction_make`, or `ged_draw_apply_transaction` directly, or
reading `txn->view`.  The retained GED draw adapter calls and transaction field
access are confined to `QgLegacyView.cpp`, and the pivot guard now requires
the helper route while rejecting those direct tokens from returning to qged.

The qtcad canvas-state/backend bridge moved another batch of staged view access
behind `QgLegacyView`.  `QgLegacyView` now owns local view create/free helpers,
refresh consume/complete, rotation/scale/perspective/hash reads, display-manager
attachment, and scale-storage access.  `QgCanvasState.h` uses those helpers for
Obol camera and faceplate snapshots, view-hash diffing, refresh requests, AET
updates, and external-view display-manager attachment; `QgGL.cpp` and
`QgSW.cpp` use them for widget-local view lifetime, refresh completion, DM
scale coupling, and SW screenshot model2view loads.  Those three files no
longer include `rt/view_legacy_bsg.h` or call the direct RT legacy-view wrappers;
the remaining `qgcanvas_bsg_view` bridge is confined to lower libdm entry points
that still require the retained BSG view pointer.  The local-free helper keeps
the historical qtcad storage-release behavior, because these canvas views can
outlive GED scene cleanup during shutdown/test teardown and full retained-view
cleanup can traverse stale scene-node refs in that ordering.

QgModel's draw transaction boundary has also moved behind `QgLegacyView`.
`QgLegacyView` now exposes helper accessors for draw scene revisions, draw
result names, transaction path lists, and material/view-only transaction
classification, plus qtcad-local draw/erase apply helpers that own GED draw
transaction result lifetime internally.  `QgModel.cpp` uses those helpers for
observer registration/removal, command draw-revision checks, deferred draw
notification coalescing, and model-driven draw/erase actions.  It no longer
includes `ged/bsg_ged_draw.h`, calls `ged_draw_observer_*`,
`ged_draw_scene_revision`, `ged_draw_transaction_make`,
`ged_draw_apply_transaction`, or GED draw result init/free directly, names
`GED_DRAW_TXN_*`, or reads `txn->...` / `result->names` fields.

The Obol draw-sync transaction boundary now follows the same helper route.
`QgLegacyView` exposes qtcad-local draw mode constants, qtcad-local draw
transaction kind names, transaction kind/path/mode helpers, and draw-result
status/revision helpers, plus a GED database accessor for sync paths that need
`db_i` without reading the public GED struct directly.  `QgObolDrawSync.cpp`
uses those helpers for result success filtering, revision folding, transaction
path extraction, default draw mode resolution, database-source lookup, and sync
policy dispatch.  It no longer includes
`ged/bsg_ged_draw.h`, names `GED_DRAW_TXN_*` or `BSG_DRAW_MODE_*`, calls
`ged_draw_default_mode` directly, reads `gedp->dbip`, or reads `txn->...` /
`result->...` fields.
The pivot guard now requires the helper route and rejects those direct tokens
from returning to the Obol draw-sync implementation.

The ELL edit plugin draw-highlight boundary now also routes through
`QgLegacyView`.  `QgLegacyView` exposes helpers for highlighting a drawn shape
by name and clearing the current highlighted draw shape; `QEll.cpp` uses those
helpers instead of calling `ged_draw_highlight_shape_ref_by_name`,
`ged_draw_set_highlighted_shape_ref`, or naming `GED_DRAW_SHAPE_REF_NULL`
directly.  The pivot guard now requires that helper route for the plugin and
rejects the direct GED draw-highlight tokens from returning.

The qtcad fallback canvas DM bridge has been pulled out of the canvas files.
`QgLegacyView` now exposes `qg_legacy_view_dm_open_context` and
`qg_legacy_view_dm_draw`, which own the retained BSG cast and the
`dm_draw_objs` handoff needed by the temporary libdm fallback.  `QgCanvasState.h`
now includes only `QgLegacyView.h`, and `QgGL.cpp` / `QgSW.cpp` no longer
include `QgLegacyViewBsg.h`, call `qg_legacy_view_to_bsg`, spell
`struct bsg_view`, call `dm_draw_objs` directly, or use the old
`qgcanvas_bsg_view` helper.  The pivot guard now requires the named
`QgLegacyView` DM bridge helpers and rejects the direct canvas-side BSG adapter
tokens from returning.

The qtcad canvas DM state boundary now also routes retained libdm hash,
native-repaint, configure-window, and size get/set operations through
`QgLegacyView` helpers.  This is a structural cleanup more than a behavior
change: `QgCanvasState.h` no longer includes `dm.h` or defines `DM_WITH_RT`,
so shared canvas state can carry only opaque DM/FB/PTBL handles while hash
diffing, repaint wakeups, external-view attachment, and size synchronization
use qtcad-local helper names.  `QgGL.cpp` and `QgSW.cpp` still include `dm.h`
for the retained fallback backend lifecycle, draw begin/end, framebuffer
readback, and backend-specific setup they still own, but their direct
hash/native-repaint/configure-window/size calls now go through the same bridge.
The pivot guard now requires those helpers and rejects the migrated direct DM
state calls from returning to the qtcad canvas layer.

The retained canvas DM/view binding sequence has also been lifted into the same
bridge.  `QgLegacyView` now exposes `qg_legacy_view_dm_bind`, which owns the
temporary `dm_set_vp` scale pointer handoff and the retained BSG view
display-manager assignment, plus `qg_legacy_view_dm_sync_dimensions`, which
copies current DM dimensions back into the legacy view record.  `QgGL.cpp`,
`QgSW.cpp`, and `QgCanvasState.h` use those semantic helpers instead of
assembling scale storage, display-manager assignment, and width/height sync
inline.  This keeps the remaining fallback backend files responsible for
opening/configuring/drawing the retained DM, while the fragile view/DM coupling
stays in the qtcad legacy-view bridge that can later be replaced by
Obol/libbrlobol state.  The pivot guard now rejects `dm_set_vp`,
`qg_legacy_view_scale_storage_get`, and direct display-manager setter use from
returning to the canvas layer.

The retained canvas existing-framebuffer setup details have also moved behind
the same bridge.  `QgLegacyView` owns
`qg_legacy_view_dm_framebuffer_setup_existing`, which owns the
`fb_platform_specific` record creation, DM-dimension lookup, and
`fb_setup_existing` call used by the libdm fallback.  The temporary public
display-manager-set hook and its `bu_ptbl_ins_unique` insertion path have since
been retired entirely from the qtcad canvas surface.  `QgGL.cpp` and
`QgSW.cpp` no longer include `bu/ptbl.h`, call `bu_ptbl_ins_unique`, or build
the existing-framebuffer setup record inline.  The pivot guard now requires the
helper route and rejects those lower-level retained setup calls from returning
to the canvas implementations.

Raw qtcad fallback framebuffer lifetime handling has also moved into
`QgLegacyView`.  The bridge now exposes helpers for raw framebuffer creation
with the historical non-standalone marking, retained framebuffer release
(`fb_close_existing` after initialization, `fb_put` before initialization),
standalone checks used by the right-click close behavior, and framebuffer resize
configuration.  `QgGL.cpp` and `QgSW.cpp` no longer call `fb_raw`,
`fb_set_standalone`, `fb_get_standalone`, `fb_close_existing`, `fb_put`, or
`fb_configure_window` directly.  This keeps the qtcad widgets on opaque
`struct fb *` ownership while the retained libdm framebuffer lifecycle remains
centralized in the bridge.  The pivot guard now rejects those framebuffer
lifecycle calls from returning to the canvas implementations.

The retained qtcad fallback DM open/close lifecycle is now part of the bridge
as well.  `QgLegacyView` owns typed open helpers for qtgl and swrast, including
the swrast legacy-view open context and the app-widget `dm_set_udata` handoff
used by framebuffer redraw events, plus the retained `dm_close` call.  `QgGL.cpp`
and `QgSW.cpp` now call `qg_legacy_view_dm_open_qtgl`,
`qg_legacy_view_dm_open_swrast`, and `qg_legacy_view_dm_close` instead of
calling `dm_open`, `dm_set_udata`, or `dm_close` directly.  The pivot guard now
requires the typed helper route and rejects those lifecycle calls from returning
to the canvas implementations.

Retained qtcad fallback backend setup policy is now typed through the bridge as
well.  `QgLegacyView` exposes `qg_legacy_view_dm_setup_qtgl` and
`qg_legacy_view_dm_setup_swrast`; these helpers own the qtgl/SW display-manager
path names, the SW zbuffer enable, and the temporary libdm window-bound setup
using the canvas-provided near/far values.  `QgGL.cpp` and `QgSW.cpp` now call
the typed setup helpers instead of spelling `dm_set_pathname`,
`dm_set_zbuffer`, or `dm_set_win_bounds` directly.  The pivot guard requires
the backend-specific setup helper route and rejects those retained setup calls
from returning to the canvas implementations.

Draw-time retained background and draw-boundary calls now route through
`QgLegacyView` as well.  The bridge exposes copied background get/set helpers,
a background-restore helper for the normal repaint clear path, and draw
begin/end helpers.  `QgGL.cpp` and `QgSW.cpp` no longer call `dm_get_bg`,
`dm_set_bg`, `dm_draw_begin`, or `dm_draw_end` directly; the SW screenshot path
still applies its dark-background policy, but it does so through copied
qtcad-local background arrays instead of raw libdm background pointers.  The
pivot guard now requires the helper route and rejects those draw-time libdm
calls from returning to the canvas implementations.

The qged edit-preview helper now routes retained BSG feature and interaction
operations through semantic `QgLegacyView` helpers instead of owning the bridge
itself.  `QgLegacyView` defines neutral feature refs, feature families,
edit-preview lifecycle events, callback records, and label records, and owns the
conversion to `bsg_feature_ref`, `bsg_edit_preview_ops`, BSG overlay owner
registration, edit-preview interaction record creation, label replacement, point
replacement, and clear/remove/style calls.  `qged_edit_preview_util.cpp` no
longer includes `bsg/feature.h` or `bsg/interaction.h`, spells raw `bsg_view`,
maps `BSG_EDIT_PREVIEW_*` / `BSG_FEATURE_*` / `BSG_OVERLAY_*` /
`BSG_ANCHOR_*`, or calls `bsg_feature_*` / `bsg_interaction_*` directly.  The
temporary raw `qg_legacy_view_edit_preview_context` escape hatch has been
removed, and the pivot guard now requires the `QgLegacyView` helper route while
rejecting direct BSG edit-preview bridge access from returning to qged.

The qtcad draw-observer transaction type boundary has also stopped publishing
raw GED draw transaction names outside the legacy adapter.  `QgLegacyView.h`
now defines qtcad-local aliases for the retained GED draw transaction/result
types, and `QgModel`, `QgObolDrawSync`, `QgObolViewSync`, and the qged Obol
draw observer use those aliases in their signatures.  This does not change the
underlying retained GED observer ABI yet; it narrows the visible type boundary
so future Obol/libbrlobol transaction carriers can replace the backend without
rewriting qtcad-facing declarations.  The pivot guard now requires the
qtcad-local transaction names in those surfaces and rejects direct
`struct ged_draw_transaction` / `struct ged_draw_transaction_result` exposure
from returning outside `QgLegacyView` and tests.

The qged framebuffer-server setup boundary has also been tightened.  The qged
app previously included `dm/fbserv.h` and `dm.h`, wrote the retained
`gedp->ged_fbs` callback slots directly, and read `dm_init_msgs()` itself.
`fbserv.cpp` now owns that libdm/fbserv bridge: it exposes
`qdm_configure_ged_fbserv_handlers` to install the correct TCP/IPC client
callbacks for the current qtcad view type, and `qdm_init_messages` to surface
the filtered DM startup diagnostics without making `QgEdApp.cpp` include libdm
headers.  Behavior is unchanged, but the qged application object no longer
owns retained framebuffer-server table details.  The pivot guard now requires
the bridge route and rejects direct `ged_fbs` or `dm_init_msgs` use from
returning to `QgEdApp.cpp`.

The qtcad canvas fallback display-manager boundary is now complete enough that
`QgGL.cpp` and `QgSW.cpp` no longer include `dm.h` or define `DM_WITH_RT`.
Their remaining retained fallback work is expressed through `QgLegacyView`:
typed qtgl/swrast open/setup/close helpers, framebuffer setup and release
helpers, size/hash/repaint helpers, background and draw-boundary helpers, SW
display-image readback and release helpers, and a semantic helper to load the
current legacy model2view matrix into the SW display manager.  The raw
`dm_get_display_image`, `dm_loadmatrix`, and image-buffer `bu_free` details now
live beside the other retained libdm calls in `QgLegacyView.cpp`.  The pivot
guard now requires the helper route in the canvas implementations and rejects
direct `dm.h`, `DM_WITH_RT`, display-image, matrix-load, and lower-level
framebuffer/display-manager lifecycle calls from returning there.

Those retained qtcad fallback declarations are now private to libqtcad as
well.  The installed `include/qtcad/QgLegacyView.h` no longer forward-declares
`struct dm`, `struct fb`, or `struct bu_ptbl`, and no longer declares the
retained DM/fb helpers, scale-storage pointer accessor, or direct
display-manager setter.  The helper declarations moved to
`src/libqtcad/QgLegacyViewDm.h`, which is included only by the libqtcad
implementation path.  The underlying retained calls still live in
`QgLegacyView.cpp`; this slice narrows the public qtcad surface so downstream
code cannot accidentally depend on the temporary DM/fb bridge while the Obol
replacement matures.  The pivot guard now enforces both halves of that boundary:
the public header must remain free of the retained DM/fb helper names, and the
private header must carry the transitional declarations without `QTCAD_EXPORT`.

The higher-level qtcad canvas/view surface has also stopped publishing retained
DM/fb handles.  `QgCanvasBase` now exposes `legacyBackendInitialized()` as the
neutral readiness query used by `QgView`; it no longer has virtual
`displayManager()` or `frameBuffer()` accessors.  `QgView` now forwards that
neutral readiness state, no longer declares `dmp()` / `ifp()`, and no longer
accepts a retained framebuffer pointer in its public constructor.  The qtcad
Obol tests that only needed to assert "the legacy fallback was not initialized"
now call `legacyBackendInitialized()` instead of checking a raw DM pointer.
The concrete `QgGL`/`QgSW` raw getters remain temporarily for backend-specific
tests that still inspect retained DM readback directly; those are now the next
smaller public-surface cleanup target rather than being reachable through the
generic `QgView` abstraction.  The pivot guard rejects raw DM/fb accessors from
returning to the `QgCanvasBase`/`QgView` boundary.

That concrete canvas cleanup is now done as well.  The installed `QgGL.h` and
`QgSW.h` headers no longer forward-declare `struct dm` and no longer declare
`displayManager()` / `frameBuffer()` getters.  They retain the neutral
`legacyBackendInitialized()` query.  The qtcad controller and swrast integration
tests that only needed to assert fallback initialization state now use the
neutral query or `get_viewport_image()` instead of raw DM readback.  The
currently inactive DM backend benchmark remains intentionally retained-DM
oriented, but it now obtains the retained display manager and performs draw
timing through the qtcad private DM bridge rather than public canvas API,
direct BSG casts, or direct libdm calls.  The pivot guard now rejects raw DM
getter declarations from returning to the concrete canvas public headers.

The framebuffer-backed concrete canvas constructor exposure has now been
removed from the installed qtcad headers as well.  `QgGL` and `QgSW` publish
only the parent-widget constructor; the retained qtgl/swrast framebuffer
windows construct framebuffer-backed canvases through the private
`QgCanvasBridgeFactory` declarations in `src/libqtcad/QgLegacyViewDm.h`.
The libdm qtgl/swrast plugin directories
include that private qtcad bridge header for the transitional construction
hook, while `include/qtcad/QgGL.h` and `include/qtcad/QgSW.h` no longer
forward-declare `struct fb` or publish retained framebuffer constructor
parameters.  The pivot guard now checks both sides: the installed concrete
canvas headers must stay free of raw DM/fb exposure, and the libdm framebuffer
window wrappers must keep using the private bridge factory instead of direct
`new QgGL(this, fbp)` / `new QgSW(this, fbp)` construction.

The standalone qtgl/swrast Qt window headers have also been narrowed to the
same boundary.  `qtglwin.h` and `swrastwin.h` no longer include `dm.h`,
concrete qtcad canvas headers, or `struct fb`; they take the opaque qtcad
`qg_legacy_fb` handle and forward-declare only the canvas class.  The
corresponding implementation files continue to include the private
`QgLegacyViewDm.h` bridge plus the concrete canvas headers and construct
canvases through `QgCanvasBridgeFactory`.  The pivot guard now rejects `dm.h`,
`dm/` includes, `struct dm`, `struct fb`, or concrete qtcad canvas includes
from returning to those window headers.

The private qtcad retained-DM bridge now keeps its own declarations opaque as
well.  `QgLegacyViewDm.h` declares distinct opaque `struct qg_legacy_dm` and
`struct qg_legacy_fb` handle types instead of publishing `struct dm *` and
`struct fb *` through the private qtcad canvas state API.  `QgCanvasState`,
`QgGL`, and `QgSW` store and pass those opaque handles, while
`QgLegacyView.cpp` remains the single place that casts them back to retained
libdm/libfb types and calls the legacy functions.  The standalone framebuffer
wrappers now convert raw framebuffer pointers through
`qg_legacy_view_framebuffer_handle_from_raw` and use the qtcad
`qg_legacy_view_dm_open_*` helpers for both swrast and qtgl, with qtgl's
direct `dm_open`/`dm_close` calls removed from `fb-qtgl.cpp`.  The pivot guard
now rejects typed or weak-typedef DM/fb exposure from the bridge
header/canvas state and rejects direct `dm_open`/`dm_close` or local
`qg_legacy_fb` reinterpret casts from the qtgl/swrast framebuffer wrappers.

The swrast integration test expectation was adjusted to match the migration
state exposed by that cleanup.  `QgSW::paintEvent()` is allowed to satisfy
migrated content through Obol readback without opening the retained fallback
DM; the test now requires either migrated Obol image output or retained
fallback initialization, and verifies non-background pixels when a rendered
image is available.  This preserves coverage of visible qged SW output without
forcing the legacy backend to initialize for content that no longer needs it.

The last public shared-display-manager-table hook on the qtcad canvas
interface has also been removed.  `QgCanvasBase`, `QgGL`, and `QgSW` no longer
declare `setDisplayManagerSet(struct bu_ptbl *)`; `QgCanvasState` no longer
stores a caller-owned DM table pointer; and the private
`qg_legacy_view_dm_set_add` helper plus the stale `bu/ptbl.h` include in
`QgLegacyView.cpp` have been retired.  No in-tree qtcad/qged caller was using
that API after the earlier quad-view/session cleanup, so this narrows the
installed canvas surface without changing runtime behavior.  The pivot guard
now rejects `setDisplayManagerSet` and public `struct bu_ptbl` exposure from
returning to the canvas headers.

The explicit qtcad BSG cast adapter is now private as well.  The installed
`include/qtcad/QgLegacyViewBsg.h` header has been removed from the public
header list, and the adapter lives in `src/libqtcad/QgLegacyViewBsg.h`
without exported declarations.  `QgLegacyView.cpp`, the transitional qtgl and
swrast libdm framebuffer windows, and the focused qtcad tests include that
private header through implementation/test include paths.  The pivot guard now
enforces that the adapter does not return to the installed qtcad headers and
that the private copy stays non-exported.

The normal qtcad Obol workflow tests have also moved off the direct qtcad BSG
view adapter where they only needed active-view and transaction-view setup.
`test_qtcad_obol_draw_sync`, selection sync, overlay sync, pick, measure,
export, and real-model coverage now call `qg_legacy_view_ged_active_set` and,
where they build explicit GED draw transactions,
`qg_legacy_view_draw_transaction_view_set` instead of including
`QgLegacyViewBsg.h` or calling `qg_legacy_view_to_bsg`.  The draw-sync test's
center/scale setup now uses qtcad legacy-view helpers as well, backed by a
new `qg_legacy_view_scale_set` wrapper.  The pivot guard rejects raw BSG view
setup from returning to that migrated test subset.

The qtcad snap and faceplate-sync workflow tests have now moved off the direct
qtcad BSG view adapter as well.  `QgLegacyView.h` exposes
`qg_legacy_view_snap_source_db_set`, `qg_legacy_view_size_set`, and
`qg_legacy_view_view2model_set` so the snap fixture can set DB snap source,
view size, center-query dimensions, and view-to-model state through the opaque
qtcad helper surface; snap result/current-point checks and faceplate ADC, grid,
model-axes, and view-axes get/set setup now use qtcad legacy-view helpers too.
The pivot guard rejects direct `QgLegacyViewBsg.h`, `rt/view_legacy_bsg.h`,
`qg_legacy_view_to_bsg`, raw `struct bsg_view`, `ged_gvp`, transaction `.view`,
direct `rt_view_*_bsg`, and `RT_VIEW_SNAP_*` usage from returning to those
tests.  qsketch's retained edit/scene graph calls still use the private BSG
bridge because they exercise lower-level retained state helpers directly.

The QgSW swrast integration test has also moved off direct qtcad BSG view
access for its normal view binding path.  `QgLegacyView.h` now exposes
`qg_legacy_view_scene_attached`, and `ged_test_qged_swrast.cpp` uses the
opaque helper surface for active GED view binding, GED view-set registration,
unit conversion, and the retained scene-attached assertion.  The pivot guard
now rejects direct `QgLegacyViewBsg.h`, `rt/view_legacy_bsg.h`,
`qg_legacy_view_to_bsg`, `struct bsg_view`, `ged_gvp`,
`rt_view_set_add_view_bsg`, `rt_view_unit_conversion_set_bsg`, and
`bsg_view_scene_attached` usage from returning to that test.

The standalone qsketch fixture now owns its staged view as an opaque
`qg_legacy_view *` as well.  `QgLegacyView.h` exposes
`qg_legacy_view_local_destroy` for callers that need retained view teardown,
and `qsketch.cpp` uses qtcad legacy-view helpers for local view lifecycle,
AET/scale/dimension setup, fit-to-view center and size updates, and grid state
get/set.  Its remaining retained edit creation and field-backed sketch
line-set scene nodes now route through the private
`src/libqtcad/QgLegacyViewSketch.h` bridge implemented in `QgLegacyView.cpp`.
That bridge owns the transitional `rt_edit_create_bsg`, retained scene-root,
node, and line-set calls, while qsketch holds only an opaque
`qg_legacy_view_sketch_lines *`.  The FreeBSD/clang hidden-visibility build
also confirmed that the private bridge functions must use `QTCAD_EXPORT`
because qsketch links against the shared `libqtcad` target even though the
header is not installed.  The pivot guard now rejects `rt/view_legacy_bsg.h`,
`rt/edit_legacy_bsg.h`, `QgLegacyViewBsg.h`, direct `rt_view_*_bsg` view
setup/free/update/grid calls, `qg_legacy_view_to_bsg`, raw BSG node/line-set
refs, the old `m_bv` member path, and `qg_legacy_view_from_bsg` from returning
to qsketch.

The currently inactive retained-DM backend benchmark has also moved its view
setup and draw timing loop onto the qtcad private DM bridge.  `QgLegacyViewDm.h`
now exposes `qg_legacy_view_display_manager_get`, and
`ged_test_dm_backend_bench.cpp` uses `qg_legacy_view_ged_active_set`,
`qg_legacy_view_ged_view_set_add`, `qg_legacy_view_unit_conversion_set`,
`qg_legacy_view_dm_background_restore`, `qg_legacy_view_dm_draw`, and
`qg_legacy_view_dm_draw_end` instead of including `rt/view_legacy_bsg.h`,
including `QgLegacyViewBsg.h`, casting through `qg_legacy_view_to_bsg`,
writing `ged_gvp`, calling direct RT legacy view setters, or calling direct
`dm_*` draw/background functions.  If the benchmark is re-enabled, its
commented CMake include path now names `src/libqtcad` for the private bridge
header.  The pivot guard locks that qtcad bridge route.

The standalone libdm qtgl/swrast framebuffer wrappers no longer include the
RT legacy-BSG view adapter or convert their qtcad canvas view to `bsg_view *`
for framebuffer setup.  `fb-qtgl.cpp` now sets framebuffer mode through
`qg_legacy_view_framebuffer_mode_set`, while `fb-swrast.cpp` uses qtcad
legacy-view helpers for framebuffer mode, dimensions, and swrast attach via
`qg_legacy_view_dm_open_swrast`.  The swrast canvas local view is already
initialized by `qg_legacy_view_local_create`, so the extra direct
`rt_view_init_bsg` call in the framebuffer wrapper was removed.  The pivot
guard now bans those direct RT/BSG setup calls from returning to the
framebuffer wrappers and requires the qtcad helper route instead.

The qtgl display-manager context guard has been moved behind the RT legacy
view adapter as well.  `dm-qtgl.cpp` previously included `bsg/defines.h`,
cast the incoming context to `struct bsg_view *`, and checked
`BSG_VIEW_MAGIC` directly to reject the retained default view context.  The new
`rt_view_context_is_bsg` helper keeps that magic-value knowledge inside
`rt/view_legacy_bsg`, and the qtgl DM now calls the helper instead of spelling
BSG types or constants.  `rt_view_legacy_bsg` has direct unit coverage for the
null and valid-view cases, and the pivot guard rejects direct qtgl BSG context
checks from returning.

The swrast display-manager private context has also stopped exposing the
retained BSG view type.  `dm-swrast.h` now stores the retained view context as
an opaque pointer, while `dm-swrast.cpp` synchronizes width and height through
`rt_view_context_width_from_bsg`, `rt_view_context_height_from_bsg`, and
`rt_view_context_dimensions_set_bsg`.  The RT legacy adapter tests cover the
null and valid context-helper cases, and the pivot guard now rejects a return
to direct BSG view dimension calls or `struct bsg_view` exposure in swrast.

The migrated qtcad Obol workflow tests have moved their retained draw
appearance setup behind the qtcad legacy-view boundary as well.
`QgLegacyView.h` now exposes the opaque
`qg_legacy_view_draw_appearance` handle plus create/destroy and transaction
setter helpers, and `QgLegacyView.cpp` owns the backing
`bsg_appearance_settings` storage required by the current GED draw transaction
implementation.  Draw sync, selection sync, pick, measure, snap, and real-model
tests no longer include `ged/bsg_ged_draw.h`; the shaded/wire tests use
`QG_LEGACY_VIEW_DRAW_MODE_*` values through the qtcad wrapper and no longer
spell `BSG_DRAW_MODE_*`, `BSG_APPEARANCE_SETTINGS_INIT`, direct
`bsg_appearance_settings`, or transaction `.appearance` writes.  The pivot
guard now requires the wrapper helpers and rejects those retained appearance
tokens from returning to the migrated qtcad workflow tests.  Verification on
FreeBSD/clang passed with:
`ninja -C build libqtcad test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap test_qtcad_obol_real_models`
and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_measure|test_qtcad_obol_snap|test_qtcad_obol_real_models_(pinewood_wire|pinewood_shaded|havoc_wire|m35_wire_interactions|m35_wire_pick_all_stress))$"
--timeout 180 --output-on-failure`.  The focused CTest run completed 11/11
passing in 45.77 seconds; the heaviest case was
`test_qtcad_obol_real_models_havoc_wire` at 23.53 seconds, still well below the
180-second per-test ceiling.

The qtcad workflow tests have also moved their draw observer registration and
transaction apply/result lifecycle calls behind the same legacy-view wrapper.
`QgLegacyView.h` now exposes `qg_legacy_view_draw_result_init`,
`qg_legacy_view_draw_result_free`, and
`qg_legacy_view_draw_transaction_apply`; `QgLegacyView.cpp` forwards those to
the retained GED transaction functions.  `test_qtcad_obol_draw_sync.cpp` now
registers/unregisters its observer through
`qg_legacy_view_draw_observer_add/remove`, and draw sync, selection sync, pick,
measure, snap, and real-model workflow tests use the qtcad wrappers for result
init/apply/free instead of calling `ged_draw_apply_transaction` or
`ged_draw_transaction_result_*` directly.  The pivot guard now requires the
wrapper route and rejects those raw GED draw calls from returning to the
migrated qtcad workflow tests.  Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap test_qtcad_obol_real_models`
and the same focused 11-test CTest expression under `--timeout 180`; the run
completed 11/11 passing in 46.22 seconds, with
`test_qtcad_obol_real_models_havoc_wire` at 23.57 seconds.

The migrated qtcad workflow tests now route draw transaction construction
through the qtcad legacy-view boundary too.  `QgLegacyView.h` exposes
`qg_legacy_view_draw_transaction_init` and
`qg_legacy_view_draw_transaction_paths_set`, with `QgLegacyView.cpp` mapping
the qtcad transaction-kind enum to retained `GED_DRAW_TXN_*` values and
owning the borrowed multi-path field writes.  Draw sync, selection sync, pick,
measure, snap, and real-model tests now use `qg_legacy_view_draw_transaction`
locals, `QG_LEGACY_VIEW_DRAW_TXN_*` values, and the wrapper paths setter
instead of spelling `struct ged_draw_transaction`,
`ged_draw_transaction_make`, `GED_DRAW_TXN_*`, or transaction `.paths` /
`.path_count` assignments.  The pivot guard now rejects those raw construction
tokens from returning to the migrated workflow tests.  Verification on
FreeBSD/clang passed with the focused qtcad workflow build and CTest command;
the run completed 11/11 passing in 45.90 seconds, with
`test_qtcad_obol_real_models_havoc_wire` at 23.56 seconds.

`QgLegacyView.cpp` now also self-routes its retained draw convenience helpers
through that wrapper surface.  `qg_legacy_view_draw_redraw`,
`qg_legacy_view_draw_paths_apply`, and
`qg_legacy_view_draw_erase_path_apply` use
`qg_legacy_view_draw_transaction_init`, view/path setters, result
init/free, and `qg_legacy_view_draw_transaction_apply` instead of duplicating
direct `ged_draw_transaction_make`, `ged_draw_apply_transaction`, and
`ged_draw_transaction_result_*` calls.  This keeps the remaining raw retained
transaction forwarding points concentrated in the bridge mapper/inspector and
apply/result wrapper functions, while preserving caller behavior.  Verification
on FreeBSD/clang passed with:
`ninja -C build libqtcad test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap
test_qtcad_obol_real_models` and the focused 11-test CTest expression under
`--timeout 180`; the run completed 11/11 passing in 45.83 seconds, with
`test_qtcad_obol_real_models_havoc_wire` at 23.52 seconds.

The qged framebuffer-server update path now uses the same private qtcad
retained-DM bridge for framebuffer-backed canvas lookup.  `QgLegacyViewDm.h`
exposes exported private helpers for converting a raw framebuffer pointer to
the opaque `qg_legacy_fb` handle, recovering the qtgl/swrast Qt canvas used
for update signals, and forwarding DM init messages.  `fbserv.cpp` includes
that private bridge instead of `dm.h`, and its TCP and IPC client-open paths no
longer call `fb_get_dm`, `dm_get_ctx`, `dm_get_udata`, or `dm_init_msgs`
directly.  The qged target now has `src/libqtcad` on its include path for this
private bridge, and the pivot guard requires the bridge route while rejecting
those raw DM lookup calls from returning to qged.

Building `qged` on FreeBSD/clang also exposed a stale retained-view access in
the tkswrast plugin: `dm-tkswrast.cpp` still referenced the removed
`swrast_vars::v` member after the swrast private context was made opaque.
The RT legacy adapter now has `rt_view_context_refresh_request_bsg`, covered
by `rt_view_legacy_bsg`, and tkswrast uses that plus
`rt_view_context_dimensions_set_bsg` through `swrast_vars::view_ctx` for its
refresh and dimension updates.  The pivot guard now requires the context
helper route and rejects direct `sv->v`, raw refresh, or raw dimension helper
use in tkswrast.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad qged test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap
test_qtcad_obol_real_models rt_view_legacy_bsg` and:
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_measure|test_qtcad_obol_snap|test_qtcad_obol_real_models_(pinewood_wire|pinewood_shaded|havoc_wire|m35_wire_interactions|m35_wire_pick_all_stress))$"
--timeout 180 --output-on-failure`.  The focused CTest run completed 12/12
passing in 46.09 seconds; the heaviest case was
`test_qtcad_obol_real_models_havoc_wire` at 23.75 seconds.

The installed qtcad legacy-view draw API no longer aliases retained GED draw
transaction structs.  `QgLegacyView.h` now defines qtcad-owned
`qg_legacy_view_draw_transaction` and
`qg_legacy_view_draw_transaction_result` records, keeping draw transaction
kind, view, paths, appearance-mode, and result status/name data in qtcad
spelling instead of typedefing `struct ged_draw_transaction` or
`struct ged_draw_transaction_result`.  `QgLegacyView.cpp` owns conversion
helpers to translate qtcad records to retained GED transactions at apply time
and to copy retained GED results back into qtcad results.  Draw observer
registration now allocates a small qtcad bridge object; the retained GED
observer callback is translated to qtcad transaction/result records before
calling qtcad/qged observers, and observer removal unregisters the retained
token and frees the bridge.  Raw retained GED transaction/result types and
`ged_draw_apply_transaction` now remain visible only in the qtcad bridge
implementation, not in the installed qtcad header or migrated callers.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad qged test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap
test_qtcad_obol_real_models` and the focused 11-test CTest expression under
`--timeout 180`; the run completed 11/11 passing in 46.14 seconds, with
`test_qtcad_obol_real_models_havoc_wire` at 23.64 seconds.

The qtcad legacy-view draw result storage is now opaque as well.  The
installed `QgLegacyView.h` result record keeps only a private implementation
token and no longer includes `bu/vls.h` or exposes public `names` / `errors`
storage.  `QgLegacyView.cpp` owns `qg_legacy_view_draw_result_storage`, copies
retained GED result fields into it after apply/observer callbacks, and exposes
status, scene revision, names, and errors through qtcad accessor functions.
The real-model workflow diagnostic path now uses
`qg_legacy_view_draw_result_errors` instead of reading `result.errors`
directly, and the pivot guard rejects public result `bu_vls` storage plus
direct result-field reads in migrated workflow tests.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad qged test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap
test_qtcad_obol_real_models` and:
`ctest --test-dir build -R
"^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_measure|test_qtcad_obol_snap|test_qtcad_obol_real_models_(pinewood_wire|pinewood_shaded|havoc_wire|m35_wire_interactions|m35_wire_pick_all_stress))$"
--timeout 180 --output-on-failure`.  The focused CTest run completed 11/11
passing in 45.91 seconds; the heaviest case was
`test_qtcad_obol_real_models_havoc_wire` at 23.50 seconds.

The qtcad legacy-view draw transaction storage is now opaque too.  The
installed `QgLegacyView.h` transaction record no longer exposes transaction
kind/path/view/path-array/appearance/autoview or other semantic fields; it
keeps fixed in-place storage sized by
`QG_LEGACY_VIEW_DRAW_TRANSACTION_STORAGE_WORDS`.  `QgLegacyView.cpp` owns the
private `qg_legacy_view_draw_transaction_storage` record, placement-constructs
it in `qg_legacy_view_draw_transaction_init`, translates it to/from retained
GED transactions inside the bridge, and exposes the one internal convenience
write through `qg_legacy_view_draw_transaction_autoview_set`.  Existing
migrated callers still stack-allocate transaction records and use the qtcad
init/setter/accessor functions; the pivot guard now requires the opaque
storage form and rejects the old public transaction fields from returning to
the installed header.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad qged test_qtcad_obol_draw_sync
test_qtcad_obol_selection_sync test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_snap
test_qtcad_obol_real_models` and:
`ctest --test-dir build -R
"^(brlobol_pivot_guard|test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_measure|test_qtcad_obol_snap|test_qtcad_obol_real_models_(pinewood_wire|pinewood_shaded|havoc_wire|m35_wire_interactions|m35_wire_pick_all_stress))$"
--timeout 180 --output-on-failure`.  The focused CTest run completed 11/11
passing in 45.88 seconds; the heaviest case was
`test_qtcad_obol_real_models_havoc_wire` at 23.51 seconds.

The standalone qsketch fixture no longer includes `dm.h`.  Its current
retained sketch path already goes through `QgLegacyView.h` and the private
`QgLegacyViewSketch.h` bridge for view lifecycle, view-state setup, edit
creation, and line-set publication, so the direct libdm header was stale.
The qsketch pivot-guard block now rejects direct `dm.h` / `dm/` includes in
addition to the existing BSG and RT legacy-BSG bans.

Verification on FreeBSD/clang passed with:
`ninja -C build qsketch` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180
--output-on-failure`; the pivot guard passed in 8.13 seconds.

The qged framebuffer-server header boundary is now tighter than the earlier
forward-declare cleanup.  `src/qged/fbserv.h` no longer names
`struct fbserv_obj` or declares the raw framebuffer-server callback helpers;
its Qt helper classes keep only opaque `void *` server state.  `fbserv.cpp`
owns the cast back to `fbserv_obj`, the retained `dm/fbserv.h` include, and
the callback signatures that populate GED's retained framebuffer-server table.
The pivot guard now rejects `fbserv_obj` and raw qdm callback prototypes from
returning to the qged header while requiring the implementation to own the
private qtcad framebuffer bridge route.

Verification on FreeBSD/clang passed with:
`ninja -C build qged` and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180
--output-on-failure`; the pivot guard passed in 8.09 seconds.

The MGED stereo dozoom fallback no longer writes the retained
`gv_perspective` field directly when a stereo frame needs a non-zero
perspective angle.  The fallback now calls `rt_view_perspective_set_bsg`,
matching the rest of the perspective setter migration while preserving the
existing frame behavior.  The pivot guard now requires that setter in
`src/mged/dozoom.c` and rejects direct retained perspective-field access from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/mged`,
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180
--output-on-failure`, and
`ctest --test-dir build -R
"^(rt_view_legacy_bsg|regress-mged_zoom|regress-mged_set_perspective)$"
--timeout 180 --output-on-failure`.

The retained view display-manager pointer now has an RT legacy adapter
boundary.  `rt_view_display_manager_from_bsg` and
`rt_view_display_manager_set_bsg` expose the borrowed pointer without requiring
callers to write `struct bsg_view::dmp` directly.  MGED command forwarding,
screengrab, overlay execution, RT/RTCHECK view setup, DM command forwarding,
and dozoom frame setup now use the setter.  The focused RT adapter test covers
null, set/get, and clear behavior, and the pivot guard rejects direct
display-manager field writes from the migrated MGED production paths.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|regress-mged_zoom|regress-mged_refresh|regress-mged_draw)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 5/5
passing in 8.86 seconds.

The display-manager adapter cleanup now covers the libged DM command path as
well.  `src/libged/dm/dm.c` uses a local typed helper backed by
`rt_view_display_manager_from_bsg` for DM lookup/list/attach checks and uses
`rt_view_display_manager_set_bsg` when attach succeeds, leaving no direct
`dmp` field access in that production file.  TclCAD new-view creation now sets
the newly opened display manager through the RT legacy setter and reads it
through the getter for the immediate pathname handoff; the larger TclCAD DM
read cleanup remains separate.  The pivot guard now enforces the strict
libged DM boundary and rejects new direct TclCAD display-manager field writes.

While running the focused drawing tests on FreeBSD/clang, the swrast/qtgl DM
plugins failed to load because the private qtcad canvas bridge factory methods
were not exported from `libqtcad.so`.  `QgCanvasBridgeFactory` is now marked
with `QTCAD_EXPORT`, and the pivot guard requires that export marker so
`libdm-swrast.so` and `libdm-qtgl.so` can resolve the bridge symbols at
runtime.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad dm-swrast dm-qtgl libged libtclcad bin/mged
rt_view_legacy_bsg`, a dynamic-symbol check showing exported
`QgCanvasBridgeFactory::create_qtgl` and `create_swrast`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|tclcad_test_ged_cmds_exist|dm_backend_draw_item|ged_test_mged_bsg|ged_test_bsg_render_stability|ged_test_lod_crossrun|ged_test_mged_shaded_mode_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 12/12
passing in 13.66 seconds.

The compact libged command-side display-manager read batch now also goes
through the RT legacy adapter.  `screengrab`, `ert`, `fb2pix`, `fbclear`,
`pix2fb`, `png2fb`, `overlay`, `rt`, `ged_rt_fb_refresh`, and the NMG/illum
label repaint hooks use `rt_view_display_manager_from_bsg` instead of reading
`struct bsg_view::dmp` directly; `screengrab` also avoids calling
`dm_get_pathname` on a null retained display manager while matching named
views.  The pivot guard now requires `rt/view_legacy_bsg.h` plus the getter in
those production files and rejects direct display-manager field access there,
so production libged has no direct `->dmp` users outside the RT legacy adapter.

Verification on FreeBSD/clang passed with:
`ninja -C build libged bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_mged_bsg|ged_test_bsg_render_stability|ged_test_lod_crossrun|ged_test_mged_shaded_mode_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 10/10
passing in 13.45 seconds.  A targeted CTest name scan for
`fb2pix|pix2fb|png2fb|fbclear|ert|screengrab|screen_grab|overlay` found no
dedicated framebuffer command tests beyond existing overlay coverage.

The first TclCAD display-manager read cleanup batch now uses the same RT
legacy getter boundary.  `src/libtclcad/wrapper.c` reads the retained display
manager through `rt_view_display_manager_from_bsg` for wrapper dimension sync
and perspective synchronization, skipping those syncs when no display manager
is attached instead of dereferencing the raw retained field.  `view/util.c`
uses the getter for viewability pathname checks, `view/draw.c` uses it before
loading draw matrices, and `view/axes.c` uses it for fallback-width
calculations while preserving the existing 512-pixel default when no display
manager is present.  The pivot guard now requires the getter route and rejects
direct retained `dmp` field access in those files.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.10 seconds.

The TclCAD refresh and framebuffer-open paths now also avoid direct retained
display-manager field reads.  `src/libtclcad/view/refresh.c` snapshots the
borrowed display manager once through `rt_view_display_manager_from_bsg` for
refresh drawing and draw begin/end, then uses that local pointer for zbuffer,
depth-mask, framebuffer refresh dimensions, and interactive-rectangle drawing.
`src/libtclcad/fb.c` uses the same getter before asking the display manager
for its embedded framebuffer, preserving the existing Tcl error path when no
framebuffer is available.  The pivot guard now includes both files in the
TclCAD direct-`dmp` read ban.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.07 seconds.

TclCAD polygon command setup now also reads retained display managers through
the RT legacy getter.  `src/libtclcad/polygons.c` uses
`rt_view_display_manager_from_bsg` for its dimension-sync helper and for the
circle, contour, ellipse, and rectangle motion-binding pathname lookups,
preserving the existing behavior of only installing bindings when a display
manager pathname is available.  The TclCAD direct-`dmp` guard list now includes
the polygon implementation.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.06 seconds.  A CTest name scan did not find a dedicated TclCAD
polygon test; the available polygon tests are lower-level geometry tests.

TclCAD mouse command setup now has a file-local display-manager helper
boundary.  `src/libtclcad/mouse.c` uses
`rt_view_display_manager_from_bsg` inside helpers for live width, height, and
pathname queries plus dimension sync; callers now use those helpers for mouse
interaction scaling, rectangle height conversion, and BREP/joint motion
binding setup instead of reading `struct bsg_view::dmp` directly.  Width and
height helpers still prefer the live display manager and fall back to the
adapter view dimensions when no retained display manager is attached.  The
TclCAD direct-`dmp` guard list now includes `mouse.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 12.52 seconds.

TclCAD command dispatch now also avoids direct retained display-manager field
access.  `src/libtclcad/commands.c` has a local
`tclcad_commands_display_manager` helper backed by
`rt_view_display_manager_from_bsg`, and the existing new-view setup continues
to install newly opened display managers with
`rt_view_display_manager_set_bsg`.  Background, bounds, configure, default
bindings, edit-mode binding setup, image capture, rectangle mode,
font/light/transparency/zbuffer, window sizing, and view/slew screen
conversions now route their borrowed `struct dm *` through that helper instead
of spelling `struct bsg_view::dmp`.  The pivot guard now rejects any direct
display-manager field access in `commands.c`, and the production
`src/libtclcad` direct-`dmp` scan is empty.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.10 seconds.

gsh and the libdm retained-view drawing bridge now avoid direct
`struct bsg_view::dmp` access.  `src/gtools/gsh/gsh.cpp` reads the borrowed
display manager through `rt_view_display_manager_from_bsg` for display hashing,
dirty marking, and repaint checks, and closes/clears retained display managers
through `rt_view_display_manager_set_bsg` during `GshState` destruction.
`src/libdm/view.c` now uses the same getter for HUD render requests and
`dm_draw_objs`, while its remaining `->dmp` references are the unrelated
local arrow-segment display-manager payload.  The pivot guard now requires
the gsh and libdm/view display-manager adapter tokens and rejects reintroduced
direct BSG-view display-manager field access.

Verification on FreeBSD/clang passed with:
`ninja -C build bin/gsh libdm dm-swrast dm-qtgl`, `build/bin/gsh help`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_mged_bsg|ged_test_bsg_render_stability|ged_test_mged_shaded_mode_bsg|ged_test_tcl_overlay_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 11/11
passing in 11.42 seconds.

The private qtcad legacy-view display-manager bridge now also goes through the
RT legacy display-manager adapter boundary.  `qg_legacy_view_display_manager_get`
and `qg_legacy_view_display_manager_set` in `src/libqtcad/QgLegacyView.cpp`
now delegate to `rt_view_display_manager_from_bsg` and
`rt_view_display_manager_set_bsg` instead of reading or writing
`struct bsg_view::dmp` directly.  The pivot guard now requires those adapter
tokens in the qtcad bridge implementation and rejects direct retained
display-manager field access from returning there.

An initial focused qtcad CTest run on FreeBSD/clang exposed a stale
`test_qtcad_obol_edit_preview` executable still referencing the retired
`QgView(QWidget *, int, fb *)` constructor.  Rebuilding the test target
recompiled it against the current two-argument constructor and resolved the
runtime loader failure.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad qged dm-swrast dm-qtgl bin/gsh
test_qtcad_obol_edit_preview`, `build/bin/gsh help`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|dm_backend_draw_item|ged_test_qged_swrast|test_qtcad_obol_controller|test_qtcad_obol_draw_sync|test_qtcad_obol_edit_preview|test_qtcad_obol_faceplate_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_export|test_qtcad_obol_snap|test_qtcad_obol_measure|test_qtcad_obol_overlay_sync|test_qtcad_obol_real_models_(pinewood_wire|pinewood_shaded|havoc_wire|m35_wire_interactions|m35_wire_pick_all_stress))$"
--timeout 180 --output-on-failure`; the focused CTest run completed 19/19
passing in 46.78 seconds, with `test_qtcad_obol_real_models_havoc_wire` at
23.62 seconds.

MGED's remaining app-level retained runtime-view field access is now behind
RT legacy adapters.  `rt_view_update_callback_set_bsg` owns installing the
legacy BSG update callback and recursion-detection callback table, and
`src/mged/setup.c` plus `src/mged/attach.c` use it instead of writing
`gv_callback`, `gv_clientData`, or `callbacks` directly.
`rt_view_edit_matrix_set_bsg`, `rt_view_edit_matrix_clear_bsg`, and
`rt_view_frame_revision_from_bsg` now cover the dozoom draw-frame handoff, so
`src/mged/dozoom.c` no longer reads or writes the retained edit-matrix or
frame-revision fields directly.  The RT adapter test now covers null handling,
callback installation and firing, edit-matrix set/clear, and frame-revision
reads.  The pivot guard requires these adapter tokens and rejects direct MGED
callback/edit-matrix/frame-revision field access from returning.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg bin/mged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|regress-mged_draw|regress-mged_draw_e_erase_d_who|regress-mged_refresh|regress-mged_set_perspective|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 7/7
passing in 10.21 seconds.

TclCAD data-arrow feature access is now behind the private GED draw-view
adapter instead of direct `bsg_feature_*` calls.  The GED facade now exposes
`ged_draw_view_feature_visible_set`, `ged_draw_view_feature_points_copy`,
`ged_draw_view_arrow_tip_get`, `ged_draw_view_arrow_tip_set`, and
`ged_draw_view_tcl_arrows_replace`; `ged_draw_view_lines_points_copy` now
delegates to the generic point-copy helper.  `src/libtclcad/view/arrows.c`
uses those helpers for draw, color, line width, points, tip length, tip
width, and removal behavior, preserving the previous yellow/default style
fallback when rebuilding point data.  The arrows command no longer includes
`bsg/feature.h` or `bsg_move_helpers.h`.

The pivot guard now requires the data-arrow adapter tokens in
`src/libtclcad/view/arrows.c`, rejects direct feature refs and the old
move-helper include from returning there, and includes the new facade helpers
in the central `bsg_ged_draw_view` adapter inventory.  The headless
`ged_test_tcl_overlay_bsg` regression now exercises the new data-arrow facade:
replace, point copy, style readback, tip get/set, visibility, color, line
width, and removal.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.11 seconds.

TclCAD data-axes feature access is now also behind the private GED draw-view
adapter.  The GED facade now exposes
`ged_draw_view_feature_axes_centers_copy` and
`ged_draw_view_tcl_axes_replace`; `src/libtclcad/view/axes.c` uses the GED
adapter for draw, color, line width, size reads, size-preserving rebuilds,
point-center reads, point replacement, and removal.  The previous default
style fallback of visible yellow zero-width axes is preserved when rebuilding
data axes.  The axes command no longer includes `bsg/feature.h` or
`bsg_move_helpers.h`.

The pivot guard now requires the data-axes adapter tokens in
`src/libtclcad/view/axes.c`, rejects direct feature refs and the old
move-helper include from returning there, and includes the new axes facade
helpers in the central `bsg_ged_draw_view` adapter inventory.  The headless
`ged_test_tcl_overlay_bsg` regression now also exercises the data-axes facade:
replace, center copy, generated point copy/half-size preservation, style
readback, visibility, color, line width, and removal.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.08 seconds.

TclCAD data-polygon outline publishing now also routes through the private GED
draw-view adapter.  `src/libtclcad/polygons.c` now includes neutral
`bg/line_layer.h`, packs contour commands as `BG_GEOMETRY_LINE_*`, and calls
`ged_draw_view_tcl_polygons_replace` for the retained draw-view publish step.
The GED facade owns translating commands into BSG geometry commands, replacing
the typed polygon feature, applying style, and registering polygon-edit overlay
ownership.  `polygons.c` no longer includes direct BSG feature, geometry, or
overlay headers.

The pivot guard now requires the polygon publish path to include the private
GED draw-view adapter and neutral line-layer tokens, rejects direct BSG feature
and geometry access from returning to `src/libtclcad/polygons.c`, and includes
`ged_draw_view_tcl_polygons_replace` in the central `bsg_ged_draw_view`
adapter inventory.  The headless `ged_test_tcl_overlay_bsg` regression now
also exercises the polygon facade with typed outline replacement, command
copy-back, style readback, and removal.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.23 seconds.

TclCAD data-label feature access now also routes through the private GED
draw-view adapter.  The GED facade now exposes
`ged_draw_view_tcl_labels_replace`, `ged_draw_view_label_count`,
`ged_draw_view_label_copy`, and `ged_draw_view_label_point_set`;
`src/libtclcad/view/labels.c` uses the adapter for draw existence, color
readback, label enumeration, retained feature replacement, and removal when
draw is disabled or the parsed label list is empty.  The command still keeps
`gv_tcl` as the parser-owned input record for label text/point/color memory,
but no longer includes `bsg/feature.h` or calls direct `bsg_feature_*` APIs.

The pivot guard now requires the data-label adapter tokens in
`src/libtclcad/view/labels.c`, rejects direct feature refs from returning
there, and includes the new label facade helpers in the central
`bsg_ged_draw_view` adapter inventory.  The headless
`ged_test_tcl_overlay_bsg` regression now also exercises the data-label facade:
replace, count/copy, color readback, point update, and removal-on-undraw.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.19 seconds.

The TclCAD command/mouse data-label consumers now use the same GED label
facade helpers.  `src/libtclcad/commands.c` routes data/sdata label point
movement and label hit/readback loops through `ged_draw_view_feature_exists`,
`ged_draw_view_label_count`, `ged_draw_view_label_copy`, and
`ged_draw_view_label_point_set`.  `src/libtclcad/mouse.c` uses the label
facade for data-label point scaling.  Direct `bsg_feature_label_*` calls are
now gone from production `src/libtclcad`, while broader direct feature access
remains in the pending arrows/axes/lines move-helper paths.

The pivot guard now requires the label facade tokens in the command and mouse
label consumers and rejects direct `bsg_feature_label_count`,
`bsg_feature_label_copy`, `bsg_feature_label_point_set`, or direct label-name
`bsg_feature_find` calls from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.20 seconds.

The remaining TclCAD data arrow/axes/line move-helper path is now behind the
GED draw-view adapter as well.  The old `src/libtclcad/bsg_move_helpers.h`
header was retired and replaced by `src/libtclcad/draw_view_move_helpers.h`,
which routes point copy, axes-center copy, style readback, and data
arrow/line/axes replacement through the private GED draw-view facade.  The
helper preserves prior style defaults and line visibility behavior while
removing direct `bsg_feature_ref`, `bsg_feature_*`, and overlay-owner calls
from TclCAD production code.

`src/libtclcad/commands.c` now uses the neutral helper for data/sdata arrow,
axes, and line point move/readback paths, and `src/libtclcad/mouse.c` uses it
for data-arrow scaling.  `src/libtclcad/CMakeLists.txt` tracks the new helper
header and no longer lists the retired BSG helper.  A production
`src/libtclcad` scan now has no direct `bsg_feature_*`, `bsg_feature_ref`,
`bsg/feature.h`, `bsg/overlay.h`, `BSG_FEATURE_*`, or `BSG_OVERLAY_*` usage.

The pivot guard now requires `draw_view_move_helpers.h`, rejects resurrection
of `bsg_move_helpers.h`, requires command/mouse data move callers to use the
neutral helper tokens, and broadly rejects direct BSG feature/overlay API use
from production `src/libtclcad`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad bin/mged ged_test_tcl_overlay_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|tclcad_test_ged_cmds_exist|ged_test_tcl_overlay_bsg|rt_view_legacy_bsg|regress-mged_draw|regress-mged_overlay|regress-mged_refresh|regress-mged_zoom)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 8/8
passing in 10.30 seconds.

qtcad edit-preview feature helpers now route through the RT legacy view
adapter instead of direct BSG feature and interaction APIs.  `rt/view.h` now
defines neutral `rt_view_feature_ref`, `rt_view_feature_family`,
`rt_view_edit_preview_event`, `rt_view_edit_preview_callbacks`, and
`rt_view_feature_label` types.  `rt/view_legacy_bsg.h` and
`src/librt/view_legacy_bsg.c` expose and implement the transitional
`rt_view_feature_*_bsg` and `rt_view_edit_preview_publish_event_bsg` wrappers,
including the BSG edit-preview callback bridge.

`src/libqtcad/QgLegacyView.cpp` now maps its public qtcad feature refs,
families, edit-preview events, callbacks, labels, points, visibility, color,
touch, geometry-clear, lifecycle publish, and remove helpers to that RT
surface.  The file no longer includes `bsg/feature.h` or `bsg/interaction.h`
and no longer calls direct `bsg_feature_*`, `bsg_interaction_*`, or
`bsg_edit_preview_*` APIs.  Direct BSG feature/edit-preview access for this
path is now confined to `src/librt/view_legacy_bsg.c`.

`src/librt/tests/view_legacy_bsg.c` now covers the feature wrapper surface:
null handling, label ensure/replace/readback, visible/color setters,
edit-preview overlay ensure with revision/pick callback bridging, lifecycle
event publishing, point replacement/readback, geometry clear, and feature
removal.  The pivot guard now requires the neutral RT feature types and
wrappers, requires qtcad to use the RT wrapper tokens, and rejects direct BSG
feature/edit-preview names from returning to `QgLegacyView.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build librt libqtcad`, `ninja -C build rt_view_legacy_bsg libqtcad`,
direct `build/src/librt/tests/rt_view_legacy_bsg`,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_controller|test_qtcad_obol_edit_preview|test_qtcad_obol_overlay_sync)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 8.78 seconds,
and `ninja -C build qged`.

The RT snap-exclude generic feature wrapper now takes neutral
`rt_view_feature_ref` rather than exposing `bsg_feature_ref` in
`rt/view_legacy_bsg.h`.  The BSG conversion is confined to
`src/librt/view_legacy_bsg.c`, and `rt_view_polygon_snap_exclude_set_bsg`
now builds the neutral feature ref before calling the generic setter.  The
pivot guard rejects direct `bsg_feature_ref` spelling from returning to the
public RT legacy adapter header for this boundary.

The libged draw-selection bridge has been narrowed again.  `bsg_ged_draw_refs.c`
no longer includes `bsg/interaction.h` or `bsg/selection.h` and no longer
exports a `ged_draw_shape_ref_selection_record` helper.  Draw-record callers
derive normalized selection paths and call
`ged_draw_view_selection_contains_path`, `ged_draw_view_selection_add_path`,
and `ged_draw_view_selection_set_path` with neutral
`GED_DRAW_VIEW_SELECTION_*` kinds.  Temporary `bsg_selection` access,
`bsg_interaction_record` creation, duplicate checks, add, clear, and record
freeing are now confined to `src/libged/bsg_ged_draw_view.c`.

The pivot guard requires the path/kind helper boundary, rejects the old
record helper names, rejects direct BSG interaction/feature selection access
from `bsg_ged_draw_records.c` and `bsg_ged_draw_refs.c`, and keeps the
retained BSG selection operations allowlisted only in the GED view adapter.
After that narrowing, a dead-include sweep removed stale `bsg/interaction.h`
includes from the GED draw util, tree, transactions, state, scene-root,
material, highlight, and draft bridge units; the broad GED draw interaction
inventory now has the intended implementation hit only in
`src/libged/bsg_ged_draw_view.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged rt_view_legacy_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_view_command|test_qtcad_obol_controller|test_qtcad_obol_edit_preview|test_qtcad_obol_overlay_sync)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 6/6
passing in 8.90 seconds after the include sweep.

GED command-result overlay insertion now routes retained BSG overlay registry
operations through the GED view adapter.  `bsg_ged_draw_view.c` owns
`ged_draw_view_overlay_scene_find`, `ged_draw_view_overlay_name_erase`,
`ged_draw_view_overlay_scene_append`, and
`ged_draw_view_overlay_command_result_owner_set`, keeping the direct
`bsg_overlay_*` calls and `BSG_OVERLAY_CLASS_COMMAND_RESULT` metadata in the
adapter.  `bsg_ged_draw_overlay.c` now calls those helpers and no longer
includes `bsg/overlay.h` or spells `BSG_OVERLAY_*` directly; retained BSG
scene and geometry creation remains in that bridge until the backend is
replaced.

The pivot guard now requires the new overlay registry adapter tokens in
`bsg_ged_draw_view.c` and `bsg_ged_draw_view_private.h`, and rejects direct
BSG overlay registry access from returning to `bsg_ged_draw_overlay.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|ged_test_view_command|test_qtcad_obol_controller|test_qtcad_obol_edit_preview|test_qtcad_obol_overlay_sync)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 6/6
passing in 8.89 seconds.

The public legacy DM vlist header boundary has been moved off BSG spelling.
`include/dm/vlist.h` now includes `bg/vlist.h`, describes typed render records,
and exposes the `dm_draw` callback as `bg_vlist *(*callback)(void *)`.
The backend-private calltable/adapter side still stages retained BSG vlist
internals, but the installed public header no longer includes `bsg/vlist.h` or
spells `bsg_vlist`.  `src/libdm/tests/test_backend_draw_item.c` now asserts
that boundary directly, and the pivot guard checks the same public header
hygiene.

The focused hygiene run also exposed three stale boundary leaks, now fixed:
`src/libged/selection_state.cpp` includes the narrow GED draw-view bridge and
declares only the helper entry points it needs locally instead of including the
broader BSG GED draw private header, and outdated "view object" terminology was
removed from the TclCAD polygon and qtcad legacy-view comments.

Verification on FreeBSD/clang passed with:
`ninja -C build libdm test_dm_backend_draw_item libged libqtcad`,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|dm_backend_draw_item|libbsg_api_hygiene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 49.24 seconds,
and `ctest --test-dir build -R
"^(ged_test_selection_semantics|ged_test_view_command)$" --timeout 180
--output-on-failure` completing 2/2 passing in 0.32 seconds.

The public legacy DM draw-view header has also been narrowed.  `include/dm/view.h`
no longer forward declares or names `struct bsg_view`; `dm_draw_faceplate` and
`dm_draw_objs` now take opaque `void *` view contexts.  The retained cast back
to `struct bsg_view` is confined to `src/libdm/view.c`, which remains the
legacy BSG-backed draw implementation.  The backend draw-item test and pivot
guard now assert that `include/dm/view.h` exposes the draw entry points through
opaque context pointers and does not include or spell BSG view types.

Verification on FreeBSD/clang passed with:
`ninja -C build libdm libged libqtcad test_dm_backend_draw_item` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|dm_backend_draw_item|libbsg_api_hygiene)$"
--timeout 180 --output-on-failure`; the focused CTest run completed 3/3
passing in 49.43 seconds.

The public GED view header has been narrowed at the polygon export boundary.
`include/ged/view.h` no longer includes the broad `ged/bsg_ged_draw.h` header
and no longer exposes `bsg_data_polygon_state` in the `ged_export_polygon`
prototype.  The public entry now takes an opaque polygon-state context, while
`src/libged/polyclip.cpp` owns the retained `bsg_data_polygon_state` cast and
layout dependency.  The header keeps only the small `ged_draw_scene_available`
declaration needed by `GED_CHECK_DRAWABLE`.

The `ged_test_state_services` default regression was also split to respect the
180 second per-test limit on this FreeBSD runner.  The default path keeps the
state-service checks plus core event coverage through the polyclip export path
that exercises the narrowed `ged_export_polygon` API.  The extended
command-event sweep after that point is now opt-in behind
`BRLCAD_RUN_SLOW_TESTS`; the lint diagnostic-group and scripted `red` editor
checks are also slow-gated because they pushed the default run to repeated
180 second timeouts here.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad mged qged`,
`ninja -C build ged_test_state_services`,
`ctest --test-dir build -R "^ged_test_state_services$" --timeout 180
--output-on-failure` completing in 90.70 seconds, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 49.27 seconds.

The public TclCAD draw command header has been narrowed.  `include/tclcad/draw.h`
now forward declares `struct ged` and `struct bu_vls`, no longer includes
`dm.h` or `ged.h`, and exposes draw-view arguments as opaque
`void *draw_view_ctx` values.  The exported libtclcad command entry points cast
that opaque context back to the retained `struct bsg_view *` internally before
using the staged BSG-backed view state, preserving the current implementation
while removing direct BSG view spelling from the public TclCAD draw API.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged qged btclsh`,
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene)$"
--timeout 180 --output-on-failure` completing 2/2 passing in 49.19 seconds,
and `ctest --test-dir build -R "^tclcad_test_ged_cmds_exist$" --timeout 180
--output-on-failure` completing in 0.18 seconds.

The RT sketch legacy polygon adapter public boundary has been narrowed.  The
installed `include/rt/primitives/sketch_legacy_bsg.h` header now includes
`rt/view.h` and exposes sketch import/export through `rt_view_polygon_ref` plus
opaque `void *` view contexts instead of including `bsg/polygon.h`, forward
declaring `struct bsg_view`, or exporting `bsg_polygon_ref`.  The retained BSG
view cast, BSG polygon-ref token conversion, and BSG polygon record read/write
logic are localized in `src/librt/primitives/sketch/polygons_legacy_bsg.c`.
The RT view legacy wrapper now passes neutral polygon refs directly to the
sketch adapter, and the GED draw-view polygon sketch import/export bridge maps
between its private GED handle and the neutral RT handle rather than receiving
or passing BSG polygon refs at that boundary.

The `rt_bv_poly_sketch` roundtrip test now stays off direct libbsg linking and
no longer constructs BSG polygon data in the test.  Its imported-sketch and
non-origin-plane roundtrip checks now use RT-owned sketch polygon data through
`db_sketch_to_polygon`, `db_sketch_polygon_to_sketch`,
`rt_sketch_polygon_bg_polygon`, and `rt_sketch_polygon_destroy`.  This keeps
the model-space contour preservation checks intact while moving the test out
of the retained BSG view-polygon adapter path.  The target now links
`librt;libbg;libbu`; generated link commands can still carry libbsg
transitively through retained librt ownership until that lower-level library
migrates.  The pivot guard rejects direct BSG polygon/view includes or calls,
legacy sketch-adapter use, and a direct libbsg relink for this test.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_bv_poly_sketch`,
`./build/src/librt/tests/rt_bv_poly_sketch src/librt/tests/sketch.g`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene)$" --timeout 180
--output-on-failure` completing 2/2 passing in 49.84 seconds.

The RT edit legacy adapter public boundary has also been narrowed.  The
installed `include/rt/edit_legacy_bsg.h` header no longer forward declares or
spells `struct bsg_view`; `rt_edit_view_from_bsg`, `rt_edit_create_bsg`,
`rt_edit_reinit_bsg`, and `rt_edit_knob_cmd_process_bsg` now accept opaque
`const void *view_ctx` values.  The retained cast back to `const struct
bsg_view *` is now centralized in `src/librt/view_legacy_bsg.c`, and the
implementation continues to read view state through the RT view legacy adapter
instead of reaching into BSG view fields directly.  The pivot guard now
requires the opaque context token in the public edit legacy header and rejects
public BSG include or `struct bsg_view` exposure there.

Verification on FreeBSD/clang passed with:
`ninja -C build librt libqtcad mged qged rt_view_legacy_bsg`,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|rt_view_legacy_bsg|test_qtcad_obol_edit_preview|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 8.69 seconds,
`git diff --check`, and a direct scan of `include/rt/edit_legacy_bsg.h` finding
no public BSG include or `struct bsg_view` token.

The public DM backend render-item dispatch boundary has been narrowed.  The
installed `include/dm.h` header no longer forward declares or names
`bsg_render_item` or `bsg_render_request`; `dm_backend_ops::draw_item`,
`dm_backend_ops::invalidate_item`, `dm_backend_draw_item`, and
`dm_backend_invalidate_item` now use opaque `const void *render_item_ctx`
handles.  The retained BSG render-item validation, source-identity fallback,
and dispatch to registered backend ops are localized in
`src/libdm/dm-generic.c`.  The GL backend keeps its existing
`gl_draw_item(struct dm *, const struct bsg_render_item *)` implementation, but
`src/libdm/dm-gl_lod.cpp` now registers private opaque-context adapter
callbacks in `gl_backend_ops`, so the public DM dispatch table no longer needs
to expose BSG render-item types.

The libdm backend regression test callback now receives the same opaque
context and casts locally for the retained BSG-specific assertions.  The pivot
guard now requires the opaque render-item context tokens in `include/dm.h` and
rejects public `bsg_render_item` or `bsg_render_request` spelling there.

Verification on FreeBSD/clang passed with:
`ninja -C build libdm libdmgl test_dm_backend_draw_item libged`,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|dm_backend_draw_item|libbsg_api_hygiene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 49.54 seconds,
`ninja -C build libtclcad libqtcad mged qged`, `git diff --check`, and a direct
scan of `include/dm.h` finding no public `bsg_render_item`,
`bsg_render_request`, BSG backend-adapter text, BSG render struct declaration,
or BSG include token.

The public GED draw-scene header has been narrowed at the draw-mode and
draw-appearance boundary.  `include/ged/bsg_ged_draw.h` no longer includes
`bsg/draw_intent.h`, exposes `bsg_draw_mode`, or names
`bsg_appearance_settings`.  Public shape and group records now expose
draw-mode as integer legacy mode values, `ged_draw_default_mode` returns an
integer mode, and `ged_draw_transaction::appearance` is an opaque borrowed
backend appearance context.  The retained interpretation of that context as
`struct bsg_appearance_settings` is localized in
`src/libged/bsg_ged_draw_transactions.c`.

The qtcad private legacy bridge now includes `bsg/draw_intent.h` directly for
its retained draw-mode static asserts and BSG appearance storage, instead of
receiving those definitions indirectly through the installed GED draw header.
The pivot guard now requires the neutral draw-mode and opaque appearance tokens
in `include/ged/bsg_ged_draw.h` and rejects public `bsg/draw_intent.h`,
`bsg_draw_mode`, or `bsg_appearance_settings` exposure there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libqtcad mged qged`,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 9.09 seconds,
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene)$" --timeout 180
--output-on-failure` completing 2/2 passing in 49.24 seconds, `git diff --check`,
and a direct scan of `include/ged/bsg_ged_draw.h` finding no
`bsg/draw_intent.h`, `bsg_draw_mode`, or `bsg_appearance_settings` token.

The public GED draw-scene header has also been narrowed at the view-context
boundary.  `include/ged/bsg_ged_draw.h` no longer forward declares or names
`struct bsg_view`; `ged_draw_transaction::view`,
`ged_draw_group_record::view`, the view-record iteration APIs,
`ged_draw_shape_ref_publish_primitive_wireframe`,
`ged_draw_view_selection_set_highlighted_shape_ref`,
`ged_draw_path_state`, `ged_draw_group_record_in_view`,
`ged_draw_list_paths`, `ged_draw_has_paths`, and `ged_draw_clear_view` now
expose borrowed view contexts through opaque `void *` handles.  The retained
casts back to `struct bsg_view *` are confined to
`src/libged/bsg_ged_draw_records.c`, `src/libged/bsg_ged_draw_refs.c`, and
`src/libged/bsg_ged_draw_tree.c`, while qtcad's private legacy bridge casts the
opaque GED transaction view back to BSG only while translating into its private
wrapper storage.

The pivot guard now requires the opaque view tokens in the installed GED
draw-scene header and rejects public `struct bsg_view`/`bsg_view` spelling from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libqtcad mged qged`,
`ninja -C build ged_test_view_command ged_test_selection_semantics
test_qtcad_obol_draw_sync test_qtcad_obol_controller`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 6/6 passing in 49.65 seconds.
A direct scan of `include/ged/bsg_ged_draw.h` found no `struct bsg_view`,
`bsg_view`, `bsg/draw_intent.h`, `bsg_draw_mode`, or
`bsg_appearance_settings` token.

Neutral GED view-context accessors have been added as a staging boundary before
the larger task of hiding `struct ged`'s retained BSG view storage.
`GED_VIEW_NULL` is now a `void *` null token, and `include/ged/defines.h`
exports `ged_view_active_ctx`, `ged_view_active_ctx_set`, and
`ged_view_set_ctx` for borrowed current-view and view-set context access.
`include/ged/view.h`'s public `GED_CHECK_VIEW` macro now uses
`ged_view_active_ctx` instead of reading `gedp->ged_gvp` directly.

The qtcad private legacy bridge now uses those GED accessors for
`qg_legacy_view_ged_active_get`, `qg_legacy_view_ged_active_set`,
`qg_legacy_view_ged_view_set_add`, `qg_legacy_view_ged_view_set_remove`, and
`qg_legacy_view_ged_view_set_attach`.  The retained casts back to
`struct bsg_view *`/`struct bsg_view_set *` remain confined to
`src/libqtcad/QgLegacyView.cpp`, which is already the private qtcad retained
view bridge.

The pivot guard now requires the neutral GED view-context accessor tokens in
the installed GED headers and qtcad legacy bridge, keeps `GED_CHECK_VIEW` off
direct `ged_gvp` access, and rejects direct `gedp->ged_gvp` or
`gedp->ged_views` reach-through from returning to `src/libqtcad/QgLegacyView.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libqtcad mged qged`,
`ninja -C build ged_test_view_command ged_test_selection_semantics
test_qtcad_obol_draw_sync test_qtcad_obol_controller`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 6/6 passing in 49.65 seconds.
A direct scan of `src/libqtcad/QgLegacyView.cpp` and `include/ged/view.h`
found no direct `gedp->ged_gvp`, `gedp->ged_views`, or `&gedp->ged_views`
access.

The neutral GED view-context boundary has been extended to common borrowed
view-set operations.  `include/ged/defines.h` now exports
`ged_view_set_views_ctx` and `ged_view_find_ctx`, with the retained
`rt_view_set_views_bsg(&gedp->ged_views)` and
`rt_view_set_find_view_bsg(&gedp->ged_views, ...)` calls localized in
`src/libged/ged.cpp`.  TclCAD wrapper, refresh, autoview, and edit-redraw
paths now route view-set iteration, view lookup, and current-view assignment
through the neutral GED accessors instead of reaching into `gedp->ged_views`,
`top->to_gedp->ged_views`, or `gedp->ged_gvp` directly.  Those files still
consume retained `struct bsg_view` state where they are part of the
transitional TclCAD drawing layer; this slice only narrows the GED storage
boundary.

The pivot guard now requires the new GED view-set accessor tokens in
`include/ged/defines.h`, removes the old `rt_view_set_views_bsg`/
`rt_view_set_find_view_bsg` requirements from the migrated TclCAD batch, and
rejects direct `ged_gvp`, `ged_views`, or view-set RT-wrapper calls from
returning to `src/libtclcad/wrapper.c`, `src/libtclcad/view/refresh.c`,
`src/libtclcad/view/draw.c`, and `src/libtclcad/view/autoview.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad mged qged`,
`ninja -C build ged_test_view_command ged_test_selection_semantics
test_qtcad_obol_draw_sync test_qtcad_obol_controller`, and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 7/7 passing in 49.89 seconds.
A direct scan of the migrated TclCAD batch found no direct `gedp->ged_gvp`,
`gedp->ged_views`, `to_gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` tokens.

The neutral GED view-context accessor migration has been extended across the
smaller TclCAD view-object command wrappers.  `src/libtclcad/view/lines.c`,
`src/libtclcad/view/labels.c`, `src/libtclcad/view/faceplate.c`,
`src/libtclcad/view/axes.c`, and `src/libtclcad/view/arrows.c` now use
`ged_view_find_ctx` for borrowed view lookup instead of
`rt_view_set_find_view_bsg(&gedp->ged_views, ...)`.  The data-line command's
temporary active-view swaps now use `ged_view_active_ctx` and
`ged_view_active_ctx_set`, keeping `gedp->ged_gvp` storage writes inside
libged.

The pivot guard now removes the old `rt_view_set_find_view_bsg` requirement
from that migrated TclCAD view-command batch, preserves remaining legitimate
retained RT view-state tokens for faceplate and axes state access, and rejects
direct `ged_gvp`, `ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning to the migrated files.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged qged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 7/7 passing in 49.82 seconds.
A direct scan of `src/libtclcad/view/lines.c`,
`src/libtclcad/view/labels.c`, `src/libtclcad/view/faceplate.c`,
`src/libtclcad/view/axes.c`, and `src/libtclcad/view/arrows.c` found no direct
`gedp->ged_gvp`, `gedp->ged_views`, or `rt_view_set_find_view_bsg` tokens.

The TclCAD framebuffer and polygon command paths have also been moved off
direct GED view-storage access.  `src/libtclcad/fb.c` now resolves framebuffer
mode/listener views with `ged_view_find_ctx` instead of
`rt_view_set_find_view_bsg(&gedp->ged_views, ...)`.  `src/libtclcad/polygons.c`
now uses `ged_view_find_ctx` for polygon command view lookup and
`ged_view_active_ctx_set` for the polygon-mode commands that intentionally make
their resolved view active.  The immediate polygon snap preparation now uses
the already-resolved borrowed `gdvp`, avoiding direct `gedp->ged_gvp` rereads
inside the TclCAD command file.

The pivot guard now removes the old `rt_view_set_find_view_bsg` requirement
from `src/libtclcad/fb.c` and `src/libtclcad/polygons.c`, requires the neutral
GED accessor tokens there, and rejects direct `ged_gvp`, `ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning.  Their retained RT view-operation calls remain allowed for this
transitional stage.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged qged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 7/7 passing in 50.10 seconds.
A direct scan of `src/libtclcad/fb.c` and `src/libtclcad/polygons.c` found no
direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` tokens.

The TclCAD mouse command layer has been moved off direct GED view-storage
access.  `src/libtclcad/mouse.c` now uses `ged_view_find_ctx` for named view
lookup instead of `rt_view_set_find_view_bsg(&gedp->ged_views, ...)`, and uses
`ged_view_active_ctx_set` before dispatching GED commands that intentionally
operate on the resolved view.  Mouse snap and view-math helper calls now use
the already-resolved borrowed `gdvp` where they previously reread
`gedp->ged_gvp`.

The pivot guard now removes the old `rt_view_set_find_view_bsg` requirement
from `src/libtclcad/mouse.c`, requires neutral GED accessor tokens there, and
rejects direct `ged_gvp`, `ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning.  Retained mouse
view-operation calls remain allowed for this transitional stage.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged qged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 7/7 passing in 49.78 seconds.
A direct scan of `src/libtclcad/mouse.c` found no direct `gedp->ged_gvp`,
`gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` tokens.

The remaining TclCAD command-table direct GED view-storage reach-throughs have
been migrated.  `src/libtclcad/commands.c` now uses `ged_view_find_ctx` for
named view lookup, `ged_view_set_views_ctx` for view-set iteration,
`ged_view_active_ctx`/`ged_view_active_ctx_set` for current-view reads and
command dispatch, and `ged_view_set_ctx` for the retained
`rt_view_init_bsg`/`rt_view_set_add_view_bsg` view registration path.  This
keeps the retained BSG view-set storage behind the GED accessor boundary while
leaving the transitional TclCAD view-operation calls intact.

The pivot guard now removes the old `rt_view_set_views_bsg` and
`rt_view_set_find_view_bsg` requirements from `src/libtclcad/commands.c`,
requires the neutral GED accessor tokens there, and rejects direct `ged_gvp`,
`ged_views`, `rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg`
reach-through from returning.  Together with the previous wrapper, refresh,
draw, autoview, view-object, framebuffer, polygon, and mouse slices, the
current TclCAD file batch no longer directly reaches into GED's retained view
storage.

Verification on FreeBSD/clang passed with:
`ninja -C build libtclcad mged qged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|tclcad_test_ged_cmds_exist|ged_test_view_command|ged_test_selection_semantics|test_qtcad_obol_draw_sync|test_qtcad_obol_controller)$"
--timeout 180 --output-on-failure` completing 7/7 passing in 50.01 seconds.
A direct scan of `src/libtclcad` found no remaining direct `gedp->ged_gvp`,
`gedp->ged_views`, `to_gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` tokens outside the expected retained
`rt_view_set_add_view_bsg`/`rt_view_init_bsg` registration calls now routed
through `ged_view_set_ctx`.

The neutral GED view-context accessor migration has been extended into the
first libged/gsh command batch.  `src/libged/zap/zap2.cpp`,
`src/libged/draw/draw2.cpp`, `src/libged/erase/erase2.cpp`,
`src/libged/who/who2.cpp`, `src/libged/who/who_solids.cpp`,
`src/libged/dm/screengrab.c`, `src/libged/selection_state.cpp`, and
`src/gtools/gsh/gsh.cpp` now route active-view reads, named-view lookup, and
view-set iteration through `ged_view_active_ctx`, `ged_view_find_ctx`, and
`ged_view_set_views_ctx` instead of reading `gedp->ged_gvp`,
`gedp->ged_views`, or calling `rt_view_set_views_bsg`/
`rt_view_set_find_view_bsg` directly.

The `who` command paths now keep their view handles opaque as GED draw
contexts and no longer include `rt/view_legacy_bsg.h`.  The retained RT
view-operation calls in `zap`, `draw`, `erase`, `screengrab`, and `gsh` remain
transitional, but they now consume views obtained from the neutral GED
accessor boundary.  The pivot guard now requires the neutral accessor tokens in
this migrated libged/gsh batch, removes stale `rt_view_set_*_bsg` expectations
from those files, rejects direct `ged_gvp`/`ged_views` reach-through there, and
no longer requires the legacy RT view header in files that have stopped using
it.

FreeBSD/clang also exposed a strict declaration issue while rebuilding the
geometry shell: `src/gtools/gsh/gsh.cpp` used `ged_draw_scene_hash` without
including its declaration owner.  `gsh.cpp` now includes
`ged/bsg_ged_draw.h` explicitly.

Verification on FreeBSD/clang passed with:
`ninja -C build libged gsh mged qged` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|ged_test_selection_semantics|ged_test_state_services|ged_test_mged_bsg|regress-mged_draw_e_erase_d_who|regress-mged_draw|regress-mged_erase|regress-mged_refresh|regress-mged_who)$"
-E "^slow-" --timeout 180 --output-on-failure` completing 11/11 passing in
143.49 seconds.  The longest member was `ged_test_state_services` at 91.35
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
scalar active-view libged command batch.  `src/libged/arot/arot.c`,
`src/libged/zoom/zoom.c`, `src/libged/orient/orient.c`,
`src/libged/keypoint/keypoint.c`, `src/libged/isize/isize.c`,
`src/libged/get_eyemodel/get_eyemodel.c`, `src/libged/eye_pos/eye_pos.c`,
`src/libged/model2view/model2view.c`,
`src/libged/model2view_lu/model2view_lu.c`,
`src/libged/grid2view_lu/grid2view_lu.c`,
`src/libged/model2grid_lu/model2grid_lu.c`, and
`src/libged/grid2model_lu/grid2model_lu.c` now borrow the current view through
`ged_view_active_ctx` before calling the retained RT view adapters for
coord/scale/orientation/keypoint/eye-position/matrix conversion state.  This
keeps the temporary BSG view type at the operation boundary but stops these
commands from reading GED's retained `ged_gvp` storage directly.

The pivot guard now requires `ged_view_active_ctx` in this scalar command
batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_view_command|ged_test_selection_semantics|ged_test_state_services|regress-mged_arot|regress-mged_eye_pt|regress-mged_keypoint|regress-mged_orientation|regress-mged_view|regress-mged_zoom|regress-mged_refresh|regress-mged_draw)$"
-E "^slow-" --timeout 180 --output-on-failure` completing 13/13 passing in
146.47 seconds.  The longest member was `ged_test_state_services` at 93.55
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
framebuffer/display-facing libged command batch.  `src/libged/fbclear/fbclear.c`,
`src/libged/fb2pix/fb2pix.c`, `src/libged/pix2fb/pix2fb.c`,
`src/libged/png2fb/png2fb.c`, `src/libged/overlay/overlay.c`,
`src/libged/dm/ert.cpp`, and `src/libged/rt/rt.c` now borrow the active view
through `ged_view_active_ctx` and keep display-manager lookup,
framebuffer-mode get/set, perspective, and plot-overlay scale behind opaque
`rt_view_context_*_bsg` helpers.  This keeps the existing framebuffer and
raytrace command behavior in place while removing another direct dependency on
GED's retained `gedp->ged_gvp` storage and the typed retained RT view adapters
from these libged command implementations.

The pivot guard now requires `ged_view_active_ctx` in this display-facing
command batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.  A follow-up guard tightening now also requires the opaque
display-manager, framebuffer-mode, perspective, and scale context helpers and
rejects the old typed retained RT view adapter calls from returning to this
cluster.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|tcl_overlay_bsg)|test_qtcad_obol_overlay_sync|regress-mged_(overlay|refresh|draw|view|rt))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 11/11 passing in
143.82 seconds.  The longest member was `ged_test_state_services` at 92.39
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
second scalar view-state libged command batch.  `src/libged/view2model/view2model.c`,
`src/libged/view2model_lu/view2model_lu.c`,
`src/libged/view2grid_lu/view2grid_lu.c`,
`src/libged/view2model_vec/view2model_vec.c`,
`src/libged/m2v_point/m2v_point.c`, `src/libged/v2m_point/v2m_point.c`,
`src/libged/pmodel2view/pmodel2view.c`, `src/libged/rot_point/rot_point.c`,
`src/libged/scale/scale.c`, `src/libged/rmat/rmat.c`,
`src/libged/pmat/pmat.c`, `src/libged/perspective/perspective.c`,
`src/libged/setview/setview.c`, `src/libged/view/aet.c`,
`src/libged/view/viewdir.c`, `src/libged/view/ypr.c`,
`src/libged/view/qvrot.c`, `src/libged/view/quat.c`,
`src/libged/view/size.c`, `src/libged/view/center.cpp`,
`src/libged/view/eye.c`, `src/libged/view/lookat.c`, and
`src/libged/view/align.c` now borrow the active view through
`ged_view_active_ctx` before calling retained RT view adapters for matrix,
scale, perspective, center, eye, AET/orientation, and view update state.

This batch deliberately leaves the retained RT adapters in place.  The cleanup
is at the GED ownership boundary: these command implementations no longer read
`gedp->ged_gvp` directly, and `qvrot`'s local `usejoy` helper now receives the
borrowed view explicitly instead of reaching back through `gedp`.

The pivot guard now requires `ged_view_active_ctx` in this scalar view-state
batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services)|regress-mged_(view|orientation|arot|eye_pt|keypoint|zoom|refresh|draw|m2v|v2m|center|scale|setview|perspective|rmat|pmat|aet|ae|qvrot|ypr|quat|size))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 18/18 passing in
148.04 seconds.  The longest member was `ged_test_state_services` at 92.97
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through an
output/snapshot libged command batch.  `src/libged/png/png.c`,
`src/libged/ps/ps.c`, `src/libged/plot/plot.c`,
`src/libged/savekey/savekey.c`, `src/libged/view/saveview.c`, and
`src/libged/rtwizard/rtwizard.c` now borrow the active view through
`ged_view_active_ctx` before calling retained RT view adapters or GED draw
visible-record/listing helpers for projection and view snapshot state.

This batch keeps the transitional renderer/export logic intact.  The cleanup is
at the GED ownership boundary: `png`, `ps`, and `plot` helper routines now
receive the borrowed view explicitly for visible-record enumeration, `saveview`
passes the borrowed view into draw-path listing, and `savekey`/`rtwizard`
snapshot view state through the borrowed active view instead of reading
`gedp->ged_gvp` directly.

The pivot guard now requires `ged_view_active_ctx` in this output/snapshot
batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|rtwizard_bsg)|regress-mged_(plot|ps|saveview|draw|refresh|view))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 12/12 passing in
149.46 seconds.  The longest member was `ged_test_state_services` at 97.14
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
view-interaction state libged batch.  `src/libged/grid/grid.c`,
`src/libged/rect/rect.c`, `src/libged/view/snap.c`,
`src/libged/view/knob.c`, and `src/libged/view/data_lines.c` now borrow the
active view through `ged_view_active_ctx` before calling retained RT view-state
adapters or GED draw view helpers for grid snapping, interactive rectangle
state, snap candidates, knob state, and Tcl data-line overlays.

This batch keeps the transitional view-state adapters and BSG-backed draw
helpers in place, but removes direct command-level access to GED's retained
view storage.  `grid_vsnap`, `rect_zoom`, and the data-line subcommand
callbacks now receive the borrowed view explicitly.  The data-lines comment
was also updated to describe the active GED view requirement without naming
the retained storage field.

FreeBSD/clang caught an unused `gedp` parameter after the `grid_vsnap` helper
signature change; the helper now only takes the borrowed view it uses.

The pivot guard now requires `ged_view_active_ctx` in this view-interaction
batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_faceplate|drawing_select)|test_qtcad_obol_faceplate_sync|test_qtcad_obol_snap|regress-mged_(view|draw|refresh|snap|rect|grid|knob|data|center|zoom))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 15/15 passing in
162.28 seconds.  The longest member was `ged_test_state_services` at 92.02
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
small state/overlay libged command batch.  `src/libged/adc/adc.c`,
`src/libged/rot/rotate_about.c`, `src/libged/lod/lod.c`, and
`src/libged/lint/lint.cpp` now borrow the active view through
`ged_view_active_ctx` before calling retained ADC, rotate-about, LOD, or GED
draw overlay helpers.

This keeps the transitional RT/GED draw adapters in place while removing
direct command-level access to GED's retained active-view storage.  A
follow-up ADC cleanup now keeps its local anchor/print helpers on opaque view
contexts and routes ADC state get/set plus scale, model-to-view, and
view-to-model reads through `rt_view_context_*_bsg` helpers.  `lint` publishes
overlay plot lines using the borrowed view rather than reading the field
directly.

The pivot guard now requires `ged_view_active_ctx` in this state/overlay batch
and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_lod|lod_crossrun)|regress-(mged_(adc|view|draw|refresh|rot_view)|ged_lint_raytrace))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 13/13 passing in
161.40 seconds.  The longest member was `ged_test_state_services` at 93.32
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
view faceplate libged command batch.  `src/libged/view/faceplate/faceplate.c`,
`src/libged/view/faceplate/faceplate_grid.c`,
`src/libged/view/faceplate/faceplate_axes.c`, and
`src/libged/view/faceplate/interactive_rect.c` now borrow the active view
through `ged_view_active_ctx` before calling retained RT faceplate, grid,
axes, interactive-rectangle, framebuffer-mode, and HUD sync adapters.

This keeps the transitional RT faceplate state adapters in place while
removing direct command-level access to GED's retained active-view storage.
The top-level `view faceplate` command now uses the same borrowed view for its
current-view check and post-command HUD sync.

The pivot guard now requires `ged_view_active_ctx` in these faceplate command
files and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_faceplate)|test_qtcad_obol_faceplate_sync|regress-mged_(view|draw|refresh))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 10/10 passing in
145.91 seconds.  The longest member was `ged_test_state_services` at 93.12
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
diagnostic overlay libged command batch.  `src/libged/gdiff/gdiff.c`,
`src/libged/gqa/gqa.cpp`, `src/libged/check/check_overlaps.c`, and
`src/libged/brep/plot.cpp` now borrow the active view through
`ged_view_active_ctx` before falling back to retained GED draw feature,
line-layer, or indexed face-set helpers.

This keeps the neutral diagnostic line-layer publish path first for gqa,
check, and BREP plot output.  When that path does not handle the diagnostic
payload, the fallback now uses the borrowed view instead of reading GED's
retained active-view storage directly.  `gdiff` similarly removes and
publishes its retained draw features through the borrowed active view.

FreeBSD/clang caught a `-Wshadow` issue in the first version of this batch:
the new gqa borrowed-view pointer shadowed the existing per-axis loop
variable.  The pointer is now named `active_view`, and the build is
warning-clean again.

The pivot guard now requires `ged_view_active_ctx` in these diagnostic command
files and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|gqa)|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|regress-(gdiff|gchecker|gqa|mged_(view|draw|refresh)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 15/15 passing in
156.45 seconds.  The longest member was `ged_test_state_services` at 93.47
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
view-math/query libged command batch.  `src/libged/brep/pick.cpp`,
`src/libged/bot/pick.cpp`, `src/libged/solids_on_ray/solids_on_ray.c`,
`src/libged/polyclip.cpp`, `src/libged/metaball/metaball.c`,
`src/libged/pipe/pipe.c`, `src/libged/move_arb_edge/move_arb_edge.c`, and
`src/libged/bot/edbot.c` now borrow the active view through
`ged_view_active_ctx` before calling retained RT view-matrix, center,
rotation, plane, and scale adapters.

Later context-helper cleanups advanced this original accessor-only batch to
opaque RT view-context helper calls.  `src/libged/brep/pick.cpp`,
`src/libged/bot/pick.cpp`, and `src/libged/solids_on_ray/solids_on_ray.c`
now use context helpers for center, rotation, model-to-view, and scale reads.
`src/libged/polyclip.cpp`, `src/libged/metaball/metaball.c`,
`src/libged/pipe/pipe.c`, `src/libged/move_arb_edge/move_arb_edge.c`, and
`src/libged/bot/edbot.c` now use context helpers for their plane, scale,
model-to-view, and view-to-model reads.

This batch keeps the existing primitive selection/edit math intact.  The
cleanup is at the GED ownership boundary: BREP/BoT pick ray construction,
`solids_on_ray`, polygon overlap/fill projection, metaball/pipe nearest-point
and add-point workflows, `move_arb_edge`, and BoT nearest edge/point helpers
no longer read GED's retained active-view storage directly.

The pivot guard now requires `ged_view_active_ctx` in these view-math/query
files and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|regress-(bots|solids|mged_(3ptarb|arb|draw|refresh|view)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 19/19 passing in
166.18 seconds.  The longest member was `ged_test_state_services` at 91.66
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
shared libged view utility helper layer in `src/libged/vutil.c`.  `_ged_do_rot`,
`_ged_do_slew`, `_ged_do_tra`, and `nmg_plot_eu` now borrow the active view
through `ged_view_active_ctx` before calling retained RT rotate-about, matrix,
center, update, inverse-size, and diagnostic line-layer fallback helpers.

This keeps the existing view transform semantics intact while removing direct
helper-level access to GED's retained active-view storage.  The integer helper
paths now return `BRLCAD_ERROR` if called without an active view, matching the
failure mode used by surrounding command code instead of dereferencing retained
storage directly.  The NMG diagnostic plot path still publishes through the
neutral diagnostic line-layer hook first and only falls back to retained draw
view features when a borrowed active view is available.

The pivot guard now requires `ged_view_active_ctx` in `src/libged/vutil.c` and
rejects direct `gedp->ged_gvp`, `gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_basic|drawing_select)|test_qtcad_obol_(draw_sync|snap|selection_sync)|regress-(mged_(arot|mrot|orot|qorot|qvrot|rot_view|tra_view|center|lookat|view|draw|refresh|draw_e_erase_d_who|setview|sca_view)|solids))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 26/26 passing in
175.90 seconds.  The longest member was `ged_test_state_services` at 92.86
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
libged selection command implementation in `src/libged/select/select.c`.
Bot point selection, visible-record selection, partial selection, and
interactive-rectangle selection now borrow the active view through
`ged_view_active_ctx` before calling retained model-to-view,
visible-record-enumeration, and faceplate interactive-rectangle adapters.

A later context-helper cleanup advanced `src/libged/select/select.c` from the
accessor-only stage to opaque RT view-context model-to-view and
interactive-rectangle snapshot helpers.  The local BoT point, visible-record,
and partial-selection helpers now receive the opaque view context directly.

This keeps the existing selection behavior intact while removing direct
command/helper-level access to GED's retained active-view storage.  The local
selection helpers now receive the borrowed view explicitly, so the command
entry points own active-view lookup and the helper layer only consumes the
view it was handed.

The pivot guard now requires `ged_view_active_ctx` in
`src/libged/select/select.c` and rejects direct `gedp->ged_gvp`,
`gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|regress-(bots|mged_(view|draw|refresh|draw_e_erase_d_who)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 14/14 passing in
163.83 seconds.  The longest member was `ged_test_state_services` at 92.73
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
residual BoT command batch.  `src/libged/bot/bot.cpp`,
`src/libged/bot/check.cpp`, `src/libged/bot/bot_fuse.c`, and
`src/libged/bot/dump/bot_dump.cpp` now borrow the active view through
`ged_view_active_ctx` before calling retained GED draw feature, line, and
visible-record helpers.

This keeps the existing BoT visualization/export behavior intact while
removing direct command/helper-level access to GED's retained active-view
storage.  BoT face plotting, BoT check edge overlays, and `bot_fuse`
open-edge overlays now use the borrowed view for retained feature updates.
`bot_dump` visible-record export now obtains the active view through the same
neutral accessor before enumerating visible records.

The pivot guard now requires `ged_view_active_ctx` in these BoT command files
and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|selection_semantics|state_services|drawing_select|bot_dumping)|test_qtcad_obol_(pick|selection_sync)|regress-(bots|mged_(view|draw|refresh|draw_e_erase_d_who)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 14/14 passing in
165.94 seconds.  The longest member was `ged_test_state_services` at 94.39
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
overlay-label command paths in `src/libged/illum/illum.c` and
`src/libged/nmg/nmg.c`.  `labelvert` and `labelface` now borrow the active
view through `ged_view_active_ctx` before visible-record enumeration, retained
label feature replacement, and display-manager repaint notification.

This keeps the existing retained label feature behavior intact while removing
direct command-level access to GED's retained active-view storage.  The
commands now fail cleanly if invoked without an active view rather than
dereferencing retained storage directly.

The pivot guard now requires `ged_view_active_ctx` in `src/libged/illum/illum.c`
and `src/libged/nmg/nmg.c`, and rejects direct `gedp->ged_gvp`,
`gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|nmg_(mk|copy)|ged_test_(view_command|selection_semantics|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-mged_(view|draw|refresh|draw_e_erase_d_who))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 16/16 passing in
169.81 seconds.  The longest member was `ged_test_state_services` at 95.92
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
path/query/snapshot helper batch.  `src/libged/who/who.c`,
`src/libged/garbage_collect/garbage_collect.cpp`,
`src/libged/how/how.c`, `src/libged/brep/tikz.cpp`, and
`src/libged/check/check.c` now borrow the active view through
`ged_view_active_ctx` before using retained path, visible-record, display-list,
AET, and view-info/orientation snapshot helpers.

This keeps the existing command behavior intact while removing direct
command-level access to GED's retained active-view storage.  `who` now
enumerates real paths through the borrowed opaque context, `garbage_collect`
inspects the draw list through a neutral active-view lookup, `how` resolves
display listings through the borrowed opaque context, `brep tikz` snapshots
AET through the RT adapter, and `check -g` snapshots view information and
orientation through the same adapter boundary.

The pivot guard now requires `ged_view_active_ctx` in these path/query/snapshot
helper files and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|brep_ppx|brep_cdt_hole_face17|brep_cdt_hole_face27|ged_test_(view_command|selection_semantics|state_services|drawing_basic|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(gchecker|mged_(who|view|draw|refresh|draw_e_erase_d_who)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 17/17 passing in
167.32 seconds.  The longest member was `ged_test_state_services` at 97.79
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
residual command/helper batch covering `src/libged/open/open.cpp`,
`src/libged/mirror/mirror.c`, `src/libged/joint/joint.c`,
`src/libged/rtcheck/rtcheck2.cpp`, and `src/libged/nirt/nirt.cpp`.  These
commands now borrow the active view through `ged_view_active_ctx` before
calling retained RT view adapters or private GED draw view adapters.

This keeps the existing retained behavior intact while removing another set of
ordinary command-level reads from GED's retained active-view storage.
Database-open unit-conversion setup, mirror post-draw refresh, joint mesh line
publishing/removal, rtcheck prefix cleanup and glyph scale setup, and
nirt/qray center, direction, glyph scale, feature cleanup, and vnirt
view-to-model conversion now all use the borrowed active view.

A later context-helper cleanup advanced `src/libged/rtcheck/rtcheck2.cpp` and
`src/libged/nirt/nirt.cpp` from the accessor-only stage to opaque RT
view-context scale, center, rotation, and view-to-model helpers.  It also
advanced qray single-feature cleanup and rtcheck prefix cleanup to private GED
draw-view context helpers.

The pivot guard now requires `ged_view_active_ctx` in these residual command
files and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(nirt|gchecker|mged_(mirror|view|draw|refresh|draw_e_erase_d_who)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 14/14 passing in
167.21 seconds.  The longest member was `ged_test_state_services` at 97.35
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
draw/view restore helper batch covering `src/libged/draw/loadview.cpp`,
`src/libged/draw/preview.cpp`, `src/libged/view/autoview2.cpp`,
`src/libged/draw/bigE.c`, and `src/libged/draw/draw.c`.  These paths now
borrow the active view through `ged_view_active_ctx` before calling retained
RT view, LoD, autoview, and private GED draw adapters.  `autoview -V` now
resolves named views through `ged_view_find_ctx` instead of reaching through
the retained view set directly.

This keeps the existing retained behavior intact while removing another group
of draw command/helper direct reads from GED's retained active-view storage.
Loadview and preview restore camera state through the borrowed view, autoview
selects either the active view or a neutral named-view lookup, the evaluated
wireframe command seeds draw client data from the neutral active view, and
draw/tree rendering uses the local borrowed view for LoD policy snapshots,
threshold application, shape construction, and diagnostic line-layer fallback.

The pivot guard now requires `ged_view_active_ctx` in these draw/view restore
helper files, requires `ged_view_find_ctx` for the named autoview lookup, and
rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(bots|mged_(autoview|draw_e_erase_d_who|draw|loadview|refresh|view)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 16/16 passing in
170.69 seconds.  The longest member was `ged_test_state_services` at 93.14
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through
`src/libged/ged_util.cpp`, the shared libged utility helper layer.  The file
now borrows the active view through `ged_view_active_ctx` before calling
retained RT view adapters or private GED draw view adapters.

This keeps the existing utility behavior intact while removing direct helper
access to GED's retained active-view storage.  Rotation/translation argument
helpers use the borrowed view for default coordinate reads, who/path helper
counts and argv construction list visible paths through the borrowed view,
uplot feature publishing uses the borrowed view before replacing line layers,
RT eye-model and write-script helpers snapshot view state through the borrowed
view, and visible-record export helpers enumerate against the same neutral
active-view boundary.

The pivot guard now requires `ged_view_active_ctx` in `src/libged/ged_util.cpp`
and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

A follow-up context snapshot cleanup keeps default coordinate reads,
uplot line-layer feature publishing, RT eye-model snapshots, and write-script
view-info/orientation snapshots on opaque RT or GED draw-view context
helpers in `src/libged/ged_util.cpp`.  The public GED facade exposes
`ged_draw_view_context_line_layers_replace`, while the retained BSG
line-layer replacement call stays inside `src/libged/bsg_ged_draw_view.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(nirt|gchecker|bots|mged_(autoview|draw_e_erase_d_who|draw|loadview|refresh|view)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 18/18 passing in
176.87 seconds.  The longest member was `ged_test_state_services` at 90.28
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
libged view command and view-object command dispatch paths in
`src/libged/view/view.c` and `src/libged/view/objs.cpp`.  View subcommands now
use `ged_view_active_ctx`/`ged_view_active_ctx_set` for temporary current-view
swaps, `ged_view_find_ctx` for named-view lookup, and
`ged_view_set_views_ctx` for view listing.  View-object scene-root setup now
uses the same active-view accessor pair before ensuring the requested view has
retained scene storage.

This keeps the retained independent-view, selection, and view-object behavior
intact while moving another ownership-adjacent layer off direct command-level
GED view-storage reads and writes.  The remaining direct storage in libged is
now confined to core GED ownership and display-manager binding surfaces.

The pivot guard now requires the neutral accessor tokens in `view.c` and
`view/objs.cpp`, keeps the retained independent-view adapter requirements, and
rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.

A follow-up context snapshot cleanup keeps label creation, polygon creation,
`view vZ`, and view width/height queries on opaque RT view-context helpers in
`src/libged/view/labels.c`, `src/libged/view/polygons.c`, and
`src/libged/view/view.c`, removing the old typed RT screen-to-view,
screen-point, model/view matrix, and view-info reads from those view-object
command paths.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene)|test_qtcad_obol_draw_sync|regress-(bots|mged_(autoview|draw_e_erase_d_who|draw|loadview|refresh|view)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 16/16 passing in
168.67 seconds.  The longest member was `ged_test_state_services` at 90.98
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
libged display-manager command/binding implementation in
`src/libged/dm/dm.c`.  DM name lookup, current DM resolution, DM listing,
attach-time DM name collision checks, target-view lookup, and attach-time view
set registration now obtain the active view, view table, named view, and view
set through `ged_view_active_ctx`, `ged_view_set_views_ctx`,
`ged_view_find_ctx`, and `ged_view_set_ctx`.

This keeps the existing retained behavior intact while removing direct
display-manager command reach-through to GED's retained active-view and
view-set storage.  The transitional binding points that still need retained
view semantics remain explicit: display-manager get/set,
headless-dimension fallback, view-scale handoff to libdm, and temporary view
creation still use `rt/view_legacy_bsg.h` adapters.  The remaining direct
non-test, non-adapter libged view-storage access is now confined to core GED
ownership/accessor implementation in `src/libged/ged.cpp`.

The pivot guard now requires the neutral GED view accessor tokens in
`src/libged/dm/dm.c`, requires the retained view lifecycle adapter tokens
there for transitional view creation, and rejects direct `gedp->ged_gvp`,
`gedp->ged_views`, `rt_view_set_views_bsg`, or
`rt_view_set_find_view_bsg` reach-through from returning.

Verification on FreeBSD/clang passed with:
`ninja -C build libged mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast)|test_qtcad_obol_draw_sync|regress-(bots|mged_(autoview|draw_e_erase_d_who|draw|loadview|refresh|view)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 18/18 passing in
171.84 seconds.  The longest member was `ged_test_state_services` at 94.15
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through a
first MGED helper batch covering `src/mged/overlay.c`, `src/mged/plot.c`,
`src/mged/color_scheme.c`, `src/mged/rtif.c`,
`src/mged/dm-generic.c`, `src/mged/setup.c`, and
`src/mged/attach.c`.  These files now use `ged_view_active_ctx`,
`ged_view_active_ctx_set`, and/or `ged_view_set_ctx` instead of direct
`s->gedp->ged_gvp` or `s->gedp->ged_views` access for active-view handoff,
active-view save/restore, visible-record enumeration, forwarded DM command
setup, initial MGED view registration, and copied-DM view registration.

This keeps MGED's retained behavior intact while moving another group of
application-level helpers behind the neutral GED view boundary.  Display
manager handoff, update callbacks, LoD policy snapshots, copied-view
initialization, and retained view lifecycle calls remain explicit transitional
adapter uses through `rt/view_legacy_bsg.h`.

The pivot guard now requires the neutral GED view accessor tokens in this
MGED helper batch and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.  Remaining MGED direct active-view reach-through is now
concentrated in `src/mged/cmd.cpp` and `src/mged/chgview.c`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast)|test_qtcad_obol_draw_sync|regress-(nirt|mged_(autoview|draw_e_erase_d_who|draw|loadview|overlay|plot|refresh|saveview|view|who)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 21/21 passing in
169.17 seconds.  The longest member was `ged_test_state_services` at 92.20
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
remaining direct active-view command paths in `src/mged/chgview.c`.
`edit_com`, `cmd_autoview`, and `mged_zoom` now use
`ged_view_active_ctx_set` and `ged_view_active_ctx` instead of direct
`s->gedp->ged_gvp` reads or writes for temporary current-view selection,
final active-view restore, and zoom-refresh LoD policy inspection.

This keeps MGED view command behavior intact while removing another direct
application-level dependency on GED's retained active-view storage.  Retained
view-scale, autoview, refresh, knob, and LoD policy adapter calls remain
explicit transitional uses through `rt/view_legacy_bsg.h`.

The pivot guard now requires the neutral GED view accessor tokens in
`src/mged/chgview.c` and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.  Remaining MGED direct active-view reach-through is now
confined to `src/mged/cmd.cpp`; core GED ownership/accessor storage remains
in `src/libged/ged.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast)|test_qtcad_obol_draw_sync|regress-(nirt|mged_(autoview|draw_e_erase_d_who|draw|loadview|overlay|plot|refresh|saveview|view|who|zoom|rot_view|sca_view|tra_view|size|setview)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 27/27 passing in
171.47 seconds.  The longest member was `ged_test_state_services` at 91.96
seconds, below the 180 second per-test cap.

The neutral GED view-context accessor migration has been extended through the
remaining MGED command-dispatch active-view path in `src/mged/cmd.cpp`.  GED
wrapper commands, display-manager and screengrab handoff, command-window and
winset focus changes, postscript attach/release, `blast`/`draw`, simulate
view-argument injection, and `view` staging now use `ged_view_active_ctx` and
`ged_view_active_ctx_set` instead of direct `s->gedp->ged_gvp` reads or
writes.

This preserves MGED command behavior while removing the last ordinary MGED
reach-through to GED's retained active-view storage.  The command staging and
snapshot logic still intentionally uses retained view lifecycle, display
manager, view cache, and view-math adapters through `rt/view_legacy_bsg.h`;
that adapter usage is now explicit and separate from GED storage ownership.

The pivot guard now requires the neutral GED view accessor tokens in
`src/mged/cmd.cpp` and rejects direct `gedp->ged_gvp`, `gedp->ged_views`,
`rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg` reach-through from
returning there.  Remaining direct non-test, non-adapter libged/MGED
view-storage access is now confined to core GED ownership/accessor
implementation in `src/libged/ged.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged libtclcad mged qged gsh` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast)|test_qtcad_obol_draw_sync|regress-(nirt|mged_(autoview|draw_e_erase_d_who|draw|erase|loadview|overlay|plot|ps|refresh|saveview|view|who|zoom|rot_view|sca_view|tra_view|size|setview|status|knob)))$"
-E "^slow-" --timeout 180 --output-on-failure` completing 31/31 passing in
171.87 seconds.  The longest member was `ged_test_state_services` at 90.73
seconds, below the 180 second per-test cap.

The low-level utility vlist neutralization cleanup has been extended through
`src/util/plot3-dm.c`.  The plot display utility now keeps its imported
uplot storage as `struct bg_vlblock`, walks `bg_vlist` nodes, evaluates
`BG_VLIST_*` command values during size reset, and releases plot blocks with
`bg_vlblock_free`.  The direct draw call now uses a private
`dm_draw_device_bg_vlist` wrapper, which confines the required transitional
cast back to the existing BSG-shaped backend hook inside libdm rather than
leaking that spelling back into the utility.

`src/util/CMakeLists.txt` now links `plot3-dm` directly with `librt` and
`libbg` instead of `libbsg`; `librt` remains responsible for the uplot import
bridge and `libbg` owns the neutral vlist container.  The pivot guard now
includes `src/util/plot3-dm.c` in the utility vlist neutralization file set,
requiring the BG vlist include and rejecting `bsg_vlist`, `bsg_vlblock`,
`bsg_plot_vlblock`, and `BSG_*VLIST*` names from returning there.

Verification on FreeBSD/clang passed with:
`ninja -C build plot3-dm test_dm_backend_draw_item bg_vlist` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|bg_vlist_basic)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 49.78 seconds.
The longest member was `libbsg_api_hygiene` at 40.79 seconds, below the 180
second per-test cap.

The libdm direct-vlist backend path has been neutralized to BG vlist spelling.
`src/libdm/include/calltable.h`, `src/libdm/include/private.h`,
`src/libdm/dm-generic.c`, `src/libdm/null/dm-Null.*`,
`src/libdm/txt/dm-txt.c`, `src/libdm/X/dm-X.c`,
`src/libdm/plot/dm-plot.c`, `src/libdm/postscript/dm-ps.c`,
`src/libdm/dm-gl.h`, and `src/libdm/dm-gl.c` now use `bg_vlist` callback
signatures and `BG_VLIST_*` command names for the legacy direct-vlist drawing
hooks.  The plot backend's floating uplot export now routes through
`rt_vlist_to_uplot`, preserving the existing librt dependency and avoiding a
new libdm-to-libbg dependency row for this slice.

This intentionally does not attempt to migrate libdm's retained render-item
backend or LoD drawing path; those still use BSG render-item and geometry
contracts and should move as a separate Obol/libbrlobol backend migration.
The pivot guard now checks the libdm direct-vlist file set for `bg_vlist` and
rejects `bsg/vlist.h`, `bsg_vlist`, and `BSG_VLIST_*` there.  The
`dm_backend_draw_item` test now mirrors that source hygiene check and verifies
the plot backend stays on the RT vlist uplot bridge.

Verification on FreeBSD/clang passed with:
`ninja -C build libdm dm-txt dm-ps dm-plot dm-X dm-ogl dm-swrast dm-qtgl
plot3-dm test_dm_backend_draw_item` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|bg_vlist_basic)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 49.90 seconds.
The longest member was `libbsg_api_hygiene` at 40.81 seconds, below the 180
second per-test cap.

The GED draw-scene regression has one more structural BSG probe group behind
neutral GED draw context helpers.  `include/ged/bsg_ged_draw.h` now exposes
`ged_draw_scene_context_child_at`, `ged_draw_scene_context_parent`, and
`ged_draw_scene_context_name` as opaque scene-context accessors, while
`src/libged/bsg_ged_draw_refs.c` confines the retained
`bsg_scene_child_at`, `bsg_scene_parent`, and `bsg_scene_name` calls to the
bridge implementation.

`src/libged/tests/draw/ged_draw_scene.cpp` now routes its scene child count,
child lookup, parent lookup, name lookup, and group first/last-shape recursion
through the neutral tree summary and context-navigation helpers instead of
direct `bsg_scene_child_count`, `bsg_scene_child_at`, `bsg_scene_parent`, or
`bsg_scene_name` calls.  The test still intentionally carries BSG scene-node
handles for retained structural coverage, but this slice removes another
cluster of raw scene traversal API dependency from the regression source.  The
pivot guard now requires the new neutral helper tokens and rejects the removed
direct scene traversal calls from returning to `ged_draw_scene.cpp`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 52.36 seconds.
The longest member was `libbsg_api_hygiene` at 40.74 seconds; the migrated
`ged_test_ged_draw_scene` completed in 1.41 seconds.

The GED draw transaction boundary now has neutral draw-mode and appearance
spelling.  `include/ged/bsg_ged_draw.h` exposes `ged_draw_mode` constants and
`struct ged_draw_appearance_settings` with `GED_DRAW_APPEARANCE_SETTINGS_INIT`;
`src/libged/bsg_ged_draw_transactions.c` converts that neutral record into
the retained `bsg_appearance_settings` only inside the BSG-backed transaction
implementation.

`src/libged/tests/draw/ged_draw_scene.cpp` now uses `GED_DRAW_MODE_*` and
`ged_draw_appearance_settings` for draw-state and transaction appearance
coverage, and no longer includes `bsg/defines.h`, `bsg/appearance.h`, or
`bsg/draw_intent.h`.  `src/libqtcad/QgLegacyView.cpp` also stores qtcad
legacy draw appearances as `ged_draw_appearance_settings` and compares qtcad
draw-mode constants to the GED constants rather than BSG constants.  The
pivot guard now requires the neutral GED tokens and rejects the migrated
draw-scene and qtcad adapter sources from reintroducing BSG draw-mode or
appearance-setting names.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad rt_view_legacy_bsg ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_ged_draw_scene|test_qtcad_obol_draw_sync)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 52.66 seconds.
The longest member was `libbsg_api_hygiene` at 40.86 seconds; the migrated
`ged_test_ged_draw_scene` completed in 1.42 seconds and
`test_qtcad_obol_draw_sync` completed in 0.11 seconds.

The GED draw-scene regression no longer reads the retained scene root with a
direct `bsg_view_scene_ref` call.  `include/ged/bsg_ged_draw.h` now exposes
`ged_draw_view_context_scene_root` as an opaque view-context accessor, and
`src/libged/bsg_ged_draw_scene_root.c` owns the retained BSG lookup.  The
local `test_scene_root` helper in `src/libged/tests/draw/ged_draw_scene.cpp`
uses that neutral accessor, preserving the test's intentional retained
scene-node traversal while removing one more raw BSG entry point from the
regression source.  The pivot guard now requires the helper and rejects direct
`bsg_view_scene_ref` calls from returning to the draw-scene test.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad rt_view_legacy_bsg ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_ged_draw_scene|test_qtcad_obol_draw_sync)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 52.47 seconds.
The longest member was `libbsg_api_hygiene` at 40.76 seconds; the migrated
`ged_test_ged_draw_scene` completed in 1.42 seconds and
`test_qtcad_obol_draw_sync` completed in 0.11 seconds.

The GED draw-scene regression no longer manufactures retained
`bsg_scene_ref` values or unwraps scene refs with local test helpers.
`include/ged/bsg_ged_draw.h` now exposes opaque context wrappers for first
shape lookup, shape/group ref-to-context lookup, cache-ref lookup,
context-to-shape-ref conversion, shape state/source inspection, scene
fullpath lookup, group dbpath lookup, and overlay detection.  The retained
BSG conversions and structural reads are confined to
`src/libged/bsg_ged_draw_refs.c`.

`src/libged/tests/draw/ged_draw_scene.cpp` now uses those GED draw context
helpers for first-shape lookup, shape indexing and advancement, group lookup,
cache-ref lookup, dbpath/fullpath reads, submodel state/source assertions, and
overlay checks.  The pivot guard requires the new wrapper tokens and rejects
the removed local `test_scene_ref_from_node`/`test_node_from_scene_ref`
helpers plus direct test use of the underlying GED BSG scene-ref bridge
functions.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad rt_view_legacy_bsg ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_ged_draw_scene|test_qtcad_obol_draw_sync)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 52.75 seconds.
The longest member was `libbsg_api_hygiene` at 40.69 seconds; the migrated
`ged_test_ged_draw_scene` completed in 1.41 seconds and
`test_qtcad_obol_draw_sync` completed in 0.11 seconds.

The GED draw-scene regression no longer constructs retained BSG render-item
fixtures for its backend-owned resource cache check.  The check now uses the
neutral `dm_backend_resource_get`, `dm_backend_resource_touch`,
`dm_backend_resource_invalidate`, `dm_backend_release_source`, and
`dm_backend_resource_reclaim_unseen` API directly with neutral
cache/source/backend identities.  `src/libged/tests/draw/ged_draw_scene.cpp`
therefore no longer includes `bsg/render.h` or `bsg/render_item.h` and no
longer names `bsg_render_item`, `BSG_BACKEND_*`, or `BSG_INVALIDATE_*`.

The pivot guard now rejects direct retained render-item construction/freeing,
`bsg_render_item` spelling, `BSG_BACKEND_*`, and `BSG_INVALIDATE_*` from
returning to `ged_draw_scene.cpp`, while preserving the broader retained
scene-node structural coverage that still intentionally remains in that test.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_ged_draw_scene test_dm_backend_draw_item` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|dm_backend_draw_item|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 52.62 seconds.
The longest member was `libbsg_api_hygiene` at 40.74 seconds;
`dm_backend_draw_item` completed in 0.12 seconds and
`ged_test_ged_draw_scene` completed in 1.42 seconds.

The GED draw-scene regression no longer reads or mutates retained BSG view
frame-revision state directly.  `include/ged/bsg_ged_draw.h` now exposes
opaque view-context helpers for scene attachment, frame-revision readback, and
frame-revision bumping:
`ged_draw_view_context_scene_attached`,
`ged_draw_view_context_frame_revision`, and
`ged_draw_view_context_bump_frame_revision`.  The retained
`struct bsg_view::gv_frame_rev` access and scene-ref attachment check are
confined to `src/libged/bsg_ged_draw_scene_root.c`.

`src/libged/tests/draw/ged_draw_scene.cpp` now uses those GED view-context
helpers for scene-attachment and frame-revision checks, and no longer includes
`bsg/view_state.h`, spells `struct bsg_view`, calls
`rt_view_scene_attached_bsg`, or touches `gv_frame_rev`.  The pivot guard
requires the new wrapper tokens and rejects those direct BSG view-state forms
from returning to the draw-scene regression.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 52.53 seconds.
The longest member was `libbsg_api_hygiene` at 40.70 seconds, and
`ged_test_ged_draw_scene` completed in 1.41 seconds.

The GED draw-scene regression no longer includes `rt/view_legacy_bsg.h` for
the qray feature import check.  `include/ged/bsg_ged_draw.h` now exposes
`struct ged_draw_view_feature_summary`,
`GED_DRAW_VIEW_FEATURE_SUMMARY_INIT`,
`ged_draw_view_context_feature_summary`, and
`ged_draw_view_context_feature_remove` as neutral GED view-context feature
helpers.  `src/libged/bsg_ged_draw_view.c` owns the retained
`bsg_feature_find`, `bsg_feature_record_get`, and `bsg_feature_remove` calls
behind that surface.

`src/libged/tests/draw/ged_draw_scene.cpp` now verifies qray feature
summary/removal through the GED view-context helpers instead of
`rt_view_feature_summary_bsg`, `rt_view_feature_remove_bsg`, and
`RT_VIEW_FEATURE_SUMMARY_BSG_INIT`.  The pivot guard requires the new neutral
tokens and rejects the legacy RT feature wrapper include/calls from returning
to the draw-scene regression.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 52.76 seconds.
The longest member was `libbsg_api_hygiene` at 40.81 seconds, and
`ged_test_ged_draw_scene` completed in 1.42 seconds.

The GED draw-scene regression no longer names retained BSG node types or
directly includes the BSG node/support headers for its structural scene
traversal checks.  `src/libged/tests/draw/ged_draw_scene.cpp` now uses the
test-local opaque `test_scene_context` handle for scene root, child, parent,
group, shape, source, and traversal plumbing while continuing to verify the
same retained draw-scene structure through `ged_draw_scene_context_*`,
`ged_draw_shape_*_context`, and `ged_draw_view_context_*` helpers.

The pivot guard now requires the opaque `test_scene_context` token in the
draw-scene regression and rejects direct `struct bsg_node`/`bsg_node *`
spelling plus direct BSG node/support includes such as `bsg/node.h`,
`bsg/util.h`, `bsg/scene_object.h`, `bsg/payload.h`, `bsg/draw_set.h`,
`bsg/draw_source.h`, `bsg/field.h`, `bsg/material.h`, and `bsg/tcl_data.h`
from returning to that test source.  The remaining bridge header names
`ged/bsg_ged_draw.h` and `../../bsg_ged_draw_private.h` are intentionally
retained for the current GED draw bridge stage.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 52.71 seconds.
The longest member was `libbsg_api_hygiene` at 40.78 seconds, and
`ged_test_ged_draw_scene` completed in 1.46 seconds.

After the direct BSG node/support header and type spelling cleanup, the
`ged_test_ged_draw_scene` target no longer needs to request `libbsg`
directly.  `src/libged/tests/draw/CMakeLists.txt` now links the test with
`libged` only.  The generated link line still includes `libbsg` transitively
through retained owner libraries, which is expected until the libged/libdm/librt
owners migrate, but the test target is no longer a direct libbsg consumer.

The pivot guard now rejects a direct `ged_test_ged_draw_scene`/`libbsg` CMake
row from returning, and the inventory records the draw-scene test target as a
no-direct-libbsg-link guarded row.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_ged_draw_scene` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 52.76 seconds.
The longest member was `libbsg_api_hygiene` at 40.82 seconds, and
`ged_test_ged_draw_scene` completed in 1.41 seconds.

The RT legacy BSG adapter now exposes additional opaque view-context helpers:
`rt_view_context_name_set_bsg`, `rt_view_context_scale_from_bsg`,
`rt_view_context_size_from_bsg`, `rt_view_context_scale_state_set_bsg`,
`rt_view_context_lod_bounds_callback_is_bsg`,
`rt_view_context_is_independent_bsg`,
`rt_view_context_independent_scope_is_null_bsg`,
`rt_view_set_context_add_bsg`, and
`rt_view_set_context_remove_bsg`.  These keep simple retained-view setup,
view-set membership, scale state, LoD callback checks, and independent-scope
inspection behind opaque RT context calls.

`src/libged/tests/draw/view_independent.cpp` now uses those opaque context
helpers plus `ged_view_set_ctx` and `ged_view_active_ctx_set` for its two-view
setup.  It no longer includes `../../bsg_ged_draw_view_private.h`, no longer
allocates or names `struct bsg_view`, no longer calls typed
`rt_view_set_remove_view_bsg`, `rt_view_init_bsg`, `rt_view_set_add_view_bsg`,
`rt_view_scale_state_set_bsg`, `rt_view_is_independent_bsg`,
`rt_view_independent_scope_is_null_bsg`, or
`ged_draw_view_has_lod_bounds_update`, and no longer touches `gv_*` fields.

The pivot guard now requires the opaque context helpers in the independent-view
test, rejects the older typed BSG setup forms from returning there, and tracks
the new RT legacy adapter symbols in the header and implementation token
checks.  The RT legacy adapter test covers null and live opaque context setup,
view-set add/remove, scale/size readback, LoD callback inspection, and
independent-scope null checks.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_view_independent` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_independent)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 51.40 seconds.
The longest member was `libbsg_api_hygiene` at 40.76 seconds; the migrated
`rt_view_legacy_bsg` completed in 0.02 seconds and
`ged_test_view_independent` completed in 0.15 seconds.

The RT legacy BSG adapter now also exposes opaque context wrappers for the
remaining `ged_test_view_command` semantic helper boundary:
`rt_view_context_snap_candidates_result_bsg`,
`rt_view_context_measure_candidates_bsg`,
`rt_view_context_pick_semantic_path_bsg`,
`rt_view_context_render_export_consistency_bsg`, and
`rt_view_context_polygon_find_bsg`.

`src/libged/tests/draw/view_command.cpp` now creates and registers test views
through `rt_view_context_create_bsg`, `rt_view_context_name_set_bsg`,
`rt_view_context_dimensions_set_bsg`, `rt_view_set_context_add_bsg`,
`rt_view_set_context_remove_bsg`, `ged_view_set_ctx`, and
`ged_view_active_ctx_set`.  Its render/export consistency, semantic pick,
snap, measure, and polygon lookup checks all call the opaque context wrappers
instead of typed `struct bsg_view` RT helpers.  The test no longer allocates
`struct bsg_view` directly, no longer writes `gv_name`, no longer assigns
`gedp->ged_gvp`, and no longer calls the older typed RT setup or semantic
wrapper forms.

The pivot guard now requires the opaque setup and semantic context helpers in
`view_command.cpp`, tracks the new RT legacy adapter symbols in the header and
implementation checks, and rejects direct `struct bsg_view` setup, typed RT
view-set/dimension/semantic wrapper calls, `gv_*` field access, direct
`ged_gvp` assignment, direct BSG semantic/backend headers or calls, and a
direct libbsg relink for the view-command test.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_view_command` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_view_command)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 51.45 seconds.
The longest member was `libbsg_api_hygiene` at 40.81 seconds; the migrated
`rt_view_legacy_bsg` completed in 0.02 seconds and `ged_test_view_command`
completed in 0.15 seconds.

The RT legacy BSG adapter now exposes opaque context wrappers for retained
grid state, snap-source flags, and 2D snap:
`rt_view_context_grid_state_from_bsg`,
`rt_view_context_grid_state_set_bsg`,
`rt_view_context_snap_source_flags_from_bsg`,
`rt_view_context_snap_source_flags_set_bsg`, and
`rt_view_context_snap_point_2d_bsg`.  Existing opaque snap-result and measure
wrappers remain the retained semantic collection path.

`src/libged/tests/draw/snap_semantics.cpp` now creates its headless retained
view through `rt_view_context_create_bsg`, sizes it with
`rt_view_context_dimensions_set_bsg`, configures grid and snap-source state
through the opaque context wrappers, and collects snap candidates and 2D snap
results through context wrappers.  `src/libged/tests/draw/measure_semantics.cpp`
now uses the same opaque lifecycle and dimension setup and calls
`rt_view_context_measure_candidates_bsg` for all measure checks.  Neither test
names `struct bsg_view`, calls typed RT view lifecycle/dimension wrappers, or
uses typed RT snap/measure wrappers.

The `rt_view_legacy_bsg` adapter test now covers the new opaque grid,
snap-source, and 2D snap wrappers, and the pivot guard rejects typed retained
view setup/snap/measure calls from returning to both GED semantic tests while
tracking the new RT adapter symbols in the header, implementation, and adapter
test token checks.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_snap_semantics
ged_test_measure_semantics` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_snap_semantics|ged_test_measure_semantics)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 52.59 seconds.
The longest member was `libbsg_api_hygiene` at 42.06 seconds; the migrated
`rt_view_legacy_bsg` completed in 0.02 seconds, and both
`ged_test_snap_semantics` and `ged_test_measure_semantics` completed in
0.01 seconds.

The RT legacy BSG adapter now exposes neutral selection availability/count
facades over the retained backend:
`rt_view_context_selection_available` and
`rt_view_context_selection_count`.  The existing neutral pick-result context,
point-pick, selection-apply, and selection-clear facades remain the migrated
selection path.

`src/libged/tests/draw/selection_semantics.cpp` now obtains its active view
with `ged_view_active_ctx`, includes only `rt/view.h`, reads dimensions
through neutral RT context helpers, collects point-pick results with
`rt_view_context_pick_point`, inspects them with
`rt_view_pick_result_context_count`, and applies/clears selection through
neutral RT context selection wrappers.  The test no longer reads
`gedp->ged_gvp`, includes `rt/view_legacy_bsg.h`, or calls BSG-named RT
width/height, pick-result, pick, or selection wrappers.

The pivot guard now requires the neutral context pick/selection helpers in
`selection_semantics.cpp`, tracks the new RT adapter symbols in the neutral
header, retained implementation, and adapter-test inventories, and rejects
direct GED view-storage reach-through, direct BSG pick/selection headers or
calls, BSG-named RT pick/selection wrapper calls, or a direct libbsg relink
for the selection semantics target.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_selection_semantics` in
76.49 seconds; the targeted guard/runtime gate completed 3/3 passing in
18.01 seconds; the focused migration gate completed 9/9 passing in
18.83 seconds; and the standard broad focused wrapper gate completed 18/18
passing in 83.21 seconds.  The broad gate's longest members were
`libbsg_api_hygiene` at 41.00 seconds, `brlobol_pivot_guard` at
17.91 seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.52 seconds.

The RT legacy BSG adapter now exposes opaque context wrappers for retained
display-manager lookup, live scale-storage access, unit-conversion setup, and
frame-time setup:
`rt_view_context_display_manager_from_bsg`,
`rt_view_context_scale_storage_from_bsg`,
`rt_view_context_unit_conversion_set_bsg`, and
`rt_view_context_frametime_set_bsg`.  Existing opaque display-manager set and
dimension wrappers remain the setup path.  The adapter test now covers the new
null contracts and live-state behavior, including deterministic frame-time
setup via `bsg_view_frametime`.

`src/libged/tests/draw/basic.cpp`,
`src/libged/tests/draw/faceplate.cpp`, and
`src/libged/tests/draw/lod.cpp` now obtain the active retained view through
`ged_view_active_ctx` and treat it as an opaque context while setting up their
swrast display managers.  These tests no longer name `struct bsg_view`, read
`gedp->ged_gvp`, reach through `v->dmp`, call typed RT dimension,
unit-conversion, or scale-storage wrappers, or call `bsg_view_set_frametime`
directly.  The pivot guard now requires the opaque context wrappers in these
three tests, rejects the removed direct retained-view setup forms, and rejects
a direct libbsg relink for `ged_test_draw`, `ged_test_faceplate`, and
`ged_test_lod`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_draw ged_test_faceplate
ged_test_lod` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_drawing_basic|ged_test_drawing_faceplate|ged_test_drawing_lod)$"
--timeout 180 --output-on-failure` completing 6/6 passing in 71.45 seconds.
The longest member was `libbsg_api_hygiene` at 40.75 seconds; the migrated
`rt_view_legacy_bsg` completed in 0.02 seconds, `ged_test_drawing_basic`
completed in 7.55 seconds, `ged_test_drawing_faceplate` completed in
1.74 seconds, and `ged_test_drawing_lod` completed in 10.91 seconds.

The retained swrast setup migration now covers
`src/libged/tests/draw/select.cpp`,
`src/libged/tests/draw/bsg_render_stability.cpp`, and
`src/libged/tests/draw/lod_crossrun.cpp`.  These tests now use
`ged_view_active_ctx` plus opaque RT view-context wrappers for active-view
lookup, display-manager access, live scale storage, dimension setup, and unit
conversion.  `bsg_render_stability.cpp` and `lod_crossrun.cpp` still
intentionally exercise retained draw transactions and `dm_draw_objs`, but
those calls now receive the opaque active-view context instead of a locally
named `struct bsg_view`.

The pivot guard now requires the context wrapper tokens in the select,
render-stability, and LoD cross-run tests, rejects direct `gedp->ged_gvp`,
`struct bsg_view`, direct `v->dmp`, typed RT setup wrappers, and direct BSG
frame-time setup from returning to the cleaned swrast setup batch, and rejects
direct libbsg relinks for `ged_test_select_draw`,
`ged_test_bsg_render_stability`, and `ged_test_lod_crossrun`.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_select_draw ged_test_bsg_render_stability
ged_test_lod_crossrun` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_select|ged_test_bsg_render_stability|ged_test_lod_crossrun)$"
--timeout 180 --output-on-failure` completing 5/5 passing in 70.39 seconds.
The longest member was `libbsg_api_hygiene` at 40.77 seconds;
`ged_test_drawing_select` completed in 16.25 seconds,
`ged_test_bsg_render_stability` completed in 0.25 seconds, and
`ged_test_lod_crossrun` completed in 2.38 seconds.

The retained shaded-mode swrast setup migration now covers
`src/libged/tests/draw/mged_shaded_mode_bsg.cpp`.  The test still exercises
retained GED/DM shaded-mode rendering, lighting, zbuffer, zclip, screengrab,
and semantic draw-record behavior, but active-view lookup, display-manager
access, live scale storage, dimension setup, and unit conversion now route
through opaque RT view-context wrappers instead of direct `ged_gvp`,
`struct bsg_view`, `v->dmp`, or typed RT setup calls.  The pivot guard now
requires the opaque context wrapper tokens, rejects those removed direct
retained-view setup forms, and continues to reject a direct libbsg relink for
`ged_test_mged_shaded_mode_bsg`.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_mged_shaded_mode_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_mged_shaded_mode_bsg)$"
--timeout 180 --output-on-failure` completing 3/3 passing in 51.92 seconds.
The longest member was `libbsg_api_hygiene` at 40.82 seconds, and
`ged_test_mged_shaded_mode_bsg` completed in 0.48 seconds.

The retained MGED BSG draw test now routes its remaining retained-view setup
and state checks through opaque RT view-context wrappers.  `rt/view_legacy_bsg.h`
now exposes context forms for edit-matrix set/clear, center-dot get/set,
scene-attached checks, and visible render-summary checks, and
`src/librt/view_legacy_bsg.c` delegates those wrappers to the existing typed
adapter implementations.  The adapter test covers the new null and live-state
contracts.

`src/libged/tests/draw/mged_bsg.cpp` now uses `ged_view_active_ctx` plus
opaque RT view-context wrappers for active-view lookup, display-manager access,
live scale storage, dimension setup, unit conversion, scene attachment,
center-dot state, edit-matrix setup, and visible highlighted render-summary
inspection.  It still intentionally exercises retained draw transactions and
`dm_draw_objs`, but those calls now receive the opaque active-view context
instead of a locally named `struct bsg_view`.

The pivot guard now requires the new context wrapper tokens in the RT adapter
header, implementation, adapter test, and `mged_bsg.cpp`; rejects direct
`gedp->ged_gvp`, `struct bsg_view`, `v->dmp`, `gv_edit_mat`, typed RT setup
and state wrappers, direct BSG render helpers, and direct BSG view-state
headers from returning to the test; and continues to reject a direct libbsg
relink for `ged_test_mged_bsg`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_mged_bsg` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_mged_bsg)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 51.81 seconds.
The longest member was `libbsg_api_hygiene` at 40.75 seconds;
`rt_view_legacy_bsg` completed in 0.02 seconds, and `ged_test_mged_bsg`
completed in 0.53 seconds.

The retained quad-stability draw test now routes its multi-view swrast setup
through opaque GED/RT view-context wrappers.  `rt/view_legacy_bsg.h` now
exposes `rt_view_context_scene_anchor_ensure_bsg`, implemented as a delegating
context wrapper over the existing typed scene-anchor adapter, and the adapter
test covers both its null and live-view contracts.

`src/libged/tests/draw/bsg_quad_stability.cpp` now uses `ged_view_set_ctx`,
`ged_view_set_views_ctx`, `ged_view_active_ctx_set`,
`rt_view_context_create_bsg`, `rt_view_set_context_remove_bsg`,
`rt_view_set_context_add_bsg`, `rt_view_context_name_set_bsg`, and opaque RT
view-context setup helpers for display-manager access, live scale storage,
dimension setup, unit conversion, independent-scope creation, and scene-anchor
creation.  It still intentionally exercises retained draw transactions and
`dm_draw_objs`, but those calls now receive opaque view contexts instead of
locally named `struct bsg_view` pointers.

The pivot guard now requires the new context scene-anchor wrapper in the RT
adapter header, implementation, and adapter test.  It also requires the opaque
context setup tokens in `bsg_quad_stability.cpp` and rejects direct
`gedp->ged_gvp`, `struct bsg_view`, `v->dmp`, `gv_name`, typed RT view-set,
dimension, unit-conversion, scale-storage, independent-scope, and
scene-anchor calls, direct BSG scene-anchor helpers, and a direct libbsg
relink for `ged_test_bsg_quad_stability`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_bsg_quad_stability` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_bsg_quad_stability)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 51.89 seconds.
The longest member was `libbsg_api_hygiene` at 40.79 seconds;
`rt_view_legacy_bsg` completed in 0.02 seconds, and
`ged_test_bsg_quad_stability` completed in 0.53 seconds.

The retained AET draw executable now routes its multi-view swrast setup
through opaque GED/RT view-context wrappers.  `rt/view_legacy_bsg.h` now
exposes `rt_view_context_aet_set_bsg` and `rt_view_context_update_bsg`,
implemented as delegating context wrappers over the existing typed AET and
view-update adapters, and `src/librt/tests/view_legacy_bsg.c` covers their
null and live-view behavior.

`src/libged/tests/draw/aet.cpp` now uses `ged_view_set_ctx`,
`ged_view_set_views_ctx`, `ged_view_active_ctx_set`,
`rt_view_context_create_with_set_bsg`, `rt_view_set_context_remove_bsg`,
`rt_view_set_context_add_bsg`, `rt_view_context_name_set_bsg`, and opaque RT
view-context setup helpers for display-manager access/storage, live scale
storage, dimension setup, unit conversion, AET writes, and view updates.  It
still intentionally exercises retained draw transactions and `dm_draw_objs`,
but those calls now receive opaque view contexts instead of locally named
`struct bsg_view` pointers.

The pivot guard now requires the new context AET/update wrappers in the RT
adapter header, implementation, adapter test, and `aet.cpp`; rejects direct
`gedp->ged_gvp`, `struct bsg_view`, `v->dmp`, `gv_name`, typed RT view-set,
dimension, unit-conversion, scale-storage, AET, and update calls from
returning to the AET fixture; and rejects a direct libbsg relink for
`ged_test_aet`.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg ged_test_aet`,
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg)$" --timeout 180 --output-on-failure`
completing 3/3 passing in 51.36 seconds, and a direct executable run
`./ged_test_aet /usr/home/starseeker/starseeker/brlcad/src/libged/tests/draw`
from `build/src/libged/tests/draw` completing successfully in 1.20 seconds.
The executable is currently a build target but is not registered as a CTest
test in this build.

The shared GED draw-test refresh helper now uses opaque GED/RT view-context
wrappers for retained view lookup and display-manager access.  `util.cpp`
still intentionally drives retained `ged_draw_apply_transaction` and
`dm_draw_objs` behavior for the util-backed draw fixtures, but its refresh
path now uses `ged_view_set_views_ctx`, `ged_view_active_ctx`, and
`rt_view_context_display_manager_from_bsg` instead of `rt_view_set_views_bsg`,
direct `struct bsg_view`, `ged_gvp`, or `v->dmp` access.  The pivot guard now
requires those opaque helper tokens in `util.cpp` and rejects the removed
direct retained-view setup forms from returning.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_draw ged_test_bsg_render_stability ged_test_aet`,
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_basic|ged_test_bsg_render_stability)$" --timeout 180 --output-on-failure`
completing 4/4 passing in 59.53 seconds, and a direct executable run
`./ged_test_aet /usr/home/starseeker/starseeker/brlcad/src/libged/tests/draw`
from `build/src/libged/tests/draw` completing successfully in 1.19 seconds.

The retained quad draw fixture now routes its multi-view swrast setup through
opaque GED/RT view-context wrappers.  `quad.cpp` now uses `ged_view_set_ctx`,
`ged_view_set_views_ctx`, `ged_view_active_ctx_set`,
`rt_view_context_create_with_set_bsg`, `rt_view_set_context_remove_bsg`,
`rt_view_set_context_add_bsg`, `rt_view_context_name_set_bsg`, and opaque RT
view-context setup helpers for display-manager access/storage, live scale
storage, dimension setup, unit conversion, and independent-state reads.
Follow-up cleanup moved the shared-line feature geometry and named visible
line render-item assertions behind `rt_view_context_feature_geometry_summary_bsg`
and `rt_view_context_named_line_render_count_bsg`, leaving `quad.cpp` free of
direct BSG feature/render headers, `bsg_feature_*`, `bsg_render_*`, and
`BSG_RENDER_*` tokens for that validation.

The pivot guard now requires the opaque setup and assertion tokens in
`quad.cpp` and rejects direct `gedp->ged_gvp`, `struct bsg_view`, `v->dmp`,
`gv_name`, typed RT view-set/setup calls, typed independent checks, direct BSG
feature/render headers, `bsg_feature_*`, `bsg_render_*`, and `BSG_RENDER_*`
tokens from returning to the quad fixture path.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_quad` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_drawing_quad)$" --timeout 180 --output-on-failure`
completing 3/3 passing in 64.09 seconds.  The quad test itself completed in
12.66 seconds.  Follow-up verification after the shared-line assertion cleanup
passed with `ninja -C build rt_view_legacy_bsg ged_test_quad` and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_drawing_quad)$" --timeout 180 --output-on-failure`
completing 4/4 passing in 64.12 seconds, with the quad test at 12.65 seconds.

The private qtcad legacy-view name bridge now routes retained BSG view naming
through the opaque RT view-context adapter boundary.  `QgLegacyView.cpp` uses
`rt_view_context_name_set_bsg` for local-view creation and
`qg_legacy_view_name_set`, and `rt_view_context_name_from_bsg` for
`qg_legacy_view_name_get`, leaving production qtcad callers on the existing
opaque `QgLegacyView.h` helper surface while removing the bridge's direct
`gv_name` reads/writes and its older typed `rt_view_name_from_bsg` read.

The pivot guard now requires the context name helper tokens in
`QgLegacyView.cpp` and rejects direct `gv_name` access, `bu_vls_*` name-field
access, or the typed `rt_view_name_from_bsg` helper from returning to the
private qtcad legacy-view bridge.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad rt_view_legacy_bsg test_qtcad_obol_draw_sync` and
`ctest --test-dir build -R
"^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|test_qtcad_obol_draw_sync)$"
--timeout 180 --output-on-failure` completing 4/4 passing in 53.67 seconds.
The longest member was `libbsg_api_hygiene` at 40.78 seconds; the pivot guard
completed in 12.71 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and
`test_qtcad_obol_draw_sync` in 0.11 seconds.

The GED state-services retained draw-path regression now routes active-view
access through the neutral GED view-context helper.  `test_draw` in
`src/libged/tests/test_state_services.cpp` obtains the active view with
`ged_view_active_ctx`, verifies that the fixture has a non-null view context,
and passes that opaque `view_ctx` to `ged_draw_path_state` and
`ged_draw_list_paths` instead of reaching through `gedp->ged_gvp`.

The pivot guard now requires `ged_view_active_ctx`, `void *view_ctx`,
`ged_draw_path_state`, and `ged_draw_list_paths` tokens in
`test_state_services.cpp`, and the existing neutral GED accessor guard rejects
direct `ged_gvp`/`ged_views` access from returning to that file.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 14.04 seconds,
`ninja -C build ged_test_state_services`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_state_services)$" --timeout 180 --output-on-failure`
completing 3/3 passing in 147.33 seconds.  The longest member was
`ged_test_state_services` at 92.89 seconds; `libbsg_api_hygiene` completed in
40.81 seconds and the pivot guard completed in 13.58 seconds.

The GED draw-scene regression now routes its active-view checks through an
opaque GED view-context helper instead of direct `gedp->ged_gvp` field access.
`src/libged/tests/draw/ged_draw_scene.cpp` now owns a file-local
`test_active_view_ctx` wrapper around `ged_view_active_ctx` and uses it for
scene-root traversal, visible view-record queries, rendered-object summaries,
qray feature summary/removal checks, retained path/list state probes, and
manual draw transaction `view` setup.  The two remaining typed draft
construction calls in the fixture now use `ged_draw_shape_draft_create_context`
instead of `ged_draw_shape_draft_create`.

The pivot guard now requires `test_active_view_ctx`, `ged_view_active_ctx`, and
`ged_draw_shape_draft_create_context` in `ged_draw_scene.cpp`, rejects direct
`gedp->ged_gvp` access, and rejects the typed draft constructor from returning
to the fixture.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.91 seconds,
`ninja -C build ged_test_ged_draw_scene`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_ged_draw_scene)$" --timeout 180 --output-on-failure`
completing 3/3 passing in 55.93 seconds.  The migrated
`ged_test_ged_draw_scene` completed in 1.42 seconds; `libbsg_api_hygiene`
completed in 40.82 seconds and the pivot guard completed in 13.64 seconds.

The GED GQA overlap plotting regression now validates overlay geometry through
neutral GED view records instead of direct BSG export APIs.  `src/libged/tests/test_gqa.c`
no longer includes `bsg/export.h`, `bsg/draw_source.h`, or
`bsg/scene_object.h`, and no longer calls `bsg_export_scene` with
`gedp->ged_gvp`.  The test obtains the active view with `ged_view_active_ctx`,
queries view-overlay records with `ged_draw_foreach_view_record_query`, filters
`gqa::overlaps` records through `ged_draw_view_db_object_record` fields, and
writes the plot output by calling
`ged_draw_view_db_object_record_foreach_segment`.

The pivot guard now requires the neutral GQA view-record tokens in
`test_gqa.c` and rejects direct BSG includes, `gedp->ged_gvp`,
`bsg_export_*`, and `BSG_EXPORT_*` tokens from returning to that test.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.71 seconds,
`ninja -C build ged_test_gqa`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_gqa)$" --timeout 180 --output-on-failure`
completing 3/3 passing in 54.81 seconds.  The migrated `ged_test_gqa`
completed in 0.14 seconds; `libbsg_api_hygiene` completed in 40.82 seconds and
the pivot guard completed in 13.80 seconds.

The MGED RTIF current-view display-manager handoff now uses the opaque RT
view-context wrapper.  `_rtif_use_current_view` in `src/mged/rtif.c` still
installs `view_state->vs_gvp` as the active GED view context for RT-style
commands, but it now keeps the returned active view as `void *view_ctx` and
calls `rt_view_context_display_manager_set_bsg`.  The file no longer casts
`ged_view_active_ctx` back to `struct bsg_view *` or calls the typed
`rt_view_display_manager_set_bsg` setter for this handoff.

The pivot guard now splits `src/mged/rtif.c` out from the remaining MGED
typed display-manager handoff group, requires `ged_view_active_ctx` and
`rt_view_context_display_manager_set_bsg` in `rtif.c`, and rejects typed
`struct bsg_view` spelling, typed active-context casts, or direct typed
display-manager setter calls from returning there.  The older typed
display-manager guard remains in place for `cmd.cpp`, `dm-generic.c`,
`dozoom.c`, and `overlay.c` until those MGED files are migrated separately.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.65 seconds,
`ninja -C build bin/mged`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|rt_view_legacy_bsg|regress-mged_draw|regress-mged_refresh|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completing 5/5 passing in 14.99 seconds.  The MGED regressions completed in
0.41, 0.42, and 0.41 seconds respectively; `rt_view_legacy_bsg` completed in
0.02 seconds and the pivot guard completed in 13.69 seconds.

A follow-up focused hygiene gate with
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_draw|regress-mged_refresh|regress-mged_zoom)$" --timeout 180 --output-on-failure`
completed 6/6 passing in 55.73 seconds.  `libbsg_api_hygiene` completed in
40.85 seconds, `rt_view_legacy_bsg` in 0.02 seconds, and the MGED regressions
in 0.41, 0.41, and 0.41 seconds.

The MGED display-manager setter cleanup now covers all remaining
`rt_view_display_manager_set_bsg` call sites under `src/mged`.
`cmd_ged_dm_wrapper` and `cmd_screengrab` in `src/mged/cmd.cpp` now borrow the
active GED view as `void *active_view_ctx`, repair it with
`ged_view_active_ctx_set` when needed, and call
`rt_view_context_display_manager_set_bsg`.  The forwarded DM command path in
`src/mged/dm-generic.c` follows the same opaque active-view context pattern,
and `src/mged/overlay.c` now forwards its overlay command through an opaque
`void *view_ctx` before calling the context setter.  `src/mged/dozoom.c` still
has broader typed retained-view drawing and view-math work, but its per-frame
DM synchronization now calls `rt_view_context_display_manager_set_bsg` instead
of the typed setter.

The pivot guard now requires `rt_view_context_display_manager_set_bsg` for the
remaining MGED display-manager handoff group and rejects the typed
`rt_view_display_manager_set_bsg` setter from returning there.  Together with
the previous `rtif.c` split, a direct scan of `src/mged` now finds no typed
display-manager setter calls.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.69 seconds,
`ninja -C build bin/mged`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`
completing 11/11 passing in 57.78 seconds.  The longest member was
`libbsg_api_hygiene` at 40.82 seconds; `rt_view_legacy_bsg` completed in
0.02 seconds and all eight MGED regressions completed between 0.40 and
0.42 seconds.

The MGED active-view context migration now covers three small remaining
typed active-view reads in `src/mged/cmd.cpp` and `src/mged/dm-generic.c`.
Simulation view-argument injection in `cmd.cpp` now borrows the active GED
view as `void *view_ctx` and reads orientation, eye position, and size through
`rt_view_context_orientation_quat_from_bsg`,
`rt_view_context_eye_pos_from_bsg`, and `rt_view_context_size_from_bsg`.
`cmd_draw` also borrows the active view as an opaque context before syncing
display dimensions through `rt_view_context_dimensions_set_bsg`.  In
`dm-generic.c`, the idle scale redraw path now reads zoom-refresh LoD policy
with `rt_view_context_lod_policy_from_bsg` instead of casting the active GED
view to `struct bsg_view *`.

The pivot guard now requires those context-form helper tokens in the migrated
MGED paths, rejects the removed local `bv`/`gvp` typed active-view patterns
from returning to `cmd.cpp`, and rejects the typed
`rt_view_lod_policy_from_bsg` active-view read from returning to
`dm-generic.c`.  The remaining typed active-view cast in `cmd.cpp` is the
larger staged-view command path and remains explicit pending a separate
lifecycle/view-staging cleanup.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.66 seconds,
`ninja -C build bin/mged`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`
completing 11/11 passing in 57.80 seconds.  The longest member was
`libbsg_api_hygiene` at 40.83 seconds; `rt_view_legacy_bsg` completed in
0.02 seconds and all eight MGED regressions completed at 0.41 seconds.

The MGED dozoom draw-frame path now routes its retained view-state access
through opaque RT view-context helpers.  `src/mged/dozoom.c` still passes the
retained view to `dm_draw_objs`, but the surrounding refresh drawn-count
updates, saved/restored projection-matrix access, perspective reads/writes,
eye-position and model-to-view reads, edit-matrix setup/clear, and
frame-revision reads now call context-form wrappers.  This removes the old
typed `rt_view_refresh_drawn_count_set_bsg`, `rt_view_pmat_*_bsg`,
`rt_view_perspective_*_bsg`, `rt_view_eye_pos_from_bsg`,
`rt_view_model2view_from_bsg`, `rt_view_edit_matrix_*_bsg`, and
`rt_view_frame_revision_from_bsg` calls from `dozoom.c`.

The RT legacy adapter now exposes
`rt_view_context_frame_revision_from_bsg`, implemented as a delegating wrapper
over the existing typed frame-revision adapter.  The adapter test covers both
null and live-view behavior for the new context wrapper, and the pivot guard
requires the new symbol in the header, implementation, and adapter test.

The pivot guard now requires the context-form dozoom helpers and rejects the
older typed projection, perspective, edit-matrix, frame-revision, and related
view-state helper calls from returning to `dozoom.c`.  The remaining typed
view use in that path is the explicit retained `dm_draw_objs(v)` handoff,
which is still part of the retained DM draw boundary.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 13.70 seconds,
`ninja -C build rt_view_legacy_bsg bin/mged`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|regress-mged_(draw|draw_e_erase_d_who|loadview|overlay|ps|refresh|view|zoom))$" --timeout 180 --output-on-failure`
completing 11/11 passing in 57.81 seconds.  The longest member was
`libbsg_api_hygiene` at 40.81 seconds; `rt_view_legacy_bsg` completed in
0.02 seconds and all eight MGED regressions completed between 0.40 and
0.42 seconds.

The libged generic view-object command path now uses the public GED draw-view
facade instead of direct retained draw-view private headers or BSG draw-mode
headers.  `src/libged/view/objs.cpp` no longer includes
`bsg_ged_draw_view_private.h` or direct `bsg/*` headers, routes feature
existence, style get/apply, removal, realization, scene-attached checks,
record queries, and shape-ref realization through opaque
`ged_draw_view_context_*`/public GED draw helpers, and reports draw modes with
`GED_DRAW_MODE_*` constants rather than `BSG_DRAW_MODE_*` constants.  The
public `ged/bsg_ged_draw.h` facade now owns the context-form
`ged_draw_view_context_feature_style_apply` and
`ged_draw_view_context_feature_realize` declarations used by this command
surface.

The pivot guard now treats `src/libged/view/objs.cpp` as a public-facade
consumer: it requires the public GED draw-view helper tokens and rejects
private draw-view header inclusion, direct BSG headers, direct BSG feature
tokens, and BSG draw-mode constants from returning to the command file.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`, and
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|ged_draw_scene|drawing_basic|drawing_quad|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completing 12/12 passing in 79.22 seconds.  The longest member was
`libbsg_api_hygiene` at 41.00 seconds; `brlobol_pivot_guard` completed in
14.21 seconds, `ged_test_drawing_basic` in 7.52 seconds,
`ged_test_drawing_quad` in 12.70 seconds, `ged_test_ged_draw_scene` in
1.41 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, and the five MGED regressions
completed in 0.41 seconds each.

The libged deprecated gobject command path now uses the public GED draw-view
facade instead of directly owning retained draw-view private and BSG scene
setup.  `src/libged/view/gobjs.cpp` no longer includes
`bsg_ged_draw_view_private.h`, direct `bsg/*` headers, or `ged_private.h`;
its create/delete paths call public `ged_draw_view_context_*` helpers only.
The public `ged/bsg_ged_draw.h` facade now exposes
`ged_draw_view_context_gobject_create`, implemented in `src/libged/draw.cpp`
next to the retained draw tree walker.  That bridge-owned helper keeps the
transitional `bsg_scene_ref`, BSG appearance settings, RT internal user-data
ownership, and `draw_gather_paths` population details out of the command file.

The pivot guard now treats `src/libged/view/gobjs.cpp` as a public-facade
consumer: it requires `ged/bsg_ged_draw.h`,
`ged_draw_view_context_gobject_create`, and public feature existence/removal
tokens, and rejects private draw-view header inclusion, direct BSG headers,
retained scene refs, BSG appearance settings, direct overlay-create calls, and
BSG feature tokens from returning to that command file.  The public-header
guard also requires `ged_draw_view_context_gobject_create`.

An initial FreeBSD/clang `ninja -C build lib/libged.so` caught a unity-build
`-Wshadow` failure where the helper parameter name collided with an unrelated
static `view_name` in `cmd_schemas.cpp`; the helper parameter was renamed to
`gobject_name`.  The follow-up `ninja -C build lib/libged.so` completed
successfully after that fix.

Verification on FreeBSD/clang passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|ged_test_(view_command|ged_draw_scene|drawing_basic|drawing_quad|tcl_overlay_bsg)|regress-mged_(view|draw|refresh|zoom|knob))$" --timeout 180 --output-on-failure`
completing 12/12 passing in 79.11 seconds.  The longest member was
`libbsg_api_hygiene` at 40.93 seconds; `brlobol_pivot_guard` completed in
14.23 seconds, `ged_test_drawing_basic` in 7.50 seconds,
`ged_test_drawing_quad` in 12.68 seconds, `ged_test_ged_draw_scene` in
1.42 seconds, `ged_test_tcl_overlay_bsg` in 0.12 seconds,
`ged_test_view_command` in 0.15 seconds, and the five MGED regressions
completed between 0.40 and 0.41 seconds.

The libged draw command pipeline now uses the public GED draw-view facade for
its command-side retained view helpers.  `src/libged/draw/draw.c` no longer
includes `bsg_ged_draw_view_private.h` directly; view-info snapshot reads,
LoD policy reads/applies, BoT-threshold LoD policy applies, and diagnostic
edge-use line-layer fallback publication all stay on context-form helpers
declared in `ged/bsg_ged_draw.h`.  The public facade now declares
`ged_draw_view_context_info_from_bsg` alongside the existing public
context-form LoD and line-layer helpers.  `draw.c` still includes the full GED
draw bridge private header for draw-scene internals that remain bridge-owned,
but no production command-side libged file now directly includes the private
draw-view header.

The pivot guard now requires `draw.c` to include the public GED draw facade
for these context helpers and rejects direct `bsg_ged_draw_view_private.h`,
typed `struct bsg_view`, typed active-view casts, typed shape-draft creation,
typed view-info calls, and typed LoD calls from returning to the draw command
client-data path.  The public-header guard also requires
`ged_draw_view_context_info_from_bsg`.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so`.

The next TclCAD direct legacy-view batch has been migrated to public GED view
facades.  `src/libtclcad/wrapper.c`, `src/libtclcad/fb.c`,
`src/libtclcad/view/util.c`, `src/libtclcad/view/draw.c`,
`src/libtclcad/view/axes.c`, `src/libtclcad/view/faceplate.c`,
`src/libtclcad/view/arrows.c`, and `src/libtclcad/view/autoview.c` no longer
include `rt/view_legacy_bsg.h`.  Display-manager lookup, wrapper dimension
sync, wrapper perspective/LoD sync, draw model2view/perspective/projection
reads, axes info/model/view state, faceplate center-dot/params/scale-overlay
state, and framebuffer open-path lookup now route through
`ged_view_context_*` facades.  `arrows.c` and `autoview.c` only carried stale
direct legacy includes, so their behavior stays on the existing opaque GED
draw/view helper paths.

The pivot guard now removes those eight TclCAD files from the central direct
legacy-view include allowlist and transitional RT view-scope requirements,
requires `ged/view.h` plus the relevant `ged_view_context_*` tokens in the
migrated files, and rejects `rt/view_legacy_bsg.h`,
`rt_view_context_*_bsg`, typed display-manager, typed axes/faceplate, and
typed helper-signature regressions from returning.  A fresh non-test direct
legacy-view include scan now reports 15 production owners total, with TclCAD
down to five files: `commands.c`, `mouse.c`, `polygons.c`, `view/refresh.c`,
and `view_data.c`; MGED remains at zero.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libtclcad.so bin/mged`, and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 16.06 seconds.

Follow-up focused verification passed with:
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|overlay|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 28/28 passing in 208.70 seconds.  The longest single test was
`ged_test_state_services` at 112.84 seconds, below the 180 second cap;
`libbsg_api_hygiene` completed in 41.56 seconds and `brlobol_pivot_guard`
completed in 15.17 seconds inside the focused run.  Standalone
`simulate_test` passed in 1.16 seconds and `regress-mged_arb` passed in
0.52 seconds.

The TclCAD refresh helper has been moved off the direct RT legacy-view
adapter.  `src/libtclcad/view/refresh.c` now includes `ged/view.h` and
`rt/view.h`, and routes display-manager lookup, interactive-rectangle reads,
local/base unit stash and restore, refresh request/consume/complete, refresh
enabled/suppressed checks, and suppression begin/end through public
`ged_view_context_*` facades plus the neutral `GED_VIEW_REFRESH_ALL` flag.
The new GED refresh-state facade functions are
`ged_view_context_refresh_enabled_get`,
`ged_view_context_refresh_enabled_set`,
`ged_view_context_refresh_suppressed_get`,
`ged_view_context_refresh_suppress_begin`, and
`ged_view_context_refresh_suppress_end`, implemented in `src/libged/ged.cpp`
as narrow delegates to the retained adapter while the bridge remains.

The pivot guard now removes `src/libtclcad/view/refresh.c` from the central
direct legacy-view include allowlist and from the transitional TclCAD
display-manager/view-scope groups.  It requires the new GED refresh/unit/
interactive-rectangle facade tokens in `view/refresh.c`, requires the new GED
refresh-state facade declarations in `include/ged/view.h`, and rejects
`rt/view_legacy_bsg.h`, `RT_VIEW_REFRESH_*`,
`rt_view_context_refresh_*_bsg`, and the old RT context unit/interactive-
rectangle/display-manager helpers from returning there.  A fresh non-test
direct legacy-view include scan now reports 14 production owners total, with
TclCAD down to four files: `commands.c`, `mouse.c`, `polygons.c`, and
`view_data.c`; MGED remains at zero.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged`, and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 28.29 seconds.

The broad focused gate was attempted but is not clean for this batch yet.
Under heavy external system load from a concurrent GCC 14 ports build
(`uptime` load averages around 21/20/17 and many unrelated `cc1plus`
processes consuming CPU), the focused command
`ctest --test-dir build -R "^(brlobol_pivot_guard|libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|drawing_basic|drawing_quad|mged_bsg|ged_draw_scene|qged_swrast|view_independent)|test_qtcad_obol_draw_sync|regress-(mged_(adc|overlay|view|draw|refresh|draw_e_erase_d_who|setview|rot_view|sca_view|tra_view|size|zoom|knob)|nirt|gchecker|gdiff))$" -E "^slow-" --timeout 180 --output-on-failure`
completed 27/28 with `ged_test_state_services` timing out at 180.01 seconds.
Other tests were also several times slower than the previous baseline
(`libbsg_api_hygiene` 79.81 seconds, `ged_test_drawing_basic` 26.28 seconds,
`ged_test_drawing_quad` 23.63 seconds), so that batch needed a clean rerun
under normal load before treating the broad gate as passed.

The TclCAD view-data adapter has been moved off the direct RT legacy-view
adapter.  `src/libtclcad/view_data.c` still owns the TclCAD-private overlay
record initialization and the `bsg/tcl_data.h` layout compatibility
assertions, but it now includes `ged/view.h` instead of
`rt/view_legacy_bsg.h`.  View-context user data lookup, user data set, and
retained TclCAD data binding now route through
`ged_view_context_user_data_get`, `ged_view_context_user_data_set`, and
`ged_view_context_tclcad_data_set`.  Those GED functions are implemented in
`src/libged/ged.cpp` as narrow delegates to the retained adapter while the
bridge remains.

The pivot guard now removes `src/libtclcad/view_data.c` from the central
direct legacy-view include allowlist, requires the new GED data-binding
facade declarations in `include/ged/view.h`, and rejects
`rt/view_legacy_bsg.h` plus the old `rt_view_context_*_bsg` data-binding
calls from returning to `view_data.c`.  A fresh direct legacy-view include
scan now reports 13 production owners total, with TclCAD down to three files:
`commands.c`, `mouse.c`, and `polygons.c`; MGED remains at zero.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged`, and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 15.44 seconds.

A narrower smoke gate for the changed boundary passed with:
`ctest --test-dir build -R "^(libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|mged_bsg)|regress-mged_(view|refresh|draw))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 8/8 passing in 152.80 seconds.  The longest single test was
`ged_test_state_services` at 106.28 seconds, below the 180 second cap;
`libbsg_api_hygiene` completed in 43.33 seconds.  At that point the broad
focused gate was still not clean because the available reruns happened under
unrelated GCC 14 ports-build load and timed out only in
`ged_test_state_services` inside the full suite.

The TclCAD polygon command helper has been moved off the direct RT legacy-view
adapter.  `src/libtclcad/polygons.c` now includes `ged/view.h` instead of
`rt/view_legacy_bsg.h`, and routes display-manager lookup, dimension sync,
view-name reads, scale/center/rotation/model2view/view2model snapshot reads,
plane reads, screen-to-view conversion, previous-mouse writes, and Tcl snap
preparation/application through public `ged_view_context_*` facades.  New GED
facades added for this boundary are `ged_view_context_previous_mouse_set`,
`ged_view_context_prepare_tcl_snap`, and `ged_view_context_snap_point_2d`,
implemented in `src/libged/ged.cpp` as narrow delegates to the retained adapter
while the bridge remains.  Retained polygon overlay synchronization continues
to use public `ged_draw_view_context_*` helpers.

The pivot guard now removes `src/libtclcad/polygons.c` from the central direct
legacy-view include allowlist and from the transitional TclCAD display-manager,
dimension, snap, and view-scope groups.  It requires `ged/view.h` plus the GED
view-context tokens in `polygons.c`, requires the new facade declarations in
`include/ged/view.h`, and rejects `rt/view_legacy_bsg.h` plus old RT
view-context adapter calls from returning there.  A fresh direct legacy-view
include scan now reports 12 production owners total, with TclCAD down to two
files: `commands.c` and `mouse.c`; MGED remains at zero.

Verification on FreeBSD/clang passed with:
`ninja -C build lib/libged.so lib/libtclcad.so bin/mged`, and
`ctest --test-dir build -R "^brlobol_pivot_guard$" --timeout 180 --output-on-failure`
completing 1/1 passing in 15.12 seconds.

A narrower smoke gate for the changed boundary passed with:
`ctest --test-dir build -R "^(libbsg_api_hygiene|rt_view_legacy_bsg|ged_test_(view_command|state_services|mged_bsg)|regress-mged_(view|refresh|draw))$" -E "^slow-" --timeout 180 --output-on-failure`
completing 8/8 passing in 138.25 seconds.  The longest single test was
`ged_test_state_services` at 95.40 seconds, below the 180 second cap;
`libbsg_api_hygiene` completed in 40.88 seconds.  At that point the broad
focused gate still needed a clean rerun for the refresh/view-data/polygons
batches under normal load before treating that full gate as passed.

The GED draw polygon view-dependent helper cleanup has been completed.  The
context-shaped helpers in `src/libged/bsg_ged_draw_view.c` now implement
polygon find, scoped find, update, screen-point update, area, overlap, and CSG
directly against the retained BSG polygon implementation.  The old private
typed `ged_draw_view_polygon_find`, `ged_draw_view_polygon_find_scoped`,
`ged_draw_view_polygon_update`,
`ged_draw_view_polygon_update_screen_pt`, `ged_draw_view_polygon_area`,
`ged_draw_view_polygon_overlap`, and `ged_draw_view_polygon_csg` wrappers are
gone from `src/libged/bsg_ged_draw_view_private.h` and
`src/libged/bsg_ged_draw_view.c`.  Polygon
create/import/export/record/current/open/fill/color helpers remain
intentionally BSG-owned until the wider polygon backend boundary moves.

The pivot guard now requires only the context-shaped polygon helper tokens for
that view-dependent group and rejects the retired typed helper names from the
private header and BSG-backed draw-view source.  The existing still-valid
polygon create/import/export/record/current/open/fill/color helper names remain
allowed.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.78 seconds.  The focused
migration gate completed 4/4 passing in 18.17 seconds:
`brlobol_pivot_guard` 17.88 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`test_qtcad_obol_draw_sync` 0.11 seconds, and `ged_test_qged_swrast` 0.11
seconds.  The broad focused wrapper gate completed 13/13 passing in 82.55
seconds; longest members were `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 17.72 seconds, `ged_test_drawing_quad` at 12.78
seconds, and `ged_test_drawing_basic` at 7.51 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.72 seconds.

The GED draw HUD/snap helper cleanup has been completed.  The context-shaped
`ged_draw_view_context_hud_sync` helper in
`src/libged/bsg_ged_draw_view.c` now calls the retained HUD backend directly,
and `ged_draw_view_context_snap_first_candidate` in
`src/libged/ged_view_legacy.cpp` now maps the GED snap kind and calls neutral
`rt_view_context_snap_first_candidate` directly.  The old private typed
`ged_draw_view_hud_sync` and `ged_draw_view_snap_first_candidate` wrappers are
gone from `src/libged/bsg_ged_draw_view_private.h`,
`src/libged/bsg_ged_draw_view.c`, and `src/libged/ged_view_legacy.cpp`.

The pivot guard now requires the context-shaped helper tokens and rejects the
retired typed helper names from the BSG-backed draw-view source, GED
legacy-view adapter, and private header.  LoD bounds callback helpers and
retained scene-ref/root/scope handoffs remain intentionally typed until their
wider ownership boundaries move.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.59 seconds.  The focused
migration gate completed 4/4 passing in 17.96 seconds:
`brlobol_pivot_guard` 17.67 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`test_qtcad_obol_draw_sync` 0.11 seconds, and `ged_test_qged_swrast` 0.11
seconds.  The broad focused wrapper gate completed 13/13 passing in 82.38
seconds; longest members were `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 17.65 seconds, `ged_test_drawing_quad` at 12.67
seconds, and `ged_test_drawing_basic` at 7.53 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
18.00 seconds.

The GED draw feature creation/replacement helper cleanup has been completed.
The context-shaped helpers in `src/libged/bsg_ged_draw_view.c` now implement
indexed-face-set replacement, line replacement, Tcl polygon publication,
line-layer builder replacement, line-layer replacement, model annotation line
creation, line-point append, label create/replace, Tcl label/line/arrow/axes
publication, axes creation, polygon creation, and polygon sketch import
directly against the retained BSG feature/polygon implementation.  The old
private typed `ged_draw_view_indexed_face_set_replace`,
`ged_draw_view_lines_replace`, `ged_draw_view_tcl_polygons_replace`,
`ged_draw_view_line_layer_builder_replace`,
`ged_draw_view_line_layers_replace`,
`ged_draw_view_lines_create_model_annotation`,
`ged_draw_view_lines_append_point`, `ged_draw_view_label_create`,
`ged_draw_view_labels_replace`, `ged_draw_view_tcl_labels_replace`,
`ged_draw_view_tcl_lines_replace`, `ged_draw_view_tcl_arrows_replace`,
`ged_draw_view_tcl_axes_replace`, `ged_draw_view_axes_create`,
`ged_draw_view_polygon_create`, and
`ged_draw_view_polygon_import_sketch` wrappers are gone from
`src/libged/bsg_ged_draw_view_private.h` and
`src/libged/bsg_ged_draw_view.c`.

The pivot guard now requires the context-shaped helper tokens for this group
and rejects the retired typed helper names from the private header and
BSG-backed draw-view source.  The underlying feature and polygon context
implementations remain BSG-backed until the wider draw-view feature and
polygon backend boundaries move.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and an initial
standalone `brlobol_pivot_guard` run passed in 17.57 seconds.  The focused
migration gate completed 4/4 passing in 18.20 seconds:
`brlobol_pivot_guard` 17.91 seconds, `rt_view_legacy_bsg` 0.02 seconds,
`test_qtcad_obol_draw_sync` 0.11 seconds, and `ged_test_qged_swrast` 0.11
seconds.  The broad focused wrapper gate completed 13/13 passing in 82.37
seconds; longest members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 17.57 seconds, `ged_test_drawing_quad` at 12.76
seconds, and `ged_test_drawing_basic` at 7.51 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.72 seconds.

The GED draw tree independent-scope neutral-ref cleanup has been completed.
`src/libged/bsg_ged_draw_tree.c` now calls neutral
`rt_view_context_independent_scope_ref` for retained independent-view scope
roots and converts the resulting `rt_view_scene_ref` through
`ged_draw_scene_ref_from_rt_view_ref`.  That conversion helper is owned by
`src/libged/bsg_ged_draw_refs.c`, keeping raw scene-ref field access out of
the Stage 14C draw-tree bridge.  The first implementation attempt put a
direct `.opaque` copy in the draw tree; `libbsg_api_hygiene` rejected it, and
the final version preserves that guard.  The private
`ged_draw_view_context_independent_scope_ref` adapter and older
`ged_draw_view_independent_scope_ref_bsg` wrapper are retired from
`src/libged/bsg_ged_draw_view_private.h`,
`src/libged/ged_view_legacy.cpp`, and the tree.

The pivot guard now requires the bridge-owned
`ged_draw_scene_ref_from_rt_view_ref` route in draw-tree scope handling and
continues to reject the retired independent-scope helper names.  The active
status and dependency inventory now describe retained scene-ref/root/scope
handoffs as remaining work rather than listing the retired private scope
adapter as current.  Exit-criteria percentages did not change: retained
scene-ref/root handoffs, retained BSG-backed feature/polygon internals, public
BSG-shaped GED draw snap/scene-ref signatures, retained qtcad draw ownership,
and the direct `libbsg` rows in `libdm` and `libged` still determine
retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `libged.so` rebuild after the code edit passed cleanly, and standalone
`libbsg_api_hygiene` passed in 50.09 seconds after catching the first bad
raw `.opaque` placement.  The focused migration gate completed 4/4 passing in
18.26 seconds: `brlobol_pivot_guard` 17.97 seconds,
`rt_view_legacy_bsg` 0.02 seconds, `test_qtcad_obol_draw_sync` 0.11 seconds,
and `ged_test_qged_swrast` 0.11 seconds.  The broad focused wrapper gate
completed 13/13 passing in 82.52 seconds; longest members were
`libbsg_api_hygiene` at 41.05 seconds, `brlobol_pivot_guard` at 17.63
seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.53 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.65 seconds.

The GED draw scene-root neutral-ref cleanup has been completed.  A new neutral
`rt_view_context_scene_root_ref` helper is declared in `include/rt/view.h`,
implemented in `src/librt/view_legacy_bsg.c`, and covered in
`rt_view_legacy_bsg` for null, attached, and detached retained scene-root
states.  `src/libged/bsg_ged_draw_scene_root.c` now uses that neutral helper
for `ged_draw_view_context_scene_root_ref`, converts the returned
`rt_view_scene_ref` through `ged_draw_scene_ref_from_rt_view_ref`, and uses
`ged_draw_scene_ref_context` for the public context pointer helper so raw
scene-ref field access stays centralized in `src/libged/bsg_ged_draw_refs.c`.
`ged_draw_view_context_scene_attached` now delegates to neutral
`rt_view_context_scene_attached`.  Retained scene-root creation and attachment
remain BSG-backed in the scene-root bridge; this batch only migrates the
query/read side.

The pivot guard now requires `rt_view_context_scene_root_ref` in the RT public
header, RT legacy adapter, RT adapter test, and GED scene-root query path.
Status reporting and the dependency inventory now describe scene-root query
refs as neutral while keeping retained scene-root creation/attachment in the
remaining-work list.  Exit-criteria percentages did not change: retained
scene-root attachment, retained scene-ref handoffs, BSG-backed
feature/polygon internals, public BSG-shaped GED draw snap/scene-ref
signatures, retained qtcad draw ownership, and the direct `libbsg` rows in
`libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `rt/view.h` change rebuilt the affected RT/GED/qtcad slice cleanly.
Standalone `brlobol_pivot_guard` passed in 17.59 seconds, and standalone
`rt_view_legacy_bsg` passed in 0.02 seconds.  The focused migration gate
completed 4/4 passing in 18.37 seconds: `brlobol_pivot_guard` 18.09 seconds,
`rt_view_legacy_bsg` 0.02 seconds, `test_qtcad_obol_draw_sync` 0.11 seconds,
and `ged_test_qged_swrast` 0.11 seconds.  The broad focused wrapper gate
completed 13/13 passing in 82.29 seconds; longest members were
`libbsg_api_hygiene` at 40.91 seconds, `brlobol_pivot_guard` at 17.58
seconds, `ged_test_drawing_quad` at 12.67 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.60 seconds.

The GED draw scene-root attach neutral-ref cleanup has been completed.  A new
neutral `rt_view_context_scene_root_ref_attach` helper is declared in
`include/rt/view.h`, implemented in `src/librt/view_legacy_bsg.c`, and covered
in `rt_view_legacy_bsg` for null-context failure plus retained root-ref
attach round-trip behavior.  `src/libged/bsg_ged_draw_scene_root.c` now
converts retained `bsg_scene_ref` roots through
`ged_draw_scene_ref_to_rt_view_ref` and calls
`rt_view_context_scene_root_ref_attach`; it no longer calls
`bsg_view_scene_ref_attach` directly.  The inverse conversion remains
centralized in `ged_draw_scene_ref_from_rt_view_ref`, and raw scene-ref field
access stays in `src/libged/bsg_ged_draw_refs.c`.

The pivot guard now requires `rt_view_context_scene_root_ref_attach` in the RT
public header, RT legacy adapter, RT adapter test, and GED scene-root attach
path, requires the GED retained-to-neutral scene-ref conversion helper in the
scene-root bridge, and rejects `bsg_view_scene_ref_attach` from returning to
`src/libged/bsg_ged_draw_scene_root.c`.  Status reporting and the dependency
inventory now describe scene-root query and attach refs as neutral while
keeping retained scene-root group creation, retained backend attach storage,
scene-ref handoffs, BSG-backed feature/polygon internals, public BSG-shaped
draw snap/scene-ref signatures, retained qtcad draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` in the remaining-work list.
Exit-criteria percentages did not change.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The `rt/view.h` change rebuilt the affected RT/GED/qtcad slice cleanly.
Standalone `brlobol_pivot_guard` passed in 17.63 seconds, and standalone
`rt_view_legacy_bsg` passed in 0.02 seconds.  The focused migration gate
completed 4/4 passing in 17.99 seconds: `brlobol_pivot_guard` 17.70 seconds,
`rt_view_legacy_bsg` 0.02 seconds, `test_qtcad_obol_draw_sync` 0.11 seconds,
and `ged_test_qged_swrast` 0.11 seconds.  The broad focused wrapper gate
completed 13/13 passing in 82.63 seconds; longest members were
`libbsg_api_hygiene` at 40.98 seconds, `brlobol_pivot_guard` at 17.84
seconds, `ged_test_drawing_quad` at 12.69 seconds, and
`ged_test_drawing_basic` at 7.52 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.64 seconds.

The GED retained mesh-LoD scene-ref conversion cleanup has been completed.
`src/libged/ged_view_legacy.cpp` no longer owns a local raw
`bsg_scene_ref` to `rt_view_scene_ref` copy helper for the retained mesh-LoD
load path.  `ged_draw_mesh_lod_load_view_scene_ref_context` now calls
`ged_draw_scene_ref_to_rt_view_ref` before invoking the neutral
`rt_mesh_lod_load_view_scene_ref` helper.  The conversion declaration lives in
the narrow `src/libged/bsg_ged_draw_view_private.h` adapter header, and
`src/libged/bsg_ged_draw_private.h` relies on that included declaration
instead of carrying a duplicate prototype.  This keeps `ged_view_legacy.cpp`
inside the allowed narrow view-private include boundary while centralizing raw
scene-ref field conversion in `src/libged/bsg_ged_draw_refs.c`.

The pivot guard now requires `ged_draw_scene_ref_to_rt_view_ref` in the GED
legacy-view mesh-LoD route, rejects the old local
`ged_draw_scene_ref_to_rt` helper spelling there, and rejects any direct
`.opaque` reach-through in `src/libged/ged_view_legacy.cpp`.  An initial
broad focused wrapper gate caught the first version because including the full
`bsg_ged_draw_private.h` bridge from `ged_view_legacy.cpp` violated
`libbsg_api_hygiene`; the final version moved only the required conversion
prototype to the narrow view-private header.  Exit-criteria percentages did
not change: the public BSG-shaped scene-ref handoff surface, retained
scene-root group creation, retained scene-ref handoffs, BSG-backed
feature/polygon internals, public BSG-shaped draw snap/scene-ref signatures,
retained qtcad draw ownership, and the direct `libbsg` rows in `libdm` and
`libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The final rebuild after the header-boundary fix rebuilt the affected libged
slice and relinked libqtcad/test targets cleanly.  Standalone
`libbsg_api_hygiene` passed in 40.98 seconds after catching the first
too-broad include.  The focused migration gate completed 4/4 passing in 18.17
seconds: `brlobol_pivot_guard` 17.88 seconds, `rt_view_legacy_bsg` 0.02
seconds, `test_qtcad_obol_draw_sync` 0.11 seconds, and
`ged_test_qged_swrast` 0.11 seconds.  The broad focused wrapper gate
completed 13/13 passing in 82.80 seconds; longest members were
`libbsg_api_hygiene` at 41.12 seconds, `brlobol_pivot_guard` at 17.85
seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_basic` at 7.51 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.64 seconds.

The libged draw-test neutral-header cleanup has been completed.  The routine
libged tests and draw fixtures that previously included the BSG-named
`ged/bsg_ged_draw.h` compatibility wrapper now include `ged/draw.h` directly:
`test_state_services.cpp`, `test_gqa.c`, and the draw fixture sources for AET,
quad, BSG render/quad stability, LoD cross-run, mged, shaded mged, Tcl
overlay, view independent, view command, and ged_draw_scene.  The stale
`ged_draw_scene.cpp` comment now points to `include/ged/draw.h` as the
declaration owner.  At that stage the compatibility header still remained
installed as a thin wrapper for external compatibility, but normal libged test
coverage exercised the neutral facade directly.

The pivot guard for that batch scanned `src/libged/tests` and rejected
`ged/bsg_ged_draw.h` includes there.  Exit-criteria percentages did not
change: this removed test-suite reliance on the BSG-named public facade, but
at that stage the installed compatibility wrapper, public BSG-shaped
scene-ref handoff surface, retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained qtcad draw
ownership, and the direct `libbsg` rows in `libdm` and `libged` still
determined retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build ged_test_gqa ged_test_state_services ged_test_aet ged_test_quad ged_test_bsg_render_stability ged_test_bsg_quad_stability ged_test_lod_crossrun ged_test_mged_bsg ged_test_ged_draw_scene ged_test_tcl_overlay_bsg ged_test_view_independent ged_test_view_command ged_test_mged_shaded_mode_bsg`.
The build rebuilt the changed libged tests and their GED plugin dependencies
cleanly.  The targeted guard/test suite completed 14/14 passing in 172.88
seconds: `brlobol_pivot_guard` 17.68 seconds, `libbsg_api_hygiene` 41.01
seconds, `ged_test_state_services` 95.33 seconds,
`ged_test_drawing_quad` 12.68 seconds, and all remaining members below 3
seconds.  The standard broad focused wrapper gate completed 13/13 passing in
82.45 seconds; longest members were `libbsg_api_hygiene` at 41.00 seconds,
`brlobol_pivot_guard` at 17.65 seconds, `ged_test_drawing_quad` at 12.67
seconds, and `ged_test_drawing_basic` at 7.54 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.63 seconds.

The installed GED BSG draw compatibility wrapper retirement has been
completed.  `include/ged/bsg_ged_draw.h` is removed from source, and
`include/ged/CMakeLists.txt` no longer installs it.  `include/ged/draw.h`
remains the neutral installed GED draw declaration owner.  CMake regeneration
removed the stale build-tree symlink
`build/include/brlcad/ged/bsg_ged_draw.h`.  The hygiene test message now
directs installed headers to the neutral `ged/draw.h` facade, and the pivot
guard rejects the retired wrapper file itself, install-list reentry, and
source or libged-test includes of `ged/bsg_ged_draw.h`.

This is a hard-removal step rather than another staging wrapper migration:
Phase 6 readiness is now listed as 6 percent in `obol_activework.txt`.  The
larger retirement blockers remain unchanged: the wider public BSG-shaped GED
draw snap/scene-ref handoff surface, retained scene-ref/root/scope handoffs,
BSG-backed feature/polygon internals behind context helpers, qtcad retained
draw transaction/observer ownership, and the guarded direct `libbsg` rows in
`libdm` and `libged`.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so ged_test_gqa ged_test_state_services ged_test_ged_draw_scene`.
That rebuild triggered CMake regeneration, which removed the stale installed
header symlink from the build tree, and then rebuilt the affected GED/libqtcad
slice cleanly.  The focused guard/hygiene pair completed 2/2 passing in 58.92
seconds: `brlobol_pivot_guard` 17.95 seconds and `libbsg_api_hygiene` 40.93
seconds.  The standard broad focused wrapper gate completed 13/13 passing in
82.75 seconds; longest members were `libbsg_api_hygiene` at 40.91 seconds,
`brlobol_pivot_guard` at 17.92 seconds, `ged_test_drawing_quad` at 12.66
seconds, and `ged_test_drawing_basic` at 7.66 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.70 seconds.

The private GED scene-root-ref neutral-helper cleanup has been completed.
`src/libged/bsg_ged_draw_scene_root.c` no longer exposes the BSG-returning
`ged_draw_view_context_scene_root_ref` helper.  The private bridge now exposes
`ged_draw_view_context_scene_root_rt_ref`, returning the neutral
`rt_view_scene_ref` from `rt_view_context_scene_root_ref` directly.  The public
`ged_draw_view_context_scene_root` context-pointer helper still converts
through the central GED draw-ref bridge because it is a retained bridge
surface, but DB-event view-root revalidation now compares neutral
`rt_view_scene_ref` values and converts only the non-shared view root needed
by `bsg_scene_draw_intent_revalidate`.

The pivot guard now requires the neutral helper in the scene-root bridge,
requires transaction revalidation to use `rt_view_scene_ref_is_null` and
`rt_view_scene_ref_equal`, and rejects
`ged_draw_view_context_scene_root_ref` from returning to the private header,
scene-root bridge, or transaction path.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`.
Standalone `brlobol_pivot_guard` passed in 17.67 seconds.  The standard broad
focused wrapper gate completed 13/13 passing in 82.46 seconds; longest
members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 17.65 seconds, `ged_test_drawing_quad` at 12.76
seconds, and `ged_test_drawing_basic` at 7.54 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.64 seconds.

The private GED mesh-LoD load neutral-ref cleanup has been completed.
`ged_draw_mesh_lod_load_view_scene_ref_context` now takes
`rt_view_scene_ref` instead of retained `bsg_scene_ref`.  The GED legacy-view
adapter in `src/libged/ged_view_legacy.cpp` now passes that neutral ref
directly to `rt_mesh_lod_load_view_scene_ref`; it no longer calls
`ged_draw_scene_ref_to_rt_view_ref`.  Retained draw realization in
`src/libged/draw.cpp` still owns the BSG scene refs, but it converts through
the central `ged_draw_scene_ref_to_rt_view_ref` helper before calling the
mesh-LoD load helper.

The pivot guard now requires `ged_draw_scene_ref_to_rt_view_ref` in
`draw.cpp`, requires the mesh-LoD load helper to use
`rt_view_scene_ref visibility_ref`, rejects `bsg_scene_ref visibility_ref`
from the private header, and rejects retained-to-neutral scene-ref conversion
from returning to `ged_view_legacy.cpp`.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`.
Standalone `brlobol_pivot_guard` passed in 17.74 seconds.  The standard broad
focused wrapper gate completed 13/13 passing in 82.43 seconds; longest
members were `libbsg_api_hygiene` at 40.91 seconds,
`brlobol_pivot_guard` at 17.74 seconds, `ged_test_drawing_quad` at 12.69
seconds, and `ged_test_drawing_basic` at 7.52 seconds.  A standalone
`brlobol_pivot_guard` rerun after the status and inventory updates passed in
17.68 seconds.

The GED LoD bounds opaque-setter cleanup has been completed.  `include/rt/view.h`
now declares the neutral `rt_view_context_lod_bounds_callback_set` helper, and
`src/librt/view_legacy_bsg.c` implements it through the retained BSG callback
setter while preserving null-safe context semantics.  `src/libged/ged_view_legacy.cpp`
now calls the neutral helper from `ged_draw_view_context_lod_bounds_callback_set`
instead of calling `rt_view_lod_bounds_callback_set_bsg` with a
`struct bsg_view *` cast.  The `rt_view_legacy_bsg` adapter test now covers
the new neutral setter and its null handling.

The pivot guard now requires `rt_view_context_lod_bounds_callback_set` in the
GED legacy-view adapter and rejects direct
`rt_view_lod_bounds_callback_set_bsg` use from returning there.  Exit-criteria
percentages did not change: retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained qtcad draw
ownership, and the direct `libbsg` rows in `libdm` and `libged` still
determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so librt.so rt_view_legacy_bsg`.
The focused `brlobol_pivot_guard` plus `rt_view_legacy_bsg` gate completed 2/2
passing in 17.77 seconds.  The standard broad focused wrapper gate completed
13/13 passing in 82.74 seconds; longest members were `libbsg_api_hygiene` at
41.09 seconds, `brlobol_pivot_guard` at 17.68 seconds,
`ged_test_drawing_quad` at 12.84 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The GED retained view-set neutral-helper cleanup has been completed.  `include/rt/view.h`
now declares `rt_view_set_context_init`, `rt_view_set_context_free`, and
`rt_view_set_context_find_view`; `src/librt/view_legacy_bsg.c` implements
those helpers by delegating to the retained BSG view-set implementation.
`src/libged/ged_view_legacy.cpp` now uses the neutral helpers for retained
view-set initialization, teardown, and name lookup instead of calling
`rt_view_set_init_bsg`, `rt_view_set_free_bsg`, or
`rt_view_set_find_view_bsg` directly.  The `rt_view_legacy_bsg` adapter test
now covers the neutral lifecycle/find helpers and preserves the BSG adapter
coverage.

The pivot guard now requires the neutral view-set helpers in the GED
legacy-view adapter and rejects direct retained BSG view-set lifecycle/find
calls from returning there.  Exit-criteria percentages did not change:
retained scene-root group creation, retained scene-ref handoffs, BSG-backed
feature/polygon internals, retained qtcad draw ownership, and the direct
`libbsg` rows in `libdm` and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so librt.so rt_view_legacy_bsg`.
The focused `brlobol_pivot_guard` plus `rt_view_legacy_bsg` gate completed 2/2
passing in 18.03 seconds.  The standard broad focused wrapper gate completed
13/13 passing in 83.02 seconds; longest members were `libbsg_api_hygiene` at
41.54 seconds, `brlobol_pivot_guard` at 17.65 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED private active-view storage cleanup has been completed.
`src/libged/ged_private.h` now stores `ged_impl::ged_gvp` as `void *` rather
than `struct bsg_view *`.  `src/libged/ged_view_legacy.cpp` assigns and
returns that opaque context directly for default-view install, active-view
lookup, and active-view replacement, so the private storage owner no longer
casts through the retained BSG active-view type for those operations.  Retained
group/overlay/redraw/root operations still cast opaque contexts at the
BSG-backed implementation boundary; this batch only removes the typed private
active-view storage shape.

The pivot guard now requires `void *ged_gvp` in `src/libged/ged_private.h` and
rejects typed `struct bsg_view *ged_gvp` storage from returning there.  The
active-view status scans also reject `static_cast<struct bsg_view *>` and the
old `(void *)impl->ged_gvp` cast in `src/libged/ged_view_legacy.cpp`.
Exit-criteria percentages did not change: retained scene-root group creation,
retained scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`.
The focused `brlobol_pivot_guard` gate passed in 17.70 seconds before the
status updates.  The standard broad focused wrapper gate completed 13/13
passing in 82.79 seconds; longest members were `libbsg_api_hygiene` at 41.15
seconds, `brlobol_pivot_guard` at 17.77 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

The GED mesh-LoD load pass-through cleanup has been completed.
`src/libged/draw.cpp` now calls neutral `rt_mesh_lod_load_view_scene_ref`
directly for both BoT and BRep LoD realization after converting the retained
`bsg_scene_ref` through `ged_draw_scene_ref_to_rt_view_ref`.
`src/libged/ged_view_legacy.cpp` no longer implements the private
`ged_draw_mesh_lod_load_view_scene_ref_context` helper, and
`src/libged/bsg_ged_draw_view_private.h` no longer declares that helper or the
now-unused `struct rt_mesh_lod` forward declaration.  The retained
draw-realization code still owns BSG-shaped scene refs until the public
scene-ref API boundary migrates, but the mesh-LoD load operation no longer
crosses a GED private pass-through.

The pivot guard now requires `rt_mesh_lod_load_view_scene_ref` in
`src/libged/draw.cpp`, rejects `bsg_mesh_lod_load_view_scene_ref` there,
rejects the retired `ged_draw_mesh_lod_load_view_scene_ref_context` call from
returning to `draw.cpp` or `ged_view_legacy.cpp`, and rejects the helper
declaration from returning to `bsg_ged_draw_view_private.h`.  Exit-criteria
percentages did not change: retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`.
The focused `brlobol_pivot_guard` gate passed in 21.46 seconds before the
status updates.  The standard broad focused wrapper gate completed 13/13
passing in 82.84 seconds; longest members were `libbsg_api_hygiene` at 40.95
seconds, `brlobol_pivot_guard` at 18.11 seconds,
`ged_test_drawing_quad` at 12.68 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

The GED scene-root context cleanup has been completed.  `include/rt/view.h`
now declares neutral `rt_view_scene_ref_context`, and
`src/librt/view_legacy_bsg.c` implements it for neutral `rt_view_scene_ref`
values.  `src/librt/tests/view_legacy_bsg.c` covers null scene-root refs and
non-null root-ref context extraction.  `src/libged/bsg_ged_draw_scene_root.c`
now calls `rt_view_scene_ref_context(ged_draw_view_context_scene_root_rt_ref(...))`
inside `ged_draw_view_context_scene_root`, so GED no longer converts the
neutral root ref back to `bsg_scene_ref` and then calls
`ged_draw_scene_ref_context` just to extract the same opaque pointer.

The pivot guard now requires `rt_view_scene_ref_context` in the RT public
header, RT legacy adapter, RT adapter test, and GED scene-root bridge.  It
also rejects `ged_draw_scene_ref_from_rt_view_ref` and
`ged_draw_scene_ref_context` from returning to
`src/libged/bsg_ged_draw_scene_root.c`.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so librt.so rt_view_legacy_bsg`.
The focused `brlobol_pivot_guard` plus `rt_view_legacy_bsg` gate completed 2/2
passing in 21.14 seconds.  The standard broad focused wrapper gate completed
13/13 passing in 82.57 seconds; longest members were `libbsg_api_hygiene` at
40.99 seconds, `brlobol_pivot_guard` at 17.75 seconds,
`ged_test_drawing_quad` at 12.69 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The GED shape-ref context cleanup has been completed.
`src/libged/bsg_ged_draw_transactions.c` now calls
`ged_draw_shape_ref_realize_context` while realizing transaction shape refs.
`src/libged/bsg_ged_draw_refs.c` now implements
`ged_draw_shape_ref_realize_context` and `ged_draw_shape_ref_view_context`
directly against the retained scene ref lookup, rather than bouncing through
typed private `struct bsg_view *` helpers.  The typed private
`ged_draw_shape_ref_realize` and `ged_draw_shape_ref_view` declarations and
definitions are retired from `src/libged/bsg_ged_draw_private.h` and
`src/libged/bsg_ged_draw_refs.c`.

The pivot guard now requires the context-shaped shape-ref helpers in the
private header and draw-ref implementation, requires transaction realization
to use `ged_draw_shape_ref_realize_context`, and rejects the retired typed
shape-ref realize/view helpers from returning.  Exit-criteria percentages did
not change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`.
Standalone `brlobol_pivot_guard` passed in 17.74 seconds.  The standard broad
focused wrapper gate completed 13/13 passing in 82.51 seconds; longest members
were `libbsg_api_hygiene` at 41.00 seconds, `brlobol_pivot_guard` at 17.72
seconds, `ged_test_drawing_quad` at 12.66 seconds, and
`ged_test_drawing_basic` at 7.54 seconds.

The GED shape-ref view/LoD private helper cleanup has been completed.
`src/libged/bsg_ged_draw_private.h` no longer declares the unused typed
private `ged_draw_shape_ref_set_view` and `ged_draw_shape_ref_adaptive_sync`
helpers.  Their definitions were removed from
`src/libged/bsg_ged_draw_refs.c`.  The remaining
`ged_draw_shape_ref_lod_ensure` helper now accepts opaque view contexts
(`void *first_view_ctx`, `void **view_ctxs`, and `view_ctx_count`) and uses
the already-neutral `rt_view_context_lod_policy_get` plus
`ged_draw_scene_ref_realize` APIs directly.  The retained realization logic is
unchanged; the private helper boundary no longer exposes a typed
`struct bsg_view *` or `struct bsg_view **` LoD contract.

`src/libged/bsg_ged_draw_transactions.c` now builds transaction view lists as
opaque context arrays.  `ged_draw_txn_view_array` returns `void **`, and the
prepare/autoview/finalize-LoD/transaction-realization loops call neutral
view-context helpers with those opaque handles.  Transaction logic that still
requires retained BSG view semantics remains in the BSG-backed bridge, but the
shape-ref LoD and realization handoff no longer requires typed view-array
plumbing across the private shape-ref helper boundary.

The pivot guard now requires the context-shaped `ged_draw_shape_ref_lod_ensure`
declaration, requires the opaque `void **view_ctxs` token in the private draw
header, requires transaction view arrays to use the "draw transaction view
context array" allocation label, and rejects the retired
`ged_draw_shape_ref_set_view`, `ged_draw_shape_ref_adaptive_sync`, old typed
`ged_draw_shape_ref_lod_ensure(... struct bsg_view ...)` shape, and typed
`struct bsg_view **` transaction array plumbing from returning to the guarded
files.  Exit-criteria percentages did not change: retained scene-root group
creation, retained scene-ref handoffs, BSG-backed feature/polygon internals,
retained group/overlay/redraw BSG view use, retained qtcad draw ownership,
and the direct `libbsg` rows in `libdm` and `libged` still determine
retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, followed by
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
An initial standalone `brlobol_pivot_guard` run exposed a guard regex syntax
bug in the new `struct bsg_view **` pattern; after replacing the literal-star
character classes with escaped star literals, standalone `brlobol_pivot_guard`
passed in 17.94 seconds.  The focused migration gate completed 4/4 passing in
18.05 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.60 seconds; longest members were `libbsg_api_hygiene` at 40.99
seconds, `brlobol_pivot_guard` at 17.82 seconds,
`ged_test_drawing_quad` at 12.66 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The GED shape-draft/view context cleanup has been completed.
`src/libged/bsg_ged_draw_private.h` no longer declares the typed private
`ged_draw_shape_draft_create` helper.  The retained BSG draft constructor is
now file-local in `src/libged/bsg_ged_draw_draft.c` as
`_ged_draw_shape_draft_create_bsg`, and external draft creation routes through
`ged_draw_shape_draft_create_context`.  The evaluated-shape transaction path
in `src/libged/bsg_ged_draw_transactions.c` now uses that context-shaped draft
creation helper instead of calling the typed BSG-view helper directly.

`ged_draw_shape_draft_publish_primitive_wireframe` now takes an opaque
`void *view_ctx` rather than `struct bsg_view *`, and passes that context
through to `ged_draw_scene_ref_publish_primitive_wireframe`.
`ged_draw_shape_ref_publish_primitive_wireframe` in
`src/libged/bsg_ged_draw_refs.c` no longer casts `view_ctx` before collecting
an RT view-info snapshot.  Both wrappers pass a view-info pointer only when an
adaptive call has a non-null view context; the central source publisher still
falls back to its default RT view-info when the pointer is null.  The retained
direct primitive fallback remains BSG-backed and file-local inside
`src/libged/bsg_ged_draw_source.c`.

The pivot guard now requires `ged_draw_shape_draft_create_context` and the
opaque `ged_draw_shape_draft_publish_primitive_wireframe(... void *view_ctx
...)` shape in the private GED draw header, requires the file-local BSG draft
constructor and neutral RT view-info helper in the draft implementation,
requires evaluated-shape transactions to create drafts through
`ged_draw_shape_draft_create_context`, and rejects the retired typed
`ged_draw_shape_draft_create` export plus typed primitive-wireframe draft
signature from returning to the guarded bridge files.  Exit-criteria
percentages did not change: retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, followed by
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The first `libged.so` compile exposed that `rt_view_context_info_get` returns
`void`, so the draft/ref wrappers were corrected to use a non-null view-context
guard and a statement call before the passing rebuild.  Standalone
`brlobol_pivot_guard` passed in 17.75 seconds.  The focused migration gate
completed 4/4 passing in 18.10 seconds.  The standard broad focused wrapper
gate completed 13/13 passing in 83.03 seconds; longest members were
`libbsg_api_hygiene` at 41.16 seconds, `brlobol_pivot_guard` at 17.98
seconds, `ged_test_drawing_quad` at 12.77 seconds, and
`ged_test_drawing_basic` at 7.53 seconds.

The GED realization-policy/frame-revision context cleanup has been completed.
`src/libged/bsg_ged_draw_private.h` no longer declares the typed private
`ged_draw_scene_ref_realization_set_bsg_view_policy` helper.  The only
cross-file policy snapshot boundary is now
`ged_draw_scene_ref_realization_set_view_context_policy`.  Its implementation
in `src/libged/bsg_ged_draw_view.c` reads LoD policy and scale directly from
the opaque RT view context with `rt_view_context_lod_policy_get` and
`rt_view_context_scale_get`, then stores the existing draw realization policy
fields on the scene ref.

`src/libged/bsg_ged_draw_scene_root.c` now reads frame revisions through
`rt_view_context_frame_revision_get` instead of casting the view context to a
retained `struct bsg_view` for the read-side helper.  The
`ged_draw_view_context_bump_frame_revision` path still increments
`gv_frame_rev` directly because there is not yet a neutral RT-side
increment/set API.  That retained write remains a real remaining backend
ownership item rather than a local wrapper rename.

The pivot guard now rejects
`ged_draw_scene_ref_realization_set_bsg_view_policy` from returning to
`src/libged/bsg_ged_draw_view.c` or `src/libged/bsg_ged_draw_private.h`,
requires the context-shaped realization policy helper in the private header,
and requires `src/libged/bsg_ged_draw_scene_root.c` to keep using
`rt_view_context_frame_revision_get`.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, followed by
`ctest --test-dir build -R '^brlobol_pivot_guard$' --timeout 180 --output-on-failure`,
then
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.06 seconds:
`brlobol_pivot_guard` at 17.77 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.49 seconds; longest members were `libbsg_api_hygiene` at 40.97
seconds, `brlobol_pivot_guard` at 17.78 seconds,
`ged_test_drawing_quad` at 12.67 seconds, and `ged_test_drawing_basic` at
7.50 seconds.

The RT/GED frame-revision bump cleanup has been completed.
`include/rt/view.h` now exposes
`rt_view_context_frame_revision_bump(void *ctx)` beside the existing neutral
`rt_view_context_frame_revision_get` API.  The retained implementation in
`src/librt/view_legacy_bsg.c` owns the transitional `gv_frame_rev` mutation,
returns 0 for null contexts, and returns the incremented revision for valid
contexts.  `src/librt/tests/view_legacy_bsg.c` now verifies both the null
case and the non-null bump/readback behavior.

`src/libged/bsg_ged_draw_scene_root.c` now implements
`ged_draw_view_context_bump_frame_revision` by calling the neutral RT context
helper instead of casting the GED draw view context to `struct bsg_view` and
incrementing `gv_frame_rev` directly.  With the previous read-side cleanup,
GED scene-root frame-revision read and bump operations now both cross the
neutral RT context boundary.  The retained storage is still BSG-backed, but
that backend detail is isolated in the RT legacy adapter.

The pivot guard now requires `rt_view_context_frame_revision_bump` in
`include/rt/view.h`, requires the RT test to cover the null bump case, requires
`src/libged/bsg_ged_draw_scene_root.c` to call the neutral bump helper, and
rejects direct `gv_frame_rev` access from returning to that GED bridge file.
Exit-criteria percentages did not change: retained scene-root group creation,
retained scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build rt_view_legacy_bsg libged.so`, followed by standalone
`rt_view_legacy_bsg` and `brlobol_pivot_guard`, then
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.00 seconds:
`brlobol_pivot_guard` at 17.71 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.52 seconds; longest members were `libbsg_api_hygiene` at 40.96
seconds, `brlobol_pivot_guard` at 17.72 seconds,
`ged_test_drawing_quad` at 12.75 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

The GED scene-root attach context cleanup has been completed.
`src/libged/bsg_ged_draw_scene_root.c` no longer uses the typed
`_sg_view_scene_root_set(struct bsg_view *, ...)` helper for existing-root
attachment.  The replacement `_sg_view_context_scene_root_set(void *, ...)`
calls `rt_view_context_scene_root_ref_attach` with an opaque view context.
The active-view helper now returns `void *`, and view-set iteration attaches
roots using the raw context entries from `BU_PTBL_GET` instead of casting each
entry to `struct bsg_view *`.

The only retained BSG view cast left in that scene-root path is the first-time
root group creation call to `bsg_scene_group_create`, which still requires the
retained backend view handle.  This keeps the semantic ownership unchanged:
existing-root reattachment and per-view traversal registration now cross the
neutral RT context boundary, while retained root group creation remains a
known BSG-backed ownership item.

The pivot guard now requires `_sg_view_context_scene_root_set` and
`_sg_scene_root_active_view_ctx` in `bsg_ged_draw_scene_root.c`, and rejects
the retired typed attach helper, typed active-view helper, and typed
`BU_PTBL_GET` view-set iteration casts from returning.  Exit-criteria
percentages did not change: retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.01 seconds:
`brlobol_pivot_guard` at 17.72 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.12 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.73 seconds; longest members were `libbsg_api_hygiene` at 40.98
seconds, `brlobol_pivot_guard` at 17.98 seconds,
`ged_test_drawing_quad` at 12.66 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

The GED overlay geometry context cleanup has been completed.
`src/libged/bsg_ged_draw_overlay.c` no longer creates overlay geometry scenes
through a typed `_ged_overlay_create_scene(struct bsg_view *, ...)` helper.
The retained BSG geometry creation now lives behind
`ged_draw_view_context_overlay_geometry_create` in
`src/libged/bsg_ged_draw_view.c`, alongside the existing overlay find, erase,
append, and owner helpers.  The private draw bridge declares the helper after
the neutral `ged_draw_overlay_geometry_kind` enum, keeping the narrow
view-private header from depending on that fuller draw-bridge enum.

`ged_draw_overlay_geometry_insert` now keeps the active view as an opaque
`void *` context.  It creates overlay geometry scenes through the new
view-context helper, appends through `ged_draw_view_context_overlay_scene_append`,
and updates bounds through `ged_draw_scene_ref_update_bounds_context` instead
of calling `bsg_scene_update_bounds` with a typed BSG view.  Direct BSG overlay
registry access and geometry construction are now both outside this insertion
workflow.

The pivot guard now requires `ged_draw_view_context_overlay_geometry_create`
in `src/libged/bsg_ged_draw_view.c` and `src/libged/bsg_ged_draw_private.h`,
requires `src/libged/bsg_ged_draw_overlay.c` to call the overlay geometry
context helper plus `ged_draw_scene_ref_update_bounds_context`, and rejects
the retired `_ged_overlay_create_scene`, direct `bsg_*_ref_create`, direct
`bsg_scene_update_bounds`, and typed `view_ctx` cast forms from returning to
the overlay insertion bridge.  Exit-criteria percentages did not change:
retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.05 seconds:
`brlobol_pivot_guard` at 17.76 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.74 seconds; longest members were `libbsg_api_hygiene` at 40.96
seconds, `brlobol_pivot_guard` at 17.97 seconds,
`ged_test_drawing_quad` at 12.67 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

The GED overlay geometry publication cleanup has been completed.
`src/libged/bsg_ged_draw_overlay.c` no longer publishes overlay geometry by
calling `bsg_geometry_ref_set_line_set`,
`bsg_geometry_ref_set_point_set`, or `bsg_geometry_ref_set_indexed_face_set`
directly.  Overlay insertion now delegates line sets, point sets, and
indexed-face sets to scene-ref publication helpers.  The existing line-set and
indexed-face helpers were reused, and the missing
`ged_draw_scene_ref_publish_point_set` helper was added to
`src/libged/bsg_ged_draw_source.c` and declared in the private draw bridge.

The new point-set helper follows the same contract as the existing line-set
facade: it requires shape state, permits an empty point set, rejects non-empty
sets with a null point array, updates `geometry_command_count`, increments
`geometry_revision`, and invalidates the scene after successful publication.
This keeps point overlay behavior consistent with the previous direct BSG
publication while making overlay insertion independent of BSG geometry refs.

The pivot guard now requires overlay insertion to call
`ged_draw_scene_ref_publish_line_set`,
`ged_draw_scene_ref_publish_point_set`,
`ged_draw_scene_ref_publish_indexed_face_set`, and
`ged_draw_scene_ref_update_bounds_context`, and rejects direct `bsg/geometry.h`,
`bsg_geometry_ref_set_*`, and `bsg_scene_ref_as_geometry` use from returning to
`src/libged/bsg_ged_draw_overlay.c`.  It also requires
`ged_draw_scene_ref_publish_point_set` in the private draw bridge.
Exit-criteria percentages did not change: retained scene-root group creation,
retained scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.15 seconds:
`brlobol_pivot_guard` at 17.86 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.50 seconds; longest members were `libbsg_api_hygiene` at 40.90
seconds, `brlobol_pivot_guard` at 17.75 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The GED overlay scene-ref facade cleanup has been completed.
`src/libged/bsg_ged_draw_overlay.c` no longer includes direct BSG overlay
scene headers and no longer calls BSG scene naming, null-check, destruction,
or legacy display-attribute setters directly.  Overlay insertion now uses
`ged_draw_scene_ref_is_null` for scene-ref null checks, names the overlay
scene through `ged_draw_scene_ref_set_name`, releases failed scene refs through
`ged_draw_scene_ref_release`, and applies the legacy overlay display defaults
through `ged_draw_scene_ref_apply_overlay_geometry_attributes`.

The new source-facade helpers live in `src/libged/bsg_ged_draw_source.c`.
`ged_draw_scene_ref_set_name` validates the scene ref and name before calling
the retained BSG setter.  `ged_draw_scene_ref_apply_overlay_geometry_attributes`
keeps the prior overlay behavior intact: highlighted off, solid line style,
legacy evaluated-region flag on, legacy/base display color from the overlay
RGB value, region/work flags zeroed, and the requested transparency and draw
mode applied.  The helper intentionally uses the retained BSG setters inside
the source facade rather than changing draw-intent semantics in the overlay
bridge.

The pivot guard now requires the overlay bridge to use the scene-ref facade
tokens for naming, null checks, release, geometry publication, bounds update,
and overlay display attributes.  It also rejects direct BSG overlay scene
headers, `bsg_scene_ref_is_null`, `bsg_scene_ref_destroy`, and the retired
`bsg_scene_set_*` overlay attribute calls from returning to
`src/libged/bsg_ged_draw_overlay.c`.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.08 seconds:
`brlobol_pivot_guard` at 17.80 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.98 seconds; longest members were `libbsg_api_hygiene` at 41.13
seconds, `brlobol_pivot_guard` at 17.95 seconds,
`ged_test_drawing_quad` at 12.77 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED transaction scene-ref setter cleanup has been completed.
`src/libged/bsg_ged_draw_transactions.c` no longer mutates transaction
visibility or transparency with direct `bsg_scene_set_visible` or
`bsg_scene_set_transparency` calls.  The transaction callbacks still compare
against their shape/group record snapshots before writing, but the actual
scene-ref mutation now routes through `ged_draw_scene_ref_set_visible` and
`ged_draw_scene_ref_set_transparency`.

The new helpers live in `src/libged/bsg_ged_draw_source.c` beside the other
scene-ref source facade setters.  They validate the retained scene ref before
calling the underlying BSG setter and return success so transaction changed
counters only advance when a scene ref exists and the write is applied.  This
keeps the current behavior while moving another transaction write boundary
behind the scene-ref facade.

The pivot guard now requires the transaction bridge to use both scene-ref
setter tokens and rejects direct BSG visibility/transparency setters from
returning to `src/libged/bsg_ged_draw_transactions.c`.  The private draw
bridge guard also requires both helper declarations.  Exit-criteria
percentages did not change: retained scene-root group creation, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.06 seconds:
`brlobol_pivot_guard` at 17.77 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.69 seconds; longest members were `libbsg_api_hygiene` at 41.02
seconds, `brlobol_pivot_guard` at 17.85 seconds,
`ged_test_drawing_quad` at 12.67 seconds, and `ged_test_drawing_basic` at
7.57 seconds.

The GED shape-ref setter facade cleanup has been completed.
`src/libged/bsg_ged_draw_refs.c` no longer mutates public shape-ref
visibility, display color, material color, or evaluated-region state with
direct BSG setters.  `ged_draw_shape_ref_set_flag` and
`ged_draw_shape_ref_set_visible` now route through
`ged_draw_scene_ref_set_visible`; `ged_draw_shape_ref_set_color` routes
through `ged_draw_scene_ref_set_color`; `ged_draw_shape_ref_set_material_color`
routes through `ged_draw_scene_ref_set_material_rgb`; and
`ged_draw_shape_ref_set_evaluated_region` routes through
`ged_draw_scene_ref_set_legacy_eval_flag`.  The visibility toggles inside
`ged_draw_shape_ref_sync_settings` also use the scene-ref visibility setter.

The new `ged_draw_scene_ref_set_color` and
`ged_draw_scene_ref_set_legacy_eval_flag` helpers live in
`src/libged/bsg_ged_draw_source.c` with the existing scene-ref source facade.
They validate the retained scene ref before calling the underlying BSG setter.
`ged_draw_scene_ref_set_material_rgb` now also validates the scene ref before
calling the retained material setter.  This keeps current behavior while
moving another public shape-ref write boundary behind the scene-ref facade.

The pivot guard now requires `src/libged/bsg_ged_draw_refs.c` to use the
scene-ref facade setter tokens and rejects direct BSG visibility/color,
material-color, and evaluated-region setters from returning there.  The
private draw bridge guard also requires the new helper declarations.
Exit-criteria percentages did not change: retained scene-root group creation,
retained scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.49 seconds:
`brlobol_pivot_guard` at 18.20 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.92 seconds; longest members were `libbsg_api_hygiene` at 41.13
seconds, `brlobol_pivot_guard` at 17.94 seconds,
`ged_test_drawing_quad` at 12.71 seconds, and `ged_test_drawing_basic` at
7.54 seconds.

The GED highlight scene-ref facade cleanup has been completed.
`src/libged/bsg_ged_draw_highlight.c` no longer mutates highlighted state
through direct `bsg_scene_set_highlighted` calls.  Highlight set/clear paths,
single-highlight tracking updates, the O(1) highlighted-shape clear fast path,
path-prefix highlighting, and explicit shape-ref highlight mutation now route
through `ged_draw_scene_ref_set_highlighted`.  The file also uses
`ged_draw_scene_ref_is_null` for retained scene-ref null checks, includes the
private draw bridge directly, and no longer includes direct BSG headers or
depends on BSG's legacy `UP`/`DOWN` macros.

The new `ged_draw_scene_ref_set_highlighted` helper lives in
`src/libged/bsg_ged_draw_source.c` with the existing scene-ref source facade.
It validates the retained scene ref before calling the underlying BSG setter
and returns success so multi-record highlight operations can continue to count
only applied mutations.  This keeps the current highlight behavior while
moving another direct scene-state write boundary out of the highlight bridge.

The pivot guard now requires the highlight bridge to include
`bsg_ged_draw_private.h` and use `ged_draw_scene_ref_set_highlighted` plus
`ged_draw_scene_ref_is_null`.  It rejects direct BSG headers,
`bsg_scene_set_highlighted`, `bsg_scene_ref_is_null`, and legacy BSG
`UP`/`DOWN` flag tokens from returning to
`src/libged/bsg_ged_draw_highlight.c`.  The private draw bridge guard also
requires the new helper declaration.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.13 seconds:
`brlobol_pivot_guard` at 17.85 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.50 seconds; longest members were `libbsg_api_hygiene` at 41.00
seconds, `brlobol_pivot_guard` at 17.74 seconds,
`ged_test_drawing_quad` at 12.63 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

The GED material-refresh scene-ref facade cleanup has been completed.
`src/libged/bsg_ged_draw_material.c` no longer mutates material RGB, material
revision, or legacy default-color flags through direct
`bsg_scene_material_set_rgb`, `bsg_scene_material_set_revision`, or
`bsg_scene_set_legacy_uses_default_color` calls.  Material refresh still reads
the retained legacy region/color fields to choose the same effective material
color, but scene-state writes now route through
`ged_draw_scene_ref_set_material_rgb`,
`ged_draw_scene_ref_set_material_revision`, and
`ged_draw_scene_ref_set_legacy_uses_default_color`.

The new default-color and material-revision helpers live in
`src/libged/bsg_ged_draw_source.c` with the existing scene-ref source facade.
They validate the retained scene ref before calling the underlying BSG setter.
This keeps the current color-soltab behavior and per-shape material-revision
stamping while moving another material write boundary out of the material
bridge.

The pivot guard now requires the material bridge to include
`bsg_ged_draw_private.h` and use the scene-ref facade tokens for material
RGB, material revision, and legacy default-color writes.  It rejects the
retired direct BSG material/default-color setters from returning to
`src/libged/bsg_ged_draw_material.c`.  The private draw bridge guard also
requires the new helper declarations.  Exit-criteria percentages did not
change: retained scene-root group creation, retained scene-ref handoffs,
BSG-backed feature/polygon internals, retained group/overlay/redraw BSG view
use, retained qtcad draw ownership, and the direct `libbsg` rows in `libdm`
and `libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.12 seconds:
`brlobol_pivot_guard` at 17.84 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.75 seconds; longest members were `libbsg_api_hygiene` at 41.02
seconds, `brlobol_pivot_guard` at 17.89 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED shape-ref invalidation facade cleanup has been completed.
`src/libged/bsg_ged_draw_refs.c` no longer invalidates retained scene refs
through direct `bsg_scene_invalidate` calls.  Shape-ref geometry-change,
reset-node, and LoD/view-input invalidation paths now route through
`ged_draw_scene_ref_invalidate`, keeping invalidation ownership with the
scene-ref source facade while preserving the existing geometry revision,
mesh-LoD cleanup, realization reset, and view-input change behavior.

The pivot guard now requires `ged_draw_scene_ref_invalidate` in
`src/libged/bsg_ged_draw_refs.c` and rejects direct `bsg_scene_invalidate`
from returning there.  Exit-criteria percentages did not change: retained
scene-root group creation, retained scene-ref handoffs, BSG-backed
feature/polygon internals, retained group/overlay/redraw BSG view use,
retained qtcad draw ownership, and the direct `libbsg` rows in `libdm` and
`libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.10 seconds:
`brlobol_pivot_guard` at 17.81 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.12 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 83.04 seconds; longest members were `libbsg_api_hygiene` at 41.01
seconds, `brlobol_pivot_guard` at 17.99 seconds,
`ged_test_drawing_quad` at 12.67 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED scene-root visibility facade cleanup has been completed.
`src/libged/bsg_ged_draw_scene_root.c` no longer marks the retained draw root
visible through a direct `bsg_scene_set_visible` call.  Root creation now uses
`ged_draw_scene_ref_set_visible`, matching the transaction visibility facade
used elsewhere while leaving retained root group creation and draw-context
attachment in the BSG-backed construction path.

The pivot guard now requires the scene-root bridge to use
`ged_draw_scene_ref_set_visible` and rejects direct `bsg_scene_set_visible`
from returning to `src/libged/bsg_ged_draw_scene_root.c`.  Exit-criteria
percentages did not change: retained scene-root group creation and draw-context
storage, retained scene-ref handoffs, BSG-backed feature/polygon internals,
retained group/overlay/redraw BSG view use, retained qtcad draw ownership, and
the direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.14 seconds:
`brlobol_pivot_guard` at 17.85 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.92 seconds; longest members were `libbsg_api_hygiene` at 40.89
seconds, `brlobol_pivot_guard` at 18.19 seconds,
`ged_test_drawing_quad` at 12.73 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED tree/state draw-intent construction facade cleanup has been completed.
`src/libged/bsg_ged_draw_tree.c` no longer renames groups or creates missing
group draw intents through direct `bsg_scene_set_name`,
`bsg_scene_set_draw_intent`, or `bsg_draw_intent_create` calls.
`src/libged/bsg_ged_draw_state.c` also no longer creates missing semantic
shape draw intents through direct BSG construction.  Group renames now use
`ged_draw_scene_ref_set_name`, and missing retained draw-intent construction
uses the new `ged_draw_scene_ref_ensure_draw_intent` scene-ref facade helper.
Existing draw-intent reads and existing-intent path/mode/appearance updates
remain BSG-backed until the wider group API boundary migrates.

The pivot guard now requires the new helper declaration in
`src/libged/bsg_ged_draw_private.h`, requires tree group mutation to route
through `ged_draw_scene_ref_set_name` and
`ged_draw_scene_ref_ensure_draw_intent`, and rejects direct draw-intent
construction from returning to `src/libged/bsg_ged_draw_tree.c` or
`src/libged/bsg_ged_draw_state.c`.  Exit-criteria percentages did not change:
retained scene-root group creation and draw-context storage, retained
scene-ref handoffs, BSG-backed feature/polygon internals, retained
group/overlay/redraw BSG view use, retained qtcad draw ownership, and the
direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.31 seconds:
`brlobol_pivot_guard` at 18.02 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.54 seconds; longest members were `libbsg_api_hygiene` at 41.01
seconds, `brlobol_pivot_guard` at 17.78 seconds,
`ged_test_drawing_quad` at 12.64 seconds, and `ged_test_drawing_basic` at
7.52 seconds.

The GED scene-root draw-context facade cleanup has been completed.
`src/libged/bsg_ged_draw_scene_root.c` no longer includes `bsg/draw_ctx.h` or
attaches the retained root draw context through direct
`bsg_scene_set_draw_ctx`.  Root draw-context attachment now uses
`ged_draw_scene_ref_set_draw_context`, keeping the BSG draw-context cast and
setter inside the scene-ref source facade in
`src/libged/bsg_ged_draw_source.c`.

The pivot guard now requires `ged_draw_scene_ref_set_draw_context` in
`src/libged/bsg_ged_draw_private.h` and
`src/libged/bsg_ged_draw_scene_root.c`, and rejects direct `bsg/draw_ctx.h`
includes or `bsg_scene_set_draw_ctx` calls from returning to the scene-root
bridge.  Exit-criteria percentages did not change: retained scene-root group
creation, retained scene-ref handoffs, BSG-backed feature/polygon internals,
retained group/overlay/redraw BSG view use, retained qtcad draw ownership, and
the direct `libbsg` rows in `libdm` and `libged` still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.09 seconds:
`brlobol_pivot_guard` at 17.80 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.76 seconds; longest members were `libbsg_api_hygiene` at 40.98
seconds, `brlobol_pivot_guard` at 17.92 seconds,
`ged_test_drawing_quad` at 12.72 seconds, and `ged_test_drawing_basic` at
7.55 seconds.

The GED tree/refs scene-ref detach/release facade cleanup has been completed.
`src/libged/bsg_ged_draw_refs.c` no longer removes a shape/source ref from its
parent or destroys it through direct `bsg_scene_remove_child` and
`bsg_scene_ref_destroy` calls.  Shape-ref release now detaches through
`ged_draw_scene_ref_detach` and releases through `ged_draw_scene_ref_release`.
`src/libged/bsg_ged_draw_tree.c` now uses the same detach facade before group
release in clear-all group cleanup.  The direct retained child removal and
destroy operations are contained in the scene-ref owning implementation in
`src/libged/bsg_ged_draw_node.c`.

The pivot guard now requires `ged_draw_scene_ref_detach` in
`src/libged/bsg_ged_draw_private.h`, requires tree/refs callers to use it, and
rejects direct `bsg_scene_remove_child` or `bsg_scene_ref_destroy` from
returning to `src/libged/bsg_ged_draw_tree.c` or
`src/libged/bsg_ged_draw_refs.c`.  Exit-criteria percentages did not change:
retained scene-root group creation, retained scene-ref handoffs, BSG-backed
feature/polygon internals, retained group/overlay/redraw BSG view use,
retained qtcad draw ownership, and the direct `libbsg` rows in `libdm` and
`libged` still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.26 seconds:
`brlobol_pivot_guard` at 17.97 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.87 seconds; longest members were `libbsg_api_hygiene` at 40.96
seconds, `brlobol_pivot_guard` at 18.09 seconds,
`ged_test_drawing_quad` at 12.67 seconds, and `ged_test_drawing_basic` at
7.57 seconds.

The GED tree group-free facade cleanup has been completed.
`src/libged/bsg_ged_draw_tree.c` no longer erases retained groups or clears
group contents through direct `bsg_scene_free_group` or
`bsg_scene_free_group_contents` calls.  Erase-by-path, scoped erase, name
erase, empty-group pruning, prefix erase, clear-in-scope, and clear-all group
paths now route those retained operations through
`ged_draw_scene_ref_free_group` and
`ged_draw_scene_ref_free_group_contents`.  The direct BSG group-free calls are
contained in the scene-ref owning helper implementation in
`src/libged/bsg_ged_draw_node.c`.

The pivot guard now requires the new group-free helper declarations in
`src/libged/bsg_ged_draw_private.h`, requires the tree wrapper to use them,
and rejects direct `bsg_scene_free_group` or
`bsg_scene_free_group_contents` from returning to
`src/libged/bsg_ged_draw_tree.c`.  Exit-criteria percentages did not change:
retained scene-root group creation, retained scene-ref handoffs, BSG-backed
feature/polygon internals, retained group/overlay/redraw BSG view use,
retained qtcad draw ownership, and the direct `libbsg` rows in `libdm` and
`libged` still determine retirement readiness.  Direct child append for shape
attachment remains in the tree wrapper for a later attachment-boundary
migration.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so`, standalone `brlobol_pivot_guard`, and
`ninja -C build rt_view_legacy_bsg libged.so libqtcad.so test_qtcad_obol_draw_sync qsketch`.
The focused migration gate completed 4/4 passing in 18.08 seconds:
`brlobol_pivot_guard` at 17.79 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `ged_test_qged_swrast` at
0.11 seconds.  The standard broad focused wrapper gate completed 13/13
passing in 82.76 seconds; longest members were `libbsg_api_hygiene` at 40.93
seconds, `brlobol_pivot_guard` at 18.02 seconds,
`ged_test_drawing_quad` at 12.68 seconds, and `ged_test_drawing_basic` at
7.51 seconds.

The qtcad input/screen/matrix public helper cleanup has been completed.
`include/qtcad/QgLegacyView.h` and `src/libqtcad/QgLegacyView.cpp` no longer
export or implement the public qtcad helpers for width/height reads,
screen-to-view conversion, screen-point projection, current-point state,
mouse state, view/model matrix reads and writes, selection ray construction,
or interactive-rectangle state.  The migrated qtcad filters and host paths
now reach the private qtcad legacy-view context adapter and call neutral
`rt_view_context_*` APIs directly in `QgViewFilter.cpp`,
`QgSketchFilter.cpp`, `QgSelectFilter.cpp`, `QgMeasureFilter.cpp`,
`QgPolyFilter.cpp`, `QgCanvasInput.cpp`, `QgQuadView.cpp`, `QgGL.cpp`,
`QgSW.cpp`, and `test_qtcad_obol_snap.cpp`.

The pivot guard now requires the neutral RT context calls in the migrated
qtcad paths and rejects the retired public qtcad input/screen/matrix helper
names from returning to the installed header, legacy-view implementation, or
qtcad callers.  `obol_activework.txt` now lists the remaining pre-feature
implementation estimate as 2-4 cycles if the remaining handoffs stay shallow,
or 5-7 cycles if a retained draw/backend path needs a new neutral contract.
The qtcad/qged migration estimate is now 84 percent.  Remaining blockers are
still retained draw-sync/backend ownership, retained GED scene-ref/root/scope
handoffs, direct lower `libbsg` rows in `libdm` and `libged`, `qged`'s
direct `dm_plugins` dependency, and lower display/backend ownership.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so test_qtcad_obol_snap test_qtcad_obol_pick
test_qtcad_obol_measure test_qtcad_obol_draw_sync qsketch` in 32.70 seconds.
The focused qtcad/guard gate completed 6/6 passing in 18.76 seconds:
`brlobol_pivot_guard` at 18.15 seconds, `test_qtcad_obol_draw_sync` at
0.11 seconds, `test_qtcad_obol_pick` at 0.13 seconds,
`test_qtcad_obol_snap` at 0.11 seconds, `test_qtcad_obol_measure` at
0.10 seconds, and `ged_test_qged_swrast` at 0.11 seconds.  The full qtcad
Obol group completed 16/16 passing in 42.40 seconds; longest was
`test_qtcad_obol_real_models_havoc_wire` at 26.90 seconds.  The standard
broad focused wrapper gate completed 28/28 passing in 116.35 seconds; longest
members were `libbsg_api_hygiene` at 40.97 seconds,
`brlobol_pivot_guard` at 17.92 seconds, `ged_test_drawing_select` at
16.32 seconds, and `ged_test_drawing_quad` at 12.70 seconds.

The qged edit-preview feature helper cleanup has been completed.
`src/qged/plugins/edit/qged_edit_preview_util.cpp` no longer routes
edit-preview feature operations through public qtcad legacy-view feature
wrappers.  It now includes the private `QgLegacyViewContext.h` adapter and
`rt/view.h`, converts qged feature refs to `rt_view_feature_ref`, and calls
neutral `rt_view_context_edit_preview_publish_event`,
`rt_view_context_feature_overlay_ensure`,
`rt_view_context_feature_label_ensure`,
`rt_view_context_feature_remove`, `rt_view_feature_set_context`,
`rt_view_feature_set_visible`, `rt_view_feature_set_color`,
`rt_view_feature_touch`, `rt_view_feature_labels_replace`,
`rt_view_feature_points_replace`, and `rt_view_feature_clear_geometry`
directly.

`include/qtcad/QgLegacyView.h` and `src/libqtcad/QgLegacyView.cpp` no longer
export or implement the public qtcad feature/edit-preview helper layer:
`qg_legacy_view_feature_ref_is_null`,
`qg_legacy_view_edit_preview_publish_event`,
`qg_legacy_view_feature_overlay_ensure`,
`qg_legacy_view_feature_label_ensure`,
`qg_legacy_view_feature_remove`, `qg_legacy_view_feature_set_view`,
`qg_legacy_view_feature_set_visible`,
`qg_legacy_view_feature_set_color`, `qg_legacy_view_feature_touch`,
`qg_legacy_view_feature_labels_replace`,
`qg_legacy_view_feature_points_replace`, and
`qg_legacy_view_feature_clear_geometry`, along with the qtcad feature/edit
typedefs and `QG_LEGACY_VIEW_FEATURE_*` /
`QG_LEGACY_VIEW_EDIT_PREVIEW_*` aliases, are retired.  The pivot guard now
requires qged edit-preview to use the neutral RT feature APIs and rejects the
retired qtcad feature/edit-preview surface from returning to the installed
header, qtcad legacy-view implementation, or qged edit-preview utility.

`obol_activework.txt` now records the qged/qtcad migration estimate as 86
percent and the remaining pre-feature implementation estimate as 2-3 cycles
if the remaining ownership handoffs stay shallow, or 4-5 cycles if a
retained draw/backend path needs a new neutral contract.  Exit-criteria
percentages for qtcad visible Obol rendering did not change: retained RT
feature internals, retained polygon internals, qtcad draw-sync/backend
ownership, GED scene-ref/root/scope handoffs, direct lower `libbsg` rows,
and `qged`'s direct `dm_plugins` dependency still determine retirement
readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so qged_ell qged_revolve
test_qtcad_obol_edit_preview` in 29.16 seconds.  The targeted guard/runtime
verification completed 2/2 passing in 17.96 seconds:
`brlobol_pivot_guard` at 17.84 seconds and
`test_qtcad_obol_edit_preview` at 0.07 seconds.  The full qtcad Obol workflow
group completed 16/16 passing in 42.42 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.87 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.06 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.30
seconds; longest members were `libbsg_api_hygiene` at 40.92 seconds,
`brlobol_pivot_guard` at 17.84 seconds, `ged_test_drawing_select` at 16.31
seconds, and `ged_test_drawing_quad` at 12.67 seconds.

The qtcad polygon public-helper cleanup has been completed.
`src/libqtcad/QgPolyFilter.cpp`,
`src/qged/plugins/polygon/QPolyCreate.cpp`, and
`src/qged/plugins/polygon/QPolyMod.cpp` no longer route polygon operations
through the installed qtcad legacy-view polygon helper surface.  They now
include the private `QgLegacyViewContext.h` adapter and `rt/view.h`, convert
the qtcad legacy view handle to an opaque view context with
`qg_legacy_view_to_context`, and call neutral RT polygon APIs directly:
`rt_view_context_polygon_create`, `rt_view_context_polygon_select`,
`rt_view_context_polygon_find`, `rt_view_context_polygon_dup`,
`rt_view_context_polygon_visit_records`,
`rt_view_context_polygon_snap_count`,
`rt_view_context_polygon_clear_point_selection`,
`rt_view_context_polygon_snap_exclude_set`,
`rt_view_polygon_ref_is_null`, `rt_view_polygon_record_get`,
`rt_view_polygon_update_context`,
`rt_view_polygon_update_screen_pt_context`, `rt_view_polygon_move`,
`rt_view_polygon_set_name`, `rt_view_polygon_set_context`,
`rt_view_polygon_set_visual`, `rt_view_polygon_set_open`,
`rt_view_polygon_close`, `rt_view_polygon_clear_selected_point`,
`rt_view_polygon_remove`, `rt_view_polygon_user_data`,
`rt_view_polygon_user_data_set`, `rt_view_polygon_csg`,
`rt_view_polygon_import_sketch_context`, and
`rt_view_polygon_export_sketch` where applicable.

`include/qtcad/QgLegacyView.h` and `src/libqtcad/QgLegacyView.cpp` no longer
export or implement `qg_legacy_view_polygon_*` helpers or
`qg_legacy_view_polygon_record_callback_t`.  The stale installed-header
`bg/polygon.h`, `rt/view.h`, `bu_color`, and `directory` exposure in
`QgLegacyView.h` was also trimmed after the polygon declarations were
removed.  The pivot guard now requires the migrated qtcad/qged polygon paths
to use the neutral RT polygon APIs and rejects the retired qtcad polygon
helper surface from returning to the installed header, legacy-view
implementation, qtcad polygon filter, or qged polygon plugins.

`obol_activework.txt` now records the qged/qtcad migration estimate as 87
percent and the remaining pre-feature implementation estimate as 2-3 cycles
if the remaining ownership handoffs stay shallow, or 3-5 cycles if a
retained draw/backend path needs a new neutral contract.  Exit-criteria
percentages for qtcad visible Obol rendering did not change: retained RT
polygon internals, qtcad draw-sync/backend ownership, GED scene-ref/root/scope
handoffs, direct lower `libbsg` rows, and `qged`'s direct `dm_plugins`
dependency still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so qged_polygon_create qged_polygon_modify
test_qtcad_obol_snap test_qtcad_obol_draw_sync test_qtcad_obol_pick
test_qtcad_obol_measure qsketch` in 33.27 seconds.  The targeted
guard/runtime verification completed 5/5 passing in 18.29 seconds:
`brlobol_pivot_guard` at 17.80 seconds, `test_qtcad_obol_draw_sync` at 0.11
seconds, `test_qtcad_obol_pick` at 0.12 seconds,
`test_qtcad_obol_snap` at 0.11 seconds, and
`test_qtcad_obol_measure` at 0.10 seconds.  The full qtcad Obol workflow
group completed 16/16 passing in 42.44 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.97 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.03 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.18 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.50
seconds; longest members were `libbsg_api_hygiene` at 40.99 seconds,
`brlobol_pivot_guard` at 18.00 seconds, `ged_test_drawing_select` at 16.30
seconds, and `ged_test_drawing_quad` at 12.67 seconds.

The qtcad pick/selection public-helper cleanup has been completed.
`include/qtcad/QgLegacyView.h` and `src/libqtcad/QgLegacyView.cpp` no longer
export or implement the installed qtcad pick/result/selection helper surface:
`qg_legacy_view_pick_point`, `qg_legacy_view_pick_nearest`,
`qg_legacy_view_pick_rect`, `qg_legacy_view_pick_result_create`,
`qg_legacy_view_pick_result_free`, `qg_legacy_view_pick_result_count`,
`qg_legacy_view_pick_result_path`,
`qg_legacy_view_pick_result_hit_dist`,
`qg_legacy_view_pick_result_append_copy`,
`qg_legacy_view_pick_result_filter_first`,
`qg_legacy_view_selection_set_pick_result_ref`, and
`qg_legacy_view_selection_clear`.  The public
`qg_legacy_view_pick_result` and
`qg_legacy_view_selection_path_callback_t` qtcad selection types are retired
from the installed header as well.

`include/qtcad/QgSelectFilter.h` keeps the selected-result handle opaque with
`void *`, and `src/libqtcad/QgSelectFilter.cpp` now reaches the private
`QgLegacyViewContext.h` adapter plus neutral RT/context APIs directly.
Selection result ownership and extraction use
`rt_view_pick_result_context_create`,
`rt_view_pick_result_context_count`, `rt_view_pick_result_context_path`,
`rt_view_pick_result_context_hit_dist`,
`rt_view_pick_result_context_append_copy`,
`rt_view_pick_result_context_filter_first`, and
`rt_view_pick_result_context_free`.  Point, nearest, rectangle, ray-filtered,
and path-only selections use `rt_view_context_pick_point`,
`rt_view_context_pick_nearest`, `rt_view_context_pick_rect`,
`rt_view_context_selection_set_pick_result_context`, and
`rt_view_context_selection_clear`.  The retained RT pick/selection
implementation remains BSG-backed until the wider selection backend moves,
but the installed qtcad boundary is no longer shaped like the old legacy
wrapper layer.

`obol_activework.txt` now records the qged/qtcad migration estimate as 88
percent and the remaining pre-feature implementation estimate as 2-3 cycles
if the remaining ownership handoffs stay shallow, or 3-4 cycles if a
retained draw/backend path needs a new neutral contract.  Exit-criteria
percentages for qtcad visible Obol rendering did not change: retained RT
selection, feature, and polygon internals, qtcad draw-sync/backend ownership,
GED scene-ref/root/scope handoffs, direct lower `libbsg` rows, and `qged`'s
direct `dm_plugins` dependency still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so qged_select test_qtcad_obol_pick
test_qtcad_obol_selection_sync` in 28.31 seconds.  The targeted
guard/runtime verification completed 3/3 passing in 18.05 seconds:
`brlobol_pivot_guard` at 17.78 seconds,
`test_qtcad_obol_selection_sync` at 0.10 seconds, and
`test_qtcad_obol_pick` at 0.13 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.45 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.99 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.01 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.18 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.30
seconds; longest members were `libbsg_api_hygiene` at 40.96 seconds,
`brlobol_pivot_guard` at 17.74 seconds, `ged_test_drawing_select` at 16.30
seconds, and `ged_test_drawing_quad` at 12.71 seconds.

The qtcad sketch private-helper cleanup has been completed.  The private
`src/libqtcad/QgLegacyViewSketch.h` bridge is deleted, and
`src/libqtcad/QgLegacyView.cpp` no longer implements
`qg_legacy_view_sketch_edit_create`,
`qg_legacy_view_sketch_lines_create`,
`qg_legacy_view_sketch_lines_is_null`,
`qg_legacy_view_sketch_lines_set_points`, or
`qg_legacy_view_sketch_lines_destroy`.  This removes the remaining
qtcad-owned sketch-specific wrapper layer from the legacy-view implementation.

`src/libqtcad/tests/qsketch.cpp` now owns its small local `qsketch_line_set`
handle and calls the neutral RT/context APIs directly:
`rt_edit_create_context`, `rt_view_context_line_set_create`,
`rt_view_line_set_context_is_null`,
`rt_view_line_set_context_set_points`, and
`rt_view_line_set_context_destroy`.  The qtcad legacy-view handle is still
converted through the private `QgLegacyViewContext.h` adapter.  The retained
RT edit and line-set implementation remains BSG-backed until the wider
feature and line-set backend moves, but qtcad no longer has a legacy-shaped
sketch bridge.

`obol_activework.txt` records this as a private-wrapper cleanup and keeps the
qged/qtcad migration estimate at 88 percent.  Exit-criteria percentages did
not change: retained RT feature/line-set/polygon/selection internals, qtcad
draw-sync/backend ownership, GED scene-ref/root/scope handoffs, direct lower
`libbsg` rows, and `qged`'s direct `dm_plugins` dependency still determine
retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libqtcad.so qsketch` in 88.73 seconds.  The build included a
CMake regenerate after deleting the private bridge header.  The targeted
guard/runtime verification completed 4/4 passing in 17.98 seconds:
`brlobol_pivot_guard` at 17.70 seconds, `rt_view_legacy_bsg` at 0.02 seconds,
`test_qtcad_obol_draw_sync` at 0.11 seconds, and `test_qtcad_obol_snap` at
0.11 seconds.  The full qtcad Obol workflow group completed 16/16 passing in
42.40 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.90 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.31
seconds; longest members were `libbsg_api_hygiene` at 41.14 seconds,
`brlobol_pivot_guard` at 17.76 seconds, `ged_test_drawing_select` at 16.31
seconds, and `ged_test_drawing_quad` at 12.70 seconds.

The GED draw active-scope facade cleanup has been completed.  The private
`ged_draw_scene_ref_independent_scope` helper is retired from
`src/libged/bsg_ged_draw_private.h` and `src/libged/bsg_ged_draw_refs.c`.
`src/libged/bsg_ged_draw_refs.c` now owns
`ged_draw_scene_ref_active_scope`, which resolves the active draw scope
through neutral root storage and neutral RT independent-scope refs before
converting at the retained scene-ref boundary.  `src/libged/bsg_ged_draw_tree.c`
uses that facade for draw-path group creation, scoped erase/prune paths, and
view-clear independent-scope cleanup.  Tree-side direct
`rt_view_context_independent_scope_ref`,
`ged_draw_scene_ref_from_rt_view_ref`, and retired
`ged_draw_scene_ref_independent_scope` handoffs are now guarded against
returning.

`obol_activework.txt` records this as a private retained scene-ref/root/scope
handoff cleanup and keeps the qged/qtcad migration estimate at 88 percent.
Exit-criteria percentages did not change: retained draw/backend ownership,
retained RT feature/line-set/polygon/selection internals, remaining GED
scene-ref/root handoffs, direct lower `libbsg` rows, and `qged`'s direct
`dm_plugins` dependency still determine retirement readiness.  The remaining
pre-feature implementation estimate remains 2-3 cycles if the remaining
ownership handoffs stay shallow, or 3-4 cycles if a retained draw/backend
path needs a new neutral contract.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so libqtcad.so ged_test_view_independent
test_qtcad_obol_draw_sync` in 21.08 seconds.  The targeted guard/runtime
verification completed 8/8 passing in 68.04 seconds; longest members were
`brlobol_pivot_guard` at 17.68 seconds, `ged_test_drawing_select` at 16.34
seconds, `ged_test_drawing_quad` at 12.74 seconds, and
`ged_test_drawing_lod` at 11.02 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.45 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.95 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.10
seconds; longest members were `libbsg_api_hygiene` at 40.88 seconds,
`brlobol_pivot_guard` at 17.64 seconds, `ged_test_drawing_select` at 16.41
seconds, and `ged_test_drawing_quad` at 12.70 seconds.

The GED draw scene-root traversal facade cleanup has been completed.  The
scene-root bridge now owns private root traversal/read facades:
`ged_draw_scene_root_foreach_shape`,
`ged_draw_scene_root_foreach_group`,
`ged_draw_scene_root_subtree_bounds`,
`ged_draw_scene_root_last_group_ref`, and
`ged_draw_scene_root_has_groups`.  These helpers keep the neutral GED root
storage to retained scene-ref conversion at the scene-root boundary while
allowing callers to request semantic traversal/read operations.

`src/libged/bsg_ged_draw_navigation.c` now uses
`ged_draw_scene_root_foreach_shape` for shape existence checks, first-shape
lookup, and shape snapshot construction.  `src/libged/bsg_ged_draw_records.c`
now uses the root traversal facades for draw path state, draw path listing,
has-path filtering, group-record iteration, and shape-record iteration.
`src/libged/bsg_ged_draw_tree.c` now uses scene-root facades for root-level
subtree bounds, append-to-last-group, and has-groups checks.  The remaining
direct `ged_draw_scene_root_ref` callers outside
`src/libged/bsg_ged_draw_scene_root.c` are the complex tree erase/name/path
and full-clear mutation paths, which should move only behind a neutral
scene-root mutation contract.

The pivot guard now requires the new root traversal/read facades in the
private draw header and relevant GED draw implementation files, rejects direct
retained root reads from returning to records/navigation, and expects tree
root-level bounds to go through `ged_draw_scene_root_subtree_bounds` instead
of the lower scene-ref subtree helper.  `obol_activework.txt` narrows the
remaining pre-feature implementation estimate to about 2 cycles if remaining
ownership handoffs stay shallow, or about 3 cycles if a retained draw/backend
path needs a new neutral contract.  Exit-criteria percentages did not change:
retained draw/backend ownership, retained RT feature/line-set/polygon/selection
internals, remaining tree erase/clear root mutation ownership, direct lower
`libbsg` rows, and `qged`'s direct `dm_plugins` dependency still determine
retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so ged_test_view_independent
ged_test_selection_semantics test_qtcad_obol_draw_sync` in 18.95 seconds
after an initial focused build in 22.42 seconds.  The targeted guard/runtime
verification completed 9/9 passing in 65.89 seconds; longest members were
`brlobol_pivot_guard` at 17.81 seconds, `ged_test_drawing_select` at 16.29
seconds, `ged_test_drawing_quad` at 12.71 seconds, and
`ged_test_drawing_lod` at 10.96 seconds.  The full qtcad Obol workflow group
completed 16/16 passing in 42.43 seconds; longest members were
`test_qtcad_obol_real_models_havoc_wire` at 26.99 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.00 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.17 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.08
seconds; longest members were `libbsg_api_hygiene` at 40.95 seconds,
`brlobol_pivot_guard` at 17.69 seconds, `ged_test_drawing_select` at 16.36
seconds, and `ged_test_drawing_quad` at 12.70 seconds.

The GED draw scene-root mutation facade cleanup has been completed.  The
scene-root bridge now owns private `ged_draw_scene_root_mutate`, which
resolves the retained GED root at the scene-root boundary and invokes a
tree-local mutation callback.  This keeps neutral GED root storage to retained
scene-ref conversion out of tree code while preserving the existing erase and
clear semantics.

`src/libged/bsg_ged_draw_tree.c` now routes the root-scoped erase path,
erase-all-names, erase-all-paths, and full draw-clear mutation paths through
`ged_draw_scene_root_mutate`.  Direct `ged_draw_scene_root_ref` calls are now
limited to `src/libged/bsg_ged_draw_scene_root.c`.  Navigation and records
already use the root traversal facades, and simple tree root reads already
use root read facades, so this removes the last direct tree-side retained
root open.

The pivot guard now requires `ged_draw_scene_root_mutate` in the private draw
header and tree implementation, and rejects direct `ged_draw_scene_root_ref`
calls from returning to tree, records, or navigation.  `obol_activework.txt`
narrows the remaining pre-feature implementation estimate to about 1-2 cycles
if remaining ownership handoffs stay shallow, or about 2-3 cycles if a
retained draw/backend path needs a new neutral contract.  Exit-criteria
percentages did not change: retained draw/backend ownership, retained RT
feature/line-set/polygon/selection internals, public BSG-shaped GED
draw/snap/scene-ref bridge shims, direct lower `libbsg` rows, and `qged`'s
direct `dm_plugins` dependency still determine retirement readiness.

Verification on FreeBSD/clang passed with:
`ninja -C build libged.so ged_test_view_independent
ged_test_selection_semantics test_qtcad_obol_draw_sync` in 18.65 seconds.
The targeted guard/runtime verification completed 9/9 passing in 66.04
seconds; longest members were `brlobol_pivot_guard` at 17.79 seconds,
`ged_test_drawing_select` at 16.55 seconds, `ged_test_drawing_quad` at 12.66
seconds, and `ged_test_drawing_lod` at 10.92 seconds.  The full qtcad Obol
workflow group completed 16/16 passing in 42.55 seconds; longest members
were `test_qtcad_obol_real_models_havoc_wire` at 27.05 seconds,
`test_qtcad_obol_real_models_m35_wire_pick_all_stress` at 8.04 seconds, and
`test_qtcad_obol_real_models_m35_wire_interactions` at 4.19 seconds.  The
expanded broad focused wrapper gate completed 28/28 passing in 116.32
seconds; longest members were `libbsg_api_hygiene` at 40.94 seconds,
`brlobol_pivot_guard` at 17.83 seconds, `ged_test_drawing_select` at 16.29
seconds, and `ged_test_drawing_quad` at 12.73 seconds.

The Capability 1 closeout guard refresh has been completed for the current
Obol-primary GED draw-scene boundary.  The stale guard assumptions that still
expected retained BSG group display/draw-intent fallback markers were removed,
and the guard now checks the database path-state/listing, source-summary,
component rename, prefix event ordering, and split draw-scene shard boundaries.
The libged Obol draw bridge guard still rejects direct BSG/qtcad/DM ownership
dependencies, but it allows the private transitional draw facade include while
that neutral handle boundary exists.

`backend_capabilities.txt`, `obol_activework.txt`, and
`doc/notes/obol_legacy_dependency_inventory.txt` now describe Capability 1 as
guarded closeout: the remaining work is a database scene/source BSG-use audit
and the broad closeout gate, not another compatibility-shim pass.  Verification
passed with:
`ctest --test-dir build -R '^brlobol_pivot_guard$' --timeout 180 --output-on-failure`.

The first database-source API audit pass found direct `bsg_database_source_*`
and `BSG_DATABASE_SOURCE_*` use only in `src/libged/bsg_ged_draw_source.c`.
That remaining use is classified as source-adapter/no-owned-scene legacy
compatibility; records, transactions, the libged Obol bridge, and the public
GED draw header are clean for that BSG API family.  The source adapter comment
and pivot guard token now say retained BSG record apply is no-owned-scene
legacy compatibility only, not active database fallback.

The core Capability 1 closeout gate now passes in this build for the
draw-scene shard set, `ged_test_state_services`, `ged_test_obol_draw_sync`, and
`brlobol_pivot_guard`.  The corrected shard command is:
`ctest --test-dir build -R '^ged_test_ged_draw_scene_.*$' -j 8 --timeout 60 --output-on-failure`,
which completed 16/16 passing in 2.45 seconds.

The qtcad closeout subset is not a valid signal in this headless setup.  The
non-GL qtcad Obol tests in the broad command passed, but the draw-sync,
selection, pick, snap, and measure tests failed immediately under CTest's
`QT_QPA_PLATFORM=offscreen` environment because the Qt offscreen plugin cannot
create an OpenGL context.  A verbose rerun confirmed the test environment and
the standalone `test_qtcad_obol_draw_sync` failure.  Capability 1 should not be
declared complete until that subset is rerun on a GL-capable Qt platform or an
intentional non-GL qtcad verification path is added.  The following entry
supersedes this blocker by adding that intentional non-GL path.

The qtcad Obol closeout signal now has an intentional non-system-GL path.  The
private qtcad Obol context manager recognizes `QTCAD_OBOL_FORCE_OSMESA=1` using
`bu_str_true` and routes libbrlobol rendering through the OSMesa context
manager.  qtcad Obol workflow tests and `ged_test_qged_swrast` set that
environment in CTest, and the pivot guard requires the swrast test
environment plus the context-manager tokens.

The swrast failures were fixed in the scene/view plumbing rather than in
`viewAll` itself.  Obol-backed neutral source records now default to visible
wire state when no display summary is available, qtcad exact-pick fallback
uses a center-correct normalized screen ray, qtcad draw sync preserves retained
source display/revision state when replacing sources, and the command-level
`autoview` path frames `ged_draw_bounds` before falling back to the retained
view autoview implementation.  The qtcad path assertions now normalize leading
slashes so GED and Obol path spellings do not produce false failures.

Verification passed with:
`cmake --build build --target test_qtcad_obol_draw_sync -j2`;
`ctest --test-dir build -R '^test_qtcad_obol_draw_sync$' --timeout 180 --output-on-failure`;
`ctest --test-dir build -R '^(test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_snap|test_qtcad_obol_measure|ged_test_qged_swrast)$' -j4 --timeout 180 --output-on-failure`,
which passed 6/6; `ctest --test-dir build -R '^brlobol_pivot_guard$' --timeout
180 --output-on-failure`; and
`ctest --test-dir build -R '^(brlobol_pivot_guard|ged_test_state_services|ged_test_obol_draw_sync|test_qtcad_obol_draw_sync|test_qtcad_obol_selection_sync|test_qtcad_obol_pick|test_qtcad_obol_snap|test_qtcad_obol_measure|ged_test_qged_swrast)$' -j4 --timeout 180 --output-on-failure`,
which passed 9/9.  System OpenGL remains a follow-up spot check, but the
deterministic Qt+OSMesa path is now the Capability 1 qtcad closeout gate, so
the next implementation phase should move to Capability 2 Obol/libbrlobol
node/store work.

Capability 2 has started with an aggressive Obol-first owner slice.  The
libbrlobol view store now exposes owner-aware feature records, owned lookup,
owned removal, index/normal snapshots, and record iteration.  Local feature
keys include the owning view token/id, so two views can own local features
with the same user-facing name without collision.  GED's Obol bridge now uses
those owned handles for feature publish/remove/readback, computes feature
depth directly from Obol feature records, exports feature records through the
owner-aware iterator, and filters `view-feature`/`view-polygon` scene nodes out
of generic scene traversal so local feature records no longer leak between
views.

The grouped view-object path also moved toward Obol ownership.  When GED owns
the Obol scene, `view obj -g <dbpath> create <name>` creates an Obol overlay
group parent under `_overlays/<name>` and moves database leaves into that
group, rather than preserving a retained BSG overlay parent as the active
owner.  `view obj remove <name>` can now delete that Obol overlay group
directly; the `g2` regression in `ged_test_view_command` is green.

The RT view implementation boundary now has a neutral scene-adapter hook.
`include/rt/view.h` declares `rt_view_context_scene_adapter`, adapter set/get
helpers, and a neutral pick-result append helper.  `src/librt/view_legacy_bsg.c`
dispatches semantic-path pick and render/export consistency through the
adapter before falling back to BSG.  GED-owned views install an Obol-backed
adapter that answers those RT view-context calls from Obol/GED export records
and Obol database-source paths.  This closes the previous
`ged_test_view_command` RT consistency and semantic-pick failures without
adding a libged dependency to librt.

Verification passed with:
`cmake --build build --target libged libqtcad ged_test_view_command
ged_test_obol_draw_sync test_brlobol_view_store -j2`;
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_obol_draw_sync|ged_test_view_command)$' -j3 --timeout 180 --output-on-failure`;
and `git diff --check`.  The focused Capability 2 gate passed 3/3.  The next
Capability 2 work should continue direct Obol owner moves for polygon and
selection stores, annotations/auxiliary records, qray/NIRT and vdraw/local
helper artifacts, and display-stream paths, while keeping the Capability 1
closeout gate and qtcad Qt+OSMesa swrast signal passing.

The next Capability 2 slice moved GED-owned view polygon storage to
libbrlobol.  `BRLObolPolygonStore` now has a model-point update API so callers
can convert screen input once and mutate Obol polygon geometry directly.
`include/rt/view.h` now declares a neutral `rt_view_context_polygon_adapter`
with explicit ref ownership; `src/librt/view_legacy_bsg.c` dispatches
adapter-owned polygon refs through that contract and keeps retained BSG only
as compatibility for unowned contexts.  GED-owned views install an
Obol-backed polygon adapter, so qtcad/qged callers using neutral
`rt_view_polygon_*` and `rt_view_context_polygon_*` APIs can read and mutate
GED-owned polygons from libbrlobol rather than retained BSG nodes.

The GED polygon facade now routes create/find/import/export/update/current,
open/close, fill/fill-color, area/overlap, CSG, snap, user-data, and record
readback through Obol/libbrlobol for owned views.  The retained BSG polygon
implementation remains compiled for no-owned-context compatibility, but it is
not the primary GED-owned polygon backend.

Verification passed with:
`cmake --build build --target libbrlobol librt libged ged_test_view_command test_brlobol_view_store -j2`;
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_view_command|brlobol_pivot_guard)$' -j3 --timeout 180 --output-on-failure`;
and
`ctest --test-dir build -R '^(ged_test_obol_draw_sync|test_qtcad_obol_(pick|snap|selection_sync|overlay_sync))$' -j4 --timeout 180 --output-on-failure`.
`git diff --check` was clean, and the final combined focused run
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_view_command|ged_test_obol_draw_sync|test_qtcad_obol_(pick|snap|selection_sync|overlay_sync)|brlobol_pivot_guard)$' -j4 --timeout 180 --output-on-failure`
passed 8/8.
That left selection stores, annotations/auxiliary records, qray/NIRT and
vdraw/local-helper artifacts, overlay/diagnostic geometry, and display-stream
paths as the next Capability 2 families.

The next Capability 2 slice moved GED-owned view selection storage to
libbrlobol.  `BRLObolSelectionStore` now records a selection kind and
`BRLObolFeatureOwner`, so selected paths and highlighted refs can be isolated
per view owner while preserving the existing path-only convenience helpers for
default selected-path use.  `include/rt/view.h` now declares a neutral
`rt_view_context_selection_adapter`; `src/librt/view_legacy_bsg.c` dispatches
selection availability, count, clear, and pick-result selection updates through
that adapter before falling back to retained BSG storage.

GED-owned views install an Obol-backed selection adapter.  GED draw selection
facades now route selection count/path iteration/clear/add/set/contains
through Obol for owned views, and qtcad selection updates that already call
neutral `rt_view_context_selection_*` APIs now update the Obol selection store
for GED-owned view contexts.  The retained BSG selection implementation
remains compiled for unowned/legacy contexts, but it is no longer the primary
GED-owned selection backend.

Verification passed with:
`cmake --build build --target libbrlobol librt libged ged_test_selection_semantics ged_test_state_services test_brlobol_view_store test_qtcad_obol_selection_sync -j2`;
and
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_selection_semantics|ged_test_state_services|test_qtcad_obol_selection_sync|ged_test_obol_draw_sync|test_qtcad_obol_pick|brlobol_pivot_guard)$' -j4 --timeout 180 --output-on-failure`,
which passed 7/7.  The pivot guard now also requires the selection adapter and
owner/kind selection store tokens.  The next Capability 2 work should move to
overlay/diagnostic geometry, annotation/auxiliary records, qray/NIRT and
vdraw/local-helper artifacts, and display-stream paths.

The next Capability 2 slice added typed Obol feature-overlay metadata without
adding another compatibility shim.  `BRLObolOverlayInfo` now records whether a
feature is an overlay plus role, class, lifecycle, ordering, source path, sort
order, and owner token.  `BRLObolFeatureStore` persists that metadata, exposes
it through summaries/records/readback, and keeps overlay classification
separate from generic scene traversal.

GED-owned feature publishers now stamp the Obol feature store where the old
BSG path registered overlay ownership: Tcl line/arrow/axes features use
`TclOverlay` with per-command post-transparent ordering, Tcl polygon helper
features use `PolygonEdit` with per-tool post-transparent ordering, and model
annotation line features use `UserAnnotation` with persistent model ordering.
Feature-store nodes intentionally keep the `view-feature` scene `recordRole`;
this preserves the owner-scoped feature export path and prevents local view
features from leaking through the generic Obol scene walk.

Verification passed with:
`cmake --build build --target libbrlobol libged ged_test_obol_draw_sync ged_test_view_command test_brlobol_view_store -j2`;
and
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_obol_draw_sync|test_qtcad_obol_overlay_sync|ged_test_tcl_overlay_bsg|ged_test_view_command|brlobol_pivot_guard)$' -j4 --timeout 180 --output-on-failure`,
which passed 6/6.  `ged_test_view_command` caught an initial node-role leak
when model-annotation features were marked as scene overlays; the final
boundary keeps overlay semantics in feature metadata instead.

The next Capability 2 slice moved GED-owned RT feature/edit-preview facade
traffic to Obol feature records.  `include/rt/view.h` now declares a neutral
`rt_view_context_feature_adapter`; `src/librt/view_legacy_bsg.c` stores
per-context feature adapter bindings and dispatches adapter-owned
`rt_view_feature_ref` operations before retained BSG compatibility.  GED-owned
views install the Obol-backed adapter from `src/libged/draw_obol.cpp`, so
qged/qtcad edit-preview callers that already use neutral `rt_view_*` feature
APIs now publish edit-preview overlays, labels, style changes, touch/update,
geometry replacement, clear, remove, and lifecycle events into
`BRLObolFeatureStore`.

libbrlobol now exposes `BRLObolFeatureStore::replaceEditPreviewGeometry`.
That lets the RT feature adapter update preview points/commands without
losing edit-preview callbacks, and edit-preview realization now preserves
`SoBRLEditPreview` node identity through geometry/style mutations instead of
falling back to a generic VLIST node.  The GED Obol sync test now exercises
the public RT feature API on a GED-owned active view and checks typed Obol
overlay metadata, preview geometry records, callback dispatch, label records,
clear/remove, and prefix cleanup.

Verification passed with:
`cmake --build build --target libbrlobol librt libged test_brlobol_view_store ged_test_obol_draw_sync -j2`;
`cmake --build build --target qged_ell qged_revolve qged_extrude qged_bot -j2`;
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_obol_draw_sync|brlobol_pivot_guard)$' -j3 --timeout 240 --output-on-failure`;
and
`ctest --test-dir build -R '^(test_qtcad_obol_edit_preview|test_qtcad_obol_overlay_sync|test_qtcad_obol_draw_sync|ged_test_view_command|ged_test_tcl_overlay_bsg)$' -j3 --timeout 240 --output-on-failure`.
The focused Capability 2 feature-adapter gate passed 8/8.  Remaining
Capability 2 work should move command-result owner callbacks, diagnostic
overlay producers, annotation/auxiliary records, qray/NIRT and
vdraw/local-helper artifacts, and display-stream paths onto typed Obol records
instead of retained BSG registries.

The next Capability 2 slice moved diagnostic line-layer fallback producers
onto a typed GED/Obol path.  `include/ged/draw.h` now exposes
`ged_draw_view_context_diagnostic_line_layer_builder_replace`, with an
Obol-backed attach point in `src/libged/draw_obol.cpp` for GED-owned views.
Successful diagnostic line-layer publishes now stamp
`Diagnostic`/`PerCommand`/`PostTransparent` overlay metadata in
`BRLObolFeatureStore`; the existing retained line-layer replacement remains
only as compatibility for unowned contexts.

The diagnostic fallback call sites in check-overlaps, gqa overlap overlays,
BREP plot, draw edge-use, and vutil NMG edge-use now use the typed diagnostic
facade instead of anonymous generic line-layer replacement.  The GED Obol sync
test adds a `cap2::diagnostic` publish/readback check, and the pivot guard now
requires the diagnostic overlay token path.

Verification passed for the focused slice with:
`cmake --build build --target libged ged_test_obol_draw_sync -j2`;
`ctest --test-dir build -R '^(ged_test_obol_draw_sync|brlobol_pivot_guard)$' -j2 --timeout 240 --output-on-failure`;
`ctest --test-dir build -R '^(test_qtcad_obol_overlay_sync|ged_test_tcl_overlay_bsg|ged_test_view_command)$' -j3 --timeout 240 --output-on-failure`;
and `git diff --check`.
`ged_test_gqa` also passed when run with producer-adjacent tests.
`ged_test_drawing_basic` failed in existing swrast polygon/baseline
comparisons and a polygon boolean `bu_malloc(0)` path; that should be triaged
separately because it does not exercise the diagnostic line-layer producers
changed in this slice.

The `ged_test_drawing_basic` triage fixed the hard polygon CSG failure and
confirmed the remaining failure is the legacy swrast renderer boundary.  The
libbg polygon copier now accepts valid empty polygons, the Obol polygon store
guards empty/no-overlap CSG cases and union into an empty target, GED `zap`
clears Obol-owned view feature/polygon/selection stores, and Obol polygon
records are exported through the neutral draw-record list path.  The test now
completes polygon CSG union/subtraction/intersection command phases; retained
output images are still blank because `dm_draw_objs` renders retained BSG
scene children while source-owner membership has intentionally moved to Obol.

Verification for the triage passed:
`cmake --build build --target libged ged_test_draw ged_test_obol_draw_sync -j2`;
`ctest --test-dir build -R '^(libbrlobol_view_store|ged_test_obol_draw_sync|brlobol_pivot_guard)$' -j3 --timeout 240 --output-on-failure`;
and `git diff --check`.  A current-tree
`ctest --test-dir build -R '^ged_test_drawing_basic$' --timeout 240 --output-on-failure`
still fails only on image comparisons.  The next renderer slice should give
GED/swrast verification an explicit Obol/libbrlobol offscreen or backing-store
render path instead of restoring retained BSG source-owner duplication.

After the interrupted libbrlobol hardening batch was resumed on 2026-07-03, the
focused LoD update crash was reproduced as a custom-action traversal problem:
`SoBRLSourceMeshPickAction` reached `SoLightModel::doAction` even though the
action only enabled the elements required for focused geometry traversal.
Export, measure, snap, and source-mesh pick actions now skip arbitrary
scene-state nodes in their generic node handlers and delegate only to group and
transformation nodes, preserving traversal and model-matrix updates without
invoking unrelated state elements.

The qtcad draw-sync expectation was also updated for representation-specific
database-source instances.  A shaded `ball.s` source and a shared wire `ball.s`
source intentionally coexist, so direct and scoped qtcad source-owner tests now
assert both owners remain while wire `box.s` add/remove/clear operations target
their own owner scope.  The pivot guard now checks shared-geometry `geom`
command-stream traversal, hardened owner string assignment, instance-scoped GED
draw-source controller calls, and qtcad camera sync through the Obol controller
rather than low-level RT view-context getters in `QgCanvasState.h`.

Verification passed with:
`ninja -C build libbrlobol test_brlobol_lod_update_action test_brlobol_prototype ged_test_obol_draw_sync test_qtcad_obol_draw_sync`;
`ctest --test-dir build -R
'^(brlobol_pivot_guard|libbrlobol_prototype|libbrlobol_lod_update_action|ged_test_obol_draw_sync|test_qtcad_obol_draw_sync)$'
-j3 --timeout 240 --output-on-failure`;
and `git diff --check`.
