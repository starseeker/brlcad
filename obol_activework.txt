BRL-CAD Obol Active Work Guide
==============================

Purpose
-------

This is the first file a new or resumed AI session should read before working
on the Obol migration.  It is intentionally current, action-oriented, and
short enough to keep in context.

Use obol_worklog.txt for the longer rationale, historical notes, and detailed
design records consolidated from the former root guidance files:

  fb_plan.txt
  lod_pipeline.txt
  lod_result_wakeup.txt
  modern_drawing.txt
  obol_migration.txt
  obol_prototype.txt
  pop_lod.txt
  review.txt


Working Rules
-------------

* Work on the oboltest branch unless the user redirects.
* Do not rebase oboltest again unless the user explicitly asks.
* Primary BRL-CAD build directory is build/ and uses Ninja.
* If modifying embedded Obol sources, build in obol/build and run install so
  BRL-CAD CMake can detect the staged update.
* The compile/test cycle is slow.  Batch low-risk related changes when root
  causing failures will still be straightforward.
* Keep all Obol-canonical code free of new public BSG/libdm dependencies.
* Preserve user and existing worktree changes.  Do not revert unrelated edits.
* Update this file when the active work queue, percentages, or next-step
  priorities materially change.  Put longer rationale in obol_worklog.txt.


Migration Intent
----------------

BRL-CAD should use Obol as the canonical scene, view, interaction, and
rendering API for interactive 3D applications.

The migration is parallel-first: build the Obol path beside the current
BSG/DM path until it reaches production parity, then remove public libbsg,
libdm, dm_plugins, and BSG/DM-shaped application APIs.  Temporary internal
bridges are acceptable while a phase is incomplete.  No long-lived public BSG
or DM compatibility layer is part of the target design.

libbrlobol is the BRL-CAD-specific Obol extension library.  It should define
BRL-CAD Obol nodes, actions, details, and narrow application services.  It
must not become a generic replacement for BSG's render-item/backend-scene
model.

The first production target is qged/qtcad.  After qged is usable on Obol,
migrate GED/TclCAD/mged, then remove lower-level BSG/DM utility dependencies.


Current Completion Estimates
----------------------------

These percentages measure exit-criteria completion, not effort spent.

Next Push:

* Obol realization coverage matrix: 98 percent.
* libbrlobol database realization: 98 percent.
* Obol diagnostic overlays: 45 percent.
* qtcad visible Obol rendering: 67 percent.
* Guardrails and inventory: 99 percent.
* Low-level cleanup: 99 percent.
* libimgstream migration batch: 57 percent.
* Framebuffer display ownership: 58 percent.

Migration Phases:

* Phase 1, architecture freeze and guardrails: 80 percent.
* Phase 2, productionize libbrlobol: 97 percent.
* Phase 3, migrate qged/qtcad: 67 percent.
* Phase 4, migrate libged, libtclcad, and mged: 52 percent.
* Phase 5, remove lower-level BSG/DM dependencies: 67 percent.
* Phase 6, hard removal: 5 percent.


Decisions That Should Guide New Work
------------------------------------

* Obol cameras, transforms, SoViewport, SoRenderManager, actions, nodes,
  fields, and details are authoritative for scene, view, render, and
  interaction state.
* New drawing code should use Obol directly or use BRL-CAD Obol extensions.
  Do not add new BSG/DM dependencies outside explicit legacy allowlists.
* C and Tcl callers may use opaque bridges backed by Obol; C++ is acceptable
  for code directly interacting with the drawing API.
* bg_vlist and bg_line_layer are low-level geometry/debug containers, not
  scene APIs.  Application-visible drawing state should be Obol nodes.
* libbg should gain normal implementation units for neutral plot/debug
  helpers.  plot3.h is unlikely to be retired.
* Keep the bg_line_layer-to-plot3 diagnostic path.  It is useful for simple
  debugging output and should not require Obol execution.
* Scene text, labels, HUD text, and annotations should use Obol 2D/3D text.
  Flattened text geometry should be generated from an Obol-backed path when
  export requires it.
* MGED should target a proper Tcl/Tk Obol widget for performant native
  rendering when system OpenGL/Vulkan/etc. are available.  OSMesa/offscreen
  rendering plus image presentation is a slower fallback, not the target.
* Obol owns image display, placement, layering, texture upload, readback, and
  rendering backend selection.
* libimgstream owns live mutable image streams, async image producers,
  dirty-update notification, transport, fan-out, fbserv compatibility, and
  fb_* wrappers during migration.
* libicv owns static image containers, codecs, image file I/O, image
  transforms, and neutral colormap/lookup-table operations.


Recently Completed Slices
-------------------------

* The `ged_test_mged_bsg` draw test still links libbsg directly for retained
  BSG render request/batch coverage, but its utility view-state checks no
  longer use direct BSG scene or faceplate state APIs.  Scene attachment now
  goes through `rt_view_scene_attached_bsg`, and center-dot state read/write
  goes through `rt_view_center_dot_state_from_bsg` and
  `rt_view_center_dot_state_set_bsg` with a neutral `rt_view_other_state`
  record.  The pivot guard now rejects direct `bsg/util.h`,
  `bsg/view_state.h`, `bsg_view_scene_attached`,
  `bsg_view_center_dot_get/set`, or `struct bsg_other_state` use in this test
  while preserving its intentional BSG render API coverage.

* The `ged_test_bsg_quad_stability` and `ged_test_rtwizard_bsg` draw tests no
  longer link libbsg directly.  Both tests still validate retained GED/DM
  BSG-backed scene behavior, but their scene-anchor setup, attached checks,
  and shared-scene checks now route through `rt/view_legacy_bsg.h` wrappers
  (`rt_view_scene_anchor_ensure_bsg`, `rt_view_scene_attached_bsg`, and
  `rt_view_scene_shared_bsg`) instead of direct BSG scene utility headers and
  calls.  The `rt_view_legacy_bsg` adapter test covers the new scene-anchor
  helpers, and the pivot guard rejects direct BSG scene-anchor source use or a
  direct libbsg relink for these two tests.  Generated link lines may still
  carry libbsg transitively through retained `libged`/`libdm`/`librt` owners
  until those paths migrate.

* The `ged_test_mged_shaded_mode_bsg` draw test no longer links libbsg
  directly.  The test still exercises retained GED/DM shaded-mode rendering,
  lighting, zbuffer, zclip, screengrab, and semantic draw-record behavior, but
  it no longer includes the stale `bsg/defines.h` header and does not require
  a direct libbsg target dependency.  Its retained view setup continues to use
  `rt/view_legacy_bsg.h` wrappers, and the pivot guard now rejects a direct
  libbsg relink or direct BSG defines use for this shaded-mode test.  The
  generated link can still carry libbsg transitively through retained
  `libged`/`libdm` owners until those paths migrate.

* The `ged_test_view_independent` draw test no longer links libbsg directly.
  The test still exercises the retained BSG-backed independent-view scope
  behavior through GED/RT bridge types, but its independent-scope null
  inspection now uses the new `rt_view_independent_scope_is_null_bsg` wrapper
  instead of direct `bsg_scene_ref_is_null` utility access.  The
  `rt_view_legacy_bsg` adapter test covers null, live, and destroyed
  independent-scope cases, and the pivot guard rejects both direct BSG utility
  source use and a direct libbsg relink for the independent-view test.
  Generated link lines still carry libbsg transitively through retained
  `libged`/`librt` paths until those phase owners migrate.

* The `ged_test_view_command` draw test still links libbsg directly for its
  retained backend-scene render/export coverage, but its semantic utility
  checks no longer reach through direct BSG snap, measure, or polygon record
  APIs.  `rt/view_legacy_bsg.h` now exposes
  `rt_view_snap_result_source_path_bsg` beside the existing opaque snap-result
  helpers, and `view_command.cpp` now uses RT wrappers for snap candidates,
  measurement candidates, and polygon record lookup.  The `rt_view_legacy_bsg`
  adapter test covers the snap source-path accessor, and the pivot guard now
  rejects direct BSG semantic utility headers/calls and the stale
  `bsg/util.h` include from returning to the view-command test while
  preserving its intentional structural BSG coverage.

* The `ged_test_measure_semantics` draw test no longer links libbsg
  directly.  `rt/view.h` now owns a neutral `rt_view_measure_result` value
  record, and `rt/view_legacy_bsg.h` exposes a retained BSG-backed
  `rt_view_measure_candidates_bsg` adapter that converts BSG measurement
  results into that neutral record.  `measure_semantics.cpp` now routes view
  lifecycle, dimension setup, measurement candidate collection, and result
  inspection through the RT surface instead of direct BSG measure headers and
  APIs.  The `rt_view_legacy_bsg` adapter test covers the new conversion, and
  the pivot guard rejects direct BSG measure source use or a direct libbsg
  relink for the GED measure semantics target.  The generated link still
  carries libbsg transitively through retained `librt` until that lower-level
  owner migrates.

* The `ged_test_snap_semantics` draw test no longer links libbsg directly.
  `rt/view_legacy_bsg.h` now exposes opaque snap-result helpers for retained
  BSG-backed views, and `snap_semantics.cpp` routes view lifecycle, grid
  state, snap-source setup, candidate collection, snap-result inspection, and
  2D snap calls through that RT adapter surface instead of direct BSG
  snap/grid headers and APIs.  The `rt_view_legacy_bsg` adapter test covers
  the new snap-result helpers, and the pivot guard now rejects direct BSG snap
  source use or a direct libbsg relink for the GED snap semantics target.
  The generated link still carries libbsg transitively through retained
  `librt` until that lower-level owner migrates.

* Stale build-only direct libbsg links have been removed from selected
  converter and simulation targets whose source trees do not use BSG APIs.
  The Unigraphics `ug-g`, Cubit `g-sat`, and Creo `creo-brl` shim/link
  package rows now rely on their neutral RT/WDB/BG/NMG dependencies instead
  of directly listing libbsg.  The `ged-simulate` plugin link row now omits
  libbsg as well.  Generated link commands still pull libbsg transitively
  through retained libraries such as librt, libwdb, and libged until those
  phase owners migrate.  The pivot guard records this dependency-boundary
  cleanup and rejects both direct libbsg relinks in those CMake files and
  direct BSG API use in the corresponding source trees.

* The `ged_test_qged_swrast` qtcad/QgSW integration test no longer links
  libbsg directly.  The test already routes view binding and scene checks
  through `qtcad/QgLegacyView.h` helpers; it still links the retained
  libqtcad/libged/libdm path and therefore still sees libbsg transitively
  until those owner libraries migrate.  The disabled DM backend benchmark
  link template was adjusted the same way, and the pivot guard now rejects
  direct libbsg relinks for this qtcad bridge-test path.

* The `ged_test_selection_semantics` draw test no longer links libbsg
  directly.  Its source still validates the transitional retained view path,
  but pick/result/selection access is now through `rt/view_legacy_bsg.h`
  helper APIs rather than direct BSG pick or selection headers.  The pivot
  guard now rejects a direct libbsg relink while preserving the existing
  source-level adapter-surface checks.

* The libdm direct-vlist backend path now uses neutral BG vlist spelling.
  `src/libdm/include/calltable.h`, `src/libdm/include/private.h`,
  `dm-generic.c`, the null/txt/X/plot/postscript backends, and the shared GL
  vlist path now use `bg_vlist` callback signatures and `BG_VLIST_*` command
  names instead of `bsg_vlist`/`BSG_VLIST_*`.  The plot backend routes
  floating uplot export through `rt_vlist_to_uplot`; the retained render-item
  and LoD BSG backend work is unchanged.  The pivot guard and
  `dm_backend_draw_item` test now reject BSG vlist spelling from returning to
  these direct-vlist files.

* The low-level utility vlist cleanup has been extended through
  `src/util/plot3-dm.c`.  The utility now stores imported plot data as
  `struct bg_vlblock`, iterates `bg_vlist`, uses `BG_VLIST_*` command names,
  and frees plot blocks with `bg_vlblock_free` instead of source-level BSG
  vlist aliases.  Direct device drawing is routed through the private
  `dm_draw_device_bg_vlist` wrapper, which is now BG-typed through libdm's
  direct-vlist backend hooks.  `plot3-dm` now links directly to `librt`/`libbg`
  instead of `libbsg`, and the pivot guard includes `plot3-dm.c` in the
  utility vlist neutralization check.

* The neutral GED view-context accessor migration has been extended through
  the remaining MGED command-dispatch active-view path in `cmd.cpp`.  GED
  wrapper commands, DM/screengrab handoff, command-window and winset focus
  changes, postscript attach/release, `blast`/`draw`, simulate view-argument
  injection, and `view` staging now use `ged_view_active_ctx` and
  `ged_view_active_ctx_set` instead of reaching through `s->gedp->ged_gvp`.
  Retained display-manager, view lifecycle, view cache, and view-math
  operations remain routed through `rt/view_legacy_bsg.h` adapters.  The
  pivot guard now requires the neutral accessor tokens in `cmd.cpp` and
  rejects direct GED view-storage reach-through from returning.  Remaining
  direct non-test, non-adapter libged/MGED view-storage access is now confined
  to core GED ownership/accessor implementation in `ged.cpp`.

* The neutral GED view-context accessor migration has been extended through
  the remaining `chgview.c` active-view command paths.  `edit_com`,
  `cmd_autoview`, and `mged_zoom` now use `ged_view_active_ctx_set` and
  `ged_view_active_ctx` for temporary current-view selection, final active
  view restore, and zoom-refresh LoD policy inspection.  Retained view-scale,
  autoview, refresh, knob, and LoD adapters remain routed through
  `rt/view_legacy_bsg.h`.  The pivot guard now requires the neutral accessor
  tokens there and rejects direct GED view-storage reach-through from
  returning.  Remaining MGED direct active-view reach-through is now confined
  to `cmd.cpp`.

* The neutral GED view-context accessor migration has been extended through
  a first MGED helper batch.  `overlay.c`, `plot.c`, `color_scheme.c`,
  `rtif.c`, `dm-generic.c`, `setup.c`, and `attach.c` now use
  `ged_view_active_ctx`, `ged_view_active_ctx_set`, and/or
  `ged_view_set_ctx` for active-view handoff, active-view save/restore,
  visible-record enumeration, DM command forwarding, startup view
  registration, and copied-DM view registration.  Retained display-manager,
  update-callback, LoD, and view lifecycle operations remain routed through
  `rt/view_legacy_bsg.h` adapters.  The pivot guard now requires the neutral
  accessor tokens in this MGED batch and rejects direct GED view-storage
  reach-through from returning there.  Remaining MGED direct active-view
  reach-through is concentrated in `cmd.cpp` and `chgview.c`.

* The neutral GED view-context accessor migration has been extended through
  the libged display-manager command/binding path.  `dm.c` now uses
  `ged_view_active_ctx`, `ged_view_set_views_ctx`, `ged_view_find_ctx`, and
  `ged_view_set_ctx` for current-view lookup, DM listing/name collision
  checks, target-view lookup, and transitional view registration.  The
  retained DM pointer, fallback dimensions, view-scale, and view creation
  operations remain routed through `rt/view_legacy_bsg.h` adapters.  The
  pivot guard now requires the neutral GED accessor tokens there and rejects
  direct GED view-storage reach-through from returning.  Remaining direct
  non-test, non-adapter libged view-storage access is now confined to core GED
  ownership/accessor implementation in `ged.cpp`.

* The neutral GED view-context accessor migration has been extended through
  the libged view command and view-object command dispatch paths.  `view.c`
  now uses `ged_view_active_ctx`, `ged_view_active_ctx_set`,
  `ged_view_find_ctx`, and `ged_view_set_views_ctx` for temporary current-view
  swaps, named-view lookup, view listing, and default current-view selection.
  `view/objs.cpp` now uses the active-view accessors before ensuring a
  view-specific scene root.  The pivot guard now requires those neutral
  accessor tokens there and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through
  `ged_util.cpp`, the shared libged utility helper layer.  Rotation/translation
  default coordinate reads, visible path listing, uplot feature publishing,
  RT eye-model and write-script snapshots, and visible-record export helpers
  now borrow the current view with `ged_view_active_ctx` before calling
  retained RT view or private GED draw adapters.  The pivot guard now requires
  the neutral accessor token there and rejects direct GED view-storage
  reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  draw/view restore helper batch.  `draw/loadview.cpp`, `draw/preview.cpp`,
  `view/autoview2.cpp`, `draw/bigE.c`, and `draw/draw.c` now borrow the
  current view with `ged_view_active_ctx`, and `autoview -V` uses
  `ged_view_find_ctx`, before invoking retained RT view, LoD, autoview, or
  private GED draw adapters.  The pivot guard now requires those neutral
  accessor tokens there and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through a
  residual command/helper batch covering database-open view unit setup,
  mirror post-draw refresh, joint overlay publishing, rtcheck overlap glyphs,
  and nirt/qray view snapshots.  `open.cpp`, `mirror.c`, `joint.c`,
  `rtcheck2.cpp`, and `nirt.cpp` now borrow the current view with
  `ged_view_active_ctx` before calling retained RT view or private GED draw
  adapters.  The pivot guard now requires the neutral accessor token in those
  files and rejects direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  path/query/snapshot helper batch.  `who.c`, `garbage_collect.cpp`, `how.c`,
  `brep/tikz.cpp`, and `check.c` now borrow the current view with
  `ged_view_active_ctx` before visible-path enumeration, garbage-collection
  draw-list inspection, display/listing lookups, TikZ AET output, and
  view-derived check setup.  The pivot guard now requires the neutral accessor
  token in those files and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through
  the `labelvert` and `labelface` overlay-label command paths.  `illum.c` and
  `nmg.c` now borrow the current view with `ged_view_active_ctx` before
  visible-record enumeration, retained label feature replacement, and display
  manager repaint notification.  The pivot guard now requires the neutral
  accessor token there and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through a
  residual BoT command batch.  BoT face plotting, BoT check edge overlays,
  `bot_fuse` open-edge overlays, and `bot_dump` visible-record export now
  borrow the current view with `ged_view_active_ctx` before calling retained
  GED draw feature, line, and visible-record helpers.  The pivot guard now
  requires the neutral accessor token in those BoT command files and rejects
  direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through
  the libged `select`/`rselect` command implementation.  Bot point selection,
  visible-record selection, partial selection, and interactive-rectangle
  selection now borrow the current view with `ged_view_active_ctx` before
  calling retained model-to-view, visible-record enumeration, and faceplate
  interactive-rectangle adapters.  The pivot guard now requires the neutral
  accessor token there and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through
  `vutil.c`, the shared libged view utility helper layer.  `_ged_do_rot`,
  `_ged_do_slew`, `_ged_do_tra`, and `nmg_plot_eu` now borrow the current view
  with `ged_view_active_ctx` before calling retained RT rotate-about, matrix,
  center, update, inverse-size, and diagnostic line-layer fallback helpers.
  The pivot guard now requires the neutral accessor token there and rejects
  direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  view-math/query command batch.  BREP pick, BoT pick, `solids_on_ray`,
  polygon clipping/fill helpers, metaball point selection/addition, pipe
  point selection/addition, `move_arb_edge`, and BoT nearest edge/point
  helpers now borrow the current view with `ged_view_active_ctx` before
  calling retained RT view-matrix, center, rotation, plane, and scale
  adapters.  The pivot guard now requires the neutral accessor token in those
  files and rejects direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  diagnostic overlay command batch.  `gdiff`, `gqa`, `check overlaps`, and
  BREP plot publishing now borrow the current view with `ged_view_active_ctx`
  before falling back to retained GED draw feature, line-layer, or indexed
  face-set helpers.  The neutral diagnostic line-layer publishing path remains
  first, and the pivot guard now requires the neutral accessor token in those
  files so direct GED view-storage reach-through cannot return.

* The neutral GED view-context accessor migration has been extended through a
  view faceplate command batch.  `view faceplate`, `center_dot`, `fb`,
  `scale`, `params`, `grid`, `model_axes`, `view_axes`, and `irect` now
  borrow the current view with `ged_view_active_ctx` before calling retained
  RT faceplate/grid/axes/interactive-rectangle adapters or HUD sync helpers.
  The pivot guard now requires the neutral accessor token in those faceplate
  command files and rejects direct GED view-storage reach-through from
  returning.

* The neutral GED view-context accessor migration has been extended through a
  small state/overlay command batch.  `adc`, `rotate_about`, `lod`, and
  `lint` now borrow the current view with `ged_view_active_ctx` before calling
  retained ADC, rotate-about, LOD, or GED draw overlay helpers.  The pivot
  guard now requires the neutral accessor token in those files and rejects
  direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  view-interaction state batch.  `grid`, `rect`, `view snap`, `view knob`, and
  `view data_lines` now borrow the current view with `ged_view_active_ctx`
  before calling retained RT view-state adapters or GED draw helpers.  Local
  helpers and data-line subcommand callbacks now receive the borrowed view
  explicitly instead of rereading GED view storage.  The pivot guard now
  requires the neutral accessor token in those files and rejects direct GED
  view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through an
  output/snapshot libged command batch.  `png`, `ps`/`postscript`, `plot`,
  `savekey`, `saveview`, and `rtwizard` now borrow the current view with
  `ged_view_active_ctx` before visible-record enumeration and projection/view
  snapshot calls.  Their retained RT and GED draw adapters remain in place,
  but helper routines now receive the borrowed view explicitly where they need
  it.  The pivot guard now requires the neutral accessor token in those files
  and rejects direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  second scalar view-state libged batch.  `view2model`, `view2model_lu`,
  `view2grid_lu`, `view2model_vec`, `m2v_point`, `v2m_point`, `pmodel2view`,
  `rot_point`, `scale`, `rmat`, `pmat`, `perspective`, `setview`, and the
  compact `view` subcommands for `aet`, `viewdir`, `ypr`, `qvrot`, `quat`,
  `size`, `center`, `eye`, `lookat`, and `align` now borrow the current view
  with `ged_view_active_ctx` before calling retained RT view adapters.  The
  pivot guard now requires the neutral accessor token in those files and
  rejects direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  framebuffer/display-facing libged command batch.  `fbclear`, `fb2pix`,
  `pix2fb`, `png2fb`, `overlay`, `ert`, and `rt` now borrow the current view
  with `ged_view_active_ctx` before using the retained RT framebuffer,
  display-manager, scale, perspective, and framebuffer-mode adapters.  The
  commands still keep their transitional RT/DM behavior, but no longer read
  GED's retained `ged_gvp` storage directly.  The pivot guard now requires the
  neutral accessor token in those files and rejects direct GED view-storage
  reach-through from returning.

* The neutral GED view-context accessor migration has been extended through a
  scalar active-view libged command batch.  `arot`, `zoom`, `orient`,
  `keypoint`, `isize`, `get_eyemodel`, `eye_pos`, `model2view`,
  `model2view_lu`, `grid2view_lu`, `model2grid_lu`, and `grid2model_lu` now
  borrow the current view with `ged_view_active_ctx` before calling retained
  RT view adapters, instead of reading `gedp->ged_gvp` directly.  The pivot
  guard now requires that neutral accessor token in those files and rejects
  direct GED view-storage reach-through from returning.

* The neutral GED view-context accessor migration has started moving libged
  command paths and gsh off direct GED view-storage reach-through.  The
  `zap`, `draw`, `erase`, `who`, `who solids`, `screengrab`, native-selection
  sync, and geometry-shell display-hash paths now use `ged_view_active_ctx`,
  `ged_view_set_views_ctx`, and/or `ged_view_find_ctx` instead of reading
  `gedp->ged_gvp`, `gedp->ged_views`, or calling the retained view-set RT
  wrappers directly.  The `who` command files now treat the view as an opaque
  GED draw context and no longer include `rt/view_legacy_bsg.h`.  The pivot
  guard now requires the neutral GED accessor tokens in this migrated
  libged/gsh batch and rejects the old direct storage routes from returning.

* The remaining TclCAD command-table direct GED view-storage reach-throughs
  have been migrated.  `src/libtclcad/commands.c` now uses
  `ged_view_find_ctx` for named view lookup, `ged_view_set_views_ctx` for
  view-set iteration, `ged_view_active_ctx`/`ged_view_active_ctx_set` for
  current-view reads and command dispatch, and `ged_view_set_ctx` for the
  retained view initialization/registration path.  This completes the current
  TclCAD file batch that was directly reaching into `gedp->ged_gvp`,
  `gedp->ged_views`, or `current_top->to_gedp->ged_views`.  The pivot guard
  now rejects those direct storage routes from returning to the migrated
  TclCAD files.

* The TclCAD mouse command layer has been moved off direct GED view-storage
  access.  `src/libtclcad/mouse.c` now uses `ged_view_find_ctx` for named view
  lookup and `ged_view_active_ctx_set` before dispatching commands that must
  operate on the resolved view.  Mouse snap and view-math helper calls now use
  the already-resolved borrowed view where they previously reread
  `gedp->ged_gvp`.  The pivot guard now rejects direct `ged_gvp`,
  `ged_views`, `rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg`
  reach-through from returning to `mouse.c`.

* The TclCAD framebuffer and polygon command paths have been moved off direct
  GED view-storage access.  `src/libtclcad/fb.c` now uses
  `ged_view_find_ctx` for framebuffer mode/listener view lookup, and
  `src/libtclcad/polygons.c` now uses `ged_view_find_ctx` for polygon command
  view lookup plus `ged_view_active_ctx_set` for the polygon-mode commands
  that intentionally make the command view active.  Polygon snap preparation
  now operates on the already-resolved borrowed view instead of rereading
  `gedp->ged_gvp`.  The pivot guard now rejects direct `ged_gvp`,
  `ged_views`, `rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg`
  reach-through from returning to those files while preserving their retained
  transitional RT view-operation calls.

* The neutral GED view-context accessor migration has been extended across the
  smaller TclCAD view-object command wrappers.  Data-line, data-label,
  faceplate, data/model/view axes, and data-arrow TclCAD commands now find
  borrowed views with `ged_view_find_ctx`, and the data-line command's
  temporary current-view swaps now use `ged_view_active_ctx` and
  `ged_view_active_ctx_set` instead of directly reading or assigning
  `gedp->ged_gvp`.  The pivot guard now rejects direct `ged_gvp`,
  `ged_views`, `rt_view_set_views_bsg`, or `rt_view_set_find_view_bsg`
  reach-through from returning to this migrated view-command batch.

* The neutral GED view-context accessor boundary has been extended to cover
  common borrowed view-set operations.  `include/ged/defines.h` now exports
  `ged_view_set_views_ctx` and `ged_view_find_ctx`, and `src/libged/ged.cpp`
  localizes the retained calls to `rt_view_set_views_bsg` and
  `rt_view_set_find_view_bsg` behind those APIs.  TclCAD wrapper,
  refresh/autoview, and edit-redraw callers now use the neutral GED accessors
  for view lookup, view-set iteration, and current-view assignment instead of
  reaching directly into `gedp->ged_views`, `top->to_gedp->ged_views`, or
  `gedp->ged_gvp`.  The pivot guard now requires those neutral GED accessor
  tokens in the migrated TclCAD batch and rejects the old direct
  view-set/current-view route from returning there.

* Neutral GED view-context accessors have been introduced as the next
  preparatory step toward hiding GED's retained BSG view storage.  Public
  `GED_VIEW_NULL` is now a `void *` null token, `ged_view_active_ctx`,
  `ged_view_active_ctx_set`, and `ged_view_set_ctx` expose the current
  borrowed view and view-set context opaquely, and the public `GED_CHECK_VIEW`
  macro no longer reaches directly into `gedp->ged_gvp`.  qtcad's private
  legacy bridge now uses these GED accessors for active-view get/set and
  view-set add/remove/attach instead of touching `struct ged` view fields
  directly.  The pivot guard now requires those accessor tokens and rejects
  direct `ged_gvp`/`ged_views` field access from returning to the qtcad legacy
  bridge.

* The public GED draw-scene header has been narrowed at the view-context
  boundary.  `include/ged/bsg_ged_draw.h` no longer forward declares or names
  `struct bsg_view`; draw transactions, group records, view-record iteration,
  path/list queries, view-scope clearing, highlighted-selection mirroring, and
  primitive-wireframe publishing now expose borrowed view contexts as opaque
  `void *` handles.  The retained casts back to `struct bsg_view *` are
  localized in the GED draw implementation files, and qtcad's private legacy
  bridge performs the only C++ cast needed when translating a GED transaction
  into its private wrapper.  The pivot guard now rejects public BSG view
  spelling from returning to the installed GED draw-scene header.

* The public GED draw-scene header has been narrowed at the draw-mode and
  draw-appearance boundary.  `include/ged/bsg_ged_draw.h` no longer includes
  `bsg/draw_intent.h`, exposes `bsg_draw_mode`, or names
  `bsg_appearance_settings`; public shape/group records and
  `ged_draw_default_mode` now use integer draw-mode values, and
  `ged_draw_transaction::appearance` is an opaque borrowed backend appearance
  context.  The retained BSG appearance cast is confined to
  `src/libged/bsg_ged_draw_transactions.c`, with qtcad's private legacy bridge
  explicitly including `bsg/draw_intent.h` for its retained draw-mode asserts
  and appearance storage.

* The public DM backend render-item dispatch contract has been narrowed.
  `include/dm.h` no longer forward declares or names `bsg_render_item` or
  `bsg_render_request`; `dm_backend_ops::draw_item`,
  `dm_backend_ops::invalidate_item`, `dm_backend_draw_item`, and
  `dm_backend_invalidate_item` now use opaque `const void *render_item_ctx`
  handles.  The retained BSG render-item validation and fallback invalidation
  logic is localized in `src/libdm/dm-generic.c`, while GL backend dispatch
  uses private adapter callbacks in `src/libdm/dm-gl_lod.cpp` to cast back to
  the retained BSG item before calling the existing GL renderer.  The pivot
  guard now rejects public BSG render-item/request spelling from returning to
  `include/dm.h`.

* The public RT edit legacy adapter has been narrowed.  The installed
  `include/rt/edit_legacy_bsg.h` header no longer forward declares or names
  `struct bsg_view`; its transitional edit snapshot, create/reinit, and knob
  command wrappers now take opaque `const void *view_ctx` values.  The retained
  BSG cast remains confined to `src/librt/edit_legacy_bsg.cpp`, which already
  routes view reads through `rt/view_legacy_bsg.h`.  The pivot guard now rejects
  public BSG view exposure from returning to the installed edit legacy header.

* The public RT sketch legacy adapter has been narrowed.  The installed
  `include/rt/primitives/sketch_legacy_bsg.h` header no longer includes
  `bsg/polygon.h`, forward declares `struct bsg_view`, or exposes
  `bsg_polygon_ref`; sketch import/export now use neutral `rt_view_polygon_ref`
  handles and opaque `void *` view contexts.  The retained BSG view cast,
  polygon-ref conversion, and polygon record reads are confined to
  `src/librt/primitives/sketch/polygons_legacy_bsg.c`.  The RT view bridge,
  GED draw-view polygon sketch bridge, and `rt_bv_poly_sketch` roundtrip test
  now call the sketch adapter through the neutral handle boundary, and the
  pivot guard prevents public BSG polygon/view spelling from returning to the
  installed sketch adapter header.

* libged `view obj ... line`, `view obj ... label`, `view obj ... axes`,
  Tcl data-line, gobject overlay create, and generic view-object feature
  draw/color/arrow/update/removal command paths now route user annotation,
  data axes, Tcl line/label, and gobject feature existence, creation, overlay
  ownership, style, point-copy, point-replace, axes-state get/replace,
  realize/update, removal, `view vZ` feature-depth reads, and
  `view selections` count queries, faceplate HUD sync, and `view snap`
  grid/endpoint first-candidate queries through the private GED draw view
  adapter instead of calling BSG feature, selection, snap-result, geometry,
  HUD, or overlay APIs directly from the command files.  `view obj ... polygon`
  creation, edit/update, area/overlap, fill/fill-color, sketch import/export,
  and CSG command paths now also use neutral polygon handles and records from
  that adapter, leaving the retained BSG polygon refs, data reads, and
  sketch-legacy conversion calls inside the bridge.  Joint mesh,
  BoT fuse/check
  and face-plot, lint plot, gdiff, and preview eye-path diagnostic line
  overlays now also publish/remove named line features through the same
  adapter boundary, and `labelvert`/`labelface` now replace named label
  features through neutral label records owned by that adapter.  Diagnostic
  line-layer fallbacks in check-overlaps, gqa, draw edge-use, vutil NMG edge,
  and brep plot publishing also route their retained BSG fallback through the
  adapter while the primary handler remains `bg_line_layer`-based.  Uplot
  feature publishing now builds neutral named line-layer records with BG line
  commands and routes replacement through the same adapter.  The pivot guard
  now prevents those direct feature-overlay calls from returning to
  `src/libged/view/view.c`, `src/libged/view/lines.c`,
  `src/libged/view/labels.c`, `src/libged/view/axes.c`,
  `src/libged/view/data_lines.c`, and `src/libged/view/gobjs.cpp`, and
  prevents direct BSG feature API use from returning to
  `src/libged/view/objs.cpp` or the migrated diagnostic line-overlay
  producers, including the migrated label, snap-result, and line-layer
  fallback producers.  The libged `adc` command now also operates on neutral
  `rt_view_adc_state` records and RT ADC math helpers, and the standalone
  libged `grid` command and `view faceplate grid` command now use neutral
  `rt_view_grid_state` records.  The libged faceplate center-dot, scale, and
  params commands now use neutral RT scalar faceplate records, with the
  temporary BSG faceplate ADC/grid/scalar get/set conversion confined to the
  RT legacy view adapter.
  Qray stale-feature cleanup in `src/libged/nirt/nirt.cpp` and rtcheck
  prefix cleanup in `src/libged/rtcheck/rtcheck2.cpp` now route through the
  same private adapter.  BRep indexed face-set publishing in
  `src/libged/brep/plot.cpp` now also uses the adapter boundary.  Evaluated
  wireframe line-set construction in `src/libged/draw/bigE.c` and
  `src/libged/wireframe_eval.c` now uses neutral BG line-command tokens
  instead of including BSG geometry just for move/draw commands.  Vdraw
  overlay send now uses neutral BG line-command tokens and GED-private
  overlay geometry kind values, leaving the BSG mapping inside the overlay
  bridge.  Mesh LoD indexed-face geometry updates in `src/libged/draw.cpp`
  now route through a GED draw bridge helper, and the raw
  `ged_draw_scene_ref_geometry_ref` escape hatch has been retired.  Broader
  non-feature edit paths and lower-level BSG/geometry bridge cleanup remain
  follow-up cleanup candidates.
* qtcad migrated-content readback, GL/SW capture, navigation, selection,
  snap, measurement, and overlay helpers can use Obol without initializing
  legacy DM for migrated content.  qtcad Obol camera sync, snap tolerance,
  input scaling, sketch transform, qtcad sketch/measurement screen-to-view
  conversion, and selection ray/rectangle view snapshots now use the RT legacy
  view adapter boundary instead of direct BSG view reads.
  qtcad framebuffer-mode toggle/readback now uses the same adapter boundary
  rather than calling BSG view-state helpers directly.
  qtcad view-control toolbar construction now receives `QgSession` instead of
  a raw GED pointer, and its framebuffer mode toggle/status path reaches the
  active view through the session-owned transitional accessor.
  qged active-view writes now use the session-owned setter, and the main-window
  public interface no longer exposes a BSG-returning `CurrentView()` method.
  qtcad quad-view construction now receives `QgSession`; its startup, quad
  layout, and temporary per-view draw propagation active-view swaps use the
  session-owned read/write accessors instead of direct GED active-view writes.
  The unused BSG-returning `QgQuadView::view(int)` public accessor has been
  removed, leaving the quad-view header free of raw GED/BSG view exposure.
  qged command-time unit conversion and palette element dimension refresh now
  use the session active-view accessor rather than reading the current display
  widget's staged view pointer directly.
  qged draw-observer transaction scoping now uses the qtcad Obol view-sync
  helper rather than comparing display and transaction BSG view pointers in app
  code.
  qtcad 2D measurement view-to-model conversion, quad-view source unit/size
  copies, and SW/GL fallback framebuffer-size, matrix-load, display-image
  readback, DM include, and fallback framebuffer/DM lifecycle calls now follow
  the same adapter boundary while legacy view state writes remain
  transitional.  The retained qtcad DM/fb fallback helper declarations have
  also moved out of the installed `QgLegacyView.h` API into the private
  `src/libqtcad/QgLegacyViewDm.h` bridge header, leaving the public
  legacy-view header focused on neutral transitional view operations.
  `QgCanvasBase` and `QgView` now expose a neutral
  `legacyBackendInitialized()` readiness query instead of raw `struct dm *` /
  `struct fb *` accessors, and `QgView` no longer accepts a retained
  framebuffer pointer in its public constructor.  `QgGL` and `QgSW` public
  headers now also expose `legacyBackendInitialized()` without raw
  display-manager or framebuffer getters; tests that only checked fallback
  readiness use that neutral query, and the retained raw-DM benchmark source
  now uses the private qtcad DM bridge for retained display-manager lookup,
  view binding, background restore, and draw/end timing.  The remaining
  framebuffer-backed `QgGL`/`QgSW` construction path used by the qtgl/swrast
  libdm framebuffer windows now routes through the private
  `QgCanvasBridgeFactory` declarations in `src/libqtcad/QgLegacyViewDm.h`;
  the installed concrete canvas headers no longer forward-declare `struct fb`
  or publish retained framebuffer constructor parameters.  The standalone
  qtgl/swrast Qt window headers now use the opaque qtcad `qg_legacy_fb`
  framebuffer handle and forward-declared canvas classes instead of including
  `dm.h` or concrete qtcad canvas headers; retained display-manager details
  stay in the private implementation/bridge path.  The private qtcad retained-DM
  bridge now exposes distinct opaque `struct qg_legacy_dm` and
  `struct qg_legacy_fb` handle types instead of `struct dm *` /
  `struct fb *`, keeping the retained casts inside `QgLegacyView.cpp`; the
  qtgl/swrast framebuffer wrappers convert raw framebuffer pointers through
  the same named bridge helper and qtgl now uses the same bridge open/close
  helpers as swrast.  The dead
  `setDisplayManagerSet(struct bu_ptbl *)` public hook and its private
  display-manager-table insert helper have also been retired, leaving the
  public canvas surface free of the retained shared-DM-table vocabulary.
  The explicit BSG cast adapter `QgLegacyViewBsg.h` has also moved out of the
  installed qtcad headers into `src/libqtcad`, leaving it available only to
  the bridge implementation, libdm plugin fallback setup, and remaining
  legacy-facing tests.  The normal qtcad Obol workflow tests for draw sync,
  selection sync, overlay sync, pick, measure, export, real-model, snap, and
  faceplate-sync workflows now set active GED view, transaction view scope,
  retained draw appearance, snap query state, and faceplate state through the
  opaque qtcad legacy-view helper surface instead of including
  `QgLegacyViewBsg.h`, including
  `rt/view_legacy_bsg.h`, or converting to `bsg_view *` directly.
  `ged_test_qged_swrast` now also binds its QgSW view, view-set registration,
  unit conversion, and scene-attached assertion through qtcad legacy-view
  helpers instead of reaching through the private BSG cast adapter.
  The standalone qtgl/swrast framebuffer wrappers now use qtcad legacy-view
  helpers for framebuffer mode, dimensions, and swrast attach setup instead of
  including `rt/view_legacy_bsg.h` or converting the canvas view to a raw BSG
  pointer themselves.
  The qtcad legacy-view draw bridge now owns migrated workflow test
  transaction construction, appearance setup, observer registration,
  apply/result lifecycle, and the redraw/draw/erase convenience helper
  forwarding path, leaving raw retained GED transaction make/apply/result
  calls concentrated in `QgLegacyView.cpp` wrapper functions.  The installed
  qtcad header now uses qtcad-owned draw transaction/result records instead of
  typedefing the retained GED transaction structs; observer callbacks are
  bridged through `QgLegacyView.cpp`.
  qtcad edit-preview feature helpers now also route through the RT legacy view
  adapter instead of calling BSG feature/interaction APIs directly.  Neutral
  `rt_view_feature_ref`, edit-preview event/callback, and feature-label types
  live in `rt/view.h`; `rt/view_legacy_bsg.h` exposes the transitional
  `rt_view_feature_*_bsg` wrappers; and `QgLegacyView.cpp` maps its public
  qtcad feature helpers to that RT surface.  Direct `bsg/feature.h`,
  `bsg/interaction.h`, `bsg_feature_*`, `bsg_interaction_*`,
  `BSG_FEATURE_*`, and `BSG_OVERLAY_*` usage is now guarded out of
  `QgLegacyView.cpp`.
  The remaining generic snap-exclude feature wrapper now takes neutral
  `rt_view_feature_ref` at the RT legacy adapter boundary; conversion to
  `bsg_feature_ref` stays inside `src/librt/view_legacy_bsg.c`, and the
  polygon snap-exclude wrapper feeds that neutral feature ref internally.
  The qtgl display-manager open path now also rejects retained BSG view
  contexts through `rt_view_context_is_bsg`, so `dm-qtgl.cpp` no longer
  includes `bsg/defines.h` or checks `BSG_VIEW_MAGIC` directly.
  The swrast display-manager private context now keeps the retained view as an
  opaque pointer and synchronizes dimensions through RT legacy context helpers,
  leaving `dm-swrast.h` free of `struct bsg_view` exposure.
  The tkswrast plugin now uses the same opaque swrast view-context route for
  refresh requests and dimension updates, with a context refresh helper in the
  RT legacy adapter.  qged framebuffer-server update callbacks now recover
  framebuffer-backed Qt canvases and DM init messages through the private
  qtcad DM bridge instead of including `dm.h` or calling `fb_get_dm`,
  `dm_get_ctx`, or `dm_get_udata` directly.
  qged current-view unit refresh and GED open/knob database-unit setup now
  route through the RT legacy view adapter rather than writing BSG unit fields
  directly.
  GED draw-test dimension/unit setup and scale-pointer handoff now use the
  same adapter boundary.
  qtcad quad-view visible-path propagation now uses Obol export identity in
  display-level mode instead of a direct BSG export query.
  qtcad selection's public header now exposes selected paths without naming
  legacy BSG pick-result objects, and the remaining BSG pick-result fallback
  now uses opaque qtcad legacy-view pick-result handles for fallback point,
  rectangle, and exact-ray candidate picks, result count/path/free operations,
  first-result filtering, ray-hit result copying, rectangle state, selection
  application, and screen-ray construction.  Its protected selection helper
  API is now view-implicit, keeping `bsg_view` out of `QgSelectFilter.h`, and
  `QgSelectFilter.cpp` no longer includes `QgLegacyViewBsg.h` or
  `rt/view_legacy_bsg.h`, calls `qg_legacy_view_to_bsg`, spells
  `struct bsg_view`, or calls the retained RT legacy-BSG pick/selection/view
  helpers directly.
  The old public
  `rt_view_selection_set_pick_result_bsg` BSG-shaped setter has been retired;
  callers and tests use `rt_view_selection_set_pick_result_ref_bsg` with
  opaque `rt_view_pick_result_bsg` handles.  BSG pick record construction and
  result ownership are confined to the RT legacy adapter.  The libged draw
  command and selection-semantics tests now use the same opaque pick-result
  helpers, including the semantic-path pick wrapper, instead of including
  `bsg/pick.h` or reading `bsg_pick_record` fields directly.
  The public `rt_view_selection_bsg` selection-object getter has also been
  retired; migrated tests and GED selection-count plumbing use
  `rt_view_selection_available_bsg` / `rt_view_selection_count_bsg` plus the
  existing clear/apply helpers, leaving raw `bsg_selection` access inside the
  GED-private draw bridge where records still need the staged BSG backing
  object.  That GED-private bridge has been narrowed as well: draw records no
  longer include `bsg/selection.h` or receive raw selection objects through
  `ged_draw_view_selection_bsg`; they use neutral path/kind helpers
  (`ged_draw_view_selection_*_path` plus `GED_DRAW_VIEW_SELECTION_*`), while
  temporary `bsg_selection` and `bsg_interaction_record` operations stay
  confined to `bsg_ged_draw_view.c`.  The draw ref bridge no longer includes
  `bsg/interaction.h` or `bsg/selection.h` for selection-record construction.
  A follow-up GED bridge include sweep removed stale `bsg/interaction.h`
  includes from draw util/tree/transaction/state/root/material/highlight/draft
  units; the remaining interaction-record dependency in the GED draw bridge is
  the intended view adapter.  GED command-result overlay insertion now also
  routes BSG overlay registry find/erase/append/owner-tagging through the same
  view adapter; `bsg_ged_draw_overlay.c` no longer includes `bsg/overlay.h` or
  spells `BSG_OVERLAY_*` directly, while retained scene/geometry creation stays
  in that bridge for the staged BSG backend.
  The public legacy `dm_draw` surface in `include/dm/vlist.h` now exposes
  typed render records through neutral `bg_vlist` and `bg/vlist.h`, with
  direct public `bsg/vlist.h`, `bsg_vlist`, and `BSG_VLIST_*` exposure guarded
  out; backend-private retained vlist internals remain staged behind the DM
  bridge.  The public legacy DM draw entries in `include/dm/view.h` now take
  opaque view-context pointers instead of naming `struct bsg_view`; the cast
  back to retained BSG state is confined to `src/libdm/view.c` until the
  backend is replaced.  `include/ged/view.h` no longer includes the broad
  `ged/bsg_ged_draw.h` header or exposes `bsg_data_polygon_state` for polygon
  export; `ged_export_polygon` takes an opaque polygon-state context and the
  retained BSG cast lives in `src/libged/polyclip.cpp`.  `selection_state.cpp`
  now uses the narrow GED draw-view bridge include plus local helper
  declarations instead of the broader private BSG draw header, and stale "view
  object" terminology was removed from the TclCAD polygon and qtcad legacy-view
  comments.  `include/tclcad/draw.h` has also been narrowed to forward
  declarations plus opaque `void *` draw-view contexts; the retained
  `struct bsg_view` casts now live at the libtclcad exported command entry
  points instead of in the public TclCAD draw API, and the header no longer
  drags in `dm.h`, `ged.h`, or direct BSG spelling.
  The public GED draw header no longer includes `bsg/interaction.h` or
  exports `ged_draw_shape_interaction_record`; draw-scene selection tests use
  neutral `ged_draw_shape_record.selected` state, leaving retained BSG
  interaction record construction in the GED view adapter only.
  qtcad GED draw observer sync and real-model Obol draw/readback workflows now
  keep controller-owned database sources realized after delayed Obol field
  sensors drain, and the full in-tree qtcad Obol workflow gate passes for the
  pinewood.g, havoc.g, and m35.g baseline.
* libbrlobol database realization handles real hierarchies, mesh/vlist
  shapes, identity fields, partial refresh, cached RT LoD metadata and
  optional active RT mesh normal payload copying,
  provider-neutral LoD request/result records, async LoD service skeleton,
  result-ready wakeup, staged result application, and first dedicated
  SoBRLLodMeshShape threshold use.  Controller-owned database-source
  replacement now configures watched source fields atomically so realization is
  not invalidated by stale delayed field sensors immediately after sync.
* Mesh export/measure/snap/pick now distinguish display-level geometry from
  full-detail/detail-preserving policy.  Resident full-detail preservation is
  an interim bridge only; it is not the large-geometry target.  Exact
  export/measure/snap now record source-backed full-detail request records
  when a LoD-backed mesh has only active display geometry resident, preserving
  source identity, counts, and bounds.  The first RT source full-detail
  provider can reload direct BoT objects into owned Obol mesh payloads through
  the LoD service, with stale source-count detection and whole-object
  face/point limits for memory-budgeted callers.  BRLObolViewController now
  owns an exact full-detail budget, and qtcad exact export/pick/snap/measure
  submissions pass that budget to the source provider so over-budget reloads
  can fall back before full mesh allocation.  Export, measure, and snap
  actions can now convert recorded source-backed requests into
  submit-ready RT full-detail LoD requests, and the LoD service has a helper
  that submits those requests with provider-owned cleanup.  Those actions can
  also consume returned full-detail mesh payloads using the recorded
  transform and shape metadata, preserving exact export/measure/snap
  accumulation paths once a caller drains provider results.  They also have
  bundled submit and drained-result consume helpers for recorded source-backed
  requests.  Exact mesh picking now has a source-backed traversal action that
  collects full-detail source requests for LoD-backed meshes in exact-pick
  mode, can submit those requests through the LoD service, and can consume
  drained full-detail results via the shared pick-result ray tester to build
  the same BRL-CAD pick-detail identity used by normal Obol traversal.  The
  LoD service now supports filtered result draining by request identity so
  exact-operation callers can consume matching source-backed full-detail
  results without stealing unrelated display-LoD results from the shared
  queue.  BRLObolViewController now exposes nonblocking source-backed exact
  helpers for export, measure, snap, and mesh ray picking: they drain only
  matching ready full-detail source results, submit missing source-backed
  requests when a database source is available, resolve each request to its
  owning database source instead of the first source in the view, stamp
  controller-submitted requests with database/source revision identity, and
  use the controller exact full-detail budget.  The LoD service now tracks
  active request keys and the RT source full-detail submit helper uses
  duplicate-active suppression, so repeated exact-operation retries do not
  enqueue the same source reload while an identical provider task is already
  queued or running.  When an exact helper drains only some ready source
  results for a multi-mesh operation, it now submits the still-missing source
  requests in the same pass instead of waiting for the next retry.  The qtcad
  point-pick helper
  now delegates to that
  shared source-backed exact-pick helper after ordinary Obol display picking
  and preserves nearest-hit ordering through a pick-distance field; its public
  record now also preserves source-backed mesh face, vertex, nearest edge, and
  edit-intent identity.  qtcad now also
  exposes explicit full-detail snap, export, and geometry-measure helpers that
  delegate source-backed LoD drain/submit work to BRLObolViewController while
  preserving display-level snapping as the interactive default; their result
  records carry source-backed mesh face, vertex/edge-nearest, and edit-intent
  identity as appropriate and report full-detail
  exported triangle identity or counts, bounds, area/length, and
  nearest-primitive plus source-backed face/vertex/edge and edit-intent
  identity without initializing the legacy display manager.  Those qtcad exact
  helpers now also report when a call submitted source-backed detail work but
  has no exact result yet, so tool callers can distinguish pending exact
  detail from a true miss.  qtcad point, rectangle, and ray selection filters
  use that pending signal to clear the immediate selection and skip legacy BSG
  fallback while exact source-backed pick detail is still loading.  The
  selection explicit-ray path now derives screen-to-view and view-radius data
  through the RT legacy view adapter boundary.
  Obol mesh nodes can now evict both
  preserved full-detail arrays and active display-LoD payloads while retaining
  source-backed request identity, source counts, bounds, and exact-operation
  provider routing.  SoBRLMeshResidencyAction and BRLObolViewController's
  explicit and optional controller-owned mesh residency budget paths can now
  enforce a resident mesh byte budget across a scene by evicting preserved
  full detail first and source-backed active display payloads second,
  including automatic enforcement after LoD results are applied.  Measure,
  snap, and pick source-backed exact requests now carry source-local
  query metadata into LoD provider params, giving the future chunked BoT reader
  object identity plus nearest-query bounds, snap bounds/tolerance, or ray
  information without retaining the full mesh in memory.  The current direct
  BoT source provider can also trim returned face payloads and compact subset
  vertices for snap requests that include bounds plus tolerance and for
  exact-pick requests carrying a source-local ray, while keeping measure hint
  requests whole-object for correctness.  Source-local scoped snap/pick
  requests now apply exact full-detail face/point budgets after direct BoT
  payload reduction instead of rejecting solely from whole-object source
  counts, mixed-kind, malformed, duplicate, or non-source-local query metadata
  stays whole-object for correctness, and scoped misses return a no-result
  fallback rather than expanding to a whole-object payload.  Source-backed LoD mesh
  payloads now
  carry optional source face- and vertex-index maps, so scoped subset payloads
  preserve original BoT ids through export, measure, snap, and pick consumers
  instead of reporting packed subset slots.  RT generated-mesh LoD storage and
  the transitional BSG cache API now document their optional normal payloads
  as triangle-corner arrays in face order, and rt_view_info verifies generated
  stores round-trip `face_count * 3` cached normals through active LoD data.
  libbrlobol also has the first
  librt exact ray-pick helper for implicit-solid and implicit-backed comb
  hits; it returns hit
  point, normal, distance, region/material identity, source primitive name,
  and BRL-CAD pick-detail identity without relying on tessellated display
  geometry.  qtcad point, explicit ray, and rectangle picks now use that
  helper as a database-source exact fallback for implicit/comb hits when
  ordinary Obol display and source-backed mesh passes do not produce a pick.
  The view controller owns reusable RT exact-pick caches for its database sources,
  invalidates them when scene/source ownership changes, exposes a shared
  cache-backed exact ray helper for non-qtcad callers, and qtcad point, ray,
  and rectangle picks reuse those caches across repeated exact rays.
* PNTS shaded point primitives have Obol realization/export/snap coverage,
  including all RT PNTS point-record layouts with optional color, scale, and
  normal attributes on the SoBRLVListShape point fallback.  The tests cover
  global scale fallback, per-point scale precedence, and transform-aware point
  attribute export/bounds.  A native point/marker node remains deferred.
* Sketch and annotation shaded database realization now keeps curve-like
  non-surface primitives drawable through the SoBRLVListShape fallback instead
  of treating them as failed mesh tessellations, with pick/snap/export/measure
  coverage.  Native Obol curve/text nodes remain deferred.
* Standalone material database objects now realize as SoBRLMaterialObject
  metadata nodes in both wire and shaded modes.  They preserve material
  identity and grouped properties while remaining non-drawable for bbox,
  pick, snap, export, and measure actions.
* Mesh sub-entity identity is now available through Obol actions.  Mesh snaps
  can opt into source-indexed vertex and edge-nearest candidates, and mesh
  measurement keeps nearest-face vertex/edge identity alongside the existing
  face result, with prototype coverage on transformed mesh geometry.
* The prototype's required pinewood.g/havoc.g hierarchy checks now avoid
  recursive random-access `getRealizedShape/Mesh(i)` counting over large
  realized scenes.  They use summary export and measure action counts for the
  same coverage, and the count-only wire hierarchy measure now disables the
  connected-angle search that default measurement still provides.  Default
  wire measurement now finds connected-angle candidates through endpoint
  buckets, so sparse line sets no longer pay an all-pairs segment scan for
  unrelated wires.  pinewood.g keeps the shaded hierarchy export/measure
  coverage, while havoc.g stays a required larger wire hierarchy traversal to
  avoid duplicating its roughly 5 second shaded realization in the prototype
  gate.  That keeps the required hierarchy coverage while reducing the
  FreeBSD/clang prototype test from roughly 107 seconds to roughly 1.4 seconds
  in this build.  The residual hierarchy measurement slowness was an
  accidental indexed vlist scan: SoBRLMeasureAction asked for each segment by
  ordinal, and each lookup rescanned the command stream.  Measure and snap
  actions now walk SoBRLVListShape command arrays once, preserving segment
  and point primitive identity while reducing this build's prototype CTest
  from about 1.44 seconds to about 0.96 seconds.
* Edit-preview geometry now publishes typed edit-intent metadata through
  Obol shape fields, pick details, snap candidates, measure nearest results,
  export records, and source-backed mesh request identity.  The prototype
  covers preview role/id preservation across pick, snap, measure, and export,
  and the qtcad edit-preview helper test now verifies the same typed intent
  through controller-published geometry and snap/measure/export traversal.
  SoBRLEditPreview now has explicit edit-intent id/role fields, and qtcad has
  an additive update-with-intent helper, so live edit tools can distinguish the
  transient preview node from the semantic operation or target being previewed
  without losing the old preview-id fallback.
* RT owns neutral view info, view metric helpers, LoD policy snapshots,
  mesh-LoD handles, sketch polygon handles, and legacy BSG adapters.
* libged/libtclcad/MGED read-only view numeric paths have been steadily moved
  behind rt/view_legacy_bsg.h helpers.  Recent batches covered libtclcad view
  draw/wrapper/commands/axes, MGED title/status, qged view-info display,
  simple libged model/view/grid conversion helpers, viewdir/view2model vector
  helpers, solids_on_ray, rtcheck2 overlap glyph scaling, NIRT shot/qray/vnirt
  setup, GED eye-model helper snapshots, BoT/BREP viewport pick rays, BREP
  TikZ view AET output, libged selection/polyclip/view-snap/view-polygon/vZ
  coordinate transforms, pipe/metaball/rot_point/label transform helpers, and
  BoT/ARB nearest-subentity transform helpers, draw refresh view snapshots
  through the private GED draw bridge, and libged view size/center/AET plus
  ypr/quat/rmat/perspective/eye_pos/width/height query output.  TclCAD
  polygon command view snapshots and coordinate conversions now use the same
  adapter boundary, with Tcl scratch/display-size state left unchanged.  TclCAD
  data move/pick/screen-to-model command conversions now also snapshot
  model-to-view and view-to-model matrices through the adapter.  TclCAD
  mouse BREP/joint selection rays and simple metaball/pipe point lookups now
  snapshot view-to-model matrices and view rotation through that boundary;
  remaining TclCAD mouse inverse-rotation edit-motion calculations now use a
  local adapter helper rather than direct `gv_rotation` reads.  TclCAD mouse
  BoT single-point setup, polygon circle/contour/ellipse/rectangle creation,
  and data-scale display conversions now also snapshot model-to-view,
  view-to-model, and scale values through the same adapter boundary.  RT now
  exposes legacy raw width/height, view-size, inverse-size, initial-scale,
  absolute-scale, unit-conversion, keypoint, rotate-about, and coord adapters
  as well; TclCAD mouse delta scaling uses the size adapter instead of direct
  `gv_size` reads, and libged isize/keypoint/rotate_about queries plus
  rotation/translation utility helper reads use the same boundary.  libged,
  TclCAD, and MGED default/query coord reads plus MGED edit-knob coord save
  reads now use that adapter boundary while coord setters and unsupported
  staging/hash fields remain transitional.  MGED view command staging/cache/hash
  reads for adapter-covered fields, MGED temp staging width/height reads, and
  libged DM fallback width/height checks now use the same boundary.  libged
  pmat/pmodel2view query output, TclCAD draw projection-matrix loads, and MGED
  staging/cache/hash projection-matrix reads now also snapshot through the
  adapter; MGED staging/cache/hash initial/absolute scale reads plus libged
  and MGED knob absolute-scale query output now also use scalar adapters, and
  MGED cache plus TclCAD refresh unit-conversion stash reads use the same
  boundary.  MGED cmd staging/cache restore/copy now uses RT legacy
  exact-state setters for scalar tuples, unit conversions, raw AET,
  eye/keypoint, rotation/center, derived matrices, and projection matrices;
  shared staging unit-conversion refresh uses the same boundary, while
  hash/address handling and RT-native/non-BSG setup fields stay transitional.
  MGED VIEWSIZE/VIEWFACTOR macros now expand through the adapter
  boundary while projection, hash/address, and RT-native/non-BSG setup writes remain
  transitional.  libged AET incremental updates now avoid direct readbacks
  after writing transitional view state, and libged eye-position plus
  perspective command matrix updates now use local/adapter snapshots and
  projection-matrix/eye-position setters instead of direct BSG readbacks or
  direct field writes.  libged knob keypoint-origin rotation now
  snapshots keypoint through the adapter, libged size/scale/zoom commands
  compute paired size/scale/isize values from local snapshots rather than
  direct view readbacks, and libged pure-rotation plus MGED view-scale ratio
  readbacks now snapshot current view state before writing transitional BSG
  fields.  MGED simulation output view
  argument injection now snapshots orientation, eye position, and size through
  the adapter boundary, and MGED command autoview ring-scale snapshots use the
  same read boundary.  MGED make default center/scale reads and area
  view-rotation diagnostic output
  now use the adapter boundary as well.  libged ADC view scale and conversion
  matrix reads and MGED slider view-scale reads now use the RT adapter
  boundary.  libged grid vsnap, eye, and rectangle-zoom read-side view
  snapshots now also use RT adapters while their live view writes remain
  transitional.  libged lookat and qvrot read-side view snapshots now follow
  the same pattern.  MGED axes overlay view size/rotation/model-to-view reads
  now use RT adapters, and MGED rectangle zoom, pipe pick direction,
  view-centered transform, and initial view-center setup reads use the same
  adapter boundary.  MGED status query output now reads view scale and
  matrices through that boundary as well.  MGED grid auto-size, draw/snap
  scaling, and grid coordinate-transform reads now use RT adapter snapshots;
  live grid view-center writes remain transitional.  MGED ADC view scale and
  matrix conversions, absolute translation bookkeeping, and chgview
  knob/edit/slew/size read snapshots now use the same adapter boundary.  MGED
  generic mouse and X motion/ADC input conversions now snapshot view scale,
  center, and matrices through that boundary while leaving event dispatch
  unchanged.  MGED edit matrix composition and rate-knob translation scaling
  now snapshot model-to-view matrices and scale through the adapter, and
  libged align plus rotation/slew/translate utility helpers now snapshot their
  center, AET, rotation, and matrix inputs before applying the existing
  transitional BSG state updates.  libged preview camera initialization and
  eye-path marker calculations now use adapter snapshots for rotation and
  view-to-model inputs while preserving the current preview view mutations.
  MGED saved-view snapshots and RT command eyepoint correction now read
  scale/rotation/center/view-to-model state through the same adapter boundary,
  and the RT command view update path now uses the RT legacy scale, rotation,
  and center setters while preserving the existing `new_mats` refreshes.
  `mged_vscale` now routes its view-scale update through the RT legacy scale
  setter while preserving absolute-scale knob bookkeeping, and live chgview
  initial/absolute scale bookkeeping now uses RT legacy setters.  Libged pmat,
  perspective, and eye_pos projection-matrix/eye-position updates plus MGED
  dozoom temporary projection-matrix updates now use RT legacy setters while
  preserving their existing `bsg_update` or frame-local restore behavior.  MGED
  autoview ring-scale updates and view-ring save-current snapshots now also
  read through the adapter while preserving view-ring restore writes.  The
  first narrow `edsol.c` slice now routes absolute-translation bookkeeping
  scale/model-to-view/view-to-model reads, solid-rotation rotate-about
  view/eye points, NMG/ARS/metaball view-direction snapshots, and BoT/NMG
  edit-pick plus solid/object edit mouse-projection view snapshots through the
  adapter.  MGED dozoom perspective, eye-position, model-to-view, and saved
  projection-matrix read snapshots now also use the adapter while live
  projection-matrix writes remain transitional; the stereo fallback perspective
  write now uses the RT legacy perspective setter.
  The libged selection semantics pick-point width/height reads and rtwizard
  view-matrix comparison reads now use the same adapter boundary.  The librt
  edit BSG
  compatibility bridge now builds `rt_edit_view` snapshots through the same
  RT view adapter boundary instead of reading BSG view fields directly.
  Production DM view-scale pointer setup now uses
  `rt_view_scale_storage_from_bsg`, preserving legacy DM's live pointer
  contract while removing direct `gv_scale` address reads from libged DM
  attach, MGED attach, and qtcad GL/SW fallback setup.  qtcad drag-bounds
  callback suspend/restore now also uses RT legacy adapter helpers instead of
  reading or writing `gv_bounds_update` directly.  qtcad snap policy reads,
  simple GED/TclCAD snap-line access, TclCAD Tcl-snap preparation, TclCAD
  snap-line recentering, and qtcad/libged snap tolerance factor reads and
  writes now also cross the RT legacy view adapter instead of calling BSG
  view-state helpers directly.
  TclCAD/MGED/libged z-clip view-state access also now routes through that
  adapter boundary.  libged/qtcad framebuffer-mode command and toggle access
  now also use `rt_view_framebuffer_mode_*_bsg` helpers, while broader
  framebuffer display ownership remains part of the libimgstream/Obol work.
  gsh/libged cleared-state tracking now uses `rt_view_cleared_*_bsg` helpers
  instead of direct BSG view-state calls.  libged/TclCAD polygon clipping and
  area/overlap helpers now derive their view plane through
  `rt_view_plane_from_bsg` instead of calling `bsg_view_plane` directly.
  MGED shared-settings checks now use `rt_view_settings_shared_bsg` instead
  of calling the BSG view-state helper directly.  libged loadview restore
  writes now use RT legacy setter adapters for size, rotation, center, and
  perspective instead of calling BSG utility setters directly.  qtcad
  keybinding/canvas AET writes now use the same RT legacy utility-setter
  boundary; the qtcad Obol draw-sync and snap test center/query setup now use
  the opaque qtcad legacy-view wrapper for that same retained state.  The
  qtcad Obol snap source/line/current-point checks and faceplate-sync state
  setup now also use qtcad legacy-view helpers.  qsketch view setup/fit-to-view
  writes now do as well.  qged polygon
  snap-line/source-flag/snap-exclude UI state
  and libged snap-semantics test setup now route through the same snap-state
  adapter helpers, and the libged view-independent oversized view setup now
  uses an exact scale-state adapter while polygon refs and grid state remain
  transitional.
  libdm framebuffer-mode draw/open checks also use the RT legacy framebuffer
  mode helpers, while broader framebuffer display ownership remains queued for
  libimgstream/Obol display-host migration.  A low-risk setter batch
  routes qged polygon align-to-poly and libged view/zoom/scale/rect/orient/
  rmat/setview/vutil writes for AET, center, rotation, size, and scale through
  the RT legacy setter boundary; unsupported derived view-to-model/projection
  writes and lifecycle/ownership changes remain separate transitional work.
  A follow-on simple-field setter batch adds RT legacy adapters for keypoint,
  coord, and rotate-about writes and routes libged keypoint/rotate_about,
  TclCAD set_coord, and MGED coord/rotate-about setting hooks through them.
  MGED chgview's librt-edit temporary coord override/restore now uses that
  same coord setter boundary.  MGED view-command cache/staging restore/copy
  paths now also use the RT legacy setters for perspective, coord, and
  rotate-about while leaving scalar/matrix staging writes transitional.  MGED
  saved-view and view-ring restore paths now use the RT legacy setters for
  scale, rotation, and center before their existing `new_mats` refresh.  MGED
  and libged grid center updates now use the RT legacy center setter while
  preserving the existing `new_mats`/`bsg_update` calls.  libged preview
  camera rotation/center writes now use the RT legacy setters while preserving
  the existing preview `bsg_update` calls.
  The libged perspective command and MGED perspective variable hooks now also
  route direct perspective field writes through the RT legacy setter while
  preserving their existing projection-matrix and display-manager sync logic.
  MGED command forwarding, screengrab, overlay execution, RT/RTCHECK view
  setup, DM command forwarding, and dozoom frame setup now update the retained
  view display-manager pointer through `rt_view_display_manager_set_bsg`
  instead of writing the BSG `dmp` field directly.
  libged DM command lookup/list/attach now reads and writes that borrowed
  display-manager pointer through the same RT legacy boundary, and TclCAD
  new-view DM setup uses the setter/getter for its initial handoff while the
  broader TclCAD DM read cleanup remains queued.  The private qtcad canvas
  bridge factory is also exported so FreeBSD/clang runtime plugin loading can
  resolve the swrast/qtgl DM plugin bridge symbols.
  The remaining compact libged command-side display-manager consumers
  (`screengrab`, `ert`, `fb2pix`, `fbclear`, `pix2fb`, `png2fb`, `overlay`,
  `rt`, `ged_rt_fb_refresh`, and the label repaint hooks) now read the
  retained pointer through `rt_view_display_manager_from_bsg`, leaving no
  production libged direct `dmp` field access outside the RT legacy adapter.
  The first TclCAD display-manager read batch now uses the same getter in
  wrapper dimension sync and perspective sync, viewability pathname checks,
  view draw setup, and axes fallback-width calculations; direct retained
  `dmp` reads are guarded out of those files.
  TclCAD refresh and framebuffer-open paths now also use the getter for
  draw-begin/end, zbuffer/depth-mask, framebuffer refresh dimensions,
  interactive-rectangle drawing, and display-manager framebuffer lookup.
  TclCAD polygon dimension sync and motion-binding pathname lookup now use the
  same display-manager getter, leaving polygon command setup free of direct
  retained `dmp` reads.
  TclCAD mouse command setup now has a local display-manager helper boundary
  for live width/height/pathname queries and dimension sync, so mouse
  interaction scaling and motion-binding setup no longer read `dmp` directly.
  TclCAD command-dispatch display-manager access is now behind a local helper
  backed by `rt_view_display_manager_from_bsg`, and the new-view handoff still
  uses `rt_view_display_manager_set_bsg`; production `src/libtclcad` no longer
  reads or writes the retained `dmp` field directly.
  gsh display hashing, dirty marking, view refresh, and display-manager close
  cleanup now also use `rt_view_display_manager_from_bsg` /
  `rt_view_display_manager_set_bsg`; `src/libdm/view.c` uses the getter for
  HUD render requests and `dm_draw_objs`, leaving the remaining `dmp` hits in
  that file limited to a local arrow-drawing payload rather than
  `struct bsg_view`.
  The private qtcad legacy-view display-manager bridge now also delegates its
  get/set implementation to the RT legacy display-manager adapters, removing
  the last direct `struct bsg_view::dmp` access from `QgLegacyView.cpp`.
  MGED view setup/attach now installs retained update callbacks through
  `rt_view_update_callback_set_bsg`, and MGED draw framing uses
  `rt_view_edit_matrix_*_bsg` plus `rt_view_frame_revision_from_bsg` instead
  of touching retained callback/edit-matrix/frame-revision fields directly.
  TclCAD data-arrow feature access now routes through the private GED
  draw-view adapter for feature existence, visibility, style, point-copy,
  replace, removal, and arrow-tip get/set operations; `view/arrows.c` no
  longer includes `bsg/feature.h` or the TclCAD BSG move-helper header.
  TclCAD data-axes feature access now uses the same GED draw-view adapter
  boundary for feature existence, visibility, style, point-copy, center-copy,
  replacement, removal, and size-preserving rebuilds; `view/axes.c` no longer
  includes `bsg/feature.h` or the TclCAD BSG move-helper header.
  TclCAD data-polygon outline publishing now packs local contour points with
  neutral `BG_GEOMETRY_LINE_*` commands and delegates typed polygon feature
  replacement plus overlay ownership to the private GED draw-view adapter;
  `polygons.c` no longer includes direct BSG feature, geometry, or overlay
  headers.
  TclCAD data-label feature access now routes label publish, draw existence,
  color readback, label enumeration, and retained label point updates through
  the same GED draw-view adapter boundary; `view/labels.c` no longer includes
  direct `bsg/feature.h` or calls `bsg_feature_*`.  TclCAD command and mouse
  label movement/readback paths now use the same GED label facade helpers,
  and the old TclCAD BSG move-helper header has been retired in favor of a
  neutral draw-view move-helper that routes data arrows, axes, and lines
  movement/readback/scaling through GED draw-view helpers.  Production
  `src/libtclcad` no longer includes direct BSG feature/overlay headers or
  calls `bsg_feature_*`.
  qtcad/qged production viewport dimension sync now uses a paired RT legacy
  width/height setter for GL, SW, shared canvas-state, quad-view, and
  current-view application updates; test fixture direct width/height setup
  remains transitional.  TclCAD wrapper, command, mouse, and polygon
  DM-to-view width/height sync now uses the same RT dimensions setter, and
  MGED draw/temp-staging plus libged headless DM fallback dimension writes now
  do the same; the pivot guard rejects reintroduced direct production
  assignments there.  Direct screen-to-view conversion calls now route through
  `rt_view_screen_to_view_from_bsg` in libged label creation, TclCAD
  mouse/polygon/screen conversion paths, and legacy libdm label-arrow drawing,
  leaving remaining direct `bsg_screen_to_view` usage contained to the
  adapter/test implementation.  Direct screen-pixel-to-model-point
  conversions now route through `rt_view_screen_point_from_bsg` in libged
  view-object/polygon commands and qtcad view, sketch, and polygon filters,
  leaving direct `bsg_screen_pt` usage contained to the adapter/test
  implementation plus libbsg internals.  Interactive rectangle state access
  now routes through RT legacy adapters in qtcad box selection, libged
  rectangle/select/faceplate commands, and TclCAD refresh/rectangle-refresh
  paths; those command and tool paths now use neutral
  `rt_view_interactive_rect_state` records and wrappers, leaving BSG-shaped
  rectangle records in the legacy adapter/test implementation plus libbsg
  internals.  The public libdm rectangle draw helper also takes neutral
  interactive rectangle state.  View refresh
  request/dirty/consume/complete/enabled/suppression/drawn-count state now
  also routes through RT legacy refresh helpers in qtcad canvas/view refresh,
  TclCAD refresh dispatch, MGED redraw/drawn-count paths, and Tk swrast
  repaint requests, leaving direct BSG refresh-state calls contained to the
  adapter/test implementation plus libbsg internals.  Faceplate center-dot,
  scale overlay, and params scalar state now route through RT legacy adapters,
  with direct BSG calls contained to the adapter/test implementation plus
  libbsg internals.  QGED view settings now uses neutral
  `rt_view_adc_state`, `rt_view_grid_state`, `rt_view_axes_state`,
  `rt_view_other_state`, and `rt_view_params_state` records for its top-level
  faceplate state, while qtcad Obol faceplate sync and qtcad keybinding
  toggles use neutral ADC/grid/axes records for the state they publish.
  QGED polygon grid-snapping controls, qtcad sketch grid setup, TclCAD
  command grid-snap decisions, and MGED grid command/structparse defaults,
  share/rset copies, refresh draw toggles, and motion/common-DM snap reads
  now also use neutral `rt_view_grid_state` records for grid
  draw/snap/settings state.
  TclCAD center-dot, view-params, view-scale, model-axes, and view-axes
  commands now use neutral `rt_view_other_state`, `rt_view_params_state`, and
  `rt_view_axes_state` records for their top-level faceplate state updates.
  The libged ADC command now snapshots and commits ADC state through
  the same adapter boundary, standalone libged grid and faceplate grid
  commands now use neutral `rt_view_grid_state` records, and libged faceplate
  model-axes/view-axes commands now use neutral `rt_view_axes_state` records
  with conversion confined to the RT legacy view adapter.  MGED ADC command,
  draw/status/scroll/keybinding helpers, share/startup copies, and grid-state
  wrappers/startup defaults now use the same neutral state-record adapter
  boundary, leaving direct top-level faceplate state calls contained to the
  adapter implementation and tests.  The public libdm ADC/grid/axes draw
  helpers now also take neutral `rt_view_adc_state`, `rt_view_grid_state`,
  and `rt_view_axes_state` records; the retained BSG HUD grid and axes
  payload conversions are local to `src/libdm/view.c`.  MGED direct
  edit/model/view axes drawing now constructs neutral axes state before
  calling libdm.  The obsolete BSG-shaped RT legacy faceplate state wrappers
  have been removed from `rt/view_legacy_bsg.h`; the neutral `_state_`
  adapters are now the only RT legacy wrapper surface for ADC, grid, axes,
  scalar faceplate, and interactive rectangle state.  Adapter/test-only
  const-selection, snap-exclude getter, and knob-state hash RT wrappers were
  also removed from that legacy header, and the guard rejects the underlying
  direct const-selection BSG getter from returning to that adapter/test
  surface.
  qtcad AET keybinding/
  canvas updates, libged grid vsnap recentering, and MGED `new_mats` derived
  view refresh now route through `rt_view_update_bsg`.  libged view update
  commands for zoom, size, scale, center, eye, eye_pos, AET/YPR/quaternion/
  rotation setters, perspective/projection, lookat, align, rectangle zoom, and
  vutil transformations now use the same derived-view refresh adapter.  libged
  preview/loadview camera restoration, mirror post-draw refresh, and QGED
  polygon alignment now use that adapter as well.  GED autoview, qtcad
  quad-view default framing, and GED draw transaction autoview now route
  through RT/GED private autoview adapters instead of direct BSG utility
  calls.  qtcad mouse and wheel navigation now route BSG-backed adjust
  math through RT view-adjust adapter flags rather than calling
  `bsg_adjust` or naming BSG adjust-mode constants directly.  qtcad canvas
  view-hash tracking now uses `rt_view_hash_bsg` instead of calling
  `bsg_hash` directly, and gsh display-dirty view hashing now uses the same
  RT view-hash adapter.  libged/MGED independent-view checks, independent
  scope lookup, zap clearing, and MGED view init-copy now use RT or GED-private
  legacy adapters instead of direct BSG utility entry points.  TclCAD 2D
  snapping, GED view snap candidate queries, and GED grid-center snapping now
  route through RT snap adapters instead of direct BSG snap utility calls.  GED,
  MGED, qtcad, and GED draw-record selection storage lookups now route through
  RT or GED-private selection adapters instead of calling `bsg_view_selection`
  directly.  Libged native selection draw-sync path enumeration, selection
  clearing, and shape-ref selection adds now also route through GED private
  selection helpers instead of opening BSG selection/interaction records in
  `selection_state.cpp`.  qtcad selection filters now apply pick results,
  clear view selection, run fallback picks, maintain rectangle state, and
  derive exact-pick rays through the qtcad opaque legacy-view helper surface
  instead of opening BSG selection/interaction/pick/view records in
  `QgSelectFilter.cpp`; retained BSG pick-result production remains staged
  inside the legacy-view adapter.  qged
  edit-preview plugins now publish begin/update/commit lifecycle events
  through the shared qged preview helper instead of constructing BSG
  interaction records directly in each plugin.  The ELL, revolve, extrude,
  and BOT edit-preview plugins now also route preview feature refs, overlay
  creation/removal, visibility, color/touch updates, and ELL edit labels
  through that helper, leaving the retained BSG feature/overlay bridge in
  `qged_edit_preview_util.cpp`, qged-owned line-command tokens in place of
  BSG geometry command names, and the retained BSG interaction-record
  create/free bridge in `qged_edit_preview_util.cpp` instead of the plugin
  files.  The edit-preview plugin implementations no longer include
  `ged/bsg_ged_draw.h` directly, and their plugin headers no longer expose a
  private `bsg_view`-returning `getView()` method.  The qged edit-preview
  helper API now takes `QgPluginContext` for staged view access, leaving
  direct `bsg_view` spelling inside `qged_edit_preview_util.cpp` rather than
  the plugin helper header or plugin files.  MGED highlighted-shape selection
  mirroring now uses a GED draw helper instead of constructing BSG interaction
  records in MGED.
  TclCAD, GED, gsh, and retained GED draw view enumeration and view-name
  lookup now use RT or GED-private view-set adapters instead of calling
  `bsg_set_views` or `bsg_set_find_view` directly.  TclCAD edit-redraw DB
  object iteration now uses a GED draw callback helper instead of direct BSG
  export requests, and MGED area-plot DB object checks plus segment iteration
  now use the same GED draw callback boundary.  Libged view vZ DB-object
  segment iteration now uses that boundary as well.  Libged plot, PS, and PNG
  diagnostic export loops now consume visible records and segments through the
  same GED draw facade instead of direct BSG export requests.  Libged
  labelvert, labelface, and select visible DB-object scans now also use that
  facade for record, segment, and point iteration instead of direct BSG export
  requests, and libged RT-write full-path flag/animation scans plus BoT dump
  visible DB-object traversal now use the same boundary.  The `who solids`
  detailed visible DB-object report path now also uses the facade, including
  draw-mode filtering, highlighted state, vlist counts, and geometry-report
  forwarding.  Libged view-object list/find/info export scans now use a
  general GED draw view-record query facade for view-object, DB-object,
  local-only, and glob-filtered queries instead of direct BSG export requests.
  GED, MGED, qtcad, TclCAD,
  and libdm BSG-backed view initialization, free, and view-set membership
  updates now route through RT lifecycle adapters.  GED and MGED view knob
  reset/hash/command-process/transform/rate-flag utility calls now route
  through RT legacy view adapters, and retained GED draw root recycle-pool
  lookup now uses a GED-private adapter instead of calling `bsg_set_fsos`
  directly.  Libged retained draw debug logging now goes through
  `ged_draw_log`, which lets the broad `ged_private.h` header avoid
  `bsg/util.h`; the helper is self-contained, disabled by default like the
  old BSG logging path, and split GED draw bridge files no longer include the
  broad utility header.
  TclCAD view creation now initializes its transitional
  `bsg_data_tclcad` block through a libtclcad-private view-data helper, and
  that helper now routes initialization through `rt_view_tclcad_data_init_bsg`
  instead of calling `bsg_data_tclcad_init` from application code; public
  `tclcad/draw.h` no longer exposes the concrete TclCAD
  view-data layout or BSG Tcl data payload.
  qtcad, TclCAD, MGED, and Tk swrast refresh requests now use RT legacy
  refresh-flag aliases instead of naming `BSG_VIEW_REFRESH_*` constants
  directly; direct BSG refresh flags are left only in libbsg and adapter
  parity tests.
  qtcad snap refinement, qged polygon snap toggles, and the libged snap
  command now also use RT legacy snap source/kind aliases instead of naming
  `BSG_SNAP_*` constants directly.  GED zap clear-scope flags now use RT
  legacy clear aliases instead of naming `BSG_SOURCE_*` constants directly.
	  qged polygon line-snap toggles, top-level point-selection clears,
	  create/modify view-polygon lookup/duplication/enumeration, and qtcad
	  polygon create/select filters now route polygon view-level calls through
	  RT legacy adapters instead of calling the BSG polygon view API directly.
	  qtcad polygon filter type and update-mode names are now qtcad-owned
	  constants instead of public BSG polygon names; BSG polygon refs, records,
	  and object mutation calls remain staged for the later polygon-object
	  migration.
	  qtcad/qged polygon filter and plugin headers now also use qtcad-owned
	  polygon ref spelling and null initialization (`qg_polygon_ref` and
	  `QG_POLYGON_REF_NULL_INIT`) instead of exposing `bsg_polygon_ref` in that
	  caller boundary.  BSG polygon predicates, records, and object mutation
	  calls remain staged for the later polygon-object migration.
	  qtcad/qged polygon callers now route low-level polygon predicates, record
	  reads, basic mutations, movement, CSG/remove, and user-data access through
	  RT legacy polygon-object adapters.  The staged BSG polygon record shape
	  remains follow-up neutralization work.
	  RT now owns a neutral polygon record (`rt_view_polygon_record`) and the
	  qtcad/qged polygon tools use the qtcad alias `qg_polygon_record` instead
	  of naming `struct bsg_polygon_record` or including `bsg/polygon.h`
	  directly.  BSG record copying is confined to the RT legacy adapter; the
	  lower-level sketch conversion remains staged in librt.
	  qged polygon sketch import/export now also routes through RT legacy view
	  wrappers instead of including `rt/primitives/sketch_legacy_bsg.h` or
	  calling the BSG sketch bridge symbols directly.
	  qtcad/qged polygon refs now use RT-owned `rt_view_polygon_ref` storage
	  through the `qg_polygon_ref` alias, and the polygon RT legacy adapters
	  convert to BSG feature refs internally.  qged polygon line-snap exclusion
	  uses a polygon-specific wrapper instead of passing polygon refs through
	  the generic feature-exclude setter.
	  qged polygon create/modify/settings widget headers no longer expose
	  private `bsg_view` accessors or include the qtcad view/filter headers for
	  polygon state; the remaining staged view lookup is implementation-local
	  and comes through `QgPluginContext`.  Polygon view-name validation now uses
	  that context instead of `gedp->ged_gvp`, and the validation-only
	  `uniq_obj_name(NULL, ...)` path correctly reports uniqueness without
	  requiring an output buffer.
	  qtcad Obol snap and faceplate-sync tests now route setup view-state through
	  opaque qtcad legacy-view helpers instead of direct BSG constants/getters/
	  setters or direct RT legacy-BSG view calls.
	  QgSW offscreen integration test view-set/unit-conversion setup now routes
	  through qtcad legacy-view helpers, and qsketch view lifecycle,
	  derived-update, fit-to-view, and grid setup paths now use the same opaque
	  qtcad helper surface instead of direct BSG view utility calls.
	  GED multi-view draw, view-command, view-independent, rtwizard, shared
	  draw utility, and qtcad DM-backend benchmark test setup now route
	  view-set lifecycle, dimensions, and unit-conversion writes through the
	  same RT legacy view adapters.
	  libdm swrast/Tk swrast view-dimension synchronization, qtcad quad-view
	  unit copying, and libdm backend draw-item test dimensions now also use
	  RT legacy view adapters instead of direct BSG view field access.
	  qged current-view unit refresh and GED open/knob database-unit setup now
	  also route through RT legacy view adapters instead of direct BSG unit
	  field writes.
	  Remaining GED draw-test dimension/unit setup and scale-pointer handoff now
	  also uses RT legacy view adapters instead of direct BSG view fields.
	  GED view-object mouse/current-point updates and align command view2model
	  writeback now also use the RT legacy view adapter boundary, while qtcad
	  snap-test current-point checks use the opaque qtcad legacy-view helper
	  backed by that boundary.
	  TclCAD refresh unit stashing, TclCAD command snap setup, and MGED knob
	  view-mode unit synchronization now set legacy view unit factors through
	  the same adapter boundary instead of writing BSG view fields directly.
	  RT now exposes a narrow previous-mouse adapter, and TclCAD polygon-mode
	  and command seed points use it instead of reading or writing previous
	  mouse fields directly.
	  TclCAD mouse simple previous-mouse readback, BREP/joint selection drag
	  start points, polygon point seeds, and shared mouse-scale updates now use
	  that previous-mouse adapter, and the remaining TclCAD mouse delta paths
	  now snapshot/update previous mouse through a local adapter helper instead
	  of reading or writing previous mouse fields directly.  TclCAD mouse
	  delta clamp and rotate/scale factors now use a grouped RT adapter
	  instead of reading BSG mouse delta fields directly.  TclCAD snap mask
	  preparation now uses the RT adapter's neutral integer mask type instead
	  of naming the BSG snap-mask typedef in caller code, and TclCAD polygon
	  command modes now use TclCAD-owned constants instead of BSG polygon-mode
	  names.
	  Public `dm.h` no longer includes the BSG umbrella header; it forward declares
  the remaining pointer-only transitional BSG draw-state types, and libged
  selection handling now includes its concrete BSG selection dependency
  directly.  The public BSG framebuffer payload member is now named `fb`
  instead of `fbp`, avoiding MGED's legacy `fbp` macro in FreeBSD/clang unity
  builds.  The GED BSG-backed view adapter declarations now live in a narrow
  `bsg_ged_draw_view_private.h` header, so LoD commands and view-independent
  draw tests no longer include the full retained draw-scene private bridge.
  Public `dm/view.h` now keeps `struct bsg_view` opaque instead of including
  `bsg/defines.h` solely for draw-entry pointer declarations.
  Top-level public `ged.h` no longer directly includes BSG headers; the
  remaining public GED BSG dependency is isolated to `ged/defines.h` while
  `struct ged` still embeds transitional `bsg_view_set` storage.  The swrast
  private header now forward declares its retained `struct bsg_view` context
  pointer instead of pulling in the broad BSG umbrella, and `dm-gl.h` no longer
  includes `bsg/defines.h` directly beyond the retained vlist boundary.
  qtcad/qged-facing polygon, sketch, view-info, and polygon-settings headers
  no longer include the broad BSG umbrella; retained transitional polygon/view
  declarations now use narrow BSG headers or pointer-only forward declarations.
  qtcad canvas, quad-view, selection, measurement, polygon, and shared
  view-filter implementation files now also use narrow BSG headers instead of
  the umbrella.  libged grid, view object, label, line, polygon, axes, and
  faceplate command files now also include only their needed BSG subsystem
  headers instead of the umbrella.  The remaining production broad BSG
  umbrella includes in gsh, libged utility code, and TclCAD mouse/polygon
  handlers were likewise replaced with narrower subsystem headers.  The
  matching GED/qtcad/librt test fixtures now also include specific BSG
  subsystem headers instead of the umbrella.  libged polygon clipping and zoom
  command code now also avoid `bsg/util.h`; their retained view reads stay on
  the RT legacy adapter boundary.  qtcad sketch editing also avoids
  `bsg/util.h`, reads Qt event coordinates directly instead of reaching
  back into `bsg_view` mouse fields, and now routes its retained
  screen-to-view, screen-point, width, and model-to-view reads through the
  opaque `QgLegacyView.h` helper surface.  qtcad 2-D/3-D measurement filters now
  capture Qt event coordinates in the filter state and use those for
  screen-to-view and Obol pick queries instead of reading BSG mouse fields.
  The private measurement overlay clear helper is now view-implicit, and
  `QgMeasureFilter.cpp` routes its retained screen-to-view and view-to-model
  reads through the opaque `QgLegacyView.h` helper surface instead of including
  `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h` directly.
  qtcad point, box, and ray selection filters likewise use Qt event
  coordinates for Obol and retained BSG fallback pick queries, and shared
  qtcad view-sync now pushes legacy mouse/current-point state through the
  opaque `QgLegacyView.h` helper surface.  The shared view filter also reads
  retained snap source/kind/tolerance state through qtcad-local helpers instead
  of including the qtcad BSG adapter or `rt/view_legacy_bsg.h` directly.  qtcad polygon create,
  update, select, point-edit, and move filters now pass Qt event
  coordinates/points into the retained BSG polygon API instead of reading BSG
  mouse/current/previous point fields directly.  qtcad view updates and qged
  view-change dispatch no longer keep `bsg/util.h` solely for disabled
  `bsg_log` tracing; local qtcad slot tracing remains.  qged polygon
  settings now performs retained view-object unique-name checks through the RT
  legacy adapter boundary instead of including `bsg/util.h` directly.
  qged view-info and host-status active-view names now route through
  `rt_view_name_from_bsg` instead of reading `gv_name` directly.  qged view
  info/select/measure widget headers no longer include `ged.h`, raytrace, or
  qtcad selection/measure filter headers for pointer-only state, and selector
  and measure implementations resolve the staged active view through
  `QgPluginContext` instead of `gedp->ged_gvp`.
  `QgPluginContext` no longer exposes a public BSG active-view accessor; qged
  plugins now resolve staged compatibility view pointers through
  `getViewWidget()` in implementation-local helpers, with the existing
  `QgModel`/`QgSession` fallback kept for startup/null-host paths.
  `QgPluginContext::activeView()` now owns that fallback as an opaque
  `qg_legacy_view *` convenience method, so qged plugins no longer open-code
  the widget/session lookup.  Plugins that still call retained RT/BSG APIs
  convert only that opaque handle at the local legacy boundary, and the pivot
  guard rejects direct qged plugin `ctx->model->session()->activeView()`
  fallback logic from returning.
  `QgViewFilter.h` also no longer exposes raw BSG view setter/getter methods;
  installed filters now keep only the `QgView` widget in the public base API,
  and retained compatibility view resolution is implementation-local to the
  filter and plugin `.cpp` files.
  The shared qtcad view-filter implementation now depends on
  `rt/view_legacy_bsg.h` for retained snap policy and tolerance reads without
  including `bsg/defines.h` directly.  The qtcad draw-sync test likewise now
  routes active-view, transaction-view, center, and scale setup through
  qtcad legacy-view helpers; its staged draw transaction coverage still uses
  `ged/bsg_ged_draw.h` where retained GED transaction types are the behavior
  under test.  The pivot guard rejects the stale direct BSG view setup from
  returning to this path.
  The remaining stale public qtcad model/sketch header BSG spellings were
  removed as well: `QgModel.h` no longer forward declares `bsg_view`, and the
  sketch-filter usage notes describe the `QgView` installation path without
  exposing the retained view pointer.
  qged's framebuffer server header no longer exposes the retained
  `fbserv_obj` type or raw framebuffer-server callback prototypes.  Its Qt
  helper classes carry opaque `void *` server state, while the libdm
  framebuffer-server dependency and callback signatures are confined to
  `fbserv.cpp` as the broader framebuffer/display-host migration remains
  queued.
  GED's public defines header likewise now forward declares `fbserv_obj` for
  the retained `ged_fbs` pointer, with libged/qged implementation files
  including `dm/fbserv.h` only where they dereference or allocate the object.
  The obsolete qtcad `bindings.h`/`bindings.cpp` free-function input layer was
  deleted entirely; `QgCanvasInput` is now the only qtcad CAD input-binding
  implementation, keeping drag-update state per canvas instead of in retired
  global maps.  `QgCanvasInput.h` now accepts the opaque `qg_legacy_view *`
  handle and hides drag callback storage behind an implementation pimpl,
  leaving `rt/view_legacy_bsg.h` and the qtcad/BSG adapter in
  `QgCanvasInput.cpp` where the retained staged view is actually adapted.
  qtcad/qged Obol overlay, selection, and draw observer sync now route
  active-display acceptance through `qg_obol_display_accepts_ged_active_view`,
  keeping the transitional `ged_gvp` comparison behind a qtcad helper whose
  public header exposes only `struct ged` and `QgView` forward declarations.
	  qged draw-observer registration/removal, explicit redraw transactions, and
	  draw-transaction view-presence checks now route through `QgLegacyView`
	  helpers too.  `QgEdApp.cpp` no longer includes `ged/bsg_ged_draw.h`, calls
	  GED draw observer/transaction functions directly, or reads `txn->view`;
	  those retained GED draw adapter operations are confined to `QgLegacyView.cpp`.
	  qtcad tree draw toggles and model `DrawnDisplayRole` queries now use
	  `QgModel::drawnPathState`, with the remaining active-view lookup confined to
	  `QgModel.cpp` instead of having `QgTreeView` read `gedp->ged_gvp` directly.
	  The qgmodel model-tester draw-state assertions now use the same
	  `QgModel::drawnPathState` boundary instead of including
	  `ged/bsg_ged_draw.h` and passing `gedp->ged_gvp` to
	  `ged_draw_path_state` directly.
	  `QgSession::activeView` now owns the qtcad session-level read of GED's
	  current BSG-backed active view, so `QgModel` draw-state helpers and qged
	  plugin-context fallback view access no longer read `ged_gvp` directly.
  The main qtcad canvas/session public boundary now exposes the opaque
  `qg_legacy_view *` handle instead of `struct bsg_view *`.  `QgCanvasBase`,
  `QgView`, `QgSession`, `QgGL`, and `QgSW` no longer publish BSG view spelling
  in their APIs; `QgLegacyViewBsg.h` is the explicit transitional adapter used
  by implementation files, qged plugin helpers, libdm Qt framebuffer bridges,
	  and BSG-facing tests that still need the retained staged view pointer.  The
	  pivot guard now requires the opaque handle headers and rejects BSG view
	  exposure from those public qtcad headers.
	  `QgLegacyView.cpp` now owns neutral qtcad helpers for legacy-view dimension
	  and unit-conversion writes.  qged command execution and palette activation
	  call those helpers on the opaque session active view instead of including
	  `rt/view_legacy_bsg.h` or `QgLegacyViewBsg.h` in `QgEdApp.cpp`; the retained
	  BSG cast and RT adapter calls are confined to libqtcad.
	  QgViewCtrl framebuffer mode toggle/status updates now use the same opaque
	  legacy-view helper boundary, so the toolbar no longer includes the qtcad
	  BSG adapter or calls the RT framebuffer-mode wrappers directly.
	  QgGL and QgSW viewport-size synchronization now call the opaque
	  legacy-view dimension helpers as well; retained RT dimension get/set calls
	  for those widgets are confined to `QgLegacyView.cpp`.
	  QgQuadView initial and post-propagation quadrant unit/size copies now use
	  the same opaque legacy-view helpers instead of direct RT legacy
	  unit/dimension get/set wrappers.
	  QgCanvasState view swaps and QgCanvasInput motion scaling now use the
	  opaque dimension/height helpers as well, keeping those size reads and
	  writes behind the qtcad legacy-view boundary.
	  QgViewFilter snap tolerance, QgSketchFilter vertex-snap scaling, and
	  QgSelectFilter box-selection dimensions now use the same opaque
	  width/height helpers; those filters keep BSG adapters only for the
	  retained staged snap, transform, selection, and pick APIs.
	  CADViewSettings now reads and writes LoD, framebuffer, and top-level
	  faceplate state through opaque qtcad legacy-view helpers as well; the
	  settings plugin includes neutral `rt/view.h` state records but no longer
	  includes the qtcad BSG adapter or the RT legacy-BSG wrapper header.
	  CADViewModel and HostStatusCommon now use opaque qtcad legacy-view
	  helpers for view info, name, AET, and center reads, so those lightweight
	  qged plugins no longer include the qtcad BSG adapter or RT legacy-BSG
	  wrapper header either.
	  CADViewMeasure and CADViewSelector now use opaque qtcad legacy-view
	  handles for their qged-side active-view availability checks, so those
	  plugin files no longer include `QgLegacyViewBsg.h` or spell raw
	  `bsg_view` either; selection and measurement behavior still lives in the
	  libqtcad filters.
	  QPolySettings now uses the opaque qtcad legacy-view helper for
	  view-object unique-name checks, removing the qtcad BSG adapter and
	  RT legacy-BSG wrapper header from the polygon settings widget.
	  QPolyCreate and QPolyMod now use the same opaque qtcad legacy-view
	  helper surface for polygon duplicate/find/visit, sketch import/export,
	  snap source/line/exclude state, grid snap state, alignment view writes,
	  and polygon ref operations.  The qged polygon create/modify plugin
	  files no longer include the qtcad BSG adapter or `rt/view_legacy_bsg.h`,
	  call `qg_legacy_view_to_bsg`, spell `struct bsg_view`, or call the
	  RT legacy-BSG polygon/view wrappers directly.
	  QgPolyFilter now consumes that opaque helper surface too for screen-point
	  conversion, create/select, screen-point update, close, move, visual/name,
	  CSG, and ref predicates.  The libqtcad polygon filter no longer includes
	  `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
	  `qg_legacy_view_to_bsg`, spells `struct bsg_view`, or names the retained
	  RT legacy-BSG polygon helpers directly.
	  QgMeasureFilter now uses the same opaque qtcad helper surface for 2-D
	  screen-to-view and view-to-model conversion, so the measurement filter
	  implementation no longer includes the qtcad BSG adapter or
	  `rt/view_legacy_bsg.h`, calls `qg_legacy_view_to_bsg`, or spells
	  `struct bsg_view`.
	  QgSketchFilter now uses the opaque helper surface for screen-to-view,
	  screen-point, width, and model-to-view reads, so the sketch filter
	  implementation no longer includes the qtcad BSG adapter or
	  `rt/view_legacy_bsg.h`, calls `qg_legacy_view_to_bsg`, spells
	  `struct bsg_view`, or names those retained RT legacy-BSG view helpers
	  directly.
	  QgViewFilter now uses the same opaque helper surface for snap policy,
	  snap kind, tolerance, current-point, and mouse-state synchronization, so
	  the shared filter no longer includes the qtcad BSG adapter or
	  `rt/view_legacy_bsg.h`.  QgSession and the Obol active-view sync helper
	  now also use qtcad legacy-view helpers for GED active-view and
	  draw-transaction view handles instead of adapting through
	  `QgLegacyViewBsg.h` or reading `ged_gvp` / `txn->view` directly.
	  QgView refresh requests now use qtcad-local legacy-view refresh flag
	  names and `qg_legacy_view_refresh_request`, so the wrapper widget no
	  longer includes the qtcad BSG adapter or `rt/view_legacy_bsg.h` for
	  staged-view refresh requests.
	  QgCanvasInput keypress handling now toggles ADC/model-axes/view-axes
	  state and applies AET presets through `QgLegacyView.h` helpers instead
	  of converting the opaque view to `bsg_view *` for those key-driven state
	  updates.  Mouse adjustment, drag-center lookup, and drag bounds callback
	  suspend/restore now use the same helper surface, so direct input-side
	  adapter use is confined to `QgLegacyView.cpp`.
	  QgModel draw-state queries and draw/erase transaction view scoping now
	  use qtcad legacy-view helpers for path-state checks, blank-slate checks,
	  and transaction view assignment instead of locally adapting the session
	  active view through `QgLegacyViewBsg.h`.
	  QgModel draw observer registration, scene-revision checks, draw-result
	  path extraction, transaction path inspection, view-only transaction
	  classification, and draw/erase transaction application now route through
	  the same helper surface.  `QgModel.cpp` no longer includes
	  `ged/bsg_ged_draw.h`, calls GED draw observer/transaction APIs directly,
	  names `GED_DRAW_TXN_*`, or reads draw transaction/result fields.
	  QgObolDrawSync now uses qtcad legacy-view helpers for transaction view
	  lookup, GED database access, transaction kind/path/mode/result inspection,
	  full-scene draw-list enumeration, and draw-state mode checks.  The Obol
	  draw sync implementation no longer includes `ged/bsg_ged_draw.h` or
	  `QgLegacyViewBsg.h`, calls `qg_legacy_view_to_bsg`, spells
	  `struct bsg_view`, names `GED_DRAW_TXN_*` / `BSG_DRAW_MODE_*`, calls
	  `ged_draw_path_state` / `ged_draw_list_paths` / `ged_draw_default_mode`
	  directly, reads `gedp->dbip`, or reads GED draw transaction/result fields.
	  QEll draw-highlight updates now use `QgLegacyView` helpers as well, so the
	  ELL edit plugin no longer calls `ged_draw_highlight_shape_ref_by_name`,
	  `ged_draw_set_highlighted_shape_ref`, or names `GED_DRAW_SHAPE_REF_NULL`
	  directly.
	  qtcad mouse-adjust mode values are now expressed as
	  `QG_LEGACY_VIEW_ADJUST_*` constants in `QgLegacyView.h`, matching the
	  retained RT adapter bit values without exposing those RT names to the UI
	  controller path.  `QgViewCtrl.cpp` no longer includes
	  `rt/view_legacy_bsg.h`; `QgGL.cpp` and `QgSW.cpp` keep the lower legacy
	  backend bridge where they still set up the retained view adapter.
	  QgQuadView now uses qtcad legacy-view helpers for quad view naming,
	  GED view-set add/remove/attach operations, LoD policy copy, and default
	  autoview/bounds updates.  The quad-view widget no longer includes
	  `QgLegacyViewBsg.h` or `rt/view_legacy_bsg.h`, calls
	  `qg_legacy_view_to_bsg`, spells `struct bsg_view`, or names those
	  retained RT view-set/LoD/autoview adapters directly.
	  The private qtcad canvas state now stores `qg_legacy_view *` handles for
	  its active and widget-local views as well.  `QgGL`, `QgSW`, and
	  `QgCanvasState` route the remaining legacy DM open/draw bridge through
	  `QgLegacyView` helpers, so raw `bsg_view *` storage and adapter spelling
	  are no longer part of the shared canvas pimpl or fallback canvas
	  implementations.  The pivot guard requires the opaque canvas-state fields
	  and rejects reverting `qgcanvas_set_view` or the canvas DM bridge to raw
	  BSG view access.
	  qtcad canvas hash, native-repaint, configure-window, and DM size
	  operations now also route through `QgLegacyView` helpers.
	  `QgCanvasState.h` no longer includes `dm.h` or defines `DM_WITH_RT`; it
	  keeps only forward-declared retained DM/FB/PTBL handles while the direct
	  libdm fallback lifecycle stays local to `QgGL.cpp` and `QgSW.cpp`.
	  The retained canvas DM/view binding sequence is now semantic as well:
	  `QgLegacyView` owns the temporary `dm_set_vp` scale pointer coupling,
	  BSG view display-manager assignment, and view-dimension sync from the
	  DM.  `QgGL.cpp`, `QgSW.cpp`, and `QgCanvasState.h` use
	  `qg_legacy_view_dm_bind` and `qg_legacy_view_dm_sync_dimensions` instead
	  of assembling those retained backend details inline, and the pivot guard
	  rejects direct scale-storage/display-manager coupling from returning to
	  the canvas layer.
	  Retained canvas DM-table registration and existing-framebuffer setup are
	  now bridged through `QgLegacyView` as well.  `QgGL.cpp` and `QgSW.cpp`
	  no longer include `bu/ptbl.h`, call `bu_ptbl_ins_unique`, or assemble
	  `fb_platform_specific` / `fb_setup_existing` records directly for the
	  temporary libdm fallback; the pivot guard requires the helper route and
	  rejects those lower-level setup calls from returning to the canvas
	  implementations.
	  Raw framebuffer creation, retained framebuffer release, standalone
	  checks, and resize configuration now also route through `QgLegacyView`.
	  `QgGL.cpp` and `QgSW.cpp` no longer call `fb_raw`,
	  `fb_set_standalone`, `fb_get_standalone`, `fb_close_existing`,
	  `fb_put`, or `fb_configure_window` directly; the pivot guard rejects
	  those retained framebuffer lifecycle calls from returning to the canvas
	  implementations.
	  The retained qtcad fallback DM open/close lifecycle is now bridged too.
	  `QgLegacyView` owns qtgl and swrast `dm_open` calls, SW app-widget
	  udata attachment, and `dm_close`; `QgGL.cpp` and `QgSW.cpp` use typed
	  `qg_legacy_view_dm_open_*` / `qg_legacy_view_dm_close` helpers instead
	  of opening, closing, or assigning SW udata directly.
	  Retained backend setup policy is now typed as well.  `QgLegacyView`
	  owns the qtgl/SW display-manager path names, SW zbuffer enable, and
	  temporary window-bound setup; `QgGL.cpp` and `QgSW.cpp` call
	  `qg_legacy_view_dm_setup_qtgl` or `qg_legacy_view_dm_setup_swrast`
	  rather than spelling `dm_set_pathname`, `dm_set_zbuffer`, or
	  `dm_set_win_bounds` directly.
	  Draw-time retained background and draw-boundary calls are now behind
	  `QgLegacyView` helpers as well.  The canvas files no longer call
	  `dm_get_bg`, `dm_set_bg`, `dm_draw_begin`, or `dm_draw_end` directly;
	  SW screenshot background adjustment uses copied qtcad bridge records
	  instead of raw libdm background pointers.
	  The canvas-state snapshot path has now moved its local view lifetime,
	  camera rotation/center/scale/perspective reads, faceplate state reads,
	  refresh request/consume/complete handling, view hashes, AET updates,
	  display-manager attachment, scale-storage coupling, and SW screenshot
	  model2view load through `QgLegacyView` helpers.  `QgCanvasState.h`,
	  `QgGL.cpp`, and `QgSW.cpp` no longer include `rt/view_legacy_bsg.h` or call
	  those RT legacy-view wrappers directly; the remaining lower libdm
	  retained-view handoff is named explicitly through `QgLegacyView` DM bridge
	  helpers.
	  The qged edit-preview compatibility helper now routes retained
	  feature/interaction operations through semantic `QgLegacyView` helpers.
	  `QgLegacyView` owns the staged BSG feature ref conversion, edit-preview
	  callback bridge, overlay owner registration, lifecycle event record
	  creation, label replacement, point replacement, and clear/remove/style
	  operations.  `qged_edit_preview_util.cpp` no longer includes
	  `bsg/feature.h` or `bsg/interaction.h`, spells raw `bsg_view`,
	  names `bsg_feature_*` / `bsg_interaction_*` APIs, maps
	  `BSG_EDIT_PREVIEW_*`, `BSG_FEATURE_*`, `BSG_OVERLAY_*`, or
	  `BSG_ANCHOR_*` constants, or uses the old raw
	  `qg_legacy_view_edit_preview_context` escape hatch.  The pivot guard now
	  requires the `QgLegacyView` helper route and rejects direct BSG
	  edit-preview bridge access from returning to qged.
	  qged framebuffer-server setup now also has a named bridge.  `fbserv.cpp`
	  owns the retained `dm/fbserv.h` and `dm.h` dependencies, assigns the GED
	  framebuffer-server callback table through
	  `qdm_configure_ged_fbserv_handlers`, and exposes `qdm_init_messages` for
	  filtered DM startup diagnostics.  `QgEdApp.cpp` no longer includes
	  `dm/fbserv.h` or `dm.h`, writes `gedp->ged_fbs` callback slots directly,
	  or calls `dm_init_msgs` directly.  The pivot guard requires this route and
	  rejects direct qged app-side framebuffer-server/libdm setup from returning.
	  `fbserv.h` now also avoids exposing the retained `fbserv_obj` type or raw
	  callback prototypes; its Qt helper classes keep only opaque `void *`
	  server state, and the pivot guard rejects retained framebuffer-server type
	  leakage from returning to the qged header.
	  The qtcad draw-observer transaction type boundary now uses qtcad-local
	  `qg_legacy_view_draw_transaction` and
	  `qg_legacy_view_draw_transaction_result` aliases from `QgLegacyView.h`.
	  `QgModel`, `QgObolDrawSync`, `QgObolViewSync`, and the qged Obol draw
	  observer no longer expose `struct ged_draw_transaction` or
	  `struct ged_draw_transaction_result` in their qtcad/qged-facing
	  signatures; those raw GED names are confined to the central legacy-view
	  adapter and tests.  The pivot guard now requires the qtcad-local names and
	  rejects direct GED draw transaction type exposure from returning to those
	  headers and implementations.
	  The installed qtcad draw transaction record has now been tightened from a
	  public semantic struct to fixed opaque in-place storage.  `QgLegacyView.cpp`
	  owns the private transaction storage record, uses placement construction in
	  `qg_legacy_view_draw_transaction_init`, and translates to/from retained
	  GED transactions inside the bridge.  Existing migrated callers continue to
	  use stack transaction records plus qtcad init/setter/accessor functions,
	  including the new `qg_legacy_view_draw_transaction_autoview_set`; the pivot
	  guard rejects the old public transaction fields from returning to
	  `QgLegacyView.h`.
	  The qtcad draw result record has now been tightened further: the installed
	  `QgLegacyView.h` result type stores only an opaque private implementation
	  token, no longer includes `bu/vls.h`, and exposes result status, scene
	  revision, names, and errors only through qtcad accessor functions.
	  `QgLegacyView.cpp` owns the result storage and copies retained GED result
	  data into it; the real-model diagnostic path now uses
	  `qg_legacy_view_draw_result_errors` instead of reading `result.errors`
	  directly.  The pivot guard rejects public result `bu_vls` storage and
	  direct migrated-test result-field reads from returning.
	  qtcad dense real-model coverage is now split into named CTest cases with
	  180-second per-test timeouts.  The m35 interaction path uses nearest-pick
	  semantics for its normal correctness assertions, while
	  `m35_wire_pick_all_stress` keeps the real-world dense wireframe pick-all
	  trial as a separate stress case.  The qtcad Obol pick wrapper now appends
	  pick-all hits and stable-sorts them once, avoiding the old quadratic
	  middle-insert ordering path while preserving nearest-first records; the
	  pivot guard rejects the previous insert loop and requires the stress case.
	  The havoc.g real-model test runtime was also traced to the test helper's
	  realized-geometry validation, not Obol sync: direct timing showed roughly
	  22 seconds in the legacy GED draw transaction, 0.5 seconds in Obol sync,
	  and 49 seconds in a quadratic `getRealizedShapeCount` /
	  `getRealizedShape(i)` loop.  The helper now traverses the realized scene
	  graph once to count wire shapes/segments or meshes/triangles, preserving the
	  same correctness checks while reducing the havoc geometry-count phase to
	  about 0.010 seconds and the full havoc case to about 23 seconds.
	  The standalone qsketch fixture now owns its staged view as an opaque
	  `qg_legacy_view *`, uses qtcad legacy-view helpers for view lifecycle,
	  setup, fit-to-view, and grid state.  Its retained edit creation and
	  field-backed sketch line-set scene nodes now route through the private
	  `QgLegacyViewSketch.h` qtcad bridge, so `qsketch.cpp` no longer includes
	  `QgLegacyViewBsg.h`, calls `qg_legacy_view_to_bsg`, or spells raw BSG
	  node/line-set refs.  The stale direct `dm.h` include is gone too, leaving
	  qsketch's retained display access through qtcad view/capture helpers and
	  the private sketch bridge; the pivot guard now rejects direct libdm or BSG
	  headers from returning to qsketch.
	  The migrated qtcad Obol workflow tests now also route retained draw
	  appearance through `qg_legacy_view_draw_appearance` and
	  `qg_legacy_view_draw_transaction_appearance_set`.  Draw sync, selection,
	  pick, measure, and real-model coverage keep their wire/shaded assertions,
	  but no longer include `ged/bsg_ged_draw.h`, spell
	  `bsg_appearance_settings`, write transaction `.appearance` directly, or
	  use `BSG_DRAW_MODE_*` constants.  The pivot guard enforces that the
	  retained appearance storage stays inside `QgLegacyView.cpp`.  The same
	  migrated workflow tests now also route draw observer registration and draw
	  transaction result init/apply/free through `QgLegacyView.h` helpers,
	  keeping raw `ged_draw_observer_*`, `ged_draw_apply_transaction`, and
	  `ged_draw_transaction_result_*` calls out of those tests while the
	  retained calls remain centralized in `QgLegacyView.cpp`.  Draw transaction
	  construction and borrowed multi-path setup now also use
	  `qg_legacy_view_draw_transaction_init` and
	  `qg_legacy_view_draw_transaction_paths_set`, so the same tests no longer
	  spell `ged_draw_transaction_make`, `GED_DRAW_TXN_*`, raw
	  `struct ged_draw_transaction`, or transaction `.paths` / `.path_count`
	  assignments.
	  libdm normalized view clipping, ADC drawing, label placement, GL z-clipping,
	  framebuffer canvas dimension setup, display-manager default clip bounds,
	  and internal pixel/view conversion macros now use RT legacy view adapters or
	  `RT_VIEW_MIN/MAX`, `RT_VIEW_RANGE`, `RT_INV_VIEW`, and `RT_INV_4096` from
	  `rt/view.h` instead of naming BSG view-state details directly; libdm scene
	  drawing no longer keeps `bsg/util.h` solely for disabled `bsg_log` tracing.
* libimgstream exists with core in-memory streams, dirty notifications,
  snapshots, several fb compatibility wrappers, raw file, stack fan-out,
  diagnostic sinks, colormap state, spec parsing, and display-host delegation.


Active Work Queue
-----------------

1. Finish low-risk RT view-info adapter cleanup.

   The broad direct-production view-field scan for libged, libtclcad, MGED,
   qged, and qtcad is now clean for the tracked assignment/matrix-copy
   patterns.  The former `src/libged/edit/edit.cpp` `rt_edit_view` initializer
   hit now relies on neutral `rt_edit_view_init` defaults rather than a direct
   field write or the BSG adapter boundary.  Treat further RT-view work as
   surgical cleanup or guard tightening rather than a broad `gv_*` migration
   pass.

   If more direct BSG view access appears, batch mechanical adapter migrations
   by field family when failures will still be easy to root-cause, but avoid
   mixing derived matrix/projection writes, lifecycle ownership, or interactive
   edit redesigns into the same batch.

   After each small batch, update brlobol_pivot_guard so the cleaned files
   cannot regress to direct BSG fields.

2. Productionize source-backed LoD for large BoTs.

   Dedicated LoD-backed nodes must not require full-detail mesh arrays to stay
   resident.  They should retain source identity, bounds, counts, policy, and
   the current display payload only while memory policy keeps it resident;
   exact operations reload or stream bounded full-detail data from the .g/RT
   provider path on demand.

   The remaining design and implementation pieces are:

   * POP provider payload ownership behind the neutral RT/provider API.
     The RT mesh-LoD detail callback bridge now validates optional normal
     payload ownership metadata before publishing borrowed full-detail arrays
     to the POP implementation, and replacing detail callbacks now frees the
     previous callback owner before installing the next one.  Callers can also
     explicitly clear detail callbacks on a live RT mesh-LoD handle, releasing
     provider-owned full-detail state and returning to cached POP-only data.
     Failed full-detail callback setup now fails the requested level load and
     invalidates the active level while clearing stale active mesh arrays
     instead of reporting a data-less full-detail level as current.
     The RT callback bridge also rejects full-detail payloads with missing
     faces, display points, or original-point arrays before lending them to
     BSG, and now rejects face indices outside either the supplied display
     point array or original-point array, so malformed provider data cannot
     become the current active level.
     RT active-data helpers now use that same contract: faces, display points,
     and original points must all be present before a mesh-LoD handle reports
     usable active data.
     If a producer setup callback returns failure after filling a partial
     detail record, the RT bridge now calls the producer clear hook before
     failing the load, so transient provider-owned buffers do not leak.
     Built-in BoT detail publication and cache generation now use the same
     mesh-array validation as neutral callbacks, so invalid source BoT arrays
     are rejected before BSG cache/detail ownership is involved.
     Explicit generated-mesh stores now capture current object/cache status
     before rejecting invalid replacement arrays, preserving existing cache
     entries and useful diagnostics when a bad replacement request arrives.
     Replacing or clearing a full-detail callback while borrowed detail is
     active also invalidates the active level before the next load, while
     callback-owner changes made during POP display preserve the active POP
     arrays.
     Broader provider-owned POP payload lifetime and source-side chunk/index
     work remain.
   * View-policy active-level scheduling beyond forced-level tests is now
     pinned at the RT provider/Obol submit-action boundary by comparing
     submitted results with direct `rt_mesh_lod_load_view` selection; richer
     provider/policy-aware scheduling still needs production work.  The
     submit action now avoids duplicate RT provider work when the same
     view/policy/provider request key is already resident on the mesh or
     already queued/running in the LoD service, while still allowing
     forced-level and reset submissions.
   * Memory-budget eviction of resident detail and active payloads.
     qtcad exact source-backed submissions now have a controller-level
     whole-object face/point budget, and Obol mesh nodes expose byte
     estimates plus explicit preserved full-detail and active display-payload
     eviction while retaining source-backed request metadata.  A scene-level
     residency action plus explicit and optional controller-owned budget paths
     now enforce resident mesh byte budgets by evicting full-detail backups
     first and source-backed display payloads second.  True chunked/bounded
     source reads remain, but source-backed measure, snap, and pick requests
     now preserve source-local nearest-query bounds, optional bounded-measure
     tolerance, snap bounds/tolerance, and ray metadata in provider params.
     The direct BoT provider uses snap bounds/tolerance, explicit bounded
     measure bounds/tolerance, and pick rays to reduce returned face payloads,
     compact subset vertices, preserve source face/vertex ids, and accept
     source-local scoped exact measure/snap/pick payloads that meet face/point
     budgets after reduction without expanding scoped misses to whole-object
     payloads or trusting mixed/ambiguous/malformed/non-source-local query
     metadata.  Unbounded measure keeps whole-object source reads for global
     nearest correctness.  Source-backed full-detail request conversion now
     strips stale template query params before adding current source-query
     metadata.
   * Wire remaining higher-level export/measure/snap/pick UI/tool callers to
     the controller-level source-backed submit/drain helpers.  qtcad point and
     rectangle picks now have the first nonblocking exact mesh pick
     integration through SoBRLSourceMeshPickAction, filtered LoD-result drain,
     and source-backed request submission.  qtcad also has explicit
     full-detail export, snap, and geometry-measure helpers that delegate
     source-backed drain/submit paths to BRLObolViewController while
     preserving display-level snapping as the interactive default.  The shared
     controller helpers now submit each missing exact full-detail request
  through the matching database source in multi-source views, and source
  full-detail submit avoids duplicate active provider work for exact retry
  loops.  Multi-request exact helpers now consume any ready source-backed
  payloads and submit still-missing requests in the same retry, so one ready
  mesh does not delay the remaining exact reloads.  Other tools still need
  their caller-side helper integration.
   * Chunked or bbox/intersection-targeted BoT detail reads for meshes too
     large to fit in RAM.
   * Optional coarse POP-build spatial/index sidecar to prune exact queries.

3. Add exact picking providers.

  Mesh display picks may use active display LoD.  Exact mesh picks should use
  resident preserved detail only when it exists, otherwise request
  source-backed full detail or bounded chunks through the provider path.

  CSG implicit solids and implicit-solid-backed comb instances require an
  actual librt ray query for exact picks.  The first libbrlobol helper now
  has a reusable BRLObolRtPickCache that prepares explicit database object
  paths once and shoots repeated rays through that RT instance, plus a
  one-shot wrapper for existing callers.  It returns the nearest implicit/comb
  hit with point, normal, distance, region/material data, and source primitive
  identity.  BRLObolViewController now owns the database-source RT pick cache
  lifetime, reuses unchanged prepared caches across point, ray, and rectangle
  pick calls, and clears or rebuilds them when scene roots, database sources,
  or source revisions change.  qtcad point, explicit ray, and rectangle picks
  now have the first database-source fallback integration for implicit/comb
  hits, including camera-derived rays for point-pick scenes with no
  display-pickable geometry.  qtcad point and ray picks now merge librt exact
  candidates with display/source-backed mesh results, suppress same-path
  RT/display duplicates, and keep nearest and pick-all records ordered by ray
  distance; rectangle picks inherit that ordering through their sampled point
  picks.  QgSelectRayFilter now tries the Obol explicit-ray helper before its
  legacy BSG/RT fallback, and the Obol ray path no longer requires the
  filter's legacy `dbip` field.  The source-backed mesh exact ray helper and
  cache-backed RT exact ray helper are now exposed on BRLObolViewController
  for non-qtcad callers, and qtcad delegates to those shared helpers.
  Remaining work: wire additional non-qtcad exact-pick tools to the shared
  helpers and carry the same display/source/RT duplicate-suppression policy
  into those callers.

4. Make qtcad visibly render through Obol by default.

   qtcad needs Obol visible paint, navigation, autoview, selection, picking,
   snapping, measuring, overlays, edit preview, and screenshot/readback to work
   for real .g workflows without requiring libdm for migrated content.

5. Replace diagnostic publication with Obol overlays.

   Define Obol-side lines, points, labels, axes, grids, faceplate/HUD, edit
   previews, and selectable diagnostics.  Keep bg_line_layer as a neutral
   producer/container and keep plot3 export as a simple diagnostic output path.

6. Continue libimgstream/fb display ownership migration.

   Route display-capable framebuffer requests through registered Obol
   display/window hosts.  Add dirty-region texture uploads, broader dm_open
   routing, hardware-preferred Qt host selection, and Tk/MGED adapters.

7. Harden guardrails and dependency inventory.

   Keep brlobol_pivot_guard in sync with each migrated boundary.  Convert to
   strict/final allowlists only after the corresponding legacy path is gone.


Large-Geometry LoD Policy
-------------------------

Do not design large BoT LoD around "preserve full detail in memory plus LoD".
That works only as an interim correctness bridge for already-resident meshes.

For very large geometry, including pathological cases like many Lucy-size mesh
primitives, the intended behavior is:

* Scene nodes keep source identity, display policy, bounds, counts, and the
  current display payload.
* Full-detail arrays are not required to remain resident.
* Exact operations fetch data from the authoritative .g/librt source when
  needed.
* If whole-object detail reload is too large, use chunks, bbox-targeted reads,
  or intersection-targeted reads behind the provider contract.
* A cached coarse sidecar can help exact pick/snap by mapping spatial leaves
  to source face ids or ranges, but it only pays off when librt can actually
  provide partial BoT reads or cached full-detail chunks.

The POP build pass is a plausible place to emit that sidecar because it already
walks source triangles, normalizes bounds, assigns active levels, and tracks
source face ids.  Keep the sidecar optional and coarse; it should not slow the
display POP path enough to hurt the main LoD benefit.


Test Candidates and Gates
-------------------------

Required/current model gates:

* share/db/pinewood.g
* share/db/havoc.g
* share/db/m35.g

Large maturity candidate:

* share/db/faa/Generic_Twin.g

Generic_Twin.g is the largest in-tree example, includes valid and invalid BoTs,
and mixes implicit solids.  Use it once the staged large-model LoD path is
ready for routine gating.  The qtcad real-model test already exposes it as an
opt-in BRLOBOL_QTCAD_GENERIC_TWIN=1 draw/readback check.

Short baseline after doc-only or small migration changes:

* git diff --check
* rg -n '^<<<<<<< |^=======$|^>>>>>>> |bsg/tig\.h|bsg/vectfont\.h' include src obol_activework.txt obol_worklog.txt CMakeLists.txt misc/CMake

Focused build/test baseline after RT-view or Obol migration code changes:

* ninja -C build lib/libged.so lib/libtclcad.so bin/mged qged_view_info
  libexec/ged/libged-bot.so
* ctest --test-dir build -R '^(brlobol_pivot_guard|rt_view_info|rt_view_legacy_bsg|ged_test_view_command|tclcad_test_ged_cmds_exist)$' --output-on-failure

Broader gate before removing a legacy path:

* ninja -C build
* ctest --test-dir build --output-on-failure
* qged/qtcad workflow tests against pinewood.g, havoc.g, m35.g, and later
  share/db/faa/Generic_Twin.g.
* Obol backend tests for Qt GL, offscreen/OSMesa, and NanoRT/software paths as
  applicable.
* libimgstream lifecycle, dirty-region, producer/subscriber, snapshot,
  transport, and framebuffer compatibility tests as applicable.


Open Questions
--------------

* Which Obol upstream APIs need to be added or improved once qtcad uses Obol
  as the visible renderer?  This needs more implementation evidence.
* Which residual libdm behaviors survive only as compatibility spelling, and
  which should be deleted after libimgstream/display-host replacement?  The
  main ownership split is recorded, but final behavior needs review as callers
  migrate.
* How much BoT subset-read/index-sidecar complexity is justified for exact
  queries?  Evaluate after the first source-backed full-detail provider path
  exists.


Final Done Criteria
-------------------

The migration is complete when:

* Obol is the only scene/rendering API used by BRL-CAD interactive
  applications.
* qged/qtcad and mged pass their Obol display workflows.
* libbsg, libdm, and dm_plugins are absent from build, install, public
  headers, pkg-config files, and linked target graphs.
* libbrlobol remains BRL-specific and does not contain a generic BSG
  replacement.
* Documentation names Obol as the canonical scene API and explains where
  BRL-CAD Obol extensions live.
