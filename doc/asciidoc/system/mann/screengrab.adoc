= SCREENGRAB(nged)
:doctype: manpage
:mansource: BRL-CAD
:manmanual: BRL-CAD User Commands

== NAME
screengrab, screen_grab - Save the current MGED display visual or framebuffer contents to an image file.

== SYNOPSIS

[source]
----
screengrab [-F] [-V view] [--format fmt] image_file
----


[[description]]
== DESCRIPTION

Generates an image file containing the current display manager's rendered scene. When `-F` is specified, captures the embedded framebuffer instead.

`image_file` is required. The output format is inferred from the file extension; use `--format` to override.

This command is also available as *screen_grab* (with an underscore). Both names invoke identical behavior.

=== Options

*-V view*, *--view view*::
Capture the named GED view endpoint. If omitted, the active view endpoint is used.

*-F*, *--fb*::
Capture the retained framebuffer stream without scene or faceplate composition.

*--format fmt*::
Force the output image format (e.g. `png`, `jpg`). If not specified, the format is inferred from the file extension.

*-h*::
Print usage help.


[[examples]]
== EXAMPLES

.Capture a specific named view endpoint
[example]
====
mged> **screengrab -V Mged0 output.png**

Captures the view endpoint named `Mged0` to _output.png_.
====

.Capture the scene display (default, no flags needed)
[example]
====
mged> **screengrab scene.png**

Writes the active view endpoint's composed scene to _scene.png_.
====

.Capture only the retained framebuffer
[example]
====
mged> **screengrab -F framebuffer.png**

Writes the active view endpoint's framebuffer plane to _framebuffer.png_.
====

.Force JPEG format regardless of extension
[example]
====
mged> **screengrab --format jpg view.img**

Writes the display manager scene to _view.img_ in JPEG format.
====


[[author]]
== AUTHOR

BRL-CAD Team


[[bug_reports]]
== BUG REPORTS

Reports of bugs or problems should be submitted via electronic mail to devs@brlcad.org
