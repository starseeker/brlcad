BRL-CAD Obol Active Work
========================

Last refreshed: 2026-07-08
Branch: oboltest2

Purpose
-------

Read this file first when resuming Obol/libbrlobol migration work.  It records
the current target state, invariants, known risks, and next implementation
steps.  Use `doc/notes/` for durable design contracts.
Current debug draw performance characterization is in
`doc/notes/obol_draw_perf_debug.txt`.

Workspace
---------

* Build directory: `.build/`, generated with Ninja.
* Python runtime for smoke scripts is auto-discovered (`python3.11`,
  `python3.14`, `python3`, then `python`).
* Debugger: `lldb`.
* Preserve unrelated worktree changes.
* Retire BSG and immediate-mode drawing paths when they conflict with a clean
  Obol/libbrlobol answer.
* Keep this file current when architectural state or next steps change.

Target Architecture
-------------------

Obol/libbrlobol is the canonical scene, view, interaction, display, and
framebuffer presentation stack for migrated interactive workflows.  Legacy BSG
and libdm code may remain compiled during transition, but they should not mask
missing Obol behavior or define new semantics.

libbv owns common non-scene view mechanics: camera state, matrices, autoview
bounds math, screen/model transforms, input/knob state, and view-set
bookkeeping.  It also owns passive view snapshots and sampling preferences
through `bv_view_info` and `bv_lod_policy`.  The old `rt_view_info` /
`rt_view_lod_policy` names are LoD compatibility aliases/wrappers over libbv
only; the RT view-context and scene-ref facade has been deleted.
Primitive draw/LoD callbacks use `bv_view_info` directly.  ABI compatibility is
not a goal for this branch; clean direct ownership is.  libbv must not own
scene objects, display lists, polygon stores, snap/selection state, realized
LoD payloads, LoD caches, or retained scene graph behavior.

libbrlobol owns the retained Obol scene graph: database sources, source-local
geometry, per-instance placement and identity, view-aware LoD payloads,
metadata/material/color-table resolution, draw proxy/cache ownership, mesh POP
LoD, command visuals, and framebuffer stream scene presentation.

librt supplies geometry evaluation/query services such as evaluated wireframe
generation.  It should not own interactive draw-cache policy or application
scene state.

Core Invariants
---------------

* Database source geometry is source-local.  Placement comes from explicit
  instance transforms, not baked point coordinates.
* Reused geometry must stay reusable.  Multiple comb instances may point to
  the same source geometry while preserving per-instance identity.
* Visible leaf/source instances own path identity, material/region state,
  selection, visibility, highlight, edit intent, and export metadata.
* View LoD is view-specific.  Multiple views may show different LoD for the
  same shared source while database edits, selection, picking, and erasure
  remain semantically identical.
* Active display LoD is bound through the traversing view state, not by
  mutating shared source nodes.
* Persistent draw cache data lives under `BU_DIR_CACHE/.DbDrawCache`.
* Legacy BSG mesh LoD is quarantined under `.BsgMeshLodCache` until deleted.
* AABB/OBB proxies are leaf-solid geometry only.  Comb bounds are bottom-up
  aggregates of transformed leaf facts.
* Command-generated visuals are Obol scene objects.  They are shared by
  default unless the command explicitly requests view-local scope.
* Faceplate/work-grid drawing is an Obol HUD overlay.  Grid snapping is
  semantic model-space state on `SoBRLGrid`, not a side effect of visible line
  geometry.  A hidden grid may still snap when the view state enables snap.
* Effective BRL-CAD material and color-table lookup belongs in libbrlobol.
  GED owns revision bookkeeping and requests refreshes.
* Draw-facing `rt_db_internal` interpretation belongs in libbrlobol database
  sources.  GED may load/select objects, but primitive publication is a
  libbrlobol responsibility.
* No-owned-scene or missing-feature-store cases should return explicit
  no-op/empty results, not recreate retained BSG fallback state.

Current Baseline
----------------

* Normal database draw/erase/redraw flows use Obol/libbrlobol providers rather
  than `_ged_drawtrees` retained BSG ownership.
* Draw transactions no longer keep parallel retained BSG scene ownership.
* View features, annotations, polygons, database-source records, material
  refresh, and primitive wireframe publication have Obol/libbrlobol paths.
* Faceplate grid publication uses `SoBRLGrid` with adaptive minor/major/axis
  HUD layers.  Bounds/autoview ignore the grid, while semantic snap traversals
  can still use the grid state.
* Command-scene coverage includes rtcheck, NIRT/qray, check overlap, GQA,
  BREP/NMG diagnostics, overlay uplot import, `vdraw send`, preview eye paths,
  and simple visualizations.
* qtcad ordinary painting is Obol-only.  qged framebuffer service traffic uses
  the shared libged Obol/imgstream fbserv backend with Qt-owned transport and
  a borrowed `QgObolWindowHost`.  GSH and MGED have Obol attach/screengrab,
  progressive-LoD, and ERT smoke coverage.
* `dm-obol` is the headless Obol display plugin.  Immediate-mode callbacks are
  no-ops; high-level GED draw transactions are expected.
* Progressive LoD tests cover qtcad startup, explicit expand, automatic
  expand, Generic_Twin startup/expand, wire LoD, shaded BoT LoD, and
  GSH/MGED attached-view image progression.
* Compact CAD source-local assembly and view-LoD CAD payloads exist.  Root
  aggregation was not kept because current benchmarks did not justify the
  added complexity.
* Evaluated wireframe defaults to the boolweave-backed librt path.  The old
  BigE-style method remains only as a comparison option.
* Evaluated-points mode is an Obol/libbrlobol external representation.  CLINE
  ray-sampling normals now degrade to radial or empty normals instead of
  aborting the process on grazing/degenerate sample rays.
* Current focused image gates pass for GED draw, faceplate/grid, LoD,
  selection, snap semantics, Obol draw sync, and libbrlobol prototype/LoD
  update tests.
* qtcad/qged/MGED production passive view state now uses libbv directly.  MGED
  view knobs use `struct bv_knobs`, qsketch publishes Obol feature-store line
  sets directly, and GED draw tests use libbv or final GED/libbrlobol APIs
  rather than RT context adapters.
* GED draw LoD policy storage uses `struct bv_lod_policy`; realized LoD
  payloads remain libbrlobol/Obol-owned and view-specific.
* librt primitive plotting and LoD callbacks use `bv_view_info` directly.
  The `rt_view_info` name remains only as a transitional compatibility alias
  and wrapper test target.
* Passive overlay, faceplate, snap, ADC, axes, parameter-display, and input
  records use libbv names in production code.  The old passive `rt_view_*`
  record names/init macros and ADC/knob helpers are no longer exposed by
  `rt/view.h`.
* The old librt `rt_view_context_*`, `rt_view_scene_ref`, feature/polygon
  adapter, and `view_legacy_bsg` implementation/declaration paths have been
  removed.
* GED draw plumbing uses `ged_draw_scene_handle` for opaque scene identity
  instead of RT-owned scene refs.  libbrlobol no longer exposes RT scene-ref
  bridge helpers on `SoBRLSceneController` or mesh-LoD cache APIs.
* libbrlobol has `BRLObolViewAttachment` for per-view Obol attachment state.
  GED view host records forward LoD policy, independent-scope state, and
  Obol scene-root tokens through it.  `BRLObolViewController` also owns its
  view-local realized LoD payloads through the attachment instead of raw
  controller members.
* GED view contexts and attached Obol controllers share one
  `BRLObolViewAttachment` while attached.  Attach, replacement, detach, and
  teardown explicitly bind/unbind the attachment, and `ged_test_mged_view_state`
  verifies multi-view controller attachment sharing plus GED-to-controller LoD
  policy propagation.
* MGED edit setup converts `bv_context` to `rt_edit_view` snapshots directly
  in librt; the stale BSG edit adapter header has been removed.
* The no-op GED edit-matrix view API and unused BSG `gv_edit_mat` display
  field have been removed.  MGED Obol refresh no longer stages edit state
  through per-frame display-path matrices.
* qged edit plugins now update edit previews only through GED/libbrlobol
  feature refs.  The direct qtcad widget-preview fallback has been removed
  from production qged edit code, and the pivot guard now rejects reintroducing
  that low-level qtcad helper in qged.
* Classic MGED modified-shape preview now also uses transient
  libbrlobol/Obol GED view features.  It no longer republishes modified
  geometry into canonical database-source records during edit display.
* Obol viewport readback is centralized in
  `BRLObolViewController::renderToImage`.  `dm-obol` no longer owns a separate
  display-image cache/copy path, and GED screengrab uses the Obol bridge for
  Obol-backed views.
* The Obol view-image bridge is exposed through `ged/draw_obol.h` for
  app-host readback.  TclCAD's legacy `to_pix` and `to_png` image exporters
  now use that bridge for Obol-backed views before falling back to raw
  GL/display-manager readback for non-Obol views.
* App-host helpers can now resolve and attach the Obol controller for a view
  through GED's opaque controller association API.  fbserv/readback helpers
  and MGED/GSH refresh use that association instead of probing the libdm Obol
  backend.  Draw-test helpers use the same association path, and direct
  `dm_obol_controller` access is limited to libdm and the GED Obol bridge.
* qged no longer carries a separate Obol framebuffer operation-table adapter.
  It binds the active GED view to the `QgView` controller and installs the
  shared libged fbserv backend in UI-thread present-on-flush mode.
* Obol DM attachment clears GED's cached standalone `rt` framebuffer device,
  and refresh keeps it clear for Obol/non-legacy display managers.  Obol views
  therefore do not inherit stale `/dev/swrast`, `/dev/qtgl`, `/dev/ogl`, or
  `/dev/wgl` window-open behavior from older DM attachments.
* Legacy `fb_open("/dev/qtgl")` and `fb_open("/dev/swrast")` can now use a
  registered libimgstream display host.  `QgObolWindowHost` and headless Obol
  hosts service those calls as image-stream presentation roots without opening
  nested legacy qtcad/libdm display-manager windows; unregistered sessions keep
  the old fallback behavior.
* The fbserv framebuffer backend operation contract and transitional
  object/transport type contract now live in `include/imgstream/fbserv.h`.
  `dm/fbserv.h` remains the transitional libpkg/TCP/IPC server wrapper and
  legacy `struct fb` fallback implementation, but Obol/libged/qged backends
  use an image-stream-owned type surface.  The fbserv wire-protocol message IDs
  also live in that image-stream contract, with legacy `MSG_*` aliases retained
  for existing callers.  Session-token authentication helpers now live in
  libimgstream as compiled protocol support instead of a standalone fbserv
  source-local header.  Non-I/O fbserv object state/accessor helpers are also
  implemented in libimgstream, with libdm's `fbs_*` entry points acting as
  compatibility wrappers while the packet loop remains in libdm.  The fbserv
  libpkg PKG message switch is now also image-stream-owned and shared by
  libdm and standalone `fbserv`, leaving the remaining TCP/IPC event loops and
  handler bodies as transitional host code.  Backend-operation dispatch
  through `fbserv_fb_ops` is implemented in libimgstream; libdm now wraps that
  path only to preserve the legacy `struct fb` fallback.
* qged, libged, and TclCAD use `fbs_set_legacy_framebuffer` /
  `fbs_legacy_framebuffer` instead of direct `fbserv_obj::fbs_fbp` access.
  The pivot guard keeps that direct legacy field access quarantined out of
  those migrated hosts, while TclCAD's older framebuffer-object commands
  remain legacy `struct fb` compatibility code behind the accessor boundary.
* libged and GSH use `fbs_*` transport/accessor helpers for fbserv
  initialization, handler install/clear, IPC/TCP capability checks,
  listener-port lookup, client-slot cleanup, and shutdown checks instead of
  direct listener/client/callback field access.  qged and TclCAD still carry
  toolkit-specific transport wiring while the packet layer remains in libdm,
  but their listener/client channel state is now accessed through `fbs_*`
  helpers instead of direct fbserv field reads/writes.
* qged and TclCAD setup paths no longer assign fbserv callback fields
  directly.  qged installs its Qt callback table with `fbs_set_transport`;
  TclCAD centralizes platform-specific TCP/IPC callback selection in
  `tclcad_fbserv_set_transport`.
* qged tests use `fbs_framebuffer_*` operation wrappers instead of reaching
  through `fbserv_obj::fbs_fb_ops` / `fbs_fb_ctx`; the backend table layout is
  treated as transitional fbserv implementation detail.

Known Risks
-----------

* Display stream and fbserv packet-transport ownership is still transitional
  in libdm.  The long-term home should be a small image/framebuffer protocol
  layer beside libimgstream/libicv; the backend operation contract has already
  moved to `include/imgstream/fbserv.h`, the fbserv wire message IDs now live
  there too, qged/libged/TclCAD direct legacy framebuffer field access has
  been quarantined behind accessors, and libged/GSH transport field reads now
  go through `fbs_*` helper APIs.
  qged/TclCAD callback installation is also table-based, and their toolkit
  listener/client channel plumbing is accessor-mediated while the state still
  remains in the transitional libdm fbserv object layout.
  qged direct backend-table access is guarded out in favor of
  `fbs_framebuffer_*` wrappers.  Session-token auth generation/verification has
  moved into libimgstream as a first packet-support extraction, and
  `fbserv_obj`/listener/client/transport callback types are also
  image-stream-owned.  Non-I/O object state helpers and the shared libpkg PKG
  message switch now follow the same ownership split.  Backend operation
  dispatch has also moved to libimgstream, with libdm retaining only fallback
  wiring for legacy `struct fb` callers.
* Older application hosts still need more Obol-native readback/fbserv/display
  cleanup, but their passive view-state mechanics should not grow new
  `rt_view_context_*` dependencies.
* Compact CAD records still need tighter promote/demote and off-scene handle
  APIs before they are a fully stable extension point.
* Progressive large-root behavior still needs stronger handling for
  duplicate-occurrence proxy/frontier expansion, non-union CSG children,
  pending-autoview scheduling/cancellation, and avoiding whole-root
  synchronous full-detail promotion.
* Evaluated wireframe still has open performance/candidate-generation work for
  very thin shell models such as `havoc.g`.
* Full shaded/hidden-line draws on large implicit-heavy models remain dominated
  by tessellation and can emit verbose NMG diagnostics.  This is separate from
  normal wire draw and should be tackled as a shaded-mode cleanup/optimization.
* Normal wire draw for Generic_Twin/havoc is still mostly source publication
  and render traversal cost, not primitive plotting.  Fresh benchmark evidence
  is required before reintroducing CAD aggregation complexity.
* Slow ERT streaming still needs a deterministic intermediate-frame image
  gate.

Next Steps
----------

1. Retire remaining application-host legacy dependencies: raw dm readback,
   fbserv hookup, and `dm_plugins` assumptions.  Obol/libbrlobol/libbv should
   own the modern behavior.
2. Move the remaining fbserv packet transport out of libdm after qged/GSH/MGED
   ERT paths are stable.  Preserve Auth/TLS and make the destination an
   image/framebuffer stream protocol layer, not libbrlobol scene code.
3. Finish residual command/import/readback/no-owned-scene/no-feature-store
   edge cases and tests.  Command-scene and overlay routing are mostly
   migrated; the remaining work is coverage and cleanup, not a second drawing
   path.
4. Finish edit visualization modernization on top of librt edit operations,
   libged command policy, and libbrlobol/Obol preview/manipulator
   realization.  Preview publication is now feature-based in qged and classic
   MGED; the remaining work is manipulator/object-edit realization and final
   command-policy cleanup.
5. Complete progressive large-root policy gaps: duplicate-occurrence
   proxy/frontier identity, CSG-aware non-union children, automatic
   pending-autoview settlement/cancellation, and no whole-root synchronous
   full-detail promotion on startup refinement.
6. Harden compact CAD records, explicit promote/demote APIs, publication
   batching, and tree-walk/path-state cost.  Do not reintroduce root/group
   CAD aggregation without fresh benchmark evidence.
7. Continue evaluated-wire follow-up: redesign thin-shell/havoc candidate
   generation, add deterministic parallel ray/classification work with
   per-thread resources, and keep boolweave as the default evaluator.
8. Keep GED basic/LoD/selection/faceplate image baselines verified against
   Obol outputs and reduce remaining DM/swrast baseline assumptions.
9. Remove libbsg mesh payload/LoD internals and remaining libbsg tests once
   retained BSG rendering no longer needs them.
10. Add deterministic slow-stream ERT tests that verify intermediate
    framebuffer updates as well as final output.

Verification Gates
------------------

Use focused gates during edits and broader gates before handoff:

* `ninja -C .build`
* `ctest --test-dir .build -R 'brlobol_pivot_guard|libbsg_api_hygiene|ged_test_obol_draw_sync|ged_test_view_command|mged_obol_smoke' --output-on-failure`
* `ctest --test-dir .build -R 'test_qtcad_obol_progressive_lod|gsh_obol_progressive_lod|mged_obol_progressive_lod' --output-on-failure`
* libbrlobol core tests
* `ctest --test-dir .build -R 'test_qged_obol_fbserv|gsh_obol_ert_smoke|mged_obol_ert_smoke' --output-on-failure`
* `git diff --check`
