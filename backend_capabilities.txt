Obol/libbrlobol Backend Capabilities
====================================

Purpose
-------

This file is the current status and remaining-work guide for retiring both
libbsg and libdm from the GED/qtcad drawing path.  Detailed migration history
and completed bridge-retirement notes live in `backend_worklog.txt`.

The target architecture is:

* Public and cross-library contracts remain neutral: `ged/draw.h`,
  `rt/view.h`, qtcad Obol APIs, and libbrlobol APIs.
* Obol/libbrlobol owns concrete scene, view, source, feature, polygon,
  render/export, framebuffer, and display backend implementation.
* libbsg is removed from the primary GED/qtcad drawing path.
* libdm is removed from the primary qged/qtcad drawing path.  Any remaining
  libdm use is isolated legacy compatibility, not a design input for new
  backend behavior.

Working environment notes:  development frequently happens on FreeBSD - in
those cases we're using clang and lldb.  If you need python on those systems
its currently python3.11

We usually build in a build/ direcotory in the source tree using the ninja
build tool.

Guiding Constraints
-------------------

* Do not expose BSG types, BSG enum names, or BSG ownership rules from new
  public APIs.
* Do not expose Coin/Inventor implementation details through GED or RT public
  APIs unless the API is explicitly libbrlobol-facing.
* Preserve current GED/qtcad behavior first.  Defer broad performance work
  until the migration is structurally complete unless a test exceeds the
  agreed 180 second limit or an obvious catastrophic slowdown is introduced.
* Keep new contracts narrow enough to replace remaining bridge-owned behavior,
  not broad enough to become a second generic scene graph.
* Keep guard coverage aligned with each retired BSG/libdm boundary.
* Any libdm-facing adapter must be disposable glue around a permanent
  Obol/libbrlobol capability.
* Work should focus on the highest priority capability, but need not shy away
  from changes that impact other categories solely to keep work "pure" to one
  category.  The priority is making good progress towards our desired final
  end state.
* Capability boundaries are sequencing aids, not reasons to preserve
  transitional ownership layers once a direct Obol/libbrlobol move is ready.
* Prefer straight owner replacement with targeted parity tests over new
  compatibility layers.  Add compatibility glue only when it directly keeps a
  required behavior or test live during a larger owner move.
* Retained BSG scene/source records are temporary backing-store state only.
  They are not target architecture and should be removed as soon as the Obol
  owner can carry the behavior.


Implementation Order
--------------------

1. Obol scene/source ownership.
2. View feature and polygon store.
3. Obol render/export record query.
4. Obol display backend draw stream, with temporary libdm compatibility.
5. qtcad/qged final display ownership and dependency-row removal.

The process should not strictly block on category boundaries when a step
naturally crosses them.  Large owner moves that retire retained BSG ownership
are preferred over smaller compatibility-preserving slices when the affected
behavior family can be covered with focused parity checks.


Current Status
--------------

Capability 1, scene/source ownership, is the active migration phase.

Current state:

* Obol/qtcad has staged scene, group, source, placement, display-state,
  revision, summary, and draw-sync coverage.
* GED public records and transaction paths increasingly use neutral summaries,
  source-state records, draw-state summaries, source snapshots, and facade
  operations instead of direct BSG field access.
* The current BSG-backed source adapter still owns the transitional retained
  scene implementation, geometry publication, source realization, LoD
  realization, child/source attachment, and remaining scene-ref mutation.
* `src/libged/bsg_ged_draw_private.h` still exposes transitional
  `rt_view_scene_ref` facades because live GED internals still need neutral
  handles for retained scene tokens while the Obol scene/source owner is
  incomplete.
* The pivot guard is enforcing staged boundary movement and should be updated
  when a bridge is intentionally retired.
* The focused scene regression and the Obol/qtcad guard group are the main
  fast checks for this phase.

Capability 1 completion path:

* Establish an owned libbrlobol scene-controller path inside libged, so Obol is
  available as a GED-owned scene/source target instead of only as a qtcad-
  supplied mirror.
* Make GED draw transactions write through to the Obol owner first, preserving
  the current BSG-backed source adapter as a temporary backing-store/display
  adapter.
* Switch GED neutral record/query APIs to prefer Obol/libbrlobol scene,
  source, display, material, and bounds summaries, using BSG only for missing
  compatibility paths.
* Take the remaining Capability 1 work in behavior-family owner moves with
  targeted parity tests; do not add new compatibility-shim families where a
  direct Obol/libbrlobol owner move can prove the behavior.
* Replace BSG-backed root/group/shape/source mutation calls in
  `bsg_ged_draw_source.c` with operations on the libbrlobol owner; keep BSG
  synchronized from Obol only while legacy display paths still require it.
* Retire the draw registry's retained scene-ref ownership fields and any
  retained BSG record state once typed GED refs resolve to semantic Obol
  identities/paths and the BSG adapter no longer owns source state.
* Allow source-adapter-local mutation work to move together by behavior family
  when that shortens the path to owner replacement; do not hold direct owner
  moves back to preserve narrower local-mutation boundaries.
* Declare Capability 1 complete only when root/group/shape/source/draw-intent
  operations and GED records work through the Obol owner, the pivot guard
  rejects new BSG-owned scene/source mutations outside the adapter, and the
  draw transaction/redraw/material/highlight/erase/bounds/source-summary
  regression set passes.  Retained BSG scene/source state may remain only as a
  disposable display/backing-store adapter, not as an authoritative owner.

Recent cleanup state:

* libged can now create and own an internal `SoBRLSceneController` through
  `ged_draw_obol_scene_controller_ensure`; owned controllers full-sync the
  current GED draw set, receive subsequent draw transactions, and are cleaned
  up during GED teardown.
* GED shape-record database-source summaries now prefer the libged-owned Obol
  scene controller when present, so public GED records can report Obol source
  revisions while the BSG source adapter remains the fallback/backing store.
* GED shape-ref display and material summaries now also prefer the
  libged-owned Obol database source when present, moving visible/highlight,
  line-style, transparency, material color, and material revision readback to
  Obol-owned state for that path.
* Public GED shape records now reuse the Obol-first shape display-summary
  facade for visible, highlighted, draw mode, line width, and transparency
  fields instead of leaving those fields on retained BSG shape summaries.
* GED shape-ref geometry summaries now prefer the libged-owned Obol realized
  shape summary when present, moving realized VLIST/mesh geometry name and
  point/index-count readback to Obol-owned state for that path.  Shape-context
  line and geometry readbacks now recover semantic source paths and prefer the
  same Obol-owned realized data before retained fallback.
* GED shape-ref line summary, point, command, and last-point readback now
  prefer the libged-owned Obol VLIST shape when present, moving another
  evidence-producing geometry path to Obol-owned realized data.
* GED shape-ref VLIST translation now mutates the libged-owned Obol VLIST shape
  directly when present, leaving the BSG geometry mutation path as a fallback
  for callers without an owned Obol source instead of treating the adapter as a
  second authoritative store.
* GED shape-ref VLIST publish and geometry-clear now also mutate the
  libged-owned Obol VLIST when present, so public line-set replacement,
  clearing, line readback, geometry summaries, and non-overlay draw bounds all
  observe the Obol owner before BSG fallback.
* GED shape-ref indexed-face publication now writes point/index geometry into
  the libged-owned Obol mesh shape when present, including legacy face-stream
  validation and fan triangulation into Obol triangle fields.  Geometry clear
  now also clears owned Obol mesh fields before falling back to the BSG
  adapter.
* Static source-adapter geometry clear, line-set publication, and point-set
  publication now ensure/use the owned Obol source first and leave retained BSG
  VLIST/point geometry as fallback only.  Submodel auxiliary staging reads
  temporary leaf wireframes from owned Obol VLISTs first and removes any
  temporary leaf source created for the copy.  Static indexed-face publication
  and mesh-LoD indexed-face updates do the same for owned Obol mesh sources,
  so source realization and redraw paths no longer treat the BSG geometry node
  as a parallel publication owner.
* NMG-region realization output now routes its filled line-set and
  indexed-face geometry through the same Obol-first publication helpers, with
  indexed-face retained BSG publication occurring only when the owned Obol
  source path is unavailable.
* NMG auxiliary normal/remainder line output now has a named auxiliary VLIST
  API on libbrlobol database sources and publishes those auxiliary line sets
  into the owned Obol source, with geometry clear removing auxiliary realized
  shapes before retained BSG fallback.  NMG normal/remainder publication now
  treats the owned Obol auxiliary VLIST as the publication owner and creates
  retained BSG auxiliary geometry only when the Obol publish path is
  unavailable.  Submodel leaf wireframe publication now treats the named
  Obol auxiliary VLISTs as the surviving owner when available, releasing the
  temporary retained child source/shape topology after the owned auxiliary
  publish succeeds.  libbrlobol database-source realization now preserves
  auxiliary VLIST children instead of treating primary realization rebuilds as
  auxiliary clears.  Auxiliary line-set publication can now carry inherited
  display/draw-intent metadata through an explicit libbrlobol display state and
  syncs owner placement onto auxiliary VLIST shapes.
* ANNOT line-segment realization now has Obol-owned source metadata and
  model-space VLIST coordinates in libbrlobol database-source realization, and
  GED geometry summaries report the owned Obol source as `annotation` before
  retained BSG fallback.  The richer typed BSG annotation payload remains the
  compatibility/export path for text and non-line annotation segments.
* GED draw-state summaries now prefer libged-owned Obol database-source draw
  mode and realized-shape draw-matrix metadata when available, and retained
  redraw line-set publication can create/update the owned Obol VLIST through
  semantic source paths before preserving the temporary BSG geometry update.
* GED source realization current, stale, and failed transitions now update the
  libged-owned Obol database source realization status, stale metadata, and
  realized revision fields through libbrlobol source/controller APIs before
  preserving BSG adapter status for transitional display compatibility.
* GED source rename transactions now use a libbrlobol database-source retarget
  API to rename the owned Obol source in place, preserve display/material
  state, update realized child source/owner metadata, and refresh realization
  against the renamed database object before falling back to full-scene sync.
  The C transaction path now invokes that owned-source rename directly, and
  the Obol observer treats an already-renamed source at the new path as handled
  instead of rebuilding it through full-scene sync.
* GED source-update, source-removal, and non-root reference-removal
  transactions now collect matching libged-owned Obol database sources by
  path/component and apply targeted stale, redraw, or removal behavior before
  falling back to full-scene sync.  Global component erasure in the current
  source adapter removes matching owned Obol-only sources, and scoped/view-
  specific component erasure removes matched owned Obol database sources
  through the record-filtered shape-ref release path.
* GED visibility, stale-source, material-change, material-refresh,
  transparency, and highlight transactions no longer force a full Obol scene
  rebuild after the lower-level owner-first source/group/display mutation has
  run.  Visibility and stale-source transactions also have explicit targeted
  Obol source/group handling in the transaction observer; view-scoped stale
  sync refreshes and realizes matching current sources, while non-view stale
  marks matching owned sources stale and preserves Obol-only sources that are
  not present in the transitional retained draw list.
* GED redraw and erase-prefix transactions now use targeted Obol
  database-source/group replacement or removal.  Redraw refreshes current
  retained draw paths without clearing unrelated Obol-only sources, and
  erase-prefix removes only matching non-overlay owned Obol path subtrees.
* Owned Obol controller detach now preserves the current libged-owned Obol
  database-source inventory as semantic draw state for the next controller
  attach; owned-to-borrowed attachment replays that inventory into the borrowed
  scene before using retained full-sync fallback paths.
* GED source realization role flags and view-policy metadata now update the
  libged-owned Obol database source through libbrlobol source/controller APIs,
  and local LoD realization decisions prefer Obol policy state before falling
  back to the BSG adapter record.
* GED shape-ref center and bounds-metadata mutation now update the
  libged-owned Obol database-source placement owner first and synchronize the
  realized VLIST draw-center/draw-size fields from that source, leaving the
  BSG placement metadata path as a fallback for non-Obol callers.
* Obol database sources now own draw placement metadata
  (draw matrix, draw center, and draw size).  libbrlobol exposes source-level
  placement mutation through `SoBRLDatabaseSource` and `SoBRLSceneController`,
  syncs that state onto realized VLIST/mesh shapes, and libged uses the new
  bridge when applying database-leaf draw placement, source-adapter geometry
  publication placement snapshots, and draw-state matrix tests.
* Non-overlay GED draw-bounds readback now prefers the libged-owned Obol scene
  database-source bounds aggregate when present.  Overlay-inclusive bounds
  remain on the BSG adapter until Capability 2 owns overlay geometry.
* Public GED shape display/material mutations now update the libged-owned
  Obol database source for visibility, highlight, color override,
  transparency, and material color, and the retained shape mutation fallback
  is retired for those public setters.
  libbrlobol database-source display mutation now propagates that owner state
  onto database-intent realized VLIST/mesh shapes so rendering/export actions
  consume the same visibility, highlight, line, color, and material fields as
  GED summaries while prototype/non-database diagnostic shapes keep their local
  presentation.  Database-source draw-mode mutation now also routes through
  `SoBRLDatabaseSource`, immediately synchronizing database-intent realized
  shape draw modes instead of relying on deferred field-sensor propagation.
  Database-source material-policy mutation likewise routes through
  `SoBRLDatabaseSource`, so sanitization/no-op behavior is owned by the source
  API instead of direct controller field writes.
* Public GED evaluated-region mutation now updates the libged-owned Obol
  realized shape metadata directly; the retained BSG legacy-eval fallback is
  retired for the public setter.
* GED material color refresh now uses Obol-first material summary readback as
  the current-revision authority, stamps the libged-owned Obol database source
  with the selected material color and explicit material revision, and no
  longer falls back to retained adapter material RGB/revision mutation.
* Source-adapter material summaries now prefer the libged-owned Obol database
  source before retained material fields, so internal material-copy/readback
  paths consume the same owner-first material state as public shape refs.
* Source-adapter display summaries and draw-state line-style readback now also
  prefer the libged-owned Obol database source before retained display fields,
  keeping internal redraw/display decisions aligned with public owner-first
  shape-ref readback.
* GED shape source snapshots now prefer libged-owned Obol database-source
  runtime state for the borrowed database pointer and tessellation tolerance,
  caching converted tolerance values in GED shape state for the public
  snapshot lifetime before falling back to retained source-state data.
* GED source runtime summaries now consume those Obol-first source snapshots
  for database, fullpath, name, and tolerance data before using the retained
  source-state record only for temporary mesh-LoD cache fields.  Typed GED
  shape-ref view-context and LoD ensure entry points now recover cached source
  refs when strict scene-revision refs are stale.  Public shape-ref
  realize-context now realizes the owned Obol database source first; retained
  scene realization is kept as temporary adapter synchronization when a
  retained ref still exists.
* Public GED shape display/metadata mutations now resolve semantic Obol
  shape/source identities directly and no longer mutate retained shape state
  as a fallback.  The Obol draw-sync parity test tracks retained shape
  mutation fallback use for evaluated-region metadata, explicit center
  placement, and visible/highlight/color/material display setters.
* Public GED database-change/stale marking now writes stale state into the
  libged-owned Obol database source, using libbrlobol source stale flags; the
  retained BSG source-adapter stale mutation fallback is retired for the
  public setter.
* GED group visibility, public draw-mode, DB-path, and appearance mutations now
  write through to the libged-owned Obol group and leave retained BSG group
  mutation as fallback only.  DB-path mutation uses an explicit owned Obol group
  rename/update path and updates retained registry bookkeeping without
  synchronizing retained group state when Obol owns the group.  The Obol
  draw-sync parity test tracks retained group mutation fallback use and asserts
  that owned source-root groups, normal groups, and nested group renames do not
  mutate retained group state.
* GED group lookup/create now returns a libged-owned Obol group ref directly
  when an owned Obol scene is active, including empty groups with draw intent
  and no database sources; retained path construction remains only the
  no-owned-scene fallback.
* GED database-leaf draft commit now ensures the corresponding owned Obol
  database source at commit time and direct source/group-ref commits move it
  into the owned Obol group tree before falling back to retained parent
  append.  The retained scene-ref database-leaf path also tries the owned Obol
  group move before using the BSG source adapter.
  Generic draft path/source-state attachment now also ensures the owned Obol
  database source immediately and removes that newly-created source if the
  draft is destroyed before commit, so draft geometry/display follow-up work
  no longer has to wait for retained append to establish an Obol owner.
  Draft display/material application now updates that owned Obol source first
  for draw mode, line style, line width, transparency, color override,
  highlight/visibility, and material color, leaving retained draft display
  fields as fallback when no owned source is available.  Draft tree legacy
  material-color selection now also publishes the selected material color into
  the owned Obol source before falling back to retained legacy color fields.
  Draft tree-result evaluated-region clearing now updates the owned Obol
  realized shape metadata before falling back to the retained legacy eval flag,
  and draft tree-result highlight/line-style mutation now updates the owned
  Obol source before retained fallback.  Draft tree-result region metadata now
  writes region id, air code, LOS, and material id into owned Obol realized
  shape fields before falling back to the retained legacy region id and
  retained registry region bookkeeping only when no owned Obol source handles
  the mutation.  Draft tree-result line width, transparency, and
  color-override state now update the owned Obol source through a libbrlobol
  display patch without rewriting source revisions or draw mode.  Draft
  display/material/tree-result mutation now uses the tracked draft Obol source
  path before retained draft scene refs, and parity coverage asserts the
  tracked-source draft family and direct database-leaf commit do not mutate
  retained shape state.  Libbrlobol database sources now own explicit display
  names, scene summaries expose those names on database-source nodes, and GED
  draft display-name mutation writes through the tracked Obol source before
  retained draft naming fallback.  Draft draw-matrix and draw-size placement
  mutation now also writes the tracked Obol source placement state before
  retained draft shape fields.  Draft evaluated-path transform placement now
  writes that draw matrix to the tracked Obol source before falling back to
  retained draft transform fields.  Libbrlobol database sources now own explicit source
  bounds alongside placement state, with source and scene summaries preferring
  those bounds before realized geometry fallback.  Draft known-bounds mutation
  writes center/size and explicit min/max bounds through the tracked Obol
  source first and only uses retained bounds when no owned source handles the
  update.
* Generic source-owner append now moves the corresponding owned Obol database
  source into the target Obol group and returns without mirroring that
  membership into retained topology.  Retained source-owner append is now the
  no-Obol fallback only, and the draft/group commit parity test tracks that
  owned Obol commits do not perform retained source-owner appends.
* GED group-record summaries now prefer the libged-owned Obol retained group
  for the fields Obol owns (semantic draw-intent path, draw mode, visibility,
  overlay flag, and transparency), while preserving BSG adapter
  view/source-scope metadata.  Topology-only Obol groups without draw intent do
  not synthesize public group-record paths, preserving erase semantics for
  intermediate group parents.
  The source-adapter internal group summary now uses the same Obol-first
  owner readback, so erase/scope/group helper paths no longer treat retained
  BSG group display and draw-intent fields as authoritative when a semantic
  Obol group exists.
* GED group appearance-settings readback now prefers the libged-owned Obol
  retained group for draw mode, line width, transparency, and color override
  state, with BSG retained as the compatibility fallback.
* GED group-record shape counts now prefer recursive database-source counts
  from the libged-owned Obol group tree, leaving BSG traversal as the fallback
  for callers without an owned Obol scene.
* Direct GED shape-source release now removes the corresponding owned Obol
  database source and treats retained source-owner release as the fallback
  only when no owned Obol source path is available, moving a non-transaction
  lifecycle path to direct Obol ownership while still bumping the GED draw
  structural revision.
* Direct GED root/active-scope exact path erase now removes matching owned
  Obol database sources and exact/nested Obol groups first and treats retained
  source-adapter erase as fallback only when no owned Obol path is handled.
  Direct path-prefix erase now removes prefix-matching owned Obol database
  sources and Obol groups first and treats retained source-adapter erase as
  fallback only when no owned Obol prefix is handled.  Owned exact-path and
  path-prefix erase bump the GED draw structural revision directly at the
  Obol owner boundary.  Direct source-root and scoped database-group clear now
  clear owned Obol database sources/groups first and treat retained clear as
  fallback only when no active Obol scene clear is handled, extending owner
  lifecycle behavior to root-level and scoped draw-scene operations.
  Shared-root active-scope component erase now removes matching owned Obol
  database sources directly with non-root and draw-mode filtering before
  retained component scans are considered, leaving retained erase as fallback
  for independent-view scoped cases or no-owner scenes.  Obol source removal
  now prunes empty non-overlay topology ancestors as well as draw-intent groups
  so owner-side component and source erase cleanup does not leave empty
  intermediate groups behind.
  Libbrlobol database source ownership now separates source instance identity
  from BRL-CAD database path: source nodes expose `instanceKey`, summaries
  report both `instanceKey` and `path`, and scene/view controllers provide
  instance-key create, rename, move, remove, auxiliary, and state mutation
  APIs.  This removes the path-key uniqueness blocker for independent-view
  scope ownership while preserving existing path APIs as `instanceKey == path`
  shorthand until higher library callers are migrated.
  GED Obol transaction sync now assigns stable source instance keys for
  independent-view database sources and routes scoped draw, erase, stale,
  visibility, redraw, erase-prefix, and clear-scope work through libbrlobol
  instance-key owners.  Direct active-scope exact/path-prefix erase and scoped
  database-group clear now use scoped Obol source removal and avoid unscoped
  group deletion for independent views, so shared and view-local owners for
  the same database path can coexist.  qtcad private direct database-source
  sync now uses the same stable independent-view source instance keys for
  replace, remove, and scoped clear, so direct qtcad source updates can
  coexist with shared GED/qtcad owners without a path-key compatibility shim.
  Draw clear uses a dedicated Obol scene clear helper so explicit empty Obol
  groups are removed even when no retained group children exist.
  Public exact-path and path-prefix erase wrappers now return the source-
  adapter erase result first, so Obol-owned deletion determines transaction
  status without requiring a retained draw-revision delta.
  Root and scoped exact-path/path-prefix erase now also remove matching
  database sources from the active attached Obol scene, so borrowed controller
  command paths do not require retained draw inventory to report or mirror
  deletion.
  Root and scoped clear likewise clear the active attached Obol scene directly,
  so borrowed controller clear/zap paths do not depend on retained draw
  inventory.
* Direct GED root group-name erase now treats matching non-overlay owned Obol
  draw-group removal as authoritative and leaves retained group-name erase as
  the no-owned-group fallback, covering another retained group lifecycle path
  outside transaction observer cleanup while preserving GED draw structural
  revision accounting.
* Source-adapter retained group free now removes the corresponding owned Obol
  group subtree first, so active-scope erase, nested group erase, empty-group
  prune, and clear-in-scope paths no longer leave Obol group/source children
  behind when they free retained groups directly.
* Source-adapter group-content clear and clear-scope teardown now route through
  owned Obol group clear/remove bridges before retained child free,
  detach, and release, so direct draw clear removes Obol-only group children
  and matching owned group subtrees instead of relying only on database-source
  pruning.
* Exact and nested GED group path erase now tries owned Obol group subpath
  removal before retained BSG fallback, so Obol-only nested groups created by
  the source adapter are removed through the libbrlobol owner and count as real
  erases for active-scope path erase.
* Transaction clear/teardown accounting now uses tagged scene-ref tree summary
  data instead of a private child-count or context-shaped bridge.
* Shape owning-group summaries now carry typed `ged_draw_group_ref` values
  instead of private scene context pointers or neutral scene-ref tokens.
* Shape-state source ownership now stores a tagged `rt_view_scene_ref
  source_ref` instead of an opaque `source_ctx` pointer.
* Shape-draft temporary source/shape ownership is source-adapter-local again
  and stores retained `bsg_scene_ref` handles directly; tagged source-ref
  conversion happens only at the registry handoff points that still require it.
* The libged Obol scene-sync bridge reads shape display state through the
  shape-ref display-summary facade instead of reopening shape scene contexts.
* Scene-root clear, erase, root creation/activation, and root attachment now
  call explicit source-root facades; retained root-ref recovery and view-root
  attachment live inside the source adapter.
* Scene-root shape/group traversal, subtree bounds, and group-presence
  readback now call source-root facades instead of walking generic retained
  scene-ref children or converting scene refs in the scene-root bridge.
* View-root context readback now uses `ged_draw_view_context_scene_root`; the
  private `ged_draw_view_context_scene_root_rt_ref` export is retired from the
  shared draw-private API.
* Transaction root child-count accounting now calls a source-root child-count
  facade which prefers the libged-owned Obol root group child count before
  falling back to retained BSG tree summaries.
* Source-root group-presence readback now uses a libbrlobol-owned recursive
  scene-group count when an owned Obol scene is attached, leaving retained
  root traversal only as the no-owner fallback.
* Source-root shape traversal now enumerates libged-owned Obol database
  sources before retained root traversal and returns typed shape refs whose
  owner group refs mutate the corresponding owned Obol scene groups.
* Source-root group traversal now enumerates libged-owned Obol draw groups
  before retained root traversal and returns typed group refs whose public
  record iteration and redraw behavior use owned Obol group/source membership.
* Component shape/group indexes now ask the libged-owned Obol source/group
  inventory first and only use retained registry index tables when no owned
  Obol scene is available, so index-driven stale/redraw/reexpand transactions
  can target Obol-only refs without retained fallback scans.  Source reexpand
  transaction fallback is now split by group and shape inventory so a miss in
  one retained adapter index no longer forces both retained scans when the
  other owner index succeeded.
* Shape path-hash indexes now ask the libged-owned Obol database-source
  inventory first and only use retained path-index tables when no owned Obol
  scene is available.
* Public primitive wireframe publication for Obol shape refs now plots through
  librt and publishes the resulting VLIST directly into the libged-owned Obol
  database source, so Obol-owned primitive publication no longer recovers a
  retained runtime scene ref through retained path-hash tables.
  Generic current-wireframe primitive realization now uses the same source-path
  direct Obol publisher for owned database sources before falling back to the
  retained per-primitive line-set publishers for non-Obol refs.
  Submodel primitive publication now has a direct Obol auxiliary path as well:
  it walks the submodel tree, plots each leaf, and publishes named auxiliary
  VLISTs under the owning Obol database source instead of staging retained
  child source/shape nodes as the owner.
  Generic current-wireframe submodel realization now resolves the source
  snapshot's owned Obol path and uses the same auxiliary publisher before
  falling back to retained child-source staging for non-Obol refs.
* Obol shape-ref wire redraw now reuses that direct owner publication path
  after applying the owned source draw matrix, instead of creating a retained
  draft/source-state pair as the redraw publication sink.
* Owned VLIST bounds updates now complete the source placement tuple with a
  valid identity draw matrix when deriving draw center/size from geometry, so
  auxiliary Obol VLISTs inherit complete source placement state.
* Obol-only shape refs now support source snapshots, material refresh,
  wireframe redraw, LoD ensure, public geometry publication/clear, and
  record/context readback from database-source summaries and context tokens,
  rather than recovering retained shape refs as the primary owner.
  BoT and BREP mesh-LoD runtime state now follows the same owner direction:
  `SoBRLDatabaseSource` owns the `rt_mesh_lod` handle and native bounds for
  owned Obol database sources, Obol-only LoD ensure realizes cached BoT and
  BREP LoD meshes directly into that source, and retained mesh-LoD runtime
  state is only the no-Obol fallback.
  Adaptive CSG wireframe LoD now follows that owner direction as well:
  Obol-only LoD ensure realizes primitive line sets with `ft_lod_realize`,
  publishes them into the owned source, and has a parity sentinel proving the
  retained adaptive primitive publisher is not used for owned CSG sources.
  Typed BREP/BSPLINE current-wireframe realization now uses the same owner
  rule: their native line-set realization is published directly to the owned
  Obol source before retained fallback, with a targeted BREP parity sentinel
  proving owned wireframe draw avoids retained primitive publication.
* Root-level database-source rename now retargets the matching owned Obol
  source-owner group and erase prunes that group, so Obol group traversal does
  not surface stale retained-style group records.
* Draft group commit now moves the already-owned Obol database source into the
  target Obol group and returns an Obol-backed shape ref, leaving the retained
  draft topology as transient source-adapter-local generation state.
* Typed GED shape and group refs now expose registry semantic paths for
  Obol-first lookup.  Shape-ref display/source/material/geometry readbacks,
  geometry publication/mutation, stale marking, and release now try the
  semantic path before retained shape-summary fallback; group-ref
  record/count/display/appearance/draw-intent paths do the same for group
  semantics.
* GED group tree summaries now prefer libged-owned Obol group tree metadata
  through a persistent Obol context token, including group/shape kind, parent
  state, name, DB fullpath when resolvable, draw-tree depth, and child count;
  retained BSG summaries remain only as fallback and as a borrowed fullpath
  fallback for virtual Obol group paths.
* Scene-context fullpath readback now prefers the registry's semantic
  fullpath, scene-context source/display summaries use the registry semantic
  path to query the libged-owned Obol source or group before retained fallback,
  and semantic scene-context tree summaries now use persistent Obol context
  tokens as the primary metadata source before retained BSG fallback.
* Scene-context overlay and subtree-bounds readbacks now use semantic
  Obol lookup when the context has GED registry ownership: libbrlobol exposes
  `SoBRLSceneController::getSceneSubtreeBounds`, and libged routes database
  source/group context bounds through the Obol bridge before retained fallback.
* Semantic scene-context child traversal can now return GED-owned Obol context
  tokens instead of retained adapter children.  libbrlobol exposes
  path-addressed tree-summary child queries, and libged uses those to make
  `ged_draw_scene_context_child_at`/`parent` work for Obol-only group/source
  children while preserving retained fallback for non-semantic contexts.
  Retained semantic database-source contexts now also return owned Obol parent
  tokens before retained BSG parent fallback.
* Typed shape-ref context creation now returns owned Obol database-source
  context tokens when semantic source paths are available.  Source context
  child traversal summarizes realized shape/material nodes under libbrlobol
  source children instead of exposing anonymous realization wrappers.
* Typed group-ref context creation now returns owned Obol group context tokens
  when semantic group paths are available.  libged builds the Obol parent-token
  chain directly from path-addressed libbrlobol tree summaries and resolves
  real nested DB fullpaths onto those Obol tokens, leaving retained group
  contexts as fallback only when no owned Obol group resolves.
* Shape evaluated-region mutation and record readback now prefer owned Obol
  realized-shape `regionId` metadata through semantic source paths, keeping the
  retained legacy eval flag only as transitional synchronization/fallback.
* GED-private source-record read/apply bridges now expose Obol-owned
  database-source draw mode, revision, stale/current, role, and view-policy
  metadata through semantic source paths.  Source-adapter database-source
  record mutation now writes the owned Obol source first and applies the
  retained BSG source record only as the no-owned-source fallback.
* Obol database-source material policy is now explicit libbrlobol source
  state.  GED source-record read/apply bridges read and write it through
  libbrlobol controller APIs instead of deriving it from retained adapter
  database-source records.
* Private source-adapter erase, visibility, child-iteration, and record-summary
  context pass-through wrappers are retired where ref-shaped facades exist.
* Active-scope lookup now resolves through the source-adapter-local
  `_ged_draw_active_scope_group_ref`; independent-view scope refs no longer
  cross the scene-root bridge or shared private draw header.
* Scene-context child lookup uses the scene-ref child iterator facade instead
  of a private direct child-index-to-context bridge.
* Scene-context parent lookup remains behind the source-adapter
  parent-context facade; moving raw parent traversal into the refs layer is
  intentionally guarded against.
* The standalone retained scene-ref/RT scope bridge file is retired; retained
  root/scope conversion now lives at the scene-root ownership boundary.
* Highlight tracking now routes retained highlight-bit mutation through a
  shape-ref source-adapter facade, leaving the highlight owner on neutral
  draw refs and records instead of cross-file scene refs.
* Transaction database-change, visibility, and transparency record callbacks
  now use shape/group-ref source-adapter facades instead of resolving
  retained scene refs solely for mutation.
* Transaction reexpand bookkeeping now reads retained group appearance through
  a group-ref tree facade instead of opening the group scene ref directly.
* Transaction redraw now routes shape-level retained geometry clear,
  wireframe publication, and database-source redraw-result state through a
  shape-ref source-adapter facade.
* Transaction group redraw now routes retained group lookup and shape
  traversal through a group-ref source-adapter facade instead of opening the
  group scene ref in transaction code.
* Transaction source update/redraw/reexpand index hits now use shape/group-ref
  index callbacks instead of retained scene-ref index callbacks.
* Material refresh now routes lazy per-shape material color/revision updates
  through a shape-ref source-adapter facade instead of resolving scene refs in
  the material walker.
* Navigation first/count/index/advance helpers now use scene-root shape-ref
  traversal, and owning-group lookup lives in the source adapter instead of
  the navigation module.
* Records-side path state, path listing, has-path checks, and public
  shape-record filling now read shape/source metadata through shape-ref
  source-adapter facades instead of resolving retained shape scene refs in
  the records module.
* Records-side public group-record filling and group-record iteration now use
  group-ref summary/count facades and scene-root group-ref traversal instead
  of resolving retained group scene refs in the records module.
* Public group-ref path, draw-mode, and appearance updates now live in the
  source adapter beside group-ref readback, rather than reopening retained
  group scene refs in the tree module.
* Full draw-clear independent-scope/root child traversal and public
  has-groups readback now route through scene-root facades instead of tree
  code resolving retained scopes, composing retained child clear/release
  operations, or running retained group traversal callbacks directly.
* Shape-draft group append now lives in the source adapter, including retained
  group lookup, intermediate child-group creation, and source-owner attachment,
  instead of tree code appending retained shape/source refs directly.
* Explicit group lookup/create now lives in the source adapter, including
  active-scope selection, retained child-group creation, draw-intent
  initialization, and path-state application, instead of tree code creating
  retained groups directly.
* Empty-group pruning after shape release now routes through a source-adapter
  facade instead of tree code recursively traversing retained children and
  freeing empty groups directly.
* Nested DB subpath erase now routes through a source-adapter facade, including
  subpath component extraction, retained shape path matching, and empty-parent
  cleanup, instead of tree code building retained nested-erase callback inputs
  or child-count/free checks directly.
* Recursive subgroup erase-by-name now routes through a source-adapter facade
  instead of tree code traversing retained child groups and freeing matching
  subgroup names directly.
* Group path-prefix erase now routes group-child snapshotting, path-prefix
  matching, scope filtering, and retained group free through a source-adapter
  facade.
* View-scoped clear now routes active-scope lookup, group-child snapshotting,
  scope filtering, and retained group free through a source-adapter facade
  instead of a tree-local retained group clear helper.
* Exact group path erase and scoped exact group path erase now route group-child
  snapshotting, path comparison, nested-subpath ancestor matching, and retained
  group/nested erase dispatch through a source-adapter facade.
* All-paths DB-subpath group erase now routes restartable group-child
  traversal, optional scope filtering, full-path subset matching,
  nested-subpath ancestor matching, and retained group/nested erase dispatch
  through a source-adapter facade.
* Erase-by-name group traversal now routes top-level group path-component
  matching, overlay-skipping child snapshotting, recursive subgroup erase
  dispatch, and retained group free through a source-adapter facade; tree keeps
  overlay name erase and root mutation orchestration.
* Exact DB-subpath, path-prefix, and component-name shape-record fallback erase
  now routes shape-record iteration, overlay/view/mode filtering, shape-ref
  release, and empty-group pruning through source-adapter facades; tree keeps
  DB path conversion, draw-revision fallback decisions, overlay-name erasure,
  and scope/mode orchestration.
* Root and scoped exact-path/path-prefix erase now route DB-path conversion,
  DB-index fallback, retained group/shape erase dispatch, and active-scope
  retained-ref resolution through source-adapter facades.
* Root exact-path, path-prefix, and group-name erase now route root retained-ref
  resolution through explicit scene-root facades instead of tree-local root
  mutation callbacks; tree keeps public wrapper dispatch/status plumbing and
  overlay-name erasure.
* Overlay name erase and overlay geometry insertion now route retained overlay
  scene lookup, geometry publication, registry entry, append, bounds,
  appearance, color-soltab policy, and release through source-adapter facades;
  overlay code keeps public validation and active-view dispatch.
* Retained overlay scene lookup, overlay name erasure, overlay scene append,
  command-result owner registration, overlay geometry creation, and internal
  overlay feature creation now live in the source adapter instead of the view
  adapter; the view adapter still owns the later feature/polygon overlay
  behavior until Capability 2 replaces that backend.
* Gobject overlay draw walking now carries a typed
  `ged_draw_group_ref draw_parent_group_ref` overlay parent instead of a raw
  BSG scene ref, opaque backend context, or neutral scene-ref token; retained
  overlay creation, database-leaf draft commit, and scene-ref realization
  dispatch live behind source-adapter facades, leaving `draw.cpp` and
  `draw_data_t` without direct retained scene-ref tokens.
* The gobject database-leaf draft commit facade is now source/group-ref
  specific (`ged_draw_source_group_ref_commit_database_leaf_draft`), and
  overlay parent creation returns `ged_draw_group_ref` through
  `ged_draw_view_context_overlay_internal_create_group_ref`.
* The standalone retained path-state bridge file is retired; indexed fullpath
  setup and semantic draw-intent path updates now live in the source adapter
  beside the retained draw-intent setters.
* The standalone retained database-source/source-state binding file is retired;
  source-owner lookup, database-source record conversion/sync, source-state
  attachment, and draw-source mesh-LoD cleanup now live in the source adapter.
* The standalone retained shape-draft bridge file is retired; draft lifecycle,
  geometry publication, display-state application, source-state setup, and
  group/database-leaf commit now live in the source adapter.
* BREP/BSPLINE retained line-set publication now lives in the source adapter;
  the BREP C++ helper remains only for BREP mesh-LoD callback/detail setup.
* Source-only retained geometry publication, primitive wireframe/face-set
  publication, NMG-region publication, and geometry bounds update helpers are
  now file-local in the source adapter instead of exposed through the shared
  private draw header.
* Source-only retained overlay scene helpers, low-level retained scene
  creation/attachment helpers, draft-pair creation, source classification,
  scene release, and group-ref append helpers are now file-local in the source
  adapter instead of exposed through the shared private draw header.
* Source-only retained draw-state, draw-intent, display attribute, material
  color, path-state, realization invalidation, and overlay-attribute mutation
  helpers are now file-local in the source adapter instead of exposed through
  the shared private draw header.
* Source-only retained group/shape erase internals, database-source record
  sync helpers, source-owner/parent readback, source-summary/draw-state
  readback, material refresh, soltab coloring, and realization dispatch helpers
  are now file-local in the source adapter instead of exposed through the
  shared private draw header.
* Raw retained scene-root shape/group traversal callbacks are now file-local in
  the scene-root bridge; shared callers use the shape-ref/group-ref traversal
  facades instead of private `bsg_scene_ref` callback entry points.
* Group shape-count/redraw traversal in the source adapter now uses a
  source-local retained shape iterator, and the shared private draw header no
  longer exposes the raw scene-ref shape traversal entry point.
* Scene-root clear-all-scope now reaches retained child teardown through an
  opaque scene-context facade; the raw retained scope-child clear helper is
  source-local instead of shared through the private draw header.
* Scene-root subtree-bounds readback now reaches retained bounds traversal
  through an opaque scene-context facade; the raw retained subtree-bounds
  helper is source-local instead of shared through the private draw header.
* Scene-root creation now creates the retained draw root through
  `ged_draw_source_root_create_group_ref`; GED stores the root as a
  cache-style typed `ged_draw_group_ref`, while retained root recovery remains
  at the source-adapter/scene-root bridge edge.
* Root visibility setup and retained draw bookkeeping attachment happen inside
  the source adapter; the raw retained group creation, visibility setter,
  draw-context attachment, and old `ged_scene_root_rt_ref` storage accessors
  no longer cross the shared GED root-storage boundary.
* Shape-draft region metadata now reaches registry state through typed
  `ged_draw_registry_shape_ref_apply_region`; the draft registry-region helper
  no longer exposes a source ref, retained scene ref, or raw backend context
  through the shared private draw header.
* Source-adapter registry-entry creation and indexed fullpath setup now go
  through typed draw refs at source-local retained operation points
  (`_ged_draw_shape_ref_from_scene_ref`,
  `ged_draw_registry_shape_ref_set_indexed_fullpath`, and
  `ged_draw_registry_group_ref_set_indexed_fullpath`).  Registry entry
  classification is supplied by the call site instead of asking the source
  adapter through a neutral source-ref callback.
* Registry-owner lookup is now registry-local through the global source-ref
  entry table; the source-adapter `ged_draw_source_ref_registry_owner`
  callback and raw retained registry-owner helper are retired from the shared
  private draw header.
* Scene release highlight cleanup now reaches registry state through
  `ged_draw_registry_source_ref_highlight_free`; the raw retained
  highlight-free callback no longer crosses the shared private draw header.
* Scene-root and registry summary reads now use opaque scene-context summary
  facades; raw retained tree/shape/group summary helpers no longer cross the
  shared private draw header.
* Registry raw scene-ref index callbacks are now registry-local; shared callers
  use shape-ref/group-ref index facades instead of a private `bsg_scene_ref`
  callback surface.
* Registry path-state setup and source-adapter shape/group ref recovery now use
  tagged scene-ref facades; raw retained shape-state ensure, indexed-fullpath,
  and registry context recovery helpers are registry-local or retired instead
  of exposed through the shared private draw header.
* Generic retained scene-ref child traversal, child/parent lookup,
  name/fullpath readback, display/source/tree/group summary readback, subtree
  bounds, and active-scope component erase helpers are source-local; the
  shared private draw header now exposes context/public wrappers or
  source/registry-specific facades instead of those `ged_draw_scene_rt_ref_*`
  exports.
* Scene-context to tagged-scene-ref conversion (`ged_draw_scene_context_rt_ref`)
  is now source-local; draw-scene tests use context-level traversal and summary
  checks instead of minting neutral scene refs from opaque scene contexts.
* Shape/group draw-ref creation from source refs is now named as a registry
  service (`ged_draw_registry_shape_ref_from_source_ref` and
  `ged_draw_registry_group_ref_from_source_ref`); the old generic
  `ged_draw_*_ref_from_scene_rt_ref` names are retired from the shared private
  boundary.


Capability 1 Remaining Work: Scene And Source Ownership
-------------------------------------------------------

Goal:

Replace retained `bsg_scene_ref` ownership with an Obol/libbrlobol-owned
scene/source model that backs the existing neutral GED draw APIs.

Required behavior:

* Scene root creation, attachment, reattachment, lifecycle, active view scope,
  and independent view scope ownership.
  Libbrlobol now has the source instance identity API needed for independent
  view scopes to own duplicate database paths without path-key collapse.  GED
  transactions and qtcad private direct database-source sync now use those
  instance owners for scoped database-source replacement, removal, stale,
  visibility, redraw, erase-prefix, and clear work; remaining work in this
  item is the root/group/shape ownership still mediated by the retained
  adapter.
* Group creation, lookup, rename, erase, clear, nested subpath erase,
  child append/remove, traversal, and subtree bounds.
* Shape references with stable identity, parent/group ownership, full path,
  leaf directory, source state, source ref, shape state, and draw context.
* Database source nodes with source revision, input revision, realized
  revision, realization identity, stale flag, stale reason, material policy,
  and source-owned realization status.
* Draw intent ownership: semantic path, draw mode, fallback mode, overlay
  flag, appearance, revalidation, and copy.
* Display attributes: visible, highlighted, selected, transparency, color,
  material revision/color, line style, line width, draw matrix, transform,
  draw center, draw size, bounds, and draw tree depth.
* Structural revision and frame revision accounting.
* Neutral summary exports for GED records:
  `ged_draw_scene_display_summary`,
  `ged_draw_database_source_summary`,
  `ged_draw_scene_tree_summary`,
  `ged_draw_shape_record_summary`,
  `ged_draw_group_record_summary`,
  `ged_draw_scene_draw_state_summary`,
  `ged_draw_shape_geometry_summary`, and
  `ged_draw_shape_material_summary`.

Near-term work:

* Prefer straight owner moves by behavior family, backed by targeted parity
  tests, over adding new retained/Obol compatibility shims.
* Be willing to take larger churn windows when they remove an ownership layer
  outright.  Do not split a direct owner migration into extra bridge stages
  only to preserve temporary compatibility structure.
* Continue shrinking cross-file `bsg_scene_ref` bridge exposure where callers
  can use summaries, records, source snapshots, child iterators, or
  transaction/source-specific facade operations.
* Move source ownership, source-state mutation, database-source record
  synchronization, source realization transitions, and related root/group/
  shape work together by behavior family when that is the shortest path to
  direct Obol/libbrlobol ownership.
* Avoid moving raw retained parent/child traversal into higher-level wrappers;
  use source-adapter facades or neutral summaries instead.
* Keep draw transactions, redraw, material refresh, highlight, erase, bounds,
  scoped source-update, and database-update behavior passing while the backend
  owner is swapped.
* Remove retained BSG records instead of preserving dual authoritative state
  once the corresponding Obol/libbrlobol owner path has parity coverage.
* When a bridge is retired, update `misc/CMake/brlobol_pivot_guard.cmake` so
  the guard tracks the new intended boundary instead of stale transitional
  tokens.

Capability 1 acceptance criteria:

* GED root, group, shape, source, and draw-intent code can operate without
  calling BSG scene/source APIs outside the backend adapter boundary.
* Obol/libbrlobol is the authoritative owner for scene/source state used by
  GED records and transactions; retained BSG state, if any, is adapter-only
  and not required to describe or mutate semantic draw ownership.
* Retained BSG record/state structures are removed or reduced to disposable
  display compatibility glue with no independent ownership semantics.
* Public GED records keep their current fields and semantics.
* Existing draw transaction, redraw, material refresh, highlight, erase,
  bounds, source summary, and database-update tests pass.


Capability 2 Remaining Work: View Feature And Polygon Store
-----------------------------------------------------------

Goal:

Replace the BSG-backed implementation behind neutral `rt_view_*` and
`ged_draw_view_*` feature/polygon APIs.

Required feature behavior:

* Named feature lookup, creation, removal, visibility, color, line width, and
  style.
* Overlay feature ownership and command-result owner callbacks.
* Line, indexed line, point, label, arrow, axes, line-layer, and edit-preview
  feature publication.
* Feature summaries: point/command counts, geometry kind, visible state,
  realized state, local/shared scope, and owner metadata.
* Selection store: selected path add/set/clear/contains/count and pick-result
  application.
* Edit-preview callbacks and pick/update lifecycle.

Required polygon behavior:

* Polygon create, select, find, duplicate, update, update screen point, move,
  rename, remove, and user-data association.
* Neutral polygon record visit/readback.
* Current/open/closed contour state, selected point state, visual style, edge
  color, fill state, fill color, fill slope, CSG, sketch import/export,
  snap count, snap exclusion, and point-selection clear.

Acceptance criteria:

* `src/librt/view_legacy_bsg.c` feature/polygon implementation is replaced or
  reduced to a compatibility adapter.
* `src/libged/bsg_ged_draw_view.c` no longer owns the primary feature/polygon
  backend.
* qtcad and qged polygon/edit/selection paths continue using neutral RT/GED
  APIs without reintroducing qtcad public legacy-view helpers.


Capability 3 Remaining Work: Render And Export Record Query
-----------------------------------------------------------

Goal:

Replace BSG export and backend-scene record queries with Obol/libbrlobol
queries that emit neutral records GED already exposes.

Current state:

* Obol export action can emit staged object records with stable ordering,
  identity, filtering, geometry detail summaries, and source-backed
  full-detail request metadata.
* GED view-record wrappers call a neutral backend-adapter producer.
* The temporary BSG export producer still exists in the BSG-backed source
  adapter pending Obol query hookup.

Remaining work:

* Connect GED record APIs to the Obol query path.
* Add any missing annotation, segment, line, point, and surface iterator
  parity.
* Replace the temporary BSG-backed export producer in
  `src/libged/bsg_ged_draw_source.c`.
* Preserve render/export consistency summaries, LoD/full-detail request
  reporting, and deterministic guard-test ordering.

Acceptance criteria:

* `src/libged/bsg_ged_draw_records.c` no longer needs BSG export/render or
  backend-scene APIs.
* GED draw-scene, view-command, qged swrast, export, pick, snap, measure, and
  qtcad Obol workflow tests preserve current behavior.


Capability 4 Remaining Work: Obol Display Backend Draw Stream
-------------------------------------------------------------

Goal:

Implement the permanent Obol/libbrlobol display draw stream.  During migration,
this stream may feed temporary libdm compatibility paths so libdm can first
drop its direct BSG dependency, then leave the primary qged/qtcad display path.

Required behavior:

* Ordered draw items for scene, HUD, overlay, annotation, framebuffer, and
  geometry passes.
* Geometry item kinds for lines, indexed lines, points, indexed face sets,
  meshes, labels/text, annotations, images, overlays, HUD widgets, CSG/BREP
  proxies, and edit previews.
* Per-item visibility, role, source scope, material/color, alpha, line
  width/style, transform, coordinate space, draw center, bounds,
  selected/highlighted state, and owner/cache identity.
* Batch lifecycle: request creation, collection, dispatch, payload preparation,
  payload freeing, invalidation, begin/end request, sorted-alpha pass,
  visible-only pass, and HUD pass.
* HUD/faceplate support for axes, grid, ADC, center dot, scale overlay,
  params, labels, interactive rectangle, and other RT view records.
* Framebuffer/image items with viewport, cursor, layer, and image-source
  synchronization.

Acceptance criteria:

* `src/libdm/view.c` no longer includes BSG headers or calls BSG render APIs.
* `libdm` can drop its direct `libbsg` dependency row.
* The same draw stream is usable directly by Obol/qtcad without routing
  through libdm.
* DM backend draw-item tests and qged swrast smoke tests continue to pass.


Capability 5 Remaining Work: qtcad And WindowHost Display Ownership
-------------------------------------------------------------------

Goal:

Make qtcad/qged display ownership primarily Obol/libbrlobol-owned instead of
retained DM/BSG-owned.

Required behavior:

* `BRLObolViewController` owns visible scene root, camera/view state, viewport
  size, render requests, LoD service integration, and result consumption.
* `BRLObolWindowHost` owns framebuffer attachment, image source, viewport
  image nodes, flush/reset/viewport/cursor operations, and render requests.
* qtcad draw synchronization updates Obol scene/source state directly from
  neutral GED draw notifications and transactions.
* qtcad selection, overlay, database-source sync, edit-preview publication,
  snap, pick, measure, export, and real-model workflows use Obol/libbrlobol
  data as the primary display path.
* qged can launch and render its normal Obol/qtcad display path without a
  direct `dm_plugins` build dependency.

Acceptance criteria:

* qtcad visible Obol rendering reaches parity for guarded real-model and qged
  swrast workflows.
* qtcad no longer needs retained DM/framebuffer ownership for its normal Obol
  display path.
* `qged` direct `dm_plugins` dependency can be retired safely.


Dependency Retirement Gates
---------------------------

`libged` direct `libbsg` dependency can be removed after:

* GED retained scene/source/root/scope operations are backed by Obol or a
  neutral owner.
* GED feature/polygon and export/render record queries no longer require BSG.
* Public BSG-shaped GED snap/scene-ref shims are removed or reduced to
  non-linking compatibility wrappers.

`libdm` direct `libbsg` dependency can be removed after:

* DM draw dispatch, while it still exists, uses the neutral Obol/libbrlobol
  draw stream instead of BSG render-item APIs.
* HUD, overlay, annotation, framebuffer, and scene draw tests pass without BSG
  render-item APIs.

`libdm` can be removed from the primary drawing path after:

* qtcad/qged visible rendering uses Obol/libbrlobol directly.
* Remaining compatibility renderers are explicitly isolated as legacy
  consumers.
* No primary qged/qtcad runtime path depends on libdm backend plugins.

`qged` direct `dm_plugins` dependency can be removed after:

* qtcad/Obol owns normal display backend selection and runtime rendering.
* qged no longer depends on build-time DM plugin availability for its primary
  Obol display path.


Guard And Test Gates
--------------------

Fast guard set for current scene/source work:

* `ninja -C build libged ged_test_ged_draw_scene`
* `ctest --test-dir build -R '^ged_test_ged_draw_scene$' --timeout 180 --output-on-failure`
* `ctest --test-dir build -R '^(brlobol_pivot_guard|ged_test_ged_draw_scene|test_qtcad_obol_draw_sync|libbrlobol_lod_update_action|test_qtcad_obol_export|test_qtcad_obol_overlay_sync)$' --timeout 180 --output-on-failure`

Broader coverage should scale with the touched capability:

* Scene/source: shape/group/source records, draw transactions, erase/redraw,
  material refresh, stale state, source summaries, and database-update tests.
* Feature/polygon: qged polygon create/modify, selection, edit-preview,
  feature summaries, and snap exclusion.
* Render/export records: GED draw-scene, view-command, visible DB-object
  records, annotation/line/surface iterators, and qged swrast.
* Display stream: DM backend draw-item tests, faceplate/HUD tests, qged
  swrast, and selected real-model qtcad workflows.
* qtcad ownership: qtcad Obol draw, pick, snap, measure, export, real-model
  havoc/stress tests, and qged smoke tests.


Non-Goals
---------

* Do not build a one-for-one public BSG compatibility layer in Obol.
* Do not build permanent libdm-only features.
* Do not make GED public APIs depend on Coin/Inventor node classes.
* Do not retire dependency rows before the corresponding backend capability
  exists and is tested.
* Do not optimize broad performance behavior before the replacement path is
  structurally complete, except for clear regressions or tests exceeding the
  agreed 180 second limit.
